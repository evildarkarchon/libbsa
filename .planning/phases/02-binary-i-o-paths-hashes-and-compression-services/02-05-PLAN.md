---
phase: 02-binary-i-o-paths-hashes-and-compression-services
plan: 05
type: tdd
wave: 5
depends_on: [02-04]
files_modified:
  - CMakeLists.txt
  - tests/CMakeLists.txt
  - tests/unit/public_include_boundary_tests.cpp
  - src/detail/bethesda_hash.hpp
  - src/detail/bethesda_hash.cpp
  - tests/unit/bethesda_hash_tests.cpp
autonomous: true
requirements: [BIN-08]
must_haves:
  truths:
    - "TES3, TES4-family, and FO4/BA2 hash functions return constants traced from TES5Edit behavior."
    - "Non-obvious hash compatibility rules are documented near the implementation/tests."
    - "Public headers still do not expose private dependency or TES5Edit types after Phase 2 primitives are added."
    - "D-01: Hash and boundary work keeps primitives internal."
    - "D-04: Final boundary checks keep dependency services private."
    - "D-17: Hash algorithms are proven with reference-traced constants."
    - "D-19: TES5Edit stays read-only and compatibility rules cite relevant reference files/functions."
    - "D-20: Hash proof uses unit vectors before generated binary fixtures."
  artifacts:
    - path: "src/detail/bethesda_hash.hpp"
      provides: "Internal Bethesda archive hash functions"
      exports: ["hash_tes3", "hash_tes4", "hash_fo4"]
    - path: "tests/unit/bethesda_hash_tests.cpp"
      provides: "Reference constant tests for BIN-08"
    - path: "tests/unit/public_include_boundary_tests.cpp"
      provides: "Final public dependency boundary regression coverage"
  key_links:
    - from: "tests/unit/bethesda_hash_tests.cpp"
      to: "TES5Edit/Core/wbBSArchive.pas"
      via: "constants copied from Phase 2 research trace"
      pattern: "0x0E5C1667258ACAD8|0x8A690D8F6D0EE172|0x89046777"
---

<objective>
Add internal Bethesda hash functions and final Phase 2 boundary verification.

Purpose: Format phases need compatible path hashes while public headers remain clean and TES5Edit stays read-only.
Output: TES3, TES4-family, and FO4/BA2 hash implementation with reference constants and boundary tests.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-binary-i-o-paths-hashes-and-compression-services/02-SPEC.md
@.planning/phases/02-binary-i-o-paths-hashes-and-compression-services/02-CONTEXT.md
@.planning/phases/02-binary-i-o-paths-hashes-and-compression-services/02-RESEARCH.md
@src/detail/archive_path.hpp
@include/libbsa/libbsa.hpp
@tests/unit/public_include_boundary_tests.cpp

<interfaces>
Create `src/detail/bethesda_hash.hpp`:
```cpp
namespace libbsa::detail {
std::uint64_t hash_tes3(std::string_view archive_path);
std::uint64_t hash_tes4(std::string_view name_or_path);
std::uint64_t hash_tes4(std::string_view name_without_extension, std::string_view extension_with_dot);
std::uint32_t hash_fo4(std::string_view archive_path);
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED specify TES3, TES4, and FO4/BA2 hash constants</name>
  <files>tests/unit/bethesda_hash_tests.cpp, tests/CMakeLists.txt</files>
  <read_first>tests/CMakeLists.txt; .planning/phases/02-binary-i-o-paths-hashes-and-compression-services/02-RESEARCH.md; .planning/phases/02-binary-i-o-paths-hashes-and-compression-services/02-CONTEXT.md</read_first>
  <behavior>
    - `hash_tes3("meshes/foo/bar.nif") == 0x0E5C1667258ACAD8`.
    - `hash_tes4("meshes/foo/bar.nif") == 0x8A690D8F6D0EE172`.
    - `hash_fo4("meshes/foo/bar.nif") == 0x89046777` and equals `hash_fo4("meshes\\foo\\bar.nif")`.
    - Additional constants from `02-RESEARCH.md` are asserted for `textures/actors/armor.dds`, `sound/fx/test.wav`, and `bookart/sample.txt`.
    - Split TES4 overload constants are asserted: `hash_tes4("meshes/foo", "") == 0xA0E6BEDF6D0A6F6F` and `hash_tes4("bar", ".nif") == 0x92CD45FD6203E172`.
  </behavior>
  <action>Create `tests/unit/bethesda_hash_tests.cpp` with tags `[unit][hash][compat]`, register it, and make it fail before production code exists. Include a short comment citing `TES5Edit/Core/wbBSArchive.pas` lines 646-800 as the read-only source for the constants per D-17 through D-19.</action>
  <verify><automated>cmake --build --preset windows-msvc-debug-static --config Debug; ctest --preset windows-msvc-debug-static -R bethesda_hash --output-on-failure</automated></verify>
  <acceptance_criteria>
    - `tests/unit/bethesda_hash_tests.cpp` contains `0x0E5C1667258ACAD8`, `0x8A690D8F6D0EE172`, `0x89046777`, and `TES5Edit/Core/wbBSArchive.pas`.
    - `tests/CMakeLists.txt` contains `unit/bethesda_hash_tests.cpp`.
    - The test file does not read from or write to `TES5Edit/` at runtime.
  </acceptance_criteria>
  <done>Failing hash tests lock traced TES3, TES4-family, and FO4/BA2 expected values.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN implement internal Bethesda hash service</name>
  <files>CMakeLists.txt, src/detail/bethesda_hash.hpp, src/detail/bethesda_hash.cpp, tests/unit/bethesda_hash_tests.cpp</files>
  <read_first>CMakeLists.txt; src/detail/archive_path.hpp; tests/unit/bethesda_hash_tests.cpp; .planning/phases/02-binary-i-o-paths-hashes-and-compression-services/02-RESEARCH.md</read_first>
  <action>Implement `hash_tes3`, both `hash_tes4` overloads, and `hash_fo4` under `libbsa::detail`. Port the traced behavior from `02-RESEARCH.md`: ASCII-only `LowerByte`; TES3 half-string XOR/rotate combination; TES4 extension special bits for `.kf`, `.nif`, `.dds`, `.wav`; FO4 CRC32 with initial value `0`, ASCII lowercase folding, skipping bytes greater than 127, and `/` treated as `\\`. Add comments near each algorithm explaining the compatibility rule and reference file/function. Add Doxygen comments in the header. Add `src/detail/bethesda_hash.cpp` to `CMakeLists.txt`. Do not modify anything under `TES5Edit/`.</action>
  <verify><automated>cmake --build --preset windows-msvc-debug-static --config Debug; ctest --preset windows-msvc-debug-static -R bethesda_hash --output-on-failure</automated></verify>
  <acceptance_criteria>
    - `src/detail/bethesda_hash.cpp` contains `CreateHashTES3`, `CreateHashTES4`, and `CreateHashFO4` in comments or references.
    - `src/detail/bethesda_hash.cpp` contains `.nif`, `.dds`, `.wav`, and `.kf`.
    - `src/detail/bethesda_hash.hpp` contains `hash_tes3`, `hash_tes4`, and `hash_fo4`.
    - The bethesda hash test filter exits 0.
  </acceptance_criteria>
  <done>Internal hash functions pass all reference constant tests and document compatibility constraints.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Verify public boundary and TES5Edit read-only guard</name>
  <files>tests/unit/public_include_boundary_tests.cpp</files>
  <read_first>tests/unit/public_include_boundary_tests.cpp; include/libbsa/libbsa.hpp; include/libbsa/archive.hpp; include/libbsa/result.hpp; include/libbsa/version.hpp; AGENTS.md</read_first>
  <behavior>
    - Public include boundary tests still compile with `<libbsa/libbsa.hpp>` only.
    - Public headers do not contain `libdeflate`, `lz4`, `DirectXTex`, `Windows.h`, `TES5Edit`, `std::expected`, `bethesda_hash`, `compression_router`, or `archive_path_key`.
    - `git -C TES5Edit status --short` prints no output.
  </behavior>
  <action>Extend `tests/unit/public_include_boundary_tests.cpp` only if needed to assert Phase 2 private names are absent from the public umbrella include. Run grep gates from PowerShell without self-invalidating comments: use file content filters that exclude comment-only lines before counting forbidden tokens. Do not remove existing comments. This finalizes D-01 through D-04 and D-19.</action>
  <verify><automated>cmake --build --preset windows-msvc-debug-static --config Debug; ctest --preset windows-msvc-debug-static -R "(public_include_boundary|bethesda_hash)" --output-on-failure; git -C TES5Edit status --short</automated></verify>
  <acceptance_criteria>
    - `tests/unit/public_include_boundary_tests.cpp` contains `public boundary`.
    - `git -C TES5Edit status --short` produces no output.
    - `Select-String -Path include/libbsa/*.hpp -Pattern 'libdeflate|lz4|DirectXTex|Windows.h|TES5Edit|std::expected|bethesda_hash|compression_router|archive_path_key'` returns no matches outside comments; if a PowerShell grep gate is added to tests or docs, it excludes comment-only lines before counting.
    - The public include boundary and hash test filters exit 0.
  </acceptance_criteria>
  <done>Hash behavior is implemented and Phase 2 private dependencies/primitives remain outside public headers with TES5Edit unmodified.</done>
</task>

</tasks>

<source_decision_coverage>
- D-01: Hashes and final boundary checks keep Phase 2 primitives internal and out of public headers.
- D-04: Final boundary verification checks private dependency adapters do not expose dependency types or public codec utilities.
- D-17: Hash tests assert TES3, TES4-family, and FO4/BA2 constants traced from TES5Edit behavior.
- D-19: Hash implementation/tests document non-obvious compatibility rules with `TES5Edit/Core/wbBSArchive.pas` references while leaving the submodule read-only.
- D-20: Hash proof uses unit vectors and traced constants rather than committed generated binary fixtures.
</source_decision_coverage>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| archive path→hash service | Untrusted archive names become integer lookup keys. |
| reference submodule→implementation | Read-only compatibility evidence informs new code outside the submodule. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05-01 | T | `bethesda_hash` | mitigate | Assert traced constants for multiple extensions and separator/case behaviors. |
| T-02-05-02 | R | `TES5Edit/` boundary | mitigate | Verify `git -C TES5Edit status --short` is empty and do not use the submodule as fixture data. |
| T-02-05-03 | I | public headers | mitigate | Extend boundary checks so private hash/codec/path names and dependency names do not leak through installed headers. |
</threat_model>

<verification>
Run `cmake --build --preset windows-msvc-debug-static --config Debug`, `ctest --preset windows-msvc-debug-static -L unit --output-on-failure`, and `git -C TES5Edit status --short`.
</verification>

<success_criteria>
BIN-08 is covered by reference constant tests; all Phase 2 unit tests pass; public headers remain dependency-light; `TES5Edit/` has no status output.
</success_criteria>

<output>
After completion, create `.planning/phases/02-binary-i-o-paths-hashes-and-compression-services/02-05-SUMMARY.md`.
</output>
