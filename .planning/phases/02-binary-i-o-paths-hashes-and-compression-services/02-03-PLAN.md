---
phase: 02-binary-i-o-paths-hashes-and-compression-services
plan: 03
type: tdd
wave: 3
depends_on: [02-02]
files_modified:
  - CMakeLists.txt
  - tests/CMakeLists.txt
  - src/detail/deflate_codec.hpp
  - src/detail/deflate_codec.cpp
  - tests/unit/deflate_codec_tests.cpp
autonomous: true
requirements: [BIN-04, BIN-07]
must_haves:
  truths:
    - "Deflate payloads compress and decompress through libdeflate private adapters."
    - "Deflate decompression fails unless the produced byte count exactly equals the expected size."
    - "D-04: Deflate dependency service is a private adapter only."
    - "D-18: Deflate tests cover round-trip, corrupt/truncated input, and exact-size mismatch rejection."
    - "D-20: Deflate proof uses unit vectors before generated binary fixtures."
  artifacts:
    - path: "src/detail/deflate_codec.hpp"
      provides: "Internal raw deflate compression/decompression API"
      exports: ["libbsa::detail::compress_deflate", "libbsa::detail::decompress_deflate_exact"]
    - path: "tests/unit/deflate_codec_tests.cpp"
      provides: "Round-trip, corrupt input, and exact-size mismatch tests"
  key_links:
    - from: "src/detail/deflate_codec.cpp"
      to: "libdeflate"
      via: "private implementation include/link"
      pattern: "libdeflate_deflate_(compress|decompress)"
---

<objective>
Add the private raw deflate adapter for exact-size compression and decompression.

Purpose: TES4-family BSA and BA2 parser/writer phases need a single libdeflate boundary that rejects silent corruption.
Output: Internal deflate codec with unit and malformed tests.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-binary-i-o-paths-hashes-and-compression-services/02-SPEC.md
@.planning/phases/02-binary-i-o-paths-hashes-and-compression-services/02-CONTEXT.md
@.planning/phases/02-binary-i-o-paths-hashes-and-compression-services/02-RESEARCH.md
@include/libbsa/result.hpp
@CMakeLists.txt
@vcpkg.json

<interfaces>
Create `src/detail/deflate_codec.hpp`:
```cpp
namespace libbsa::detail {
result<std::vector<std::byte>> compress_deflate(std::span<const std::byte> input, int compression_level = 6);
result<std::vector<std::byte>> decompress_deflate_exact(std::span<const std::byte> compressed, std::size_t expected_size);
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED specify exact-size deflate codec behavior</name>
  <files>tests/unit/deflate_codec_tests.cpp, tests/CMakeLists.txt</files>
  <read_first>tests/CMakeLists.txt; .planning/phases/02-binary-i-o-paths-hashes-and-compression-services/02-SPEC.md; .planning/phases/02-binary-i-o-paths-hashes-and-compression-services/02-RESEARCH.md</read_first>
  <behavior>
    - `compress_deflate` followed by `decompress_deflate_exact(..., original.size())` returns the original bytes for a literal vector containing `"libbsa deflate vector"` repeated four times.
    - Decompressing a truncated compressed buffer returns `error_code::format_error`.
    - Decompressing valid compressed bytes with `expected_size = original.size() + 1` returns `error_code::format_error`.
    - Decompressing valid compressed bytes with `expected_size = original.size() - 1` returns `error_code::format_error`.
  </behavior>
  <action>Create `tests/unit/deflate_codec_tests.cpp` with tags `[unit][malformed][compression][deflate]`, register it in `tests/CMakeLists.txt`, and make it fail before the adapter exists. This implements BIN-04 and the deflate portion of BIN-07 while preserving D-04 dependency privacy.</action>
  <verify><automated>cmake --build --preset windows-msvc-debug-static --config Debug; ctest --preset windows-msvc-debug-static -R deflate_codec --output-on-failure</automated></verify>
  <acceptance_criteria>
    - `tests/unit/deflate_codec_tests.cpp` contains `compress_deflate`, `decompress_deflate_exact`, `original.size() + 1`, and `original.size() - 1`.
    - `tests/CMakeLists.txt` contains `unit/deflate_codec_tests.cpp`.
    - No file under `include/libbsa/` contains `libdeflate`.
  </acceptance_criteria>
  <done>Failing deflate tests lock round-trip, corrupt input, and exact-size mismatch behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN implement private libdeflate adapter</name>
  <files>CMakeLists.txt, src/detail/deflate_codec.hpp, src/detail/deflate_codec.cpp, tests/unit/deflate_codec_tests.cpp</files>
  <read_first>CMakeLists.txt; vcpkg.json; tests/unit/deflate_codec_tests.cpp; include/libbsa/result.hpp</read_first>
  <action>Add `find_package(libdeflate CONFIG REQUIRED)` to `CMakeLists.txt`, link `libdeflate::libdeflate_static` privately to `libbsa`, and add `src/detail/deflate_codec.cpp` to private sources. Implement `compress_deflate` using `libdeflate_alloc_compressor`, `libdeflate_deflate_compress_bound`, and `libdeflate_deflate_compress`. Implement `decompress_deflate_exact` by allocating exactly `expected_size` bytes, calling `libdeflate_deflate_decompress`, translating non-success return codes to `error_code::format_error`, and requiring `actual_out == expected_size`. Use RAII wrappers or local cleanup comments so allocators are always freed. Add Doxygen comments for both functions. Do not enable zlib/gzip paths because Phase 2 requires raw deflate.</action>
  <verify><automated>cmake --build --preset windows-msvc-debug-static --config Debug; ctest --preset windows-msvc-debug-static -R deflate_codec --output-on-failure</automated></verify>
  <acceptance_criteria>
    - `src/detail/deflate_codec.cpp` contains `libdeflate_deflate_compress` and `libdeflate_deflate_decompress`.
    - `src/detail/deflate_codec.cpp` does not contain `zlib` or `gzip`.
    - `CMakeLists.txt` contains `find_package(libdeflate CONFIG REQUIRED)` and `PRIVATE libdeflate::libdeflate_static`.
    - The deflate codec test filter exits 0.
  </acceptance_criteria>
  <done>Deflate compression/decompression passes exact-size tests through a private dependency adapter.</done>
</task>

</tasks>

<source_decision_coverage>
- D-04: libdeflate is linked privately and exercised only through internal adapter functions.
- D-18: Deflate tests use small round-trip vectors plus corrupt/truncated and exact-size mismatch rejection.
- D-20: Deflate proof uses source literal unit vectors instead of committed generated binary fixtures.
</source_decision_coverage>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| compressed bytes→libdeflate | Malformed archive compression data enters native codec code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | T/D | `decompress_deflate_exact` | mitigate | Allocate only `expected_size`, translate codec errors, and fail unless output count equals metadata size. |
| T-02-03-02 | I | `deflate_codec.hpp` | mitigate | Keep libdeflate includes and types out of public headers and expose only libbsa-owned result/vector types. |
</threat_model>

<verification>
Run `cmake --build --preset windows-msvc-debug-static --config Debug` and `ctest --preset windows-msvc-debug-static -R deflate_codec --output-on-failure`.
</verification>

<success_criteria>
BIN-04 and the deflate compression portion of BIN-07 are covered; public headers still do not expose libdeflate; exact-size mismatch tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/02-binary-i-o-paths-hashes-and-compression-services/02-03-SUMMARY.md`.
</output>
