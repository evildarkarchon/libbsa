# Phase 17.1 Verification

**Verified:** 2026-05-15
**Status:** passed

## Scope

Phase 17.1 closed the v1.1 milestone-audit tech-debt follow-up without editing `.planning/v1.1-MILESTONE-AUDIT.md`, without modifying `TES5Edit/`, without public API expansion, and without adding dependencies.

## Automated Verification Commands

| Lane | Command | Status | Evidence |
|------|---------|--------|----------|
| Debug focused host-path/reader/writer policy | `ctest --preset windows-msvc-debug-static --output-on-failure -R "host_path_correctness_boundary|archive_reader_dispatch|host_file|host_file_path"` | passed | 30/30 tests passed; includes combined non-ASCII reader-surface coverage, reader dispatch policy, host-file path policy, and writer host-path inventory policy. |
| MSVC AddressSanitizer focused host-path/reader/writer policy | `cmake --build --preset windows-msvc-asan-static --target libbsa_tests && ctest --preset windows-msvc-asan-static --output-on-failure -R "host_path_correctness_boundary|archive_reader_dispatch|host_file|host_file_path"` | passed | 30/30 tests passed after rebuilding the ASan test executable so discovery used the current Phase 17.1 test sources. |
| Task 03 writer source-policy focus | `ctest --preset windows-msvc-debug-static --output-on-failure -R host_file` | passed | 22/22 tests passed, including the new writer inventory matrix policy tests. |

## Requirement Evidence

| Requirement | Status | Evidence |
|-------------|--------|----------|
| SPEC-1 | satisfied | `tests/unit/host_path_correctness_boundary_tests.cpp` contains the combined non-ASCII copied-archive reader matrix. Debug/ASan focused lanes include `host_path_correctness_boundary non-ASCII host path exercises reader dispatch surface`. |
| SPEC-2 | satisfied | `src/detail/host_file_path.hpp` now stores only the resolved host path; `tests/unit/host_file_path_tests.cpp`, `tests/unit/host_file_tests.cpp`, and `tests/unit/host_file_writer_name_tests.cpp` guard that removed diagnostics-only state does not return. |
| SPEC-3 | satisfied | `src/archive.cpp` removed stored `archive_reader::state::backend_identity` while keeping the file-local open-time backend selector; `tests/unit/archive_reader_dispatch_policy_tests.cpp` guards against stored identity and repeated public-reader dispatch. |
| SPEC-4 | satisfied | `tests/unit/host_file_writer_name_tests.cpp` now contains `writer_host_path_inventory_cases` covering TES3 BSA, TES4-family BSA, BA2 GNRL, and BA2 DX10 source/output/finalization/dedupe dispositions, plus this verification table cites source, test, and publish-policy evidence. |

## Locked Decision Evidence

| Decision | Status | Evidence |
|----------|--------|----------|
| D-01 | satisfied | `original_utf8` was removed from `detail::host_file_path` in Plan 17.1-02 rather than consumed by a new diagnostics path. |
| D-02 | satisfied | `backend_identity` was removed from `archive_reader::state`; only file-local open-time table-selection plumbing remains. |
| D-03 | satisfied | Stale diagnostics/state tests and comments were updated in Plan 17.1-02, and Plan 17.1-03 added live contract policy coverage. |
| D-04 | satisfied | No new diagnostics surface was added; future diagnostics remain future scope. |
| D-05 | satisfied | Plan 17.1-01 added a reader-matrix combined regression. |
| D-06 | satisfied | The combined regression covers `entries`, `find`, `contains`, and `extract_entries`. |
| D-07 | satisfied | The combined regression lives in `tests/unit/host_path_correctness_boundary_tests.cpp`. |
| D-08 | satisfied | The combined regression remains success-path focused and does not duplicate missing/invalid dispatch negative cases. |
| D-09 | satisfied | Writer host-path closure started from the explicit inventory in `17.1-RESEARCH.md` and was made executable through source-policy tests. |
| D-10 | satisfied | The inventory explicitly covers TES3 BSA, TES4-family BSA, BA2 GNRL, and BA2 DX10 source/output/finalization/dedupe categories. |
| D-11 | satisfied | This file cites source and policy evidence for each writer path category below. |
| D-12 | satisfied | No narrow raw host-path reopen was found in the supported writer source/output/finalization/dedupe paths; no migration was required beyond policy hardening. |
| D-13 | satisfied | Runtime proof comes from the combined non-ASCII reader regression; source-policy proof comes from host-file, reader-dispatch, and writer inventory policy tests. |
| D-14 | satisfied | Focused Debug and MSVC AddressSanitizer lanes passed; no Release package proof was required because scope did not change. |
| D-15 | satisfied | Source-policy tests guard negative invariants and bad patterns such as removed state and raw output path reopening without freezing every helper name. |
| D-16 | satisfied | `.planning/v1.1-MILESTONE-AUDIT.md` was not edited; audit artifact updates remain delegated to the later dedicated slash-command. |

## Writer Host-Path Disposition Table

| Writer family | Source path evidence | Output path evidence | Finalization evidence | Dedupe evidence | Disposition |
|---------------|----------------------|----------------------|-----------------------|-----------------|-------------|
| TES3 BSA | `src/formats/bsa/tes3_bsa_prepare.cpp` resolves `entry.host_path` with `resolve_tes3_source_path` and stores `resolved_host_path`; policy row `tes3_bsa` checks this. | `src/formats/bsa/tes3_bsa_writer.cpp` calls `resolve_host_file_path(output_host_path)`, passes `output_path.value().resolved`, and uses `detail::publish_writer_output`. | `src/formats/bsa/tes3_bsa_serialize.cpp` streams disk payloads through `detail::open_host_file(host_path, ...)`. | Explicit N/A: TES3 has no dedupe option/path; policy checks `deduplicate_payloads` is absent from the TES3 writer file. | Closed with policy/source evidence; no raw host-path reopen found. |
| TES4-family BSA | `src/formats/bsa/tes4_bsa_prepare.cpp` resolves source paths through `resolve_tes4_source_path(entry.host_path)` and uses host-file helpers for reads/probes. | `src/formats/bsa/tes4_bsa_writer.cpp` calls `resolve_host_file_path(output_host_path)`, passes `.resolved`, and publishes through `detail::publish_writer_output`. | `src/formats/bsa/tes4_bsa_serialize.cpp` streams raw disk payloads through `detail::open_host_file(host_path, ...)`. | `src/formats/bsa/tes4_bsa_layout.cpp` resolves `entry.raw_disk_host_path` via `resolve_tes4_dedupe_source_path` before exact equality/chunk reads. | Closed with policy/source evidence; no raw host-path reopen found. |
| BA2 GNRL | `src/formats/ba2/ba2_gnrl_prepare.cpp` resolves `entry.host_path` with `resolve_ba2_gnrl_source_path`, stores `resolved_source_path`, and uses host-file helpers. | `src/formats/ba2/ba2_gnrl_writer.cpp` calls `resolve_host_file_path(output_host_path)`, passes `.resolved`, and publishes through `detail::publish_writer_output`. | `src/formats/ba2/ba2_gnrl_serialize.cpp` streams disk payloads through `detail::open_host_file(host_path, ...)`. | `src/formats/ba2/ba2_gnrl_layout.cpp` compares disk-backed dedupe candidates through `resolved_source_path` and `detail::open_host_file` / `detail::for_each_host_file_chunk`. | Closed with policy/source evidence; no raw host-path reopen found. |
| BA2 DX10 | `src/formats/ba2/ba2_dx10_snapshot_builder.cpp` resolves DDS host paths once with `resolve_host_file_path(dds_host_path)` and reads through `detail::read_host_file_exact`. | `src/formats/ba2/ba2_dx10_writer.cpp` calls `resolve_host_file_path(output_host_path)`, passes `.resolved`, and publishes through `detail::publish_writer_output`. | `src/formats/ba2/ba2_dx10_serialize.cpp` writes prepared chunk payload bytes (`chunk.stored_payload`) and does not reopen caller DDS host paths. | Explicit no caller host-path dedupe reopen: dedupe/layout works over prepared chunks/stored payloads, and policy checks `dds_host_path` is absent from `ba2_dx10_serialize.cpp`. | Closed with policy/source evidence; abnormal-termination cleanup remains accepted residual risk and out of scope. |

## Source-Policy Evidence

- `tests/unit/host_file_writer_name_tests.cpp` now includes `writer_host_path_inventory_cases` with literal family rows `tes3_bsa`, `tes4_bsa`, `ba2_gnrl`, and `ba2_dx10`.
- The same test file references `src/detail/writer_publish.cpp` and `src/detail/writer_publish.hpp`, requiring the publish seam and reparse-point overwrite guard.
- Writer output policy forbids `std::filesystem::path{output_host_path}` in TES3, TES4, BA2 GNRL, and BA2 DX10 writer output files.
- Removed-state policy checks live contract files for absence of the removed `original_utf8` token.

## Residual Risks and Out-of-Scope Items

- BA2 DX10 abnormal-termination cleanup remains an accepted residual risk and is out of scope for Phase 17.1. Ordinary success/failure cleanup behavior from Phase 17 remains unchanged.
- Release package proof was not run for Phase 17.1 because D-14 scoped closure to focused Debug and MSVC AddressSanitizer lanes and no packaging/public-surface change occurred.
- `.planning/v1.1-MILESTONE-AUDIT.md` remains unmodified by this phase.

## Forbidden-Scope Checks

| Check | Status |
|-------|--------|
| No files under `TES5Edit/` modified | passed |
| No public headers under `include/libbsa/` modified or expanded | passed |
| No new external dependencies introduced | passed |
| `.planning/v1.1-MILESTONE-AUDIT.md` unmodified | passed |

## Final Disposition

All Phase 17.1 audit tech-debt items are closed or explicitly accepted as residual risk. SPEC-1 through SPEC-4 and D-01 through D-16 have committed source, test, policy, and/or verification evidence.
