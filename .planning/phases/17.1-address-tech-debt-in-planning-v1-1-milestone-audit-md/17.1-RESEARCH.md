# Phase 17.1: Address Tech Debt in v1.1 Milestone Audit - Research

**Researched:** 2026-05-14
**Domain:** C++20 internal cleanup, Windows host-file boundary, reader dispatch regression, writer I/O policy evidence
**Confidence:** HIGH

<user_constraints>
## User Constraints (from CONTEXT.md)

### Locked Decisions

#### Unused state disposition
- **D-01:** Remove `detail::host_file_path::original_utf8` from the internal host-file path contract. Do not add a new private diagnostic consumer in Phase 17.1.
- **D-02:** Remove `archive_reader::state::backend_identity` from reader state. It may remain as file-local open-time table-selection plumbing only if needed, but it must not be stored beside the active backend table.
- **D-03:** Apply strict cleanup with the removals: remove or rewrite comments and tests that claim diagnostics-only preservation, and update policy evidence so the removed unused members do not quietly return.
- **D-04:** The removal decision is locked. If future diagnostics need original UTF-8 host-path text or reader backend identity, that belongs to a later requirement rather than this cleanup phase.

#### Combined non-ASCII reader regression
- **D-05:** Add a reader-matrix combined regression rather than a single-fixture smoke test. The matrix should reuse the current representative reader families from Phase 15 where practical, but it should not recreate every family-specific edge case.
- **D-06:** The combined regression should cover the SPEC surface only: `entries`, `find`, `contains`, and `extract_entries` through the public API from archives copied under non-ASCII Windows host paths.
- **D-07:** Place the combined regression in `tests/unit/host_path_correctness_boundary_tests.cpp` so it reuses the non-ASCII path setup, UTF-8 conversion helper, fixture-copy pattern, and manifest expectations already established there.
- **D-08:** Keep the new regression success-path focused: manifest entry count/path expectations, canonical `find` and `contains`, and `extract_entries` payload bytes. Do not duplicate the negative missing-path or invalid-path dispatch matrix unless implementation uncovers a real gap.

#### Writer host-path disposition
- **D-09:** Close the writer host-path audit warning through an explicit evidence inventory first. If the inventory shows supported writer I/O already routes through the shared host-file boundary, no broad writer redesign is needed.
- **D-10:** The inventory must explicitly cover the SPEC categories: TES3 BSA, TES4-family BSA, BA2 GNRL, and BA2 DX10 writer source, output, finalization, and dedupe host-file I/O paths.
- **D-11:** Final verification must cite the source, test, and/or policy evidence that closes each writer path category. Planning-only notes are not enough for closure.
- **D-12:** If the inventory finds a narrow raw host-path reopen in a currently supported writer source/output/finalization/dedupe path, migrate that specific reopen behind the shared host-file boundary in Phase 17.1.

#### Proof and verification style
- **D-13:** Use both runtime and source-policy proof. Runtime tests should prove public reader behavior under non-ASCII paths; source-policy tests should prevent dead-state and raw-host-path boundary drift.
- **D-14:** Closure evidence should target focused Debug and MSVC AddressSanitizer lanes when touching host-path, reader dispatch, or writer I/O seams. Release package proof is not required unless packaging or public surface changes unexpectedly.
- **D-15:** Source-policy tests should guard negative invariants, not freeze exact helper names. Forbid specific bad patterns such as the removed state members or raw string reopens while allowing equivalent shared-host-file seam implementations.
- **D-16:** Do not update `.planning/v1.1-MILESTONE-AUDIT.md` directly as part of this phase. Audit artifact updates are handled by a dedicated slash-command after closure evidence exists.

### the agent's Discretion
None. The user selected concrete decisions for state removal, regression breadth, test location, writer audit closure, proof mix, verification lanes, policy strictness, and audit-file update handling. Downstream agents should not reopen these choices.

### Deferred Ideas (OUT OF SCOPE)
- Updating `.planning/v1.1-MILESTONE-AUDIT.md` after closure evidence exists is handled by a dedicated slash-command, not by Phase 17.1 implementation.
</user_constraints>

## Project Constraints (from AGENTS.md)

- Implementation work must remain in C++ and outside `TES5Edit/`; `TES5Edit/` is a read-only reference submodule that must not be edited, formatted, staged, compiled, or vendored. [VERIFIED: `AGENTS.md`:20-41]
- libbsa is Windows-only; do not add Linux/macOS/POSIX portability work for this phase. [VERIFIED: `AGENTS.md`:16-18]
- Do not introduce speculative dependencies; approved dependency management is vcpkg, with libdeflate, official lz4, and DirectXTex already required. [VERIFIED: `AGENTS.md`:43-53] [VERIFIED: `vcpkg.json`:1-17]
- Preserve accurate comments, remove/rewrite comments only when the described code is removed or becomes misleading, and mention comment removals/rewrites in implementation summaries. [VERIFIED: `AGENTS.md`:55-61]
- Add focused tests for archive parsing, writing, round-tripping, and compatibility behavior; do not keep production code exclusively for test compatibility. [VERIFIED: `AGENTS.md`:63-68]

## Summary

Phase 17.1 should be planned as a small, evidence-driven cleanup phase, not a feature phase. The locked work is one combined non-ASCII host-path plus reader-surface runtime regression, removal of two private unused-state members, and a bounded writer host-path disposition with source-policy evidence. [VERIFIED: `17.1-SPEC.md`:17-38] [VERIFIED: `17.1-CONTEXT.md`:40-65]

The safest implementation path is to extend existing tests and policies rather than creating new suites. `tests/unit/host_path_correctness_boundary_tests.cpp` already owns non-ASCII temp roots, archive-copy helpers, UTF-8 path conversion, manifest parsing, canonical payload expectations, and the public-API-only policy check; `tests/unit/archive_reader_dispatch_tests.cpp` already owns the representative reader fixture matrix and `extract_entries` sink-factory pattern that should be adapted. [VERIFIED: `host_path_correctness_boundary_tests.cpp`:21-214] [VERIFIED: `archive_reader_dispatch_tests.cpp`:49-187]

Writer host-path inventory found no broad redesign need in the currently supported writer source/output/finalization/dedupe flows: supported writer output paths resolve UTF-8 text once then publish using `std::filesystem::path`, disk-source preparation and serialization use `detail::host_file_path` plus `detail::open_host_file`/read helpers, and BA2 DX10 source reads snapshot from a resolved path before later writes consume snapshot bytes. [VERIFIED: `tes3_bsa_writer.cpp`:82-115] [VERIFIED: `tes4_bsa_layout.cpp`:64-196] [VERIFIED: `ba2_gnrl_layout.cpp`:96-107,179-252] [VERIFIED: `ba2_dx10_snapshot_builder.cpp`:62-68,156-197]

**Primary recommendation:** Plan three tightly scoped waves: (1) combined host-path/reader regression, (2) remove dead state plus stale diagnostics comments/tests, (3) writer host-path inventory/policy hardening and Debug/ASan validation evidence. [VERIFIED: `17.1-CONTEXT.md`:49-65]

## Architectural Responsibility Map

| Capability | Primary Tier | Secondary Tier | Rationale |
|------------|--------------|----------------|-----------|
| Non-ASCII host-path conversion | Shared detail services | Public facade/orchestration | Public APIs accept UTF-8 strings, but Windows-native path resolution belongs in `detail::resolve_host_file_path` and filesystem helper seams. [VERIFIED: `host_file_path.cpp`:16-42] [VERIFIED: `archive.cpp`:160-171] |
| Reader backend dispatch | Public facade/orchestration | Format pipeline layer | `archive_reader::open` selects format callbacks once; public reader operations use `state_->backend_table` instead of repeated family dispatch. [VERIFIED: `archive.cpp`:176-186,266-302] |
| Combined reader regression | Test layer | Public API layer | The test should call only `archive_reader::open`, `entries`, `find`, `contains`, and `extract_entries` against copied fixtures under non-ASCII host paths. [VERIFIED: `17.1-SPEC.md`:19-23] |
| Writer source reads | Format pipeline layer | Shared detail services | TES3/TES4/BA2 writer preparation, layout, and serialization own format-specific staging while host-file reads route through `detail::host_file_path` and `detail::host_file` helpers. [VERIFIED: `tes3_bsa_prepare.cpp`:38-49,111-121] [VERIFIED: `tes4_bsa_serialize.cpp`:79-115] |
| Writer output/finalization | Facade/orchestration | Shared detail services | Top-level writer functions resolve output UTF-8 text and call `detail::publish_writer_output`; publish safety is centralized in `writer_publish.cpp`. [VERIFIED: `tes4_bsa_writer.cpp`:92-149] [VERIFIED: `writer_publish.cpp`:60-148] |
| BA2 DX10 snapshot lifecycle | Format pipeline layer | Shared detail services | BA2 DX10 `add_file` snapshots source DDS bytes into writer-owned temp files, and `write_to` consumes the writer and best-effort cleans snapshot directories after ordinary attempts. [VERIFIED: `ba2_dx10_writer.cpp`:21-45,62-109] |

## Standard Stack

### Core
| Library / Component | Version / Location | Purpose | Why Standard |
|---------------------|--------------------|---------|--------------|
| C++20 public/internal implementation | `CMakePresets.json` sets `CMAKE_CXX_STANDARD` to `20` | Implement cleanup without public API expansion | The project contract is C++20 and Phase 17.1 forbids new public path/diagnostics/backend APIs. [VERIFIED: `CMakePresets.json`:13-19] [VERIFIED: `17.1-SPEC.md`:59-68] |
| Catch2 + CTest | Catch2 dependency in `vcpkg.json`; CTest discovery in `tests/CMakeLists.txt` | Runtime and source-policy validation | Existing unit executable already includes all target suites; adding to existing files avoids CMake churn. [VERIFIED: `vcpkg.json`:4-16] [VERIFIED: `tests/CMakeLists.txt`:65-117,119-138] |
| `detail::host_file_path` / `detail::host_file` | `src/detail/host_file_path.*`, `src/detail/host_file.*` | Windows UTF-8-to-native host path boundary and shared I/O helpers | This is the existing host-file boundary that current reader/parser/writer paths use. [VERIFIED: `host_file_path.cpp`:16-42] [VERIFIED: `host_file.cpp`:68-237] |
| `reader_backend` callback table | File-local in `src/archive.cpp` | Open-time reader backend selection | It already keeps public reader behavior on one callback seam; remove stored `backend_identity` but keep the table. [VERIFIED: `archive.cpp`:37-88,176-186] |

### Supporting
| Component | Version / Location | Purpose | When to Use |
|-----------|--------------------|---------|-------------|
| Generated fixture archives/manifests | `tests/fixtures/generated/archives/` | Manifest-backed expected counts, paths, and payload bytes | Use for combined reader-matrix regression. [VERIFIED: Glob `tests/fixtures/generated/archives/*manifest.json`] |
| `recording_sink_factory` pattern | `tests/unit/archive_reader_dispatch_tests.cpp` | Captures `extract_entries` payload bytes and create counts | Adapt for success-path `extract_entries` assertions in host-path suite. [VERIFIED: `archive_reader_dispatch_tests.cpp`:99-153,272-309] |
| Source-policy tests | `host_file_writer_name_tests.cpp`, `archive_reader_dispatch_policy_tests.cpp` | Guard negative invariants after cleanup | Use to forbid removed state and raw-host-path reopen drift without freezing all helper names. [VERIFIED: `host_file_writer_name_tests.cpp`:25-208] [VERIFIED: `archive_reader_dispatch_policy_tests.cpp`:46-103] |

### Alternatives Considered
| Instead of | Could Use | Tradeoff |
|------------|-----------|----------|
| Extend `host_path_correctness_boundary_tests.cpp` | Add a new combined regression suite | New suite would require CMake registration and duplicate non-ASCII setup; locked decision says use existing host-path suite. [VERIFIED: `17.1-CONTEXT.md`:49-54,103-105] |
| Remove unused state | Add private diagnostic consumers | Locked decision forbids adding new diagnostic consumers in Phase 17.1. [VERIFIED: `17.1-CONTEXT.md`:43-48] |
| Source-policy negative invariants | Snapshot exact helper implementations | Negative invariants prevent bad patterns while allowing equivalent shared-host-file seam implementations. [VERIFIED: `17.1-CONTEXT.md`:61-65] |
| Broad writer rewrite | Bounded inventory plus narrow migration if needed | Phase boundaries explicitly exclude a full writer architecture rewrite. [VERIFIED: `17.1-SPEC.md`:49-57] |

**Installation:** No new packages should be installed for Phase 17.1. [VERIFIED: `17.1-SPEC.md`:59-68] Existing dependencies are declared in `vcpkg.json`. [VERIFIED: `vcpkg.json`:1-17]

**Version verification:** Package versions are not changed by this phase. CMake/CTest availability on this machine is `4.3.2`. [VERIFIED: `cmake --version`] [VERIFIED: `ctest --version`]

## Architecture Patterns

### System Architecture Diagram

```text
Public UTF-8 host path
        |
        v
archive_reader::open / writer::write_to / writer::add_file
        |
        +--> detail::resolve_host_file_path (Windows UTF-8 -> native filesystem path)
                  |
                  v
          detail::host_file_path { resolved }
                  |
                  +--> reader parse/extract callbacks selected once at open
                  |        |
                  |        v
                  |   entries/find/contains/extract_entries via state_->backend_table
                  |
                  +--> writer prepare/layout/serialize source reads via detail::host_file helpers
                  |
                  +--> writer output publish via detail::publish_writer_output(resolved output path)
```

[VERIFIED: `host_file_path.cpp`:16-42] [VERIFIED: `archive.cpp`:160-186,266-408] [VERIFIED: `host_file.cpp`:68-237] [VERIFIED: `writer_publish.cpp`:60-148]

### Recommended Project Structure

```text
src/
├── detail/host_file_path.*        # remove original_utf8 and stale diagnostics comments
├── archive.cpp                    # remove backend_identity from reader state only
├── formats/bsa/                   # inventory TES3/TES4 writer source/output/finalization/dedupe paths
└── formats/ba2/                   # inventory BA2 GNRL/DX10 writer source/output/finalization/dedupe paths

tests/unit/
├── host_path_correctness_boundary_tests.cpp  # add combined non-ASCII reader matrix
├── host_file_path_tests.cpp                  # remove original_utf8 preservation test
├── host_file_tests.cpp                       # update any original_utf8 assertion
├── host_file_writer_name_tests.cpp           # strict cleanup/writer source-policy evidence
└── archive_reader_dispatch_policy_tests.cpp  # ensure public methods stay backend-table based
```

[VERIFIED: `17.1-CONTEXT.md`:96-122] [VERIFIED: grep `original_utf8|backend_identity` in `src` and `tests/unit`]

### Pattern 1: Remove dead private state at the seam

**What:** Keep only operational data in private state; when the locked decision says remove diagnostics-only fields, update comments and tests at the same time. [VERIFIED: `17.1-CONTEXT.md`:43-48]

**When to use:** Apply to `detail::host_file_path::original_utf8` and `archive_reader::state::backend_identity`. [VERIFIED: `host_file_path.hpp`:11-18] [VERIFIED: `archive.cpp`:81-88]

**Example:**
```cpp
// Target shape after cleanup: actual member names may remain as-is for the operational field.
struct host_file_path {
  std::filesystem::path resolved;
};

struct archive_reader::state {
  archive_metadata metadata;
  std::vector<entry_metadata> entries;
  detail::host_file_path host_path;
  const reader_backend* backend_table;
};
```
Source: current fields to remove are in `host_file_path.hpp` and `archive.cpp`. [VERIFIED: `host_file_path.hpp`:11-18] [VERIFIED: `archive.cpp`:81-88]

### Pattern 2: Combined host-path plus reader-surface success matrix

**What:** Copy representative archives under non-ASCII paths, open through public API, assert `entries`, `find`, `contains`, and `extract_entries` using manifest-backed expectations. [VERIFIED: `17.1-SPEC.md`:19-23]

**When to use:** Add to `tests/unit/host_path_correctness_boundary_tests.cpp`; reuse setup from host-path suite and sink pattern from dispatch suite. [VERIFIED: `17.1-CONTEXT.md`:49-54]

**Example:**
```cpp
for (const auto& archive_case : representative_archive_cases()) {
  const auto manifest = read_json_file(generated_archive_dir() / std::string{archive_case.manifest_file});
  const auto copied_archive = copy_archive_under_test(archive_case, unique_non_ascii_root());
  auto opened = libbsa::archive_reader::open(utf8_string_from_path(copied_archive));
  REQUIRE(opened.has_value());

  auto entries = opened.value().entries();
  REQUIRE(entries.has_value());
  CHECK(entries.value().size() == manifest.at("entries").size());

  const auto& expected = canonical_entry_from_manifest(manifest);
  const auto canonical_path = expected.at("path").get<std::string>();
  REQUIRE(opened.value().find(canonical_path).value().has_value());
  REQUIRE(opened.value().contains(canonical_path).value());

  recording_sink_factory sinks;
  std::vector requests{libbsa::bulk_extract_request{.path = canonical_path}};
  auto extracted = opened.value().extract_entries(requests, sinks);
  REQUIRE(extracted.has_value());
}
```
Source: adapt from existing host-path and dispatch suites. [VERIFIED: `host_path_correctness_boundary_tests.cpp`:117-212] [VERIFIED: `archive_reader_dispatch_tests.cpp`:121-153,272-309]

### Pattern 3: Writer host-path inventory table as closure evidence

**What:** Verification should include a source/test/policy evidence table for TES3, TES4-family, BA2 GNRL, and BA2 DX10 across source, output, finalization, and dedupe host-file paths. [VERIFIED: `17.1-CONTEXT.md`:55-60]

**When to use:** Use in final verification and source-policy tests; do not edit `.planning/v1.1-MILESTONE-AUDIT.md` directly. [VERIFIED: `17.1-CONTEXT.md`:61-65]

**Evidence inventory found:**

| Writer | Source path | Output path | Finalization source reopen | Dedupe path | Disposition |
|--------|-------------|-------------|----------------------------|-------------|-------------|
| TES3 BSA | `resolve_tes3_source_path` -> `resolve_host_file_path`; size via `inspect_host_file_size`. [VERIFIED: `tes3_bsa_prepare.cpp`:38-49,111-121] | `write_tes3_bsa_archive` resolves output and publishes `output_path.value().resolved`. [VERIFIED: `tes3_bsa_writer.cpp`:82-115] | `stream_disk_payload_to_output` accepts `detail::host_file_path` and uses `open_host_file`. [VERIFIED: `tes3_bsa_serialize.cpp`:72-112] | No TES3 dedupe path detected in current TES3 writer flow. [VERIFIED: `tes3_bsa_writer.cpp`:100-115] | Close with policy evidence; no migration found. |
| TES4-family BSA | `resolve_tes4_source_path` and `read_host_file_exact/prefix/inspect` cover source reads. [VERIFIED: `tes4_bsa_prepare.cpp`:171-225] | `write_tes4_bsa_archive` resolves output and publishes `output_path.value().resolved`. [VERIFIED: `tes4_bsa_writer.cpp`:92-149] | `stream_disk_payload_to_output` accepts `detail::host_file_path` and uses `open_host_file`. [VERIFIED: `tes4_bsa_serialize.cpp`:79-115] | Dedupe resolves disk paths before chunk reads and exact equality fallback. [VERIFIED: `tes4_bsa_layout.cpp`:71-196,201-215] | Close with policy evidence; no migration found. |
| BA2 GNRL | `resolve_ba2_gnrl_source_path` plus exact/prefix/chunk reads; raw disk stream stores `resolved_source_path`. [VERIFIED: `ba2_gnrl_prepare.cpp`:51-77,88-109,244-261] | `write_ba2_gnrl_archive` resolves output and publishes `output_path.value().resolved`. [VERIFIED: `ba2_gnrl_writer.cpp`:108-151] | `stream_disk_payload` accepts `detail::host_file_path` and uses `open_host_file`. [VERIFIED: `ba2_gnrl_serialize.cpp`:103-137] | Exact equality uses `resolved_source_path` and shared host-file helpers. [VERIFIED: `ba2_gnrl_layout.cpp`:96-107,179-252] | Close with policy evidence; no migration found. |
| BA2 DX10 | `read_dds_file` resolves DDS host path once, reads exact bytes, then snapshots subresources. [VERIFIED: `ba2_dx10_snapshot_builder.cpp`:62-68,156-197] | `write_ba2_dx10_archive` resolves output and publishes `output_path.value().resolved`. [VERIFIED: `ba2_dx10_writer.cpp`:116-157] | Final archive serialization writes prepared chunk bytes, not caller DDS host paths. [VERIFIED: `ba2_dx10_serialize.cpp`:95-160] | No currently supported BA2 DX10 disk-source dedupe reopen path detected; dedupe operates over prepared chunk stored payloads. [VERIFIED: `ba2_dx10_writer.cpp`:140-157] [VERIFIED: `ba2_dx10_serialize.cpp`:149-155] | Close writer host-path warning; keep abnormal-termination cleanup as accepted residual risk. [VERIFIED: `17.1-SPEC.md`:49-57] |

### Anti-Patterns to Avoid

- **Adding public diagnostics to justify state:** Phase 17.1 must remove unused state rather than adding new public or private diagnostic surface. [VERIFIED: `17.1-CONTEXT.md`:43-48]
- **Duplicating dispatch negative tests in the combined regression:** The combined regression is success-path focused and should not recreate every missing/invalid archive-path dispatch case. [VERIFIED: `17.1-CONTEXT.md`:49-54]
- **Freezing exact helper names in policy tests:** Guard bad patterns such as removed state members and raw string reopens, but allow equivalent host-file seam implementations. [VERIFIED: `17.1-CONTEXT.md`:61-65]
- **Updating the audit file directly:** `.planning/v1.1-MILESTONE-AUDIT.md` is source audit input for this phase, not an implementation target. [VERIFIED: `17.1-CONTEXT.md`:65,166-170]
- **Reopening BA2 DX10 abnormal-termination cleanup:** Crash/forced-termination cleanup is explicitly out of scope and accepted as residual risk. [VERIFIED: `17.1-SPEC.md`:49-57]

## Don't Hand-Roll

| Problem | Don't Build | Use Instead | Why |
|---------|-------------|-------------|-----|
| Windows UTF-8 host path conversion | New per-call conversion or narrow `std::ifstream` paths | `detail::resolve_host_file_path` + `detail::host_file` helpers | Existing boundary centralizes strict UTF-8 conversion and native path I/O. [VERIFIED: `host_file_path.cpp`:16-42] [VERIFIED: `host_file.cpp`:68-237] |
| Reader backend dispatch | New plugin/registry/extensibility framework | Existing file-local `reader_backend` callbacks selected at open | Phase boundary forbids replacing the Phase 15 seam. [VERIFIED: `17.1-SPEC.md`:49-57] [VERIFIED: `archive.cpp`:37-77] |
| Writer publication | Direct writes to final destination or ad hoc temp files | `detail::publish_writer_output` | Publish helper owns overwrite/reparse-point checks, temp directory reservation, and atomic replacement/no-replace publish. [VERIFIED: `writer_publish.cpp`:60-148] |
| Bulk extraction test capture | General mocking framework | File-local `bulk_extract_sink_factory` concrete test double | Existing tests use simple concrete sinks/factories and no mocking framework. [VERIFIED: `.planning/codebase/TESTING.md`:90-121] |
| Audit closure evidence | Manual claims only | Runtime test + source-policy test + final verification inventory | Locked decisions require runtime and policy proof plus cited evidence. [VERIFIED: `17.1-CONTEXT.md`:61-65] |

**Key insight:** This phase closes audit ambiguity by removing stale private state and making seams falsifiable; custom abstractions or public API changes would expand v1.1 scope and contradict locked decisions. [VERIFIED: `17.1-SPEC.md`:59-80]

## Runtime State Inventory

| Category | Items Found | Action Required |
|----------|-------------|-----------------|
| Stored data | None — Phase 17.1 targets source code, tests, and planning evidence; no database/datastore state stores `original_utf8` or `backend_identity`. [VERIFIED: `17.1-SPEC.md`:17-38] | None. |
| Live service config | None — project is a local C++ library with no web/API/service routes in this phase. [VERIFIED: `.planning/v1.1-MILESTONE-AUDIT.md`:101-114] | None. |
| OS-registered state | None — no Task Scheduler/service registration is part of this cleanup phase. [VERIFIED: `17.1-SPEC.md`:39-68] | None. |
| Secrets/env vars | None — no secret or environment variable names are changed; vcpkg toolchain env is unchanged. [VERIFIED: `vcpkg.json`:1-17] [VERIFIED: `CMakePresets.json`:13-19] | None. |
| Build artifacts | Build tree may contain old object files if incremental build is stale; normal CMake rebuild handles source changes. [ASSUMED] | Planner can use existing build preset; clean build only if incremental artifacts misbehave. |

## Common Pitfalls

### Pitfall 1: Removing `original_utf8` but leaving diagnostics comments/tests
**What goes wrong:** Tests or comments continue claiming UTF-8 diagnostic preservation after the member is removed. [VERIFIED: `host_file_path.hpp`:11-20] [VERIFIED: `host_file_path_tests.cpp`:72-80]
**Why it happens:** The field was intentionally introduced in Phase 13 as diagnostics-only state. [VERIFIED: `.planning/STATE.md`:55-56]
**How to avoid:** Update `host_file_path.hpp`, `host_file_path.cpp`, `host_file.hpp` comments, `host_file_path_tests.cpp`, `host_file_tests.cpp`, and policy tests together. [VERIFIED: grep `original_utf8` in `src` and `tests/unit`]
**Warning signs:** Any remaining `original_utf8` token outside historical planning docs after implementation. [VERIFIED: `17.1-CONTEXT.md`:43-48]

### Pitfall 2: Removing `backend_identity` incorrectly and losing open-time table selection
**What goes wrong:** Public methods regain archive-family branching or `backend_table` is not initialized correctly. [VERIFIED: `archive_reader_dispatch_policy_tests.cpp`:46-103]
**Why it happens:** `reader_backend_identity` is still useful as file-local open-time selector, but the stored state field is unused. [VERIFIED: `archive.cpp`:37-88,176-186]
**How to avoid:** Keep the file-local enum and `reader_backend_table(identity)` if useful; remove only `archive_reader::state::backend_identity` and adjust `state{...}` construction. [VERIFIED: `17.1-CONTEXT.md`:43-48]
**Warning signs:** `backend_identity` appears in `archive_reader::state` or any public method body. [VERIFIED: `17.1-SPEC.md`:29-33]

### Pitfall 3: Combined regression accidentally becomes a broad dispatch duplicate
**What goes wrong:** The host-path test duplicates missing-path, invalid-path, `extract`, and `extract_bytes` matrices already covered by dispatch tests. [VERIFIED: `archive_reader_dispatch_tests.cpp`:221-310]
**Why it happens:** Phase 15 dispatch test is comprehensive; Phase 17.1 only needs the seam boundary under non-ASCII host paths. [VERIFIED: `17.1-CONTEXT.md`:49-54]
**How to avoid:** Assert manifest entry count/path, canonical `find`, canonical `contains`, and `extract_entries` payload bytes only. [VERIFIED: `17.1-CONTEXT.md`:49-54]
**Warning signs:** New negative missing/invalid path loops appear in host-path suite without a discovered implementation gap. [VERIFIED: `17.1-CONTEXT.md`:52-54]

### Pitfall 4: Treating BA2 DX10 abnormal termination as a Phase 17.1 bug
**What goes wrong:** Planner adds crash-proof cleanup or OS-shutdown cleanup tasks. [VERIFIED: `17.1-SPEC.md`:49-57]
**Why it happens:** Audit mentions BA2 DX10 abnormal termination as residual risk. [VERIFIED: `.planning/v1.1-MILESTONE-AUDIT.md`:24-26,116-123]
**How to avoid:** Record it as accepted residual risk in final evidence; do not implement new cleanup guarantees. [VERIFIED: `17.1-CONTEXT.md`:28-36]
**Warning signs:** Plans modify snapshot lifecycle beyond ordinary success/failure cleanup evidence. [VERIFIED: `ba2_dx10_writer.cpp`:31-44,94-109]

### Pitfall 5: Missing writer inventory categories
**What goes wrong:** Verification says writer paths are closed but does not cite TES3, TES4, BA2 GNRL, BA2 DX10 source/output/finalization/dedupe categories. [VERIFIED: `17.1-CONTEXT.md`:55-60]
**Why it happens:** Some formats have no dedupe path or no disk-source finalization path, so absence must be explicit. [VERIFIED: writer inventory table above]
**How to avoid:** Use the evidence table in this research as the verification checklist and add/adjust source-policy tests for drift. [VERIFIED: `17.1-CONTEXT.md`:55-65]
**Warning signs:** Final verification relies only on planning notes or only on one writer family. [VERIFIED: `17.1-CONTEXT.md`:55-60]

## Code Examples

Verified patterns from existing source:

### Resolving public UTF-8 host paths once
```cpp
auto output_path = detail::resolve_host_file_path(output_host_path);
if (!output_path) {
  return output_path.error();
}
return detail::publish_writer_output(output_path.value().resolved, options.overwrite_existing, "TES4 BSA writer", ...);
```
Source: `tes4_bsa_writer.cpp`. [VERIFIED: `tes4_bsa_writer.cpp`:104-149]

### Public reader methods using the selected backend table
```cpp
result<std::vector<entry_metadata>> archive_reader::entries() const {
  if (!state_) {
    return error{error_code::unsupported, "archive reader is not open"};
  }
  return state_->backend_table->entries(state_->entries);
}
```
Source: `archive.cpp`. [VERIFIED: `archive.cpp`:266-271]

### Bulk extraction sink capture for `extract_entries`
```cpp
class recording_sink_factory final : public libbsa::bulk_extract_sink_factory {
 public:
  libbsa::result<std::unique_ptr<libbsa::payload_sink>> create(std::string_view path,
                                                               const libbsa::entry_metadata&) override {
    auto capture = std::make_shared<sink_capture>();
    captures_[std::string{path}].push_back(capture);
    return std::unique_ptr<libbsa::payload_sink>{new capturing_sink{std::move(capture)}};
  }
};
```
Source: `archive_reader_dispatch_tests.cpp`. [VERIFIED: `archive_reader_dispatch_tests.cpp`:99-153]

### Writer dedupe exact comparison stays behind host-file helpers
```cpp
auto compared = detail::for_each_host_file_chunk(
    source_path.value(), entry.raw_disk_size, tes4_dedupe_source_context,
    [&](std::span<const std::byte> chunk) -> result<void> {
      hash_payload_bytes(hash, chunk);
      return {};
    });
```
Source: `tes4_bsa_layout.cpp`. [VERIFIED: `tes4_bsa_layout.cpp`:176-196]

## State of the Art

| Old Approach | Current Approach | When Changed | Impact |
|--------------|------------------|--------------|--------|
| Reader/parser paths used narrower/raw host path opens inconsistently. | `archive_reader::open` resolves host path once and parser/extract reads use `detail::host_file_path`. | Phase 13. [VERIFIED: `.planning/STATE.md`:55-56] | Phase 17.1 can remove diagnostics-only text without losing operational path correctness. |
| Public reader operations repeated family dispatch. | `archive_reader::open` selects a backend callback table once, and public methods dereference `backend_table`. | Phase 15. [VERIFIED: `.planning/STATE.md`:59-60] | Phase 17.1 should remove only stored `backend_identity`, not the open-time seam. |
| TES4/BA2 GNRL dedupe compared many candidates directly. | Dedupe uses narrowing identities but exact stored-byte equality remains authority. | Phase 17. [VERIFIED: `.planning/STATE.md`:63-65] | Writer inventory must preserve exact fallback semantics. |
| BA2 DX10 snapshots could linger after normal write attempts. | `write_to` consumes the writer and performs best-effort cleanup after ordinary attempts; abnormal termination remains accepted residual risk. | Phase 17. [VERIFIED: `.planning/STATE.md`:66-69] | Phase 17.1 should document residual risk only, not implement crash-proof cleanup. |

**Deprecated/outdated:**
- `detail::host_file_path::original_utf8` as diagnostics-only state is explicitly obsolete for Phase 17.1 and should be removed. [VERIFIED: `17.1-CONTEXT.md`:43-48]
- `archive_reader::state::backend_identity` as stored reader state is explicitly obsolete for Phase 17.1 and should be removed. [VERIFIED: `17.1-CONTEXT.md`:43-48]

## Assumptions Log

| # | Claim | Section | Risk if Wrong |
|---|-------|---------|---------------|
| A1 | Normal CMake incremental rebuild should handle source changes; clean build only if stale artifacts misbehave. | Runtime State Inventory | Low — planner may add an unnecessary clean build step if conservative. |

## Open Questions (RESOLVED)

1. **RESOLVED: Should the combined matrix include TES3?**
   - What we know: Phase 15 representative dispatch fixtures include TES3, while the current Phase 13 host-path representative set excludes `tes3_success.bsa`. [VERIFIED: `archive_reader_dispatch_tests.cpp`:58-69] [VERIFIED: `host_path_correctness_boundary_tests.cpp`:100-115,257-274]
   - Resolution: The current plan set includes TES3 through the Phase 15 representative reader-family matrix, per D-05, and keeps the combined regression in `tests/unit/host_path_correctness_boundary_tests.cpp` per D-07. `17.1-01-PLAN.md` Task 1 explicitly instructs the executor to include Phase 15 representative families where practical and, if adding TES3 requires changing the host-path suite source-policy allowlist, to update only that policy assertion in the same file so `tes3_success.bsa` is allowed for the combined matrix. [VERIFIED: `17.1-CONTEXT.md`:49-54] [VERIFIED: `17.1-01-PLAN.md`:75-109]
   - Verification implication: SPEC-1 remains satisfied by the combined public-API regression over existing supported archive fixtures, and the TES3 host-path suite policy update is part of the combined matrix task rather than a separate replanning item. [VERIFIED: `17.1-SPEC.md`:19-23] [VERIFIED: `17.1-01-PLAN.md`:96-103]

## Environment Availability

| Dependency | Required By | Available | Version | Fallback |
|------------|-------------|-----------|---------|----------|
| CMake | Configure/build validation | ✓ | 4.3.2 | None needed. [VERIFIED: `cmake --version`] |
| CTest | Runtime/source-policy validation | ✓ | 4.3.2 | None needed. [VERIFIED: `ctest --version`] |
| vcpkg dependencies | Catch2/nlohmann-json/lib dependencies | Declared | Manifest lists dependencies, installed state not probed. | Existing configured build tree or vcpkg restore during configure. [VERIFIED: `vcpkg.json`:1-17] |
| Generated fixture archives/manifests | Combined regression | ✓ | Committed fixture files present | None needed. [VERIFIED: Glob `tests/fixtures/generated/archives/*.{bsa,ba2}`] |

**Missing dependencies with no fallback:** None found for research/planning. [VERIFIED: environment probes above]

**Missing dependencies with fallback:** vcpkg installed package state was not probed; CMake configure will restore/resolve manifest dependencies when VCPKG_ROOT is set. [ASSUMED]

## Validation Architecture

### Test Framework
| Property | Value |
|----------|-------|
| Framework | Catch2 3 via `Catch2::Catch2WithMain`; CTest labels derive from Catch2 tags. [VERIFIED: `.planning/codebase/TESTING.md`:5-19,189-200] |
| Config file | `tests/CMakeLists.txt`. [VERIFIED: `tests/CMakeLists.txt`:65-138] |
| Quick run command | `ctest --preset windows-msvc-debug-static --output-on-failure -R "host_path_correctness_boundary|archive_reader_dispatch|host_file|host_file_path"` [VERIFIED: `CMakePresets.json`:103-144] |
| Full suite command | `ctest --preset windows-msvc-debug-static --output-on-failure` [VERIFIED: `.planning/codebase/TESTING.md`:14-19] |
| ASan gate command | `ctest --preset windows-msvc-asan-static --output-on-failure -R "host_path_correctness_boundary|archive_reader_dispatch|host_file|host_file_path"` [VERIFIED: `CMakePresets.json`:61-74,137-143] |

### Phase Requirements → Test Map
| Req ID | Behavior | Test Type | Automated Command | File Exists? |
|--------|----------|-----------|-------------------|--------------|
| SPEC-1 | Non-ASCII copied archive supports `entries`, `find`, `contains`, `extract_entries` through public API | unit/fixture regression | `ctest --preset windows-msvc-debug-static --output-on-failure -R host_path_correctness_boundary` | ✅ extend existing file [VERIFIED: `host_path_correctness_boundary_tests.cpp`:216-274] |
| SPEC-2 | `original_utf8` removed and stale diagnostics-preservation comments/tests gone | source-policy/unit cleanup | `ctest --preset windows-msvc-debug-static --output-on-failure -R "host_file_path|host_file"` | ✅ update existing files [VERIFIED: `host_file_path_tests.cpp`:72-80] [VERIFIED: `host_file_writer_name_tests.cpp`:52-68] |
| SPEC-3 | `backend_identity` not stored in reader state; public reader methods keep one backend seam | source-policy + fixture regression | `ctest --preset windows-msvc-debug-static --output-on-failure -R archive_reader_dispatch` | ✅ existing policy/runtime files [VERIFIED: `archive_reader_dispatch_policy_tests.cpp`:46-103] [VERIFIED: `archive_reader_dispatch_tests.cpp`:189-311] |
| SPEC-4 | Writer host-path warning closed with source/test/policy evidence | source-policy + verification inventory | `ctest --preset windows-msvc-debug-static --output-on-failure -R host_file` | ✅ extend existing policy file [VERIFIED: `host_file_writer_name_tests.cpp`:25-208] |

### Sampling Rate
- **Per task commit:** `ctest --preset windows-msvc-debug-static --output-on-failure -R "host_path_correctness_boundary|archive_reader_dispatch|host_file|host_file_path"` [VERIFIED: `CMakePresets.json`:103-144]
- **Per wave merge:** `ctest --preset windows-msvc-debug-static --output-on-failure` [VERIFIED: `.planning/codebase/TESTING.md`:14-19]
- **Phase gate:** Focused Debug + focused MSVC ASan lanes before verification, matching D-14. [VERIFIED: `17.1-CONTEXT.md`:61-65]

### Wave 0 Gaps
- [ ] Extend `tests/unit/host_path_correctness_boundary_tests.cpp` — covers SPEC-1 combined matrix. [VERIFIED: `17.1-CONTEXT.md`:49-54]
- [ ] Update `tests/unit/host_file_path_tests.cpp` and `tests/unit/host_file_tests.cpp` — remove `original_utf8` preservation assertions. [VERIFIED: grep `original_utf8` in `tests/unit`]
- [ ] Update/extend `tests/unit/host_file_writer_name_tests.cpp` — source-policy evidence for removed state and writer inventory categories. [VERIFIED: `host_file_writer_name_tests.cpp`:25-208]
- [ ] Update `tests/unit/archive_reader_dispatch_policy_tests.cpp` only if needed to forbid stored `backend_identity` outside public method bodies. [VERIFIED: `archive_reader_dispatch_policy_tests.cpp`:46-103]

## Security Domain

### Applicable ASVS Categories

| ASVS Category | Applies | Standard Control |
|---------------|---------|------------------|
| V2 Authentication | no | No authentication subsystem; local C++ archive library. [VERIFIED: `.planning/codebase/ARCHITECTURE.md`:202-207] |
| V3 Session Management | no | No sessions or web state. [VERIFIED: `.planning/codebase/ARCHITECTURE.md`:202-207] |
| V4 Access Control | no | No authorization tier; file access is caller/environment controlled. [VERIFIED: `.planning/codebase/ARCHITECTURE.md`:202-207] |
| V5 Input Validation | yes | Preserve UTF-8 validation, archive-path normalization, public error codes, and source-policy tests. [VERIFIED: `host_file_path.cpp`:22-40] [VERIFIED: `.planning/codebase/ARCHITECTURE.md`:194-200] |
| V6 Cryptography | no for new work | Phase 17.1 does not add cryptography; existing BCrypt use for BA2 DX10 temp suffix generation remains private and unchanged. [VERIFIED: `ba2_dx10_snapshot_builder.cpp`:70-89] |

### Known Threat Patterns for C++ archive host-path hardening

| Pattern | STRIDE | Standard Mitigation |
|---------|--------|---------------------|
| Invalid or locale-misinterpreted UTF-8 host path | Tampering/Denial of Service | Strict `MultiByteToWideChar(CP_UTF8, MB_ERR_INVALID_CHARS)` conversion at boundary. [VERIFIED: `host_file_path.cpp`:26-40] |
| Raw string reopen drift for non-ASCII paths | Reliability / Tampering | Source-policy tests forbid raw caller-text reopen patterns and require shared host-file seam. [VERIFIED: `host_file_writer_name_tests.cpp`:88-208] |
| Reparse-point output replacement | Tampering/Elevation of Privilege | `writer_publish` rejects reparse-point overwrite targets before atomic replacement. [VERIFIED: `writer_publish.cpp`:38-50,75-89,121-145] |
| BA2 DX10 snapshot persistence after abnormal termination | Information Disclosure | Keep documented residual risk out of Phase 17.1; do not claim crash-proof cleanup. [VERIFIED: `17.1-SPEC.md`:49-57] [VERIFIED: `.planning/codebase/CONCERNS.md`:33-40] |

## Sources

### Primary (HIGH confidence)
- `.planning/phases/17.1-address-tech-debt-in-planning-v1-1-milestone-audit-md/17.1-SPEC.md` — locked requirements, boundaries, acceptance criteria.
- `.planning/phases/17.1-address-tech-debt-in-planning-v1-1-milestone-audit-md/17.1-CONTEXT.md` — locked implementation decisions and canonical refs.
- `.planning/v1.1-MILESTONE-AUDIT.md` — source audit tech debt and integration warnings.
- `src/detail/host_file_path.hpp`, `src/detail/host_file_path.cpp`, `src/detail/host_file.hpp`, `src/detail/host_file.cpp` — current host-file boundary.
- `src/archive.cpp` — reader state and open-time backend dispatch.
- `tests/unit/host_path_correctness_boundary_tests.cpp`, `tests/unit/archive_reader_dispatch_tests.cpp`, `tests/unit/archive_reader_dispatch_policy_tests.cpp`, `tests/unit/host_file_writer_name_tests.cpp` — regression and policy surfaces.
- TES3/TES4/BA2 writer source files listed in the writer inventory table — current writer source/output/finalization/dedupe paths.

### Secondary (MEDIUM confidence)
- `.planning/codebase/ARCHITECTURE.md`, `.planning/codebase/TESTING.md`, `.planning/codebase/CONCERNS.md` — codebase maps and known concerns current as of 2026-05-12.
- `.planning/STATE.md`, `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md` — project history and v1.1 traceability.

### Tertiary (LOW confidence)
- None used for technical conclusions. No web-only claims are included.

## Metadata

**Confidence breakdown:**
- Standard stack: HIGH — based on checked-in `vcpkg.json`, `CMakePresets.json`, `tests/CMakeLists.txt`, and codebase maps.
- Architecture: HIGH — based on direct source reads for `archive.cpp`, host-file helpers, writer pipelines, and policy tests.
- Pitfalls: HIGH — based on SPEC/CONTEXT locked decisions, source audit findings, and current tests containing the exact stale-state assertions.

**Research date:** 2026-05-14
**Valid until:** 2026-06-13, or until Phase 17.1 implementation changes any listed source/test file.
