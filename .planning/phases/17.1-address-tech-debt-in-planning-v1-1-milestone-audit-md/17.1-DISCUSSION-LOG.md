# Phase 17.1: address-tech-debt-in-planning-v1-1-milestone-audit-md - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md - this log preserves the alternatives considered.

**Date:** 2026-05-14
**Phase:** 17.1-address-tech-debt-in-planning-v1-1-milestone-audit-md
**Areas discussed:** Unused state, Combined regression, Writer path closure, Proof style

---

## Unused State

### `detail::host_file_path::original_utf8` disposition

| Option | Description | Selected |
|--------|-------------|----------|
| Remove member | Remove `original_utf8` and stale diagnostics-preservation comments/tests unless a real runtime consumer exists; keeps host-file state minimal. | yes |
| Private diagnostic | Keep it only if implementation adds a real private diagnostic or policy consumer with committed pass/fail evidence; no public diagnostics API. | |
| You decide | Let downstream agents choose the smallest valid option after inspecting implementation impact. | |

**User's choice:** Remove member
**Notes:** Removal is locked; this phase must not replace the cleanup with new diagnostics scope.

### `archive_reader::state::backend_identity` disposition

| Option | Description | Selected |
|--------|-------------|----------|
| Remove state | Remove the state member; keep any file-local enum only as open-time table-selection plumbing if needed. | yes |
| Private consumer | Keep it only if a real private diagnostic or policy path uses it outside public reader method bodies, with tests proving it is not dead state. | |
| You decide | Let downstream agents choose between removal and verified consumption based on the smallest safe diff. | |

**User's choice:** Remove state
**Notes:** Open-time backend table selection may keep local plumbing, but reader state should not retain unused identity storage.

### Cleanup strictness

| Option | Description | Selected |
|--------|-------------|----------|
| Strict cleanup | Remove or rewrite comments/tests that claim diagnostics-only preservation, and add/update policy evidence so the old unused members cannot quietly return. | yes |
| Code only | Remove the members and only adjust tests that fail; avoid adding new source-policy checks unless already nearby. | |
| You decide | Let downstream agents balance minimal diff against regression-proofing after seeing affected tests. | |

**User's choice:** Strict cleanup
**Notes:** Stale comments and tests are part of the cleanup target.

### Override allowance

| Option | Description | Selected |
|--------|-------------|----------|
| No override | Removal is locked for Phase 17.1; any future diagnostics use should be a separate later requirement. | yes |
| Narrow override | Allow keeping a member only if the planner proves a real non-test runtime consumer and committed coverage, without public API expansion. | |
| You decide | Let downstream agents decide only if removal becomes riskier than expected. | |

**User's choice:** No override
**Notes:** The phase should not grow a new diagnostics requirement to justify retaining unused state.

---

## Combined Regression

### Regression breadth

| Option | Description | Selected |
|--------|-------------|----------|
| One fixture | Use one representative supported archive copied under a non-ASCII path; smallest test that satisfies SPEC acceptance. | |
| Reader matrix | Cover the current Phase 15 representative reader set under non-ASCII paths, reusing manifest-driven assertions without deeper family edge cases. | yes |
| You decide | Let downstream agents choose the smallest fixture set that proves the Phase 13 and Phase 15 seam boundary. | |

**User's choice:** Reader matrix
**Notes:** The test should prove the split Phase 13/Phase 15 boundary, not just a single smoke case.

### Reader operations

| Option | Description | Selected |
|--------|-------------|----------|
| Spec surface | Assert `entries`, `find`, `contains`, and `extract_entries`; leave `extract`/`extract_bytes` to existing Phase 13 and dispatch suites. | yes |
| Full surface | Also assert `extract` and `extract_bytes` in this combined non-ASCII matrix, duplicating more of the existing dispatch suite. | |
| You decide | Let downstream agents add only the operations needed to make the seam proof clear and non-duplicative. | |

**User's choice:** Spec surface
**Notes:** Existing suites already cover direct extraction and dispatch negatives separately.

### Test location

| Option | Description | Selected |
|--------|-------------|----------|
| Host-path suite | Extend `host_path_correctness_boundary_tests.cpp` so the non-ASCII fixture setup and token stay in one place. | yes |
| Dispatch suite | Extend `archive_reader_dispatch_tests.cpp` with non-ASCII copies, keeping all public reader-surface checks together. | |
| New suite | Create a small dedicated combined-boundary suite if downstream agents think reuse would otherwise make either existing file noisy. | |

**User's choice:** Host-path suite
**Notes:** Reuse the existing non-ASCII setup rather than creating a new suite.

### Negative cases

| Option | Description | Selected |
|--------|-------------|----------|
| Success only | Verify manifest count/path, canonical `find`/`contains`, and successful `extract_entries` bytes under non-ASCII paths; avoid duplicating dispatch error matrix. | yes |
| Add negatives | Also include missing-path and invalid-path `find`/`contains`/bulk results under non-ASCII paths for stronger but more duplicative coverage. | |
| You decide | Let downstream agents include negatives only if the implementation diff naturally exposes a gap. | |

**User's choice:** Success only
**Notes:** Keep the regression focused and non-duplicative.

---

## Writer Path Closure

### Closure threshold

| Option | Description | Selected |
|--------|-------------|----------|
| Evidence inventory | Inventory source, output, finalization, and dedupe writer I/O paths and close the warning if they already route through the shared host-file boundary. | yes |
| Migrate found gaps | Do the inventory and migrate any narrow raw host-path reopen found behind `detail::host_file_path` within this phase. | |
| Future requirement | Treat broader writer hardening as future scope unless a very specific v1.1 gap is proven during planning. | |

**User's choice:** Evidence inventory
**Notes:** Closure starts from explicit evidence rather than a broad writer redesign.

### Inventory categories

| Option | Description | Selected |
|--------|-------------|----------|
| Spec categories | Cover currently supported TES3 BSA, TES4-family BSA, BA2 GNRL, and BA2 DX10 source, output, finalization, and dedupe host-file I/O paths. | yes |
| Known warnings | Limit inventory to audit-mentioned paths: writer raw reopen concerns and Phase 17 BA2 GNRL disk-backed dedupe remediation evidence. | |
| You decide | Let downstream agents scope the inventory to the minimum set needed to close the audit warning clearly. | |

**User's choice:** Spec categories
**Notes:** Verification must be able to cite each currently supported writer path category.

### Evidence location

| Option | Description | Selected |
|--------|-------------|----------|
| Verification evidence | Implementation should update tests/policy as needed, and final verification must cite the exact source/test evidence that closes each writer path category. | yes |
| Planning only | Record the inventory in planning/summary artifacts without requiring new source-policy evidence unless code changes are needed. | |
| Source policy | Add a source-policy test that enumerates the writer path boundaries, with verification pointing to that policy test. | |

**User's choice:** Verification evidence
**Notes:** Planning-only inventory is insufficient for closure.

### Found raw reopen handling

| Option | Description | Selected |
|--------|-------------|----------|
| Migrate narrow | Migrate the specific raw reopen behind the shared host-file boundary if it is in current supported writer source/output/finalization/dedupe flow. | yes |
| Defer specific | Record it as a specific future requirement if it is broader than Phase 17.1 or risks expanding the hardening scope. | |
| Ask again | Require downstream agents to stop and ask before choosing migration versus deferral for any found gap. | |

**User's choice:** Migrate narrow
**Notes:** Narrow supported-flow raw reopen gaps should be fixed in this phase.

---

## Proof Style

### Proof mix

| Option | Description | Selected |
|--------|-------------|----------|
| Runtime plus policy | Behavior regressions for reader/non-ASCII and source-policy guardrails for removed state and writer path-boundary inventory. | yes |
| Runtime mainly | Prefer committed runtime tests; use source-policy only where code structure cannot be proven through public behavior. | |
| Policy mainly | Prefer source-policy tests for this audit cleanup, with minimal runtime additions beyond the required combined regression. | |

**User's choice:** Runtime plus policy
**Notes:** Use behavior tests for public boundary proof and policy tests for internal drift prevention.

### Verification lanes

| Option | Description | Selected |
|--------|-------------|----------|
| Focused Debug | Run focused Debug tests/policy suites for the changed areas; enough for small audit cleanup if no risky parser/writer behavior changes land. | |
| Debug plus ASan | Run focused Debug and MSVC ASan lanes when touching host-path, reader dispatch, or writer I/O seams; Release package proof only if packaging/public surface changes unexpectedly. | yes |
| Ship gate full | Repeat the broader Phase 17 ship-gate style: focused Debug, ASan, full Debug regression, review, and Release package proof. | |

**User's choice:** Debug plus ASan
**Notes:** Release package proof is conditional, not mandatory for this cleanup phase.

### Source-policy strictness

| Option | Description | Selected |
|--------|-------------|----------|
| Negative invariant | Forbid specific bad patterns like old unused members or raw string reopens, but do not freeze exact helper names beyond existing shared seam tokens. | yes |
| Exact inventory | Require policy tests to enumerate expected files and exact host-file seam calls so changes are intentionally reviewed. | |
| You decide | Let downstream agents choose per area, using existing policy-test style as the guide. | |

**User's choice:** Negative invariant
**Notes:** Preserve the existing policy-test style that protects behavior without freezing implementation details.

### Milestone audit artifact update

| Option | Description | Selected |
|--------|-------------|----------|
| Update audit | Update `.planning/v1.1-MILESTONE-AUDIT.md` so the old warnings are explicitly closed or accepted after verification evidence exists. | |
| Verification only | Leave the original audit immutable and record closure only in Phase 17.1 verification/summary artifacts. | |
| You decide | Let downstream agents choose the least disruptive planning update needed to avoid stale tech-debt status. | |

**User's choice:** Updating the audit has its own slash-command, do not worry about this.
**Notes:** Phase 17.1 should not update `.planning/v1.1-MILESTONE-AUDIT.md`; that is handled separately after closure evidence exists.

---

## the agent's Discretion

None. The user did not delegate any selected area to the agent.

## Deferred Ideas

- Updating `.planning/v1.1-MILESTONE-AUDIT.md` after closure evidence exists is handled by a dedicated slash-command, not by Phase 17.1 implementation.
