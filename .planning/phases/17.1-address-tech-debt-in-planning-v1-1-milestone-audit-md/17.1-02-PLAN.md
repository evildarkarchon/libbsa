---
phase: 17.1-address-tech-debt-in-planning-v1-1-milestone-audit-md
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/detail/host_file_path.hpp
  - src/detail/host_file_path.cpp
  - src/archive.cpp
  - tests/unit/host_file_path_tests.cpp
  - tests/unit/host_file_tests.cpp
  - tests/unit/archive_reader_dispatch_policy_tests.cpp
autonomous: true
requirements: [SPEC-2, SPEC-3, D-01, D-02, D-03, D-04, D-13, D-14, D-15]
must_haves:
  truths:
    - "detail::host_file_path no longer stores original_utf8 or claims diagnostics-only preservation."
    - "archive_reader::state no longer stores backend_identity beside backend_table."
    - "Public reader methods continue to use the open-time selected backend_table and do not regain repeated family dispatch."
    - "D-01: detail::host_file_path::original_utf8 is removed from the internal host-file path contract with no new private diagnostic consumer."
    - "D-02: archive_reader::state::backend_identity is removed from stored reader state; backend identity may exist only as file-local open-time table-selection plumbing if needed."
    - "D-03: Comments, tests, and policy evidence are strictly cleaned so diagnostics-only preservation claims and the removed unused members do not quietly return."
    - "D-04: Future diagnostics for original UTF-8 text or backend identity are excluded from this cleanup phase and must be a later requirement."
    - "D-13: This plan supplies source-policy proof for dead-state removal alongside runtime host-file and reader dispatch tests."
    - "D-15: Source-policy tests guard negative invariants such as removed state members without freezing exact helper names."
  artifacts:
    - path: "src/detail/host_file_path.hpp"
      provides: "Internal host-file path contract containing only operational resolved path state"
      contains: "std::filesystem::path resolved"
    - path: "src/archive.cpp"
      provides: "archive_reader state without stored backend_identity"
      contains: "const reader_backend* backend_table"
    - path: "tests/unit/host_file_path_tests.cpp"
      provides: "Source/runtime proof for strict UTF-8 conversion without original_utf8 preservation"
      contains: "MultiByteToWideChar"
    - path: "tests/unit/archive_reader_dispatch_policy_tests.cpp"
      provides: "Policy proof that stored backend_identity does not return and public methods avoid repeated dispatch"
      contains: "backend_identity"
  key_links:
    - from: "src/detail/host_file_path.cpp"
      to: "src/detail/host_file_path.hpp"
      via: "resolve_host_file_path returns host_file_path{resolved}"
      pattern: "host_file_path\{std::filesystem::path"
    - from: "src/archive.cpp"
      to: "reader_backend_table"
      via: "open-time table selection before state construction"
      pattern: "&reader_backend_table\(backend_identity\)"
---

<objective>
Remove the two private unused-state members named by the audit while preserving the operational host-file boundary and open-time reader backend seam.

Purpose: Close SPEC-2 and SPEC-3 by deleting diagnostics-only/private duplicate state instead of adding new diagnostic consumers, exactly per D-01 through D-04.
Output: Simplified internal contracts plus focused runtime/source-policy test updates proving the removed members do not return.
</objective>

<execution_context>
@J:/libbsa/.opencode/get-shit-done/workflows/execute-plan.md
@J:/libbsa/.opencode/get-shit-done/templates/summary.md
@J:/libbsa/.opencode/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/v1.1-MILESTONE-AUDIT.md
@.planning/phases/17.1-address-tech-debt-in-planning-v1-1-milestone-audit-md/17.1-SPEC.md
@.planning/phases/17.1-address-tech-debt-in-planning-v1-1-milestone-audit-md/17.1-CONTEXT.md
@.planning/phases/17.1-address-tech-debt-in-planning-v1-1-milestone-audit-md/17.1-RESEARCH.md
@.planning/phases/17.1-address-tech-debt-in-planning-v1-1-milestone-audit-md/17.1-VALIDATION.md
@.planning/phases/17.1-address-tech-debt-in-planning-v1-1-milestone-audit-md/17.1-PATTERNS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/TESTING.md
@.planning/phases/15-reader-backend-dispatch-cleanup/15-CONTEXT.md
@.planning/phases/15-reader-backend-dispatch-cleanup/15-01-SUMMARY.md
@.planning/phases/15-reader-backend-dispatch-cleanup/15-LEARNINGS.md
@src/detail/host_file_path.hpp
@src/detail/host_file_path.cpp
@src/archive.cpp
@tests/unit/host_file_path_tests.cpp
@tests/unit/host_file_tests.cpp
@tests/unit/archive_reader_dispatch_policy_tests.cpp

<interfaces>
Target internal contract after D-01:
- `struct libbsa::detail::host_file_path { std::filesystem::path resolved; };`
- `result<host_file_path> resolve_host_file_path(std::string_view host_path);`

Target reader state after D-02:
- `archive_reader::state` keeps `archive_metadata metadata`, `std::vector<entry_metadata> entries`, `detail::host_file_path host_path`, and `const reader_backend* backend_table`.
- `reader_backend_identity` may remain file-local in `src/archive.cpp` for open-time `reader_backend_table(identity)` selection only; it must not be a member of `archive_reader::state`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove original_utf8 from host-file path contract and tests</name>
  <files>src/detail/host_file_path.hpp, src/detail/host_file_path.cpp, tests/unit/host_file_path_tests.cpp, tests/unit/host_file_tests.cpp</files>
  <read_first>
    - .planning/phases/17.1-address-tech-debt-in-planning-v1-1-milestone-audit-md/17.1-SPEC.md
    - .planning/phases/17.1-address-tech-debt-in-planning-v1-1-milestone-audit-md/17.1-CONTEXT.md
    - .planning/phases/17.1-address-tech-debt-in-planning-v1-1-milestone-audit-md/17.1-RESEARCH.md
    - .planning/phases/17.1-address-tech-debt-in-planning-v1-1-milestone-audit-md/17.1-PATTERNS.md
    - .planning/v1.1-MILESTONE-AUDIT.md
    - src/detail/host_file_path.hpp
    - src/detail/host_file_path.cpp
    - tests/unit/host_file_path_tests.cpp
    - tests/unit/host_file_tests.cpp
  </read_first>
  <behavior>
    - Resolving an empty host path still returns an empty resolved `std::filesystem::path` result.
    - Resolving a non-ASCII UTF-8 host path still decodes through `MultiByteToWideChar(CP_UTF8, MB_ERR_INVALID_CHARS)` and returns the expected native path in `.resolved`.
    - Invalid UTF-8 still returns `error_code::invalid_argument` with the existing error message.
    - The live source/test contract no longer contains `original_utf8` outside historical planning artifacts.
  </behavior>
  <action>Per D-01, D-03, D-04, D-13, and D-15, remove the `std::string original_utf8` member from `src/detail/host_file_path.hpp` and rewrite the struct Doxygen/comment text so it describes only the resolved Windows host-file boundary, not diagnostics-only preservation. In `src/detail/host_file_path.cpp`, keep a local `std::string` buffer for UTF-8 decoding and size checks, preserve `MultiByteToWideChar(CP_UTF8, MB_ERR_INVALID_CHARS)`, preserve the existing invalid UTF-8 and too-long error messages, and return `host_file_path{std::filesystem::path{...}}` or equivalent single-member construction. In `tests/unit/host_file_path_tests.cpp`, remove the diagnostics-preservation test and replace it with a negative source-policy assertion that live `src/detail/host_file_path.hpp` and `src/detail/host_file_path.cpp` do not contain the token `original_utf8`. In `tests/unit/host_file_tests.cpp`, remove any assertion against `resolved.value().original_utf8` while preserving exact/prefix/chunk/missing-source behavior checks.</action>
  <acceptance_criteria>
    - Source assertion: `src/detail/host_file_path.hpp` has no `original_utf8` token.
    - Source assertion: `src/detail/host_file_path.cpp` has no returned or stored `original_utf8` member; a local variable may be named differently and must not be a member field.
    - Source assertion: `tests/unit/host_file_path_tests.cpp` no longer contains the test name `preserves caller UTF-8 bytes for diagnostics`.
    - Source assertion: `tests/unit/host_file_tests.cpp` contains no `.original_utf8` access.
    - Behavior assertion: strict UTF-8 conversion tests and host-file helper tests still pass.
  </acceptance_criteria>
  <verify>
    <automated>ctest --preset windows-msvc-debug-static --output-on-failure -R "host_file_path|host_file"</automated>
  </verify>
  <done>`detail::host_file_path::original_utf8` is removed, stale diagnostics comments/tests are gone, and focused host-file tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove stored backend_identity while preserving open-time backend seam</name>
  <files>src/archive.cpp, tests/unit/archive_reader_dispatch_policy_tests.cpp</files>
  <read_first>
    - .planning/phases/17.1-address-tech-debt-in-planning-v1-1-milestone-audit-md/17.1-SPEC.md
    - .planning/phases/17.1-address-tech-debt-in-planning-v1-1-milestone-audit-md/17.1-CONTEXT.md
    - .planning/phases/17.1-address-tech-debt-in-planning-v1-1-milestone-audit-md/17.1-RESEARCH.md
    - .planning/phases/17.1-address-tech-debt-in-planning-v1-1-milestone-audit-md/17.1-PATTERNS.md
    - .planning/phases/15-reader-backend-dispatch-cleanup/15-CONTEXT.md
    - .planning/phases/15-reader-backend-dispatch-cleanup/15-01-SUMMARY.md
    - .planning/phases/15-reader-backend-dispatch-cleanup/15-LEARNINGS.md
    - src/archive.cpp
    - tests/unit/archive_reader_dispatch_policy_tests.cpp
  </read_first>
  <behavior>
    - `archive_reader::open` still selects one backend table once at open time.
    - `entries`, `find`, `contains`, `extract`, `extract_bytes`, and `extract_entries` still avoid archive-family branching and backend identity references in public method bodies.
    - `archive_reader::state` no longer stores `backend_identity`.
    - Representative archive reader dispatch runtime coverage still passes.
  </behavior>
  <action>Per D-02, D-03, D-04, D-13, and D-15, remove only the `backend_identity` member from `archive_reader::state` in `src/archive.cpp`. Keep the file-local `reader_backend_identity` enum and `reader_backend_table(reader_backend_identity)` helper if needed for open-time table selection. Update the `make_opened_reader` state construction so it stores `&reader_backend_table(backend_identity)` but not `backend_identity` itself. Rewrite the `host_path` comment in `archive_reader::state` if it still refers to caller UTF-8 diagnostics; it must describe resolved host path reuse for payload reopens. Extend `tests/unit/archive_reader_dispatch_policy_tests.cpp` with a focused source-policy check that slices or inspects the `archive_reader::state` definition and fails if that state contains `backend_identity`; do not forbid the file-local enum or open-time table-selection helper outside state/public method bodies.</action>
  <acceptance_criteria>
    - Source assertion: `archive_reader::state` in `src/archive.cpp` contains no member declaration named `backend_identity`.
    - Source assertion: `src/archive.cpp` still contains `reader_backend_table(reader_backend_identity` or equivalent open-time table selector.
    - Source assertion: public method policy still forbids `backend_identity`, `reader_backend_identity::`, `tes3_bsa_backend`, `tes4_bsa_backend`, `ba2_gnrl_backend`, and `ba2_dx10_backend` inside `entries`, `find`, `contains`, `extract`, `extract_bytes`, and `extract_entries` bodies.
    - Behavior assertion: `archive_reader_dispatch` runtime and policy tests pass.
  </acceptance_criteria>
  <verify>
    <automated>ctest --preset windows-msvc-debug-static --output-on-failure -R archive_reader_dispatch</automated>
    <automated>ctest --preset windows-msvc-debug-static --output-on-failure -R "host_path_correctness_boundary|archive_reader_dispatch|host_file|host_file_path"</automated>
  </verify>
  <done>`archive_reader::state::backend_identity` is gone, public reader methods remain on the backend table seam, and focused dispatch/host-file tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public UTF-8 host path -> `detail::host_file_path` | Caller text is validated and converted once into a native path. |
| `archive_reader::open` -> later public reader methods | Archive family selection crosses into persistent reader state used by lookup/extraction calls. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17.1-03 | Tampering/DoS | `resolve_host_file_path` | mitigate | Preserve strict `MultiByteToWideChar(CP_UTF8, MB_ERR_INVALID_CHARS)` conversion and focused host-file tests while removing diagnostics-only state. |
| T-17.1-04 | Tampering/Reliability | `archive_reader::state` | mitigate | Remove duplicate stored backend identity and keep source-policy tests forbidding public-method family dispatch drift. |
</threat_model>

<verification>
Run focused Debug CTest after each task and at plan end:
- `ctest --preset windows-msvc-debug-static --output-on-failure -R "host_file_path|host_file"`
- `ctest --preset windows-msvc-debug-static --output-on-failure -R archive_reader_dispatch`
- `ctest --preset windows-msvc-debug-static --output-on-failure -R "host_path_correctness_boundary|archive_reader_dispatch|host_file|host_file_path"`
</verification>

<success_criteria>
- SPEC-2 and SPEC-3 are closed by removal, not by adding diagnostics consumers.
- D-01 through D-04 are implemented exactly.
- No public headers under `include/libbsa/` are modified.
- `.planning/v1.1-MILESTONE-AUDIT.md` is not modified.
- No files under `TES5Edit/` are modified.
</success_criteria>

<output>
After completion, create `.planning/phases/17.1-address-tech-debt-in-planning-v1-1-milestone-audit-md/17.1-02-SUMMARY.md`.
</output>
