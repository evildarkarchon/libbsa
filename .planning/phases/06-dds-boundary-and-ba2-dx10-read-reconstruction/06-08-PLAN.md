---
phase: 06-dds-boundary-and-ba2-dx10-read-reconstruction
plan: 08
type: execute
wave: 7
depends_on: [06-06]
files_modified:
  - tests/fixtures/generated/generate_ba2_dx10_fixtures.cpp
  - tests/fixtures/generated/archives/ba2_dx10_duplicate_canonical_path.ba2
  - tests/fixtures/generated/archives/ba2_dx10_unsupported_compression.ba2
  - tests/fixtures/generated/archives/ba2_dx10_malformed_manifest.json
  - tests/unit/ba2_dx10_malformed_tests.cpp
autonomous: true
gap_closure: true
requirements: [DDS-01, DDS-02, DDS-03, DDS-05]
must_haves:
  truths:
    - "Malformed DX10 fixtures fail closed with stable error codes and include duplicate canonical path and unsupported compression coverage."
    - "Malformed manifest tests fail loudly when an expected error-code string is unknown."
  artifacts:
    - path: "tests/fixtures/generated/generate_ba2_dx10_fixtures.cpp"
      provides: "Generated malformed duplicate canonical path and unsupported compression cases"
      contains: "ba2_dx10_duplicate_canonical_path"
    - path: "tests/unit/ba2_dx10_malformed_tests.cpp"
      provides: "Required-case assertions and strict manifest error-code conversion"
      contains: "FAIL("
  key_links:
    - from: "tests/fixtures/generated/generate_ba2_dx10_fixtures.cpp"
      to: "tests/unit/ba2_dx10_malformed_tests.cpp"
      via: "ba2_dx10_malformed_manifest.json case IDs"
      pattern: "ba2_dx10_duplicate_canonical_path|ba2_dx10_unsupported_compression"
---

<objective>
Close the malformed DX10 coverage gap by adding the missing generated malformed cases and making malformed manifest parsing fail loudly on unknown error-code strings.

Purpose: Prevent malformed coverage from silently regressing and ensure Phase 6 fail-closed behavior covers duplicate canonical paths and unsupported compression metadata.
Output: Updated DX10 malformed generator/manifest/byproducts and stricter malformed tests.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-dds-boundary-and-ba2-dx10-read-reconstruction/06-VERIFICATION.md
@.planning/phases/06-dds-boundary-and-ba2-dx10-read-reconstruction/06-06-SUMMARY.md
@tests/fixtures/generated/generate_ba2_dx10_fixtures.cpp
@tests/unit/ba2_dx10_malformed_tests.cpp

<interfaces>
Current malformed test helper:
```cpp
libbsa::error_code error_code_from_manifest(std::string_view value) {
  if (value == "format_error") {
    return libbsa::error_code::format_error;
  }
  if (value == "unsupported") {
    return libbsa::error_code::unsupported;
  }
  return libbsa::error_code::invalid_argument;
}
```

Replace the fallback with a test failure (`FAIL("unknown BA2 DX10 malformed expected_error: " &lt;&lt; value)`) or equivalent strict assertion before returning a dummy value only for compiler control-flow satisfaction.

Current required-case list omits:
```text
ba2_dx10_duplicate_canonical_path
ba2_dx10_unsupported_compression
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Require missing malformed DX10 cases and strict error strings</name>
  <files>tests/unit/ba2_dx10_malformed_tests.cpp</files>
  <read_first>
    - tests/unit/ba2_dx10_malformed_tests.cpp
    - .planning/phases/06-dds-boundary-and-ba2-dx10-read-reconstruction/06-VERIFICATION.md
  </read_first>
  <behavior>
    - Required malformed case assertions include `ba2_dx10_duplicate_canonical_path` and `ba2_dx10_unsupported_compression`.
    - Unknown `expected_error` strings in `ba2_dx10_malformed_manifest.json` fail the test instead of mapping to `invalid_argument`.
  </behavior>
  <action>
    Update `tests/unit/ba2_dx10_malformed_tests.cpp` before generator changes. Add `ba2_dx10_duplicate_canonical_path` and `ba2_dx10_unsupported_compression` to the required case list. Change `error_code_from_manifest` so recognized strings are exactly `format_error`, `unsupported`, and any other existing stable project error strings that are actually present in the manifest; for any unknown string, call Catch2 `FAIL("unknown BA2 DX10 malformed expected_error: " &lt;&lt; value)` and return a dummy value only after `FAIL` if the compiler requires it. Do not assert diagnostic message text per D-33.</action>
  <verify>
    <automated>cmake --build --preset windows-msvc-debug-static --target libbsa_tests &amp;&amp; ctest --preset windows-msvc-debug-static -R ba2_dx10_malformed --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `tests/unit/ba2_dx10_malformed_tests.cpp` contains `ba2_dx10_duplicate_canonical_path`.
    - `tests/unit/ba2_dx10_malformed_tests.cpp` contains `ba2_dx10_unsupported_compression`.
    - `tests/unit/ba2_dx10_malformed_tests.cpp` contains `unknown BA2 DX10 malformed expected_error` and no longer uses `return libbsa::error_code::invalid_argument;` as the unknown-string fallback.
  </acceptance_criteria>
  <done>Malformed tests fail until the generator/manifest supplies the two required cases, and unknown manifest error strings cannot pass silently.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Generate duplicate canonical path and unsupported compression fixtures</name>
  <files>tests/fixtures/generated/generate_ba2_dx10_fixtures.cpp, tests/fixtures/generated/archives/ba2_dx10_duplicate_canonical_path.ba2, tests/fixtures/generated/archives/ba2_dx10_unsupported_compression.ba2, tests/fixtures/generated/archives/ba2_dx10_malformed_manifest.json</files>
  <read_first>
    - tests/fixtures/generated/generate_ba2_dx10_fixtures.cpp
    - tests/fixtures/generated/archives/ba2_dx10_malformed_manifest.json
    - tests/unit/ba2_dx10_malformed_tests.cpp
    - .planning/phases/06-dds-boundary-and-ba2-dx10-read-reconstruction/06-VERIFICATION.md
  </read_first>
  <behavior>
    - Generated malformed manifest contains both missing case IDs with stable expected error codes.
    - Duplicate canonical path case fails during `archive_reader::open` with `error_code::format_error`.
    - Unsupported compression case fails closed with a stable error code matching the manifest and does not infer compression from paths.
  </behavior>
  <action>
    Extend `generate_ba2_dx10_fixtures.cpp` malformed generation. Add a duplicate canonical path fixture by building a DX10 archive with two names that canonicalize to the same key, such as `Textures/Generated/Duplicate.dds` and `textures/generated/duplicate.dds`, while preserving valid record/chunk layout; manifest case ID must be `ba2_dx10_duplicate_canonical_path`, phase `open`, expected_error `format_error`. Add an unsupported compression fixture that exercises unsupported DX10 compression routing: create a structurally valid Starfield v3 DX10 archive whose header `CompressionMethod` is an unsupported value (for example `99`) while containing a packed chunk that would require a compression route; manifest case ID must be `ba2_dx10_unsupported_compression`, phase `open` if detector rejects the method before parser work, expected_error `unsupported`. Regenerate malformed byproducts with the existing `generate_ba2_dx10_fixtures` target. Preserve legal provenance strings and do not use `TES5Edit/` as a fixture workspace per D-24 and D-34.</action>
  <verify>
    <automated>cmake --build --preset windows-msvc-debug-static --target generate_ba2_dx10_fixtures &amp;&amp; cmake --build --preset windows-msvc-debug-static --target libbsa_tests &amp;&amp; ctest --preset windows-msvc-debug-static -R ba2_dx10_malformed --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `tests/fixtures/generated/archives/ba2_dx10_malformed_manifest.json` contains `ba2_dx10_duplicate_canonical_path`.
    - `tests/fixtures/generated/archives/ba2_dx10_malformed_manifest.json` contains `ba2_dx10_unsupported_compression`.
    - `tests/fixtures/generated/archives/ba2_dx10_duplicate_canonical_path.ba2` exists.
    - `tests/fixtures/generated/archives/ba2_dx10_unsupported_compression.ba2` exists.
    - `tests/fixtures/generated/generate_ba2_dx10_fixtures.cpp` contains `CompressionMethod` or `compression_method` logic for the unsupported compression case.
  </acceptance_criteria>
  <done>Malformed generator, committed byproducts, and manifest cover duplicate canonical paths and unsupported compression routing with stable error-code expectations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Archive names -&gt; canonical lookup table | Untrusted DX10 archive names can collide after normalization. |
| Archive compression metadata -&gt; codec routing | Untrusted Starfield compression method fields can request unsupported codec behavior. |
| Manifest text -&gt; test expectations | Fixture manifest strings define expected errors and must not mask typos. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-08-01 | T | DX10 canonical path handling | mitigate | Add generated duplicate canonical path fixture and assert open-time `format_error`. |
| T-06-08-02 | T/D | DX10 compression routing | mitigate | Add unsupported compression fixture and assert stable fail-closed error instead of path/extension inference. |
| T-06-08-03 | R | malformed manifest tests | mitigate | Unknown expected error-code strings call `FAIL` so fixture/test mistakes are observable. |
</threat_model>

<verification>
Run `ctest --preset windows-msvc-debug-static -R ba2_dx10_malformed --output-on-failure`, then `ctest --preset windows-msvc-debug-static -R "ba2_dx10|public_include_boundary" --output-on-failure`, and `git -C TES5Edit status --short`.
</verification>

<success_criteria>
The verifier's malformed coverage truth is closed: required duplicate canonical path and unsupported compression cases exist in the generated manifest/byproducts, are asserted by test case ID, and unknown manifest error strings fail loudly.
</success_criteria>

<output>
After completion, create `.planning/phases/06-dds-boundary-and-ba2-dx10-read-reconstruction/06-08-SUMMARY.md`.
</output>
