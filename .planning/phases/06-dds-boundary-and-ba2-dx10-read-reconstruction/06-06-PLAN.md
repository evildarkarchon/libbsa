---
phase: 06-dds-boundary-and-ba2-dx10-read-reconstruction
plan: 06
type: execute
wave: 6
depends_on: [06-05]
files_modified:
  - tests/unit/ba2_dx10_malformed_tests.cpp
  - tests/unit/ba2_dx10_parser_tests.cpp
  - tests/unit/ba2_dx10_extraction_tests.cpp
  - src/formats/ba2/ba2_dx10_parser.cpp
  - src/formats/ba2/ba2_dx10_reader.cpp
autonomous: true
requirements: [DDS-01, DDS-02, DDS-03, DDS-04, DDS-05, DDS-06, DDS-07]
must_haves:
  truths:
    - "Malformed DX10 fixtures fail closed with stable error codes and no diagnostic-message assertions."
    - "Existing TES3, TES4-family BSA, BA2 GNRL, public include boundary, and TES5Edit read-only regressions remain green."
    - "Phase 6 acceptance commands validate all DDS requirements end-to-end."
  artifacts:
    - path: "tests/unit/ba2_dx10_malformed_tests.cpp"
      provides: "Manifest-driven malformed DX10 coverage"
      contains: "ba2_dx10_malformed"
    - path: "src/formats/ba2/ba2_dx10_parser.cpp"
      provides: "Final parser hardening for malformed open-time cases"
      contains: "format_error"
    - path: "src/formats/ba2/ba2_dx10_reader.cpp"
      provides: "Final extraction hardening for corrupt chunk cases"
      contains: "format_error"
  key_links:
    - from: "tests/unit/ba2_dx10_malformed_tests.cpp"
      to: "tests/fixtures/generated/archives/ba2_dx10_malformed_manifest.json"
      via: "manifest-driven error-code assertions"
      pattern: "ba2_dx10_malformed_manifest.json"
---

<objective>
Close Phase 6 malformed, regression, and final verification coverage.

Purpose: The final plan proves all DDS-01 through DDS-07 behaviors together and ensures parser/extractor security controls remain fail-closed under malformed texture archives.
Output: Malformed manifest tests, any required hardening fixes, full regression verification.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-dds-boundary-and-ba2-dx10-read-reconstruction/06-CONTEXT.md
@.planning/phases/06-dds-boundary-and-ba2-dx10-read-reconstruction/06-SPEC.md
@.planning/phases/06-dds-boundary-and-ba2-dx10-read-reconstruction/06-VALIDATION.md
@tests/unit/ba2_dx10_malformed_tests.cpp
@tests/unit/ba2_gnrl_reader_tests.cpp
@tests/unit/public_include_boundary_tests.cpp
</context>

<decision_coverage>
- D-16: Final malformed gates prove metadata-detectable DX10 layout problems fail during `archive_reader::open` with `format_error`.
- D-17: Final malformed gates prove corrupt compressed chunks and decoded-size mismatches fail with `format_error`.
- D-22: Final malformed tests cover the focused Phase 6 malformed fixture matrix from the generator manifest.
- D-26: Final public include boundary regression keeps DirectXTex, DXGI, Windows SDK, and private dependency types out of installed public headers.
- D-33: Malformed and regression tests assert stable `error_code` values only, never diagnostic message text.
- D-34: Final verification runs `git -C TES5Edit status --short` and requires an empty result to preserve the read-only reference boundary.
</decision_coverage>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add manifest-driven malformed DX10 regression tests</name>
  <files>tests/unit/ba2_dx10_malformed_tests.cpp</files>
  <read_first>
    - tests/unit/ba2_dx10_malformed_tests.cpp
    - tests/unit/ba2_gnrl_reader_tests.cpp
    - tests/fixtures/generated/generate_ba2_dx10_fixtures.cpp
    - .planning/phases/06-dds-boundary-and-ba2-dx10-read-reconstruction/06-SPEC.md
    - .planning/phases/06-dds-boundary-and-ba2-dx10-read-reconstruction/06-VALIDATION.md
  </read_first>
  <behavior>
    - Malformed manifest cases for truncated DX10 headers/records/chunks, invalid payload spans, inconsistent chunk sizes/counts, corrupt compressed chunks, decoded-size mismatch, mip/face coverage gaps, duplicate coverage, duplicate canonical path, and unsupported compression fail with expected stable `error_code` values.
    - Tests assert `error().code` only, never diagnostic message text, per D-33.
  </behavior>
  <action>Implement `ba2_dx10_malformed_tests.cpp` with `error_code_from_manifest` helper copied from BA2 GNRL tests. For cases marked `open`, call `archive_reader::open`; for cases marked `extract`, open first then call `extract_bytes` on the manifest path. Assert only stable `libbsa::error_code` values. This task may start RED. Acceptable RED outcomes are limited to `ctest -R ba2_dx10_malformed` exiting nonzero because a newly added assertion proves production accepts bad metadata, returns the wrong stable `error_code`, or has not yet implemented the malformed fixture path. Compile errors, missing manifest files, crashes/UB, fixture-generation failures, or tests asserting diagnostic message text are not acceptable RED outcomes and must be fixed inside Task 1 before committing. Commit as `test(06-06): add DX10 malformed manifest tests`.</action>
  <verify>
    <automated>ctest --preset windows-msvc-debug-static -R ba2_dx10_malformed --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "ba2_dx10_malformed_manifest.json" tests/unit/ba2_dx10_malformed_tests.cpp` finds manifest loading.
    - `grep -n "error_code_from_manifest" tests/unit/ba2_dx10_malformed_tests.cpp` finds stable error-code mapping.
    - `grep -n "message" tests/unit/ba2_dx10_malformed_tests.cpp` returns no diagnostic-message assertion.
    - `grep -n "format_error" tests/unit/ba2_dx10_malformed_tests.cpp` finds expected malformed code coverage.
    - If the RED run fails, failure output is limited to expected Catch2 assertion failures for accepted-bad-metadata or wrong-stable-code cases; build, manifest-load, crash, and diagnostic-message failures are fixed before Task 1 is considered done.
  </acceptance_criteria>
  <done>Malformed DX10 tests exercise every SPEC-required parser/extractor fail-closed case.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harden parser/extractor until malformed and regression suites pass</name>
  <files>src/formats/ba2/ba2_dx10_parser.cpp, src/formats/ba2/ba2_dx10_reader.cpp, tests/unit/ba2_dx10_parser_tests.cpp, tests/unit/ba2_dx10_extraction_tests.cpp</files>
  <read_first>
    - src/formats/ba2/ba2_dx10_parser.cpp
    - src/formats/ba2/ba2_dx10_reader.cpp
    - tests/unit/ba2_dx10_malformed_tests.cpp
    - tests/unit/ba2_dx10_parser_tests.cpp
    - tests/unit/ba2_dx10_extraction_tests.cpp
    - .planning/phases/06-dds-boundary-and-ba2-dx10-read-reconstruction/06-CONTEXT.md
    - .planning/phases/06-dds-boundary-and-ba2-dx10-read-reconstruction/06-RESEARCH.md
  </read_first>
  <action>Fix only concrete failures from Task 1 and DX10 regression runs. Required hardening targets are concrete: checked record/chunk count arithmetic, `chunk_header_size == 24`, archive-absolute payload spans within archive size, filename table before first chunk payload, duplicate canonical paths, invalid archive paths, mip/face coverage gaps and duplicates, raw chunk short reads, corrupt deflate/raw-LZ4 data, exact decoded-size mismatch, and unsupported LZ4-frame routing. Preserve D-33 by keeping tests on `error_code` only. Do not add lenient recovery, compatibility warnings, or writer behavior. Commit as `fix(06-06): harden BA2 DX10 malformed handling` if production changes are needed; if tests already pass, document that no production hardening was required in SUMMARY.</action>
  <verify>
    <automated>ctest --preset windows-msvc-debug-static -R "ba2_dx10|public_include_boundary" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `ctest --preset windows-msvc-debug-static -R "ba2_dx10|public_include_boundary" --output-on-failure` exits 0.
    - `grep -n "format_error" src/formats/ba2/ba2_dx10_parser.cpp` finds malformed parser rejection paths.
    - `grep -n "decompress_payload_exact" src/formats/ba2/ba2_dx10_reader.cpp` still finds exact-size decompression.
    - `grep -n "message" tests/unit/ba2_dx10_malformed_tests.cpp` returns no diagnostic-message assertion.
  </acceptance_criteria>
  <done>DX10 malformed and focused public-boundary suites pass with fail-closed parser/extractor behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Run final Phase 6 regression gates</name>
  <files>tests/unit/ba2_dx10_malformed_tests.cpp, tests/unit/ba2_dx10_parser_tests.cpp, tests/unit/ba2_dx10_extraction_tests.cpp</files>
  <read_first>
    - tests/unit/ba2_dx10_malformed_tests.cpp
    - tests/unit/ba2_dx10_parser_tests.cpp
    - tests/unit/ba2_dx10_extraction_tests.cpp
    - .planning/phases/06-dds-boundary-and-ba2-dx10-read-reconstruction/06-VALIDATION.md
    - .planning/phases/06-dds-boundary-and-ba2-dx10-read-reconstruction/06-SPEC.md
  </read_first>
  <action>Run the final automated gates and record outputs in the summary: `ctest --preset windows-msvc-debug-static -R "ba2_dx10|public_include_boundary" --output-on-failure`, `ctest --preset windows-msvc-debug-static --output-on-failure`, and `git -C TES5Edit status --short`. If shared preset exists and is configured, also run `ctest --preset windows-msvc-debug-shared --output-on-failure`; if not configured, document the preset absence rather than inventing a new preset. Do not modify TES5Edit. Do not update schema or run schema push tasks.</action>
  <verify>
    <automated>ctest --preset windows-msvc-debug-static --output-on-failure</automated>
    <automated>git -C TES5Edit status --short</automated>
  </verify>
  <acceptance_criteria>
    - Full static preset CTest exits 0.
    - `git -C TES5Edit status --short` produces no output.
    - `ctest --preset windows-msvc-debug-static -R "ba2_dx10|public_include_boundary" --output-on-failure` exits 0.
    - SUMMARY records whether shared preset was run or absent.
  </acceptance_criteria>
  <done>All Phase 6 automated acceptance gates pass and TES5Edit remains read-only/clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| malformed archive bytes → parser/extractor | Intentionally invalid fixtures probe fail-closed handling. |
| public include boundary → consumers | Regression gate ensures private dependencies remain private. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-19 | Tampering/DoS | malformed DX10 metadata | mitigate | Manifest tests cover truncation, spans, counts, gaps, duplicates, and impossible coverage. |
| T-06-20 | DoS/Tampering | corrupt compressed chunks | mitigate | Extraction tests assert corrupt streams and exact-size mismatches return `format_error`. |
| T-06-21 | Information Disclosure | public dependency leakage | mitigate | Final regression includes `public_include_boundary`. |
| T-06-22 | Tampering | TES5Edit boundary | mitigate | Final gate checks `git -C TES5Edit status --short` is empty. |
</threat_model>

<verification>
Run `ctest --preset windows-msvc-debug-static -R "ba2_dx10|public_include_boundary" --output-on-failure`, `ctest --preset windows-msvc-debug-static --output-on-failure`, and `git -C TES5Edit status --short`.
</verification>

<success_criteria>
- All DDS-01 through DDS-07 tests pass.
- Malformed fixtures fail with stable error codes.
- Existing archive-family and public-boundary regressions pass.
- TES5Edit status is empty.
</success_criteria>

<output>
After completion, create `.planning/phases/06-dds-boundary-and-ba2-dx10-read-reconstruction/06-06-SUMMARY.md`.
</output>
