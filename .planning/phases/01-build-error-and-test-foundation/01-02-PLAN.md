---
phase: 01-build-error-and-test-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - include/libbsa/result.hpp
  - tests/foundation_tests.cpp
  - CMakeLists.txt
autonomous: true
requirements:
  - FND-03
user_setup: []
must_haves:
  truths:
    - "Consumer can represent success values and structured failures through a C++20-compatible libbsa-owned API."
    - "Consumer can use `result<void>` for operations that can fail without returning a value."
    - "Maintainer can run unit tests proving representative error categories and propagation behavior."
  artifacts:
    - path: "include/libbsa/result.hpp"
      provides: "Public `libbsa::result<T>`, `libbsa::result<void>`, categorized error codes, and helper constructors"
      contains: "class result"
    - path: "tests/foundation_tests.cpp"
      provides: "Catch2 unit coverage for result/error behavior"
      contains: "TEST_CASE"
    - path: "CMakeLists.txt"
      provides: "Foundation test target linked to libbsa and Catch2"
      contains: "libbsa_foundation_tests"
  key_links:
    - from: "tests/foundation_tests.cpp"
      to: "include/libbsa/result.hpp"
      via: "#include <libbsa/result.hpp>"
      pattern: "#include <libbsa/result.hpp>"
    - from: "CMakeLists.txt"
      to: "tests/foundation_tests.cpp"
      via: "add_executable(libbsa_foundation_tests ...)"
      pattern: "add_executable\(libbsa_foundation_tests"
---

<objective>
Implement and test the Phase 1 structured result/error API without exposing C++23 `std::expected`, parser-only context, monadic helpers, dependency types, or global state.

Purpose: Later archive parsers and codecs need a stable, tested failure surface before they start handling malformed bytes and decompression errors.
Output: Complete `include/libbsa/result.hpp`, `tests/foundation_tests.cpp`, and CMake registration for the foundation unit tests.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-build-error-and-test-foundation/01-SPEC.md
@.planning/phases/01-build-error-and-test-foundation/01-CONTEXT.md
@.planning/phases/01-build-error-and-test-foundation/01-RESEARCH.md
@.planning/phases/01-build-error-and-test-foundation/01-PATTERNS.md
@.planning/phases/01-build-error-and-test-foundation/01-01-SUMMARY.md

<interfaces>
From Plan 01, `include/libbsa/result.hpp` must already contain the libbsa namespace, `error_code`, `error`, `result<T>` forward declaration, and `result<void>` forward declaration. Complete those declarations in place.

Expected public API surface for this plan:
```cpp
namespace libbsa {
enum class error_code { unsupported_format, malformed_archive, io_failure, decompression_failure };
struct error { error_code code; std::string message; };
template <class T> class result { /* has_value(), value(), error() */ };
template <> class result<void> { /* has_value(), value(), error() */ };
template <class T> result<T> success(T value);
result<void> success();
template <class T> result<T> failure(error err);
result<void> failure(error err);
} // namespace libbsa
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify result/error behavior with failing Catch2 tests</name>
  <files>tests/foundation_tests.cpp, CMakeLists.txt</files>
  <read_first>tests/foundation_tests.cpp if it exists, CMakeLists.txt, include/libbsa/result.hpp, .planning/phases/01-build-error-and-test-foundation/01-CONTEXT.md, .planning/phases/01-build-error-and-test-foundation/01-SPEC.md, .planning/phases/01-build-error-and-test-foundation/01-PATTERNS.md</read_first>
  <behavior>
    - Test 1: `libbsa::success(42)` returns `result<int>` where `has_value()` is true and `value()` is 42.
    - Test 2: `libbsa::failure<int>({error_code::unsupported_format, "unsupported archive format"})` returns no value and exposes the same code/message through `error()`.
    - Test 3: `libbsa::success()` returns successful `result<void>` and `value()` is callable.
    - Test 4: `libbsa::failure<void>({error_code::io_failure, "read failed"})` exposes categorized error details.
    - Test 5: A helper function returning `result<int>` can propagate a `malformed_archive` failure by returning the failed result unchanged.
    - Test 6: Representative codes include `unsupported_format`, `malformed_archive`, `io_failure`, and `decompression_failure`.
    - This is a TDD red step: the expected outcome after writing the tests is a failing build or failing unit test until Task 2 implements the API.
  </behavior>
  <action>Create `tests/foundation_tests.cpp` using Catch2 and `#include <libbsa/result.hpp>`. Add the exact behavior tests above per D-08, keeping test labels in the Catch2 test names/tags as `[unit]`. Update `CMakeLists.txt` inside `if(BUILD_TESTING)` to include `CTest`, add `list(APPEND CMAKE_MODULE_PATH ${Catch2_DIR}/../..)` only if needed by the installed Catch2 package, include `Catch`, define `add_executable(libbsa_foundation_tests tests/foundation_tests.cpp)`, link `libbsa::libbsa` and `Catch2::Catch2WithMain`, and register with `catch_discover_tests(libbsa_foundation_tests PROPERTIES LABELS "unit")` per D-09 and D-10. Run the expected-failure command below before implementing the missing API behavior; the command itself must exit successfully only when the build or unit tests fail as expected for this red step.</action>
  <verify>
    <automated>$failed = $false; cmake --build build/windows-msvc-vcpkg --config Debug; if ($LASTEXITCODE -ne 0) { $failed = $true }; if (-not $failed) { ctest --test-dir build/windows-msvc-vcpkg --output-on-failure -L unit; if ($LASTEXITCODE -ne 0) { $failed = $true } }; if (-not $failed) { throw "Expected the result/error tests to fail before Task 2 implements the API" }; "Expected TDD red-step failure observed"</automated>
  </verify>
  <acceptance_criteria>`tests/foundation_tests.cpp` contains at least six `TEST_CASE` sections or sections covering the six behaviors; it includes `<libbsa/result.hpp>` and does not include any libdeflate, LZ4, DirectXTex, Windows, Delphi, or TES5Edit headers; `CMakeLists.txt` contains `libbsa_foundation_tests`, `Catch2::Catch2WithMain`, and `catch_discover_tests`; the automated verify command prints `Expected TDD red-step failure observed` before Task 2 implementation.</acceptance_criteria>
  <done>The result/error behavior contract is encoded as Catch2 tests and registered with CTest.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement C++20 result and error API</name>
  <files>include/libbsa/result.hpp</files>
  <read_first>include/libbsa/result.hpp, tests/foundation_tests.cpp, AGENTS.md, .planning/phases/01-build-error-and-test-foundation/01-CONTEXT.md, .planning/phases/01-build-error-and-test-foundation/01-SPEC.md</read_first>
  <behavior>
    - `result<T>` stores either `T` or `libbsa::error` and exposes `bool has_value() const noexcept`.
    - `result<T>::value()` returns `T&`/`const T&` for successful results and throws `std::logic_error` for programmer misuse on failures.
    - `result<T>::error()` returns `const libbsa::error&` for failures and throws `std::logic_error` for programmer misuse on successes.
    - `result<void>` supports `has_value()`, `value()`, and `error()` with the same semantics.
    - Helper constructors `success(T)`, `success()`, `failure<T>(error)`, and `failure(error)`/`failure<void>(error)` create the expected specialization without exposing `std::expected`.
  </behavior>
  <action>Complete `include/libbsa/result.hpp` per D-05 through D-08 using only C++20 standard library headers such as `<stdexcept>`, `<string>`, `<type_traits>`, `<utility>`, and `<variant>` or an equivalent local representation. Keep `error` as category/code plus short `std::string message` only; do not add offsets, archive paths, nested causes, source locations, `and_then`, or `transform`. Add Doxygen comments for `error_code`, `error`, `result<T>`, `result<void>`, `has_value()`, `value()`, `error()`, `success`, and `failure`. Ensure the public header has no dependency/platform/TES5Edit includes and no global mutable state. Run the tests from Task 1 and make them pass.</action>
  <verify>
    <automated>cmake --build build/windows-msvc-vcpkg --config Debug; ctest --test-dir build/windows-msvc-vcpkg --output-on-failure -L unit</automated>
  </verify>
  <acceptance_criteria>`include/libbsa/result.hpp` contains `class result`, `result<void>`, `has_value`, `value`, `error`, `success`, and `failure`; `include/libbsa/result.hpp` contains no `std::expected`, `and_then`, `transform`, `source_location`, `offset`, `archive_path`, `libdeflate`, `LZ4`, `DirectX`, `Windows.h`, or `TES5Edit`; `ctest ... -L unit` passes.</acceptance_criteria>
  <done>The local structured result/error API passes unit tests and remains C++20/public-header isolated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Consumer code → public result API | Consumer observes success/failure state through public headers. |
| Future archive parsers → structured errors | Later untrusted binary input handling will rely on these categories instead of ad hoc failures. |
| Public headers → consumer compilation unit | Public API must not leak implementation dependencies, platform headers, or reference-code types. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Denial of Service | `result<T>::value()` / `error()` misuse | mitigate | Treat wrong observer usage as programmer precondition violation by throwing `std::logic_error`; tests cover correct state checks before access. |
| T-01-02-02 | Tampering | Future parser failure handling | mitigate | Lock `unsupported_format`, `malformed_archive`, `io_failure`, and `decompression_failure` categories with tests so malformed archive paths can return structured errors. |
| T-01-02-03 | Information Disclosure | `include/libbsa/result.hpp` | mitigate | Keep header limited to C++20 standard library/libbsa-owned types; grep acceptance blocks dependency, platform, Delphi, and TES5Edit tokens. |
| T-01-02-04 | Elevation of Privilege | Result API | accept | This plan introduces no parser, decompressor, file I/O, network, process execution, or privileged operation; no high-severity ASVS L1 threat applies. |
</threat_model>

<verification>
Run `cmake --build build/windows-msvc-vcpkg --config Debug` and `ctest --test-dir build/windows-msvc-vcpkg --output-on-failure -L unit`. Confirm public header grep acceptance criteria remain true.
</verification>

<success_criteria>
- `libbsa::result<T>` and `libbsa::result<void>` support success, failure, value access, error access, and representative propagation.
- Unit tests pass through CTest/Catch2.
- Public result header does not expose C++23 `std::expected`, implementation dependencies, TES5Edit, parser-only context, monadic helpers, or global mutable state.
</success_criteria>

<output>
After completion, create `.planning/phases/01-build-error-and-test-foundation/01-02-SUMMARY.md`.
</output>
