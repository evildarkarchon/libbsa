---
phase: 01-build-error-and-test-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - CMakeLists.txt
  - tests/public_header_smoke.cpp
  - README.md
autonomous: true
requirements:
  - FND-02
  - FND-04
  - VAL-01
user_setup: []
must_haves:
  truths:
    - "D-09: Maintainer can run one initial Catch2 foundation test executable rather than many premature domain executables."
    - "D-10: Maintainer can rely on Catch2 CMake discovery through CTest where available."
    - "D-11: Maintainer can run CTest for the foundation suite with `unit` and `smoke` labels while reserving fixture, roundtrip, compat, and slow labels for later phases."
    - "D-12: Consumer-style code can include the public foundation header through a compile-only smoke test without private dependency/platform/reference headers."
  artifacts:
    - path: "tests/public_header_smoke.cpp"
      provides: "Consumer-style public header isolation translation unit"
      contains: "#include <libbsa/result.hpp>"
    - path: "CMakeLists.txt"
      provides: "CTest registration for unit and smoke tests"
      contains: "libbsa_public_header_smoke"
    - path: "README.md"
      provides: "Documented CTest commands and label policy"
      contains: "ctest"
  key_links:
    - from: "tests/public_header_smoke.cpp"
      to: "include/libbsa/result.hpp"
      via: "consumer include only"
      pattern: "#include <libbsa/result.hpp>"
    - from: "CMakeLists.txt"
      to: "tests/public_header_smoke.cpp"
      via: "smoke executable target and add_test"
      pattern: "libbsa_public_header_smoke"
    - from: "CTest"
      to: "foundation tests"
      via: "unit and smoke labels"
      pattern: "LABELS.*(unit|smoke)"
---

<objective>
Finish the Phase 1 CTest foundation by adding public-header isolation smoke coverage, label hygiene, and final verification that no global mutable state, UI/tooling dependency, or TES5Edit source inclusion crossed into the public library foundation.

Purpose: Consumers need confidence that public headers are clean, and maintainers need a test structure that later phases can extend for parser, codec, fixture, and round-trip behavior.
Output: `tests/public_header_smoke.cpp`, final CTest/CMake wiring, and README test-label guidance.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-build-error-and-test-foundation/01-SPEC.md
@.planning/phases/01-build-error-and-test-foundation/01-CONTEXT.md
@.planning/phases/01-build-error-and-test-foundation/01-RESEARCH.md
@.planning/phases/01-build-error-and-test-foundation/01-VALIDATION.md
@.planning/phases/01-build-error-and-test-foundation/01-PATTERNS.md
@.planning/phases/01-build-error-and-test-foundation/01-01-SUMMARY.md
@.planning/phases/01-build-error-and-test-foundation/01-02-SUMMARY.md

<interfaces>
Use the public API completed by Plan 02 exclusively from the smoke translation unit:

```cpp
#include <libbsa/result.hpp>

int main() {
    libbsa::result<void> ok = libbsa::success();
    return ok.has_value() ? 0 : 1;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add public-header isolation smoke target</name>
  <files>tests/public_header_smoke.cpp, CMakeLists.txt</files>
  <read_first>tests/public_header_smoke.cpp if it exists, CMakeLists.txt, include/libbsa/result.hpp, .planning/phases/01-build-error-and-test-foundation/01-CONTEXT.md, .planning/phases/01-build-error-and-test-foundation/01-SPEC.md, .planning/phases/01-build-error-and-test-foundation/01-PATTERNS.md</read_first>
  <action>Create `tests/public_header_smoke.cpp` as a consumer-style translation unit that includes only `<libbsa/result.hpp>` plus standard C++ headers if absolutely needed, constructs a `libbsa::result<void>` through `libbsa::success()`, checks `has_value()`, and returns 0 on success. Do not include private headers, libdeflate, LZ4, DirectXTex, Windows SDK, Delphi, TES5Edit, or test framework headers. Update `CMakeLists.txt` with `add_executable(libbsa_public_header_smoke tests/public_header_smoke.cpp)`, link only `libbsa::libbsa`, register `add_test(NAME libbsa.public_header_smoke COMMAND libbsa_public_header_smoke)`, and set `LABELS smoke` per D-12. Keep the existing `libbsa_foundation_tests` registration and ensure it has `unit` label per D-11.</action>
  <verify>
    <automated>cmake --build build/windows-msvc-vcpkg --config Debug; ctest --test-dir build/windows-msvc-vcpkg --output-on-failure -L smoke</automated>
  </verify>
  <acceptance_criteria>`tests/public_header_smoke.cpp` contains exactly one project include: `<libbsa/result.hpp>`; it contains no `libdeflate`, `lz4`, `LZ4`, `DirectX`, `Windows`, `TES5Edit`, `Catch2`, or `src/`; `CMakeLists.txt` contains `libbsa_public_header_smoke`, `add_test(NAME libbsa.public_header_smoke`, and `LABELS smoke`; `ctest ... -L smoke` passes.</acceptance_criteria>
  <done>The public-header isolation smoke test compiles and runs through CTest.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize CTest labels, foundation docs, and boundary gates</name>
  <files>CMakeLists.txt, README.md</files>
  <read_first>CMakeLists.txt, README.md, .planning/phases/01-build-error-and-test-foundation/01-VALIDATION.md, .planning/phases/01-build-error-and-test-foundation/01-CONTEXT.md, .planning/phases/01-build-error-and-test-foundation/01-SPEC.md, AGENTS.md</read_first>
  <action>Finalize CTest hygiene so Phase 1 has one Catch2 executable labeled `unit` and one public-header smoke executable labeled `smoke` per D-09 through D-12. Update `README.md` to document `ctest --test-dir build/windows-msvc-vcpkg --output-on-failure`, `ctest --test-dir build/windows-msvc-vcpkg --output-on-failure -L unit`, and `ctest --test-dir build/windows-msvc-vcpkg --output-on-failure -L smoke`, and state that `unit` and `smoke` are the only Phase 1 labels while `fixture`, `roundtrip`, `compat`, and `slow` are reserved until those test types exist per D-11. Add or preserve grep-friendly text that public headers must not expose libdeflate, LZ4, DirectXTex, Windows SDK, Delphi, UI/tooling, or TES5Edit types, and that archive parsing/hash/compression/DDS/writer behavior is outside this phase. Do not add any archive parser, hash algorithm, compression adapter, DDS wrapper, fixture archive, CLI, GUI, or global singleton configuration.</action>
  <verify>
    <automated>cmake --build build/windows-msvc-vcpkg --config Debug; ctest --test-dir build/windows-msvc-vcpkg --output-on-failure; pwsh -NoProfile -Command "if ((git status --short TES5Edit).Length -ne 0) { exit 1 }; if ((Select-String -LiteralPath 'CMakeLists.txt' -Pattern 'GLOB' -SimpleMatch).Count -ne 0) { exit 1 }; if ((Select-String -LiteralPath 'include/libbsa/result.hpp' -Pattern 'std::expected|libdeflate|LZ4|DirectX|Windows.h|TES5Edit' -AllMatches).Count -ne 0) { exit 1 }"</automated>
  </verify>
  <acceptance_criteria>Full CTest passes; `README.md` contains `-L unit`, `-L smoke`, `fixture`, `roundtrip`, `compat`, and `slow`; `git status --short TES5Edit` is empty; `CMakeLists.txt` contains no `GLOB`; `include/libbsa/result.hpp` contains none of `std::expected`, `libdeflate`, `LZ4`, `DirectX`, `Windows.h`, or `TES5Edit`.</acceptance_criteria>
  <done>The test foundation satisfies FND-02, FND-04, and VAL-01 with automated unit/smoke verification and clean public-header boundaries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Consumer translation unit → installed public headers | Smoke target proves consumers can compile against public API without private dependency exposure. |
| Test labels → maintainer validation workflow | CTest label selection controls which checks maintainers run during later phases. |
| Repository reference submodule → git/build state | Final gates verify TES5Edit remains unchanged and outside source discovery. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | Information Disclosure | `tests/public_header_smoke.cpp` / public headers | mitigate | Compile a consumer-style smoke target including only `<libbsa/result.hpp>` and grep-block private dependency/platform/reference tokens. |
| T-01-03-02 | Tampering | CTest validation graph | mitigate | Require `unit` and `smoke` labels and document reserved later labels so future parser/codec tests attach to a known structure. |
| T-01-03-03 | Tampering | `TES5Edit/` submodule | mitigate | Final automated gate checks `git status --short TES5Edit` is empty and `CMakeLists.txt` contains no source globbing. |
| T-01-03-04 | Denial of Service | Untrusted archive parser scope creep | mitigate | Acceptance criteria and README state archive parsing, compression adapters, DDS wrappers, fixtures, and writers are outside Phase 1; no untrusted archive bytes are parsed in this plan. |
| T-01-03-05 | Elevation of Privilege | Foundation tests | accept | Tests compile and run local library code only; no privileged file mutation beyond build artifacts, no network service, and no high-severity ASVS L1 threat applies. |
</threat_model>

<verification>
Run `cmake --build build/windows-msvc-vcpkg --config Debug`, `ctest --test-dir build/windows-msvc-vcpkg --output-on-failure`, and the final PowerShell grep/git gate from Task 2.
</verification>

<success_criteria>
- `ctest --test-dir build/windows-msvc-vcpkg --output-on-failure`, `-L unit`, and `-L smoke` all pass.
- Public-header smoke target compiles as consumer code without private dependency/platform/reference includes.
- CMake source lists remain explicit and TES5Edit remains unmodified/uncompiled.
- The foundation test structure is ready for later hash, compression, header parsing, record serialization, fixture, and round-trip tests.
</success_criteria>

<output>
After completion, create `.planning/phases/01-build-error-and-test-foundation/01-03-SUMMARY.md`.
</output>
