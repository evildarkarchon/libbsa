---
phase: 09-bsa-writers
plan: 02
type: tdd
wave: 2
depends_on: [09-01]
files_modified:
  - src/bsa_writer.cpp
  - tests/bsa_writer_tests.cpp
autonomous: true
requirements: [WRT-01]
must_haves:
  truths:
    - "Consumer can write native TES4-family BSA bytes for v103, v104, and v105 from memory entries."
    - "Written TES4-family archives reopen through open_bsa with expected paths, counts, hashes, offsets, and raw payload bytes."
    - "BSA plan preview exposes native folder/file table regions and archive-absolute offsets."
  artifacts:
    - path: "src/bsa_writer.cpp"
      provides: "TES4-family native layout planner and finalizer for raw memory entries"
      contains: "BSA\0"
    - path: "tests/bsa_writer_tests.cpp"
      provides: "RED/GREEN tests for v103/v104/v105 raw layout read-after-write"
      contains: "TES4-family"
  key_links:
    - from: "src/bsa_writer.cpp"
      to: "src/hash.cpp"
      via: "TES4 folder/file hash emission"
      pattern: "hash_tes4_path"
    - from: "tests/bsa_writer_tests.cpp"
      to: "open_bsa / extract_bsa_entry"
      via: "generated memory_source read-back"
      pattern: "open_bsa"
---

<objective>
TDD native TES4-family BSA raw layout and read-after-write behavior.

Purpose: WRT-01 requires production TES4 v103/v104/v105 BSA bytes, not the Phase 8 `LBSW` harness.
Output: Passing tests and implementation for native TES4-family table serialization, raw payload emission, and streaming finalization.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-bsa-writers/09-CONTEXT.md
@.planning/phases/09-bsa-writers/09-SPEC.md
@.planning/phases/09-bsa-writers/09-RESEARCH.md
@.planning/phases/09-bsa-writers/09-PATTERNS.md
@include/libbsa/bsa_writer.hpp
@src/bsa_writer.cpp
@src/bsa_reader.cpp
@src/bsa_reader.hpp
@src/hash.cpp
@tests/bsa_reader_tests.cpp
@tests/bsa_writer_tests.cpp

<interfaces>
Implement against Plan 01 public contracts. TES4-family native rules: magic `BSA\0`, versions `0x67`, `0x68`, `0x69`, 36-byte header, v103/v104 16-byte folder records, v105 24-byte folder records, folder/file hash sorting per D-14, folder blocks, global null-terminated file-name table, then payload bytes.
</interfaces>
</context>

<feature>
  <name>TES4-family raw BSA writer layout</name>
  <files>src/bsa_writer.cpp, tests/bsa_writer_tests.cpp</files>
  <behavior>
    - Given memory entries for `meshes/a.nif`, `textures/t.dds`, and `sounds/s.wav`, planning `oblivion_v103`, `fo3_fnv_skyrim_le_v104`, and `skyrim_se_ae_v105` succeeds and exposes native table/data regions.
    - Finalizing each plan to `memory_sink` emits bytes whose first fields are `BSA\0`, target version `0x67/0x68/0x69`, folders offset `36`, correct folder/file counts, and computed TES4-family header `FileFlags` derived from entry path extensions.
    - Reopening the bytes with `open_bsa` returns normalized paths and metadata with TES4 hashes and archive-absolute payload offsets matching the plan.
    - Extracting every entry returns the original raw payload bytes.
  </behavior>
  <implementation>After RED tests fail against placeholders, implement normalization, folder/file split, TES4 hash sorting, reference-style computed header `FileFlags` from path extensions, checked TES4 table sizing, raw native payload regions, table byte serialization, and `finalize_bsa_write` streaming native chunks through `byte_sink` without recomputing layout.</implementation>
</feature>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1 RED: Add failing TES4-family raw layout tests</name>
  <files>tests/bsa_writer_tests.cpp</files>
  <read_first>
    - tests/bsa_writer_tests.cpp
    - tests/bsa_reader_tests.cpp
    - src/bsa_reader.cpp
    - .planning/phases/09-bsa-writers/09-SPEC.md requirements 1, 3, 6, 7
    - .planning/phases/09-bsa-writers/09-RESEARCH.md TES4 layout notes
  </read_first>
  <action>Add tests named `plans and finalizes TES4-family raw BSA archives for every required version`, `exposes native TES4 table regions and offsets before finalization`, `computes TES4 FileFlags from entry extensions`, and `orders TES4 folders and files by reference hashes independent of input order`. Use public `plan_bsa_write`, `finalize_bsa_write`, `memory_sink`, `memory_source`, `open_bsa`, and `extract_bsa_entry`. Assert byte-level magic/version/counts/FileFlags plus metadata-level folder/file hashes, offsets, stored sizes, normalized paths, and extracted payloads. Commit RED as `test(09-02): add failing TES4-family BSA writer layout tests` after confirming failure is due to placeholder or missing implementation, not syntax.</action>
  <verify>
    <automated>cmake --build build/windows-vs2026-vcpkg --config Debug --target libbsa_bsa_writer_tests &amp;&amp; ctest --test-dir build/windows-vs2026-vcpkg --output-on-failure -C Debug -R libbsa_bsa_writer_tests</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "TES4-family raw BSA|native TES4 table regions|computes TES4 FileFlags|orders TES4 folders and files" tests/bsa_writer_tests.cpp` finds the RED cases.
    - RED run fails before implementation because native BSA writer behavior is absent.
  </acceptance_criteria>
  <done>Failing behavior tests define exact TES4-family native layout expectations before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 GREEN: Implement TES4-family raw native writer</name>
  <files>src/bsa_writer.cpp</files>
  <read_first>
    - src/bsa_writer.cpp
    - src/bsa_reader.cpp
    - src/bsa_reader.hpp
    - src/hash.cpp
    - include/libbsa/bsa_writer.hpp
    - tests/bsa_writer_tests.cpp
  </read_first>
  <action>Replace TES4 memory-entry planning placeholder with implementation for `oblivion_v103`, `fo3_fnv_skyrim_le_v104`, and `skyrim_se_ae_v105`. Normalize archive paths with `normalize_archive_path`, reject duplicates with `error_code::malformed_archive`, split normalized paths into folder and filename, compute folder and file hashes using existing TES4 detail hash routines (move declarations only if needed), sort by directory hash then file hash per D-14, build only folders implied by entries per D-16, compute reference-style header `FileFlags` from known file extensions and expose the computed value in `bsa_write_plan`, compute header/table/name/payload offsets using checked add/multiply and uint32 fit checks, and create plan-owned native raw payloads. Implement native finalization for these plans: write header, folder records, folder blocks, global file-name table, then payload regions to the caller-owned `byte_sink`, returning the first sink error unchanged. Do not add public caller overrides for `FileFlags`; do not call or expose `finalize_archive_write` for production BSA bytes; do not emit `LBSW` per D-04.</action>
  <verify>
    <automated>cmake --build build/windows-vs2026-vcpkg --config Debug --target libbsa_bsa_writer_tests &amp;&amp; ctest --test-dir build/windows-vs2026-vcpkg --output-on-failure -C Debug -R libbsa_bsa_writer_tests</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "BSA\\0|0x67|0x68|0x69|folder_record|file_name|hash_tes4|archive_default_compressed" src/bsa_writer.cpp` finds native TES4 implementation markers.
    - `rg -n "LBSW|finalize_archive_write" src/bsa_writer.cpp` returns no production BSA emission path usage.
    - Focused BSA writer tests pass after GREEN.
  </acceptance_criteria>
  <done>TES4 v103/v104/v105 raw archives are planned, finalized, reopened, inspected, and extracted successfully.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| caller memory entries -> TES4 planner | Untrusted virtual paths and payload bytes become native BSA tables. |
| TES4 plan -> byte_sink | Planned native chunks are written to caller-owned output. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 | Tampering / DoS | TES4 layout arithmetic | mitigate | Checked add/multiply and uint32 fit checks for counts, table lengths, names, offsets, and stored sizes before finalization. |
| T-09-05 | Tampering | archive path handling | mitigate | Use `normalize_archive_path`; never treat archive paths as host `std::filesystem::path`. |
| T-09-06 | Repudiation | sink finalization | mitigate | Return first `byte_sink::write` failure unchanged; no partial-success reporting. |
</threat_model>

<verification>
Focused: `ctest --test-dir build/windows-vs2026-vcpkg --output-on-failure -C Debug -R libbsa_bsa_writer_tests`
</verification>

<success_criteria>
- RED and GREEN commits exist for `09-02`.
- TES4 raw native writer behavior passes read-after-write tests for v103/v104/v105.
- No public or production path treats `LBSW` as a real BSA archive.
</success_criteria>

<output>
After completion, create `.planning/phases/09-bsa-writers/09-02-SUMMARY.md`.
</output>
