---
phase: 09-bsa-writers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - include/libbsa/bsa_writer.hpp
  - include/libbsa/bsa.hpp
  - src/bsa_writer.cpp
  - CMakeLists.txt
  - tests/bsa_writer_tests.cpp
autonomous: true
requirements: [WRT-01, WRT-04]
must_haves:
  truths:
    - "Consumer can request BSA writing through BSA-specific wrappers layered over Phase 8 semantics (D-01) and explicit BSA target values, not filename inference (D-02)."
    - "Consumer can provide separate memory-backed and disk-backed BSA inputs without invalid byte/path combinations (D-08)."
    - "BSA write plans expose native BSA preview fields needed by later TES3/TES4 behavior tests (D-03), while production BSA paths remain separate from the test-only LBSW harness (D-04)."
  artifacts:
    - path: "include/libbsa/bsa_writer.hpp"
      provides: "Public BSA writer target, input, options, plan, and finalize contracts"
      contains: "enum class bsa_write_target"
    - path: "src/bsa_writer.cpp"
      provides: "BSA writer implementation seam with structured placeholder behavior"
      contains: "plan_bsa_write"
    - path: "tests/bsa_writer_tests.cpp"
      provides: "Focused BSA writer Catch2 target scaffold"
      contains: "[bsa-writer]"
  key_links:
    - from: "include/libbsa/bsa.hpp"
      to: "include/libbsa/bsa_writer.hpp"
      via: "public include for BSA writer discoverability"
      pattern: "#include <libbsa/bsa_writer.hpp>"
    - from: "CMakeLists.txt"
      to: "src/bsa_writer.cpp"
      via: "explicit libbsa source list"
      pattern: "src/bsa_writer.cpp"
---

<objective>
Create the public and build-system seam for production BSA writers.

Purpose: Phase 9 needs BSA-specific writer wrappers over Phase 8 plan/finalize semantics before TDD behavior slices can target stable public contracts.
Output: Public BSA writer header, implementation placeholder, focused test target, and explicit CMake wiring.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-bsa-writers/09-CONTEXT.md
@.planning/phases/09-bsa-writers/09-SPEC.md
@.planning/phases/09-bsa-writers/09-RESEARCH.md
@.planning/phases/09-bsa-writers/09-PATTERNS.md
@include/libbsa/bsa.hpp
@include/libbsa/writer.hpp
@include/libbsa/io.hpp
@include/libbsa/compression.hpp
@CMakeLists.txt

<interfaces>
Existing public shape to mirror:
```cpp
[[nodiscard]] result<bsa_archive> open_bsa(const byte_source& source);
[[nodiscard]] result<void> extract_bsa_entry(const bsa_archive& archive,
                                            const byte_source& source,
                                            std::string path,
                                            byte_sink& sink);
[[nodiscard]] result<write_plan> plan_archive_write(const writer_target& target,
                                                    std::span<const writer_entry> entries,
                                                    writer_options options = {});
[[nodiscard]] result<void> finalize_archive_write(const write_plan& plan, byte_sink& sink);
```
Create BSA-specific equivalents named `plan_bsa_write` and `finalize_bsa_write`; keep `LBSW` only in existing generic writer tests per D-04.
</interfaces>
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add public BSA writer contracts</name>
  <files>include/libbsa/bsa_writer.hpp, include/libbsa/bsa.hpp</files>
  <read_first>
    - include/libbsa/bsa_writer.hpp (create)
    - include/libbsa/bsa.hpp
    - include/libbsa/writer.hpp
    - include/libbsa/compression.hpp
    - .planning/phases/09-bsa-writers/09-CONTEXT.md (D-01 through D-16)
    - .planning/phases/09-bsa-writers/09-SPEC.md
  </read_first>
  <action>Create `include/libbsa/bsa_writer.hpp` with Doxygen comments and libbsa-owned C++20 value types only: `enum class bsa_write_target { tes3_morrowind, oblivion_v103, fo3_fnv_skyrim_le_v104, skyrim_se_ae_v105 };`, `struct bsa_write_options { bool archive_default_compressed{}; bool embedded_names{}; bool deduplicate{}; };`, `struct bsa_memory_entry { std::string path; std::vector<std::byte> payload; compression_policy compression{compression_policy::archive_default}; };`, `struct bsa_disk_entry { std::string host_path; std::string path; compression_policy compression{compression_policy::archive_default}; };`, `struct planned_bsa_table_region`, `struct planned_bsa_data_region`, `struct planned_bsa_entry`, `struct bsa_write_plan`, and overloads `plan_bsa_write(bsa_write_target, std::span<const bsa_memory_entry>, bsa_write_options = {})`, `plan_bsa_write_from_disk(bsa_write_target, std::span<const bsa_disk_entry>, bsa_write_options = {})`, and `finalize_bsa_write(const bsa_write_plan&, byte_sink&)`. Expose BSA-native preview fields required by D-03: folder/file table region names, folder/file hashes, flags, archive-absolute payload offsets, stored sizes, compression state, and data-region IDs. Include this header from `include/libbsa/bsa.hpp` so the API is discoverable beside `open_bsa` per D-01. Do not expose libdeflate, LZ4, DirectXTex, Windows, Delphi, TES5Edit, or raw BSA high-bit compression flag controls; callers use semantic compression policies per D-09 and D-10.</action>
  <verify>
    <automated>rg -n "enum class bsa_write_target|struct bsa_memory_entry|struct bsa_disk_entry|struct bsa_write_plan|plan_bsa_write|plan_bsa_write_from_disk|finalize_bsa_write" include/libbsa/bsa_writer.hpp &amp;&amp; rg -n "#include &lt;libbsa/bsa_writer.hpp&gt;" include/libbsa/bsa.hpp</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "tes3_morrowind|oblivion_v103|fo3_fnv_skyrim_le_v104|skyrim_se_ae_v105" include/libbsa/bsa_writer.hpp` finds all explicit targets per D-02.
    - `rg -n "std::filesystem::path|libdeflate|lz4|LZ4_|DirectXTex|DXGI|Windows.h|TES5Edit|FILE_SIZE_COMPRESS" include/libbsa/bsa_writer.hpp` returns no matches.
    - `rg -n "folder_hash|file_hash|flags|offset|stored_size|data_region_id" include/libbsa/bsa_writer.hpp` finds plan preview fields per D-03.
  </acceptance_criteria>
  <done>Public BSA writer types and declarations compile as a consumer-facing API, with separate memory/disk inputs per D-08 and no directory-scanning helpers per D-07.</done>
</task>

<task type="auto">
  <name>Task 2: Wire placeholder implementation and focused test target</name>
  <files>src/bsa_writer.cpp, tests/bsa_writer_tests.cpp, CMakeLists.txt</files>
  <read_first>
    - src/bsa_writer.cpp (create)
    - tests/bsa_writer_tests.cpp (create)
    - CMakeLists.txt
    - src/writer.cpp
    - tests/writer_core_tests.cpp
    - tests/bsa_reader_tests.cpp
  </read_first>
  <action>Add `src/bsa_writer.cpp` implementing the three public functions with structured `unsupported_format` placeholders whose messages are exactly `BSA writer planning is not implemented`, `BSA disk writer planning is not implemented`, and `BSA writer finalization is not implemented`. Add `tests/bsa_writer_tests.cpp` with Catch2 tests that include only public BSA writer headers and assert those placeholder errors for memory, disk, and finalize calls. Update `CMakeLists.txt` explicitly: add `src/bsa_writer.cpp` to `target_sources(libbsa PRIVATE ...)`, add `include/libbsa/bsa_writer.hpp` to the public header file set, and add `libbsa_bsa_writer_tests` with `tests/bsa_writer_tests.cpp`, linked to `libbsa::libbsa` and `Catch2::Catch2WithMain`, discovered with prefix `libbsa_bsa_writer_tests.` and labels `unit;fixture;codec;roundtrip`. Keep `TES5Edit/` out of all source lists per project boundary.</action>
  <verify>
    <automated>cmake --build build/windows-vs2026-vcpkg --config Debug --target libbsa_bsa_writer_tests &amp;&amp; ctest --test-dir build/windows-vs2026-vcpkg --output-on-failure -C Debug -R libbsa_bsa_writer_tests</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "src/bsa_writer.cpp|include/libbsa/bsa_writer.hpp|libbsa_bsa_writer_tests|tests/bsa_writer_tests.cpp" CMakeLists.txt` finds explicit wiring.
    - `rg -n "BSA writer planning is not implemented|BSA disk writer planning is not implemented|BSA writer finalization is not implemented" src/bsa_writer.cpp tests/bsa_writer_tests.cpp` finds placeholder contract coverage.
    - `rg -n "TES5Edit" CMakeLists.txt | rg -v "read-only reference"` returns no source-list inclusion.
  </acceptance_criteria>
  <done>Focused BSA writer target builds and passes placeholder tests, creating a stable Wave 1 seam for TDD behavior plans.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| public caller -> BSA writer planning | Untrusted archive virtual paths, host paths, options, and payload bytes enter writer code. |
| BSA writer finalization -> byte_sink | Planned archive bytes cross into caller-owned output storage. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | `bsa_write_target` API | mitigate | Explicit enum values only; no extension or game-name inference per D-02. |
| T-09-02 | Tampering | `bsa_memory_entry` / `bsa_disk_entry` | mitigate | Separate public types prevent invalid bytes+host-path combinations per D-08. |
| T-09-03 | Information Disclosure | public headers | mitigate | Public header grep must remain clean for private dependency/platform/TES5Edit tokens. |
</threat_model>

<verification>
Run focused build/tests plus public boundary greps:
`cmake --build build/windows-vs2026-vcpkg --config Debug --target libbsa_bsa_writer_tests && ctest --test-dir build/windows-vs2026-vcpkg --output-on-failure -C Debug -R libbsa_bsa_writer_tests`
</verification>

<success_criteria>
- BSA writer API surface exists with explicit targets and separate memory/disk inputs.
- Placeholder implementation and tests are wired without touching `TES5Edit/`.
- Later TDD plans can replace placeholders without changing public function names.
</success_criteria>

<output>
After completion, create `.planning/phases/09-bsa-writers/09-01-SUMMARY.md`.
</output>
