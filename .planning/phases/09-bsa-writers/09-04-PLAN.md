---
phase: 09-bsa-writers
plan: 04
type: tdd
wave: 4
depends_on: [09-03]
files_modified:
  - src/bsa_writer.cpp
  - tests/bsa_writer_tests.cpp
autonomous: true
requirements: [WRT-01]
must_haves:
  truths:
    - "Disk-backed BSA inputs use explicit host-file plus archive-virtual-path pairs (D-05) and read host file bytes during planning, not finalization (D-06)."
    - "Memory-backed and disk-backed entries with the same archive paths and bytes produce read-back-equivalent BSA archives while staying separate public input shapes (D-08)."
    - "Duplicate normalized paths, invalid archive paths, missing disk files, impossible BSA layout arithmetic, unsupported policy combinations, and finalization sink failures return structured errors without adding directory scanning/root-recursive helpers (D-07, D-12, D-15)."
  artifacts:
    - path: "src/bsa_writer.cpp"
      provides: "Disk input ingestion and finalization failure safety"
      contains: "plan_bsa_write_from_disk"
    - path: "tests/bsa_writer_tests.cpp"
      provides: "Disk/memory equivalence and sink failure tests"
      contains: "disk-backed"
  key_links:
    - from: "plan_bsa_write_from_disk"
      to: "plan_bsa_write"
      via: "read disk bytes into memory entries during planning"
      pattern: "bsa_memory_entry"
---

<objective>
TDD disk-backed BSA input planning and finalization failure behavior.

Purpose: WRT-01 requires creation from disk paths and in-memory buffers while preserving Phase 8 deterministic finalization semantics.
Output: Disk planning helper that owns bytes before finalization, equivalence tests, and negative tests for missing files and sink failures.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-bsa-writers/09-CONTEXT.md
@.planning/phases/09-bsa-writers/09-SPEC.md
@.planning/phases/09-bsa-writers/09-PATTERNS.md
@include/libbsa/bsa_writer.hpp
@include/libbsa/io.hpp
@src/bsa_writer.cpp
@tests/bsa_writer_tests.cpp
@tests/writer_core_tests.cpp
</context>

<feature>
  <name>Disk input planning and sink failure safety</name>
  <files>src/bsa_writer.cpp, tests/bsa_writer_tests.cpp</files>
  <behavior>
    - Given temporary disk files and equivalent `bsa_memory_entry` values, both outputs reopen with matching normalized paths, sizes, compression states, hashes, and extracted payload bytes.
    - Changing or deleting disk files after planning does not affect finalization output because bytes are owned by the plan.
    - Missing disk input fails during planning with `io_failure` or `malformed_archive` as appropriate, before any sink is touched.
    - A failing `byte_sink` returns the original injected error during `finalize_bsa_write`.
  </behavior>
  <implementation>Read each `bsa_disk_entry::host_path` into bytes during `plan_bsa_write_from_disk`, convert to memory entries preserving archive virtual paths and compression policy, and delegate to native planning. Keep traversal, symlink policy, and root-recursive helpers out of scope per D-05 and D-07.</implementation>
</feature>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1 RED: Add failing disk equivalence and finalization failure tests</name>
  <files>tests/bsa_writer_tests.cpp</files>
  <read_first>
    - tests/bsa_writer_tests.cpp
    - tests/writer_core_tests.cpp
    - include/libbsa/bsa_writer.hpp
    - .planning/phases/09-bsa-writers/09-CONTEXT.md D-05 through D-08
    - .planning/phases/09-bsa-writers/09-SPEC.md requirements 2, 7, 8
    - .planning/phases/09-bsa-writers/09-PATTERNS.md tests/bsa_writer_tests.cpp malformed/failure assertion analogs
  </read_first>
  <action>Add failing tests named `disk-backed and memory-backed BSA inputs read back equivalently`, `disk files are read during planning and not reopened during finalization`, `duplicate normalized BSA paths fail during planning`, `invalid archive paths fail during planning`, `missing disk inputs fail before finalization`, `impossible BSA layout arithmetic fails before finalization`, `unsupported BSA target version combinations fail during planning`, and `BSA finalization returns first sink failure unchanged`. Follow `09-PATTERNS.md` analogs from `tests/writer_core_tests.cpp` for sink failure and deterministic planning and from `tests/bsa_reader_tests.cpp` for exact structured failure assertions. Replace the Plan 09-01 disk-entry `BSA disk writer planning is not implemented` placeholder expectation with disk-backed behavior/failure assertions in this RED step; after this plan, required disk paths must not retain a placeholder `unsupported_format` expectation. Use temporary files created under the test runtime temp directory, explicit host-file plus archive-virtual-path pairs, `memory_sink`, and a local `failing_sink` matching the Phase 8 pattern. Assert expected `error_code` values: duplicate normalized paths, invalid archive paths, impossible BSA layout arithmetic, and unsupported target/version combinations use `malformed_archive` or `unsupported_format` as appropriate; missing/unreadable host files and injected sink failure use `io_failure`. Commit RED as `test(09-04): add failing disk and BSA writer failure tests`.</action>
  <verify>
    <automated>cmake --build build/windows-vs2026-vcpkg --config Debug --target libbsa_bsa_writer_tests &amp;&amp; ctest --test-dir build/windows-vs2026-vcpkg --output-on-failure -C Debug -R libbsa_bsa_writer_tests</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "disk-backed and memory-backed|read during planning|duplicate normalized|invalid archive paths|missing disk inputs|impossible BSA layout|unsupported BSA target|first sink failure" tests/bsa_writer_tests.cpp` finds RED cases.
    - RED run fails because disk planning/failure behavior is not implemented.
  </acceptance_criteria>
  <done>Disk and failure behaviors are specified by failing tests before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 GREEN: Implement disk planning and failure propagation</name>
  <files>src/bsa_writer.cpp</files>
  <read_first>
    - src/bsa_writer.cpp
    - include/libbsa/bsa_writer.hpp
    - include/libbsa/io.hpp
    - tests/bsa_writer_tests.cpp
    - .planning/phases/09-bsa-writers/09-PATTERNS.md src/bsa_writer.cpp structured errors and checked arithmetic analogs
  </read_first>
  <action>Implement `plan_bsa_write_from_disk` by opening each `bsa_disk_entry::host_path` as a host file, reading its full bytes during planning into a `std::vector<std::byte>`, pairing those bytes with the caller-provided archive virtual path, and delegating to `plan_bsa_write`. Remove or update any Plan 09-01 placeholder tests that still assert `BSA disk writer planning is not implemented`; required disk-entry paths must now assert disk-backed planning behavior or structured disk/input failures. Using the `09-PATTERNS.md` analogs from `src/writer.cpp`, ensure both memory and disk planning reject duplicate normalized paths and invalid archive paths with structured `malformed_archive` errors, reject unsupported/invalid BSA target-version combinations with `unsupported_format`, and reject impossible BSA table/payload layout arithmetic via checked add/multiply and 32-bit fit checks before any sink write. Return structured `io_failure` for missing/unreadable host files before any finalization sink access. Do not add directory traversal, recursive root packing, symlink handling, include/exclude policy, or path inference. Ensure `finalize_bsa_write` writes only plan-owned chunks and returns the first `byte_sink::write` failure unchanged.</action>
  <verify>
    <automated>cmake --build build/windows-vs2026-vcpkg --config Debug --target libbsa_bsa_writer_tests &amp;&amp; ctest --test-dir build/windows-vs2026-vcpkg --output-on-failure -C Debug -R libbsa_bsa_writer_tests</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "plan_bsa_write_from_disk|std::ifstream|bsa_memory_entry|host_path|io_failure|duplicate|normalize_archive_path|unsupported_format|checked|sink\.write" src/bsa_writer.cpp` finds disk/failure implementation markers.
    - `rg -n "recursive|directory_iterator|symlink|include|exclude" include/libbsa/bsa_writer.hpp src/bsa_writer.cpp` returns no directory-scanning policy additions.
    - Focused BSA writer tests pass after GREEN.
  </acceptance_criteria>
  <done>Disk input plans own bytes before finalization, equivalence tests pass, and sink/missing-file failures are structured.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| host filesystem -> disk planning | Host file paths and mutable disk contents enter planning. |
| finalization -> byte_sink | Sink may fail mid-stream. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-10 | Tampering | disk file mutation | mitigate | Read disk bytes during planning and store plan-owned bytes; finalization never reopens host files per D-06. |
| T-09-11 | Tampering | host/archive path confusion | mitigate | Host path and archive virtual path are separate fields; archive path still normalizes through libbsa rules. |
| T-09-12 | Repudiation / Integrity | partial sink failure | mitigate | Return first sink error unchanged; test custom failing sink. |
</threat_model>

<verification>
Focused: `ctest --test-dir build/windows-vs2026-vcpkg --output-on-failure -C Debug -R "libbsa_bsa_writer_tests|libbsa_writer_tests"`
</verification>

<success_criteria>
- RED and GREEN commits exist for `09-04`.
- Disk and memory inputs produce equivalent read-back archives.
- Mutating/deleting disk files after planning does not alter finalization output.
</success_criteria>

<output>
After completion, create `.planning/phases/09-bsa-writers/09-04-SUMMARY.md`.
</output>
