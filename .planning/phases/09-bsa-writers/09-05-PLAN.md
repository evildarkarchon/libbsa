---
phase: 09-bsa-writers
plan: 05
type: tdd
wave: 5
depends_on: [09-04]
files_modified:
  - src/bsa_reader.cpp
  - src/bsa_writer.cpp
  - tests/bsa_reader_tests.cpp
  - tests/bsa_writer_tests.cpp
autonomous: true
requirements: [WRT-04]
must_haves:
  truths:
    - "Consumer can write TES3 Morrowind BSA archives with raw payloads only."
    - "TES3 records, names, and hash table are sorted and serialized according to TES3-compatible hash order."
    - "TES3 record offsets are data-section-relative while plan and read-back metadata expose archive-absolute offsets."
  artifacts:
    - path: "src/bsa_writer.cpp"
      provides: "TES3 native BSA layout planner/finalizer"
      contains: "tes3_morrowind"
    - path: "tests/bsa_writer_tests.cpp"
      provides: "TES3 hash sorting, offset, and read-after-write tests"
      contains: "TES3-compatible hash sorting"
  key_links:
    - from: "src/bsa_writer.cpp"
      to: "src/hash.cpp"
      via: "TES3 hash ordering"
      pattern: "hash_tes3_path"
---

<objective>
TDD TES3 Morrowind native BSA writer behavior.

Purpose: WRT-04 requires correct TES3 hash sorting, name tables, raw payloads, and data-section-relative offsets.
Output: Passing TES3 byte-level and read-after-write tests plus implementation in the BSA writer.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-bsa-writers/09-CONTEXT.md
@.planning/phases/09-bsa-writers/09-SPEC.md
@.planning/phases/09-bsa-writers/09-RESEARCH.md
@src/bsa_reader.cpp
@src/hash.cpp
@src/bsa_writer.cpp
@tests/bsa_reader_tests.cpp
@tests/bsa_writer_tests.cpp
</context>

<feature>
  <name>TES3 BSA native writer layout</name>
  <files>src/bsa_writer.cpp, tests/bsa_writer_tests.cpp</files>
  <behavior>
    - Given multiple memory entries in arbitrary caller order, `plan_bsa_write(tes3_morrowind, ...)` orders records deterministically by TES3 hash with deterministic tie handling.
    - Finalized bytes contain TES3 magic/version `0x00000100`, `HashOffset` relative to byte 12, file records, name offsets, null-terminated name block, hash table, and raw payloads.
    - Serialized file record offsets are relative to the TES3 data section; plan and `open_bsa` metadata offsets are archive-absolute and match payload positions.
    - Compressed or embedded-name requests for TES3 fail structurally during planning.
  </behavior>
  <implementation>Implement TES3-specific branch using raw payloads, TES3 hash computation, hash-sorted records, checked table/name/data offsets, and native finalization chunks. TES3 hash table bytes must follow the resolved TES5Edit/BSArchPro order: each hash is serialized as little-endian high 32 bits first, then little-endian low 32 bits. If current reader/test helper assumptions differ, update `open_tes3_bsa` parsing and reader fixture assertions so libbsa read-back matches reference bytes.</implementation>
</feature>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1 RED: Add failing TES3 native writer tests</name>
  <files>tests/bsa_writer_tests.cpp</files>
  <read_first>
    - tests/bsa_writer_tests.cpp
    - tests/bsa_reader_tests.cpp
    - src/bsa_reader.cpp
    - src/hash.cpp
    - .planning/phases/09-bsa-writers/09-RESEARCH.md Pitfall 1 and TES3 Layout Cursor
    - .planning/phases/09-bsa-writers/09-SPEC.md requirement 5
  </read_first>
  <action>Add failing tests named `writes TES3 BSA records in TES3-compatible hash sorting order`, `serializes TES3 name offsets hash table and data-section-relative offsets`, `round trips TES3 raw payloads through open_bsa and extract_bsa_entry`, and `rejects TES3 compression and embedded-name requests`. Include known paths with distinct TES3 hashes, inspect emitted record/hash/name bytes, assert metadata offset equals `data_section_offset + relative_offset`, and extract all payloads. Commit RED as `test(09-05): add failing TES3 BSA writer tests`.</action>
  <verify>
    <automated>cmake --build build/windows-vs2026-vcpkg --config Debug --target libbsa_bsa_writer_tests &amp;&amp; ctest --test-dir build/windows-vs2026-vcpkg --output-on-failure -C Debug -R libbsa_bsa_writer_tests</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "TES3-compatible hash sorting|data-section-relative offsets|round trips TES3 raw|rejects TES3 compression" tests/bsa_writer_tests.cpp` finds RED cases.
    - RED run fails due to absent TES3 writer behavior, not fixture/test syntax.
  </acceptance_criteria>
  <done>TES3 behavior is captured as failing byte-level and read-after-write tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 GREEN: Implement TES3 native writer</name>
  <files>src/bsa_reader.cpp, src/bsa_writer.cpp, tests/bsa_reader_tests.cpp</files>
  <read_first>
    - src/bsa_writer.cpp
    - src/bsa_reader.cpp
    - src/hash.cpp
    - tests/bsa_writer_tests.cpp
  </read_first>
  <action>Implement `bsa_write_target::tes3_morrowind` in `plan_bsa_write`: normalize/reject duplicate paths, reject any compressed entry policy other than raw/archive-default-resolving-raw and any `embedded_names` request with structured `unsupported_format` or `malformed_archive`, compute `hash_tes3_path`, sort by TES3 hash then normalized path for deterministic ties per D-13, build file records with size and data-section-relative offset, name offset table, null-terminated name block, TES3 hash table bytes, and raw data regions. Serialize each TES3 hash as two little-endian `uint32_t` words in TES5Edit order: high 32 bits first, low 32 bits second. `HashOffset` must equal record table size + name offset table size + name block size and be stored relative to byte 12. Expose archive-absolute payload offsets in `bsa_write_plan` and write native TES3 chunks during finalization. If existing `open_tes3_bsa` hash parsing or `tests/bsa_reader_tests.cpp::tes3_archive_bytes` assumes little-endian `uint64_t`, update them to parse/build the resolved TES5Edit high32-low32 word order and add a code comment near the parser explaining the compatibility constraint.</action>
  <verify>
    <automated>cmake --build build/windows-vs2026-vcpkg --config Debug --target libbsa_bsa_writer_tests &amp;&amp; ctest --test-dir build/windows-vs2026-vcpkg --output-on-failure -C Debug -R libbsa_bsa_writer_tests</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "tes3_morrowind|hash_tes3|HashOffset|relative_offset|name_offset|data_section" src/bsa_writer.cpp` finds TES3 implementation markers.
    - `rg -n "high 32|Hash shr 32|TES5Edit.*hash" src/bsa_reader.cpp tests/bsa_reader_tests.cpp src/bsa_writer.cpp` finds the resolved TES3 hash byte-order documentation or assertions.
    - `rg -n "deflate|lz4_frame|embedded_names" src/bsa_writer.cpp` shows TES3 branch rejects rather than routes those requests.
    - Focused BSA writer tests pass after GREEN.
  </acceptance_criteria>
  <done>TES3 writer emits valid native Morrowind BSA bytes that reopen and extract correctly.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| caller entries -> TES3 tables | Untrusted paths and sizes become TES3 table offsets/hashes. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-13 | Tampering / DoS | TES3 offset arithmetic | mitigate | Checked arithmetic for HashOffset, record/name/hash table sizes, data-section offsets, and uint32 fit. |
| T-09-14 | Tampering | TES3 path/hash ordering | mitigate | Reuse existing TES3 hash implementation and deterministic tie handling. |
| T-09-15 | Integrity | unsupported TES3 compression/embed | mitigate | Planning fails structurally; no raw downgrade or embedded-name skip. |
</threat_model>

<verification>
Focused: `ctest --test-dir build/windows-vs2026-vcpkg --output-on-failure -C Debug -R libbsa_bsa_writer_tests`
</verification>

<success_criteria>
- RED and GREEN commits exist for `09-05`.
- TES3 output byte layout and read-after-write tests pass.
- TES3 compression/embedded-name unsupported requests fail during planning.
</success_criteria>

<output>
After completion, create `.planning/phases/09-bsa-writers/09-05-SUMMARY.md`.
</output>
