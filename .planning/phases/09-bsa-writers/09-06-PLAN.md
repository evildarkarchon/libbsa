---
phase: 09-bsa-writers
plan: 06
type: execute
wave: 6
depends_on: [09-05]
files_modified:
  - tests/public_header_smoke.cpp
  - README.md
  - .planning/phases/09-bsa-writers/09-VALIDATION.md
autonomous: true
requirements: [WRT-01, WRT-04]
must_haves:
  truths:
    - "Consumer-style public smoke creates and finalizes one TES3 and three TES4-family BSA archives using public headers only."
    - "Documentation accurately states Phase 9 BSA writer support without claiming BA2 writer support."
    - "Full validation gates pass and TES5Edit remains untouched."
  artifacts:
    - path: "tests/public_header_smoke.cpp"
      provides: "Public-only BSA writer compile/link coverage"
      contains: "plan_bsa_write"
    - path: "README.md"
      provides: "Consumer-facing BSA writer scope notes"
      contains: "BSA writer"
    - path: ".planning/phases/09-bsa-writers/09-VALIDATION.md"
      provides: "Nyquist validation sign-off"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "tests/public_header_smoke.cpp"
      to: "include/libbsa/bsa_writer.hpp"
      via: "public include and function pointers"
      pattern: "#include <libbsa/bsa_writer.hpp>"
---

<objective>
Finalize Phase 9 public smoke, documentation, and validation gates.

Purpose: WRT-01 and WRT-04 are only complete when consumers can use BSA writer APIs through public headers and maintainers have full validation evidence.
Output: Public smoke coverage, README updates, validation status update, and full gate commands recorded in the summary.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-bsa-writers/09-CONTEXT.md
@.planning/phases/09-bsa-writers/09-SPEC.md
@.planning/phases/09-bsa-writers/09-VALIDATION.md
@include/libbsa/bsa_writer.hpp
@tests/public_header_smoke.cpp
@README.md
@CMakeLists.txt
</context>

<tasks>
<task type="auto">
  <name>Task 1: Extend public-header smoke for BSA writers</name>
  <files>tests/public_header_smoke.cpp</files>
  <read_first>
    - tests/public_header_smoke.cpp
    - include/libbsa/bsa_writer.hpp
    - include/libbsa/bsa.hpp
    - .planning/phases/09-bsa-writers/09-SPEC.md acceptance criteria
  </read_first>
  <action>Update the existing public smoke executable to include `<libbsa/bsa_writer.hpp>` explicitly, take function pointers for `plan_bsa_write`, `plan_bsa_write_from_disk`, and `finalize_bsa_write`, construct public `bsa_memory_entry` values, plan and finalize one archive for each target (`tes3_morrowind`, `oblivion_v103`, `fo3_fnv_skyrim_le_v104`, `skyrim_se_ae_v105`) using only public headers, and reopen/extract at least one generated archive through `open_bsa` and `extract_bsa_entry`. Do not include private headers or test-only harness declarations.</action>
  <verify>
    <automated>cmake --build build/windows-vs2026-vcpkg --config Debug --target libbsa_public_header_smoke &amp;&amp; ctest --test-dir build/windows-vs2026-vcpkg --output-on-failure -C Debug -R libbsa.public_header_smoke</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "#include <libbsa/bsa_writer.hpp>|plan_bsa_write|plan_bsa_write_from_disk|finalize_bsa_write|tes3_morrowind|oblivion_v103|fo3_fnv_skyrim_le_v104|skyrim_se_ae_v105" tests/public_header_smoke.cpp` finds public smoke coverage.
    - `rg -n "DirectXTex|DXGI_FORMAT|Windows.h|libdeflate|lz4.h|lz4frame.h|LZ4_|TES5Edit|writer_harness" tests/public_header_smoke.cpp include/libbsa` returns no public leakage matches.
  </acceptance_criteria>
  <done>Consumer-style smoke proves all BSA writer variants are usable through public headers only.</done>
</task>

<task type="auto">
  <name>Task 2: Document BSA writer support and update validation sign-off</name>
  <files>README.md, .planning/phases/09-bsa-writers/09-VALIDATION.md</files>
  <read_first>
    - README.md
    - .planning/phases/09-bsa-writers/09-VALIDATION.md
    - .planning/phases/09-bsa-writers/09-SPEC.md boundaries
    - .planning/ROADMAP.md Phase 9 and Phase 10 boundaries
  </read_first>
  <action>Update README to describe Phase 9 production BSA writer support: explicit BSA targets, memory and disk inputs, TES4 v103/v104/v105 native headers/tables/compression/embedded-name behavior, TES3 hash-sorted raw archives with data-section-relative offsets, streaming finalization through `byte_sink`, and read-after-write validation. Explicitly do not claim BA2 writer, DDS packing, CLI/GUI, corpus comparison, performance/multithreading, or in-place mutation support. Update `09-VALIDATION.md` frontmatter to `nyquist_compliant: true` and `wave_0_complete: true` after all task commands pass; update task rows from TBD to plan/wave IDs and status green.</action>
  <verify>
    <automated>ctest --test-dir build/windows-vs2026-vcpkg --output-on-failure -C Debug -R "libbsa_bsa_writer_tests|libbsa.public_header_smoke"</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "BSA writer|TES3|v103|v104|v105|byte_sink|read-after-write" README.md` finds scope documentation.
    - `rg -n "BA2 writer|DDS packing|CLI|GUI|in-place mutation|multi-threaded" README.md` either returns no new Phase 9 claims or text explicitly marks them deferred/out of scope.
    - `rg -n "nyquist_compliant: true|wave_0_complete: true|09-01|09-02|09-03|09-04|09-05" .planning/phases/09-bsa-writers/09-VALIDATION.md` finds validation updates.
  </acceptance_criteria>
  <done>README and validation strategy accurately reflect completed Phase 9 behavior and boundaries.</done>
</task>

<task type="auto">
  <name>Task 3: Run final Phase 9 boundary and regression gates</name>
  <files>tests/public_header_smoke.cpp, README.md, .planning/phases/09-bsa-writers/09-VALIDATION.md</files>
  <read_first>
    - CMakeLists.txt
    - include/libbsa/bsa_writer.hpp
    - tests/public_header_smoke.cpp
    - .planning/phases/09-bsa-writers/09-VALIDATION.md
  </read_first>
  <action>Run the full Phase 9 gate set and record results in the plan summary: focused BSA writer tests, existing writer-core tests, BSA reader tests, compression policy tests, public-header smoke, full CTest, public-header private-token grep, CMake glob/source-boundary grep, stale-placeholder grep, and `git status --short TES5Edit`. The stale-placeholder grep must prove no required BSA target behavior test still asserts Plan 09-01 `not implemented` placeholders after implementation: search `tests/bsa_writer_tests.cpp` for `BSA writer planning is not implemented`, `BSA disk writer planning is not implemented`, and `BSA writer finalization is not implemented`; any remaining occurrence must be scoped only to an intentionally unsupported branch and accompanied by an assertion for that unsupported condition, not to `tes3_morrowind`, `oblivion_v103`, `fo3_fnv_skyrim_le_v104`, `skyrim_se_ae_v105`, disk-backed required paths, or normal finalization. Fix direct stale-placeholder regressions before completing the phase. If an unrelated pre-existing failure appears, record it with exact command/output and do not hide it.</action>
  <verify>
    <automated>ctest --test-dir build/windows-vs2026-vcpkg --output-on-failure -C Debug &amp;&amp; git status --short TES5Edit</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "DirectXTex|DXGI_FORMAT|Windows\.h|libdeflate|TES5Edit|lz4\.h|lz4frame\.h|LZ4_" include/libbsa` returns no public leakage matches.
    - `rg -n "^[^#]*\b(GLOB|GLOB_RECURSE)\b" CMakeLists.txt` returns no matches.
    - `rg -n "BSA (disk )?writer (planning|finalization) is not implemented" tests/bsa_writer_tests.cpp` returns no matches for required BSA targets, disk-backed required paths, or normal finalization; any remaining match is explicitly limited to an unsupported branch with an adjacent unsupported-condition assertion.
    - `git status --short TES5Edit` returns empty output.
  </acceptance_criteria>
  <done>All automated gates pass, public boundary checks are clean, and TES5Edit is unmodified.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| public API -> consumers | Public headers define supported writer surface and must not leak private dependencies. |
| validation -> release confidence | Tests and docs must not overclaim unsupported BA2/performance/corpus behavior. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-16 | Information Disclosure | public headers | mitigate | Private-token grep over `include/libbsa` must be clean. |
| T-09-17 | Repudiation | README/support claims | mitigate | Document BSA writer support narrowly and explicitly defer BA2/corpus/performance/in-place features. |
| T-09-18 | Tampering | TES5Edit submodule | mitigate | `git status --short TES5Edit` must be empty. |
</threat_model>

<verification>
Task-level smoke gates stay narrow for fast feedback: public smoke runs only `libbsa.public_header_smoke`, documentation/validation runs `libbsa_bsa_writer_tests|libbsa.public_header_smoke`, and the final task preserves the full gate.

Full gate: `ctest --test-dir build/windows-vs2026-vcpkg --output-on-failure -C Debug && git status --short TES5Edit`
</verification>

<success_criteria>
- Public smoke creates all four required BSA variants using public headers only.
- README and validation status are accurate and phase-scoped.
- Full regression and boundary gates pass.
</success_criteria>

<output>
After completion, create `.planning/phases/09-bsa-writers/09-06-SUMMARY.md`.
</output>
