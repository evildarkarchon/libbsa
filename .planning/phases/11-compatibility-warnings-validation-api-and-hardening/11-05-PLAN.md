---
phase: 11-compatibility-warnings-validation-api-and-hardening
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/tes3_bsa_reader_tests.cpp
  - tests/unit/tes4_bsa_reader_tests.cpp
  - tests/unit/ba2_gnrl_reader_tests.cpp
autonomous: true
requirements:
  - COMP-04
must_haves:
  truths:
    - "Malformed manifest tests fail loudly when expected error-code strings are unknown."
    - "TES3, TES4-family BSA, and BA2 GNRL malformed tests keep stable public error-code assertions."
    - "D-13: malformed manifest expectations become strict enough to support the consolidated matrix."
    - "D-17: malformed tests branch on stable public error_code values."
  artifacts:
    - path: "tests/unit/tes3_bsa_reader_tests.cpp"
      provides: "Strict TES3 malformed expected_error mapping"
      contains: "unknown TES3 malformed expected_error"
    - path: "tests/unit/tes4_bsa_reader_tests.cpp"
      provides: "Strict TES4 malformed expected_error mapping"
      contains: "unknown TES4 malformed expected_error"
    - path: "tests/unit/ba2_gnrl_reader_tests.cpp"
      provides: "Strict BA2 GNRL malformed expected_error mapping"
      contains: "unknown BA2 GNRL malformed expected_error"
  key_links:
    - from: "tests/unit/*reader_tests.cpp"
      to: "tests/fixtures/generated/archives/*_manifest.json"
      via: "manifest expected_error values"
      pattern: "FAIL\\(\"unknown"
---

<objective>
Harden existing malformed manifest error-code mapping.

Purpose: Phase 11 requires malformed manifests to reject unknown `expected_error` strings instead of silently mapping typos to another public category.
Output: Strict manifest helper behavior in TES3, TES4-family BSA, and BA2 GNRL reader tests.
</objective>

<execution_context>
@C:/Users/evild/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/11-compatibility-warnings-validation-api-and-hardening/11-SPEC.md
@.planning/phases/11-compatibility-warnings-validation-api-and-hardening/11-CONTEXT.md
@.planning/phases/11-compatibility-warnings-validation-api-and-hardening/11-RESEARCH.md
@.planning/phases/11-compatibility-warnings-validation-api-and-hardening/11-PATTERNS.md
@tests/unit/tes3_bsa_reader_tests.cpp
@tests/unit/tes4_bsa_reader_tests.cpp
@tests/unit/ba2_gnrl_reader_tests.cpp
@tests/unit/ba2_dx10_malformed_tests.cpp

<interfaces>
Strict helper pattern to copy from `tests/unit/ba2_dx10_malformed_tests.cpp`:
```cpp
FAIL("unknown BA2 DX10 malformed expected_error: " << value);
return libbsa::error_code::format_error;
```

Files to update:
- `tests/unit/tes3_bsa_reader_tests.cpp`: message must contain `unknown TES3 malformed expected_error`.
- `tests/unit/tes4_bsa_reader_tests.cpp`: message must contain `unknown TES4 malformed expected_error`.
- `tests/unit/ba2_gnrl_reader_tests.cpp`: message must contain `unknown BA2 GNRL malformed expected_error`.
</interfaces>
</context>

<decision_trace>
- D-13: Existing malformed manifest checks are hardened so matrix/manifests prove stable expected-error rows.
- D-17: Tests continue branching on stable `error_code` values instead of diagnostic strings.
</decision_trace>

<tasks>

<task type="auto">
  <name>Task 1: Replace silent malformed expected_error fallbacks</name>
  <read_first>
    - tests/unit/tes3_bsa_reader_tests.cpp
    - tests/unit/tes4_bsa_reader_tests.cpp
    - tests/unit/ba2_gnrl_reader_tests.cpp
    - tests/unit/ba2_dx10_malformed_tests.cpp
    - .planning/phases/11-compatibility-warnings-validation-api-and-hardening/11-CONTEXT.md
  </read_first>
  <files>tests/unit/tes3_bsa_reader_tests.cpp, tests/unit/tes4_bsa_reader_tests.cpp, tests/unit/ba2_gnrl_reader_tests.cpp</files>
  <action>
    In each `error_code_from_manifest(std::string_view value)` helper, preserve the existing `"format_error"` and `"unsupported"` branches, then replace the final `return libbsa::error_code::invalid_argument;` fallback with `FAIL("unknown ... malformed expected_error: " << value); return libbsa::error_code::format_error;` using the exact family-specific messages listed in `<interfaces>`. This implements D-13 and D-17 by making manifest typos fail tests loudly while keeping public error-code assertions stable.
  </action>
  <verify>
    <automated>cmake --build --preset windows-msvc-debug-static --target libbsa_tests && ctest --preset windows-msvc-debug-static -R "tes3_bsa_malformed|tes4_bsa_malformed|ba2_gnrl_malformed|ba2_dx10_malformed" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "unknown TES3 malformed expected_error" tests/unit/tes3_bsa_reader_tests.cpp` finds the TES3 strict helper.
    - `rg -n "unknown TES4 malformed expected_error" tests/unit/tes4_bsa_reader_tests.cpp` finds the TES4 strict helper.
    - `rg -n "unknown BA2 GNRL malformed expected_error" tests/unit/ba2_gnrl_reader_tests.cpp` finds the BA2 GNRL strict helper.
    - `rg -n "return libbsa::error_code::invalid_argument;" tests/unit/tes3_bsa_reader_tests.cpp tests/unit/tes4_bsa_reader_tests.cpp tests/unit/ba2_gnrl_reader_tests.cpp` returns no matches from `error_code_from_manifest`.
  </acceptance_criteria>
  <done>Reader malformed manifest helpers reject unknown expected-error categories instead of silently remapping them.</done>
</task>

<task type="auto">
  <name>Task 2: Verify malformed reader suites and strict mapping gates</name>
  <read_first>
    - tests/unit/tes3_bsa_reader_tests.cpp
    - tests/unit/tes4_bsa_reader_tests.cpp
    - tests/unit/ba2_gnrl_reader_tests.cpp
    - tests/unit/ba2_dx10_malformed_tests.cpp
  </read_first>
  <files>tests/unit/tes3_bsa_reader_tests.cpp, tests/unit/tes4_bsa_reader_tests.cpp, tests/unit/ba2_gnrl_reader_tests.cpp</files>
  <action>
    Run the targeted malformed suites and confirm the changed helpers did not alter any valid manifest expectations. Do not modify production parser behavior in this plan; the purpose is test oracle hardening for D-13.
  </action>
  <verify>
    <automated>ctest --preset windows-msvc-debug-static -L malformed --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `ctest --preset windows-msvc-debug-static -L malformed --output-on-failure` passes.
    - `git status --short TES5Edit` produces no output.
  </acceptance_criteria>
  <done>All malformed label tests pass with strict manifest mapping and the read-only reference submodule remains untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated malformed manifests -> test assertions | JSON expected-error strings drive public error-code assertions. |
| malformed archive bytes -> parser tests | Invalid archive structures exercise parser failure paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-13 | Tampering | malformed manifest expected_error | mitigate | Fail loudly on unknown strings instead of mapping to `invalid_argument`. |
| T-11-14 | Denial of Service | malformed parser tests | mitigate | Keep existing malformed tests running through strict parser paths and stable public error-code assertions. |
| T-11-15 | Repudiation | malformed coverage evidence | mitigate | Use targeted malformed CTest labels and family-specific helper messages for traceable failures. |
</threat_model>

<verification>
Run `cmake --build --preset windows-msvc-debug-static --target libbsa_tests`, `ctest --preset windows-msvc-debug-static -L malformed --output-on-failure`, and `git status --short TES5Edit`.
</verification>

<success_criteria>
- No TES3/TES4/BA2 GNRL malformed helper silently maps unknown manifest strings.
- Malformed label tests pass.
- Production parser behavior is unchanged by this test-oracle hardening plan.
</success_criteria>

<output>
After completion, create `.planning/phases/11-compatibility-warnings-validation-api-and-hardening/11-05-SUMMARY.md`.
</output>
