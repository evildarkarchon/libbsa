---
phase: 11-compatibility-warnings-validation-api-and-hardening
plan: 06
type: tdd
wave: 4
depends_on:
  - 11-02
  - 11-05
files_modified:
  - tests/fixtures/generated/compatibility_matrix.json
  - tests/fixtures/generated/validate_fixture_manifests.py
  - tests/unit/compatibility_matrix_tests.cpp
  - tests/unit/validation_api_tests.cpp
  - tests/CMakeLists.txt
autonomous: true
requirements:
  - COMP-04
  - COMP-05
must_haves:
  truths:
    - "Maintainer can see one consolidated malformed matrix spanning all supported families and risk categories."
    - "Validation reports fatal errors for matrix malformed cases without returning a usable reader."
    - "Matrix checks reject unknown expected-error values and missing manifest case references."
    - "D-02: validation does not make malformed archives usable."
    - "D-03: matrix malformed failures populate validation_report.errors."
    - "D-13: consolidated matrix spans TES3 BSA, TES4-family BSA, BA2 GNRL, BA2 DX10, compression, oversized arithmetic, unsupported routes, and DDS chunk/layout cases."
    - "D-17: matrix and validation tests assert stable public error_code categories."
    - "D-18: matrix evidence uses generated legal fixtures and manifests."
  artifacts:
    - path: "tests/fixtures/generated/compatibility_matrix.json"
      provides: "Consolidated malformed hardening matrix"
      contains: "tes3"
    - path: "tests/unit/compatibility_matrix_tests.cpp"
      provides: "Machine validation for matrix rows and manifest references"
      contains: "compatibility_matrix"
    - path: "tests/unit/validation_api_tests.cpp"
      provides: "Validation report coverage for malformed matrix cases"
      contains: "validate_entry_extractability"
  key_links:
    - from: "tests/fixtures/generated/compatibility_matrix.json"
      to: "tests/fixtures/generated/archives/*_manifest.json"
      via: "manifest and case_id references"
      pattern: "manifest"
    - from: "tests/unit/validation_api_tests.cpp"
      to: "libbsa::validate_archive"
      via: "malformed report assertions"
      pattern: "validate_archive"
---

<objective>
Add the consolidated malformed hardening matrix and validation-report coverage.

Purpose: Phase 11 needs one machine-checked view of malformed coverage across TES3, TES4-family BSA, BA2 GNRL, BA2 DX10, compression, oversized arithmetic, unsupported routes, and DDS chunk/layout cases.
Output: Matrix JSON, validator updates, C++ matrix tests, and validation API malformed-report tests.
</objective>

<execution_context>
@C:/Users/evild/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/11-compatibility-warnings-validation-api-and-hardening/11-SPEC.md
@.planning/phases/11-compatibility-warnings-validation-api-and-hardening/11-CONTEXT.md
@.planning/phases/11-compatibility-warnings-validation-api-and-hardening/11-RESEARCH.md
@.planning/phases/11-compatibility-warnings-validation-api-and-hardening/11-PATTERNS.md
@tests/fixtures/generated/validate_fixture_manifests.py
@tests/fixtures/generated/archives/tes3_malformed_manifest.json
@tests/fixtures/generated/archives/malformed_manifest.json
@tests/fixtures/generated/archives/ba2_gnrl_malformed_manifest.json
@tests/fixtures/generated/archives/ba2_dx10_malformed_manifest.json
@tests/unit/validation_api_tests.cpp
@tests/CMakeLists.txt

<interfaces>
Matrix JSON shape:
```json
{
  "matrix_kind": "phase11_malformed_hardening",
  "requirements": ["COMP-04", "COMP-05"],
  "rows": [
    {
      "id": "tes3_truncated_header",
      "family": "tes3_bsa",
      "category": "truncated_structure",
      "archive": "tes3_truncated_header.bsa",
      "manifest": "tes3_malformed_manifest.json",
      "case_id": "tes3_truncated_header",
      "phase": "open",
      "expected_error": "format_error"
    }
  ]
}
```

Required category coverage:
- `truncated_structure`
- `duplicate_canonical_path`
- `invalid_payload_span`
- `unsupported_route`
- `decompression_failure`
- `oversized_arithmetic`
- `dds_chunk_layout`
</interfaces>
</context>

<decision_trace>
- D-02: Matrix validation proves malformed archives do not become usable readers through validation.
- D-03: Matrix validation asserts fatal diagnostics land in `validation_report.errors`.
- D-13: The consolidated matrix spans TES3 BSA, TES4-family BSA, BA2 GNRL, BA2 DX10, compression, oversized arithmetic, unsupported routes, and DDS chunk/layout cases.
- D-17: Matrix and validation tests assert stable public `error_code` categories.
- D-18: Matrix rows reference generated legal fixtures and manifests, not local copyrighted data.
</decision_trace>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED malformed matrix and validation-report tests</name>
  <read_first>
    - tests/fixtures/generated/compatibility_matrix.json
    - tests/fixtures/generated/validate_fixture_manifests.py
    - tests/unit/compatibility_matrix_tests.cpp
    - tests/unit/validation_api_tests.cpp
    - tests/CMakeLists.txt
    - tests/fixtures/generated/archives/tes3_malformed_manifest.json
    - tests/fixtures/generated/archives/malformed_manifest.json
    - tests/fixtures/generated/archives/ba2_gnrl_malformed_manifest.json
    - tests/fixtures/generated/archives/ba2_dx10_malformed_manifest.json
  </read_first>
  <files>tests/unit/compatibility_matrix_tests.cpp, tests/unit/validation_api_tests.cpp, tests/CMakeLists.txt</files>
  <behavior>
    - Matrix test fails when `compatibility_matrix.json` is missing.
    - Matrix test requires every row to reference an existing archive, manifest, and manifest case id.
    - Validation API malformed tests require open-phase matrix rows to return invalid reports and preserve strict `archive_reader::open` failures.
    - Validation API extraction-phase tests require `validate_entry_extractability=true` to surface extraction/decompression errors as report errors.
  </behavior>
  <action>
    Add `tests/unit/compatibility_matrix_tests.cpp`, register it in `tests/CMakeLists.txt`, and extend `tests/unit/validation_api_tests.cpp`. Test names must include lowercase `compatibility_matrix` or `validation_api`. In matrix tests, parse the matrix and referenced manifests with nlohmann-json, assert required categories and all four families (`tes3_bsa`, `tes4_bsa`, `ba2_gnrl`, `ba2_dx10`) are present, assert each referenced archive exists under `tests/fixtures/generated/archives`, and assert each `case_id` exists in its manifest. In validation tests, run `libbsa::validate_archive` over representative matrix rows and assert report errors contain stable `error_code` values without exact message checks. This implements D-02, D-03, D-13, D-17, and D-18.
  </action>
  <verify>
    <automated>cmake --build --preset windows-msvc-debug-static --target libbsa_tests</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "compatibility_matrix" tests/unit/compatibility_matrix_tests.cpp tests/unit/validation_api_tests.cpp` finds selectable test names.
    - `rg -n "truncated_structure|duplicate_canonical_path|invalid_payload_span|unsupported_route|decompression_failure|oversized_arithmetic|dds_chunk_layout" tests/unit/compatibility_matrix_tests.cpp` finds required category assertions.
    - `rg -n "validate_entry_extractability" tests/unit/validation_api_tests.cpp` finds extraction-style validation report coverage.
    - RED build or focused run fails before `tests/fixtures/generated/compatibility_matrix.json` exists.
  </acceptance_criteria>
  <done>RED matrix and validation-report tests exist and fail for the missing consolidated matrix.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN consolidated matrix and Python manifest validator</name>
  <read_first>
    - tests/fixtures/generated/compatibility_matrix.json
    - tests/fixtures/generated/validate_fixture_manifests.py
    - tests/unit/compatibility_matrix_tests.cpp
    - tests/unit/validation_api_tests.cpp
    - tests/fixtures/generated/archives/tes3_malformed_manifest.json
    - tests/fixtures/generated/archives/malformed_manifest.json
    - tests/fixtures/generated/archives/ba2_gnrl_malformed_manifest.json
    - tests/fixtures/generated/archives/ba2_dx10_malformed_manifest.json
  </read_first>
  <files>tests/fixtures/generated/compatibility_matrix.json, tests/fixtures/generated/validate_fixture_manifests.py, tests/unit/compatibility_matrix_tests.cpp, tests/unit/validation_api_tests.cpp</files>
  <action>
    Create `tests/fixtures/generated/compatibility_matrix.json` with rows covering at least: `tes3_truncated_header`, `tes3_invalid_payload_span`, `tes3_inconsistent_counts_offsets`, `malformed_truncated_table`, an oversized TES4-family count/offset arithmetic case already covered in `tests/unit/tes4_bsa_reader_tests.cpp`, `malformed_corrupt_compressed_payload`, `ba2_truncated_name_table`, `ba2_unsupported_v3_compression_method`, `ba2_exact_size_mismatch`, `ba2_dx10_truncated_records`, `ba2_dx10_decoded_size_mismatch`, `ba2_dx10_duplicate_mip_face`, and `ba2_dx10_unsupported_compression`. Extend `validate_fixture_manifests.py` with `validate_compatibility_matrix()` using the existing `AssertionError` style: require top-level keys, require known `expected_error` values only (`format_error`, `unsupported`), require all referenced manifests/archives exist, and require each `case_id` exists in the referenced manifest. Keep all archives generated/legal and outside `TES5Edit/`.
  </action>
  <verify>
    <automated>python tests/fixtures/generated/validate_fixture_manifests.py && cmake --build --preset windows-msvc-debug-static --target libbsa_tests && ctest --preset windows-msvc-debug-static -R "compatibility_matrix|validation_api" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "phase11_malformed_hardening" tests/fixtures/generated/compatibility_matrix.json` finds the matrix kind.
    - `rg -n "tes3_bsa|tes4_bsa|ba2_gnrl|ba2_dx10" tests/fixtures/generated/compatibility_matrix.json` finds all family tokens.
    - `rg -n "truncated_structure|duplicate_canonical_path|invalid_payload_span|unsupported_route|decompression_failure|oversized_arithmetic|dds_chunk_layout" tests/fixtures/generated/compatibility_matrix.json` finds all required categories.
    - `rg -n "validate_compatibility_matrix" tests/fixtures/generated/validate_fixture_manifests.py` finds the Python validator.
    - `python tests/fixtures/generated/validate_fixture_manifests.py` passes.
  </acceptance_criteria>
  <done>Consolidated matrix exists, is machine-validated, and validation reports cover malformed matrix rows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| matrix JSON -> C++/Python validators | Matrix rows drive test selection and evidence claims. |
| malformed archive bytes -> validation API | Invalid archives are passed through strict open and optional extraction validation. |
| compression/DDS metadata -> validation report | Corrupt payloads and invalid texture chunks must become stable diagnostics, not crashes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-16 | Tampering | compatibility_matrix.json | mitigate | Python and C++ checks require known categories, known expected errors, existing archive files, and existing manifest case ids. |
| T-11-17 | Denial of Service | malformed validation path | mitigate | Matrix includes oversized arithmetic, truncation, decompression, and DDS layout rows; validation uses strict parser/extraction errors. |
| T-11-18 | Repudiation | malformed coverage evidence | mitigate | Consolidated matrix ties each row to a manifest case and generated archive artifact. |
</threat_model>

<verification>
Run `python tests/fixtures/generated/validate_fixture_manifests.py`, `cmake --build --preset windows-msvc-debug-static --target libbsa_tests`, `ctest --preset windows-msvc-debug-static -R "compatibility_matrix|validation_api" --output-on-failure`, and `ctest --preset windows-msvc-debug-static -L malformed --output-on-failure`.
</verification>

<success_criteria>
- Consolidated matrix spans TES3, TES4-family BSA, BA2 GNRL, BA2 DX10, compression, oversized arithmetic, unsupported routes, and DDS chunk/layout cases.
- Matrix references are validated by Python and C++ tests.
- Validation API reports fatal errors for malformed matrix cases without weakening strict open.
</success_criteria>

<output>
After completion, create `.planning/phases/11-compatibility-warnings-validation-api-and-hardening/11-06-SUMMARY.md`.
</output>
