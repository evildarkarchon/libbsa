---
phase: 11-compatibility-warnings-validation-api-and-hardening
plan: 07
type: execute
wave: 3
depends_on:
  - 11-04
files_modified:
  - CMakePresets.json
  - tests/fixtures/README.md
  - tests/unit/validation_policy_tests.cpp
autonomous: true
requirements:
  - COMP-05
  - COMP-06
must_haves:
  truths:
    - "Maintainer has an additive sanitizer-oriented malformed/parser/compression/validation path."
    - "Default Windows MSVC static/shared CI remains unchanged."
    - "Fixture policy documents the sanitizer command path and labels to run."
    - "D-15: sanitizer path is additive and targets malformed, parser, compression, and validation labels."
    - "D-19: default CI and policy tests preserve the TES5Edit read-only boundary."
  artifacts:
    - path: "CMakePresets.json"
      provides: "Additive Clang/GCC sanitizer-oriented configure/build/test presets"
      contains: "linux-clang-asan-ubsan"
    - path: "tests/fixtures/README.md"
      provides: "Documented sanitizer malformed/validation command path"
      contains: "linux-clang-asan-ubsan"
    - path: "tests/unit/validation_policy_tests.cpp"
      provides: "Policy tests preserving default CI and sanitizer path"
      contains: "linux-clang-asan-ubsan"
  key_links:
    - from: "CMakePresets.json"
      to: "tests/fixtures/README.md"
      via: "documented preset command"
      pattern: "linux-clang-asan-ubsan"
---

<objective>
Add the sanitizer-oriented hardening path without changing default Windows CI.

Purpose: Maintainers need a documented ASan/UBSan path for malformed parser, compression, and validation tests where the toolchain supports it.
Output: Additive sanitizer preset/docs plus policy tests preserving static/shared Windows and `TES5Edit` boundaries.
</objective>

<execution_context>
@C:/Users/evild/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/11-compatibility-warnings-validation-api-and-hardening/11-SPEC.md
@.planning/phases/11-compatibility-warnings-validation-api-and-hardening/11-CONTEXT.md
@.planning/phases/11-compatibility-warnings-validation-api-and-hardening/11-RESEARCH.md
@.planning/phases/11-compatibility-warnings-validation-api-and-hardening/11-PATTERNS.md
@CMakePresets.json
@.github/workflows/ci.yml
@tests/fixtures/README.md
@tests/unit/validation_policy_tests.cpp

<interfaces>
Sanitizer preset names to add:
- Configure preset: `linux-clang-asan-ubsan`
- Build preset: `linux-clang-asan-ubsan`
- Test preset: `linux-clang-asan-ubsan`

Documented command path:
```powershell
cmake --preset linux-clang-asan-ubsan
cmake --build --preset linux-clang-asan-ubsan
ctest --preset linux-clang-asan-ubsan -L "malformed|validation|compression" --output-on-failure
```

Default Windows names that must remain:
- `windows-msvc-debug-static`
- `windows-msvc-debug-shared`
</interfaces>
</context>

<decision_trace>
- D-15: The plan adds an additive non-Windows Clang/GCC sanitizer-oriented preset and documented malformed/validation/compression label path.
- D-19: Default CI and policy tests preserve the `TES5Edit/` read-only boundary.
</decision_trace>

<tasks>

<task type="auto">
  <name>Task 1: Add policy tests for sanitizer path and CI preservation</name>
  <read_first>
    - tests/unit/validation_policy_tests.cpp
    - CMakePresets.json
    - .github/workflows/ci.yml
    - tests/fixtures/README.md
  </read_first>
  <files>tests/unit/validation_policy_tests.cpp</files>
  <action>
    Extend `tests/unit/validation_policy_tests.cpp` with a test named `sanitizer validation path is additive and documented`. Assert `CMakePresets.json` contains `linux-clang-asan-ubsan`, `-fsanitize=address,undefined`, and the existing `windows-msvc-debug-static` / `windows-msvc-debug-shared` presets. Assert `.github/workflows/ci.yml` still contains both Windows preset names and `git status --short TES5Edit`. Assert `tests/fixtures/README.md` contains `ctest --preset linux-clang-asan-ubsan -L "malformed|validation|compression" --output-on-failure`. This implements D-15 while preserving D-19.
  </action>
  <verify>
    <automated>cmake --build --preset windows-msvc-debug-static --target libbsa_tests && ctest --preset windows-msvc-debug-static -R "sanitizer validation path|validation_policy" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "sanitizer validation path is additive and documented" tests/unit/validation_policy_tests.cpp` finds the new policy test.
    - `rg -n "linux-clang-asan-ubsan|-fsanitize=address,undefined|windows-msvc-debug-static|windows-msvc-debug-shared" tests/unit/validation_policy_tests.cpp` finds the exact required tokens.
    - RED focused run fails before CMake presets/docs are updated.
  </acceptance_criteria>
  <done>Policy test exists and fails until the additive sanitizer path is present and documented.</done>
</task>

<task type="auto">
  <name>Task 2: Add additive sanitizer preset and fixture documentation</name>
  <read_first>
    - CMakePresets.json
    - tests/fixtures/README.md
    - tests/unit/validation_policy_tests.cpp
    - .github/workflows/ci.yml
  </read_first>
  <files>CMakePresets.json, tests/fixtures/README.md, tests/unit/validation_policy_tests.cpp</files>
  <action>
    Add `linux-clang-asan-ubsan` configure/build/test presets to `CMakePresets.json`. The configure preset must be additive and use Debug, C++20, `$env{VCPKG_ROOT}/scripts/buildsystems/vcpkg.cmake`, `LIBBSA_BUILD_TESTS=ON`, `BUILD_SHARED_LIBS=OFF`, `CMAKE_CXX_COMPILER=clang++`, `CMAKE_CXX_FLAGS_DEBUG=-fsanitize=address,undefined -fno-omit-frame-pointer`, and `CMAKE_EXE_LINKER_FLAGS_DEBUG=-fsanitize=address,undefined`. Add a build preset and test preset with the same name. Do not modify existing Windows static/shared preset names or `.github/workflows/ci.yml`. Update `tests/fixtures/README.md` with the exact documented command path from `<interfaces>`, state it is for supported non-Windows Clang/GCC-style toolchains, and state default Windows MSVC CI remains unchanged.
  </action>
  <verify>
    <automated>cmake --list-presets=all && cmake --build --preset windows-msvc-debug-static --target libbsa_tests && ctest --preset windows-msvc-debug-static -R "sanitizer validation path|validation_policy|public_include_boundary" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "\"name\": \"linux-clang-asan-ubsan\"" CMakePresets.json` finds configure/build/test preset entries.
    - `rg -n "-fsanitize=address,undefined|-fno-omit-frame-pointer|CMAKE_CXX_COMPILER|clang\\+\\+" CMakePresets.json` finds sanitizer compiler/linker settings.
    - `rg -n "ctest --preset linux-clang-asan-ubsan -L \"malformed\\|validation\\|compression\" --output-on-failure" tests/fixtures/README.md` finds the documented command.
    - `rg -n "linux-clang-asan-ubsan" .github/workflows/ci.yml` returns no matches, proving the default CI workflow was not changed.
    - `git status --short TES5Edit` produces no output.
  </acceptance_criteria>
  <done>Additive sanitizer preset and documentation exist while Windows static/shared CI remains unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sanitizer preset -> developer machines | Toolchain-specific flags may fail on unsupported local environments. |
| CI workflow -> default acceptance | Default Windows static/shared jobs must not require sanitizer support. |
| malformed tests -> sanitizer runtime | Untrusted bytes exercise parser/compressor/DDS paths under sanitizer instrumentation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-19 | Denial of Service | sanitizer preset | mitigate | Make the preset additive and documented for supported non-Windows Clang/GCC-style toolchains; do not add it to default Windows CI. |
| T-11-20 | Tampering | CMakePresets.json | mitigate | Policy tests assert existing Windows static/shared presets remain present. |
| T-11-21 | Repudiation | hardening evidence | mitigate | Document the exact CTest label selection `malformed|validation|compression` and machine-check that documentation. |
</threat_model>

<verification>
Run `cmake --list-presets=all`, `cmake --build --preset windows-msvc-debug-static --target libbsa_tests`, `ctest --preset windows-msvc-debug-static -R "sanitizer validation path|validation_policy|public_include_boundary" --output-on-failure`, and `git status --short TES5Edit`.
</verification>

<success_criteria>
- Sanitizer-oriented preset/docs exist and are additive.
- Default Windows MSVC static/shared CI is not changed.
- Policy tests prove the sanitizer path and default CI boundaries.
</success_criteria>

<output>
After completion, create `.planning/phases/11-compatibility-warnings-validation-api-and-hardening/11-07-SUMMARY.md`.
</output>
