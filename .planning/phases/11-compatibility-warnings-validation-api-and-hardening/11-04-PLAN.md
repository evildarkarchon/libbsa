---
phase: 11-compatibility-warnings-validation-api-and-hardening
plan: 04
type: tdd
wave: 2
depends_on:
  - 11-01
files_modified:
  - docs/compatibility-evidence.md
  - tests/unit/validation_policy_tests.cpp
  - tests/fixtures/README.md
autonomous: true
requirements:
  - COMP-01
  - COMP-03
  - COMP-06
must_haves:
  truths:
    - "Maintainer can trace every public warning code to rule and evidence text."
    - "Generated legal fixtures and writer-output archives are the mandatory evidence path."
    - "Optional local game or BSArchPro-derived checks remain skipped by default."
    - "D-12: compatibility evidence catalog is committed and machine-checked."
    - "D-14: optional local game or BSArchPro-derived checks are smoke/compare only and require requires-game-fixture."
    - "D-18: generated legal fixtures and writer-produced archives remain mandatory evidence."
    - "D-19: TES5Edit remains read-only and is not used as a fixture workspace."
  artifacts:
    - path: "docs/compatibility-evidence.md"
      provides: "Human-readable compatibility evidence catalog"
      contains: "compressed_sound_payload"
    - path: "tests/unit/validation_policy_tests.cpp"
      provides: "Machine check that warning codes are cataloged"
      contains: "compatibility-evidence.md"
    - path: "tests/fixtures/README.md"
      provides: "Fixture policy update for compatibility evidence and optional corpus checks"
      contains: "compatibility evidence"
  key_links:
    - from: "include/libbsa/validation.hpp"
      to: "docs/compatibility-evidence.md"
      via: "warning code catalog coverage"
      pattern: "compressed_sound_payload|bsa_embedded_name_compatibility_risk|target_family_mismatch"
---

<objective>
Add a machine-checked compatibility evidence catalog.

Purpose: Compatibility rules should be traceable to generated fixtures, writer-output tests, reference notes, or optional local corpus checks without requiring maintainers to rediscover Phase 11 context.
Output: `docs/compatibility-evidence.md`, policy tests, and fixture documentation updates.
</objective>

<execution_context>
@C:/Users/evild/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/11-compatibility-warnings-validation-api-and-hardening/11-SPEC.md
@.planning/phases/11-compatibility-warnings-validation-api-and-hardening/11-CONTEXT.md
@.planning/phases/11-compatibility-warnings-validation-api-and-hardening/11-RESEARCH.md
@.planning/phases/11-compatibility-warnings-validation-api-and-hardening/11-PATTERNS.md
@include/libbsa/validation.hpp
@tests/unit/validation_policy_tests.cpp
@tests/fixtures/README.md

<interfaces>
Catalog entries must include these exact public warning code tokens:
- `compressed_sound_payload`
- `bsa_embedded_name_compatibility_risk`
- `target_family_mismatch`

Each entry must include:
- Rule: one sentence describing the compatibility rule.
- Evidence: at least one generated fixture, writer-output test name, TES5Edit/BSArchPro read-only reference note, or optional local corpus check.
- Default gate: whether default CTest validates it or whether it is opt-in `requires-game-fixture`.
</interfaces>
</context>

<decision_trace>
- D-12: The plan adds `docs/compatibility-evidence.md` and a machine check for public warning-code coverage.
- D-14: Optional game/BSArchPro-derived checks remain `requires-game-fixture`, skipped by default, and smoke/compare only.
- D-18: Generated legal fixtures and writer-produced archives remain the mandatory evidence path.
- D-19: `TES5Edit/` remains read-only and is not used as a fixture workspace.
</decision_trace>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED catalog coverage check</name>
  <read_first>
    - include/libbsa/validation.hpp
    - tests/unit/validation_policy_tests.cpp
    - docs/compatibility-evidence.md
    - tests/fixtures/README.md
  </read_first>
  <files>tests/unit/validation_policy_tests.cpp</files>
  <behavior>
    - Test fails when `docs/compatibility-evidence.md` is missing.
    - Test fails when a public warning code token is missing from the catalog.
    - Test fails when a catalog entry lacks `Rule:` or `Evidence:` text.
  </behavior>
  <action>
    Extend `tests/unit/validation_policy_tests.cpp` with a test named `compatibility evidence catalog documents public warning codes`. Use the existing `read_text_file` helper pattern. Explicitly list `compressed_sound_payload`, `bsa_embedded_name_compatibility_risk`, and `target_family_mismatch` because C++20 has no enum reflection. Require the catalog to contain each code token, `Rule:`, `Evidence:`, `generated`, and `writer-output` strings. This implements D-12 and D-17 before catalog content exists.
  </action>
  <verify>
    <automated>cmake --build --preset windows-msvc-debug-static --target libbsa_tests && ctest --preset windows-msvc-debug-static -R "compatibility evidence|validation_policy" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "compatibility evidence catalog documents public warning codes" tests/unit/validation_policy_tests.cpp` finds the new test.
    - `rg -n "compressed_sound_payload|bsa_embedded_name_compatibility_risk|target_family_mismatch" tests/unit/validation_policy_tests.cpp` finds the explicit warning-code list.
    - RED focused run fails before `docs/compatibility-evidence.md` exists.
  </acceptance_criteria>
  <done>RED policy check exists and fails on the missing compatibility evidence catalog.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN evidence catalog and fixture policy update</name>
  <read_first>
    - docs/compatibility-evidence.md
    - tests/fixtures/README.md
    - tests/unit/validation_policy_tests.cpp
    - .planning/phases/11-compatibility-warnings-validation-api-and-hardening/11-CONTEXT.md
    - .planning/phases/11-compatibility-warnings-validation-api-and-hardening/11-SPEC.md
  </read_first>
  <files>docs/compatibility-evidence.md, tests/fixtures/README.md, tests/unit/validation_policy_tests.cpp</files>
  <action>
    Create `docs/compatibility-evidence.md` with a concise catalog table for the three warning codes. For `compressed_sound_payload`, cite writer-output warning coverage from `tests/unit/compatibility_warning_tests.cpp` and the PRD known-quirk direction; mark default gate as generated/writer-output. For `bsa_embedded_name_compatibility_risk`, cite TES4-family writer embedded-name output and `archive_reader` extraction proof; mark default gate as writer-output. For `target_family_mismatch`, cite generated BA2 fixture or BA2 writer-output validation with an expected BSA type; mark default gate as generated/writer-output. Include a section stating optional local game or BSArchPro-derived compare checks are smoke/compare only, must use `[requires-game-fixture]`, must skip when `LIBBSA_GAME_FIXTURES` is unset, and must not commit copyrighted bytes. Update `tests/fixtures/README.md` to point to `docs/compatibility-evidence.md` and to describe optional compatibility corpus checks per D-14, D-18, and D-19 without weakening the existing generated/local/TES5Edit policies.
  </action>
  <verify>
    <automated>cmake --build --preset windows-msvc-debug-static --target libbsa_tests && ctest --preset windows-msvc-debug-static -R "compatibility evidence|validation_policy|requires-game-fixture" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "compressed_sound_payload|bsa_embedded_name_compatibility_risk|target_family_mismatch" docs/compatibility-evidence.md` finds all public warning codes.
    - `rg -n "Rule:|Evidence:|generated|writer-output|requires-game-fixture|LIBBSA_GAME_FIXTURES" docs/compatibility-evidence.md` finds required catalog/provenance text.
    - `rg -n "compatibility evidence|docs/compatibility-evidence.md|requires-game-fixture" tests/fixtures/README.md` finds fixture policy updates.
    - `git status --short TES5Edit` produces no output.
  </acceptance_criteria>
  <done>Every public warning code is cataloged with rule and evidence text, and optional local corpus policy remains skip-by-default.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| compatibility evidence -> maintainer decisions | Catalog text influences whether maintainers trust compatibility rules. |
| optional local corpus -> default test suite | Local game/BSArchPro-derived data may exist outside the repository and must not become a required gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-10 | Repudiation | docs/compatibility-evidence.md | mitigate | Machine-check each public warning code token and require `Rule:` plus `Evidence:` text. |
| T-11-11 | Tampering | optional corpus policy | mitigate | Keep optional corpus checks labeled `requires-game-fixture`, skipped without `LIBBSA_GAME_FIXTURES`, and absent from default acceptance. |
| T-11-12 | Information Disclosure | fixture policy | mitigate | Documentation must state copyrighted local data is not committed and `TES5Edit/` is not a fixture workspace. |
</threat_model>

<verification>
Run `cmake --build --preset windows-msvc-debug-static --target libbsa_tests` and `ctest --preset windows-msvc-debug-static -R "compatibility evidence|validation_policy|requires-game-fixture" --output-on-failure`.
</verification>

<success_criteria>
- `docs/compatibility-evidence.md` exists and covers every public warning code.
- Policy tests fail on missing catalog coverage.
- Fixture policy documents mandatory generated evidence, optional local checks, and the `TES5Edit/` boundary.
</success_criteria>

<output>
After completion, create `.planning/phases/11-compatibility-warnings-validation-api-and-hardening/11-04-SUMMARY.md`.
</output>
