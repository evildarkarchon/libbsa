---
phase: 04-tes4-family-bsa-read-and-extract
plan: 02
type: tdd
wave: 2
depends_on: [04-01]
files_modified:
  - src/bsa_reader.cpp
  - src/bsa_reader.hpp
  - tests/bsa_reader_tests.cpp
autonomous: true
requirements: [BSA-01, BSA-02, BSA-03]
must_haves:
  truths:
    - "Consumer can open and list Oblivion v103 BSA metadata."
    - "Consumer can open and list FO3/FNV/Skyrim LE v104 BSA metadata."
    - "Consumer can open and list Skyrim SE/AE v105 BSA metadata."
  artifacts:
    - path: "src/bsa_reader.cpp"
      provides: "TES4-family table parser"
      contains: "open_bsa"
    - path: "tests/bsa_reader_tests.cpp"
      provides: "v103/v104/v105 table parsing tests"
  key_links:
    - from: "src/bsa_reader.cpp"
      to: "src/hash.hpp"
      via: "metadata stores table hashes and normalized paths"
      pattern: "hash_tes4"
---

<objective>
Implement metadata parsing for TES4-family BSA folder, file, and name tables across v103, v104, and v105.

Purpose: Listing, inspection, and lookup must work before extraction can safely read payload bytes.
Output: Passing parser tests with normalized paths and precise metadata for all Phase 04 variants.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-tes4-family-bsa-read-and-extract/04-RESEARCH.md
@include/libbsa/bsa.hpp
@src/bsa_reader.hpp
@src/bsa_reader.cpp
@tests/bsa_reader_tests.cpp
@TES5Edit/Core/wbBSArchive.pas
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing parser behavior tests for v103, v104, and v105</name>
  <files>tests/bsa_reader_tests.cpp</files>
  <read_first>tests/bsa_reader_tests.cpp; include/libbsa/bsa.hpp; src/bsa_reader.hpp; TES5Edit/Core/wbBSArchive.pas lines 1201-1238</read_first>
  <behavior>
    - Test 1: `open_bsa(memory_source(v103_bytes))` returns `archive_format::tes4_bsa`, version `0x67`, one normalized path `meshes/armor/iron.nif`, size `4`, packed_size `4`, and compression `raw`.
    - Test 2: `open_bsa(memory_source(v104_bytes))` returns `archive_format::fo3_bsa`, version `0x68`, and a deflate-capable entry whose actual compression state is derived from XOR rules.
    - Test 3: `open_bsa(memory_source(v105_bytes))` returns `archive_format::sse_bsa`, version `0x69`, parses the v105 24-byte folder record, and lists `textures/actors/hero.dds`.
  </behavior>
  <action>Extend the fixture builder so it emits the complete TES4-family layout: magic/version, 28-byte TES4 header, all folder records, each length-prefixed folder name, each file record `(hash:u64, size:u32, offset:u32)`, the global null-terminated file-name table, and payload bytes at the declared offsets. For v105 folder records, emit `(hash:u64, file_count:u32, unk32:u32, offset:u64)`. Add failing tests for each version using `open_bsa`, `paths`, `entry`, `summary`, and metadata fields. Include explicit assertions for `archive_format::tes4_bsa`, `archive_format::fo3_bsa`, `archive_format::sse_bsa`, versions `0x67/0x68/0x69`, and normalized path strings.</action>
  <verify>
    <automated>cmake --build build/local-vs2026-vcpkg --config Debug; ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug -R libbsa_bsa_reader_tests</automated>
  </verify>
  <acceptance_criteria>
    - `tests/bsa_reader_tests.cpp` contains `open_bsa(memory_source`.
    - `tests/bsa_reader_tests.cpp` contains `archive_format::tes4_bsa`.
    - `tests/bsa_reader_tests.cpp` contains `archive_format::sse_bsa`.
    - The targeted test command fails before implementation and passes after Task 2.
  </acceptance_criteria>
  <done>Parser behavior tests exist for all TES4-family versions and fail for missing implementation before Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Implement bounded TES4-family table parsing</name>
  <files>src/bsa_reader.cpp, src/bsa_reader.hpp</files>
  <read_first>src/bsa_reader.cpp; src/bsa_reader.hpp; include/libbsa/bsa.hpp; src/detect.cpp; src/hash.hpp; TES5Edit/Core/wbBSArchive.pas lines 255-284 and 1201-1238</read_first>
  <action>Implement `open_bsa` for versions `0x67`, `0x68`, and `0x69`. Read little-endian integers through bounded helpers that call `byte_source::read_at` and fail with `{error_code::malformed_archive, "truncated BSA table"}` for unsatisfied reads. Validate `FoldersOffset`, folder count, file count, folder-name/file-name table traversal, and payload offsets against `source.size()`. Build full paths as `folder_name + "/" + file_name`, normalize via `archive_view`, and store copied `entry_metadata` with `name_hash`, `directory_hash`, `offset`, `packed_size`, and logical table size. Derive actual compression state using `(flags & 0x0004) XOR (raw_size & 0x40000000)`: compressed v103/v104 = `deflate`, compressed v105 = `lz4_frame`, uncompressed = `raw`. Mask `0x40000000` out of packed size before storing. Return `unsupported_format` with message `unsupported BSA version` for any BSA version outside `0x67`, `0x68`, `0x69`.</action>
  <verify>
    <automated>cmake --build build/local-vs2026-vcpkg --config Debug; ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug -R libbsa_bsa_reader_tests</automated>
  </verify>
  <acceptance_criteria>
    - `src/bsa_reader.cpp` contains `unsupported BSA version`.
    - `src/bsa_reader.cpp` contains `0x40000000` or `file_size_compress`.
    - `src/bsa_reader.cpp` contains `compression_state::lz4_frame`.
    - `src/bsa_reader.cpp` contains `compression_state::deflate`.
    - Targeted BSA reader tests pass.
  </acceptance_criteria>
  <done>`open_bsa` returns correct summaries, paths, lookup metadata, and compression states for v103/v104/v105 fixtures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| archive bytes -> table parser | Untrusted BSA table bytes become trusted metadata only after bounds checks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | D | `open_bsa` | mitigate | Bound folder/file counts and every read against `byte_source::size()` before allocating or reading. |
| T-04-04 | T | compression metadata | mitigate | Mask `FILE_SIZE_COMPRESS` and use XOR with `ARCHIVE_COMPRESS` exactly as reference behavior. |
</threat_model>

<verification>
Targeted BSA parser tests pass for all versions.
</verification>

<success_criteria>
- `open_bsa` opens and lists v103/v104/v105 synthetic archives.
- Metadata uses normalized paths and structured failures.
</success_criteria>

<output>
After completion, create `.planning/phases/04-tes4-family-bsa-read-and-extract/04-02-SUMMARY.md`.
</output>
