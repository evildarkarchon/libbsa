---
phase: 04-tes4-family-bsa-read-and-extract
plan: 05
type: execute
wave: 5
depends_on: [04-04]
files_modified:
  - tests/public_header_smoke.cpp
  - README.md
autonomous: true
requirements: [BSA-01, BSA-02, BSA-03, BSA-05]
must_haves:
  truths:
    - "Consumer-style smoke coverage includes the TES4-family BSA public API."
    - "Maintainer can run documented Phase 04 validation commands."
    - "Public headers and TES5Edit boundary remain clean after BSA implementation."
  artifacts:
    - path: "tests/public_header_smoke.cpp"
      provides: "Consumer-style BSA API smoke usage"
    - path: "README.md"
      provides: "Phase 04 BSA read/extract documentation and validation commands"
  key_links:
    - from: "tests/public_header_smoke.cpp"
      to: "include/libbsa/bsa.hpp"
      via: "consumer include and API construction"
      pattern: "libbsa/bsa.hpp"
---

<objective>
Finalize Phase 04 with public-header smoke coverage, README documentation, and boundary validation gates.

Purpose: BSA read/extract behavior must be visible to consumers and protected by final verification commands before execution is considered complete.
Output: Updated smoke test, README section, and passing full validation gates.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-tes4-family-bsa-read-and-extract/04-RESEARCH.md
@include/libbsa/bsa.hpp
@tests/public_header_smoke.cpp
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand public-header smoke for BSA API</name>
  <files>tests/public_header_smoke.cpp</files>
  <read_first>tests/public_header_smoke.cpp; include/libbsa/bsa.hpp; include/libbsa/io.hpp</read_first>
  <action>Add `#include &lt;libbsa/bsa.hpp&gt;` to `tests/public_header_smoke.cpp`. Exercise the public API without reading real archive files: construct a `bsa_archive` from an `archive_summary` and one `entry_metadata`, call `paths()`, `contains("meshes\\armor\\iron.nif")`, and `entry("meshes/armor/iron.nif")`; keep the existing return expression and extend it so success requires the BSA path lookup to work. Do not include private headers or implementation-library tokens.</action>
  <verify>
    <automated>cmake --build build/local-vs2026-vcpkg --config Debug; ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug -R libbsa.public_header_smoke</automated>
  </verify>
  <acceptance_criteria>
    - `tests/public_header_smoke.cpp` contains `#include &lt;libbsa/bsa.hpp&gt;` or `#include <libbsa/bsa.hpp>`.
    - `tests/public_header_smoke.cpp` contains `libbsa::bsa_archive`.
    - `tests/public_header_smoke.cpp` contains `contains("meshes`.
    - Public-header smoke test passes.
  </acceptance_criteria>
  <done>Consumer-style smoke coverage uses the BSA API through public headers only.</done>
</task>

<task type="auto">
  <name>Task 2: Document TES4-family BSA read/extract support</name>
  <files>README.md</files>
  <read_first>README.md; .planning/phases/04-tes4-family-bsa-read-and-extract/04-RESEARCH.md; include/libbsa/bsa.hpp</read_first>
  <action>Add a README section `## TES4-family BSA read and extract` after the Compression services section. State exactly that Phase 04 supports opening, listing, lookup, metadata inspection, and extraction for Oblivion v103, FO3/FNV/Skyrim LE v104, and Skyrim SE/AE v105 BSA archives; uses `byte_source` and `byte_sink`; handles `ARCHIVE_COMPRESS` XOR `FILE_SIZE_COMPRESS`; routes v103/v104 compressed payloads to deflate and v105 compressed payloads to LZ4 frame; skips v104/v105 embedded filename prefixes before output; and keeps `TES5Edit/` read-only. Include validation commands: full build, `ctest ... -R libbsa_bsa_reader_tests`, `ctest ... -L fixture`, public-header smoke, public-header forbidden-token `rg`, CMake GLOB gate, and `git status --short TES5Edit`.</action>
  <verify>
    <automated>ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug -R libbsa.public_header_smoke; rg -n "TES4-family BSA read and extract|ARCHIVE_COMPRESS|FILE_SIZE_COMPRESS|libbsa_bsa_reader_tests" README.md</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` contains `## TES4-family BSA read and extract`.
    - `README.md` contains `Oblivion v103`.
    - `README.md` contains `FO3/FNV/Skyrim LE v104`.
    - `README.md` contains `Skyrim SE/AE v105`.
    - `README.md` contains `ARCHIVE_COMPRESS` and `FILE_SIZE_COMPRESS`.
  </acceptance_criteria>
  <done>README documents the completed BSA reader/extractor behavior and exact validation commands.</done>
</task>

<task type="auto">
  <name>Task 3: Run final Phase 04 gates</name>
  <files>tests/public_header_smoke.cpp, README.md</files>
  <read_first>README.md; CMakeLists.txt; include/libbsa/bsa.hpp; tests/bsa_reader_tests.cpp</read_first>
  <action>Run final validation commands exactly: `cmake --build build/local-vs2026-vcpkg --config Debug`; `ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug`; `ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug -R libbsa_bsa_reader_tests`; `ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug -L fixture`; `ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug -R libbsa.public_header_smoke`; `rg -n "libdeflate|lz4\.h|lz4frame\.h|LZ4|DirectXTex|TES5Edit" include/libbsa`; `rg -n "^[^#]*\b(GLOB|GLOB_RECURSE)\b" CMakeLists.txt`; `git status --short TES5Edit`. The public-header `rg` gate must return no matches; if it returns documentation comments in public headers, remove only the offending public-header implementation-leak wording and mention the comment rewrite in the summary.</action>
  <verify>
    <automated>cmake --build build/local-vs2026-vcpkg --config Debug; ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug; ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug -R libbsa_bsa_reader_tests; ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug -L fixture; ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug -R libbsa.public_header_smoke; rg -n "libdeflate|lz4\.h|lz4frame\.h|LZ4|DirectXTex|TES5Edit" include/libbsa; rg -n "^[^#]*\b(GLOB|GLOB_RECURSE)\b" CMakeLists.txt; git status --short TES5Edit</automated>
  </verify>
  <acceptance_criteria>
    - Full CTest passes.
    - BSA reader tests pass.
    - Fixture label tests pass.
    - Public-header smoke passes.
    - Public-header forbidden-token gate returns no matches.
    - CMake non-comment GLOB gate returns no matches.
    - `git status --short TES5Edit` returns no output.
  </acceptance_criteria>
  <done>All final Phase 04 validation gates pass and no extra source changes are needed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public API -> consumer | Consumers compile only against stable libbsa-owned headers. |
| repository -> TES5Edit submodule | Reference submodule must remain unchanged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-11 | I | public headers | mitigate | Run forbidden-token gate over `include/libbsa`. |
| T-04-12 | T | TES5Edit boundary | mitigate | Run `git status --short TES5Edit` and keep output empty. |
</threat_model>

<verification>
Full build, full CTest, BSA reader tests, fixture label, public-header smoke, dependency-token gate, CMake glob gate, and TES5Edit status gate all pass.
</verification>

<success_criteria>
- README and smoke coverage reflect Phase 04 behavior.
- Final validation gates pass.
</success_criteria>

<output>
After completion, create `.planning/phases/04-tes4-family-bsa-read-and-extract/04-05-SUMMARY.md`.
</output>
