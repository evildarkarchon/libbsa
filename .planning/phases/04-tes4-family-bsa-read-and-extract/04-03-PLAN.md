---
phase: 04-tes4-family-bsa-read-and-extract
plan: 03
type: tdd
wave: 3
depends_on: [04-02]
files_modified:
  - include/libbsa/archive.hpp
  - src/bsa_reader.cpp
  - src/bsa_reader.hpp
  - tests/bsa_reader_tests.cpp
autonomous: true
requirements: [BSA-01, BSA-02, BSA-03, BSA-05]
must_haves:
  truths:
    - "Consumer can extract raw TES4-family BSA entries to a caller-owned sink."
    - "Consumer can extract deflate-compressed v103/v104 entries."
    - "Consumer can extract LZ4-frame v105 entries."
    - "Consumer can extract entries with embedded filenames while preserving payload bytes after the embedded-name prefix."
  artifacts:
    - path: "src/bsa_reader.cpp"
      provides: "BSA extraction implementation"
      contains: "extract_bsa_entry"
    - path: "tests/bsa_reader_tests.cpp"
      provides: "Raw, compressed, and embedded-name extraction tests"
  key_links:
    - from: "src/bsa_reader.cpp"
      to: "include/libbsa/compression.hpp"
      via: "resolve_payload_codec and decompress_payload"
      pattern: "decompress_payload"
---

<objective>
Implement TES4-family BSA payload extraction through existing streaming sink and compression dispatcher APIs.

Purpose: Phase 04 is not complete until listed entries can be extracted correctly, including compression and embedded-name payload prefixes.
Output: Passing extraction tests for raw, deflate, LZ4-frame, and embedded-name entries.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-tes4-family-bsa-read-and-extract/04-RESEARCH.md
@include/libbsa/bsa.hpp
@include/libbsa/compression.hpp
@src/bsa_reader.cpp
@tests/bsa_reader_tests.cpp
@TES5Edit/Core/wbBSArchive.pas
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add extraction behavior tests for raw, compressed, and embedded entries</name>
  <files>tests/bsa_reader_tests.cpp</files>
  <read_first>tests/bsa_reader_tests.cpp; include/libbsa/bsa.hpp; include/libbsa/compression.hpp; TES5Edit/Core/wbBSArchive.pas lines 2094-2149</read_first>
  <behavior>
    - Test 1: extracting a raw v103 entry writes exact bytes `{0x10, 0x20, 0x30, 0x40}` to `memory_sink`.
    - Test 2: extracting a compressed v104 entry whose payload contains `u32 uncompressed_size` followed by deflate bytes writes the original unpacked bytes.
    - Test 3: extracting a compressed v105 entry routes through LZ4 frame and writes the original unpacked bytes.
    - Test 4: extracting a v104 or v105 embedded-name entry skips one-byte length plus name bytes before reading raw or compressed payload data.
  </behavior>
  <action>Extend fixture builders so payload bytes can be raw, deflate-compressed via existing `compress_payload(compression_algorithm::deflate, ...)`, or LZ4-frame compressed via `compress_payload(compression_algorithm::lz4_frame, ...)`. For compressed fixtures, write optional embedded-name prefix first, then little-endian `u32` uncompressed size, then compressed bytes. Add tests calling `open_bsa`, `extract_bsa_entry(archive, source, path, sink)`, and `sink.bytes()` assertions. Include explicit embedded name `meshes\\armor\\iron.nif` and assert the output excludes that prefix.</action>
  <verify>
    <automated>cmake --build build/local-vs2026-vcpkg --config Debug; ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug -R libbsa_bsa_reader_tests</automated>
  </verify>
  <acceptance_criteria>
    - `tests/bsa_reader_tests.cpp` contains `extract_bsa_entry`.
    - `tests/bsa_reader_tests.cpp` contains `compression_algorithm::deflate`.
    - `tests/bsa_reader_tests.cpp` contains `compression_algorithm::lz4_frame`.
    - `tests/bsa_reader_tests.cpp` contains `ARCHIVE_EMBEDNAME`.
  </acceptance_criteria>
  <done>Extraction behavior tests cover all required payload modes and initially fail until implementation is complete.</done>
</task>

<task type="auto">
  <name>Task 2: Implement extraction with prefix handling and dispatcher routing</name>
  <files>include/libbsa/archive.hpp, src/bsa_reader.cpp, src/bsa_reader.hpp</files>
  <read_first>include/libbsa/archive.hpp; src/bsa_reader.cpp; src/bsa_reader.hpp; include/libbsa/compression.hpp; TES5Edit/Core/wbBSArchive.pas lines 812-819 and 2121-2149</read_first>
  <action>Implement `extract_bsa_entry` by looking up the normalized path in `bsa_archive`, reading the bounded payload range from `byte_source`, and writing final bytes to `byte_sink`. Add internal metadata needed to remember actual table offset, packed byte count, archive flags, format, and whether embedded names are enabled without exposing implementation headers. If public `entry_metadata` needs one extra libbsa-owned field, add `std::uint64_t stored_size{}` with a Doxygen comment explaining it is the packed on-disk record span before embedded/compression prefixes; preserve existing fields. For v104/v105 with `ARCHIVE_EMBEDNAME`, read the first byte as `name_length`, skip `1 + name_length`, and fail with `{error_code::malformed_archive, "truncated embedded BSA name"}` if the prefix exceeds the record span. For compressed entries, read a little-endian `u32` expected output size after the embedded prefix, pass remaining bytes to `resolve_payload_codec` and `decompress_payload`, then write decompressed bytes. For raw entries, pass the post-embedded bytes through `decompress_payload(compression_algorithm::none, ..., expected_size)` or equivalent exact-size logic, then write them. Return sink write failures unchanged.</action>
  <verify>
    <automated>cmake --build build/local-vs2026-vcpkg --config Debug; ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug -R libbsa_bsa_reader_tests</automated>
  </verify>
  <acceptance_criteria>
    - `src/bsa_reader.cpp` contains `truncated embedded BSA name`.
    - `src/bsa_reader.cpp` contains `resolve_payload_codec`.
    - `src/bsa_reader.cpp` contains `decompress_payload`.
    - `include/libbsa/archive.hpp` has Doxygen for any added public field.
    - Targeted BSA reader tests pass.
  </acceptance_criteria>
  <done>Extraction writes exact output bytes for raw, deflate, LZ4-frame, and embedded-name entries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| archive payload -> sink | Untrusted payload bytes become caller-visible output bytes. |
| compressed payload -> codec | Archive-declared sizes guide decompression. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | T | embedded-name skip | mitigate | Validate length prefix fits within record span before skipping. |
| T-04-06 | D | decompression | mitigate | Enforce exact expected output size through `decompress_payload`. |
| T-04-07 | I | extraction sink | mitigate | Write only post-prefix logical payload bytes, never table or embedded-name bytes. |
</threat_model>

<verification>
Run targeted BSA extraction tests and codec-related fixture tests.
</verification>

<success_criteria>
- Raw, deflate, LZ4-frame, and embedded-name extraction tests pass.
- Extraction uses existing compression dispatcher, not native codec headers.
</success_criteria>

<output>
After completion, create `.planning/phases/04-tes4-family-bsa-read-and-extract/04-03-SUMMARY.md`.
</output>
