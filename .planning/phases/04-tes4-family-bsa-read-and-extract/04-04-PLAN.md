---
phase: 04-tes4-family-bsa-read-and-extract
plan: 04
type: tdd
wave: 4
depends_on: [04-03]
files_modified:
  - src/bsa_reader.cpp
  - tests/bsa_reader_tests.cpp
autonomous: true
requirements: [BSA-01, BSA-02, BSA-03, BSA-05]
must_haves:
  truths:
    - "Malformed TES4-family BSA archives fail with structured errors instead of crashes."
    - "Compatibility-sensitive BSA compression and embedded-name behavior is locked by tests."
  artifacts:
    - path: "tests/bsa_reader_tests.cpp"
      provides: "Malformed and compatibility regression tests"
    - path: "src/bsa_reader.cpp"
      provides: "Structured parser/extractor failure handling"
  key_links:
    - from: "tests/bsa_reader_tests.cpp"
      to: "TES5Edit/Core/wbBSArchive.pas"
      via: "inline provenance comments for compression and embedded-name behavior"
      pattern: "wbBSArchive.pas"
---

<objective>
Harden TES4-family BSA parsing and extraction against malformed data and lock reference compatibility edge cases.

Purpose: Archive bytes are untrusted; compatibility claims require explicit tests for the places most likely to regress.
Output: Passing malformed-input and compatibility tests with provenance comments.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-tes4-family-bsa-read-and-extract/04-RESEARCH.md
@src/bsa_reader.cpp
@tests/bsa_reader_tests.cpp
@TES5Edit/Core/wbBSArchive.pas
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add malformed archive and compatibility regression tests</name>
  <files>tests/bsa_reader_tests.cpp</files>
  <read_first>tests/bsa_reader_tests.cpp; src/bsa_reader.cpp; TES5Edit/Core/wbBSArchive.pas lines 802-819, 1201-1238, 2121-2149</read_first>
  <behavior>
    - Test 1: unsupported BSA version `0x6a` returns `error_code::unsupported_format` and message `unsupported BSA version`.
    - Test 2: folder record range past source size returns `error_code::malformed_archive` and message `truncated BSA table`.
    - Test 3: payload offset plus stored size past source size returns `error_code::malformed_archive` and message `BSA payload range exceeds source size`.
    - Test 4: archive default compression plus clear file flag and archive raw plus set file flag both produce compressed extraction through XOR behavior.
    - Test 5: embedded-name prefix longer than record span returns `truncated embedded BSA name`.
  </behavior>
  <action>Add Catch2 sections for malformed headers, malformed tables, impossible payload ranges, compression XOR inversion, and embedded-name truncation. Place short provenance comments next to the XOR and embedded-name tests: `// Reference: TES5Edit/Core/wbBSArchive.pas TwbBSFileTES4.Compressed` and `// Reference: TES5Edit/Core/wbBSArchive.pas ExtractFileData embedded-name skip`. Keep fixtures in-memory and deterministic; do not read files from `TES5Edit/` at test runtime.</action>
  <verify>
    <automated>cmake --build build/local-vs2026-vcpkg --config Debug; ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug -R libbsa_bsa_reader_tests</automated>
  </verify>
  <acceptance_criteria>
    - `tests/bsa_reader_tests.cpp` contains `unsupported BSA version`.
    - `tests/bsa_reader_tests.cpp` contains `BSA payload range exceeds source size`.
    - `tests/bsa_reader_tests.cpp` contains `TwbBSFileTES4.Compressed`.
    - `tests/bsa_reader_tests.cpp` contains `ExtractFileData embedded-name skip`.
  </acceptance_criteria>
  <done>Malformed and compatibility tests exist and fail before any missing hardening is implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Implement structured hardening paths</name>
  <files>src/bsa_reader.cpp</files>
  <read_first>src/bsa_reader.cpp; tests/bsa_reader_tests.cpp; include/libbsa/result.hpp</read_first>
  <action>Update parser and extractor error paths to satisfy the tests exactly: use `{error_code::unsupported_format, "unsupported BSA version"}` for unsupported versions; `{error_code::malformed_archive, "truncated BSA table"}` for incomplete table/name reads; `{error_code::malformed_archive, "BSA payload range exceeds source size"}` when `offset + stored_size` exceeds `byte_source::size()` or overflows; `{error_code::malformed_archive, "truncated embedded BSA name"}` when embedded prefix length exceeds record span. Add overflow-safe helper checks before additions. Preserve successful behavior from Plans 02 and 03.</action>
  <verify>
    <automated>cmake --build build/local-vs2026-vcpkg --config Debug; ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug -R libbsa_bsa_reader_tests</automated>
  </verify>
  <acceptance_criteria>
    - `src/bsa_reader.cpp` contains `BSA payload range exceeds source size`.
    - `src/bsa_reader.cpp` contains `truncated BSA table`.
    - `src/bsa_reader.cpp` contains an overflow-safe bounds helper or equivalent explicit overflow check.
    - Targeted BSA reader tests pass.
  </acceptance_criteria>
  <done>Malformed archives produce stable structured failures and all BSA reader tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| malformed archive -> parser | Attacker-controlled offsets/counts/sizes attempt to trigger OOB reads or large allocations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08 | D | table parser | mitigate | Add tests and checks for truncated tables and bounded counts before allocation. |
| T-04-09 | E | payload offsets | mitigate | Use overflow-safe range validation before every payload read. |
| T-04-10 | R | compatibility behavior | mitigate | Provenance comments tie XOR and embedded-name tests to TES5Edit reference lines. |
</threat_model>

<verification>
Targeted malformed and compatibility tests pass.
</verification>

<success_criteria>
- Unsupported versions, truncated tables, impossible payload ranges, XOR compression cases, and embedded-name truncation are tested and handled.
</success_criteria>

<output>
After completion, create `.planning/phases/04-tes4-family-bsa-read-and-extract/04-04-SUMMARY.md`.
</output>
