---
phase: 07-ba2-dds-read-and-dds-reconstruction
plan: 05
type: tdd
wave: 4
depends_on: [07-03, 07-04]
files_modified:
  - src/ba2_reader.cpp
  - src/texture/dds_reconstruction.cpp
  - src/texture/dds_validation.cpp
  - tests/ba2_dds_reader_tests.cpp
autonomous: true
requirements: [BA2-05, BA2-06]
must_haves:
  truths:
    - "D-10: extraction validates, decompresses, reconstructs, and validates the full DDS byte vector before any sink write."
    - "D-11: DX10 chunk codec routing is per chunk from archive variant, chunk sizes, and CompressionMethod."
    - "D-12: unsupported or inconsistent chunk codec metadata fails structurally with no fallback codec guessing."
    - "D-14/D-15/D-16: reconstructed DDS bytes are validated privately before sink writes where practical."
    - "D-22/D-23: positive extracted DDS fixtures are DirectXTex-validated for semantic metadata/layout equivalence."
---

<objective>
Implement BA2 DX10 single-entry extraction with complete DDS reconstruction and per-chunk codec routing.

Purpose: Turn parsed texture metadata and chunk payloads into valid, loadable DDS output while preserving no-partial-write guarantees.
Output: `extract_ba2_entry` supports generated FO4 and Starfield DX10 texture fixtures.
</objective>

<decision_coverage>
- D-10 through D-16, D-22, and D-23 are implemented in the extraction path.
</decision_coverage>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
@C:/Users/evild/.config/opencode/get-shit-done/references/tdd.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/07-ba2-dds-read-and-dds-reconstruction/07-SPEC.md
@.planning/phases/07-ba2-dds-read-and-dds-reconstruction/07-CONTEXT.md
@.planning/phases/07-ba2-dds-read-and-dds-reconstruction/07-RESEARCH.md
@include/libbsa/ba2.hpp
@include/libbsa/compression.hpp
@src/ba2_reader.cpp
@src/compression.cpp
@src/texture/dds_reconstruction.hpp
@src/texture/dds_validation.hpp
@tests/ba2_dds_fixture_helpers.hpp
@tests/ba2_dds_reader_tests.cpp
</context>

<feature>
  <name>BA2 DX10 extraction</name>
  <files>src/ba2_reader.cpp, tests/ba2_dds_reader_tests.cpp</files>
  <behavior>
    - FO4 DX10 deflate fixtures extract to DirectXTex-loadable DDS bytes with expected metadata.
    - Starfield DX10 v3 method-3 LZ4-block fixtures extract to DirectXTex-loadable DDS bytes with expected metadata.
    - Raw/unpacked chunks remain raw.
    - Lookup failures, codec failures, validation failures, and later-chunk failures leave the caller sink empty.
  </behavior>
  <implementation>When archive summary format is DX10, lookup generic and texture metadata, read each chunk by validated range, resolve per-chunk codec state, decompress into ordered payload bytes, reconstruct DDS, validate with private DirectXTex helper, then write to sink once. Keep GNRL extraction behavior unchanged.</implementation>
</feature>

<tasks>

<task type="auto">
  <name>Task 1 RED: Add BA2 DX10 extraction tests</name>
  <read_first>tests/ba2_dds_reader_tests.cpp, tests/ba2_dds_fixture_helpers.hpp, tests/ba2_reader_tests.cpp</read_first>
  <files>tests/ba2_dds_reader_tests.cpp</files>
  <action>Add failing extraction tests for FO4 deflate chunk, Starfield v3 method-3 LZ4-block chunk, raw chunk, one-mip, multi-mip, and cubemap/array fixtures. Each positive test must call `extract_ba2_entry`, validate output through the private DirectXTex test helper, and assert expected metadata. Add a no-partial-write test where a later chunk or validation failure occurs after earlier chunks would have decompressed.</action>
  <verify>
    <automated>ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug -R libbsa_ba2_dds_reader_tests</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "FO4.*deflate|Starfield.*LZ4|raw chunk|no partial|LoadFromDDSMemory|extract_ba2_entry" tests/ba2_dds_reader_tests.cpp` prints all patterns.
    - RED run fails because DX10 extraction is not implemented, not because metadata parsing is broken.
  </acceptance_criteria>
  <done>RED commit `test(07-05): add failing BA2 DDS extraction tests` exists.</done>
</task>

<task type="auto">
  <name>Task 2 GREEN/REFACTOR: Implement DX10 extraction assembly</name>
  <read_first>src/ba2_reader.cpp, src/compression.cpp, src/texture/dds_reconstruction.hpp, src/texture/dds_validation.hpp, tests/ba2_dds_reader_tests.cpp</read_first>
  <files>src/ba2_reader.cpp, src/texture/dds_reconstruction.cpp, src/texture/dds_validation.cpp, tests/ba2_dds_reader_tests.cpp</files>
  <action>Extend `extract_ba2_entry` with DX10 extraction dispatch. Read and validate each chunk range, route codecs per chunk, append decompressed chunk bytes in mip/chunk order, call DDS reconstruction, validate reconstructed DDS, and call `sink.write` only after all prior steps succeed. Add comments for the no-partial-write boundary and Starfield raw LZ4 block route. Preserve GNRL tests.</action>
  <verify>
    <automated>ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug -R "libbsa_ba2_dds_reader_tests|libbsa_ba2_reader_tests|libbsa_dds_reconstruction_tests"</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "resolve_payload_codec|decompress_payload|reconstruct_dds|validate_dds|sink.write" src/ba2_reader.cpp` prints all patterns.
    - `ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug -R "libbsa_ba2_dds_reader_tests|libbsa_ba2_reader_tests|libbsa_dds_reconstruction_tests"` passes.
  </acceptance_criteria>
  <done>GREEN commit `feat(07-05): extract BA2 DX10 textures as DDS` exists.</done>
</task>

</tasks>

<verification>
Run BA2 DDS, BA2 GNRL, and DDS reconstruction focused tests.
</verification>

<success_criteria>
- Supported DX10 fixtures extract to DirectXTex-loadable DDS bytes.
- Codec confusion and no-partial-write boundaries are enforced.
- GNRL extraction remains green.
</success_criteria>

<output>
After completion, create `.planning/phases/07-ba2-dds-read-and-dds-reconstruction/07-05-SUMMARY.md`.
</output>

