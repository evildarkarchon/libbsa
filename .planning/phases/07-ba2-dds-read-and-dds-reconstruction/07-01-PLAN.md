---
phase: 07-ba2-dds-read-and-dds-reconstruction
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - include/libbsa/ba2.hpp
  - src/ba2_reader.cpp
  - tests/public_header_smoke.cpp
  - CMakeLists.txt
autonomous: true
requirements: [BA2-05, BA2-06]
must_haves:
  truths:
    - "D-01: ba2_archive exposes a dedicated texture_metadata(path) API instead of adding texture fields to generic entry_metadata."
    - "D-02: texture_metadata(path) returns result<texture_metadata> and fails structurally when texture metadata is unavailable."
    - "D-03: texture_metadata includes dimensions, dxgi_format, mip count, array size, cubemap state, and chunk summaries."
    - "D-04: texture_metadata is returned by value."
    - "D-05/D-06/D-07: public dxgi_format is libbsa-owned, preserves the raw numeric DXGI value, and supports known-name lookup."
    - "D-08: unknown DXGI values stay inspectable; unsupported reconstruction fails during extraction, not open."
  artifacts:
    - path: "include/libbsa/ba2.hpp"
      provides: "Public BA2 texture metadata API"
      contains: "texture_metadata"
    - path: "tests/public_header_smoke.cpp"
      provides: "Public-header dependency leakage smoke coverage"
      contains: "dxgi_format"
---

<objective>
Add the Phase 7 public texture metadata surface with TDD smoke coverage.

Purpose: Lock the consumer-facing API before parser and extraction work depends on it.
Output: Public libbsa-owned `texture_metadata`, `texture_chunk_metadata`, `dxgi_format`, `dxgi_format_name`, and `ba2_archive::texture_metadata(path)` API.
</objective>

<decision_coverage>
- D-01 through D-08 are directly implemented by this plan.
</decision_coverage>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
@C:/Users/evild/.config/opencode/get-shit-done/references/tdd.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/07-ba2-dds-read-and-dds-reconstruction/07-SPEC.md
@.planning/phases/07-ba2-dds-read-and-dds-reconstruction/07-CONTEXT.md
@.planning/phases/07-ba2-dds-read-and-dds-reconstruction/07-RESEARCH.md
@.planning/phases/07-ba2-dds-read-and-dds-reconstruction/07-PATTERNS.md
@include/libbsa/ba2.hpp
@include/libbsa/archive.hpp
@src/ba2_reader.cpp
@tests/public_header_smoke.cpp
@CMakeLists.txt
</context>

<feature>
  <name>BA2 texture metadata public API</name>
  <files>include/libbsa/ba2.hpp, src/ba2_reader.cpp, tests/public_header_smoke.cpp</files>
  <behavior>
    - Consumer code can construct and inspect `dxgi_format` without including DXGI or DirectXTex headers.
    - Consumer code can name `texture_metadata`, `texture_chunk_metadata`, and `ba2_archive::texture_metadata`.
    - `texture_metadata(path)` returns `result<texture_metadata>` by value.
    - Unknown DXGI numeric values are preserved by `dxgi_format`.
  </behavior>
  <implementation>Add public value types with Doxygen comments. Store copied texture metadata in `ba2_archive` alongside the existing `archive_view`, add a constructor overload or compatible constructor path as needed, and implement lookup by normalized archive path. If the archive has no texture metadata for a path, return a structured failure rather than `std::optional`.</implementation>
</feature>

<tasks>

<task type="auto">
  <name>Task 1 RED: Add public texture metadata smoke expectations</name>
  <read_first>tests/public_header_smoke.cpp, include/libbsa/ba2.hpp</read_first>
  <files>tests/public_header_smoke.cpp</files>
  <action>Extend `tests/public_header_smoke.cpp` to include and use the Phase 7 public API before it exists. Construct a `libbsa::dxgi_format` from a raw value, call `libbsa::dxgi_format_name`, construct a `libbsa::texture_chunk_metadata`, construct a `libbsa::texture_metadata`, create a `libbsa::ba2_archive` with texture metadata, call `ba2.texture_metadata(\"textures/interface/lut.dds\")`, and assert the returned metadata includes the raw format value, dimensions, mip count, array size, cubemap state, and one chunk summary. Also verify the lookup failure path for a non-texture path returns a structured error. Run the public header smoke test and confirm RED fails because the API is missing.</action>
  <verify>
    <automated>ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug -R libbsa.public_header_smoke</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "dxgi_format|dxgi_format_name|texture_chunk_metadata|texture_metadata\\(" tests/public_header_smoke.cpp` prints all four patterns.
    - RED run fails for missing Phase 7 API symbols, not CMake syntax or private dependency includes.
  </acceptance_criteria>
  <done>RED commit `test(07-01): add failing public texture metadata smoke` exists.</done>
</task>

<task type="auto">
  <name>Task 2 GREEN/REFACTOR: Implement public texture metadata API</name>
  <read_first>include/libbsa/ba2.hpp, src/ba2_reader.cpp, include/libbsa/archive_path.hpp, include/libbsa/result.hpp</read_first>
  <files>include/libbsa/ba2.hpp, src/ba2_reader.cpp, tests/public_header_smoke.cpp, CMakeLists.txt</files>
  <action>Add `dxgi_format`, `texture_chunk_metadata`, and `texture_metadata` public structs with Doxygen comments. Add `dxgi_format_name(dxgi_format)` returning a libbsa-owned known label when available. Extend `ba2_archive` construction/storage so it can own copied texture metadata and expose `result<texture_metadata> texture_metadata(std::string path) const`. Keep all public headers free of DirectXTex, DXGI, Windows, libdeflate, LZ4, Delphi, and TES5Edit includes. Update explicit CMake header/source lists only if a new public header is introduced.</action>
  <verify>
    <automated>cmake --build build/local-vs2026-vcpkg --config Debug && ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug -R libbsa.public_header_smoke</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "struct dxgi_format|struct texture_chunk_metadata|struct texture_metadata|texture_metadata\\(" include/libbsa/ba2.hpp` prints all four patterns.
    - `rg -n "DirectXTex|DXGI_FORMAT|Windows\\.h|libdeflate|lz4|TES5Edit" include/libbsa` prints no matches.
    - `ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug -R libbsa.public_header_smoke` passes.
  </acceptance_criteria>
  <done>GREEN commit `feat(07-01): expose BA2 texture metadata API` exists, with optional refactor commit if storage cleanup is needed.</done>
</task>

</tasks>

<verification>
Run the public-header smoke test and the public-header dependency leakage grep from the acceptance criteria.
</verification>

<success_criteria>
- Public texture metadata API compiles from consumer-style code.
- Generic `entry_metadata` remains free of texture-only fields.
- Public headers leak no private texture, codec, platform, or TES5Edit dependencies.
</success_criteria>

<output>
After completion, create `.planning/phases/07-ba2-dds-read-and-dds-reconstruction/07-01-SUMMARY.md`.
</output>

