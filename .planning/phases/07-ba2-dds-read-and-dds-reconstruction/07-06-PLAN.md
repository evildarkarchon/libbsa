---
phase: 07-ba2-dds-read-and-dds-reconstruction
plan: 06
type: tdd
wave: 5
depends_on: [07-03, 07-05]
files_modified:
  - tests/ba2_dds_reader_tests.cpp
  - src/ba2_reader.cpp
  - src/texture/dds_reconstruction.cpp
  - README.md
autonomous: true
requirements: [BA2-05, BA2-06]
must_haves:
  truths:
    - "D-12/D-16: malformed DX10 extraction and reconstruction failures return structured errors with no partial writes."
    - "D-17: unsupported-but-readable layouts remain inspectable but fail extraction structurally."
    - "D-21: malformed coverage uses targeted generated builders for truncated records, bad chunks, names, duplicates, codec routes, mip mapping, and reconstruction failures."
    - "D-22/D-23: every positive extracted DDS fixture is DirectXTex-validated semantically."
---

<objective>
Close Phase 7 malformed DX10 safety, documentation, and final validation gates.

Purpose: Ensure the implementation is robust against malformed DX10 archives and ready for execution verification.
Output: Targeted malformed tests, any final parser hardening, README update, and full validation commands.
</objective>

<decision_coverage>
- D-12, D-16, D-17, and D-21 through D-23 are finalized by this plan.
</decision_coverage>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
@C:/Users/evild/.config/opencode/get-shit-done/references/tdd.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/07-ba2-dds-read-and-dds-reconstruction/07-SPEC.md
@.planning/phases/07-ba2-dds-read-and-dds-reconstruction/07-CONTEXT.md
@.planning/phases/07-ba2-dds-read-and-dds-reconstruction/07-RESEARCH.md
@src/ba2_reader.cpp
@src/texture/dds_reconstruction.cpp
@tests/ba2_dds_fixture_helpers.hpp
@tests/ba2_dds_reader_tests.cpp
@tests/public_header_smoke.cpp
@README.md
</context>

<feature>
  <name>Malformed DX10 safety and final validation</name>
  <files>tests/ba2_dds_reader_tests.cpp, src/ba2_reader.cpp, src/texture/dds_reconstruction.cpp, README.md</files>
  <behavior>
    - Truncated records, invalid chunk ranges, name-table problems, duplicate normalized names, unsupported codec routes, inconsistent mip/chunk mapping, and reconstruction failures return structured errors.
    - No malformed extraction writes bytes to caller sinks.
    - Metadata inspection succeeds for unsupported-but-readable layouts where safe.
    - README documents BA2 DDS support, DirectXTex validation boundary, and local validation commands.
  </behavior>
  <implementation>Add RED malformed tests from fixture helpers, implement the smallest parser/reconstruction checks needed, and update README after tests pass. Do not broaden to real corpus or BSArchPro comparison gates.</implementation>
</feature>

<tasks>

<task type="auto">
  <name>Task 1 RED: Add targeted malformed DX10 tests</name>
  <read_first>tests/ba2_dds_reader_tests.cpp, tests/ba2_dds_fixture_helpers.hpp, .planning/phases/07-ba2-dds-read-and-dds-reconstruction/07-SPEC.md</read_first>
  <files>tests/ba2_dds_reader_tests.cpp</files>
  <action>Add failing tests for targeted malformed classes required by the SPEC: truncated DX10 records, invalid chunk ranges, name-table problems, duplicate normalized names, unsupported codec routes, inconsistent mip/chunk mapping, unsupported-but-readable layout extraction failure, and DDS reconstruction failure. Every extraction failure test must assert `sink.bytes().empty()`.</action>
  <verify>
    <automated>ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug -R libbsa_ba2_dds_reader_tests</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "truncated DX10|invalid chunk|duplicate normalized|unsupported codec|mip.*chunk|reconstruction failure|bytes\\(\\)\\.empty" tests/ba2_dds_reader_tests.cpp` prints all patterns.
    - RED run fails for accepted malformed behavior, not missing helper scaffolding.
  </acceptance_criteria>
  <done>RED commit `test(07-06): add failing malformed BA2 DDS tests` exists.</done>
</task>

<task type="auto">
  <name>Task 2 GREEN/REFACTOR: Harden malformed DX10 handling</name>
  <read_first>src/ba2_reader.cpp, src/texture/dds_reconstruction.cpp, tests/ba2_dds_reader_tests.cpp</read_first>
  <files>src/ba2_reader.cpp, src/texture/dds_reconstruction.cpp, tests/ba2_dds_reader_tests.cpp</files>
  <action>Implement the minimal parser, chunk, codec, mip mapping, and reconstruction precondition checks needed to make the targeted malformed tests pass. Return structured errors consistently and preserve no-partial-write behavior. Add comments for non-obvious compatibility or safety constraints.</action>
  <verify>
    <automated>ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug -R libbsa_ba2_dds_reader_tests</automated>
  </verify>
  <acceptance_criteria>
    - `ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug -R libbsa_ba2_dds_reader_tests` passes.
    - `rg -n "fallback|try.*codec" src/ba2_reader.cpp src/texture` prints no codec-guessing implementation.
  </acceptance_criteria>
  <done>GREEN commit `fix(07-06): harden malformed BA2 DDS handling` exists.</done>
</task>

<task type="auto">
  <name>Task 3: Document BA2 DDS support and run final gates</name>
  <read_first>README.md, .planning/phases/07-ba2-dds-read-and-dds-reconstruction/07-CONTEXT.md</read_first>
  <files>README.md</files>
  <action>Update README with concise BA2 DDS read/extract support notes: supported FO4 DX10 v1/v7/v8 and Starfield DX10 v3, public texture metadata API, private DirectXTex validation boundary, generated fixture acceptance corpus, and validation commands. Do not claim real game corpus or BSArchPro comparison support in Phase 7.</action>
  <verify>
    <automated>ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug && rg -n "DirectXTex|DXGI_FORMAT|Windows\\.h|libdeflate|lz4|TES5Edit" include/libbsa && git status --short TES5Edit</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "BA2 DDS|DX10|texture_metadata|DirectXTex|generated fixture" README.md` prints all patterns.
    - Full `ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug` passes.
    - Public-header leakage grep prints no forbidden private dependency matches under `include/libbsa`.
    - `git status --short TES5Edit` prints no output.
  </acceptance_criteria>
  <done>Documentation and final validation commit `docs(07-06): document BA2 DDS read support` exists.</done>
</task>

</tasks>

<verification>
Run full CTest, public-header leakage grep, CMake source-list checks, and `git status --short TES5Edit`.
</verification>

<success_criteria>
- All Phase 7 positive and malformed acceptance cases are covered by generated fixtures.
- Full test suite passes.
- Public headers remain dependency-clean.
- README documents supported Phase 7 behavior without overclaiming deferred corpus/writer work.
</success_criteria>

<output>
After completion, create `.planning/phases/07-ba2-dds-read-and-dds-reconstruction/07-06-SUMMARY.md`.
</output>

