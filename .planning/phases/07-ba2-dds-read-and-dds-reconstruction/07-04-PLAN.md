---
phase: 07-ba2-dds-read-and-dds-reconstruction
plan: 04
type: tdd
wave: 3
depends_on: [07-01, 07-02]
files_modified:
  - src/texture/dds_reconstruction.hpp
  - src/texture/dds_reconstruction.cpp
  - src/texture/dds_validation.hpp
  - src/texture/dds_validation.cpp
  - tests/dds_reconstruction_tests.cpp
  - CMakeLists.txt
autonomous: true
requirements: [BA2-06]
must_haves:
  truths:
    - "D-14: libbsa reconstructs DDS/DDS-DX10 headers itself from parsed metadata."
    - "D-15: DirectXTex validation is private and runs after reconstruction before sink writes where practical."
    - "D-16: validation or reconstruction failures return structured errors."
    - "D-17: unsupported-but-readable layouts can remain inspectable while extraction fails."
    - "D-18: DDS reconstruction and validation helpers are internal and reusable for later writer phases."
---

<objective>
Build private DDS reconstruction and DirectXTex validation helpers.

Purpose: Isolate texture-specific DDS behavior before BA2 extraction assembles real archive chunks.
Output: Internal helpers that produce complete DDS bytes and validate them without public dependency leakage.
</objective>

<decision_coverage>
- D-14 through D-18 are implemented at the private helper boundary.
</decision_coverage>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
@C:/Users/evild/.config/opencode/get-shit-done/references/tdd.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/07-ba2-dds-read-and-dds-reconstruction/07-SPEC.md
@.planning/phases/07-ba2-dds-read-and-dds-reconstruction/07-CONTEXT.md
@.planning/phases/07-ba2-dds-read-and-dds-reconstruction/07-RESEARCH.md
@.planning/phases/07-ba2-dds-read-and-dds-reconstruction/07-PATTERNS.md
@include/libbsa/ba2.hpp
@tests/ba2_dds_fixture_helpers.hpp
@CMakeLists.txt
</context>

<feature>
  <name>DDS reconstruction and validation helpers</name>
  <files>src/texture/dds_reconstruction.*, src/texture/dds_validation.*, tests/dds_reconstruction_tests.cpp</files>
  <behavior>
    - Given libbsa-owned texture metadata and decompressed image payload bytes, reconstruction returns complete DDS bytes beginning with `DDS `.
    - DirectXTex validation accepts reconstructed one-mip, multi-mip, and cubemap/array fixture outputs and reports expected metadata.
    - Unsupported reconstruction preconditions return structured failures without producing bytes for extraction to write.
  </behavior>
  <implementation>Create internal metadata/input types or adapt public `texture_metadata` carefully. Build DDS legacy and DX10 headers directly. Keep `#include <DirectXTex.h>` only in `dds_validation.cpp` and tests as needed. Translate HRESULT failures to existing `error_code::malformed_archive` or `error_code::unsupported_format` consistently.</implementation>
</feature>

<tasks>

<task type="auto">
  <name>Task 1 RED: Add reconstruction and validation helper tests</name>
  <read_first>tests/ba2_dds_fixture_helpers.hpp, include/libbsa/ba2.hpp, CMakeLists.txt</read_first>
  <files>tests/dds_reconstruction_tests.cpp, CMakeLists.txt</files>
  <action>Add focused tests for one-mip 2D, multi-mip, and cubemap/array reconstruction. Tests should call private helpers through test-visible internal headers, assert DDS magic/header size expectations, call the private DirectXTex validation helper, and assert width, height, mip count, raw DXGI value, array size, and cubemap state. Add a failure test for an unsupported reconstruction precondition. Wire `libbsa_dds_reconstruction_tests` with labels `unit;fixture`.</action>
  <verify>
    <automated>cmake --build build/local-vs2026-vcpkg --config Debug && ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug -R libbsa_dds_reconstruction_tests</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "LoadFromDDSMemory|validate_dds|reconstruct_dds|cubemap|mip" tests/dds_reconstruction_tests.cpp` prints all patterns.
    - RED run fails because reconstruction helpers are missing, not because DirectXTex target wiring is absent.
  </acceptance_criteria>
  <done>RED commit `test(07-04): add failing DDS reconstruction tests` exists.</done>
</task>

<task type="auto">
  <name>Task 2 GREEN/REFACTOR: Implement private DDS reconstruction and validation</name>
  <read_first>include/libbsa/ba2.hpp, tests/dds_reconstruction_tests.cpp, .planning/research/STACK.md, CMakeLists.txt</read_first>
  <files>src/texture/dds_reconstruction.hpp, src/texture/dds_reconstruction.cpp, src/texture/dds_validation.hpp, src/texture/dds_validation.cpp, tests/dds_reconstruction_tests.cpp, CMakeLists.txt</files>
  <action>Implement `reconstruct_dds` and DirectXTex-backed validation helpers privately. Add source files to the explicit `libbsa` source list and link/use the existing private DirectXTex target. Keep DirectXTex includes out of public headers. Add comments for non-obvious DDS/DX10 header flag choices and why validation remains private.</action>
  <verify>
    <automated>cmake --build build/local-vs2026-vcpkg --config Debug && ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug -R libbsa_dds_reconstruction_tests</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "DirectXTex|LoadFromDDSMemory" src/texture tests/dds_reconstruction_tests.cpp` finds DirectXTex only in private/test files.
    - `rg -n "DirectXTex|DXGI_FORMAT|Windows\\.h" include/libbsa` prints no matches.
    - `ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug -R libbsa_dds_reconstruction_tests` passes.
  </acceptance_criteria>
  <done>GREEN commit `feat(07-04): add private DDS reconstruction validation` exists.</done>
</task>

</tasks>

<verification>
Run helper tests and public-header leakage grep.
</verification>

<success_criteria>
- DDS reconstruction produces DirectXTex-loadable bytes for representative layouts.
- DirectXTex remains private.
- Unsupported reconstruction fails structurally.
</success_criteria>

<output>
After completion, create `.planning/phases/07-ba2-dds-read-and-dds-reconstruction/07-04-SUMMARY.md`.
</output>

