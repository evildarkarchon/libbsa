---
phase: 07-ba2-dds-read-and-dds-reconstruction
plan: 03
type: tdd
wave: 2
depends_on: [07-01, 07-02]
files_modified:
  - src/ba2_reader.cpp
  - include/libbsa/ba2.hpp
  - tests/ba2_dds_reader_tests.cpp
autonomous: true
requirements: [BA2-05, BA2-06]
must_haves:
  truths:
    - "D-01/D-02: open_ba2 accepts supported DX10 archives and texture_metadata(path) returns structured copied texture metadata."
    - "D-03/D-09/D-13: texture metadata includes dimensions, format, mip/array/cubemap state, and logical chunk summaries with mip ranges when derivable."
    - "D-05/D-07/D-08: raw DXGI values are preserved and unknown labels do not block metadata inspection."
    - "D-20: FO4 DX10 v1/v7/v8 and Starfield DX10 v3 fixtures open with expected summaries, paths, lookup, and texture metadata."
---

<objective>
Implement BA2 DX10 record, chunk, and name-table parsing into generic and texture metadata.

Purpose: Establish read/list/lookup/inspect support before extraction and reconstruction are added.
Output: `open_ba2` supports `BTDX` + `DX10` FO4 v1/v7/v8 and Starfield v3 metadata fixtures.
</objective>

<decision_coverage>
- D-01 through D-09, D-13, and D-20 are implemented for parsing and metadata inspection.
</decision_coverage>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
@C:/Users/evild/.config/opencode/get-shit-done/references/tdd.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/07-ba2-dds-read-and-dds-reconstruction/07-SPEC.md
@.planning/phases/07-ba2-dds-read-and-dds-reconstruction/07-CONTEXT.md
@.planning/phases/07-ba2-dds-read-and-dds-reconstruction/07-RESEARCH.md
@.planning/phases/07-ba2-dds-read-and-dds-reconstruction/07-PATTERNS.md
@include/libbsa/ba2.hpp
@include/libbsa/archive.hpp
@src/ba2_reader.cpp
@src/detect.cpp
@tests/ba2_dds_fixture_helpers.hpp
@tests/ba2_dds_reader_tests.cpp
@TES5Edit/BSArchPro.dpr
@TES5Edit/BSArch/
@TES5Edit/Core/wbBSArchive.pas
</context>

<feature>
  <name>BA2 DX10 metadata parser</name>
  <files>src/ba2_reader.cpp, tests/ba2_dds_reader_tests.cpp</files>
  <behavior>
    - FO4 DX10 v1/v7/v8 fixtures open and report `archive_format::fo4_ba2_dds`.
    - Starfield DX10 v3 fixtures open and report `archive_format::starfield_ba2_dds` plus `compression_method == 3` where present.
    - `paths()`, `contains()`, and `entry(path)` work for normalized texture paths.
    - `texture_metadata(path)` returns exact dimensions, `dxgi_format` raw value, mip count, array size, cubemap state, and chunk summaries.
  </behavior>
  <implementation>Trace TES5Edit DX10 record/chunk field layout before writing offsets. Add private DX10 record/chunk structs and parse functions using existing bounded read helpers. Share name-table parsing with GNRL where safe. Populate generic `entry_metadata` with archive-absolute payload/chunk range semantics and copied texture metadata keyed by normalized path.</implementation>
</feature>

<tasks>

<task type="auto">
  <name>Task 1 RED: Add DX10 metadata parser fixture tests</name>
  <read_first>tests/ba2_dds_reader_tests.cpp, tests/ba2_dds_fixture_helpers.hpp, .planning/phases/07-ba2-dds-read-and-dds-reconstruction/07-CONTEXT.md</read_first>
  <files>tests/ba2_dds_reader_tests.cpp</files>
  <action>Add failing tests for FO4 DX10 v1, v7, v8, and Starfield DX10 v3 open/list/lookup/texture metadata. Tests must assert summary format/version/subtype/file_count/file_table_offset/compression_method, normalized paths, `entry(path)` generic metadata, `texture_metadata(path)` dimensions/format/mips/array/cubemap, and exact chunk summary offsets/sizes/mip ranges for at least one single-chunk and one multi-chunk texture.</action>
  <verify>
    <automated>ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug -R libbsa_ba2_dds_reader_tests</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "DX10 v1|DX10 v7|DX10 v8|Starfield DX10 v3|texture_metadata|chunk" tests/ba2_dds_reader_tests.cpp` prints all patterns.
    - RED run fails because DX10 subtype is not yet parsed, not because fixtures are malformed or test target is missing.
  </acceptance_criteria>
  <done>RED commit `test(07-03): add failing BA2 DX10 metadata tests` exists.</done>
</task>

<task type="auto">
  <name>Task 2 GREEN/REFACTOR: Implement DX10 parser and copied texture metadata</name>
  <read_first>src/ba2_reader.cpp, include/libbsa/ba2.hpp, src/detect.cpp, tests/ba2_dds_fixture_helpers.hpp, TES5Edit/Core/wbBSArchive.pas</read_first>
  <files>src/ba2_reader.cpp, include/libbsa/ba2.hpp, tests/ba2_dds_reader_tests.cpp</files>
  <action>Implement DX10 subtype dispatch in `open_ba2`. Parse supported FO4 versions 1/7/8 and Starfield version 3. Parse texture records, variable chunk metadata, and names from `FileTableOffset` with bounded reads and overflow checks. Populate `ba2_archive` with both generic entries and copied texture metadata. Add short comments near DX10 field mapping and chunk/mip association explaining compatibility constraints traced from reference behavior. Preserve GNRL behavior.</action>
  <verify>
    <automated>ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug -R "libbsa_ba2_dds_reader_tests|libbsa_ba2_reader_tests"</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "magic_dx10|parse_ba2_dx10|texture_metadata|texture_chunk_metadata|compression_method" src/ba2_reader.cpp include/libbsa/ba2.hpp` prints all patterns.
    - `ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug -R "libbsa_ba2_dds_reader_tests|libbsa_ba2_reader_tests"` passes.
  </acceptance_criteria>
  <done>GREEN commit `feat(07-03): parse BA2 DX10 texture metadata` exists, with optional refactor commit if parser helpers are split.</done>
</task>

</tasks>

<verification>
Run focused BA2 DDS and BA2 GNRL reader tests to ensure subtype dispatch preserves Phase 6 behavior.
</verification>

<success_criteria>
- Supported DX10 archives open and expose generic plus texture metadata.
- GNRL behavior remains green.
- TES5Edit remains read-only and clean.
</success_criteria>

<output>
After completion, create `.planning/phases/07-ba2-dds-read-and-dds-reconstruction/07-03-SUMMARY.md`.
</output>

