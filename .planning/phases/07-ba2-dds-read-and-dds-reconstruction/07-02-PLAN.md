---
phase: 07-ba2-dds-read-and-dds-reconstruction
plan: 02
type: auto
wave: 1
depends_on: []
files_modified:
  - tests/ba2_dds_fixture_helpers.hpp
  - tests/ba2_dds_fixture_helpers.cpp
  - tests/ba2_dds_reader_tests.cpp
  - CMakeLists.txt
autonomous: true
requirements: [BA2-05, BA2-06]
must_haves:
  truths:
    - "D-19: Generated BA2 DDS fixtures live in a reusable private test helper module."
    - "D-20: Positive fixture helpers can build FO4 DX10 v1/v7/v8 and Starfield DX10 v3 with one-mip, multi-mip, cubemap/array, raw, deflate, and LZ4-block cases."
    - "D-21: Malformed fixture helpers support targeted failure classes instead of opaque corruptions."
    - "D-23: Fixture helpers support semantic DDS assertions without requiring universal byte-exact comparisons."
  artifacts:
    - path: "tests/ba2_dds_fixture_helpers.hpp"
      provides: "Reusable private generated BA2 DDS fixture declarations"
      contains: "ba2_dds_fixture"
---

<objective>
Create reusable generated BA2 DDS fixture helpers and wire the focused BA2 DDS test target.

Purpose: Give all later Phase 7 TDD plans a deterministic source corpus without binary fixture blobs.
Output: Private test helper module plus an initially empty/failing-capable BA2 DDS reader test target.
</objective>

<decision_coverage>
- D-19 through D-23 fixture corpus decisions are scaffolded by this plan.
</decision_coverage>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/07-ba2-dds-read-and-dds-reconstruction/07-SPEC.md
@.planning/phases/07-ba2-dds-read-and-dds-reconstruction/07-CONTEXT.md
@.planning/phases/07-ba2-dds-read-and-dds-reconstruction/07-RESEARCH.md
@.planning/phases/07-ba2-dds-read-and-dds-reconstruction/07-PATTERNS.md
@tests/ba2_reader_tests.cpp
@tests/bsa_reader_tests.cpp
@tests/detection_tests.cpp
@CMakeLists.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add generated BA2 DDS fixture helper module</name>
  <read_first>tests/ba2_reader_tests.cpp, tests/detection_tests.cpp, CMakeLists.txt</read_first>
  <files>tests/ba2_dds_fixture_helpers.hpp, tests/ba2_dds_fixture_helpers.cpp</files>
  <action>Create a private test helper module with local append/write helpers, version/subtype constants, texture layout descriptors, chunk descriptors, and functions that generate deterministic BA2 DX10 archive bytes. Include helpers for FO4 DX10 v1/v7/v8, Starfield DX10 v3 with `CompressionMethod == 3`, one-mip 2D, multi-mip, cubemap or array, raw chunk, deflate chunk, and LZ4-block chunk fixture construction. Add targeted mutation helpers for truncated records, invalid chunk ranges, duplicate normalized names, unsupported codec routes, inconsistent mip/chunk mapping, and reconstruction failure cases. Keep helpers under `tests/`; do not add them to public headers.</action>
  <verify>
    <automated>cmake --build build/local-vs2026-vcpkg --config Debug</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "VERSION_FO4_DX10_V1|VERSION_FO4_DX10_V7|VERSION_FO4_DX10_V8|VERSION_STARFIELD_DX10_V3" tests/ba2_dds_fixture_helpers.*` prints all four constants.
    - `rg -n "one_mip|multi_mip|cubemap|array|deflate|lz4_block|malformed" tests/ba2_dds_fixture_helpers.*` prints representative helper names.
    - `rg -n "DirectXTex|TES5Edit" tests/ba2_dds_fixture_helpers.*` prints no matches.
  </acceptance_criteria>
  <done>Fixture helper module exists and builds without public dependency leakage.</done>
</task>

<task type="auto">
  <name>Task 2: Wire BA2 DDS reader test target</name>
  <read_first>CMakeLists.txt, tests/ba2_reader_tests.cpp</read_first>
  <files>CMakeLists.txt, tests/ba2_dds_reader_tests.cpp</files>
  <action>Create `tests/ba2_dds_reader_tests.cpp` with a minimal Catch2 test that includes the fixture helper header and verifies a generated fixture has `BTDX` magic and `DX10` subtype bytes. Wire `libbsa_ba2_dds_reader_tests` in `CMakeLists.txt`, linking `libbsa::libbsa`, `Catch2::Catch2WithMain`, and the fixture helper source. Use `catch_discover_tests` with labels `unit;fixture;codec`.</action>
  <verify>
    <automated>cmake --build build/local-vs2026-vcpkg --config Debug && ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug -R libbsa_ba2_dds_reader_tests</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "libbsa_ba2_dds_reader_tests|ba2_dds_fixture_helpers.cpp|unit;fixture;codec" CMakeLists.txt` prints all three patterns.
    - `ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug -R libbsa_ba2_dds_reader_tests` passes the scaffold test.
  </acceptance_criteria>
  <done>BA2 DDS fixture test target is wired and ready for subsequent RED tests.</done>
</task>

</tasks>

<verification>
Run the new BA2 DDS reader test target and confirm `git status --short TES5Edit` is empty.
</verification>

<success_criteria>
- Generated BA2 DDS fixtures are source-reviewable and reusable by later Phase 7 plans.
- CMake explicitly wires the helper/test target.
- No binary fixture blobs are committed.
</success_criteria>

<output>
After completion, create `.planning/phases/07-ba2-dds-read-and-dds-reconstruction/07-02-SUMMARY.md`.
</output>

