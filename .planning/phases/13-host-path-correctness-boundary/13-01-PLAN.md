---
phase: 13-host-path-correctness-boundary
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/detail/writer_disk_source.hpp
  - src/detail/writer_disk_source.cpp
  - src/detail/host_file.hpp
  - src/detail/host_file.cpp
  - src/detail/host_path.hpp
  - src/formats/bsa/tes4_bsa_prepare.cpp
  - src/formats/bsa/tes4_bsa_layout.cpp
  - src/formats/ba2/ba2_gnrl_prepare.cpp
  - src/formats/ba2/ba2_dx10_prepare.cpp
  - tests/unit/writer_disk_source_tests.cpp
  - tests/unit/host_file_tests.cpp
  - tests/CMakeLists.txt
autonomous: true
requirements:
  - HOST-01
  - HOST-02
user_setup: []
must_haves:
  truths:
    - Existing host-file helper behavior is delivered through one shared `host_file` boundary in source and tests, with no parallel `writer_disk_source` family left behind for this phase to route around, per D-01 and D-02.
    - Existing writer-side callers already carry the shared `detail::host_file_path` value that separates diagnostics UTF-8 text from resolved Windows I/O state, so the shared-path ownership rule is delivered on live caller code rather than postponed, per D-10, D-11, and D-14.
  artifacts:
    - path: src/detail/host_file.hpp
      provides: Neutral shared host-file declarations using `host_file` and `host_file_context` names.
    - path: src/detail/host_file.cpp
      provides: Neutral shared host-file open, inspect, prefix, exact-read, and chunk helpers.
    - path: src/detail/host_path.hpp
      provides: `detail::host_file_path` storing original UTF-8 text plus resolved filesystem path.
    - path: src/formats/bsa/tes4_bsa_prepare.cpp
      provides: writer-side adoption of `detail::host_file_path` through the shared `host_file` boundary.
    - path: tests/unit/host_file_tests.cpp
      provides: Renamed helper regression coverage under the new family.
  key_links:
    - from: src/detail/host_path.hpp
      to: src/formats/bsa/tes4_bsa_prepare.cpp
      via: writer call sites construct and pass `detail::host_file_path` into shared host-file helpers now
      pattern: host_file_path
    - from: src/detail/host_file.hpp
      to: src/formats/ba2/ba2_gnrl_prepare.cpp
      via: `host_file_context` plus neutral helper calls that consume the shared host-path value
      pattern: host_file_context|read_host_file_|inspect_host_file_size
    - from: tests/CMakeLists.txt
      to: tests/unit/host_file_tests.cpp
      via: unit-test registration under the renamed helper suite
      pattern: host_file_tests
---

<objective>
Rename and generalize the existing host-file helper into the locked shared naming family.

Purpose: Implement D-01, D-02, D-04, D-05, D-06, D-11, and D-14 by replacing `writer_disk_source` with `host_file`, `host_file_context`, and `detail::host_file_path`, with writer-side call-site adoption landing in the same buildable migration slice instead of being deferred.
Output: renamed shared helper files, shared internal host-path value, migrated writer call sites, renamed unit tests, and removal of the old helper names from source/test code in one buildable slice.
Rationale: This first-wave plan stays broad on purpose because D-02 forbids compatibility shims and a half-renamed helper family would leave the repository in a misleading intermediate state; Task 1 lands live caller adoption with the new boundary, and Task 2 isolates the now-safe cleanup/removal work.
</objective>

<execution_context>
@J:/libbsa/.opencode/get-shit-done/workflows/execute-plan.md
@J:/libbsa/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-host-path-correctness-boundary/13-SPEC.md
@.planning/phases/13-host-path-correctness-boundary/13-CONTEXT.md
@.planning/phases/13-host-path-correctness-boundary/13-RESEARCH.md
@.planning/phases/13-host-path-correctness-boundary/13-VALIDATION.md
@.planning/phases/13-host-path-correctness-boundary/13-PATTERNS.md
@src/detail/writer_disk_source.hpp
@src/detail/writer_disk_source.cpp
@tests/unit/writer_disk_source_tests.cpp
@tests/CMakeLists.txt

<interfaces>
From 13-RESEARCH.md resolved naming:
- `src/detail/host_file.*`
- `host_file_context`
- `detail::host_file_path`

From 13-PATTERNS.md shared-helper analogs:
- Preserve the existing helper surface shape from `src/detail/writer_disk_source.hpp/.cpp`.
- Preserve the writer call-site diagnostics-context pattern from `tes4_bsa_prepare.cpp`, `tes4_bsa_layout.cpp`, `ba2_gnrl_prepare.cpp`, and `ba2_dx10_prepare.cpp`.
- Preserve the helper-test behavior coverage currently provided by `tests/unit/writer_disk_source_tests.cpp`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Introduce `host_file` / `host_file_path` and migrate live writer callers onto that shared path type in the same buildable slice</name>
  <read_first>
    - src/detail/writer_disk_source.hpp
    - src/detail/writer_disk_source.cpp
    - src/formats/bsa/tes4_bsa_prepare.cpp
    - src/formats/bsa/tes4_bsa_layout.cpp
    - src/formats/ba2/ba2_gnrl_prepare.cpp
    - src/formats/ba2/ba2_dx10_prepare.cpp
    - .planning/phases/13-host-path-correctness-boundary/13-RESEARCH.md
    - .planning/phases/13-host-path-correctness-boundary/13-VALIDATION.md
    - .planning/phases/13-host-path-correctness-boundary/13-PATTERNS.md
  </read_first>
  <files>src/detail/host_file.hpp, src/detail/host_file.cpp, src/detail/host_path.hpp, src/formats/bsa/tes4_bsa_prepare.cpp, src/formats/bsa/tes4_bsa_layout.cpp, src/formats/ba2/ba2_gnrl_prepare.cpp, src/formats/ba2/ba2_dx10_prepare.cpp</files>
  <acceptance_criteria>
    - `src/detail/host_file.*` exists and exposes the neutral helper family with the same layered open/inspect/prefix/exact/chunk behavior, per D-01, D-02, and D-04.
    - `src/detail/host_path.hpp` defines `detail::host_file_path` with original UTF-8 text plus resolved filesystem path, per D-11 and the locked naming from `13-RESEARCH.md`.
    - Writer callers in `tes4_bsa_prepare.cpp`, `tes4_bsa_layout.cpp`, `ba2_gnrl_prepare.cpp`, and `ba2_dx10_prepare.cpp` adopt `detail::host_file_path` and call the neutral `host_file` helpers in this task, not as a later follow-up, per D-02 and D-14.
    - New or rewritten comments/doc comments explain the shared host-file rationale and why diagnostics text remains caller-owned, per AGENTS.md and D-06.
  </acceptance_criteria>
  <action>Following the exact helper and writer-call-site analogs mapped in `13-PATTERNS.md`, land the locked D-01/D-02/D-14 migration as one buildable source slice: add `src/detail/host_file.hpp`, `src/detail/host_file.cpp`, and `src/detail/host_path.hpp`; copy the layered helper surface into the neutral `host_file` family; define `detail::host_file_path` as the shared internal value that stores both the original UTF-8 diagnostics text and the resolved filesystem path; and update the writer callers in `tes4_bsa_prepare.cpp`, `tes4_bsa_layout.cpp`, `ba2_gnrl_prepare.cpp`, and `ba2_dx10_prepare.cpp` so they construct and pass `detail::host_file_path` into the shared helper now. Keep `std::ifstream` lifetime local, keep caller-supplied diagnostics semantics unchanged per D-04 through D-06, and do not leave `detail::host_file_path` as a contract that only later read-side work uses.</action>
  <verify>
    <automated>cmake --build --preset windows-msvc-debug-static --target libbsa_tests &amp;&amp; ctest --preset windows-msvc-debug-static -R "writer_disk_source|host_file" --output-on-failure &amp;&amp; if (!(rg "host_file_path" src/formats/bsa/tes4_bsa_prepare.cpp src/formats/bsa/tes4_bsa_layout.cpp src/formats/ba2/ba2_gnrl_prepare.cpp src/formats/ba2/ba2_dx10_prepare.cpp)) { exit 1 }</automated>
  </verify>
  <done>The locked `host_file` helper family exists, live writer callers already converge on `detail::host_file_path`, and any remaining old helper files survive only as temporary cleanup debt for Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the old helper family and finish test/CMake cleanup once writer callers are already on `host_file_path`</name>
  <read_first>
    - src/detail/host_file.hpp
    - src/detail/host_file.cpp
    - src/detail/writer_disk_source.hpp
    - src/detail/writer_disk_source.cpp
    - tests/unit/writer_disk_source_tests.cpp
    - tests/CMakeLists.txt
    - .planning/phases/13-host-path-correctness-boundary/13-RESEARCH.md
    - .planning/phases/13-host-path-correctness-boundary/13-VALIDATION.md
    - .planning/phases/13-host-path-correctness-boundary/13-PATTERNS.md
  </read_first>
  <files>src/detail/writer_disk_source.hpp, src/detail/writer_disk_source.cpp, tests/unit/writer_disk_source_tests.cpp, tests/unit/host_file_tests.cpp, tests/CMakeLists.txt</files>
  <acceptance_criteria>
    - The old helper files are removed only after Task 1 has already migrated every writer caller to `host_file`, `host_file_context`, and `detail::host_file_path`, per D-02 and D-14.
    - `tests/unit/host_file_tests.cpp` replaces the old helper suite in `tests/CMakeLists.txt` and preserves helper behavior coverage under the new names.
    - The old helper/test file paths are explicitly removed or renamed as part of this task; there is no remaining CMake registration for `writer_disk_source_tests.cpp`.
    - New or rewritten comments/doc comments explain the shared host-file rationale and why diagnostics text remains caller-owned, per AGENTS.md and D-06.
  </acceptance_criteria>
  <action>Finish the locked D-01/D-02 migration by deleting the superseded `writer_disk_source` source/test family, adding the final `tests/unit/host_file_tests.cpp` coverage file if Task 1 used temporary dual registration, and switching `tests/CMakeLists.txt` to register only the `host_file` suite. Keep the helper layered, keep `std::ifstream` lifetime local, keep caller-supplied diagnostics semantics unchanged per D-04 through D-06, and add concise why-comments/doc comments anywhere the neutral shared boundary or caller-owned diagnostics rule would otherwise be easy to regress.</action>
  <verify>
    <automated>cmake --build --preset windows-msvc-debug-static --target libbsa_tests &amp;&amp; ctest --preset windows-msvc-debug-static -R host_file --output-on-failure &amp;&amp; if (rg "writer_disk_source" src tests/CMakeLists.txt tests/unit) { exit 1 }</automated>
  </verify>
  <done>The locked `host_file` naming family is the only remaining shared host-file helper family in source and unit tests, and the repo stays buildable throughout the plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Existing writer callers -&gt; shared host-file helper | Callers provide diagnostics and expected sizes into the single helper boundary that later read-side code will also consume. |
| UTF-8 text/resolved path ownership -&gt; shared helper naming family | The internal path value must keep diagnostics text separate from the filesystem path used for real I/O. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | T | `src/detail/host_path.hpp` | mitigate | Store original UTF-8 text and resolved filesystem path together in `detail::host_file_path` so later plans cannot invent divergent ownership or fallback routes, per D-11 and D-14. |
| T-13-02 | D | `src/detail/host_file.cpp` | mitigate | Keep one shared helper family for open/inspect/read mechanics so host-file behavior cannot drift across writer and read-side boundaries, per D-01 through D-06. |
| T-13-03 | R | old `writer_disk_source` names | mitigate | Remove the old helper files/tests in the same plan so debugging and later migration work have one authoritative boundary only, per D-02. |
</threat_model>

<verification>
- Build `libbsa_tests` with the debug preset.
- Run `ctest --preset windows-msvc-debug-static -R host_file --output-on-failure`.
- Run `if (rg "writer_disk_source" src tests/CMakeLists.txt tests/unit) { exit 1 }`.
</verification>

<success_criteria>
- The locked `host_file` naming family exists and the old `writer_disk_source` family is gone from source/test code.
- `detail::host_file_path` already serves migrated writer callers before later open/validation/extraction plans depend on it.
- Writer call sites still compile and use the shared helper with unchanged diagnostics semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/13-host-path-correctness-boundary/13-01-SUMMARY.md`
</output>
