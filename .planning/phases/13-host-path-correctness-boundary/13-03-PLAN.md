---
phase: 13-host-path-correctness-boundary
plan: 03
type: execute
wave: 3
depends_on:
  - 13-02
files_modified:
  - src/archive.cpp
  - src/formats/bsa/tes3_bsa_reader.hpp
  - src/formats/bsa/tes3_bsa_reader.cpp
  - src/formats/bsa/tes4_bsa_reader.hpp
  - src/formats/bsa/tes4_bsa_reader.cpp
  - src/formats/ba2/ba2_gnrl_reader.hpp
  - src/formats/ba2/ba2_gnrl_reader.cpp
  - src/formats/ba2/ba2_dx10_reader.hpp
  - src/formats/ba2/ba2_dx10_reader.cpp
autonomous: true
requirements:
  - HOST-01
  - HOST-02
user_setup: []
must_haves:
  truths:
    - Consumers can perform post-open payload reads through the public extraction surface without any reader family re-resolving or reopening from raw caller UTF-8 text, per D-12 and D-13.
    - After open succeeds, every migrated reader family reopens archive bytes only from stored resolved host-path state through the shared boundary, so no raw-text fallback path remains for payload reads, per D-13 and D-14.
  artifacts:
    - path: src/archive.cpp
      provides: extraction dispatch wired to stored `detail::host_file_path`
    - path: src/formats/bsa/tes3_bsa_reader.hpp
      provides: TES3 extraction contract taking `const detail::host_file_path&`
    - path: src/formats/bsa/tes3_bsa_reader.cpp
      provides: TES3 extraction reopen through `host_file` from stored resolved path
    - path: src/formats/bsa/tes4_bsa_reader.hpp
      provides: TES4 extraction contract taking `const detail::host_file_path&`
    - path: src/formats/bsa/tes4_bsa_reader.cpp
      provides: TES4 extraction reopen through `host_file` from stored resolved path
    - path: src/formats/ba2/ba2_gnrl_reader.hpp
      provides: BA2 GNRL extraction contract taking `const detail::host_file_path&`
    - path: src/formats/ba2/ba2_gnrl_reader.cpp
      provides: BA2 GNRL extraction reopen through `host_file` from stored resolved path
    - path: src/formats/ba2/ba2_dx10_reader.hpp
      provides: extraction contract taking `const detail::host_file_path&`
    - path: src/formats/ba2/ba2_dx10_reader.cpp
      provides: extraction reopen through `host_file` from stored resolved path
  key_links:
    - from: src/archive.cpp
      to: src/formats/bsa/tes3_bsa_reader.hpp
      via: stored-path TES3 extraction dispatch
      pattern: extract_tes3_bsa_payload\(const detail::host_file_path&
    - from: src/archive.cpp
      to: src/formats/bsa/tes4_bsa_reader.hpp
      via: stored-path TES4 extraction dispatch
      pattern: extract_tes4_bsa_payload_from_file\(const detail::host_file_path&
    - from: src/archive.cpp
      to: src/formats/ba2/ba2_gnrl_reader.hpp
      via: stored-path BA2 GNRL extraction dispatch
      pattern: extract_ba2_gnrl_payload\(const detail::host_file_path&
    - from: src/archive.cpp
      to: src/formats/ba2/ba2_dx10_reader.hpp
      via: stored-path BA2 DX10 extraction dispatch
      pattern: extract_ba2_dx10_payload\(const detail::host_file_path&
    - from: src/formats/bsa/tes3_bsa_reader.cpp
      to: src/detail/host_file.hpp
      via: TES3 reopened archive stream acquisition from stored resolved path
      pattern: open_host_file|read_host_file_
    - from: src/formats/bsa/tes4_bsa_reader.cpp
      to: src/detail/host_file.hpp
      via: TES4 reopened archive stream acquisition from stored resolved path
      pattern: open_host_file|read_host_file_
    - from: src/formats/ba2/ba2_gnrl_reader.cpp
      to: src/detail/host_file.hpp
      via: BA2 GNRL reopened archive stream acquisition from stored resolved path
      pattern: open_host_file|read_host_file_
    - from: src/formats/ba2/ba2_dx10_reader.cpp
      to: src/detail/host_file.hpp
      via: BA2 DX10 reopened archive stream acquisition from stored resolved path
      pattern: open_host_file|read_host_file_
---

<objective>
Align extraction contracts with the single stored host-path boundary.

Purpose: Implement D-12, D-13, D-14, and the extraction side of D-19 by landing extraction dispatch, reader contracts, and touched reader reopen implementations together while keeping validation cleanup isolated for a smaller follow-on review slice.
Output: archive extraction dispatch uses stored host-path state, and reader headers plus touched reader implementations take `detail::host_file_path` for every migrated family.
</objective>

<execution_context>
@J:/libbsa/.opencode/get-shit-done/workflows/execute-plan.md
@J:/libbsa/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-host-path-correctness-boundary/13-SPEC.md
@.planning/phases/13-host-path-correctness-boundary/13-CONTEXT.md
@.planning/phases/13-host-path-correctness-boundary/13-RESEARCH.md
@.planning/phases/13-host-path-correctness-boundary/13-VALIDATION.md
@.planning/phases/13-host-path-correctness-boundary/13-PATTERNS.md
@.planning/phases/13-host-path-correctness-boundary/13-02-PLAN.md
@src/archive.cpp

<interfaces>
From `13-PATTERNS.md` validation/read-dispatch analogs:
- Reader headers currently take `std::string_view host_path`; they should become stored-path contracts first.
- `archive.cpp` remains the single extraction dispatcher for `extract`, `extract_bytes`, and bulk extraction entry points.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate extraction dispatch plus reader contracts and reader reopens to the stored `detail::host_file_path`</name>
  <read_first>
    - src/archive.cpp
    - src/formats/bsa/tes3_bsa_reader.hpp
    - src/formats/bsa/tes3_bsa_reader.cpp
    - src/formats/bsa/tes4_bsa_reader.hpp
    - src/formats/bsa/tes4_bsa_reader.cpp
    - src/formats/ba2/ba2_gnrl_reader.hpp
    - src/formats/ba2/ba2_gnrl_reader.cpp
    - src/formats/ba2/ba2_dx10_reader.hpp
    - src/formats/ba2/ba2_dx10_reader.cpp
    - src/detail/host_path.hpp
    - .planning/phases/13-host-path-correctness-boundary/13-VALIDATION.md
    - .planning/phases/13-host-path-correctness-boundary/13-PATTERNS.md
  </read_first>
  <files>src/archive.cpp, src/formats/bsa/tes3_bsa_reader.hpp, src/formats/bsa/tes3_bsa_reader.cpp, src/formats/bsa/tes4_bsa_reader.hpp, src/formats/bsa/tes4_bsa_reader.cpp, src/formats/ba2/ba2_gnrl_reader.hpp, src/formats/ba2/ba2_gnrl_reader.cpp, src/formats/ba2/ba2_dx10_reader.hpp, src/formats/ba2/ba2_dx10_reader.cpp</files>
  <behavior>
    - Test 1: `extract`, `extract_bytes`, and bulk extraction dispatch stored host-path state only, per D-12 and D-13.
    - Test 2: reader header contracts and touched reader implementations no longer accept or reopen from raw `std::string_view host_path`.
    - Test 3: no new public overloads or archive-internal path behavior changes are introduced.
    - Test 4: validation extractability still exercises the same public reader reopen paths consumers use, per D-19.
  </behavior>
  <acceptance_criteria>
    - `archive.cpp` passes the stored `detail::host_file_path` through all extraction dispatch paths, per D-12 and D-13.
    - Reader headers and the touched reader implementations accept `const detail::host_file_path&` and use the stored resolved path in the same task, per D-13 and D-14.
    - The task follows the exact archive-dispatch and reader-header analogs from `13-PATTERNS.md`.
    - Rewritten reader interfaces and any substantially rewritten extraction methods carry the Doxygen-style doc comments required by AGENTS.md, and non-obvious stored-path reopen rules get concise why-comments explaining that the resolved path is the only post-open I/O source.
  </acceptance_criteria>
  <action>Using the `src/archive.cpp`, reader-header, and reader-implementation analogs in `13-PATTERNS.md`, change extraction dispatch to carry only `detail::host_file_path` from reader state and migrate the listed reader `.cpp` reopens in the same task so the repository stays buildable. Keep the public API unchanged per D-08, preserve the existing seek/read and format-specific extraction logic per D-05 through D-07, route reopen stream acquisition through `host_file`, and do not touch archive-internal path semantics. For every rewritten reader interface or substantially rewritten extraction method, add the Doxygen-style doc comments required by AGENTS.md, and add concise why-comments where the stored-path reopen rule or diagnostics-only UTF-8 text rule would otherwise be easy to regress.</action>
  <verify>
    <automated>cmake --build --preset windows-msvc-debug-static --target libbsa_tests &amp;&amp; ctest --preset windows-msvc-debug-static -R "archive_reader|bulk_extraction|tes3_bsa_reader|tes4_bsa_reader|ba2_gnrl_reader|ba2_dx10_extraction|validation_api" --output-on-failure &amp;&amp; if (rg "std::ifstream input\{std::string\{host_path\}" src/formats/bsa/tes3_bsa_reader.cpp src/formats/bsa/tes4_bsa_reader.cpp src/formats/ba2/ba2_gnrl_reader.cpp src/formats/ba2/ba2_dx10_reader.cpp) { exit 1 }</automated>
  </verify>
  <done>All public extraction dispatch, reader contracts, and touched reader reopens are anchored to the stored host-file path value without leaving a non-buildable intermediate state, and the rewritten reader surfaces document the stored-path-only reopen rule with the AGENTS.md-required doc comments and why-comments.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Opened reader state -&gt; extraction dispatch | Parsed archive state and stored host-path ownership cross into consumer-facing extraction operations. |
| Public extraction APIs -&gt; reader backends | Consumer-facing extraction must cross into each family backend using only the stored resolved host path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-06 | T | `src/archive.cpp` extraction dispatch | mitigate | Dispatch only the stored `detail::host_file_path` so later operations cannot re-resolve raw caller text or drift to another boundary, per D-12 and D-13. |
| T-13-07 | T | reader header and implementation family contracts | mitigate | Require `detail::host_file_path` in TES3, TES4, BA2 GNRL, and BA2 DX10 reader interfaces so no family can reopen from raw caller text, per D-13 and D-14. |
| T-13-08 | D | extraction reopen paths | mitigate | Route every migrated reader reopen through `host_file` so public extraction and later validation extractability prove the same stored-path boundary, per D-13 and D-19. |
</threat_model>

<verification>
- Build `libbsa_tests`.
- Run `ctest --preset windows-msvc-debug-static -R "validation_api|archive_reader|bulk_extraction|tes3_bsa_reader|tes4_bsa_reader|ba2_gnrl_reader|ba2_dx10_extraction" --output-on-failure`.
- Run `if (rg "std::ifstream input\{std::string\{host_path\}" src/formats/bsa/tes3_bsa_reader.cpp src/formats/bsa/tes4_bsa_reader.cpp src/formats/ba2/ba2_gnrl_reader.cpp src/formats/ba2/ba2_dx10_reader.cpp) { exit 1 }`.
</verification>

<success_criteria>
- Extraction contracts require the stored host-file path.
- All migrated reader families reopen through the shared `host_file` boundary from stored state only.
- Validation can consume the same stored-path extraction codepaths in the next plan without more reader contract churn.
</success_criteria>

<output>
After completion, create `.planning/phases/13-host-path-correctness-boundary/13-03-SUMMARY.md`
</output>
