---
phase: 13-host-path-correctness-boundary
plan: 03
type: execute
wave: 3
depends_on:
  - 13-02
files_modified:
  - src/archive.cpp
  - src/formats/bsa/tes3_bsa_parser.hpp
  - src/formats/bsa/tes3_bsa_parser.cpp
  - src/formats/bsa/tes4_bsa_parser.hpp
  - src/formats/bsa/tes4_bsa_parser.cpp
  - src/formats/ba2/ba2_gnrl_parser.hpp
  - src/formats/ba2/ba2_gnrl_parser.cpp
  - src/formats/ba2/ba2_dx10_parser.hpp
  - src/formats/ba2/ba2_dx10_parser.cpp
autonomous: true
requirements:
  - HOST-01
must_haves:
  truths:
    - "Consumers get one stable open-time result for a host path because detection, size probing, and parser entry reads all use the same resolved host path boundary."
    - "Unreadable host paths still fail at open time with direct diagnostics, while readable archives continue into metadata parsing through the shared host-file boundary."
    - "Once `archive_reader::open` succeeds, metadata parsing no longer depends on reinterpreting the caller's raw UTF-8 host-path text."
  artifacts:
    - "src/archive.cpp stores the shared internal host-file path value inside `archive_reader::state`."
    - "TES3/TES4/BA2 parser entry declarations consume the shared path contract or its resolved path primitive."
    - "Parser implementations open metadata streams through `detail::host_file` helpers rather than direct narrow-string `std::ifstream` calls."
  key_links:
    - "archive_reader::open -> detail::host_file_path resolution"
    - "archive_reader::open -> read_detection_prefix / archive_file_size via `host_file` helpers"
    - "parser *_archive_file functions -> helper-opened stream from resolved path"
---

<objective>
Move open-time state and parser-entry file opens onto the shared internal host-path contract so Windows-correct resolution happens once and drives metadata parsing consistently.

Purpose: Implement D-03, D-07, and D-08 through D-14 for `archive_reader::open` plus parser seams.
Output: open-time stored path state, detection/size probes using the shared helper, and parser signatures/opens migrated off raw host-path text.
</objective>

<execution_context>
@J:/libbsa/.opencode/get-shit-done/workflows/execute-plan.md
@J:/libbsa/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-host-path-correctness-boundary/13-CONTEXT.md
@.planning/phases/13-host-path-correctness-boundary/13-SPEC.md
@.planning/phases/13-host-path-correctness-boundary/13-RESEARCH.md
@.planning/phases/13-host-path-correctness-boundary/13-PATTERNS.md
@.planning/codebase/ARCHITECTURE.md

<interfaces>
Current seams to replace in this plan:
- `struct archive_reader::state { ... std::string host_path; bool is_ba2_dx10; }`
- `parse_tes3_bsa_archive_file(std::string_view host_path, ...)`
- `parse_tes4_bsa_archive_file(std::string_view host_path, ...)`
- `parse_ba2_gnrl_archive_file(std::string_view host_path, ...)`
- `parse_ba2_dx10_archive_file(std::string_view host_path, ...)`

After this plan, `archive_reader::state` should store the shared host-file path value and parser entry points should consume that contract or its resolved-path primitive.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Resolve and store the shared host-file path once in archive_reader::open</name>
  <files>src/archive.cpp</files>
  <behavior>
    - Test 1: empty input still fails fast with `invalid_argument`.
    - Test 2: successful open resolves the host path once, stores both original text and resolved path, and uses the resolved path for detection and size probing.
    - Test 3: direct setup failures still surface the shared helper/open diagnostics without changing public signatures.
  </behavior>
  <action>Per D-08 through D-12, update `archive_reader::open` so it resolves caller UTF-8 text once into the shared `host_file_path` contract created in Plan 13-02, stores that value inside `archive_reader::state`, and routes detection-prefix reads plus archive-size probes through `detail::host_file` helpers using the resolved path. Keep the public `std::string_view host_path` API unchanged. Add or update Doxygen-style doc comments on any substantially rewritten state-adjacent declarations so the diagnostics-only/original-text rule remains explicit.</action>
  <verify>
    <automated>cmake --build --preset windows-msvc-debug-static --target libbsa_tests &amp;&amp; ctest --preset windows-msvc-debug-static -R "archive_reader|public_include_boundary" --output-on-failure</automated>
  </verify>
  <done>`archive_reader::open` resolves once, stores the shared internal path value in reader state, and no longer depends on repeated raw-text conversion for detection or size probing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate TES3, TES4, and BA2 parser contracts plus parser opens onto the stored path boundary</name>
  <files>src/formats/bsa/tes3_bsa_parser.hpp, src/formats/bsa/tes3_bsa_parser.cpp, src/formats/bsa/tes4_bsa_parser.hpp, src/formats/bsa/tes4_bsa_parser.cpp, src/formats/ba2/ba2_gnrl_parser.hpp, src/formats/ba2/ba2_gnrl_parser.cpp, src/formats/ba2/ba2_dx10_parser.hpp, src/formats/ba2/ba2_dx10_parser.cpp, src/archive.cpp</files>
  <behavior>
    - Test 1: all parser entry points keep their existing metadata and range validation behavior after their host-file contract changes.
    - Test 2: parser implementations no longer reopen archives through direct narrow-string `std::ifstream` construction.
    - Test 3: TES3 participates in the seam migration per D-07 even though TES3 is outside the final representative regression suite.
  </behavior>
  <action>Per D-03, D-07, and D-13 through D-14, change the parser entry declarations and archive.cpp parser call sites so each parser receives the shared host-file path value or its resolved path primitive, then replace every direct parser `std::ifstream{std::string{host_path}}` open with the `host_file` seam. Preserve each parser's bounded metadata reads, count/range checks, payload-prefix callback patterns, and file-specific diagnostics. Add or refresh Doxygen-style doc comments on parser entry declarations and any substantially rewritten helper-facing functions so the new path contract is explicit to later phases.</action>
  <verify>
    <automated>cmake --build --preset windows-msvc-debug-static --target libbsa_tests &amp;&amp; ctest --preset windows-msvc-debug-static -R "tes3_bsa_reader|tes4_bsa_reader|ba2_gnrl_reader|ba2_dx10|archive_reader" --output-on-failure</automated>
  </verify>
  <done>Parser entry points and parser opens depend on the shared stored host-file boundary instead of raw host-path text, with parser semantics preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Sweep remaining raw host-path opens from the open/parser seam and lock the contract comments</name>
  <files>src/archive.cpp, src/formats/bsa/tes3_bsa_parser.cpp, src/formats/bsa/tes4_bsa_parser.cpp, src/formats/ba2/ba2_gnrl_parser.cpp, src/formats/ba2/ba2_dx10_parser.cpp</files>
  <action>Finish Plan 13-03 by removing any remaining direct narrow-string host-file opens in the open/parser seam and by tightening comments around the stored-path contract. Comments should state that original UTF-8 text becomes diagnostics-only after open succeeds and must not become a fallback I/O route. Mention any rewritten comments in the execution summary.</action>
  <verify>
    <automated>cmake --build --preset windows-msvc-debug-static --target libbsa_tests &amp;&amp; ctest --preset windows-msvc-debug-static -R "archive_reader|tes3_bsa_reader|tes4_bsa_reader|ba2_gnrl_reader|ba2_dx10|public_include_boundary" --output-on-failure &amp;&amp; pwsh -NoProfile -Command "rg -n 'std::ifstream\s*\{\s*std::string\{host_path\}|std::ifstream\s+input\{std::string\{host_path\}' src/archive.cpp src/formats/bsa/tes3_bsa_parser.cpp src/formats/bsa/tes4_bsa_parser.cpp src/formats/ba2/ba2_gnrl_parser.cpp src/formats/ba2/ba2_dx10_parser.cpp; if ($LASTEXITCODE -eq 0) { exit 1 } elseif ($LASTEXITCODE -eq 1) { exit 0 } else { exit $LASTEXITCODE }"</automated>
  </verify>
  <done>The open/parser seam has one stored path boundary, explicit comments about diagnostics-only original text, and no remaining direct narrow host-file opens in its owned files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller UTF-8 host path → `archive_reader::open` | untrusted host-path text enters the public facade here |
| `archive_reader::open` / parsers → host filesystem | resolved path drives detection, size, and metadata reads |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-03-01 | T/D | `src/archive.cpp` | mitigate | Resolve and store the host path once per D-09 through D-12 so open-time behavior cannot drift across repeated conversions. |
| T-13-03-02 | T | parser entry files | mitigate | Route parser entry opens through `detail::host_file` and preserve bounded metadata reads so malformed/truncated file handling remains intact. |
| T-13-03-03 | R | open-time diagnostics | mitigate | Keep the original UTF-8 text only for diagnostics while using the resolved path for all I/O after success per D-10 and D-12. |
</threat_model>

<verification>
- Focused open/parser gate: `cmake --build --preset windows-msvc-debug-static --target libbsa_tests && ctest --preset windows-msvc-debug-static -R "archive_reader|tes3_bsa_reader|tes4_bsa_reader|ba2_gnrl_reader|ba2_dx10|public_include_boundary" --output-on-failure`
- Narrow-open sweep: `rg -n "std::ifstream\s*\{\s*std::string\{host_path\}|std::ifstream\s+input\{std::string\{host_path\}" src/archive.cpp src/formats/bsa/tes3_bsa_parser.cpp src/formats/bsa/tes4_bsa_parser.cpp src/formats/ba2/ba2_gnrl_parser.cpp src/formats/ba2/ba2_dx10_parser.cpp`
</verification>

<success_criteria>
- `archive_reader::open` resolves and stores the shared host-file path once.
- Parser entry points consume that shared boundary.
- The open/parser seam no longer reopens archives through raw narrow-string host-path text.
</success_criteria>

<output>
After completion, create `.planning/phases/13-host-path-correctness-boundary/13-03-SUMMARY.md`
</output>
