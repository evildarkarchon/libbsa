---
phase: 13-host-path-correctness-boundary
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - src/detail/host_file.hpp
  - src/detail/host_file.cpp
  - src/detail/host_file_path.hpp
  - src/detail/host_file_path.cpp
  - src/formats/bsa/tes4_bsa_prepare.cpp
  - src/formats/bsa/tes4_bsa_layout.cpp
  - src/formats/ba2/ba2_gnrl_prepare.cpp
  - src/formats/ba2/ba2_dx10_prepare.cpp
autonomous: true
requirements:
  - HOST-01
must_haves:
  truths:
    - "Any caller migrated onto the shared host-file boundary now preserves original UTF-8 text for diagnostics while driving actual file I/O through one resolved Windows path."
    - "Migrated writer-side host-file reads keep the same observable diagnostics and helper behavior even though they no longer rely on raw text-only helper calls."
    - "The shared host-file boundary now enforces one path-interpretation policy for migrated callers instead of letting different seams resolve host paths independently."
  artifacts:
    - "src/detail/host_file_path.hpp and src/detail/host_file_path.cpp define the shared internal path value with original UTF-8 plus resolved filesystem path."
    - "src/detail/host_file.hpp and src/detail/host_file.cpp accept the shared path contract or its resolved-path primitive rather than only raw `std::string_view` text."
    - "Writer prepare/layout files construct and pass the shared internal path contract into the `host_file` helper family."
  key_links:
    - "writer prepare/layout files -> detail::host_file_path"
    - "detail::host_file_path -> detail::host_file helpers"
    - "shared host-file path contract -> later archive_reader::open / validate_archive migration"
---

<objective>
Make D-14 explicit by introducing the shared internal host-path contract and migrating the helper plus writer call sites onto it before read-side open/validation work begins.

Purpose: Converge migrated writer call sites with the same internal host-path contract the open/validate stack will use later.
Output: `host_file_path` detail value, `host_file` helper signatures that accept it, and migrated writer prepare/layout callers using that contract.
</objective>

<execution_context>
@J:/libbsa/.opencode/get-shit-done/workflows/execute-plan.md
@J:/libbsa/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-host-path-correctness-boundary/13-CONTEXT.md
@.planning/phases/13-host-path-correctness-boundary/13-SPEC.md
@.planning/phases/13-host-path-correctness-boundary/13-RESEARCH.md
@.planning/phases/13-host-path-correctness-boundary/13-PATTERNS.md

<interfaces>
This plan creates the D-14 contract used by later plans:
- `struct host_file_path { std::string original_utf8; std::filesystem::path resolved; }`
- `result<host_file_path> resolve_host_file_path(std::string_view host_path, ...)` or equivalent detail constructor helper
- `result<std::ifstream> open_host_file(const host_file_path& host_path, ...)` or equivalent resolved-path primitive overload

Writer prepare/layout callers should construct or receive this shared internal value and pass it to `host_file` helpers instead of raw text-only calls.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Introduce the shared host_file_path contract and teach host_file helpers to consume it</name>
  <files>src/detail/host_file.hpp, src/detail/host_file.cpp, src/detail/host_file_path.hpp, src/detail/host_file_path.cpp</files>
  <behavior>
    - Test 1: the shared path contract retains original UTF-8 text for diagnostics and one resolved filesystem path for actual I/O.
    - Test 2: `host_file` open/inspect/prefix/exact/chunk helpers can operate through the shared contract without changing caller-owned diagnostics.
    - Test 3: helper behavior for missing, changed, and allocation-related failures stays the same after the contract change.
  </behavior>
  <action>Per D-09 through D-14, add the small internal `host_file_path` value in `src/detail/` and update the `host_file` helper family so its active surface accepts that shared contract or its resolved-path primitive instead of only raw `std::string_view` text. Keep the original UTF-8 text diagnostics-only and keep all actual file opens on the resolved filesystem path. Preserve the layered helper shape and `std::ifstream` ownership model from D-04 and D-05. Add Doxygen-style doc comments on the new internal path type and any substantially rewritten helper declarations so the diagnostics-only/original-text rule is explicit.</action>
  <verify>
    <automated>cmake --build --preset windows-msvc-debug-static --target libbsa_tests &amp;&amp; ctest --preset windows-msvc-debug-static -R "host_file" --output-on-failure</automated>
  </verify>
  <done>The repo has one shared internal host-path value and the shared `host_file` helpers consume it while preserving current helper behavior and diagnostics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate writer prepare/layout call sites onto the shared internal host-path contract</name>
  <files>src/formats/bsa/tes4_bsa_prepare.cpp, src/formats/bsa/tes4_bsa_layout.cpp, src/formats/ba2/ba2_gnrl_prepare.cpp, src/formats/ba2/ba2_dx10_prepare.cpp</files>
  <behavior>
    - Test 1: writer call sites still read disk-backed sources successfully after switching from raw text-only helper calls to the shared path contract.
    - Test 2: file-specific diagnostics stay unchanged even though the shared contract now carries both original text and resolved path.
    - Test 3: no touched writer file continues to call `host_file` helpers through raw `std::string_view`-only host-path plumbing where the shared contract is required.
  </behavior>
  <action>Per D-02, D-10, D-11, and D-14, update the four migrated writer prepare/layout files so `detail::host_file` calls consume the shared `host_file_path` contract required by this phase. Keep writer behavior in scope: this is still not writer-side non-ASCII feature work or a writer redesign. Preserve existing diagnostics, size/read/chunk call patterns, and exact stored-byte semantics. Add or refresh doc comments where a helper-building step now exists specifically to keep writer callers on the same contract open/validation will use later.</action>
  <verify>
    <automated>cmake --build --preset windows-msvc-debug-static --target libbsa_tests &amp;&amp; ctest --preset windows-msvc-debug-static -R "host_file|tes4_bsa_writer|ba2_gnrl_writer|ba2_dx10_writer" --output-on-failure</automated>
  </verify>
  <done>Migrated writer call sites now pass the same shared internal host-path contract later plans will use for read-side open, validation, and extraction seams.</done>
</task>

<task type="auto">
  <name>Task 3: Prove D-14 convergence and remove raw-text helper drift from the migrated writer seam</name>
  <files>src/detail/host_file.hpp, src/detail/host_file.cpp, src/detail/host_file_path.hpp, src/detail/host_file_path.cpp, src/formats/bsa/tes4_bsa_prepare.cpp, src/formats/bsa/tes4_bsa_layout.cpp, src/formats/ba2/ba2_gnrl_prepare.cpp, src/formats/ba2/ba2_dx10_prepare.cpp</files>
  <action>Finish Plan 13-02 by sweeping the migrated writer seam for remaining raw-text-only `host_file` usage and tightening comments around the shared internal path contract. Comments should explain that D-14 convergence is deliberate: migrated writer callers now use the same internal path type later read-side plans will consume. Mention any rewritten comments in the execution summary.</action>
  <verify>
    <automated>cmake --build --preset windows-msvc-debug-static --target libbsa_tests &amp;&amp; ctest --preset windows-msvc-debug-static -R "host_file|tes4_bsa_writer|ba2_gnrl_writer|ba2_dx10_writer" --output-on-failure &amp;&amp; pwsh -NoProfile -Command "rg -n 'host_file\([^\n]*std::string_view|read_host_file_(prefix|exact)\([^\n]*std::string_view|inspect_host_file_size\([^\n]*std::string_view|for_each_host_file_chunk\([^\n]*std::string_view' src/formats/bsa/tes4_bsa_prepare.cpp src/formats/bsa/tes4_bsa_layout.cpp src/formats/ba2/ba2_gnrl_prepare.cpp src/formats/ba2/ba2_dx10_prepare.cpp; if ($LASTEXITCODE -eq 0) { exit 1 } elseif ($LASTEXITCODE -eq 1) { exit 0 } else { exit $LASTEXITCODE }"</automated>
  </verify>
  <done>D-14 is explicit in active code: the shared helper and migrated writer callers converge on one internal host-path contract with no leftover raw-text-only helper usage in the touched writer seam.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| writer prepare/layout callers → `host_file_path` | raw caller text is converted into the shared internal path contract here |
| `host_file_path` / `host_file` helpers → Windows filesystem | resolved filesystem path drives actual file I/O here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-02-01 | T/D | `src/detail/host_file_path.*` | mitigate | Store original UTF-8 text plus one resolved filesystem path per D-10 through D-12 so helper consumers do not drift across repeated conversions. |
| T-13-02-02 | T | migrated writer call sites | mitigate | Move the touched writer callers onto the shared contract per D-14 instead of leaving them on raw-text helper calls. |
| T-13-02-03 | R | writer diagnostics | mitigate | Preserve caller-owned context strings while changing the contract type so errors remain file- and operation-specific per D-06. |
</threat_model>

<verification>
- Focused D-14 gate: `cmake --build --preset windows-msvc-debug-static --target libbsa_tests && ctest --preset windows-msvc-debug-static -R "host_file|tes4_bsa_writer|ba2_gnrl_writer|ba2_dx10_writer" --output-on-failure`
- Migrated-writer contract sweep: `rg -n "host_file\([^\n]*std::string_view|read_host_file_(prefix|exact)\([^\n]*std::string_view|inspect_host_file_size\([^\n]*std::string_view|for_each_host_file_chunk\([^\n]*std::string_view" src/formats/bsa/tes4_bsa_prepare.cpp src/formats/bsa/tes4_bsa_layout.cpp src/formats/ba2/ba2_gnrl_prepare.cpp src/formats/ba2/ba2_dx10_prepare.cpp`
</verification>

<success_criteria>
- The shared host-file helper family consumes a shared internal path contract.
- Migrated writer callers also consume that same contract, satisfying D-14 explicitly.
- Later open/validation plans can reuse the same detail contract instead of inventing a second path type.
</success_criteria>

<output>
After completion, create `.planning/phases/13-host-path-correctness-boundary/13-02-SUMMARY.md`
</output>
