---
phase: 13-host-path-correctness-boundary
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - src/archive.cpp
  - src/formats/bsa/tes3_bsa_parser.hpp
  - src/formats/bsa/tes3_bsa_parser.cpp
  - src/formats/bsa/tes4_bsa_parser.hpp
  - src/formats/bsa/tes4_bsa_parser.cpp
  - src/formats/ba2/ba2_gnrl_parser.hpp
  - src/formats/ba2/ba2_gnrl_parser.cpp
  - src/formats/ba2/ba2_dx10_parser.hpp
  - src/formats/ba2/ba2_dx10_parser.cpp
autonomous: true
requirements:
  - HOST-01
  - HOST-02
user_setup: []
must_haves:
  truths:
    - Consumers can open supported archives through one Windows-correct host-path boundary that resolves UTF-8 input once before family-specific parsing begins, per D-08 through D-12.
    - Once open begins, every migrated parser family consumes the stored resolved host-path state rather than raw caller text, so non-ASCII open behavior cannot drift by archive family, per D-03, D-07, and D-14.
  artifacts:
    - path: src/archive.cpp
      provides: one-time host-path resolution, state storage, detection-prefix reads, and parser dispatch through the shared boundary
    - path: src/formats/bsa/tes3_bsa_parser.hpp
      provides: TES3 parser contract taking `const detail::host_file_path&`
    - path: src/formats/bsa/tes3_bsa_parser.cpp
      provides: TES3 parser open via `host_file` helper instead of direct narrow-string open
    - path: src/formats/bsa/tes4_bsa_parser.hpp
      provides: TES4 parser contract taking `const detail::host_file_path&`
    - path: src/formats/bsa/tes4_bsa_parser.cpp
      provides: parser open via `host_file` helper instead of direct narrow-string open
    - path: src/formats/ba2/ba2_gnrl_parser.hpp
      provides: BA2 GNRL parser contract taking `const detail::host_file_path&`
    - path: src/formats/ba2/ba2_gnrl_parser.cpp
      provides: BA2 GNRL parser open via `host_file` helper instead of direct narrow-string open
    - path: src/formats/ba2/ba2_dx10_parser.hpp
      provides: BA2 DX10 parser contract taking `const detail::host_file_path&`
    - path: src/formats/ba2/ba2_dx10_parser.cpp
      provides: BA2 DX10 parser open via `host_file` helper instead of direct narrow-string open
  key_links:
    - from: src/archive.cpp
      to: src/formats/bsa/tes3_bsa_parser.hpp
      via: TES3 parser signature taking stored host-file state
      pattern: parse_tes3_bsa_archive_file\(const detail::host_file_path&
    - from: src/archive.cpp
      to: src/formats/bsa/tes4_bsa_parser.hpp
      via: TES4 parser signature taking stored host-file state
      pattern: parse_tes4_bsa_archive_file\(const detail::host_file_path&
    - from: src/archive.cpp
      to: src/formats/ba2/ba2_gnrl_parser.hpp
      via: BA2 GNRL parser signature taking stored host-file state
      pattern: parse_ba2_gnrl_archive_file\(const detail::host_file_path&
    - from: src/archive.cpp
      to: src/formats/ba2/ba2_dx10_parser.hpp
      via: BA2 DX10 parser signature taking stored host-file state
      pattern: parse_ba2_dx10_archive_file\(const detail::host_file_path&
    - from: src/formats/bsa/tes3_bsa_parser.cpp
      to: src/detail/host_file.hpp
      via: TES3 parser open through the shared helper
      pattern: open_host_file|read_host_file_
    - from: src/formats/bsa/tes4_bsa_parser.cpp
      to: src/detail/host_file.hpp
      via: TES4 parser open through the shared helper
      pattern: open_host_file|read_host_file_
    - from: src/formats/ba2/ba2_gnrl_parser.cpp
      to: src/detail/host_file.hpp
      via: BA2 GNRL parser open through the shared helper
      pattern: open_host_file|read_host_file_
    - from: src/formats/ba2/ba2_dx10_parser.cpp
      to: src/detail/host_file.hpp
      via: BA2 DX10 parser open through the shared helper
      pattern: open_host_file|read_host_file_
---

<objective>
Move open-time resolution and parser entry onto the shared host-file boundary.

Purpose: Implement D-03, D-06, D-07, D-08, D-09, D-10, D-11, D-12, and D-14 so `archive_reader::open` resolves once, stores both forms internally, and hands `detail::host_file_path` into every parser entry point.
Output: updated open/state ownership plus parser signature and implementation migration landed together so the plan stays buildable end to end.
</objective>

<execution_context>
@J:/libbsa/.opencode/get-shit-done/workflows/execute-plan.md
@J:/libbsa/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-host-path-correctness-boundary/13-SPEC.md
@.planning/phases/13-host-path-correctness-boundary/13-CONTEXT.md
@.planning/phases/13-host-path-correctness-boundary/13-RESEARCH.md
@.planning/phases/13-host-path-correctness-boundary/13-VALIDATION.md
@.planning/phases/13-host-path-correctness-boundary/13-PATTERNS.md
@.planning/phases/13-host-path-correctness-boundary/13-01-PLAN.md
@src/archive.cpp

<interfaces>
From `13-PATTERNS.md` archive/parser analogs:
- `archive_reader::state` is the ownership point to replace the old `std::string host_path` field.
- Parser headers and `.cpp` files keep their current bounds/error logic; only the host-file input contract changes.
- Detection-prefix dispatch in `src/archive.cpp` stays orchestrated there, but reads must flow through the shared helper.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate open-time state ownership plus parser contracts and parser opens to `detail::host_file_path` in one buildable slice</name>
  <read_first>
    - src/archive.cpp
    - src/formats/bsa/tes3_bsa_parser.hpp
    - src/formats/bsa/tes3_bsa_parser.cpp
    - src/formats/bsa/tes4_bsa_parser.hpp
    - src/formats/bsa/tes4_bsa_parser.cpp
    - src/formats/ba2/ba2_gnrl_parser.hpp
    - src/formats/ba2/ba2_gnrl_parser.cpp
    - src/formats/ba2/ba2_dx10_parser.hpp
    - src/formats/ba2/ba2_dx10_parser.cpp
    - src/detail/host_path.hpp
    - .planning/phases/13-host-path-correctness-boundary/13-VALIDATION.md
    - .planning/phases/13-host-path-correctness-boundary/13-PATTERNS.md
  </read_first>
  <files>src/archive.cpp, src/formats/bsa/tes3_bsa_parser.hpp, src/formats/bsa/tes3_bsa_parser.cpp, src/formats/bsa/tes4_bsa_parser.hpp, src/formats/bsa/tes4_bsa_parser.cpp, src/formats/ba2/ba2_gnrl_parser.hpp, src/formats/ba2/ba2_gnrl_parser.cpp, src/formats/ba2/ba2_dx10_parser.hpp, src/formats/ba2/ba2_dx10_parser.cpp</files>
  <behavior>
    - Test 1: `archive_reader::open` still rejects empty input while resolving non-empty UTF-8 host paths once per D-08 and D-09.
    - Test 2: parser declarations and definitions require `const detail::host_file_path&` instead of raw `std::string_view host_path`.
    - Test 3: reader state stores original UTF-8 text for diagnostics and resolved filesystem path for actual I/O, with no fallback back to raw text after open succeeds.
    - Test 4: no listed parser file retains a direct narrow `std::ifstream{std::string{host_path}}` open after the contract migration lands.
  </behavior>
  <acceptance_criteria>
    - `archive_reader::state` stores `detail::host_file_path` instead of only `std::string host_path`, per D-10 and D-11.
    - `archive_reader::open` resolves UTF-8 input once and uses `host_file` helpers for detection-prefix and size inspection, per D-03, D-08, D-09, and D-14.
    - Parser headers and parser implementations all migrate in the same task so the repository still builds while later code cannot reintroduce raw caller-text opens.
    - Add doc comments or why-comments explaining why the original UTF-8 text remains diagnostics-only and why the resolved path is the only I/O source after open, per AGENTS.md and D-10 through D-12.
  </acceptance_criteria>
  <action>Follow the `src/archive.cpp`, parser-header, and parser-implementation analogs captured in `13-PATTERNS.md`. Replace the old state-owned host-path string with `detail::host_file_path`, resolve the public UTF-8 path exactly once at `archive_reader::open` per D-08 and D-09, update every listed parser declaration to accept `const detail::host_file_path&`, and migrate each parser implementation to obtain its input stream through the shared `host_file` helper in the same change. Keep the public API unchanged, keep archive-internal path behavior untouched, preserve current parser bounds/error logic per D-06 and D-07, and document the single-boundary ownership rule with tight comments/doc comments per AGENTS.md.</action>
  <verify>
    <automated>cmake --build --preset windows-msvc-debug-static --target libbsa_tests &amp;&amp; ctest --preset windows-msvc-debug-static -R "archive_reader|tes3_bsa_reader|tes4_bsa_reader|ba2_gnrl_reader|ba2_dx10_parser|validation_api" --output-on-failure &amp;&amp; if (rg "std::ifstream input\{std::string\{host_path\}" src/archive.cpp src/formats/bsa/tes3_bsa_parser.cpp src/formats/bsa/tes4_bsa_parser.cpp src/formats/ba2/ba2_gnrl_parser.cpp src/formats/ba2/ba2_dx10_parser.cpp) { exit 1 }</automated>
  </verify>
  <done>The open-time ownership model, parser contracts, and parser-side host-file reads all route through one resolved host-path boundary without leaving the plan non-buildable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public UTF-8 host path -&gt; `archive_reader::open` | Caller-controlled text must be resolved once into the only host-file path used for later parser I/O. |
| Stored host-file path -&gt; parser entry points | Parser code must consume the stored resolved path, not re-resolve or reopen from raw caller text. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04 | D | `src/archive.cpp` | mitigate | Resolve once and route detection-prefix/size inspection through `host_file` helpers so non-ASCII path handling cannot diverge across open substeps, per D-08 through D-12. |
| T-13-05 | T | parser headers and implementations | mitigate | Require `detail::host_file_path` in parser contracts and remove direct narrow opens so parser code cannot bypass the single stored boundary, per D-03, D-11, and D-14. |
</threat_model>

<verification>
- Build `libbsa_tests`.
- Run `ctest --preset windows-msvc-debug-static -R "archive_reader|tes3_bsa_reader|tes4_bsa_reader|ba2_gnrl_reader|ba2_dx10_parser|validation_api" --output-on-failure`.
- Run `if (rg "std::ifstream input\{std::string\{host_path\}" src/archive.cpp src/formats/bsa/tes3_bsa_parser.cpp src/formats/bsa/tes4_bsa_parser.cpp src/formats/ba2/ba2_gnrl_parser.cpp src/formats/ba2/ba2_dx10_parser.cpp) { exit 1 }`.
</verification>

<success_criteria>
- `archive_reader::open` owns one-time path resolution and state storage.
- Parser contracts consume `detail::host_file_path` only.
- Direct narrow parser opens are removed from the touched files.
</success_criteria>

<output>
After completion, create `.planning/phases/13-host-path-correctness-boundary/13-02-SUMMARY.md`
</output>
