---
phase: 13-host-path-correctness-boundary
plan: 04
type: execute
wave: 4
depends_on:
  - 13-03
files_modified:
  - src/validation.cpp
  - tests/unit/host_path_correctness_boundary_tests.cpp
  - tests/CMakeLists.txt
autonomous: true
requirements:
  - HOST-01
  - HOST-02
  - HOST-03
user_setup: []
must_haves:
  truths:
    - Consumers can validate representative TES4 and BA2 archives from non-ASCII Windows host paths through the same `archive_reader::open` setup boundary used by ordinary opens and follow-on extraction, per D-15 through D-19.
    - Consumers can open representative TES4 and BA2 archives and then complete one canonical payload extraction from the same non-ASCII Windows host paths, per D-21 through D-27.
    - Maintainers have one committed black-box suite that proves the shared host-file boundary outcome across the locked representative archive matrix, per HOST-01 through HOST-03.
  artifacts:
    - path: src/validation.cpp
      provides: validation setup unified behind `archive_reader::open` with no duplicate readability preflight
    - path: tests/unit/host_path_correctness_boundary_tests.cpp
      provides: dedicated cross-family non-ASCII host-path regression suite
    - path: tests/CMakeLists.txt
      provides: unit-test registration for the dedicated Phase 13 suite
  key_links:
    - from: src/validation.cpp
      to: src/archive.cpp
      via: `archive_reader::open` plus public `reader.extract(...)` reuse for extractability validation
      pattern: archive_reader::open|reader.extract
    - from: tests/unit/host_path_correctness_boundary_tests.cpp
      to: tests/fixtures/generated/archives/*.json
      via: manifest-backed canonical extraction assertions
      pattern: manifest|extract_bytes|validate_archive
    - from: tests/CMakeLists.txt
      to: catch_discover_tests
      via: tag-driven suite registration
      pattern: host_path_correctness_boundary(_smoke)?
---

<objective>
Finish validation setup cleanup and prove the host-path fix from the public API surface with one dedicated regression suite.

Purpose: Implement D-15 through D-27 that remain after reader migration by removing validation's duplicate readability preflight, then closing HOST-01, HOST-02, and HOST-03 with deterministic black-box coverage across the locked representative archive set.
Output: cleaned-up `src/validation.cpp`, new `tests/unit/host_path_correctness_boundary_tests.cpp`, CMake registration, and targeted smoke/full filters for non-ASCII host-path open/validate/extract proofs.
</objective>

<execution_context>
@J:/libbsa/.opencode/get-shit-done/workflows/execute-plan.md
@J:/libbsa/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-host-path-correctness-boundary/13-SPEC.md
@.planning/phases/13-host-path-correctness-boundary/13-CONTEXT.md
@.planning/phases/13-host-path-correctness-boundary/13-RESEARCH.md
@.planning/phases/13-host-path-correctness-boundary/13-VALIDATION.md
@.planning/phases/13-host-path-correctness-boundary/13-PATTERNS.md
@src/validation.cpp
@tests/CMakeLists.txt
@tests/unit/validation_api_tests.cpp
@tests/unit/tes4_bsa_reader_tests.cpp
@tests/unit/ba2_gnrl_reader_tests.cpp
@tests/unit/ba2_dx10_extraction_tests.cpp

<interfaces>
From `13-PATTERNS.md` regression-suite analogs:
- `src/validation.cpp` must keep the current result/report split while deleting the duplicate readability preflight.
- Reuse the temp-path helper style from `validation_api_tests.cpp`.
- Reuse manifest-backed public extraction assertions from the existing TES4/BA2 reader tests.
- Keep this suite black-box and public-surface only.

Locked representative scope:
- TES4 fixtures: `v103`, `v104`, `v105`
- BA2 fixtures: Fallout 4 GNRL, Fallout 4 DX10, Starfield GNRL
- non-ASCII token: `libbsa-Ångström-日本語`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove validation's duplicate readability preflight while preserving result/report semantics</name>
  <read_first>
    - src/validation.cpp
    - src/archive.cpp
    - .planning/phases/13-host-path-correctness-boundary/13-CONTEXT.md
    - .planning/phases/13-host-path-correctness-boundary/13-PATTERNS.md
  </read_first>
  <files>src/validation.cpp</files>
  <behavior>
    - Test 1: empty input still returns `invalid_argument`, per D-16.
    - Test 2: unreadable/non-openable host paths still return direct `io_error` results from the shared open boundary, per D-16 and D-17.
    - Test 3: readable malformed bytes still become validation diagnostics rather than direct setup failures, per D-16 and D-18.
    - Test 4: `validate_entry_extractability` continues to prove payload reads through `reader.extract(...)`, per D-19.
  </behavior>
  <acceptance_criteria>
    - `host_path_can_be_opened` is removed from `src/validation.cpp`, per D-15.
    - `validate_archive` trusts `archive_reader::open` as the one real setup path and continues to route extractability through `reader.extract(...)`, per D-15 through D-19.
    - Comments/doc comments explain why setup failures return direct errors while malformed readable archives still become `validation_report` content, per AGENTS.md.
  </acceptance_criteria>
  <action>Follow the `src/validation.cpp` analog and guidance in `13-PATTERNS.md`: delete the duplicate readability preflight, keep `archive_reader::open` as the only setup boundary per D-15, preserve the current result/report split per D-16 through D-18, and keep extractability on the public extraction path per D-19. Add concise why-comments or doc comments on the non-obvious contract edges so later maintainers do not reintroduce divergent validation setup behavior.</action>
  <verify>
    <automated>cmake --build --preset windows-msvc-debug-static --target libbsa_tests &amp;&amp; ctest --preset windows-msvc-debug-static -R "validation_api|archive_reader|bulk_extraction" --output-on-failure &amp;&amp; if (rg "host_path_can_be_opened" src/validation.cpp) { exit 1 }</automated>
  </verify>
  <done>Validation setup is unified behind `archive_reader::open` and still reports setup-versus-archive failures with the shipped public semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Add and register the dedicated non-ASCII host-path suite with smoke coverage</name>
  <read_first>
    - tests/CMakeLists.txt
    - tests/unit/validation_api_tests.cpp
    - tests/unit/tes4_bsa_reader_tests.cpp
    - tests/unit/ba2_gnrl_reader_tests.cpp
    - tests/unit/ba2_dx10_extraction_tests.cpp
    - .planning/phases/13-host-path-correctness-boundary/13-PATTERNS.md
  </read_first>
  <files>tests/unit/host_path_correctness_boundary_tests.cpp, tests/CMakeLists.txt</files>
  <acceptance_criteria>
    - `tests/unit/host_path_correctness_boundary_tests.cpp` exists as one dedicated suite per D-21 and is registered in `tests/CMakeLists.txt`.
    - File-local helpers build both a non-ASCII directory and non-ASCII archive filename using the exact token `libbsa-Ångström-日本語`, per D-24 and D-25.
    - The suite carries `host_path_correctness_boundary` coverage plus one stable `host_path_correctness_boundary_smoke` subset/tag for fast feedback.
    - The task follows the regression-suite analogs documented in `13-PATTERNS.md` and does not introduce white-box hooks, open-count assertions, or internal-only probes.
  </acceptance_criteria>
  <action>Use the regression analogs mapped in `13-PATTERNS.md` to create one dedicated Catch2 suite and register it in `tests/CMakeLists.txt`. Reuse the existing file-local helper style, add helpers that copy only the archive under test into a temp directory and filename containing the exact `libbsa-Ångström-日本語` token, and add a stable smoke subset/tag so task-level feedback stays fast while the broader suite remains discoverable via `host_path_correctness_boundary`.</action>
  <verify>
    <automated>cmake --build --preset windows-msvc-debug-static --target libbsa_tests &amp;&amp; ctest --preset windows-msvc-debug-static -R host_path_correctness_boundary_smoke --output-on-failure</automated>
  </verify>
  <done>The dedicated suite is built, registered, and exposes both smoke and full targeted execution paths.</done>
</task>

<task type="auto">
  <name>Task 3: Cover the locked representative archive matrix for open, validate, and canonical extraction</name>
  <read_first>
    - tests/unit/host_path_correctness_boundary_tests.cpp
    - tests/fixtures/generated/archives/tes4_v103_manifest.json
    - tests/fixtures/generated/archives/tes4_v104_manifest.json
    - tests/fixtures/generated/archives/tes4_v105_manifest.json
    - tests/fixtures/generated/archives/ba2_gnrl_fo4_manifest.json
    - tests/fixtures/generated/archives/ba2_dx10_fo4_manifest.json
    - tests/fixtures/generated/archives/ba2_gnrl_sfv3_manifest.json
    - .planning/phases/13-host-path-correctness-boundary/13-CONTEXT.md
    - .planning/phases/13-host-path-correctness-boundary/13-PATTERNS.md
  </read_first>
  <files>tests/unit/host_path_correctness_boundary_tests.cpp</files>
  <acceptance_criteria>
    - The suite covers TES4 v103/v104/v105 plus Fallout 4 BA2 GNRL, Fallout 4 BA2 DX10, and Starfield BA2 GNRL, per D-22.
    - Each case copies only the archive under test into the non-ASCII location while keeping manifests and expected payload bytes in the committed fixture tree, per D-23.
    - Each case proves `archive_reader::open`, `validate_archive(..., {.validate_entry_extractability = true})`, and one canonical manifest-backed extraction result, per D-26 and D-27.
    - The suite remains black-box and deterministic, proving only the public API surface required by HOST-01 through HOST-03.
  </acceptance_criteria>
  <action>Populate the dedicated suite using the exact representative matrix locked in D-22. For each case, copy only the archive under test into the non-ASCII temp location, call `archive_reader::open`, call `validate_archive` with `validate_entry_extractability = true`, and assert one canonical manifest-backed extraction target matches expected bytes. Keep the suite aligned with the public test analogs in `13-PATTERNS.md`, and do not add any white-box setup instrumentation or extra in-scope expansion.</action>
  <verify>
    <automated>cmake --build --preset windows-msvc-debug-static --target libbsa_tests &amp;&amp; ctest --preset windows-msvc-debug-static -R host_path_correctness_boundary --output-on-failure &amp;&amp; ctest --preset windows-msvc-debug-static --output-on-failure</automated>
  </verify>
  <done>The committed regression suite proves non-ASCII host-path open, validation, and canonical extraction across the locked Phase 13 representative archive set.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Non-ASCII temp archive path -&gt; public archive APIs | The suite must exercise the real Windows Unicode host-path boundary instead of an ASCII-only surrogate. |
| Fixture manifests -&gt; canonical extraction assertions | Expected payload bytes remain in committed fixtures while only the archive file is copied to temp. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-12 | D | `tests/unit/host_path_correctness_boundary_tests.cpp` | mitigate | Use the exact non-ASCII directory and filename token in each case so Unicode host-path regressions cannot hide behind ASCII-only coverage, per D-24 and D-25. |
| T-13-13 | T | validation/extraction proof path | mitigate | Prove `validate_entry_extractability` and one public extraction call on the same copied path so divergent setup-versus-extraction behavior is caught, per D-19, D-20, D-26, and D-27. |
| T-13-14 | R | fixture handling | mitigate | Copy only the archive under test and keep manifests/expected bytes in the committed tree so failures isolate the host-path boundary rather than temp-fixture sprawl, per D-23. |
</threat_model>

<verification>
- Build `libbsa_tests`.
- Run `ctest --preset windows-msvc-debug-static -R "validation_api|archive_reader|bulk_extraction" --output-on-failure`.
- Run `ctest --preset windows-msvc-debug-static -R host_path_correctness_boundary_smoke --output-on-failure`.
- Run `ctest --preset windows-msvc-debug-static -R host_path_correctness_boundary --output-on-failure`.
- Run the full preset suite once after the targeted suite passes.
</verification>

<success_criteria>
- Validation has one real setup path and preserved public semantics.
- One dedicated deterministic suite proves HOST-01, HOST-02, and HOST-03.
- The exact locked representative archive set and the exact locked non-ASCII token are covered.
- Coverage stays black-box and public-surface only.
</success_criteria>

<output>
After completion, create `.planning/phases/13-host-path-correctness-boundary/13-04-SUMMARY.md`
</output>
