---
phase: 13-host-path-correctness-boundary
plan: 04
type: execute
wave: 4
depends_on:
  - 13-03
files_modified:
  - src/archive.cpp
  - src/validation.cpp
  - src/formats/bsa/tes3_bsa_reader.hpp
  - src/formats/bsa/tes3_bsa_reader.cpp
  - src/formats/bsa/tes4_bsa_reader.hpp
  - src/formats/bsa/tes4_bsa_reader.cpp
  - src/formats/ba2/ba2_gnrl_reader.hpp
  - src/formats/ba2/ba2_gnrl_reader.cpp
  - src/formats/ba2/ba2_dx10_reader.hpp
  - src/formats/ba2/ba2_dx10_reader.cpp
autonomous: true
requirements:
  - HOST-01
  - HOST-02
must_haves:
  truths:
    - "After `archive_reader::open` succeeds, follow-on extraction calls still succeed from the same archive because reader reopens use the stored resolved host path."
    - "`validate_archive` keeps direct setup failures for unreadable paths separate from report-based diagnostics for readable malformed archives."
    - "When extractability validation is enabled, it still exercises real payload reads through the public reader extraction path rather than a validation-only shortcut."
  artifacts:
    - "archive.cpp extraction dispatch passes the stored host-file path into format readers."
    - "TES3/TES4/BA2 reader headers and implementations consume the shared host-file path boundary."
    - "validation.cpp removes the duplicate readability preflight and keeps stable setup-vs-report behavior."
  key_links:
    - "archive_reader::state.host_file_path -> extract_* reader calls"
    - "validate_archive -> archive_reader::open"
    - "validate_extractability -> public reader extraction path"
---

<objective>
Finish the runtime host-path boundary by moving follow-on extraction reopens and validation setup onto the stored shared host-file path while preserving the public validation contract.

Purpose: Implement D-13 through D-20 for reader reopens and validation cleanup.
Output: reader reopen contracts migrated to the shared path value, extraction dispatch updated, and `validate_archive` setup drift removed through separately gated runtime and validation tasks.
</objective>

<execution_context>
@J:/libbsa/.opencode/get-shit-done/workflows/execute-plan.md
@J:/libbsa/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-host-path-correctness-boundary/13-CONTEXT.md
@.planning/phases/13-host-path-correctness-boundary/13-SPEC.md
@.planning/phases/13-host-path-correctness-boundary/13-RESEARCH.md
@.planning/phases/13-host-path-correctness-boundary/13-PATTERNS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/milestones/v1.0-phases/11-compatibility-warnings-validation-api-and-hardening/11-02-SUMMARY.md

<interfaces>
Current seams to replace in this plan:
- `extract_tes3_bsa_payload(std::string_view host_path, ...)`
- `extract_tes4_bsa_payload_from_file(std::string_view host_path, ...)`
- `extract_ba2_gnrl_payload(std::string_view host_path, ...)`
- BA2 DX10 reader-local archive reopen from raw host-path text
- `validate_archive(std::string_view host_path, validation_options)` with duplicate readability preflight

After this plan, reader reopen declarations should consume the shared internal host-file path value and validation should rely on `archive_reader::open` as the one setup path.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewire extraction dispatch and concrete readers onto the stored shared host-file path</name>
  <files>src/archive.cpp, src/formats/bsa/tes3_bsa_reader.hpp, src/formats/bsa/tes3_bsa_reader.cpp, src/formats/bsa/tes4_bsa_reader.hpp, src/formats/bsa/tes4_bsa_reader.cpp, src/formats/ba2/ba2_gnrl_reader.hpp, src/formats/ba2/ba2_gnrl_reader.cpp, src/formats/ba2/ba2_dx10_reader.hpp, src/formats/ba2/ba2_dx10_reader.cpp</files>
  <behavior>
    - Test 1: all follow-on extraction paths reopen from the stored resolved host path rather than raw caller text.
    - Test 2: payload range checks, decompression routing, and format-specific error semantics remain unchanged after the contract swap.
    - Test 3: this task stops at reader reopen and extraction-dispatch changes; validation setup semantics stay unchanged until Task 2.
  </behavior>
  <action>Per D-13 and D-14, update `archive.cpp` extraction dispatch plus every concrete extraction header/implementation so follow-on reads reopen only from the stored internal host-file path value. Replace reader-local narrow opens with the shared `host_file` seam while preserving payload offset/span checks, decompression behavior, and error-code semantics. Do not re-resolve raw caller text anywhere in these reader paths. Keep this task strictly on reader reopen and extraction dispatch plumbing; do not remove `validate_archive` setup logic here because the validation-contract cleanup is the separate concern of Task 2. Add or refresh Doxygen-style doc comments on reader reopen declarations and any substantially rewritten extraction functions so the stored resolved-path contract is explicit.</action>
  <verify>
    <automated>cmake --build --preset windows-msvc-debug-static --target libbsa_tests &amp;&amp; ctest --preset windows-msvc-debug-static -R "tes3_bsa_reader|tes4_bsa_reader|ba2_gnrl_reader|ba2_dx10|archive_reader" --output-on-failure</automated>
  </verify>
  <done>All follow-on extraction reopens flow from the stored resolved path in reader state through the shared host-file helper.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove validation preflight drift and preserve the public result/report split</name>
  <files>src/validation.cpp, src/archive.cpp</files>
  <behavior>
    - Test 1: empty input still returns `invalid_argument` as a direct failed result.
    - Test 2: unreadable or non-openable host paths still return direct `io_error` results with shared open diagnostics.
    - Test 3: readable malformed archives still become `validation_report` diagnostics, and extractability validation continues to use the reader reopen path already landed in Task 1 rather than introducing a validation-only shortcut.
  </behavior>
  <action>Per D-15 through D-20, delete `validate_archive`'s duplicate readability preflight and make validation setup trust the shared host-file boundary plus `archive_reader::open` as the one real setup path. Preserve the existing public split exactly: unreadable host paths stay direct failed results, readable malformed bytes stay report-based diagnostics, and setup failure before parsing returns a failed `result<validation_report>` with no partial report object. Keep extractability validation on the public reader path only, reusing the reader reopen contract that Task 1 already established instead of reopening this seam here. Add or refresh Doxygen-style doc comments on any substantially rewritten validation helpers whose setup-vs-report behavior is non-obvious.</action>
  <verify>
    <automated>cmake --build --preset windows-msvc-debug-static --target libbsa_tests &amp;&amp; ctest --preset windows-msvc-debug-static -R "validation_api|archive_reader|tes3_bsa_reader|tes4_bsa_reader|ba2_gnrl_reader|ba2_dx10" --output-on-failure</automated>
  </verify>
  <done>Validation setup is unified behind `archive_reader::open`, and public failure semantics remain stable for unreadable paths, malformed bytes, and extractability checks.</done>
</task>

<task type="auto">
  <name>Task 3: Sweep remaining read/validate narrow-open drift and lock the resolved-path rule in comments</name>
  <files>src/archive.cpp, src/validation.cpp, src/formats/bsa/tes3_bsa_reader.cpp, src/formats/bsa/tes4_bsa_reader.cpp, src/formats/ba2/ba2_gnrl_reader.cpp, src/formats/ba2/ba2_dx10_reader.cpp</files>
  <action>Finish Plan 13-04 by removing any remaining direct narrow-string host-file opens from the read/validate runtime seam and by updating non-obvious comments to explain why reopened reads must come from the stored resolved path, not raw UTF-8 text. Mention any rewritten comments in the execution summary because this plan changes comment intent around reader reopen ownership and validation setup.</action>
  <verify>
    <automated>cmake --build --preset windows-msvc-debug-static --target libbsa_tests &amp;&amp; ctest --preset windows-msvc-debug-static -R "validation_api|archive_reader|tes3_bsa_reader|tes4_bsa_reader|ba2_gnrl_reader|ba2_dx10" --output-on-failure &amp;&amp; pwsh -NoProfile -Command "rg -n 'std::ifstream\s*\{\s*std::string\{host_path\}|std::ifstream\s+input\{std::string\{host_path\}' src/archive.cpp src/validation.cpp src/formats/bsa/tes3_bsa_reader.cpp src/formats/bsa/tes4_bsa_reader.cpp src/formats/ba2/ba2_gnrl_reader.cpp src/formats/ba2/ba2_dx10_reader.cpp; if ($LASTEXITCODE -eq 0) { exit 1 } elseif ($LASTEXITCODE -eq 1) { exit 0 } else { exit $LASTEXITCODE }"</automated>
  </verify>
  <done>The read/validate runtime seam uses the stored resolved path consistently, validation no longer has setup drift, and comments explain the non-obvious boundary rules.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| opened reader state → format reader reopen paths | follow-on file access crosses here |
| validation caller → `validate_archive` setup | setup-vs-report behavior crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04-01 | T/D | reader reopen paths | mitigate | Reopen only from the stored resolved path in `archive_reader::state` per D-13 and D-14 so extraction cannot regress to inconsistent host-path resolution. |
| T-13-04-02 | R | `src/validation.cpp` | mitigate | Remove the duplicate readability preflight and preserve the direct result/report split per D-15 through D-18 so setup failures cannot be misreported as archive diagnostics. |
| T-13-04-03 | D | extractability validation | mitigate | Keep extractability validation on the public reader extraction path and preserve entry-size guards so malformed metadata cannot create a new validation-only allocation route. |
</threat_model>

<verification>
- Focused runtime gate: `cmake --build --preset windows-msvc-debug-static --target libbsa_tests && ctest --preset windows-msvc-debug-static -R "validation_api|archive_reader|tes3_bsa_reader|tes4_bsa_reader|ba2_gnrl_reader|ba2_dx10" --output-on-failure`
- Narrow-open sweep: `rg -n "std::ifstream\s*\{\s*std::string\{host_path\}|std::ifstream\s+input\{std::string\{host_path\}" src/archive.cpp src/validation.cpp src/formats/bsa/tes3_bsa_reader.cpp src/formats/bsa/tes4_bsa_reader.cpp src/formats/ba2/ba2_gnrl_reader.cpp src/formats/ba2/ba2_dx10_reader.cpp`
</verification>

<success_criteria>
- Reader extraction reopens use the stored resolved path only.
- `validate_archive` no longer duplicates host-path setup logic.
- Result-level setup failures and report-level readable-archive diagnostics remain distinct.
</success_criteria>

<output>
After completion, create `.planning/phases/13-host-path-correctness-boundary/13-04-SUMMARY.md`
</output>
