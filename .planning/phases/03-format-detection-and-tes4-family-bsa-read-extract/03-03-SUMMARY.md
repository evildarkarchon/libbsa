---
phase: 03-format-detection-and-tes4-family-bsa-read-extract
plan: 03
subsystem: testing
tags: [cpp, fixtures, bsa, malformed, python, validation]

# Dependency graph
requires:
  - phase: 03-format-detection-and-tes4-family-bsa-read-extract
    provides: 03-02 success fixture generator and manifests
provides:
  - Malformed TES4-family BSA fixture archives for open-time and extraction-time parser failures
  - Machine-readable malformed fixture manifest with stable expected error categories
  - Python manifest validation script for success and malformed fixture metadata
affects: [tes4-bsa-parser, malformed-tests, fixture-manifests, compression-routing-tests]

# Tech tracking
tech-stack:
  added: [python-standard-library-json]
  patterns: [single-cpp-fixture-generator, manifest-schema-validation, tdd-red-green-script]

key-files:
  created:
    - tests/fixtures/generated/archives/malformed_unsupported_version.bsa
    - tests/fixtures/generated/archives/malformed_truncated_header.bsa
    - tests/fixtures/generated/archives/malformed_truncated_table.bsa
    - tests/fixtures/generated/archives/malformed_duplicate_canonical_path.bsa
    - tests/fixtures/generated/archives/malformed_corrupt_compressed_payload.bsa
    - tests/fixtures/generated/archives/malformed_size_mismatch.bsa
    - tests/fixtures/generated/archives/malformed_non_bsa_bytes.bsa
    - tests/fixtures/generated/archives/malformed_manifest.json
    - tests/fixtures/generated/validate_fixture_manifests.py
  modified:
    - tests/fixtures/generated/generate_tes4_bsa_fixtures.cpp

key-decisions:
  - "Malformed fixtures remain generated by the existing C++ fixture tool so success and malformed archive layouts share the same hash/compression helpers."
  - "Manifest validation stays parser-free and uses only Python's standard json module so Wave 2 validates fixture expectations before parser behavior exists."

patterns-established:
  - "Malformed manifests classify failures by stable category and phase (`open` or `extraction`) rather than implementation-specific messages."
  - "Extraction-time malformed compression fixtures preserve openable archive structure while corrupting only the payload or exact-size prefix."

requirements-completed: [FMT-01, FMT-06, BSA-07]

# Metrics
duration: 10min
completed: 2026-05-08
---

# Phase 03 Plan 03: Malformed Fixture Set and Manifest Validation Summary

**Deterministic malformed TES4-family BSA fixtures with parser-free manifest validation for future fail-closed reader tests**

## Performance

- **Duration:** 10 min
- **Started:** 2026-05-08T07:29:00Z
- **Completed:** 2026-05-08T07:38:59Z
- **Tasks:** 2
- **Files modified:** 10

## Accomplishments

- Extended the single C++ TES4-family fixture generator with all D-27 malformed cases: unsupported version, truncated header/table, duplicate canonical path, corrupt compressed payload, size mismatch, and non-BSA bytes in a `.bsa` host filename.
- Added `malformed_manifest.json` with case IDs, archive filenames, requirement/threat references, failure phase, and stable expected error categories.
- Added a parser-free Python validation script that checks the three success manifests and the malformed manifest using only the standard `json` module.

## Task Commits

Each task was committed atomically:

1. **Task 1: Generate malformed outputs with the C++ fixture generator** - `632522e` (feat)
2. **Task 2 RED: Add manifest validation script failing gate** - `086cdf2` (test)
3. **Task 2 GREEN: Add manifest validation script for success and malformed fixtures** - `9ad82ca` (feat)

**Plan metadata:** `871e2af` (docs)

_Note: Task 2 followed the TDD flow with a RED script commit followed by the passing GREEN commit._

## Files Created/Modified

- `tests/fixtures/generated/generate_tes4_bsa_fixtures.cpp` - Adds malformed-mode generation, byte mutation helpers, duplicate canonical path archive creation, and malformed manifest emission.
- `tests/fixtures/generated/archives/malformed_unsupported_version.bsa` - BSA magic with unsupported future version for detection tests.
- `tests/fixtures/generated/archives/malformed_truncated_header.bsa` - Header shorter than the fixed TES4-family header.
- `tests/fixtures/generated/archives/malformed_truncated_table.bsa` - Valid-looking header with truncated table bytes.
- `tests/fixtures/generated/archives/malformed_duplicate_canonical_path.bsa` - Archive containing two file records that normalize to `meshes/dupe/same.txt`.
- `tests/fixtures/generated/archives/malformed_corrupt_compressed_payload.bsa` - Openable archive with mutated compressed payload bytes for extraction-time `format_error` coverage.
- `tests/fixtures/generated/archives/malformed_size_mismatch.bsa` - Openable archive with deliberately wrong compressed exact-size prefix.
- `tests/fixtures/generated/archives/malformed_non_bsa_bytes.bsa` - Misleading `.bsa` filename containing non-BSA bytes.
- `tests/fixtures/generated/archives/malformed_manifest.json` - Machine-readable case index for malformed fixtures.
- `tests/fixtures/generated/validate_fixture_manifests.py` - Parser-free manifest schema and case validation script.

## Decisions Made

- Malformed archive generation stays in the C++ fixture generator instead of a Python generator so the same library internals create success and malformed archive bytes.
- Success manifests are validated for their existing schema rather than forcing a new `manifest_kind` field, keeping Plan 03-03 compatible with Plan 03-02 outputs.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

- The first direct tool invocation used the target name instead of the generated executable name; rerunning `generate_tes4_bsa_fixtures_tool.exe` with `--malformed` verified the generated files successfully.

## User Setup Required

None - no external service configuration required.

## TDD Gate Compliance

- RED gate commit present: `086cdf2` (`test(03-03): add failing manifest validation coverage`).
- GREEN gate commit present after RED: `9ad82ca` (`feat(03-03): validate generated fixture manifests`).
- Refactor gate not needed; no cleanup-only changes were required after GREEN.

## Known Stubs

None found in files created or modified by this plan.

## Next Phase Readiness

- Plan 03-04 can consume committed malformed fixtures to test byte-driven detector/open behavior before implementing full table parsing.
- Later extraction/compression tests can use `corrupt_compressed_payload` and `size_mismatch` as openable archives that fail during extraction instead of at detection.

## Self-Check: PASSED

- Verified all created malformed archives, manifests, and validation script exist.
- Verified task commits exist in git history: `632522e`, `086cdf2`, `9ad82ca`.

---
*Phase: 03-format-detection-and-tes4-family-bsa-read-extract*
*Completed: 2026-05-08*
