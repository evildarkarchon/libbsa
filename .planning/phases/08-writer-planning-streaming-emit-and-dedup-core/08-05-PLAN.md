---
phase: 08-writer-planning-streaming-emit-and-dedup-core
plan: 05
type: tdd
wave: 5
depends_on: [04]
files_modified:
  - tests/writer_harness_helpers.hpp
  - tests/writer_harness_helpers.cpp
  - tests/writer_core_tests.cpp
autonomous: true
requirements: [WRT-07, WRT-05, WRT-06]
must_haves:
  truths:
    - "Maintainer can reopen generated writer-core harness bytes in tests and compare metadata to the plan per D-13."
    - "Round-trip extraction through the harness helper returns original uncompressed input payload bytes while using a generated test-only generic harness archive format per D-11."
    - "Compression-policy integration is proven through archive-default compressed planning, exact stored-size preview assertions, and unsupported codec route failures via real codec paths per D-17."
    - "Harness helpers are test-only and do not expose a public fake archive format per D-12, and are organized for reuse by Phase 9/10 tests per D-18."
    - "Harness bytes are source-built inside tests with enough header/table/data-region/payload structure to exercise real layout decisions, with no committed binary goldens, per D-14 and D-16."
    - "Harness malformed coverage remains writer-focused around invalid inputs, unsupported options, layout validation, checked arithmetic, codec-route failures, dedup behavior, and sink failure per D-15."
  artifacts:
    - path: "tests/writer_harness_helpers.hpp"
      provides: "Test-only harness parser and assertion helper declarations"
      contains: "read_writer_harness"
    - path: "tests/writer_harness_helpers.cpp"
      provides: "Harness reader, metadata comparison, and extraction helpers"
      contains: "require_harness_matches_plan"
    - path: "tests/writer_core_tests.cpp"
      provides: "Read-after-write round-trip tests over generated harness bytes"
      contains: "reads finalized harness bytes back and compares plan metadata"
  key_links:
    - from: "tests/writer_core_tests.cpp"
      to: "tests/writer_harness_helpers.cpp"
      via: "read_writer_harness and require_harness_matches_plan"
      pattern: "read_writer_harness"
---

<objective>
TDD generated harness read-after-write validation.

Purpose: WRT-07 and D-11 through D-18 require source-built test-only harness proof of plan-to-stream, metadata comparison, and round-trip extraction without claiming production BSA/BA2 writer compatibility.
Output: Test-only harness reader/assertion helpers and passing read-after-write tests.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
@C:/Users/evild/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-writer-planning-streaming-emit-and-dedup-core/08-SPEC.md
@.planning/phases/08-writer-planning-streaming-emit-and-dedup-core/08-CONTEXT.md
@.planning/phases/08-writer-planning-streaming-emit-and-dedup-core/08-PATTERNS.md
@include/libbsa/writer.hpp
@tests/writer_harness_helpers.hpp
@tests/writer_harness_helpers.cpp
@tests/writer_core_tests.cpp
@tests/ba2_dds_fixture_helpers.hpp
@tests/ba2_dds_fixture_helpers.cpp
</context>

<feature>
  <name>Generated harness read-after-write seam</name>
  <files>tests/writer_harness_helpers.hpp, tests/writer_harness_helpers.cpp, tests/writer_core_tests.cpp</files>
  <behavior>
    - Finalized bytes from `finalize_archive_write` parse through test-only `read_writer_harness`.
    - `require_harness_matches_plan(parsed, plan)` checks path order, table/data region counts, region IDs, archive-absolute offsets, stored sizes, compression states, dedup references, and total size.
    - `extract_writer_harness_entry(parsed, "meshes/a.nif")` returns original uncompressed payload bytes for raw and deflate-compressed entries by using `decompress_payload` with the planned compression state.
    - Planning with `compression_policy::archive_default` and `writer_target::archive_default_compressed = true` records `compression_state::deflate` for a deflate-capable harness target, records an exact `stored_size` equal to `planned_data_region::stored_payload.size()`, and final emitted harness bytes contain the same stored-size metadata.
    - An unsupported codec route, such as a target whose resolved state would require LZ4 block without a Starfield method-3 descriptor, returns `error_code::unsupported_format` instead of falling back to deflate/raw.
    - No binary golden harness archive files are committed.
  </behavior>
  <implementation>Implement a parser for the `LBSW` stream emitted by Plan 04 in test helpers only. Keep helpers in `namespace libbsa::test`, use `REQUIRE`/`CHECK` assertions in `require_harness_matches_plan`, and use real `resolve_payload_codec`/`decompress_payload` for extraction. Do not add any public harness API under `include/libbsa/`.</implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED harness read-back tests</name>
  <files>tests/writer_core_tests.cpp, tests/writer_harness_helpers.hpp</files>
  <read_first>tests/writer_core_tests.cpp, tests/writer_harness_helpers.hpp, include/libbsa/writer.hpp, .planning/phases/08-writer-planning-streaming-emit-and-dedup-core/08-CONTEXT.md</read_first>
  <action>Declare test-only types/functions in `tests/writer_harness_helpers.hpp` following the exact test-helper analogs in `08-PATTERNS.md` from `tests/ba2_dds_fixture_helpers.hpp`: `struct parsed_writer_harness_entry`, `struct parsed_writer_harness`, `parsed_writer_harness read_writer_harness(std::span<const std::byte> bytes)`, `std::vector<std::byte> extract_writer_harness_entry(const parsed_writer_harness& harness, std::string_view path)`, and `void require_harness_matches_plan(const parsed_writer_harness& harness, const libbsa::write_plan& plan)`. Add tests named exactly `reads finalized harness bytes back and compares plan metadata`, `round trips compressed harness payloads through real codec routes`, `archive default compression records exact stored size in plan and harness`, and `writer planning rejects unsupported codec route without fallback`. The first test must plan raw entries with dedup enabled, finalize to `memory_sink`, read harness bytes, call `require_harness_matches_plan`, and assert extracting both duplicate paths returns original payload `{0x44,0x55}`. The second test must use `compression_policy::force_compressed` on a deflate-capable target and assert extracted bytes equal `{0x10,0x20,0x30,0x40}`. The archive-default test must set `target.archive_default_compressed = true`, use `compression_policy::archive_default`, assert the planned entry and data region are `compression_state::deflate`, assert `entry.stored_size == data_region.stored_payload.size()`, finalize, parse the harness, and assert parsed stored-size metadata equals the plan. The unsupported route test must create a target/policy combination that reaches `resolve_payload_codec` but is unsupported and assert `error_code::unsupported_format` with no successful raw/deflate fallback. Run focused writer tests and confirm RED fails due to missing helper implementations or missing unsupported-route handling.</action>
  <verify>
    <automated>cmake --build build/local-vs2026-vcpkg --config Debug --target libbsa_writer_tests &amp;&amp; ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug -R libbsa_writer_tests</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "parsed_writer_harness|read_writer_harness|extract_writer_harness_entry|require_harness_matches_plan" tests/writer_harness_helpers.hpp` finds all declarations.
    - `rg -n "reads finalized harness bytes back and compares plan metadata|round trips compressed harness payloads through real codec routes|archive default compression records exact stored size in plan and harness|writer planning rejects unsupported codec route without fallback" tests/writer_core_tests.cpp` finds all four test names.
    - RED output fails before helper implementation is complete.
  </acceptance_criteria>
  <done>RED tests define generated harness read-back and round-trip proof.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN implement test-only harness parser and assertions</name>
  <files>tests/writer_harness_helpers.cpp, tests/writer_harness_helpers.hpp</files>
  <read_first>tests/writer_harness_helpers.cpp, tests/writer_harness_helpers.hpp, tests/ba2_dds_fixture_helpers.cpp, include/libbsa/compression.hpp, include/libbsa/writer.hpp, src/writer.cpp</read_first>
  <action>Implement little-endian read helpers `read_u32` and `read_u64` in `tests/writer_harness_helpers.cpp`, following the append/read helper analogs in `08-PATTERNS.md` from `tests/ba2_dds_fixture_helpers.cpp`. Parse the Plan 04 `LBSW` stream: header with entry count, data-region count, and total size; entry records using the same byte contract Plan 02 planned and Plan 04 emitted (`data_region_id`, `offset`, `size`, `stored_size`, path length, and variable path bytes); data-region records with `id`, `offset`, `stored_size`, `unpacked_size`, and compression state; payload bytes by region offset/size. Implement `require_harness_matches_plan` to `REQUIRE` equal `total_size`, table-region names/offsets/sizes when available through the plan, entry count, data-region count, path order, each entry's `data_region_id`, `offset`, `size`, `stored_size`, compression state, and each region's `id`, `offset`, `stored_size`, `unpacked_size`, compression, and stored payload size. Implement `extract_writer_harness_entry` by locating the parsed entry path, locating its data region by ID, resolving codec with `payload_codec_request{archive_format::fo4_ba2_gnrl, entry.compression, std::nullopt}` for deflate/raw test coverage, and calling `decompress_payload` with `entry.size`; require success with Catch2 assertions. Keep all helpers under `namespace libbsa::test`; do not modify `include/libbsa/`.</action>
  <verify>
    <automated>cmake --build build/local-vs2026-vcpkg --config Debug --target libbsa_writer_tests &amp;&amp; ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug -R libbsa_writer_tests</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "read_u32|read_u64|LBSW|require_harness_matches_plan|extract_writer_harness_entry|decompress_payload|resolve_payload_codec" tests/writer_harness_helpers.cpp` finds all helper implementation tokens.
    - `rg -n "writer_harness" include/libbsa src` returns no matches, proving harness API remains outside production public/source directories.
    - Focused writer CTest passes read-back and compressed round-trip tests.
  </acceptance_criteria>
  <done>Generated harness bytes can be parsed, compared to the plan, and extracted in tests only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| emitted bytes → test harness parser | Test-only parser consumes generated bytes to validate writer output semantics. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-13 | Tampering | harness metadata comparison | mitigate | Assert exact plan metadata fields against parsed harness bytes. |
| T-08-14 | Information Disclosure | harness API boundary | mitigate | Keep harness declarations under `tests/` only; no public fake archive format in `include/libbsa/`. |
| T-08-15 | Denial of Service | harness extraction codec route | mitigate | Use existing `resolve_payload_codec` and `decompress_payload`; no mock or fallback codec route. |
</threat_model>

<verification>
Focused: `ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug -R libbsa_writer_tests`.
</verification>

<success_criteria>
- RED and GREEN TDD commits exist for `08-05`.
- Harness read-after-write tests compare metadata and round-trip raw/compressed payload bytes.
- Harness helpers remain test-only.
</success_criteria>

<output>
After completion, create `.planning/phases/08-writer-planning-streaming-emit-and-dedup-core/08-05-SUMMARY.md` with RED/GREEN/REFACTOR evidence.
</output>
