---
phase: 08-writer-planning-streaming-emit-and-dedup-core
plan: 06
type: execute
wave: 6
depends_on: [05]
files_modified:
  - tests/public_header_smoke.cpp
  - README.md
autonomous: true
requirements: [WRT-05, WRT-06, WRT-07]
must_haves:
  truths:
    - "Consumer-style public-header smoke code plans, inspects, and finalizes writer-core values using public libbsa headers only."
    - "README documents Phase 8 as writer-core foundations, not production BSA/BA2 writer compatibility."
    - "Final boundary gates prove no private dependency leakage, no CMake globbing, and no TES5Edit changes."
  artifacts:
    - path: "tests/public_header_smoke.cpp"
      provides: "Consumer compile/link smoke for writer API"
      contains: "#include <libbsa/writer.hpp>"
    - path: "README.md"
      provides: "Phase 8 writer-core documentation and validation commands"
      contains: "Writer planning and streaming finalization"
  key_links:
    - from: "tests/public_header_smoke.cpp"
      to: "include/libbsa/writer.hpp"
      via: "public include and function pointer usage"
      pattern: "finalize_archive_write"
---

<objective>
Finalize public smoke coverage, documentation, and boundary validation for Phase 8.

Purpose: WRT-05/WRT-06/WRT-07 require consumer-visible public API proof and maintainable validation gates after the TDD behavior plans land.
Output: Updated public-header smoke test, README Phase 8 section, and final validation evidence.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-writer-planning-streaming-emit-and-dedup-core/08-SPEC.md
@.planning/phases/08-writer-planning-streaming-emit-and-dedup-core/08-CONTEXT.md
@.planning/phases/08-writer-planning-streaming-emit-and-dedup-core/08-VALIDATION.md
@include/libbsa/writer.hpp
@tests/public_header_smoke.cpp
@README.md
@CMakeLists.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend public-header smoke for writer API</name>
  <files>tests/public_header_smoke.cpp</files>
  <read_first>tests/public_header_smoke.cpp, include/libbsa/writer.hpp, include/libbsa/io.hpp, .planning/phases/08-writer-planning-streaming-emit-and-dedup-core/08-CONTEXT.md</read_first>
  <action>Add `#include <libbsa/writer.hpp>` to the public smoke translation unit, following the exact public-header smoke analog in `08-PATTERNS.md` from `tests/public_header_smoke.cpp`. In `main()`, construct `libbsa::writer_target target{}` with `target.format = libbsa::archive_format::fo4_ba2_gnrl`, `target.archive_default_compressed = false`, `target.supports_compression = true`, and `target.supports_shared_data_regions = true`; construct two `libbsa::writer_entry` values with paths `meshes/armor/iron.nif` and `textures/interface/lut.dds`, payloads `{std::byte{0x01}, std::byte{0x02}}` and `{std::byte{0x03}}`, and `compression_policy::force_raw`; call `libbsa::plan_archive_write(target, std::span<const libbsa::writer_entry>{entries}, libbsa::writer_options{.deduplicate = true})`; inspect `plan.value().entries`, `plan.value().table_regions`, `plan.value().data_regions`, and `plan.value().total_size`; finalize to `libbsa::memory_sink`; and take function pointers `const auto plan_writer_fn = &libbsa::plan_archive_write;` and `const auto finalize_writer_fn = &libbsa::finalize_archive_write;`. Use only public libbsa and standard headers.</action>
  <verify>
    <automated>cmake --build build/local-vs2026-vcpkg --config Debug --target libbsa_public_header_smoke &amp;&amp; ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug -R libbsa.public_header_smoke</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "#include <libbsa/writer.hpp>|writer_target target|plan_archive_write|finalize_archive_write|writer_options\{\.deduplicate = true\}" tests/public_header_smoke.cpp` finds all smoke patterns.
    - `rg -n "DirectXTex|DXGI_FORMAT|Windows.h|libdeflate|lz4.h|lz4frame.h|LZ4_|TES5Edit" tests/public_header_smoke.cpp include/libbsa/writer.hpp` returns no matches.
  </acceptance_criteria>
  <done>Consumer-style public smoke covers writer planning, preview inspection, and finalization.</done>
</task>

<task type="auto">
  <name>Task 2: Document Phase 8 writer-core scope and validation</name>
  <files>README.md</files>
  <read_first>README.md, .planning/phases/08-writer-planning-streaming-emit-and-dedup-core/08-SPEC.md, .planning/phases/08-writer-planning-streaming-emit-and-dedup-core/08-CONTEXT.md, .planning/phases/08-writer-planning-streaming-emit-and-dedup-core/08-VALIDATION.md</read_first>
  <action>Add or update a README section titled exactly `Writer planning and streaming finalization`, using `08-SPEC.md` as the scope source and `08-PATTERNS.md` public API terminology from `include/libbsa/writer.hpp`. State these concrete points: `plan_archive_write` accepts in-memory `writer_entry` values; `write_plan` exposes sorted entries, table regions, data regions, compression states, offsets, stored sizes, and total size; `finalize_archive_write` writes plan-owned bytes to caller-owned `byte_sink`; deduplication is opt-in via `writer_options{.deduplicate = true}` and shares byte-identical post-policy stored payloads only when `writer_target::supports_shared_data_regions` is true; Phase 8 uses generated test-only harness read-back and does not claim complete TES3/TES4 BSA or BA2 GNRL/DDS writer compatibility. Include validation commands exactly `ctest --preset windows-msvc-vcpkg -R "libbsa_writer_tests|libbsa.public_header_smoke" --output-on-failure`, `ctest --preset windows-msvc-vcpkg --output-on-failure`, and `git status --short TES5Edit`.</action>
  <verify>
    <automated>ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug -R "libbsa_writer_tests|libbsa.public_header_smoke"</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "Writer planning and streaming finalization|plan_archive_write|finalize_archive_write|byte-identical post-policy stored payloads|does not claim complete TES3/TES4 BSA or BA2 GNRL/DDS writer compatibility|git status --short TES5Edit" README.md` finds all documentation strings.
  </acceptance_criteria>
  <done>README describes what Phase 8 delivers and what remains deferred to Phases 9/10/11.</done>
</task>

<task type="auto">
  <name>Task 3: Run final Phase 8 gates</name>
  <files>README.md, tests/public_header_smoke.cpp</files>
  <read_first>README.md, tests/public_header_smoke.cpp, CMakeLists.txt, include/libbsa/writer.hpp, .planning/phases/08-writer-planning-streaming-emit-and-dedup-core/08-VALIDATION.md</read_first>
  <action>Run final validation gates and record results in the plan summary: full build, focused writer/smoke tests, full CTest, public-header forbidden private-token check, non-comment CMake glob gate, and TES5Edit status. Use precise private-token checks to avoid self-invalidating grep gates: inspect non-comment public headers for `DirectXTex`, `DXGI_FORMAT`, `Windows.h`, `libdeflate`, `TES5Edit`, `lz4.h`, `lz4frame.h`, and `LZ4_`; do not fail on public enum names `lz4_frame` or `lz4_block`.</action>
  <verify>
    <automated>cmake --build build/local-vs2026-vcpkg --config Debug &amp;&amp; ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug &amp;&amp; git status --short TES5Edit</automated>
  </verify>
  <acceptance_criteria>
    - Full CTest output reports all tests passed.
    - `git status --short TES5Edit` outputs nothing.
    - `rg -n "GLOB|GLOB_RECURSE" CMakeLists.txt` has no non-comment source glob additions; ignore existing comments only by checking matches manually or filtering comments.
  </acceptance_criteria>
  <done>Phase 8 final writer, smoke, full-suite, dependency boundary, CMake, and TES5Edit gates pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public headers → consumers | Consumers compile against public writer API without private implementation dependencies. |
| repository → TES5Edit submodule | Planning and validation must not mutate read-only reference material. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-16 | Information Disclosure | public writer header | mitigate | Public smoke and grep gates ensure no private dependency, platform, DirectXTex, codec, Delphi, or TES5Edit headers leak. |
| T-08-17 | Repudiation | validation evidence | mitigate | Run and record focused, smoke, full-suite, CMake, and TES5Edit gates in SUMMARY. |
| T-08-18 | Tampering | TES5Edit boundary | mitigate | Require empty `git status --short TES5Edit` before phase completion. |
</threat_model>

<verification>
Final: `cmake --build build/local-vs2026-vcpkg --config Debug && ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug && git status --short TES5Edit`.
</verification>

<success_criteria>
- Public-header smoke includes writer planning/finalization.
- README documents writer-core scope, dedup semantics, and deferred production writers.
- Full validation gates pass with `TES5Edit/` untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/08-writer-planning-streaming-emit-and-dedup-core/08-06-SUMMARY.md`.
</output>
