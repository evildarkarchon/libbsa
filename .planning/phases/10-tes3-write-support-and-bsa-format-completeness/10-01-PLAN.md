---
phase: 10-tes3-write-support-and-bsa-format-completeness
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - include/libbsa/writer.hpp
  - include/libbsa/libbsa.hpp
  - src/formats/bsa/tes3_bsa_writer.hpp
  - src/formats/bsa/tes3_bsa_writer.cpp
  - CMakeLists.txt
  - tests/unit/public_include_boundary_tests.cpp
autonomous: true
requirements: [WBSA-04]
requirements_addressed: [WBSA-04]
must_haves:
  truths:
    - "D-01: Consumer can construct a dedicated TES3/Morrowind BSA writer from public headers without a one-value TES3 target enum or generic BSA writer redesign."
    - "D-02, D-03: Consumer can use tes3_bsa_writer_options with overwrite_existing only and call add_file, add_bytes, and write_to using only public libbsa types."
    - "D-04: Public TES3 writer comments state raw/uncompressed output with no compression, dedupe, or embedded-name controls."
    - "D-16: Public TES3 writer headers expose no std::expected, libdeflate, lz4, DirectXTex, Windows SDK, TES5Edit, private parser, private writer, private hash, or private compression types."
  artifacts:
    - path: "include/libbsa/writer.hpp"
      provides: "tes3_bsa_writer_options and tes3_bsa_writer public contract"
      contains: "class tes3_bsa_writer"
    - path: "src/formats/bsa/tes3_bsa_writer.hpp"
      provides: "private TES3 writer entry state and serializer declaration"
      contains: "write_tes3_bsa_archive"
    - path: "src/formats/bsa/tes3_bsa_writer.cpp"
      provides: "public bridge and initial writer state"
      contains: "tes3_bsa_writer::add_bytes"
    - path: "tests/unit/public_include_boundary_tests.cpp"
      provides: "public dependency-light compile contract"
      contains: "tes3_bsa_writer"
  key_links:
    - from: "include/libbsa/writer.hpp"
      to: "src/formats/bsa/tes3_bsa_writer.cpp"
      via: "public methods delegate to private writer state"
      pattern: "write_tes3_bsa_archive"
    - from: "CMakeLists.txt"
      to: "src/formats/bsa/tes3_bsa_writer.cpp"
      via: "libbsa private source registration"
      pattern: "tes3_bsa_writer.cpp"
---

<objective>
Define the dependency-light public TES3 writer contract and compile/link it through the existing library target.

Purpose: Satisfies D-01, D-02, D-03, D-04, and D-16 by exposing a dedicated raw-only `tes3_bsa_writer` without adding a generic BSA redesign or public compression/dedupe knobs.
Output: Public API declarations, private writer bridge, CMake source registration, and boundary tests that fail before the API exists and pass after it is wired.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-tes3-write-support-and-bsa-format-completeness/10-SPEC.md
@.planning/phases/10-tes3-write-support-and-bsa-format-completeness/10-CONTEXT.md
@.planning/phases/10-tes3-write-support-and-bsa-format-completeness/10-RESEARCH.md
@.planning/phases/10-tes3-write-support-and-bsa-format-completeness/10-PATTERNS.md
@AGENTS.md
@include/libbsa/writer.hpp
@include/libbsa/libbsa.hpp
@tests/unit/public_include_boundary_tests.cpp
@CMakeLists.txt

<interfaces>
Use this exact public shape per D-01 through D-04:

```cpp
struct tes3_bsa_writer_options {
  bool overwrite_existing{false};
};

class tes3_bsa_writer {
 public:
  tes3_bsa_writer();
  explicit tes3_bsa_writer(tes3_bsa_writer_options options);
  [[nodiscard]] const tes3_bsa_writer_options& options() const noexcept;
  result<void> add_file(std::string_view archive_path, std::string_view host_path);
  result<void> add_bytes(std::string_view archive_path, std::span<const std::byte> bytes);
  result<void> write_to(std::string_view host_path) const;

 private:
  struct state;
  std::shared_ptr<state> state_;
};
```

Private entry contract:

```cpp
namespace libbsa::formats::bsa {
struct tes3_writer_entry {
  std::string archive_path_original;
  std::string archive_path_canonical;
  std::string host_path;
  std::vector<std::byte> memory_bytes;
  bool from_memory{false};
};

result<void> write_tes3_bsa_archive(const tes3_bsa_writer_options& options,
                                    std::span<const tes3_writer_entry> entries,
                                    std::string_view output_host_path);
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED public TES3 writer boundary contract</name>
  <files>tests/unit/public_include_boundary_tests.cpp</files>
  <read_first>
    - `.planning/phases/10-tes3-write-support-and-bsa-format-completeness/10-SPEC.md`
    - `.planning/phases/10-tes3-write-support-and-bsa-format-completeness/10-CONTEXT.md`
    - `tests/unit/public_include_boundary_tests.cpp`
    - `include/libbsa/writer.hpp`
  </read_first>
  <behavior>
    - Static assertions recognize `libbsa::tes3_bsa_writer_options` and `libbsa::tes3_bsa_writer` as public classes.
    - `tes3_bsa_writer` is default constructible and constructible from `tes3_bsa_writer_options`.
    - A `requires` expression proves `options()`, `add_file`, `add_bytes`, and `write_to` return the exact public types.
    - Existing forbidden-token scan still rejects `std::expected`, `libdeflate`, `lz4::`, `DirectXTex`, `TES5Edit`, and private helper names in public headers per D-16.
  </behavior>
  <action>Add the boundary assertions above to `tests/unit/public_include_boundary_tests.cpp` before adding the production API. Do not add compression, embedded-name, dedupe, raw-hash, raw-offset, or target enum assertions as public TES3 features; D-02 excludes those knobs. Run the focused public boundary test and confirm it fails because `tes3_bsa_writer` symbols do not exist yet.</action>
  <verify>
    <automated>ctest --test-dir build -R public_include_boundary --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `tests/unit/public_include_boundary_tests.cpp` contains `std::is_class_v&lt;libbsa::tes3_bsa_writer_options&gt;`.
    - `tests/unit/public_include_boundary_tests.cpp` contains `std::is_class_v&lt;libbsa::tes3_bsa_writer&gt;`.
    - `tests/unit/public_include_boundary_tests.cpp` contains `writer.add_bytes("Meshes/Memory.nif", bytes)` and `writer.add_file("Textures/Disk.dds", "source.dds")` in a `requires` expression.
    - RED run fails before production API is added.
  </acceptance_criteria>
  <done>Failing public-boundary test precisely describes the locked TES3 writer API and dependency boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN public API and private bridge</name>
  <files>include/libbsa/writer.hpp, include/libbsa/libbsa.hpp, src/formats/bsa/tes3_bsa_writer.hpp, src/formats/bsa/tes3_bsa_writer.cpp, CMakeLists.txt</files>
  <read_first>
    - `include/libbsa/writer.hpp`
    - `include/libbsa/libbsa.hpp`
    - `src/formats/bsa/tes4_bsa_writer.hpp`
    - `src/formats/bsa/tes4_bsa_writer.cpp`
    - `src/detail/archive_path.hpp`
    - `CMakeLists.txt`
    - `.planning/phases/10-tes3-write-support-and-bsa-format-completeness/10-PATTERNS.md`
  </read_first>
  <action>Add Doxygen-commented `tes3_bsa_writer_options` with only `bool overwrite_existing{false};` and `tes3_bsa_writer` with default/options constructors, `options()`, `add_file`, `add_bytes`, and `write_to` signatures exactly as listed in the interfaces block. Implement `tes3_bsa_writer::state` with `tes3_bsa_writer_options options` and `std::vector<formats::bsa::tes3_writer_entry> entries`. Add `src/formats/bsa/tes3_bsa_writer.hpp/.cpp`; `add_file` must validate non-empty host path and archive path via `detail::normalize_archive_path`, convert `\` to `/` for `archive_path_original`, preserve case, store canonical path, and defer source existence checks to `write_to` per D-05/D-06. `add_bytes` must copy the byte span. `write_to` delegates to `formats::bsa::write_tes3_bsa_archive`. Register `src/formats/bsa/tes3_bsa_writer.cpp` in root `CMakeLists.txt`. Implement the empty-entry `invalid_argument` result in `write_tes3_bsa_archive`; Plan 02 owns the remaining finalization behaviors.</action>
  <verify>
    <automated>cmake --build build --target libbsa_tests &amp;&amp; ctest --test-dir build -R public_include_boundary --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `include/libbsa/writer.hpp` contains `struct tes3_bsa_writer_options` with exactly `overwrite_existing` as its only data member.
    - `include/libbsa/writer.hpp` contains Doxygen text with `raw/uncompressed` and no compression/dedupe/embedded-name controls.
    - `src/formats/bsa/tes3_bsa_writer.hpp` contains `struct tes3_writer_entry` and `write_tes3_bsa_archive`.
    - `src/formats/bsa/tes3_bsa_writer.cpp` contains `std::replace(preserved.begin(), preserved.end(), '\\', '/')`.
    - `CMakeLists.txt` contains `src/formats/bsa/tes3_bsa_writer.cpp`.
    - Focused public boundary test passes after implementation.
  </acceptance_criteria>
  <done>Public TES3 writer contract compiles, links, and remains dependency-light while preserving locked API choices D-01 through D-04 and D-16.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: REFACTOR public comments and boundary hygiene</name>
  <files>include/libbsa/writer.hpp, src/formats/bsa/tes3_bsa_writer.cpp, tests/unit/public_include_boundary_tests.cpp</files>
  <read_first>
    - `include/libbsa/writer.hpp`
    - `src/formats/bsa/tes3_bsa_writer.cpp`
    - `tests/unit/public_include_boundary_tests.cpp`
    - `AGENTS.md`
  </read_first>
  <action>Refactor only if needed after GREEN: tighten Doxygen wording, remove duplicate local helper code by matching TES4 writer style, and keep comments that explain TES3 raw-only scope and separator normalization. Do not delete accurate existing comments. If a comment becomes inaccurate due to this plan, rewrite it and mention the rewrite in the SUMMARY.</action>
  <verify>
    <automated>cmake --build build --target libbsa_tests &amp;&amp; ctest --test-dir build -R public_include_boundary --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - Public header token scan still passes.
    - `tes3_bsa_writer` public API has Doxygen comments on the class, constructors, `options`, `add_file`, `add_bytes`, and `write_to`.
    - No public header contains `std::expected`, `libdeflate`, `lz4`, `DirectXTex`, `TES5Edit`, `bethesda_hash`, `compression_router`, or `archive_path_key`.
  </acceptance_criteria>
  <done>Public API is clean, documented, and boundary-safe after the TDD cycle.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller→public writer API | Untrusted archive paths and host path strings enter libbsa. |
| public header→consumer compile boundary | Public API must not expose private/dependency implementation details. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | `tes3_bsa_writer::add_file/add_bytes` | mitigate | Validate archive paths with `detail::normalize_archive_path`; preserve case only in serialized name, not canonical duplicate key. |
| T-10-02 | Information Disclosure | `include/libbsa/writer.hpp` | mitigate | Public boundary tests scan for private/dependency tokens including `std::expected`, compression libraries, DirectXTex, Windows SDK, TES5Edit, and private helper names. |
| T-10-03 | Tampering | `TES5Edit/` submodule | mitigate | This plan touches no `TES5Edit/` files; phase verification runs `git -C TES5Edit status --short`. |
</threat_model>

<verification>
Run `cmake --build build --target libbsa_tests` and `ctest --test-dir build -R public_include_boundary --output-on-failure`.
</verification>

<success_criteria>
- `WBSA-04` has a public TES3 writer contract.
- D-01 through D-04 and D-16 are implemented in the public surface.
- No public dependency or TES5Edit leakage is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/10-tes3-write-support-and-bsa-format-completeness/10-01-SUMMARY.md` with RED/GREEN/REFACTOR notes and commits.
</output>
