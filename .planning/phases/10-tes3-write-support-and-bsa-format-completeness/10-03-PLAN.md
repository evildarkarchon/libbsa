---
phase: 10-tes3-write-support-and-bsa-format-completeness
plan: 03
type: tdd
wave: 3
depends_on: [10-02]
files_modified:
  - src/formats/bsa/tes3_bsa_writer.cpp
  - tests/unit/tes3_bsa_writer_tests.cpp
autonomous: true
requirements: [WBSA-04]
requirements_addressed: [WBSA-04]
must_haves:
  truths:
    - "D-12: TES3 writer emits byte-verified header version 0x00000100, file records, name offsets, null-terminated names, hash records, raw offsets, and raw payload bytes."
    - "D-11: Byte-level tests expose manifest-ready layout facts including raw TES3 data offsets, archive-absolute payload offsets, raw/stored sizes, and expected bytes."
    - "D-13, D-14, D-15: Stored TES3 hashes are writer-owned, computed from preserved-case serialized names for every valid archive path accepted by the shared normalizer, and records are sorted by TES3 low32/high32 sort key."
  artifacts:
    - path: "src/formats/bsa/tes3_bsa_writer.cpp"
      provides: "TES3 table serializer and checked offset arithmetic"
      contains: "tes3_hash_sort_key"
    - path: "tests/unit/tes3_bsa_writer_tests.cpp"
      provides: "byte-level header/table/hash/name/offset/payload tests"
      contains: "raw_tes3_data_offset"
  key_links:
    - from: "src/formats/bsa/tes3_bsa_writer.cpp"
      to: "src/detail/bethesda_hash.cpp"
      via: "detail::hash_tes3 and detail::tes3_hash_sort_key"
      pattern: "hash_tes3"
    - from: "TES3 file records"
      to: "reader public metadata"
      via: "raw offset + data_section_start"
      pattern: "data_section_start"
---

<objective>
Replace the scaffold output with byte-accurate raw TES3/Morrowind BSA serialization.

Purpose: Satisfies TES3 table layout, D-11, D-12, D-13, D-14, and D-15 by proving hash-sorted preserved-name records and data-section-relative offsets with byte-level tests.
Output: Checked TES3 serializer and tests that parse writer-produced bytes directly.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-tes3-write-support-and-bsa-format-completeness/10-SPEC.md
@.planning/phases/10-tes3-write-support-and-bsa-format-completeness/10-CONTEXT.md
@.planning/phases/10-tes3-write-support-and-bsa-format-completeness/10-RESEARCH.md
@.planning/phases/10-tes3-write-support-and-bsa-format-completeness/10-PATTERNS.md
@src/formats/bsa/tes3_bsa_writer.cpp
@src/formats/bsa/tes3_bsa_parser.cpp
@src/detail/bethesda_hash.hpp
@src/detail/binary_io.hpp
@tests/fixtures/generated/generate_tes3_bsa_fixtures.cpp
@tests/unit/tes3_bsa_writer_tests.cpp

<interfaces>
TES3 layout constants to implement and test:

```cpp
constexpr std::uint32_t tes3_magic_version = 0x00000100U;
constexpr std::uint32_t header_size = 12U;
constexpr std::uint32_t file_record_size = 8U;
constexpr std::uint32_t name_offset_size = 4U;
constexpr std::uint32_t hash_record_size = 8U;
```

Header fields:
- `u32 version = 0x00000100`
- `u32 hash_table_offset_minus_header = hash_table_start - 12`
- `u32 file_count`

Record order: sort prepared entries by `detail::tes3_hash_sort_key(detail::hash_tes3(serialized_name))`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED byte-level TES3 layout tests</name>
  <files>tests/unit/tes3_bsa_writer_tests.cpp</files>
  <read_first>
    - `.planning/phases/10-tes3-write-support-and-bsa-format-completeness/10-SPEC.md`
    - `.planning/phases/10-tes3-write-support-and-bsa-format-completeness/10-CONTEXT.md`
    - `tests/unit/tes3_bsa_writer_tests.cpp`
    - `tests/fixtures/generated/generate_tes3_bsa_fixtures.cpp`
    - `src/detail/bethesda_hash.hpp`
  </read_first>
  <behavior>
    - Writer output starts with version `0x00000100` and file count equal to added entries.
    - Hash-table offset equals `12 + file_count * 8 + file_count * 4 + name_table_size - 12`.
    - File record sizes match raw payload sizes; record offsets are `0`, previous offset plus previous payload size, including zero-byte entries.
    - Name offset table points to null-terminated preserved names with `\` converted to `/` and case preserved.
    - Hash records equal `detail::hash_tes3(preserved_name)` and are sorted by `detail::tes3_hash_sort_key`.
    - Payload bytes appear at `data_section_start + raw_tes3_data_offset` and exactly match the source bytes.
  </behavior>
  <action>Add byte-reader helpers for `read_u32_le_at`, `read_u64_le_at`, and null-terminated name extraction if not already present. Add tests with entries `Meshes/Mixed/Probe.NIF`, `textures\\Memory\\Probe.dds`, and `Readme.txt` with at least one zero-byte payload. The tests must compute expected serialized order by `detail::tes3_hash_sort_key(detail::hash_tes3(preserved_name))`, not by insertion or alphabetical order. Run focused TES3 writer tests and confirm RED fails against the scaffold serializer.</action>
  <verify>
    <automated>ctest --test-dir build -R tes3_bsa_writer --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `tests/unit/tes3_bsa_writer_tests.cpp` contains `0x00000100`.
    - `tests/unit/tes3_bsa_writer_tests.cpp` contains `detail::tes3_hash_sort_key` and `detail::hash_tes3`.
    - `tests/unit/tes3_bsa_writer_tests.cpp` checks `hash_table_start - 12U`.
    - `tests/unit/tes3_bsa_writer_tests.cpp` checks a zero-byte entry raw offset and extraction span.
    - RED run fails before TES3 table serializer is implemented.
  </acceptance_criteria>
  <done>Failing byte-level tests specify exact TES3 layout and ordering behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN checked TES3 serializer</name>
  <files>src/formats/bsa/tes3_bsa_writer.cpp</files>
  <read_first>
    - `src/formats/bsa/tes3_bsa_writer.cpp`
    - `src/formats/bsa/tes3_bsa_parser.cpp`
    - `src/detail/bethesda_hash.hpp`
    - `src/detail/binary_io.hpp`
    - `tests/fixtures/generated/generate_tes3_bsa_fixtures.cpp`
    - `.planning/phases/10-tes3-write-support-and-bsa-format-completeness/10-RESEARCH.md`
  </read_first>
  <action>Implement prepared TES3 entries with serialized name, canonical path, payload bytes, `std::uint64_t archive_hash`, `std::uint32_t raw_tes3_data_offset`, and size. Convert `\` to `/` in serialized names and preserve caller case per D-13. Compute `archive_hash = detail::hash_tes3(serialized_name)` and sort by `detail::tes3_hash_sort_key(archive_hash)` per D-15. Use checked uint64-to-uint32 arithmetic for file count, file record table size, name offset table size, name table size, hash table size, data section start, each payload size, and each raw offset; return structured errors if a value cannot fit in uint32. Serialize with `detail::binary_writer`: header, file records `(size, raw_offset)`, name offsets, null-terminated names, `write_u64_le(hash)`, and raw payloads. Keep output raw/uncompressed only; do not add compression or dedupe branches.</action>
  <verify>
    <automated>cmake --build build --target libbsa_tests &amp;&amp; ctest --test-dir build -R tes3_bsa_writer --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `src/formats/bsa/tes3_bsa_writer.cpp` contains `constexpr std::uint32_t tes3_magic_version = 0x00000100U` or equivalent constant.
    - `src/formats/bsa/tes3_bsa_writer.cpp` contains `detail::hash_tes3` and `detail::tes3_hash_sort_key`.
    - `src/formats/bsa/tes3_bsa_writer.cpp` writes `hash_table_start - fixed_header_size`.
    - `src/formats/bsa/tes3_bsa_writer.cpp` writes file record offsets relative to the data section, not archive-absolute offsets.
    - Focused TES3 writer tests pass.
  </acceptance_criteria>
  <done>Writer-produced TES3 archives have correct raw table bytes, hash order, name spelling, relative offsets, and payload layout.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: REFACTOR arithmetic and compatibility comments</name>
  <files>src/formats/bsa/tes3_bsa_writer.cpp, tests/unit/tes3_bsa_writer_tests.cpp</files>
  <read_first>
    - `src/formats/bsa/tes3_bsa_writer.cpp`
    - `tests/unit/tes3_bsa_writer_tests.cpp`
    - `AGENTS.md`
  </read_first>
  <action>Refactor checked arithmetic into named local helpers such as `checked_u32`, `checked_add_u32`, and `checked_mul_u32` if the implementation currently repeats raw casts. Add concise comments for non-obvious compatibility constraints: TES3 stores data-section-relative offsets, hashes are computed from preserved serialized names, and sort order uses low32/high32. Do not remove accurate existing comments.</action>
  <verify>
    <automated>ctest --test-dir build -R "tes3_bsa_writer|public_include_boundary" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - Focused TES3 writer and public boundary tests pass.
    - Serializer comments mention `data-section-relative` and `low32/high32` or equivalent exact wording.
    - No serializer branch mentions TES3 compression, embedded names, dedupe, or caller-provided hashes as supported behavior.
  </acceptance_criteria>
  <done>Serializer remains maintainable and documents the non-obvious TES3 compatibility rules.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prepared entries→binary serializer | Caller-controlled path names and payload sizes become on-disk table fields. |
| uint64 calculations→uint32 TES3 fields | Potentially large tables/payloads must not wrap into smaller TES3 fields. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-08 | Tampering | TES3 hash table | mitigate | Compute hashes from preserved serialized names and sort with `detail::tes3_hash_sort_key`; byte-level tests verify order and values. |
| T-10-09 | Tampering/DoS | TES3 table and payload offsets | mitigate | Use checked uint64/uint32 arithmetic and return structured errors on overflow before writing. |
| T-10-10 | Tampering | TES3 raw offsets | mitigate | Store data-section-relative offsets only; tests compare raw offsets and payload byte positions. |
</threat_model>

<verification>
Run `cmake --build build --target libbsa_tests` and `ctest --test-dir build -R "tes3_bsa_writer|public_include_boundary" --output-on-failure`.
</verification>

<success_criteria>
- TES3 writer emits valid raw/uncompressed table bytes.
- D-11 through D-15 are covered by byte-level assertions.
- Integer overflow and offset wrap risks are fail-closed.
</success_criteria>

<output>
After completion, create `.planning/phases/10-tes3-write-support-and-bsa-format-completeness/10-03-SUMMARY.md` with RED/GREEN/REFACTOR notes and commits.
</output>
