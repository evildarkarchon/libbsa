---
phase: 10-tes3-write-support-and-bsa-format-completeness
plan: 04
type: tdd
wave: 4
depends_on: [10-03]
files_modified:
  - src/formats/bsa/tes3_bsa_writer.cpp
  - tests/unit/tes3_bsa_writer_tests.cpp
autonomous: true
requirements: [WBSA-04]
requirements_addressed: [WBSA-04]
must_haves:
  truths:
    - "D-19: Writer-produced TES3 archives reopen through archive_reader and report BSA/TES3 metadata."
    - "D-19: Reader APIs list, find, contains, extract, and extract_bytes every writer-produced entry using normalized path semantics."
    - "D-19: Reopened public payload offsets equal data_section_start plus raw TES3 record offsets."
  artifacts:
    - path: "tests/unit/tes3_bsa_writer_tests.cpp"
      provides: "reader-backed TES3 writer round-trip oracle"
      contains: "archive_reader::open"
    - path: "src/formats/bsa/tes3_bsa_writer.cpp"
      provides: "any serializer fixes required by reader-backed validation"
      contains: "entry_compression::none"
  key_links:
    - from: "writer-produced archive"
      to: "archive_reader::open"
      via: "public reader facade"
      pattern: "archive_reader::open"
    - from: "archive_reader::extract"
      to: "source bytes"
      via: "sink and byte-vector byte comparison"
      pattern: "extract_bytes"
---

<objective>
Prove TES3 writer output through the existing public reader facade and extraction APIs.

Purpose: Satisfies D-19 and SPEC requirements 3, 4, and 6 by using reader-backed behavior as the acceptance oracle rather than trusting writer internals.
Output: End-to-end round-trip tests for disk, memory, mixed-case, mixed-separator, root-level, missing lookup, zero-byte, and sink/vector extraction behavior.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-tes3-write-support-and-bsa-format-completeness/10-SPEC.md
@.planning/phases/10-tes3-write-support-and-bsa-format-completeness/10-CONTEXT.md
@.planning/phases/10-tes3-write-support-and-bsa-format-completeness/10-RESEARCH.md
@src/archive.cpp
@include/libbsa/archive.hpp
@tests/unit/tes3_bsa_reader_tests.cpp
@tests/unit/tes4_bsa_writer_tests.cpp
@tests/unit/tes3_bsa_writer_tests.cpp
@src/formats/bsa/tes3_bsa_writer.cpp
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED reader-backed round-trip tests</name>
  <files>tests/unit/tes3_bsa_writer_tests.cpp</files>
  <read_first>
    - `.planning/phases/10-tes3-write-support-and-bsa-format-completeness/10-SPEC.md`
    - `.planning/phases/10-tes3-write-support-and-bsa-format-completeness/10-CONTEXT.md`
    - `tests/unit/tes3_bsa_writer_tests.cpp`
    - `tests/unit/tes3_bsa_reader_tests.cpp`
    - `tests/unit/tes4_bsa_writer_tests.cpp`
    - `include/libbsa/archive.hpp`
  </read_first>
  <behavior>
    - Public `archive_reader::open` on writer output returns metadata `archive_type::bsa`, `archive_variant::tes3`, version `0x00000100`, and expected file count.
    - Every entry reports `entry_compression::none`.
    - `entries()` includes each original path with case preserved and backslashes serialized as `/`.
    - `contains()` and `find()` work for original, lowercase, and separator-variant lookup paths.
    - Valid missing paths return not found; invalid path syntax returns `error_code::invalid_argument`.
    - `extract_bytes` and `extract(path, collecting_sink)` byte-compare disk and memory payloads, including zero-byte payload.
    - Reopened `payload_offset` equals byte-level `data_section_start + raw_record_offset` for each entry.
  </behavior>
  <action>Extend `tests/unit/tes3_bsa_writer_tests.cpp` with a multi-entry round-trip test that writes disk source `Meshes/Disk/Probe.NIF`, memory source `textures\\Memory\\Probe.dds`, and root-level zero-byte `Readme.txt` if accepted by `detail::normalize_archive_path`. Reuse or add a local `collecting_sink` helper matching existing writer tests. Compute expected byte-level raw offsets using the parser layout from Plan 03 and assert reopened metadata offsets match. Run focused TES3 writer tests and confirm failures identify any reader-visible gaps.</action>
  <verify>
    <automated>ctest --test-dir build -R tes3_bsa_writer --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `tests/unit/tes3_bsa_writer_tests.cpp` contains `archive_reader::open`.
    - `tests/unit/tes3_bsa_writer_tests.cpp` checks `archive_variant::tes3` and `entry_compression::none`.
    - `tests/unit/tes3_bsa_writer_tests.cpp` contains both `extract_bytes` and `collecting_sink` extraction checks.
    - `tests/unit/tes3_bsa_writer_tests.cpp` contains lookup checks for lowercase and separator variants.
    - RED run fails only if writer output is not yet fully reader-compatible.
  </acceptance_criteria>
  <done>Failing or newly added tests specify complete reader-backed TES3 writer acceptance behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN reader-visible writer corrections</name>
  <files>src/formats/bsa/tes3_bsa_writer.cpp, tests/unit/tes3_bsa_writer_tests.cpp</files>
  <read_first>
    - `src/formats/bsa/tes3_bsa_writer.cpp`
    - `src/formats/bsa/tes3_bsa_parser.cpp`
    - `src/formats/bsa/tes3_bsa_reader.cpp`
    - `src/archive.cpp`
    - `tests/unit/tes3_bsa_writer_tests.cpp`
  </read_first>
  <action>Fix any serializer or validation behavior exposed by the reader-backed tests without altering the public API. Ensure serialized names are null-terminated, data section starts immediately after the hash table, raw offsets are relative to that data section, and no compression metadata can appear for TES3 writer output. If root-level paths are accepted by `detail::normalize_archive_path`, keep `Readme.txt` in the test matrix per D-14; if the shared validator rejects root-level paths, document the observed validator behavior in the SUMMARY and keep the test assertion aligned with the actual shared validator result without adding TES4 folder restrictions.</action>
  <verify>
    <automated>cmake --build build --target libbsa_tests &amp;&amp; ctest --test-dir build -R tes3_bsa_writer --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - TES3 writer focused tests pass.
    - Reader-backed test verifies disk source bytes and copied memory bytes after the original memory vector is mutated.
    - Reopened metadata `payload_offset` checks use `data_section_start + raw_record_offset`.
    - No test uses writer internals as the sole acceptance oracle.
  </acceptance_criteria>
  <done>Writer output is accepted by the public reader facade and extraction APIs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: REFACTOR round-trip helpers</name>
  <files>tests/unit/tes3_bsa_writer_tests.cpp</files>
  <read_first>
    - `tests/unit/tes3_bsa_writer_tests.cpp`
    - `tests/unit/tes4_bsa_writer_tests.cpp`
    - `AGENTS.md`
  </read_first>
  <action>Refactor repetitive test assertions into local helper functions only when the helper names keep behavior explicit, such as `require_extracts_bytes` or `require_contains_lookup_variants`. Do not hide byte-level table checks behind opaque helpers. Keep tests asserting error codes and bytes, not diagnostic text.</action>
  <verify>
    <automated>ctest --test-dir build -R "tes3_bsa_writer|tes3_bsa_reader|tes4_bsa_writer|public_include_boundary" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - TES3 writer, TES3 reader, TES4 writer, and public boundary focused regressions pass.
    - Test helper names include the behavior being asserted.
    - No accurate comments were deleted as cleanup.
  </acceptance_criteria>
  <done>Round-trip tests are maintainable and regression-safe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| writer output→public reader | Newly written archive bytes are treated as untrusted input by existing parser/reader. |
| lookup path→reader APIs | Caller lookup strings cross existing archive path validation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-11 | Tampering | writer/reader round trip | mitigate | Reopen writer output through `archive_reader` and assert metadata, lookup, compression, offsets, and extraction bytes. |
| T-10-12 | Tampering | lookup normalization | mitigate | Test original, lowercase, and separator-variant lookup paths plus invalid path syntax. |
| T-10-13 | DoS | malformed writer bytes | mitigate | Existing strict TES3 parser rejects inconsistent bytes; this plan uses the parser as acceptance oracle. |
</threat_model>

<verification>
Run `cmake --build build --target libbsa_tests` and `ctest --test-dir build -R "tes3_bsa_writer|tes3_bsa_reader|tes4_bsa_writer|public_include_boundary" --output-on-failure`.
</verification>

<success_criteria>
- D-19 is fully covered.
- Writer output is usable through public read/list/find/contains/extract APIs.
- Existing TES3 reader and TES4 writer focused regressions remain green.
</success_criteria>

<output>
After completion, create `.planning/phases/10-tes3-write-support-and-bsa-format-completeness/10-04-SUMMARY.md` with RED/GREEN/REFACTOR notes and commits.
</output>
