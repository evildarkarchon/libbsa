---
phase: 10-tes3-write-support-and-bsa-format-completeness
plan: 05
type: execute
wave: 5
depends_on: [10-04]
files_modified:
  - tests/fixtures/generated/generate_tes3_bsa_writer_fixtures.cpp
  - tests/fixtures/generated/archives/tes3_writer_canonical.bsa
  - tests/fixtures/generated/archives/tes3_writer_canonical_manifest.json
  - tests/CMakeLists.txt
  - tests/unit/tes3_bsa_writer_tests.cpp
autonomous: true
requirements: [WBSA-04]
requirements_addressed: [WBSA-04]
must_haves:
  truths:
    - "D-09, D-10: Repository contains one representative committed TES3 writer-output fixture archive and manifest produced through the public TES3 writer API."
    - "D-11, D-12: Fixture manifest records source kind, original path, canonical path, stored hash low/high values, raw TES3 offset, archive-absolute payload offset, raw/stored sizes, and expected bytes in hex, while tests assert structural bytes instead of full archive golden equality."
    - "D-18: Phase 10 final regression checks prove TES3 writer, TES3 reader, TES4 writer, public boundary, legal synthetic fixture provenance, and TES5Edit read-only behavior remain intact."
  artifacts:
    - path: "tests/fixtures/generated/generate_tes3_bsa_writer_fixtures.cpp"
      provides: "public-writer fixture generator"
      contains: "libbsa::tes3_bsa_writer"
    - path: "tests/fixtures/generated/archives/tes3_writer_canonical.bsa"
      provides: "committed synthetic TES3 writer archive evidence"
      min_lines: 0
    - path: "tests/fixtures/generated/archives/tes3_writer_canonical_manifest.json"
      provides: "layout and provenance manifest"
      contains: "no game or TES5Edit bytes copied"
  key_links:
    - from: "fixture generator"
      to: "public TES3 writer API"
      via: "libbsa::tes3_bsa_writer add_file/add_bytes/write_to"
      pattern: "tes3_bsa_writer"
    - from: "manifest entries"
      to: "writer output bytes"
      via: "independent byte parsing and hash recomputation"
      pattern: "raw_tes3_data_offset"
---

<objective>
Add committed legal TES3 writer fixture evidence and run final BSA completeness regression gates.

Purpose: Satisfies D-09, D-10, D-11, D-12, D-18, and the Phase 10 fixture/regression acceptance criteria without adding copyrighted bytes or mutating `TES5Edit/`.
Output: Public-writer fixture generator, committed canonical writer archive and manifest, manifest validation coverage, and final focused/full verification commands.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-tes3-write-support-and-bsa-format-completeness/10-SPEC.md
@.planning/phases/10-tes3-write-support-and-bsa-format-completeness/10-CONTEXT.md
@.planning/phases/10-tes3-write-support-and-bsa-format-completeness/10-RESEARCH.md
@.planning/phases/10-tes3-write-support-and-bsa-format-completeness/10-PATTERNS.md
@AGENTS.md
@tests/fixtures/generated/generate_tes3_bsa_fixtures.cpp
@tests/CMakeLists.txt
@tests/unit/tes3_bsa_writer_tests.cpp
@TES5Edit/Core/wbBSArchive.pas
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add public-writer TES3 fixture generator</name>
  <files>tests/fixtures/generated/generate_tes3_bsa_writer_fixtures.cpp, tests/CMakeLists.txt</files>
  <read_first>
    - `.planning/phases/10-tes3-write-support-and-bsa-format-completeness/10-SPEC.md`
    - `.planning/phases/10-tes3-write-support-and-bsa-format-completeness/10-CONTEXT.md`
    - `tests/fixtures/generated/generate_tes3_bsa_fixtures.cpp`
    - `tests/CMakeLists.txt`
    - `include/libbsa/writer.hpp`
    - `src/detail/bethesda_hash.hpp`
  </read_first>
  <action>Create `tests/fixtures/generated/generate_tes3_bsa_writer_fixtures.cpp`. The generator must create synthetic bytes only, write one disk source file under a generator-owned temp/work directory outside `TES5Edit/`, add at least one disk entry and one memory entry through `libbsa::tes3_bsa_writer`, and call `write_to` to produce `tests/fixtures/generated/archives/tes3_writer_canonical.bsa`. Generate `tes3_writer_canonical_manifest.json` by independently parsing the produced archive bytes and computing manifest facts: `variant`, `version`, `file_count`, `data_section_start`, provenance generator/source strings, and per-entry `source_kind`, `original_path`, `canonical_path`, `archive_hash`, `hash_low32`, `hash_high32`, `raw_tes3_data_offset`, `payload_offset`, `raw_size`, `stored_size`, and `expected.bytes_hex`. Register executable `generate_tes3_bsa_writer_fixtures_tool` and custom target `generate_tes3_bsa_writer_fixtures` in `tests/CMakeLists.txt` with BYPRODUCTS for both committed outputs.</action>
  <verify>
    <automated>cmake --build build --target generate_tes3_bsa_writer_fixtures</automated>
  </verify>
  <acceptance_criteria>
    - Generator source contains `libbsa::tes3_bsa_writer`.
    - Generator source contains provenance text `synthetic` and `no game or TES5Edit bytes copied`.
    - `tests/CMakeLists.txt` contains `generate_tes3_bsa_writer_fixtures_tool` and `generate_tes3_bsa_writer_fixtures`.
    - Generated `.bsa` and `.json` files exist under `tests/fixtures/generated/archives/` and not under `TES5Edit/`.
  </acceptance_criteria>
  <done>Canonical fixture evidence is generated through the public TES3 writer API.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Validate committed fixture manifest evidence</name>
  <files>tests/unit/tes3_bsa_writer_tests.cpp, tests/fixtures/generated/archives/tes3_writer_canonical.bsa, tests/fixtures/generated/archives/tes3_writer_canonical_manifest.json</files>
  <read_first>
    - `tests/unit/tes3_bsa_writer_tests.cpp`
    - `tests/fixtures/generated/archives/tes3_writer_canonical_manifest.json`
    - `tests/fixtures/generated/archives/tes3_writer_canonical.bsa`
    - `tests/fixtures/generated/generate_tes3_bsa_writer_fixtures.cpp`
    - `.planning/phases/10-tes3-write-support-and-bsa-format-completeness/10-CONTEXT.md`
  </read_first>
  <behavior>
    - Manifest provenance confirms repository-owned synthetic bytes and no TES5Edit/game bytes.
    - Manifest entries include all D-11 fields.
    - Committed archive reopens through `archive_reader` as TES3 and extracts bytes matching manifest hex.
    - Manifest hash and raw-offset facts match direct byte parsing of the committed archive.
  </behavior>
  <action>Add fixture validation tests to `tests/unit/tes3_bsa_writer_tests.cpp` using the existing test-only JSON dependency pattern if already available in the test target. Validate the committed manifest and archive together: check provenance strings, required keys, archive metadata, entry count, hash low/high values, raw offsets, archive-absolute payload offsets, raw/stored sizes, and expected bytes. Use public reader extraction for byte comparisons and direct byte parsing for table facts. Do not require a full archive byte-for-byte golden comparison; D-12 requires structural and round-trip facts.</action>
  <verify>
    <automated>cmake --build build --target libbsa_tests &amp;&amp; ctest --test-dir build -R tes3_bsa_writer --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `tests/fixtures/generated/archives/tes3_writer_canonical_manifest.json` contains `source_kind`, `original_path`, `canonical_path`, `hash_low32`, `hash_high32`, `raw_tes3_data_offset`, `payload_offset`, `raw_size`, `stored_size`, and `bytes_hex`.
    - `tests/unit/tes3_bsa_writer_tests.cpp` validates provenance text and D-11 manifest fields.
    - `tests/unit/tes3_bsa_writer_tests.cpp` reopens `tes3_writer_canonical.bsa` through `archive_reader::open`.
    - Focused TES3 writer tests pass.
  </acceptance_criteria>
  <done>Committed TES3 writer evidence is validated from manifest facts, direct bytes, and public reader extraction.</done>
</task>

<task type="auto">
  <name>Task 3: Run final BSA format completeness gates</name>
  <files>tests/unit/tes3_bsa_writer_tests.cpp</files>
  <read_first>
    - `.planning/phases/10-tes3-write-support-and-bsa-format-completeness/10-VALIDATION.md`
    - `.planning/phases/10-tes3-write-support-and-bsa-format-completeness/10-SPEC.md`
    - `tests/unit/tes3_bsa_writer_tests.cpp`
    - `tests/unit/tes3_bsa_reader_tests.cpp`
    - `tests/unit/tes4_bsa_writer_tests.cpp`
    - `tests/unit/public_include_boundary_tests.cpp`
    - `AGENTS.md`
  </read_first>
  <action>Run the Phase 10 focused regression gate exactly: `ctest --test-dir build -R "tes3_bsa_writer|tes3_bsa_reader|tes4_bsa_writer|public_include_boundary" --output-on-failure`. Then run the full suite with `ctest --test-dir build --output-on-failure`. Finally run the PowerShell TES5Edit cleanliness guard exactly: `if ((git -C TES5Edit status --short) -ne '') { git -C TES5Edit status --short; exit 1 }`. If any test gap is found in the focused gate, add the missing assertion to `tests/unit/tes3_bsa_writer_tests.cpp` before rerunning; do not modify `TES5Edit/`.</action>
  <verify>
    <automated>ctest --test-dir build -R "tes3_bsa_writer|tes3_bsa_reader|tes4_bsa_writer|public_include_boundary" --output-on-failure &amp;&amp; ctest --test-dir build --output-on-failure &amp;&amp; if ((git -C TES5Edit status --short) -ne '') { git -C TES5Edit status --short; exit 1 }</automated>
  </verify>
  <acceptance_criteria>
    - Focused regression CTest command exits 0.
    - Full CTest command exits 0.
    - PowerShell TES5Edit cleanliness guard exits 0 after `git -C TES5Edit status --short` produces empty output.
    - No generated fixture path is under `TES5Edit/`.
  </acceptance_criteria>
  <done>WBSA-04 is complete without regressing BSA read/write coverage or contaminating TES5Edit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture generator→repository fixtures | Generated bytes become committed test evidence. |
| repository fixture→tests | Fixture data is read by automated tests as compatibility evidence. |
| working tree→TES5Edit submodule | Phase work must not mutate reference submodule state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-14 | Repudiation | fixture provenance | mitigate | Manifest records generator path and synthetic/no-game/no-TES5Edit provenance text. |
| T-10-15 | Tampering | committed fixture archive | mitigate | Tests validate manifest facts against direct byte parsing and public reader extraction. |
| T-10-16 | Supply-chain/Tampering | `TES5Edit/` | mitigate | Final gate uses a PowerShell guard that exits 1 when `git -C TES5Edit status --short` is non-empty and no fixture generation occurs under the submodule. |
</threat_model>

<verification>
Run `cmake --build build --target generate_tes3_bsa_writer_fixtures`, `cmake --build build --target libbsa_tests`, `ctest --test-dir build -R "tes3_bsa_writer|tes3_bsa_reader|tes4_bsa_writer|public_include_boundary" --output-on-failure`, `ctest --test-dir build --output-on-failure`, and `if ((git -C TES5Edit status --short) -ne '') { git -C TES5Edit status --short; exit 1 }`.
</verification>

<success_criteria>
- D-09 through D-12 and D-18 are implemented.
- Committed TES3 writer fixture evidence is legal, synthetic, and generated through the public writer.
- WBSA-04 and BSA format completeness regressions pass.
</success_criteria>

<output>
After completion, create `.planning/phases/10-tes3-write-support-and-bsa-format-completeness/10-05-SUMMARY.md`.
</output>
