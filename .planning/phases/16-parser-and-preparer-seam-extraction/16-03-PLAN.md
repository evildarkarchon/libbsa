---
phase: 16-parser-and-preparer-seam-extraction
plan: 03
type: execute
wave: 3
depends_on: [16-01, 16-02]
files_modified:
  - tests/unit/parser_preparer_seam_policy_tests.cpp
  - tests/CMakeLists.txt
autonomous: true
requirements: [REFA-01, REFA-02]
must_haves:
  truths:
    - "Maintainer gets a source-policy failure if TES4 parser responsibilities collapse back into one broad mixed coordinator."
    - "Maintainer gets a source-policy failure if BA2 DX10 preparer responsibilities collapse back into one broad mixed flow."
    - "Focused parser/preparer labels, existing affected public suites, full debug tests, and MSVC ASan hardening lane all run before phase closure."
  artifacts:
    - path: "tests/unit/parser_preparer_seam_policy_tests.cpp"
      provides: "Role-based source guard for parser/preparer seam separation"
      contains: "[parser_preparer_seam_policy]"
    - path: "tests/CMakeLists.txt"
      provides: "CTest discovery for parser/preparer seam policy test"
      contains: "parser_preparer_seam_policy_tests.cpp"
  key_links:
    - from: "tests/unit/parser_preparer_seam_policy_tests.cpp"
      to: "src/formats/bsa/tes4_bsa_parser.cpp"
      via: "source-reading negative invariant around parse_tes4_bsa_archive_impl responsibilities"
      pattern: "parse_tes4_bsa_archive_impl"
    - from: "tests/unit/parser_preparer_seam_policy_tests.cpp"
      to: "src/formats/ba2/ba2_dx10_prepare.cpp"
      via: "source-reading negative invariant around BA2 DX10 coordinator responsibilities"
      pattern: "ba2_dx10_prepare"
---

<objective>
Add balanced role-based structural guardrails and run the phase verification sweep for the extracted TES4 parser and BA2 DX10 preparer seams.

Purpose: Satisfy D-09, D-12, D-13, D-14, D-15, and D-16 by proving seam extraction remains balanced across both hotspots and preventing future collapse without freezing exact helper names.
Output: Dedicated parser/preparer seam policy test, registered CTest labels, and documented debug plus ASan validation commands.
</objective>

<execution_context>
@J:/libbsa/.opencode/get-shit-done/workflows/execute-plan.md
@J:/libbsa/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/16-parser-and-preparer-seam-extraction/16-SPEC.md
@.planning/phases/16-parser-and-preparer-seam-extraction/16-CONTEXT.md
@.planning/phases/16-parser-and-preparer-seam-extraction/16-RESEARCH.md
@.planning/phases/16-parser-and-preparer-seam-extraction/16-PATTERNS.md
@.planning/phases/16-parser-and-preparer-seam-extraction/16-VALIDATION.md
@.planning/phases/15-reader-backend-dispatch-cleanup/15-01-SUMMARY.md
@.planning/phases/15-reader-backend-dispatch-cleanup/15-LEARNINGS.md
@src/formats/bsa/tes4_bsa_parser.cpp
@src/formats/bsa/tes4_bsa_table.hpp
@src/formats/bsa/tes4_bsa_payload_descriptor.hpp
@src/formats/ba2/ba2_dx10_prepare.cpp
@src/formats/ba2/ba2_dx10_snapshot_builder.hpp
@src/formats/ba2/ba2_dx10_chunk_assembler.hpp
@tests/unit/archive_reader_dispatch_policy_tests.cpp
@tests/unit/validation_policy_tests.cpp
@tests/CMakeLists.txt

<interfaces>
Policy style to reuse from Phase 15:
- Source-reading Catch2 tests live under tests/unit/.
- Tests assert role-based negative invariants and separated responsibility evidence.
- Tests must not freeze one exact helper name, callback name, or implementation-only identifier where the phase only requires equivalent private seams.

Required collapse definitions from locked decisions:
- D-15 TES4 seam collapse means parse_tes4_bsa_archive_impl again directly owns table parsing, entry materialization, payload descriptor logic, duplicate canonical path rejection, and hash validation as one broad coordinator.
- D-16 BA2 DX10 seam collapse means ba2_dx10_prepare.cpp again directly owns snapshot creation, subresource assembly, chunk planning, compression routing, and entry sorting in one mixed flow without reviewable private seams.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add parser/preparer seam policy guard</name>
  <read_first>
    - .planning/phases/16-parser-and-preparer-seam-extraction/16-CONTEXT.md
    - .planning/phases/16-parser-and-preparer-seam-extraction/16-PATTERNS.md
    - src/formats/bsa/tes4_bsa_parser.cpp
    - src/formats/bsa/tes4_bsa_table.hpp
    - src/formats/bsa/tes4_bsa_payload_descriptor.hpp
    - src/formats/ba2/ba2_dx10_prepare.cpp
    - src/formats/ba2/ba2_dx10_snapshot_builder.hpp
    - src/formats/ba2/ba2_dx10_chunk_assembler.hpp
    - tests/unit/archive_reader_dispatch_policy_tests.cpp
    - tests/unit/validation_policy_tests.cpp
    - tests/CMakeLists.txt
  </read_first>
  <files>tests/unit/parser_preparer_seam_policy_tests.cpp, tests/CMakeLists.txt</files>
  <action>Create tests/unit/parser_preparer_seam_policy_tests.cpp with Catch2 tags [unit][parser_preparer_seam_policy]. Use the source_root, read_text_file, function_body, and require_absent_tokens style from archive_reader_dispatch_policy_tests.cpp. Assert role-based evidence that dedicated private TES4 table and payload descriptor seams exist under src/formats/bsa and are used by src/formats/bsa/tes4_bsa_parser.cpp. Assert role-based evidence that dedicated private BA2 DX10 snapshot builder and chunk assembler seams exist under src/formats/ba2 and are used by src/formats/ba2/ba2_dx10_prepare.cpp. For TES4, inspect parse_tes4_bsa_archive_impl and fail if it contains collapse evidence for direct raw table parsing plus payload descriptor calculations plus duplicate canonical path rejection plus hash validation all in the same broad body. For BA2 DX10, inspect the coordinator body or full ba2_dx10_prepare.cpp and fail if snapshot creation, subresource assembly, chunk planning, compression routing, and entry sorting all reappear as one mixed flow without calls/includes to the private seam files. Register the new test file in tests/CMakeLists.txt. Do not add the policy test to tests/unit/validation_policy_tests.cpp per D-14.</action>
  <verify>
    <automated>cmake --build --preset windows-msvc-debug-static --target libbsa_tests && ctest --preset windows-msvc-debug-static --output-on-failure -L "parser_preparer_seam_policy"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: tests/unit/parser_preparer_seam_policy_tests.cpp contains the exact tag string [parser_preparer_seam_policy].
    - Source assertion: tests/unit/parser_preparer_seam_policy_tests.cpp reads src/formats/bsa/tes4_bsa_parser.cpp and src/formats/ba2/ba2_dx10_prepare.cpp from LIBBSA_SOURCE_DIR.
    - Source assertion: tests/unit/parser_preparer_seam_policy_tests.cpp asserts existence or inclusion evidence for TES4 table, TES4 payload descriptor, BA2 DX10 snapshot builder, and BA2 DX10 chunk assembler private seams without requiring one exact helper function name.
    - Source assertion: tests/CMakeLists.txt lists unit/parser_preparer_seam_policy_tests.cpp in libbsa_tests.
    - Behavior assertion: the verify command passes.
  </acceptance_criteria>
  <done>Dedicated Phase 16 policy guard blocks role collapse for both targeted hotspots without expanding unrelated policy files.</done>
</task>

<task type="auto">
  <name>Task 2: Run focused and full debug verification</name>
  <read_first>
    - .planning/phases/16-parser-and-preparer-seam-extraction/16-VALIDATION.md
    - tests/CMakeLists.txt
    - CMakePresets.json
    - tests/unit/tes4_bsa_parser_seam_tests.cpp
    - tests/unit/ba2_dx10_preparer_seam_tests.cpp
    - tests/unit/parser_preparer_seam_policy_tests.cpp
  </read_first>
  <files>tests/unit/parser_preparer_seam_policy_tests.cpp, tests/CMakeLists.txt</files>
  <action>Run the Phase 16 affected-suite wave command documented in 16-VALIDATION.md: ctest --preset windows-msvc-debug-static --output-on-failure -L "tes4_bsa|ba2_dx10_writer|writer-stage|parser_preparer_seam". Then run the full debug suite: ctest --preset windows-msvc-debug-static --output-on-failure. These commands are wave/phase gates, not the sub-30s per-task smoke loop. If either command fails, fix only Phase 16 seam/test issues required to restore existing behavior; do not weaken test assertions, remove source-policy coverage, or change public APIs. Record command names, pass/fail status, and test counts in the plan summary.</action>
  <verify>
    <automated>ctest --preset windows-msvc-debug-static --output-on-failure -L "tes4_bsa|ba2_dx10_writer|writer-stage|parser_preparer_seam" && ctest --preset windows-msvc-debug-static --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - CLI assertion: focused debug command exits 0.
    - CLI assertion: full debug command exits 0.
    - Source assertion: no files under include/libbsa/ are modified by this task.
    - Source assertion: no files under TES5Edit/ are modified by this task.
  </acceptance_criteria>
  <done>Focused affected suites and full debug suite pass with parser/preparer seam guardrails registered.</done>
</task>

<task type="auto">
  <name>Task 3: Run MSVC ASan hardening lane and close phase evidence</name>
  <read_first>
    - .planning/phases/16-parser-and-preparer-seam-extraction/16-VALIDATION.md
    - CMakePresets.json
    - .planning/REQUIREMENTS.md
    - tests/unit/parser_preparer_seam_policy_tests.cpp
  </read_first>
  <files>tests/unit/parser_preparer_seam_policy_tests.cpp</files>
  <action>Run the Phase 14-supported hardening lane for this risky parser/writer refactor: cmake --build --preset windows-msvc-asan-static && ctest --preset windows-msvc-asan-static --output-on-failure. If the ASan lane fails because of a Phase 16 parser/preparer issue, fix the issue and rerun the lane. If it fails for an environment/toolchain reason outside source control, record the exact failing command and error in the summary instead of marking the lane as passed. Confirm that .planning/REQUIREMENTS.md still leaves DEDU-01, DEDU-02, DX10-01, and DX10-02 pending and that Phase 16 does not claim BA2 DX10 temp lifecycle cleanup proof.</action>
  <verify>
    <automated>cmake --build --preset windows-msvc-asan-static && ctest --preset windows-msvc-asan-static --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - CLI assertion: ASan build and CTest command exits 0, or SUMMARY.md records the exact external environment/toolchain blocker if the command cannot run to completion.
    - Source assertion: .planning/REQUIREMENTS.md still contains unchecked DEDU-01, DEDU-02, DX10-01, and DX10-02 if inspected.
    - Source assertion: Phase 16 summaries do not state that BA2 DX10 abnormal-termination temp-data cleanup proof is complete.
    - Behavior assertion: parser_preparer_seam_policy label remains discoverable after ASan build.
  </acceptance_criteria>
  <done>Phase 16 closes with focused, full-debug, and ASan evidence or an explicitly recorded external ASan blocker.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repository source -> policy test | Source text is test input; policy must detect responsibility collapse without overfitting names. |
| Parser/preparer refactor -> public behavior | Internal seam movement must not change public reader/writer signatures or fixture behavior. |
| ASan hardening lane -> phase closure | Memory-safety-oriented build/test evidence gates risky parser/preparer changes before verification. |
| Phase 16 scope -> Phase 17 scope | Temp texture persistence and dedupe optimization remain deferred responsibilities. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-07 | Tampering | parser_preparer_seam_policy_tests.cpp | mitigate | Assert separated responsibility evidence for both TES4 and BA2 DX10 seams and negative collapse invariants. |
| T-16-08 | Denial of Service | malformed archive count/size and snapshot truncation coverage | mitigate | Run focused labels plus full debug suite and ASan lane before phase closure. |
| T-16-09 | Information Disclosure | BA2 DX10 temp texture persistence | transfer | Verify Phase 17 DX10 temp lifecycle requirements remain open and do not claim cleanup proof in Phase 16. |
| T-16-10 | Elevation of Privilege | Public API expansion risk | mitigate | Source assertions require no include/libbsa/ changes for this internal-only phase. |
</threat_model>

<verification>
- cmake --build --preset windows-msvc-debug-static --target libbsa_tests && ctest --preset windows-msvc-debug-static --output-on-failure -L "parser_preparer_seam_policy"
- ctest --preset windows-msvc-debug-static --output-on-failure -L "tes4_bsa|ba2_dx10_writer|writer-stage|parser_preparer_seam"
- ctest --preset windows-msvc-debug-static --output-on-failure
- cmake --build --preset windows-msvc-asan-static && ctest --preset windows-msvc-asan-static --output-on-failure
</verification>

<success_criteria>
- REFA-01 and REFA-02 both have structural guardrail coverage.
- D-09 balanced proof is met: TES4 and BA2 DX10 each have focused runtime seam tests plus shared policy guardrails.
- D-13 and D-14 are met: a dedicated parser/preparer seam policy test exists under tests/unit/ and unrelated validation-policy tests are not expanded.
- D-15 and D-16 collapse definitions are encoded as role-based guardrails.
- DEDU-01, DEDU-02, DX10-01, and DX10-02 remain Phase 17 scope.
</success_criteria>

<source_audit>
- GOAL: Both targeted hotspots become safer to change because behavior is locked down by focused regression and source-policy coverage — COVERED by Plans 01, 02, and 03.
- REQ: REFA-01 — COVERED by Plan 01 and guarded by Plan 03.
- REQ: REFA-02 — COVERED by Plan 02 and guarded by Plan 03.
- RESEARCH: Dedicated policy guard, focused labels, existing public suites, full debug suite, MSVC ASan lane — COVERED by this plan.
- CONTEXT: D-09, D-12, D-13, D-14, D-15, D-16 — COVERED by this plan.
- DEFERRED/OUT OF SCOPE: Public API redesign, generic parser framework, BA2 writer redesign, dedupe optimization, temp lifecycle cleanup proof, new formats, TES5Edit edits, cross-platform portability — EXCLUDED from the plan set.
</source_audit>

<output>
After completion, create `.planning/phases/16-parser-and-preparer-seam-extraction/16-03-SUMMARY.md` with policy coverage, focused/full/ASan verification results, and any external ASan blocker details.
</output>
