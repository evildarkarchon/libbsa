---
phase: 05-ba2-gnrl-read-extract
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - include/libbsa/archive.hpp
  - src/formats/ba2/ba2_format_detector.hpp
  - src/formats/ba2/ba2_format_detector.cpp
  - src/archive.cpp
  - CMakeLists.txt
  - tests/unit/ba2_gnrl_reader_tests.cpp
  - tests/unit/public_include_boundary_tests.cpp
  - tests/CMakeLists.txt
autonomous: true
requirements: [GNRL-01, GNRL-02, GNRL-03, GNRL-08]
user_setup: []
must_haves:
  truths:
    - "Consumer can open generated FO4, Starfield v2, and Starfield v3 BA2 GNRL archives by bytes, not extension."
    - "Consumer can inspect typed optional Starfield BA2 header fields without dependency leakage."
    - "Valid BA2 DX10 bytes return unsupported in Phase 5."
  artifacts:
    - path: "include/libbsa/archive.hpp"
      provides: "Public optional BA2 metadata surface"
      contains: "ba2_archive_metadata"
    - path: "src/formats/ba2/ba2_format_detector.cpp"
      provides: "BTDX/GNRL/DX10 byte detector"
      contains: "detect_ba2_format"
  key_links:
    - from: "src/archive.cpp"
      to: "src/formats/ba2/ba2_format_detector.cpp"
      via: "open dispatch before BSA fallback"
      pattern: "detect_ba2_format"
---

<objective>
Define the BA2 public metadata contract and byte-driven detector/open routing skeleton.

Purpose: Later parser and extraction tasks need stable contracts for BA2 variants, Starfield fields, and unsupported DX10 handling per D-01 through D-19.
Output: Public BA2 metadata optionals, private detector, open routing for generated BA2 fixtures, and RED/GREEN detector tests.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-ba2-gnrl-read-extract/05-SPEC.md
@.planning/phases/05-ba2-gnrl-read-extract/05-CONTEXT.md
@.planning/phases/05-ba2-gnrl-read-extract/05-RESEARCH.md
@.planning/phases/05-ba2-gnrl-read-extract/05-01-SUMMARY.md
@include/libbsa/archive.hpp
@src/archive.cpp
@src/formats/bsa/bsa_format_detector.hpp
@src/formats/bsa/bsa_format_detector.cpp
@tests/unit/public_include_boundary_tests.cpp
@tests/unit/ba2_gnrl_reader_tests.cpp
@tests/CMakeLists.txt

<interfaces>
Existing public metadata in `include/libbsa/archive.hpp`:
```cpp
struct archive_metadata {
  archive_type type;
  archive_variant variant;
  std::uint32_t version;
  std::uint32_t archive_flags;
  std::uint32_t file_count;
  entry_compression default_compression;
};
```

New public contract to add per D-01/D-02/D-03:
```cpp
struct ba2_archive_metadata {
  std::optional<std::uint32_t> starfield_unknown1;
  std::optional<std::uint32_t> starfield_unknown2;
  std::optional<std::uint32_t> compression_method;
};
// archive_metadata gains: std::optional<ba2_archive_metadata> ba2;
```
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add public BA2 metadata optionals and detector tests</name>
  <files>include/libbsa/archive.hpp, tests/unit/ba2_gnrl_reader_tests.cpp, tests/unit/public_include_boundary_tests.cpp, tests/CMakeLists.txt</files>
  <read_first>include/libbsa/archive.hpp, tests/unit/public_include_boundary_tests.cpp, tests/unit/tes3_bsa_reader_tests.cpp, tests/CMakeLists.txt, tests/fixtures/generated/archives/ba2_gnrl_fo4_manifest.json, tests/fixtures/generated/archives/ba2_gnrl_sfv2_manifest.json, tests/fixtures/generated/archives/ba2_gnrl_sfv3_manifest.json</read_first>
  <behavior>
    - FO4 metadata has `metadata.ba2.has_value()` but Starfield optionals absent per D-03.
    - SFv2 metadata exposes present `starfield_unknown1` and `starfield_unknown2`, absent `compression_method` per D-01/D-02.
    - SFv3 metadata exposes present `starfield_unknown1`, `starfield_unknown2`, and `compression_method`.
  </behavior>
  <action>Add `struct ba2_archive_metadata` and `std::optional<ba2_archive_metadata> ba2;` to `archive_metadata` with Doxygen comments explaining version-gated optionals and raw unknown names. Add `tests/unit/ba2_gnrl_reader_tests.cpp` to `libbsa_tests` in `tests/CMakeLists.txt`. Write RED tests labeled `[ba2_gnrl_detector]` that open the generated FO4/SFv2/SFv3 fixtures, assert `archive_type::ba2`, `archive_variant::fallout4` or `archive_variant::starfield`, manifest version/file count/default compression, and Starfield optional values. Extend public include boundary tests to include and default-construct the new metadata without libdeflate/lz4/DirectXTex/TES5Edit headers.</action>
  <verify><automated>cmake --build --preset windows-msvc-debug-static && ctest --preset windows-msvc-debug-static -L ba2_gnrl_detector --output-on-failure</automated></verify>
  <acceptance_criteria>
    - `include/libbsa/archive.hpp` contains `struct ba2_archive_metadata`, `starfield_unknown1`, `starfield_unknown2`, and `compression_method`.
    - `tests/unit/ba2_gnrl_reader_tests.cpp` contains `[ba2_gnrl_detector]`, `archive_type::ba2`, `archive_variant::starfield`, and `metadata.value().ba2`.
    - Public include boundary test source contains `ba2_archive_metadata` and does not contain `libdeflate`, `lz4`, `DirectXTex`, or `TES5Edit` outside negative string checks.
  </acceptance_criteria>
  <done>RED tests describe the public BA2 metadata contract and detector/open behavior before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement BA2 byte detector and open skeleton</name>
  <files>src/formats/ba2/ba2_format_detector.hpp, src/formats/ba2/ba2_format_detector.cpp, src/archive.cpp, CMakeLists.txt, include/libbsa/archive.hpp</files>
  <read_first>src/formats/bsa/bsa_format_detector.hpp, src/formats/bsa/bsa_format_detector.cpp, src/archive.cpp, CMakeLists.txt, include/libbsa/archive.hpp, TES5Edit/Core/wbBSArchive.pas</read_first>
  <action>Create `src/formats/ba2/ba2_format_detector.hpp/.cpp` with `detected_ba2_format { archive_variant variant; std::uint32_t version; entry_compression default_compression; ba2_archive_metadata ba2; bool is_gnrl; bool is_dx10; }` and `detect_ba2_format(std::span<const std::byte>)`. Detect `BTDX` magic and versions used by generated FO4/SFv2/SFv3 fixtures; classify subtype `GNRL` as supported and subtype `DX10` as `error_code::unsupported` per D-16/D-17. For SFv3, accept `CompressionMethod == 3` as LZ4 block and the generated evidence-bounded non-LZ4 method as deflate if present; unknown v3 methods return `error_code::unsupported` per D-13/D-14. Update `archive_reader::open` to try BA2 detection first when prefix starts with `BTDX`; create a reader with parsed metadata and empty entries as the skeleton for this plan only, leaving full record/name parsing to Plan 03. Register detector sources in `CMakeLists.txt`.</action>
  <verify><automated>cmake --build --preset windows-msvc-debug-static && ctest --preset windows-msvc-debug-static -L ba2_gnrl_detector --output-on-failure</automated></verify>
  <acceptance_criteria>
    - `src/formats/ba2/ba2_format_detector.cpp` contains `BTDX`, `GNRL`, `DX10`, `CompressionMethod`, and `error_code::unsupported`.
    - `src/archive.cpp` contains `formats::ba2::detect_ba2_format` and does not route `BTDX` bytes through `formats::bsa::detect_bsa_format`.
    - `CMakeLists.txt` contains `src/formats/ba2/ba2_format_detector.cpp`.
    - Focused detector CTest label exits 0.
  </acceptance_criteria>
  <done>Generated BA2 GNRL fixtures open to metadata, DX10 fixture returns unsupported, and public include boundary remains green.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| archive bytes→detector | Untrusted first bytes decide parser routing. |
| public metadata→consumer | Parsed Starfield raw fields cross into public API. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | T | `ba2_format_detector.cpp` | mitigate | Byte-driven `BTDX`/version/subtype checks and explicit unsupported handling for DX10 and unknown methods. |
| T-05-04 | I | `archive.hpp` | mitigate | Version-gated `std::optional` fields distinguish absent fields from present zero values. |
</threat_model>

<verification>
Run `ctest --preset windows-msvc-debug-static -L "ba2_gnrl_detector|public_include_boundary" --output-on-failure` and `git -C TES5Edit status --short`.
</verification>

<success_criteria>
FO4/SFv2/SFv3 BA2 GNRL fixtures open and expose correct archive-level BA2 metadata, while DX10 and unsupported v3 method fixtures fail with `unsupported`.
</success_criteria>

<output>
After completion, create `.planning/phases/05-ba2-gnrl-read-extract/05-02-SUMMARY.md`.
</output>
