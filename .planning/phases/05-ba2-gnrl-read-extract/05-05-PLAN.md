---
phase: 05-ba2-gnrl-read-extract
plan: 05
type: execute
wave: 5
depends_on: [05-04]
files_modified:
  - src/formats/ba2/ba2_format_detector.cpp
  - src/formats/ba2/ba2_gnrl_parser.cpp
  - src/formats/ba2/ba2_gnrl_reader.cpp
  - tests/unit/ba2_gnrl_reader_tests.cpp
  - tests/unit/public_include_boundary_tests.cpp
autonomous: true
requirements: [GNRL-01, GNRL-02, GNRL-03, GNRL-04, GNRL-05, GNRL-06, GNRL-07, GNRL-08]
user_setup: []
must_haves:
  truths:
    - "Malformed supported BA2 GNRL bytes fail closed with format_error."
    - "Unsupported valid BA2 profiles fail with unsupported, including DX10 and unknown v3 compression methods."
    - "Existing TES3/TES4 readers, public include boundary, and TES5Edit read-only gate remain green."
  artifacts:
    - path: "tests/unit/ba2_gnrl_reader_tests.cpp"
      provides: "Malformed/unsupported BA2 and final regression coverage"
      contains: "ba2_gnrl_malformed"
    - path: "src/formats/ba2/ba2_gnrl_parser.cpp"
      provides: "Strict BA2 parser validation"
      contains: "format_error"
  key_links:
    - from: "tests/fixtures/generated/archives/ba2_gnrl_malformed_manifest.json"
      to: "tests/unit/ba2_gnrl_reader_tests.cpp"
      via: "manifest-driven malformed assertions"
      pattern: "expected_error"
---

<objective>
Close Phase 5 with malformed/unsupported BA2 validation and cross-format regression gates.

Purpose: The reader must fail closed for unsafe BA2 bytes while preserving prior BSA behavior and public dependency boundaries.
Output: Malformed tests, any parser/detector hardening needed to pass them, and final full-suite/TES5Edit verification.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-ba2-gnrl-read-extract/05-SPEC.md
@.planning/phases/05-ba2-gnrl-read-extract/05-CONTEXT.md
@.planning/phases/05-ba2-gnrl-read-extract/05-RESEARCH.md
@.planning/phases/05-ba2-gnrl-read-extract/05-04-SUMMARY.md
@tests/fixtures/generated/archives/ba2_gnrl_malformed_manifest.json
@tests/unit/ba2_gnrl_reader_tests.cpp
@tests/unit/public_include_boundary_tests.cpp
@src/formats/ba2/ba2_format_detector.cpp
@src/formats/ba2/ba2_gnrl_parser.cpp
@src/formats/ba2/ba2_gnrl_reader.cpp
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add malformed and unsupported BA2 tests</name>
  <files>tests/unit/ba2_gnrl_reader_tests.cpp</files>
  <read_first>tests/unit/ba2_gnrl_reader_tests.cpp, tests/fixtures/generated/archives/ba2_gnrl_malformed_manifest.json, .planning/phases/05-ba2-gnrl-read-extract/05-CONTEXT.md</read_first>
  <behavior>
    - Required malformed cases include truncated header, truncated records, truncated filename table, invalid payload span, duplicate canonical path, corrupt compressed payload, exact-size mismatch, unsupported BA2 subtype, DX10 unsupported, and unsupported v3 compression method per D-08/D-16/D-19.
    - Valid unsupported profiles return `error_code::unsupported`; malformed supported GNRL bytes return `error_code::format_error`.
    - Tests assert stable error codes only and never diagnostic message text per D-11.
  </behavior>
  <action>Add manifest-driven tests labeled `[ba2_gnrl_malformed]`. Read each case from `ba2_gnrl_malformed_manifest.json`, open or extract according to its `phase` field, and assert only `error().code` equals the manifest `expected_error`. Add explicit coverage checks that the manifest contains `ba2_dx10_unsupported`, `ba2_unsupported_v3_compression_method`, `ba2_duplicate_canonical_path`, `ba2_corrupt_compressed_payload`, and `ba2_exact_size_mismatch`.</action>
  <verify><automated>cmake --build --preset windows-msvc-debug-static && ctest --preset windows-msvc-debug-static -L ba2_gnrl_malformed --output-on-failure</automated></verify>
  <acceptance_criteria>
    - `tests/unit/ba2_gnrl_reader_tests.cpp` contains `[ba2_gnrl_malformed]`, `ba2_dx10_unsupported`, `ba2_unsupported_v3_compression_method`, `ba2_exact_size_mismatch`, and `expected_error`.
    - `tests/unit/ba2_gnrl_reader_tests.cpp` does not contain assertions on `.message` for malformed BA2 cases.
    - RED state fails before hardening; GREEN state passes after Task 2.
  </acceptance_criteria>
  <done>Malformed/unsupported tests define Phase 5 fail-closed BA2 behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harden BA2 detector/parser/extractor for malformed cases</name>
  <files>src/formats/ba2/ba2_format_detector.cpp, src/formats/ba2/ba2_gnrl_parser.cpp, src/formats/ba2/ba2_gnrl_reader.cpp</files>
  <read_first>src/formats/ba2/ba2_format_detector.cpp, src/formats/ba2/ba2_gnrl_parser.cpp, src/formats/ba2/ba2_gnrl_reader.cpp, tests/unit/ba2_gnrl_reader_tests.cpp</read_first>
  <action>Fix only the validation gaps exposed by Task 1. Ensure detector returns `unsupported` for valid `BTDX` with `DX10`, unknown subtype, unsupported version, or unsupported v3 `CompressionMethod` per D-14/D-16/D-19. Ensure parser returns `format_error` for truncated headers/records/name table, invalid `FileTableOffset`, invalid payload spans, duplicate canonical paths, and sentinel/record inconsistencies in supported GNRL archives. Ensure extractor returns `format_error` when compressed payloads fail codec decoding or exact-size validation. Preserve existing success-fixture behavior and do not add broad lenient recovery.</action>
  <verify><automated>cmake --build --preset windows-msvc-debug-static && ctest --preset windows-msvc-debug-static -L "ba2_gnrl_malformed|ba2_gnrl_extract|ba2_gnrl_metadata|ba2_gnrl_lookup" --output-on-failure</automated></verify>
  <acceptance_criteria>
    - `src/formats/ba2/ba2_format_detector.cpp` contains unsupported handling for `DX10` and unsupported `CompressionMethod`.
    - `src/formats/ba2/ba2_gnrl_parser.cpp` contains duplicate canonical path and payload/name table span checks.
    - `src/formats/ba2/ba2_gnrl_reader.cpp` maps compression decode failures to stable failure results without partial success.
    - Focused BA2 malformed/extract/metadata/lookup labels pass.
  </acceptance_criteria>
  <done>BA2 malformed and unsupported cases fail with the correct stable error category while success fixtures still pass.</done>
</task>

<task type="auto">
  <name>Task 3: Run final Phase 5 regression and boundary gates</name>
  <files>tests/unit/public_include_boundary_tests.cpp</files>
  <read_first>tests/unit/public_include_boundary_tests.cpp, .planning/phases/05-ba2-gnrl-read-extract/05-SPEC.md</read_first>
  <action>Run final regression gates without changing behavior unless a gate reveals a direct Phase 5 regression. Required gates: all BA2 labels; TES3/TES4 BSA labels; public include boundary; full CTest preset; and TES5Edit read-only status. If a regression is found, make the smallest targeted fix in the file that introduced it and rerun the failing gate plus the final full gate.</action>
  <verify><automated>ctest --preset windows-msvc-debug-static -L "ba2_gnrl|tes3_bsa|tes4_bsa|public_include_boundary" --output-on-failure; ctest --preset windows-msvc-debug-static --output-on-failure; if (git -C TES5Edit status --short) { exit 1 }</automated></verify>
  <acceptance_criteria>
    - `ctest --preset windows-msvc-debug-static -L "ba2_gnrl|tes3_bsa|tes4_bsa|public_include_boundary" --output-on-failure` exits 0.
    - `ctest --preset windows-msvc-debug-static --output-on-failure` exits 0.
    - `git -C TES5Edit status --short` produces no output.
  </acceptance_criteria>
  <done>Phase 5 BA2 support passes focused, regression, full-suite, public-boundary, and TES5Edit cleanliness gates.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| malformed BA2 bytes→open/extract | Attacker-controlled archive bytes exercise parser and codec failure paths. |
| source tree→TES5Edit submodule | Reference material must stay read-only. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09 | T | `ba2_gnrl_parser.cpp` | mitigate | Reject inconsistent supported GNRL structures with `format_error`. |
| T-05-10 | I | `ba2_format_detector.cpp` | mitigate | Distinguish valid unsupported BA2 profiles from malformed supported GNRL bytes. |
| T-05-11 | R | `TES5Edit/` boundary | mitigate | Final `git -C TES5Edit status --short` must produce no output. |
</threat_model>

<verification>
Run all commands from Task 3 and record outputs in the summary.
</verification>

<success_criteria>
Phase 5 fully satisfies GNRL-01 through GNRL-08 with generated fixture evidence, fail-closed malformed handling, no public dependency leaks, and no TES5Edit changes.
</success_criteria>

<output>
After completion, create `.planning/phases/05-ba2-gnrl-read-extract/05-05-SUMMARY.md`.
</output>
