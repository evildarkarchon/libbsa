---
phase: 05-ba2-gnrl-read-extract
plan: 06
type: execute
wave: 6
depends_on: [05-03, 05-05]
files_modified:
  - src/formats/ba2/ba2_gnrl_parser.cpp
  - tests/unit/ba2_gnrl_reader_tests.cpp
autonomous: true
gap_closure: true
requirements: [GNRL-01, GNRL-02, GNRL-03, GNRL-04]

must_haves:
  truths:
    - "Consumers can open, inspect, and list required BA2 GNRL archives without reading the payload region into open-time memory."
    - "BA2 names are parsed from only the bounded [FileTableOffset, first_payload_offset) filename-table interval."
    - "Malformed BA2 GNRL files still fail closed when the bounded filename table overlaps payload data or is truncated."
    - "Completed Phase 5 decisions remain preserved while closing this gap: D-01: BA2 metadata surface; D-02: raw Starfield field names; D-03: version-gated optionals; D-04: existing entry_metadata preference; D-05: required variant fixtures; D-06: edge-heavy fixture matrix; D-07: dedicated BA2 generator; D-08: focused malformed coverage; D-09: rich manifests; D-10: generated-only acceptance; D-11: stable metadata/bytes/error-code assertions; D-12: method 3 raw LZ4 route; D-13: evidence-bounded non-LZ4 routing; D-14: unsupported methods fail unsupported; D-15: metadata-driven compression routing; D-16: DX10 unsupported; D-17: no texture parsing in Phase 5; D-18: DX10 rejection fixture; D-19: unsupported vs malformed classification; D-20: archive_reader facade; D-21: path semantics; D-22: sink-first extraction; D-23: exact-size codec routing; D-24: TES5Edit read-only boundary."
  artifacts:
    - path: "src/formats/ba2/ba2_gnrl_parser.cpp"
      provides: "Bounded BA2 GNRL metadata and filename-table file parser"
      contains: "first_payload_offset"
    - path: "tests/unit/ba2_gnrl_reader_tests.cpp"
      provides: "Regression coverage for sparse large-payload BA2 open/list behavior"
      contains: "ba2_gnrl_bounded_open"
  key_links:
    - from: "src/formats/ba2/ba2_gnrl_parser.cpp"
      to: "BA2 GNRL record payload offsets"
      via: "first payload offset computed from parsed records before filename-table read"
      pattern: "first_payload_offset"
    - from: "tests/unit/ba2_gnrl_reader_tests.cpp"
      to: "libbsa::archive_reader::open"
      via: "sparse large-payload BA2 fixture opened through public reader API"
      pattern: "archive_reader::open"
---

<objective>
Close the Phase 5 verification gap where BA2 GNRL open/list reads the payload suffix into memory while parsing the filename table.

Purpose: Phase 5 promises usable Fallout 4 and Starfield BA2 GNRL read/list behavior for real archives, not only tiny generated fixtures. Open-time parsing must remain bounded to metadata and the filename table; selected payload bytes belong to extraction only.

Output: A bounded BA2 GNRL file parser that reads [0, FileTableOffset) plus only [FileTableOffset, first_payload_offset), with regression tests proving public open/list succeeds for a sparse archive whose payload region is far beyond the filename table.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-ba2-gnrl-read-extract/05-CONTEXT.md
@.planning/phases/05-ba2-gnrl-read-extract/05-SPEC.md
@.planning/phases/05-ba2-gnrl-read-extract/05-VERIFICATION.md
@.planning/phases/05-ba2-gnrl-read-extract/05-03-SUMMARY.md
@.planning/phases/05-ba2-gnrl-read-extract/05-05-SUMMARY.md
@src/formats/ba2/ba2_gnrl_parser.cpp
@tests/unit/ba2_gnrl_reader_tests.cpp

<prior_decision_coverage>
Phase 5 locked decisions were implemented by completed plans 05-01 through 05-05 and must be preserved while closing this verification gap. Prior completed coverage to preserve: D-01: public BA2 metadata surface; D-02: raw `starfield_unknown1`, `starfield_unknown2`, and `compression_method` names; D-03: version-gated optionals; D-04: no unnecessary BA2-specific entry metadata; D-05: one success fixture per required BA2 GNRL variant; D-06: edge-heavy fixture matrix; D-07: dedicated BA2 GNRL generator outside `TES5Edit/`; D-08: focused malformed BA2 coverage; D-09: rich BA2 manifests; D-10: generated-only Phase 5 acceptance gate; D-11: stable public metadata/bytes/error-code assertions without diagnostic message text; D-12: Starfield v3 method 3 raw LZ4 block route; D-13: evidence-bounded non-LZ4 routing; D-14: unsupported v3 compression methods fail open as `unsupported`; D-15: compression routing never inferred from extension/path; D-16: DX10 returns `unsupported`; D-17: DX10 rejected before texture parsing; D-18: tiny DX10 rejection fixture; D-19: unsupported-vs-malformed error classification; D-20: established public `archive_reader` facade; D-21: canonical/original path semantics; D-22: sink-first extraction and bounded `extract_bytes`; D-23: exact-size explicit codec routing; D-24: read-only `TES5Edit/` boundary.
</prior_decision_coverage>

<interfaces>
From `src/formats/ba2/ba2_gnrl_parser.cpp`:
```cpp
result<std::vector<std::byte>> read_file_bytes_at(std::ifstream& input, std::uint64_t offset, std::size_t count,
                                                  std::string_view description);
result<std::vector<gnrl_record>> read_records(detail::binary_reader& reader, std::uint32_t file_count);
result<std::vector<std::string>> read_names(std::span<const std::byte> name_table, std::uint32_t file_count,
                                            std::size_t& consumed);
result<ba2_gnrl_archive> parse_ba2_gnrl_archive_file(std::string_view host_path, std::uint64_t archive_size,
                                                     detected_ba2_format detected);
```

From `tests/unit/ba2_gnrl_reader_tests.cpp`:
```cpp
std::filesystem::path generated_archive_path(std::string_view filename);
nlohmann::json read_json_file(const std::filesystem::path& path);
std::vector<ba2_success_fixture> ba2_success_fixtures();
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add bounded-open regression coverage</name>
  <files>tests/unit/ba2_gnrl_reader_tests.cpp</files>
  <read_first>
    - tests/unit/ba2_gnrl_reader_tests.cpp
    - src/formats/ba2/ba2_gnrl_parser.cpp
    - .planning/phases/05-ba2-gnrl-read-extract/05-VERIFICATION.md
  </read_first>
  <behavior>
    - Test 1: Construct a temporary sparse BA2 GNRL archive with valid `BTDX`/`GNRL` header, one GNRL record, `FileTableOffset` immediately after records, a valid UInt16 length-prefixed filename table, and a payload offset far beyond the filename table.
    - Test 2: `libbsa::archive_reader::open(temp_archive)` returns a reader, `entries()` returns one entry, and that entry's `path`, `original_path`, `payload_offset`, `raw_size`, `stored_size`, and `compression` match the synthetic record.
    - Test 3: The regression is labeled `[unit][fixture][ba2_gnrl_bounded_open]` so it can be run independently and fails against the current implementation that reads `archive_size - FileTableOffset` at open time.
  </behavior>
  <action>
    Add a Catch2 test case named `ba2_gnrl_bounded_open opens sparse large-payload archives without reading payload bytes` with tags `[unit][fixture][ba2_gnrl_bounded_open]`.

    In the test, create a temporary file under `std::filesystem::temp_directory_path() / "libbsa-ba2-bounded-open.ba2"` and remove it at the end. Write a minimal Fallout 4 BA2 GNRL archive by bytes, using these concrete little-endian values:
    - Magic: `BTDX`
    - Version: `1`
    - Subtype: `GNRL`
    - FileCount: `1`
    - FileTableOffset: `60` (`24` byte common header + one `36` byte GNRL record)
    - GNRL record: `NameHash = 0x12345678`, skipped extension bytes any stable ASCII value, `DirHash = 0`, `Unknown = 0x0000002A`, `Offset = 0x0000000200000000` (8 GiB), `PackedSize = 0`, `Size = 1`, `Sentinel = 0xBAADF00D`
    - Filename table at offset `60`: UInt16 length for `meshes/sparse_payload.bin`, followed by those bytes.
    - Use `std::filesystem::resize_file(temp_path, 0x0000000200000001ULL)` after writing the filename table to create a sparse file size that includes the one-byte raw payload span. If `resize_file` throws or reports an error because the filesystem does not support sparse extension, call `SKIP("filesystem does not support sparse BA2 bounded-open fixture")` rather than weakening the assertion.

    The test must call only the public `libbsa::archive_reader::open` and `entries()` APIs. Do not add public API or test-only production hooks. The test proves D-20 public facade behavior, D-21 path semantics, and D-22/D-23 bounded extraction separation by opening and listing without extracting the distant payload.
  </action>
  <verify>
    <automated>cmake --build --preset windows-msvc-debug-static && ctest --preset windows-msvc-debug-static -L ba2_gnrl_bounded_open --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `tests/unit/ba2_gnrl_reader_tests.cpp` contains `ba2_gnrl_bounded_open`.
    - `tests/unit/ba2_gnrl_reader_tests.cpp` contains `0x0000000200000000`.
    - `tests/unit/ba2_gnrl_reader_tests.cpp` contains `std::filesystem::resize_file`.
    - `tests/unit/ba2_gnrl_reader_tests.cpp` contains `archive_reader::open`.
    - The focused CTest command exits 0 after Task 2.
  </acceptance_criteria>
  <done>The focused bounded-open test exists and exercises public BA2 open/list behavior against a sparse large-payload archive without relying on copyrighted fixtures or modifying `TES5Edit/`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bound BA2 filename-table reads before payload data</name>
  <files>src/formats/ba2/ba2_gnrl_parser.cpp</files>
  <read_first>
    - src/formats/ba2/ba2_gnrl_parser.cpp
    - src/formats/ba2/ba2_gnrl_parser.hpp
    - tests/unit/ba2_gnrl_reader_tests.cpp
    - .planning/phases/05-ba2-gnrl-read-extract/05-VERIFICATION.md
  </read_first>
  <behavior>
    - Test 1: The Task 1 bounded-open regression passes because open reads only the filename table interval rather than the payload suffix.
    - Test 2: Existing BA2 GNRL detector, metadata, lookup, extract, and malformed tests continue to pass.
    - Test 3: Valid malformed behavior remains stable: unsupported BA2 profiles still return `unsupported`; internally inconsistent supported GNRL bytes still return `format_error`.
  </behavior>
  <action>
    Refactor `parse_ba2_gnrl_archive_file` so it never reads from `FileTableOffset` through end-of-file. Keep the public signature unchanged.

    Implement the bounded flow in `src/formats/ba2/ba2_gnrl_parser.cpp`:
    1. Read the fixed header exactly as it does now.
    2. Validate `header.magic == BTDX`, `header.subtype == GNRL`, `header.version == detected.version`, and `header.file_count == detected.file_count` in the file-parser path before using the header values to size reads. This mirrors `parse_ba2_gnrl_archive_impl` and prevents stale detector/header mismatches.
    3. Compute `records_end = header_size_for(header.version) + header.file_count * gnrl_record_size` with existing overflow helpers.
    4. Read exactly `[0, FileTableOffset)` into `metadata_bytes` and parse the records from that span using `read_records`.
    5. Compute `first_payload_offset` from the parsed records before reading names. For each record, `stored_size = PackedSize != 0 ? PackedSize : Size`; validate the payload span using unsigned 64-bit arithmetic against `archive_size` before narrowing. Include zero-sized raw entries in the archive validity checks, but only non-zero stored payloads should lower `first_payload_offset`. If every entry is zero-sized, use `archive_size` as the filename-table upper bound.
    6. Validate `FileTableOffset <= first_payload_offset` and fail with `error_code::format_error` if the filename table would overlap payload data.
    7. Read exactly `first_payload_offset - FileTableOffset` bytes as the filename-table slice. Reject the archive if that count exceeds `std::numeric_limits<std::size_t>::max()` or stream limits.
    8. Append only that bounded filename-table slice to `metadata_bytes`, then call `parse_ba2_gnrl_archive_impl(metadata_bytes, archive_size, detected)` so existing name parsing, duplicate-path checks, sorting, metadata materialization, and error classifications remain centralized.

    Add or adjust private helpers only inside `ba2_gnrl_parser.cpp`. Do not expose parser structs in public headers. Keep comments that explain non-obvious BA2 compatibility constraints, especially why the filename table is bounded by first payload offset and why payload bytes are not read during open.
  </action>
  <verify>
    <automated>cmake --build --preset windows-msvc-debug-static && ctest --preset windows-msvc-debug-static -L "ba2_gnrl_bounded_open|ba2_gnrl" --output-on-failure</automated>
    <automated>ctest --preset windows-msvc-debug-static -L "tes3_bsa|tes4_bsa|public_include_boundary" --output-on-failure</automated>
    <automated>git -C TES5Edit status --short</automated>
  </verify>
  <acceptance_criteria>
    - `src/formats/ba2/ba2_gnrl_parser.cpp` no longer contains `archive_size - header.value().file_table_offset` in a filename-table read.
    - `src/formats/ba2/ba2_gnrl_parser.cpp` contains `first_payload_offset`.
    - `src/formats/ba2/ba2_gnrl_parser.cpp` contains `filename table overlaps payload data`.
    - `src/formats/ba2/ba2_gnrl_parser.cpp` contains a comment explaining that open reads filename-table bytes only, not payload bytes.
    - The focused BA2 and BSA/public-boundary CTest commands exit 0.
    - `git -C TES5Edit status --short` prints no output.
  </acceptance_criteria>
  <done>`archive_reader::open` for BA2 GNRL performs bounded metadata/name-table reads only, while all existing BA2 extraction and malformed behavior remains green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host archive bytes → BA2 parser | Untrusted BA2 header, records, offsets, filename table, and payload span metadata drive parser reads and allocations. |
| parser metadata → public reader facade | Parsed entries become user-visible metadata and determine later extraction seeks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06-01 | D | `parse_ba2_gnrl_archive_file` | mitigate | Bound filename-table reads to `[FileTableOffset, first_payload_offset)` so an archive cannot force open/list to allocate or read payload-region bytes. |
| T-05-06-02 | T | BA2 record offset/size validation | mitigate | Validate record payload spans with unsigned 64-bit arithmetic against `archive_size` before narrowing to `std::size_t`; reject overlap or out-of-range spans as `format_error`. |
| T-05-06-03 | I | `archive_reader::entries` metadata | mitigate | Preserve existing duplicate canonical path checks and original/canonical path normalization when routing the bounded name slice through the centralized parser. |
</threat_model>

<verification>
Run:
- `cmake --build --preset windows-msvc-debug-static && ctest --preset windows-msvc-debug-static -L "ba2_gnrl_bounded_open|ba2_gnrl" --output-on-failure`
- `ctest --preset windows-msvc-debug-static -L "tes3_bsa|tes4_bsa|public_include_boundary" --output-on-failure`
- `git -C TES5Edit status --short`
</verification>

<success_criteria>
- The Phase 5 verification gap truth is true: BA2 GNRL open/list reads bounded metadata and filename-table bytes only.
- `ba2_gnrl_bounded_open` regression coverage exists and passes.
- Existing BA2 raw/deflate/raw-LZ4 extraction tests and malformed stable error-code tests still pass.
- TES3/TES4-family and public include boundary regressions remain green.
- `TES5Edit/` remains read-only with empty git status.
</success_criteria>

<output>
After completion, create `.planning/phases/05-ba2-gnrl-read-extract/05-06-SUMMARY.md`.
</output>
