---
phase: 12-performance-concurrency-documentation-and-polish
plan: 02
type: tdd
wave: 2
depends_on: ["12-01"]
files_modified:
  - include/libbsa/writer.hpp
  - src/formats/bsa/tes3_bsa_writer.cpp
  - src/formats/bsa/tes4_bsa_writer.cpp
  - src/formats/ba2/ba2_gnrl_writer.cpp
  - src/formats/ba2/ba2_dx10_writer.cpp
  - tests/unit/public_include_boundary_tests.cpp
  - tests/unit/writer_execution_options_tests.cpp
  - tests/CMakeLists.txt
autonomous: true
requirements: [PERF-03]
user_setup: []
must_haves:
  truths:
    - "Consumer can pass write-call execution options to every public writer."
    - "All writer execution options default to single-threaded worker_count 1."
    - "worker_count 0 is rejected as invalid instead of treated as automatic worker selection."
    - "Existing one-argument write_to overloads preserve serial-default behavior."
  artifacts:
    - path: "include/libbsa/writer.hpp"
      provides: "Dependency-light write_execution_options and write_to overloads"
    - path: "tests/unit/writer_execution_options_tests.cpp"
      provides: "Runtime worker_count validation and serial default behavior tests"
    - path: "tests/unit/public_include_boundary_tests.cpp"
      provides: "Compile-time public writer overload assertions"
  key_links:
    - from: "include/libbsa/writer.hpp"
      to: "writer implementation files"
      via: "write_to(std::string_view, write_execution_options)"
      pattern: "write_execution_options"
---

<objective>
Add the shared write-call execution-options contract required by D-07 through D-09 and D-25.

Purpose: Parallel packing controls belong at finalization time, not in archive compatibility options, and every public writer should expose the same positive worker-count shape.
Output: `write_execution_options`, writer overloads, runtime validation, and public-boundary tests.
</objective>

<execution_context>
@C:/Users/evild/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-performance-concurrency-documentation-and-polish/12-SPEC.md
@.planning/phases/12-performance-concurrency-documentation-and-polish/12-CONTEXT.md
@.planning/phases/12-performance-concurrency-documentation-and-polish/12-PATTERNS.md
@include/libbsa/writer.hpp
@include/libbsa/result.hpp
@src/formats/bsa/tes3_bsa_writer.cpp
@src/formats/bsa/tes4_bsa_writer.cpp
@src/formats/ba2/ba2_gnrl_writer.cpp
@src/formats/ba2/ba2_dx10_writer.cpp
@tests/unit/public_include_boundary_tests.cpp
@tests/CMakeLists.txt

<interfaces>
New public writer execution contract:
```cpp
struct write_execution_options {
  std::uint32_t worker_count{1U};
};

result<void> write_to(std::string_view host_path) const;
result<void> write_to(std::string_view host_path, write_execution_options execution) const;
```
Apply the overload pair to `tes3_bsa_writer`, `tes4_bsa_writer`, `ba2_gnrl_writer`, and `ba2_dx10_writer`. The one-argument overload delegates to the two-argument overload with default options.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED writer execution option tests</name>
  <files>tests/unit/writer_execution_options_tests.cpp, tests/unit/public_include_boundary_tests.cpp, tests/CMakeLists.txt</files>
  <read_first>
    - tests/CMakeLists.txt
    - tests/unit/public_include_boundary_tests.cpp
    - tests/unit/tes3_bsa_writer_tests.cpp
    - tests/unit/tes4_bsa_writer_tests.cpp
    - tests/unit/ba2_gnrl_writer_tests.cpp
    - tests/unit/ba2_dx10_writer_tests.cpp
    - .planning/phases/12-performance-concurrency-documentation-and-polish/12-CONTEXT.md
  </read_first>
  <action>
    Add `tests/unit/writer_execution_options_tests.cpp` and register it. Add public-boundary static assertions that `libbsa::write_execution_options` is a class, default constructible, has `worker_count`, and every public writer has `write_to("out", libbsa::write_execution_options{}) -> libbsa::result<void>`. Add runtime tests that `write_execution_options{}.worker_count == 1U`; `worker_count = 0U` returns `error_code::invalid_argument` for TES3, TES4-family, BA2 GNRL, and BA2 DX10 without creating output files; and `worker_count = 1U` produces the same success behavior as the existing one-argument `write_to` for a minimal legal archive in each writer family. Use generated DDS source fixtures for BA2 DX10 and repository-owned temp paths only.
  </action>
  <verify>
    <automated>cmake --build --preset windows-msvc-debug-static --target libbsa_tests && ctest --preset windows-msvc-debug-static -R "writer_execution_options|public_include_boundary" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "write_execution_options|worker_count" tests/unit/writer_execution_options_tests.cpp tests/unit/public_include_boundary_tests.cpp` finds compile-time and runtime expectations.
    - The focused CTest fails before implementation because `write_execution_options` or writer overloads do not exist.
    - `tests/CMakeLists.txt` contains `unit/writer_execution_options_tests.cpp`.
  </acceptance_criteria>
  <done>RED coverage defines the exact public contract and positive worker-count behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN shared writer execution contract</name>
  <files>include/libbsa/writer.hpp, src/formats/bsa/tes3_bsa_writer.cpp, src/formats/bsa/tes4_bsa_writer.cpp, src/formats/ba2/ba2_gnrl_writer.cpp, src/formats/ba2/ba2_dx10_writer.cpp</files>
  <read_first>
    - include/libbsa/writer.hpp
    - include/libbsa/result.hpp
    - src/formats/bsa/tes3_bsa_writer.cpp
    - src/formats/bsa/tes4_bsa_writer.cpp
    - src/formats/ba2/ba2_gnrl_writer.cpp
    - src/formats/ba2/ba2_dx10_writer.cpp
    - .planning/phases/12-performance-concurrency-documentation-and-polish/12-CONTEXT.md
    - .planning/phases/12-performance-concurrency-documentation-and-polish/12-PATTERNS.md
  </read_first>
  <action>
    Add `struct write_execution_options { std::uint32_t worker_count{1U}; };` to `include/libbsa/writer.hpp` with Doxygen comments documenting D-07, D-08, and D-09. Add `write_to(std::string_view host_path, write_execution_options execution) const` overloads to `tes3_bsa_writer`, `tes4_bsa_writer`, `ba2_gnrl_writer`, and `ba2_dx10_writer`. In each writer `.cpp`, make the existing one-argument overload delegate to `{}` and make the new overload return `error{error_code::invalid_argument, "...worker_count..."}` when `execution.worker_count == 0U` before any output path or source path is touched. Pass `execution` into the private write function only if the writer implementation needs it in this plan; otherwise keep the existing private function call for `worker_count >= 1U` so serial behavior is preserved and Plans 12-03/12-04 consume the option for compression scheduling.
  </action>
  <verify>
    <automated>cmake --build --preset windows-msvc-debug-static --target libbsa_tests && ctest --preset windows-msvc-debug-static -R "writer_execution_options|public_include_boundary|tes3_bsa_writer|TES4 BSA writer|ba2_gnrl_writer|ba2_dx10_writer" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "struct write_execution_options" include/libbsa/writer.hpp` finds the public options struct.
    - `rg -n "write_to\\(std::string_view host_path, write_execution_options" include/libbsa/writer.hpp src/formats/bsa/tes3_bsa_writer.cpp src/formats/bsa/tes4_bsa_writer.cpp src/formats/ba2/ba2_gnrl_writer.cpp src/formats/ba2/ba2_dx10_writer.cpp` finds all overload declarations/definitions.
    - `rg -n "worker_count == 0U|worker_count == 0" src/formats/bsa/tes3_bsa_writer.cpp src/formats/bsa/tes4_bsa_writer.cpp src/formats/ba2/ba2_gnrl_writer.cpp src/formats/ba2/ba2_dx10_writer.cpp` finds invalid worker-count guards.
    - Focused writer execution option and public-boundary tests pass.
  </acceptance_criteria>
  <done>Every public writer exposes a write-call execution-options overload, defaults to serial, and rejects worker_count 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: REFACTOR public boundary and dependency hygiene</name>
  <files>include/libbsa/writer.hpp, tests/unit/public_include_boundary_tests.cpp</files>
  <read_first>
    - include/libbsa/writer.hpp
    - include/libbsa/libbsa.hpp
    - tests/unit/public_include_boundary_tests.cpp
    - .planning/phases/12-performance-concurrency-documentation-and-polish/12-CONTEXT.md
  </read_first>
  <action>
    Tighten public-boundary tests so `write_execution_options` does not introduce `std::thread`, `std::jthread`, `std::mutex`, libdeflate, lz4, DirectXTex, Windows, TES5Edit, private writer, or C++23 types into public headers per D-25. Keep the Doxygen comments in `writer.hpp` concise and explicit: `worker_count == 1` is serial, `worker_count > 1` opts into parallel-capable work for supported writer paths, and `worker_count == 0` is invalid.
  </action>
  <verify>
    <automated>ctest --preset windows-msvc-debug-static -R "writer_execution_options|public_include_boundary" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "std::thread|std::jthread|std::mutex|std::expected|libdeflate|lz4|DirectXTex|Windows.h|TES5Edit" include/libbsa/writer.hpp` returns no matches.
    - `rg -n "worker_count.*1|worker_count.*0|parallel" include/libbsa/writer.hpp` finds the public execution semantics comments.
    - Focused tests pass.
  </acceptance_criteria>
  <done>Public writer execution options remain dependency-light and documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller -> writer write_to | Untrusted output paths and worker_count enter writer finalization. |
| writer -> filesystem | Writer may create temp files, backups, and destination archives. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-02-01 | D | `write_execution_options::worker_count` | mitigate | Reject zero before source/output work; do not implement automatic worker selection per D-09. |
| T-12-02-02 | I | public writer headers | mitigate | Preserve forbidden-token scan for private codec, platform, TES5Edit, and C++23 leakage per D-25. |
| T-12-02-03 | T | `write_to` overload forwarding | mitigate | Existing one-argument overloads delegate to default options so serial-default behavior remains observable. |
</threat_model>

<verification>
```powershell
cmake --build --preset windows-msvc-debug-static --target libbsa_tests
ctest --preset windows-msvc-debug-static -R "writer_execution_options|public_include_boundary" --output-on-failure
git status --short TES5Edit
```
</verification>

<success_criteria>
- `write_execution_options` exists in the public API.
- TES3, TES4-family, BA2 GNRL, and BA2 DX10 writers all expose the two-argument `write_to` overload.
- `worker_count = 0U` returns `error_code::invalid_argument`.
- `git status --short TES5Edit` prints no output.
</success_criteria>

<output>
After completion, create `.planning/phases/12-performance-concurrency-documentation-and-polish/12-02-SUMMARY.md`.
</output>
