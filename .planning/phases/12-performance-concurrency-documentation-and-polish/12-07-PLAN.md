---
phase: 12-performance-concurrency-documentation-and-polish
plan: 07
type: execute
wave: 7
depends_on: ["12-01", "12-02", "12-05", "12-06"]
files_modified:
  - docs/integration-examples.md
  - docs/target-format-guide.md
  - tests/package-consumer/main.cpp
  - tests/package-consumer/CMakeLists.txt
  - tests/unit/target_format_policy_tests.cpp
  - tests/CMakeLists.txt
  - tests/fixtures/README.md
autonomous: true
requirements: [DOC-02, DOC-03]
user_setup: []
must_haves:
  truths:
    - "Consumer can read compile-checked examples for opening, listing, extracting, bulk extraction, writing, error handling, and validation."
    - "Package-consumer smoke coverage compiles representative snippets against installed libbsa::libbsa."
    - "Consumer can read target-format guidance for every supported BSA/BA2 variant and compression method."
    - "Target-format guidance references public compatibility warning codes."
  artifacts:
    - path: "docs/integration-examples.md"
      provides: "Consumer examples linked to package-consumer compiled snippets"
    - path: "docs/target-format-guide.md"
      provides: "Supported variants, compression routes, writer policies, and warning guidance"
    - path: "tests/package-consumer/main.cpp"
      provides: "Compile-checked installed-package examples"
    - path: "tests/unit/target_format_policy_tests.cpp"
      provides: "Machine checks for target-format guide coverage"
  key_links:
    - from: "docs/integration-examples.md"
      to: "tests/package-consumer/main.cpp"
      via: "matching example function names"
      pattern: "example_"
    - from: "docs/target-format-guide.md"
      to: "include/libbsa/validation.hpp"
      via: "compatibility_warning_code names"
      pattern: "compressed_sound_payload"
---

<objective>
Add consumer integration examples and target-format guidance required by DOC-02 and DOC-03.

Purpose: The public API should be usable from installed-package examples, and consumers need one guide for supported variants, compression routes, writer target policies, and compatibility warnings.
Output: Compile-checked examples, documentation pages, target-format policy tests, and fixture policy updates.
</objective>

<execution_context>
@C:/Users/evild/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-performance-concurrency-documentation-and-polish/12-SPEC.md
@.planning/phases/12-performance-concurrency-documentation-and-polish/12-CONTEXT.md
@include/libbsa/libbsa.hpp
@include/libbsa/archive.hpp
@include/libbsa/writer.hpp
@include/libbsa/validation.hpp
@docs/compatibility-evidence.md
@docs/PRD.md
@docs/thread-safety.md
@benchmarks/README.md
@tests/package-consumer/main.cpp
@tests/package-consumer/CMakeLists.txt
@tests/package-consumer/smoke.cmake
@tests/unit/validation_policy_tests.cpp
@tests/CMakeLists.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add compile-checked installed consumer examples</name>
  <files>tests/package-consumer/main.cpp, tests/package-consumer/CMakeLists.txt, docs/integration-examples.md</files>
  <read_first>
    - tests/package-consumer/main.cpp
    - tests/package-consumer/CMakeLists.txt
    - tests/package-consumer/smoke.cmake
    - include/libbsa/libbsa.hpp
    - include/libbsa/archive.hpp
    - include/libbsa/writer.hpp
    - include/libbsa/validation.hpp
    - docs/thread-safety.md
    - .planning/phases/12-performance-concurrency-documentation-and-polish/12-CONTEXT.md
  </read_first>
  <action>
    Expand `tests/package-consumer/main.cpp` into compile-checked example functions using only `<libbsa/libbsa.hpp>` and standard headers per D-22. Add these functions with exact names: `example_open_list_extract`, `example_bulk_extract`, `example_create_tes3_bsa`, `example_create_tes4_bsa`, `example_create_ba2_gnrl`, `example_create_ba2_dx10`, `example_handle_result_errors`, and `example_validate_archive`. The functions may be compile-only and not executed by `main` when they require caller-provided archive paths or DDS paths; `main` must keep the existing installed-package smoke behavior by validating a missing path and returning success only for `error_code::io_error`. Create `docs/integration-examples.md` with matching headings for each example name and code snippets that mirror the compiled function names. Include examples for `archive_reader::extract_entries`, `write_execution_options`, `validate_archive`, and checking `result.error().code` rather than exact messages. Keep examples library-oriented; do not create a CLI product.
  </action>
  <verify>
    <automated>cmake --build --preset windows-msvc-debug-static --target libbsa_tests && ctest --preset windows-msvc-debug-static -R "package_consumer_smoke" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "example_open_list_extract|example_bulk_extract|example_create_tes3_bsa|example_create_tes4_bsa|example_create_ba2_gnrl|example_create_ba2_dx10|example_handle_result_errors|example_validate_archive" tests/package-consumer/main.cpp docs/integration-examples.md` finds all compile-checked examples and docs headings.
    - `rg -n "extract_entries|write_execution_options|validate_archive|error\\(\\)\\.code" tests/package-consumer/main.cpp docs/integration-examples.md` finds required API usage.
    - `ctest --preset windows-msvc-debug-static -R "package_consumer_smoke" --output-on-failure` passes.
  </acceptance_criteria>
  <done>Installed-package consumer examples compile and docs mirror those examples.</done>
</task>

<task type="auto">
  <name>Task 2: Add target-format and compatibility guidance</name>
  <files>docs/target-format-guide.md, tests/fixtures/README.md</files>
  <read_first>
    - docs/target-format-guide.md
    - docs/compatibility-evidence.md
    - docs/PRD.md
    - tests/fixtures/README.md
    - include/libbsa/archive.hpp
    - include/libbsa/writer.hpp
    - include/libbsa/validation.hpp
    - .planning/phases/12-performance-concurrency-documentation-and-polish/12-SPEC.md
    - .planning/phases/12-performance-concurrency-documentation-and-polish/12-CONTEXT.md
  </read_first>
  <action>
    Create `docs/target-format-guide.md` with machine-checkable headings per D-24 for: `TES3 BSA`, `TES4-family BSA v103`, `TES4-family BSA v104`, `Skyrim SE/AE BSA v105`, `Fallout 4 BA2 GNRL`, `Fallout 4 BA2 DX10`, `Starfield BA2 v2 GNRL`, `Starfield BA2 v3 GNRL`, `Starfield BA2 v3 DX10`, `deflate`, `LZ4 frame`, `raw LZ4 block`, `writer target policies`, and `compatibility warnings`. Cover writer target enums, archive variants, compression routing, DX10 DDS no-transform rule, BA2 Starfield `CompressionMethod == 3`, validation warning codes `compressed_sound_payload`, `bsa_embedded_name_compatibility_risk`, and `target_family_mismatch`. Update `tests/fixtures/README.md` with a short Phase 12 benchmark/generated-data note referencing `benchmarks/README.md` and legal synthetic data policy per D-19/D-20/D-26.
  </action>
  <verify>
    <automated>rg -n "TES3 BSA|TES4-family BSA v103|TES4-family BSA v104|Skyrim SE/AE BSA v105|Fallout 4 BA2 GNRL|Fallout 4 BA2 DX10|Starfield BA2 v2 GNRL|Starfield BA2 v3 GNRL|Starfield BA2 v3 DX10|deflate|LZ4 frame|raw LZ4 block|compressed_sound_payload|bsa_embedded_name_compatibility_risk|target_family_mismatch" docs/target-format-guide.md</automated>
  </verify>
  <acceptance_criteria>
    - The automated `rg` command finds every required format, compression route, and warning code.
    - `rg -n "benchmarks/README.md|legal synthetic" tests/fixtures/README.md` finds the Phase 12 benchmark data policy note.
    - `rg -n "TES5Edit.*fixture workspace|read-only" docs/target-format-guide.md tests/fixtures/README.md` finds the read-only boundary guidance.
  </acceptance_criteria>
  <done>Target-format guidance covers all supported variants, compression methods, writer policies, and public warnings.</done>
</task>

<task type="auto">
  <name>Task 3: Add target-format and examples policy tests</name>
  <files>tests/unit/target_format_policy_tests.cpp, tests/CMakeLists.txt</files>
  <read_first>
    - tests/CMakeLists.txt
    - tests/unit/validation_policy_tests.cpp
    - docs/integration-examples.md
    - docs/target-format-guide.md
    - tests/package-consumer/main.cpp
    - include/libbsa/validation.hpp
  </read_first>
  <action>
    Add `tests/unit/target_format_policy_tests.cpp` and register it. The tests must read `docs/integration-examples.md`, `docs/target-format-guide.md`, `tests/package-consumer/main.cpp`, and `include/libbsa/validation.hpp`. Require every `example_*` function name from Task 1 to exist in both docs and package-consumer source. Require all supported target-format headings and compression method names from Task 2. Parse `enum class compatibility_warning_code` from `validation.hpp` using the existing validation policy style and assert every public warning code has a `###` or backticked entry in `docs/target-format-guide.md`. Require the guide to mention `docs/compatibility-evidence.md`.
  </action>
  <verify>
    <automated>cmake --build --preset windows-msvc-debug-static --target libbsa_tests && ctest --preset windows-msvc-debug-static -R "target_format_policy|package_consumer_smoke|validation_policy" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "target_format_policy|compatibility_warning_code|example_bulk_extract|docs/compatibility-evidence.md" tests/unit/target_format_policy_tests.cpp` finds policy coverage.
    - Focused target-format, package-consumer, and validation policy tests pass.
    - `git status --short TES5Edit` prints no output.
  </acceptance_criteria>
  <done>Examples and target-format guidance are machine-checked and package-consumer coverage remains green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation/examples -> consumers | Consumers may copy path handling and extraction code. |
| package-consumer smoke -> installed package | Installed package must compile without private headers or source-tree linkage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-07-01 | T | extraction examples | mitigate | Show caller-owned output roots and archive virtual paths as separate concepts; avoid path traversal-prone host path joins. |
| T-12-07-02 | I | examples/package consumer | mitigate | Include only `<libbsa/libbsa.hpp>` and link `libbsa::libbsa`, preserving private dependency boundaries. |
| T-12-07-03 | R | target-format guide | mitigate | Machine-check every supported variant/compression/warning token against public headers and docs. |
</threat_model>

<verification>
```powershell
cmake --build --preset windows-msvc-debug-static --target libbsa_tests
ctest --preset windows-msvc-debug-static -R "target_format_policy|package_consumer_smoke|validation_policy" --output-on-failure
git status --short TES5Edit
```
</verification>

<success_criteria>
- Integration examples compile through package-consumer smoke coverage.
- Target-format guide covers every supported variant, compression method, writer policy, and public compatibility warning code.
- Policy tests enforce docs/example drift checks.
- `git status --short TES5Edit` prints no output.
</success_criteria>

<output>
After completion, create `.planning/phases/12-performance-concurrency-documentation-and-polish/12-07-SUMMARY.md`.
</output>
