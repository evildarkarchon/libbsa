---
phase: 12-performance-concurrency-documentation-and-polish
plan: 06
type: execute
wave: 6
depends_on: ["12-01", "12-02", "12-05"]
files_modified:
  - CMakeLists.txt
  - include/libbsa/archive.hpp
  - include/libbsa/writer.hpp
  - include/libbsa/validation.hpp
  - docs/Doxyfile.in
  - docs/api-mainpage.md
  - docs/thread-safety.md
  - tests/unit/docs_policy_tests.cpp
  - tests/unit/thread_safety_docs_policy_tests.cpp
  - tests/CMakeLists.txt
autonomous: true
requirements: [PERF-06, DOC-01]
user_setup: []
must_haves:
  truths:
    - "Consumer can generate public API documentation when Doxygen is installed."
    - "Normal builds and tests do not fail when Doxygen is missing."
    - "Thread-safety rules cover readers, writers, entries, callbacks, sinks, validation, benchmarks, and bulk/parallel types."
    - "Public docs exclude private implementation, dependencies, build directories, and TES5Edit."
  artifacts:
    - path: "docs/Doxyfile.in"
      provides: "Public-header Doxygen configuration"
    - path: "docs/api-mainpage.md"
      provides: "Doxygen landing page"
    - path: "docs/thread-safety.md"
      provides: "Canonical public thread-safety rules"
    - path: "tests/unit/docs_policy_tests.cpp"
      provides: "Doxygen/config policy checks"
    - path: "tests/unit/thread_safety_docs_policy_tests.cpp"
      provides: "Thread-safety coverage policy checks"
  key_links:
    - from: "CMakeLists.txt"
      to: "docs/Doxyfile.in"
      via: "optional Doxygen target"
      pattern: "find_package\\(Doxygen QUIET\\)"
    - from: "public headers"
      to: "docs/thread-safety.md"
      via: "Thread-safety comments/reference"
      pattern: "Thread-safety"
---

<objective>
Add public API documentation generation and machine-checked thread-safety guidance.

Purpose: Consumers need documented concurrency contracts and generated public API docs without making Doxygen a runtime or normal-build dependency.
Output: Optional Doxygen CMake target, public docs pages, header thread-safety notes, and policy tests.

Scope rationale: Keep Doxygen target/config and thread-safety docs/policy in one plan because the Doxygen input must include `docs/thread-safety.md`, the public header comments link to that page, and the policy tests validate the combined docs boundary. Splitting would force cross-plan overlap on `CMakeLists.txt`, `tests/CMakeLists.txt`, and public headers without reducing the implementation risk.
</objective>

<execution_context>
@C:/Users/evild/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-performance-concurrency-documentation-and-polish/12-SPEC.md
@.planning/phases/12-performance-concurrency-documentation-and-polish/12-CONTEXT.md
@.planning/phases/12-performance-concurrency-documentation-and-polish/12-RESEARCH.md
@include/libbsa/archive.hpp
@include/libbsa/writer.hpp
@include/libbsa/validation.hpp
@CMakeLists.txt
@tests/unit/validation_policy_tests.cpp
@tests/CMakeLists.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add optional Doxygen public API target</name>
  <files>CMakeLists.txt, docs/Doxyfile.in, docs/api-mainpage.md</files>
  <read_first>
    - CMakeLists.txt
    - include/libbsa/archive.hpp
    - include/libbsa/writer.hpp
    - include/libbsa/validation.hpp
    - .planning/phases/12-performance-concurrency-documentation-and-polish/12-CONTEXT.md
    - .planning/phases/12-performance-concurrency-documentation-and-polish/12-RESEARCH.md
  </read_first>
  <action>
    Add `find_package(Doxygen QUIET)` to the root CMake file after project/options setup per D-21. If `DOXYGEN_FOUND`, configure `docs/Doxyfile.in` to `${CMAKE_CURRENT_BINARY_DIR}/Doxyfile` and add custom target `libbsa_docs` running `${DOXYGEN_EXECUTABLE}`. If Doxygen is missing, emit a `message(STATUS "Doxygen not found; libbsa_docs target not available")` and do not fail configure. Create `docs/Doxyfile.in` with `PROJECT_NAME = libbsa`, `INPUT = @CMAKE_CURRENT_SOURCE_DIR@/include/libbsa @CMAKE_CURRENT_SOURCE_DIR@/docs/api-mainpage.md @CMAKE_CURRENT_SOURCE_DIR@/docs/thread-safety.md`, `RECURSIVE = YES`, `EXCLUDE = @CMAKE_CURRENT_SOURCE_DIR@/src @CMAKE_CURRENT_SOURCE_DIR@/TES5Edit @CMAKE_CURRENT_SOURCE_DIR@/tests @CMAKE_CURRENT_BINARY_DIR@`, `GENERATE_HTML = YES`, `GENERATE_LATEX = NO`, `WARN_IF_UNDOCUMENTED = YES`, and `WARN_AS_ERROR = NO`. Create `docs/api-mainpage.md` with a Doxygen `@mainpage` that points to public archive reader, writer, validation, bulk extraction, execution options, thread-safety, and target-format docs.
  </action>
  <verify>
    <automated>pwsh -NoProfile -Command "cmake --preset windows-msvc-debug-static; if (Get-Command doxygen -ErrorAction SilentlyContinue) { cmake --build --preset windows-msvc-debug-static --target libbsa_docs } else { Write-Output 'Doxygen not installed; optional docs build skipped as designed' }"</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "find_package\\(Doxygen QUIET\\)|libbsa_docs|Doxyfile" CMakeLists.txt` finds optional target wiring.
    - `rg -n "INPUT = .*include/libbsa|EXCLUDE = .*src.*TES5Edit|GENERATE_HTML = YES|WARN_IF_UNDOCUMENTED = YES" docs/Doxyfile.in` finds public-header config.
    - `rg -n "@mainpage|archive_reader|extract_entries|write_execution_options|validate_archive" docs/api-mainpage.md` finds public API guide links.
  </acceptance_criteria>
  <done>Doxygen generation is discoverable when installed and absent Doxygen does not break normal configure/build.</done>
</task>

<task type="auto">
  <name>Task 2: Add canonical thread-safety documentation and public header notes</name>
  <files>docs/thread-safety.md, include/libbsa/archive.hpp, include/libbsa/writer.hpp, include/libbsa/validation.hpp</files>
  <read_first>
    - docs/thread-safety.md
    - include/libbsa/archive.hpp
    - include/libbsa/writer.hpp
    - include/libbsa/validation.hpp
    - .planning/phases/12-performance-concurrency-documentation-and-polish/12-CONTEXT.md
    - .planning/phases/12-performance-concurrency-documentation-and-polish/12-SPEC.md
  </read_first>
  <action>
    Create `docs/thread-safety.md` with exact headings for `archive_reader`, `payload_sink`, `bulk_extract_sink_factory`, `bulk_extract_options`, `bulk_extract_entry_result`, `tes3_bsa_writer`, `tes4_bsa_writer`, `ba2_gnrl_writer`, `ba2_dx10_writer`, `write_execution_options`, `validation_report`, `validate_archive`, and `libbsa_benchmarks`. State these rules: independently opened readers may be used concurrently; one `archive_reader` object may run concurrent const extraction calls only when each entry has a distinct caller-owned sink; sinks and sink factories are caller-owned and must be safe for the way they are used; writer mutation (`add_file`/`add_bytes`) is not concurrent with other mutation or `write_to`; independent writer objects may be used concurrently; `write_to` owns its worker scheduling for that call; validation has no global mutable state; benchmark reports are maintainer tooling and not a synchronization primitive. Add short Doxygen comments or references in `archive.hpp`, `writer.hpp`, and `validation.hpp` that point public types to `docs/thread-safety.md` and mention D-23 where the rule is non-obvious. Do not delete accurate existing comments.
  </action>
  <verify>
    <automated>cmake --build --preset windows-msvc-debug-static --target libbsa_tests && ctest --preset windows-msvc-debug-static -R "public_include_boundary" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "## archive_reader|## payload_sink|## bulk_extract_sink_factory|## tes3_bsa_writer|## tes4_bsa_writer|## ba2_gnrl_writer|## ba2_dx10_writer|## validate_archive|## libbsa_benchmarks" docs/thread-safety.md` finds all required sections.
    - `rg -n "Thread-safety" include/libbsa/archive.hpp include/libbsa/writer.hpp include/libbsa/validation.hpp` finds public header notes.
    - Public boundary tests pass.
  </acceptance_criteria>
  <done>Thread-safety guarantees are documented and referenced by public API comments.</done>
</task>

<task type="auto">
  <name>Task 3: Add docs and thread-safety policy tests</name>
  <files>tests/unit/docs_policy_tests.cpp, tests/unit/thread_safety_docs_policy_tests.cpp, tests/CMakeLists.txt</files>
  <read_first>
    - tests/CMakeLists.txt
    - tests/unit/validation_policy_tests.cpp
    - docs/Doxyfile.in
    - docs/api-mainpage.md
    - docs/thread-safety.md
    - include/libbsa/archive.hpp
    - include/libbsa/writer.hpp
    - include/libbsa/validation.hpp
  </read_first>
  <action>
    Add `tests/unit/docs_policy_tests.cpp` and `tests/unit/thread_safety_docs_policy_tests.cpp` using the existing source-text policy-test style. `docs_policy_tests.cpp` must require Doxygen optional CMake wiring, Doxyfile public inputs, `TES5Edit` exclusion, `src` exclusion, public headers listed through `include/libbsa`, `WARN_IF_UNDOCUMENTED = YES`, and no `WARN_AS_ERROR = YES`. `thread_safety_docs_policy_tests.cpp` must parse public headers for public type names and assert each required type appears in `docs/thread-safety.md`; it must also require sink factory distinct-sink wording, writer mutation/finalization wording, validation no-global-state wording, and benchmark report-tool wording. Register both tests.
  </action>
  <verify>
    <automated>cmake --build --preset windows-msvc-debug-static --target libbsa_tests && ctest --preset windows-msvc-debug-static -R "docs_policy|thread_safety_policy|public_include_boundary" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "docs_policy|Doxyfile|TES5Edit|WARN_IF_UNDOCUMENTED" tests/unit/docs_policy_tests.cpp` finds Doxygen policy coverage.
    - `rg -n "thread_safety_policy|bulk_extract_sink_factory|write_execution_options|distinct sink|no global mutable state" tests/unit/thread_safety_docs_policy_tests.cpp` finds thread-safety policy coverage.
    - Focused docs/thread-safety/public-boundary tests pass.
  </acceptance_criteria>
  <done>Docs generation and thread-safety guidance are machine-checked by default tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public API docs -> consumers | Consumers rely on documented safety and ownership contracts. |
| Doxygen config -> repository files | Docs target scans selected paths and must not include private or read-only reference material. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-06-01 | I | Doxygen input scope | mitigate | Configure input to `include/libbsa` and docs only; exclude `src`, `tests`, build dirs, and `TES5Edit`. |
| T-12-06-02 | D | missing local Doxygen | mitigate | Use `find_package(Doxygen QUIET)` and policy tests so normal builds continue when Doxygen is absent. |
| T-12-06-03 | T/D | thread-safety documentation | mitigate | Machine-check public type coverage and distinct sink/callback ownership rules per D-23. |
</threat_model>

<verification>
```powershell
cmake --build --preset windows-msvc-debug-static --target libbsa_tests
ctest --preset windows-msvc-debug-static -R "docs_policy|thread_safety_policy|public_include_boundary" --output-on-failure
pwsh -NoProfile -Command "if (Get-Command doxygen -ErrorAction SilentlyContinue) { cmake --build --preset windows-msvc-debug-static --target libbsa_docs } else { Write-Output 'Doxygen not installed; optional docs build skipped as designed' }"
git status --short TES5Edit
```
</verification>

<success_criteria>
- Optional `libbsa_docs` target is available when Doxygen is installed.
- Normal configure/build works when Doxygen is absent.
- Thread-safety docs cover every required public type and workflow.
- Policy tests pass.
- `git status --short TES5Edit` prints no output.
</success_criteria>

<output>
After completion, create `.planning/phases/12-performance-concurrency-documentation-and-polish/12-06-SUMMARY.md`.
</output>
