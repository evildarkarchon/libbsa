---
phase: 12-performance-concurrency-documentation-and-polish
plan: 05
type: execute
wave: 5
depends_on: ["12-01", "12-03", "12-04"]
files_modified:
  - CMakeLists.txt
  - benchmarks/libbsa_benchmarks.cpp
  - benchmarks/README.md
  - tests/unit/benchmark_policy_tests.cpp
  - tests/CMakeLists.txt
autonomous: true
requirements: [PERF-05]
user_setup: []
must_haves:
  truths:
    - "Maintainer can build a benchmark executable from the normal CMake build."
    - "Maintainer can run an explicit benchmark report target that compares worker_count 1 and greater than 1."
    - "Benchmark correctness checks use legal synthetic data and do not require game archives or BSArchPro output."
    - "Default CTest does not fail on a fixed speedup threshold."
  artifacts:
    - path: "benchmarks/libbsa_benchmarks.cpp"
      provides: "Synthetic benchmark runner and report writer"
    - path: "benchmarks/README.md"
      provides: "Benchmark command, report schema, and no-speed-threshold policy"
    - path: "CMakeLists.txt"
      provides: "libbsa_benchmarks executable and libbsa_benchmark_report custom target"
    - path: "tests/unit/benchmark_policy_tests.cpp"
      provides: "Policy checks for benchmark target/report/default-test behavior"
  key_links:
    - from: "CMakeLists.txt"
      to: "benchmarks/libbsa_benchmarks.cpp"
      via: "add_executable(libbsa_benchmarks ...)"
      pattern: "libbsa_benchmarks"
    - from: "benchmarks/libbsa_benchmarks.cpp"
      to: "archive_reader::extract_entries and writer write_to execution options"
      via: "single-threaded and multi-threaded scenarios"
      pattern: "worker_count"
---

<objective>
Add the Phase 12 benchmark harness required by D-19 and D-20.

Purpose: Maintainers need repeatable correctness-checked benchmark reports for serial versus opt-in parallel archive workflows, without turning host timing variance into a default CI failure.
Output: A buildable benchmark executable, explicit report target, README, and policy tests.
</objective>

<execution_context>
@C:/Users/evild/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-performance-concurrency-documentation-and-polish/12-SPEC.md
@.planning/phases/12-performance-concurrency-documentation-and-polish/12-CONTEXT.md
@.planning/phases/12-performance-concurrency-documentation-and-polish/12-RESEARCH.md
@CMakeLists.txt
@tests/CMakeLists.txt
@tests/fixtures/README.md
@tests/fixtures/generated/generate_tes3_bsa_writer_fixtures.cpp
@tests/fixtures/generated/generate_ba2_dx10_fixtures.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add synthetic benchmark runner</name>
  <files>benchmarks/libbsa_benchmarks.cpp</files>
  <read_first>
    - benchmarks/libbsa_benchmarks.cpp
    - tests/fixtures/generated/generate_tes3_bsa_writer_fixtures.cpp
    - tests/fixtures/generated/generate_ba2_dx10_fixtures.cpp
    - include/libbsa/archive.hpp
    - include/libbsa/writer.hpp
    - .planning/phases/12-performance-concurrency-documentation-and-polish/12-CONTEXT.md
  </read_first>
  <action>
    Create `benchmarks/libbsa_benchmarks.cpp` as a custom C++20 benchmark runner with no new third-party dependency. It must accept `--output-json <path>` and `--output-markdown <path>`, generate legal synthetic payloads under a temporary directory, run equivalent scenarios with `worker_count = 1U` and `worker_count = 4U`, verify correctness after each scenario by reopening and extracting through public APIs, and write both JSON and Markdown reports. Include at least these scenario names in both reports: `tes4_bsa_pack_extract`, `ba2_gnrl_pack_extract`, `ba2_dx10_pack_extract`, and `bulk_extract`. Include fields `scenario`, `worker_count`, `elapsed_ms`, `bytes_processed`, and `correctness_passed`. Do not assert a speedup threshold in the executable; return nonzero only for setup, write, open, extract, or report I/O failures.
  </action>
  <verify>
    <automated>cmake --build --preset windows-msvc-debug-static --target libbsa_benchmarks</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "tes4_bsa_pack_extract|ba2_gnrl_pack_extract|ba2_dx10_pack_extract|bulk_extract|worker_count|correctness_passed|elapsed_ms" benchmarks/libbsa_benchmarks.cpp` finds required scenario/report tokens.
    - `rg -n "speedup.*REQUIRE|speedup.*CHECK|fixed speedup|Google Benchmark" benchmarks/libbsa_benchmarks.cpp` returns no matches.
    - `cmake --build --preset windows-msvc-debug-static --target libbsa_benchmarks` succeeds.
  </acceptance_criteria>
  <done>Benchmark runner builds and can emit correctness-checked JSON/Markdown reports without speed thresholds.</done>
</task>

<task type="auto">
  <name>Task 2: Wire explicit benchmark report target and README</name>
  <files>CMakeLists.txt, benchmarks/README.md</files>
  <read_first>
    - CMakeLists.txt
    - CMakePresets.json
    - tests/fixtures/README.md
    - .planning/phases/12-performance-concurrency-documentation-and-polish/12-CONTEXT.md
    - .planning/phases/12-performance-concurrency-documentation-and-polish/12-VALIDATION.md
  </read_first>
  <action>
    Add `option(LIBBSA_BUILD_BENCHMARKS "Build libbsa benchmark tools" ON)` to the root CMake file. When enabled, add `add_executable(libbsa_benchmarks benchmarks/libbsa_benchmarks.cpp)`, link it to `libbsa::libbsa`, set C++20, and add custom target `libbsa_benchmark_report` that runs `$<TARGET_FILE:libbsa_benchmarks> --output-json "${CMAKE_BINARY_DIR}/benchmarks/libbsa-benchmark.json" --output-markdown "${CMAKE_BINARY_DIR}/benchmarks/libbsa-benchmark.md"`. Create `benchmarks/README.md` documenting the exact build and report commands, report fields, legal synthetic data policy, and the D-20 rule that default CTest does not gate on a fixed speedup threshold.
  </action>
  <verify>
    <automated>cmake --build --preset windows-msvc-debug-static --target libbsa_benchmark_report</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "LIBBSA_BUILD_BENCHMARKS|libbsa_benchmarks|libbsa_benchmark_report|libbsa-benchmark.json|libbsa-benchmark.md" CMakeLists.txt benchmarks/README.md` finds target wiring and docs.
    - `rg -n "fixed speedup threshold|does not gate|legal synthetic" benchmarks/README.md` finds benchmark policy text.
    - Building `libbsa_benchmark_report` produces `${CMAKE_BINARY_DIR}/benchmarks/libbsa-benchmark.json` and `.md`.
  </acceptance_criteria>
  <done>Benchmark target is explicit, reportable, and documented without default timing gates.</done>
</task>

<task type="auto">
  <name>Task 3: Add benchmark policy tests</name>
  <files>tests/unit/benchmark_policy_tests.cpp, tests/CMakeLists.txt</files>
  <read_first>
    - tests/CMakeLists.txt
    - tests/unit/validation_policy_tests.cpp
    - CMakeLists.txt
    - benchmarks/README.md
    - benchmarks/libbsa_benchmarks.cpp
  </read_first>
  <action>
    Add `tests/unit/benchmark_policy_tests.cpp` using the existing source-text policy-test style. Tests must require the CMake option, executable target, custom report target, JSON/Markdown report names, README command, legal synthetic data wording, and absence of fixed speedup threshold gates. Register the test file in `tests/CMakeLists.txt`.
  </action>
  <verify>
    <automated>cmake --build --preset windows-msvc-debug-static --target libbsa_tests && ctest --preset windows-msvc-debug-static -R "benchmark_policy" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "benchmark_policy|libbsa_benchmark_report|libbsa-benchmark.json|fixed speedup" tests/unit/benchmark_policy_tests.cpp` finds policy coverage.
    - `ctest --preset windows-msvc-debug-static -R "benchmark_policy" --output-on-failure` passes.
    - `ctest --preset windows-msvc-debug-static --show-only=json-v1 | rg "libbsa_benchmark_report|elapsed_ms"` returns no matches, proving the report target is not a default CTest timing gate.
  </acceptance_criteria>
  <done>Benchmark wiring is machine-checked and kept out of default CTest speed assertions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| benchmark runner -> filesystem | Runner creates synthetic inputs and report files. |
| benchmark runner -> public libbsa APIs | Runner drives pack/extract workflows with generated data. |
| report -> maintainers | Timing values are informational and host-dependent. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05-01 | T | synthetic benchmark data | mitigate | Generate only repository-owned temporary data and never use `TES5Edit/` or game archives per D-26/D-27. |
| T-12-05-02 | R | benchmark report interpretation | mitigate | Include `correctness_passed` and document that speed is report-only with no fixed default threshold per D-20. |
| T-12-05-03 | D | default CI/runtime | mitigate | Keep report generation as explicit custom target and policy-check absence from default CTest. |
</threat_model>

<verification>
```powershell
cmake --build --preset windows-msvc-debug-static --target libbsa_benchmark_report
cmake --build --preset windows-msvc-debug-static --target libbsa_tests
ctest --preset windows-msvc-debug-static -R "benchmark_policy" --output-on-failure
git status --short TES5Edit
```
</verification>

<success_criteria>
- `libbsa_benchmarks` and `libbsa_benchmark_report` build.
- JSON and Markdown benchmark reports are emitted by the explicit target.
- Default CTest has no fixed speedup threshold.
- `git status --short TES5Edit` prints no output.
</success_criteria>

<output>
After completion, create `.planning/phases/12-performance-concurrency-documentation-and-polish/12-05-SUMMARY.md`.
</output>
