---
phase: 10-ba2-writers
plan: 07
type: execute
wave: 7
depends_on: ["10-04", "10-05", "10-06"]
files_modified:
  - src/texture/dds_analysis.cpp
  - src/texture/dds_reconstruction.cpp
  - src/ba2_writer.cpp
  - tests/ba2_writer_tests.cpp
  - README.md
autonomous: true
gap_closure: true
requirements: [WRT-03]
must_haves:
  truths:
    - "Consumer can create BA2 DDS/DX10 archives from every explicitly supported DDS writer format, not only BC1."
    - "DDS/DX10 compressed chunk planning cannot emit native PackedSize == Size for compressed data that the reader will interpret as raw."
    - "README BA2 writer support claims match the implemented DDS/DX10 format and compression behavior."
  artifacts:
    - path: "src/texture/dds_analysis.cpp"
      provides: "DirectXTex-backed planning-time DDS format acceptance for the supported writer format set"
      contains: "supported_dds_writer_format"
    - path: "src/texture/dds_reconstruction.cpp"
      provides: "DDS reconstruction metadata for all supported writer formats"
      contains: "top_level_linear_size"
    - path: "src/ba2_writer.cpp"
      provides: "DX10 chunk storage disambiguation before native table emission"
      contains: "PackedSize == Size"
    - path: "tests/ba2_writer_tests.cpp"
      provides: "Regression coverage for non-BC1 DDS writer formats and equal-or-larger compressed chunk fallback"
      contains: "BA2 DDS writer accepts the supported DX10 format set"
    - path: "README.md"
      provides: "Consumer-facing DDS/DX10 writer support boundaries"
      contains: "Supported DDS writer formats"
  key_links:
    - from: "src/texture/dds_analysis.cpp"
      to: "src/ba2_writer.cpp"
      via: "detail::analyze_dds returns the accepted dxgi_format value consumed by plan_ba2_dds_write"
      pattern: "analyzed\.format = dxgi_format\{format\.value\(\)\}"
    - from: "src/ba2_writer.cpp"
      to: "src/ba2_reader.cpp"
      via: "DX10 chunk records avoid compressed PackedSize == Size states that reader classifies as raw"
      pattern: "compression_state::raw"
    - from: "tests/ba2_writer_tests.cpp"
      to: "README.md"
      via: "README names the same supported DDS writer format set proven by tests"
      pattern: "BC1.*BC3.*BC5.*BC7.*R8G8B8A8"
---

<objective>
Close Phase 10 verification gaps for BA2 DDS/DX10 writer correctness.

Purpose: WRT-03 remains blocked because DDS analysis only accepts BC1 while public support claims BA2 DDS/DX10 writing, and because a compressed DX10 chunk with native `PackedSize == Size` is read back as raw. This plan makes the implemented format boundary explicit in code/tests/docs and prevents ambiguous compressed chunk emission.

Output: Focused gap-closure code and tests for supported DDS writer formats, DX10 chunk compression disambiguation, and README support boundaries.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-ba2-writers/10-CONTEXT.md
@.planning/phases/10-ba2-writers/10-RESEARCH.md
@.planning/phases/10-ba2-writers/10-VERIFICATION.md
@.planning/phases/10-ba2-writers/10-04-SUMMARY.md
@.planning/phases/10-ba2-writers/10-05-SUMMARY.md
@.planning/phases/10-ba2-writers/10-06-SUMMARY.md

<interfaces>
Existing contracts the executor must preserve:

From `src/texture/dds_analysis.hpp`:
```cpp
struct analyzed_dds_chunk {
    std::uint16_t start_mip{};
    std::uint16_t end_mip{};
    std::vector<std::byte> payload;
};

struct analyzed_dds_texture {
    dxgi_format format{};
    std::uint32_t width{};
    std::uint32_t height{};
    std::uint32_t mip_count{};
    std::uint32_t array_size{};
    bool is_cubemap{};
    std::vector<analyzed_dds_chunk> chunks;
};

[[nodiscard]] result<analyzed_dds_texture> analyze_dds(std::span<const std::byte> dds_bytes);
```

From `tests/ba2_writer_tests.cpp` helpers:
```cpp
std::vector<std::byte> generated_bc1_dds(std::uint32_t width,
                                         std::uint32_t height,
                                         std::uint32_t mip_count,
                                         std::uint32_t array_size,
                                         bool cubemap,
                                         std::byte seed);

void require_valid_dds(std::span<const std::byte> bytes,
                       std::uint32_t width,
                       std::uint32_t height,
                       std::uint32_t mip_count,
                       std::uint32_t array_size,
                       bool cubemap);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing DDS writer regression tests</name>
  <files>tests/ba2_writer_tests.cpp</files>
  <read_first>tests/ba2_writer_tests.cpp, .planning/phases/10-ba2-writers/10-VERIFICATION.md, .planning/phases/10-ba2-writers/10-05-SUMMARY.md</read_first>
  <behavior>
    - Test 1: `BA2 DDS writer accepts the supported DX10 format set` creates generated DDS/DX10 inputs for exact supported writer formats `BC1_UNORM` value 71, `BC3_UNORM` value 77, `BC5_UNORM` value 83, `BC7_UNORM` value 98, and `R8G8B8A8_UNORM` value 28; each plans as `fallout4_dx10_v8`, reopens through `open_ba2`, reports the same `texture_metadata.format.value`, and extracts DDS bytes accepted by `detail::validate_dds`.
    - Test 2: `BA2 DDS writer rejects unsupported DX10 formats outside the supported set` mutates a generated DX10 header to format value 74 and expects `plan_ba2_dds_write(...).error().code == error_code::unsupported_format`.
    - Test 3: `BA2 DX10 forced compression stores equal or larger chunks raw to avoid PackedSize Size ambiguity` uses a tiny forced-compressed DDS input that compresses to a byte count greater than or equal to the source chunk, expects planned chunk `compression == compression_state::raw`, `packed_size == unpacked_size`, and extracted DDS validation success.
  </behavior>
  <action>
    Add RED coverage for the exact gaps in `10-VERIFICATION.md`. Replace the BC1-only helper model with format-aware generated DDS helpers:
    - Add constants: `dxgi_format_r8g8b8a8_unorm = 28U`, `dxgi_format_bc1_unorm = 71U`, `dxgi_format_bc3_unorm = 77U`, `dxgi_format_bc5_unorm = 83U`, `dxgi_format_bc7_unorm = 98U`.
    - Add a helper `dds_mip_size(format, width, height)` that returns `(max(1,(w+3)/4) * max(1,(h+3)/4) * block_bytes)` for BC formats where BC1 uses 8 bytes/block and BC3/BC5/BC7 use 16 bytes/block; for R8G8B8A8 return `max(1,w) * max(1,h) * 4`.
    - Generalize `generated_bc1_dds` into `generated_dx10_dds(format, width, height, mip_count, array_size, cubemap, seed)` and keep a wrapper named `generated_bc1_dds` if existing tests still use it.
    - Update `require_valid_dds` or add an overload so tests can assert the expected `format.value` in addition to width/height/mips/array/cubemap.
    - Add the three tests listed in `<behavior>` without weakening existing BC1, disk, compression, dedup, malformed, public-smoke, or reader tests.
    - Per D-13/D-14/D-16, tests must use actual DDS bytes and prove planning-time structural failures; do not add public native texture descriptors.
  </action>
  <verify>
    <automated>cmake --build build/windows-vs2026-vcpkg --config Debug --target libbsa_ba2_writer_tests; ctest --test-dir build/windows-vs2026-vcpkg --output-on-failure -C Debug -R libbsa_ba2_writer_tests</automated>
  </verify>
  <acceptance_criteria>
    - `tests/ba2_writer_tests.cpp` contains `dxgi_format_bc3_unorm = 77U`, `dxgi_format_bc5_unorm = 83U`, `dxgi_format_bc7_unorm = 98U`, and `dxgi_format_r8g8b8a8_unorm = 28U`.
    - `tests/ba2_writer_tests.cpp` contains test name `BA2 DDS writer accepts the supported DX10 format set`.
    - `tests/ba2_writer_tests.cpp` contains test name `BA2 DX10 forced compression stores equal or larger chunks raw to avoid PackedSize Size ambiguity`.
    - Before implementation tasks, the focused CTest command fails on these new expectations; after implementation tasks, the same command passes.
  </acceptance_criteria>
  <done>Failing tests exist for every verification gap: non-BC1 supported writer formats, unsupported-format rejection outside the supported set, and DX10 equal-or-larger compressed chunk ambiguity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement supported DDS format analysis and reconstruction</name>
  <files>src/texture/dds_analysis.cpp, src/texture/dds_reconstruction.cpp</files>
  <read_first>src/texture/dds_analysis.cpp, src/texture/dds_reconstruction.cpp, src/texture/dds_analysis.hpp, tests/ba2_writer_tests.cpp, .planning/phases/10-ba2-writers/10-VERIFICATION.md</read_first>
  <behavior>
    - `detail::analyze_dds` accepts DirectXTex-loaded 2D DDS/DX10 inputs for exactly format values 28, 71, 77, 83, and 98, carries `metadata.format` through to `analyzed.format`, and still rejects unsupported DX10 format value 74 with `unsupported_format` before returning a plan.
    - `detail::reconstruct_dds` emits a DDS/DX10 header whose format field matches `texture_metadata.format.value` and whose top-level linear-size field matches the format-specific size calculation used by generated tests.
  </behavior>
  <action>
    In `src/texture/dds_analysis.cpp`, replace `supported_bc1_unorm` and hard-coded equality checks with a named helper:
    - Add constants for the exact supported writer set: `dxgi_format_r8g8b8a8_unorm = 28U`, `dxgi_format_bc1_unorm = 71U`, `dxgi_format_bc3_unorm = 77U`, `dxgi_format_bc5_unorm = 83U`, `dxgi_format_bc7_unorm = 98U`.
    - Add `bool supported_dds_writer_format(std::uint32_t format) noexcept` returning true only for those five values.
    - `reject_unsupported_dx10_format` must call `supported_dds_writer_format(read_u32(dds_bytes, dds_dx10_format_offset))` and return `error_code::unsupported_format` with message `DDS analysis failed: unsupported DDS format` for unsupported DX10 headers.
    - After DirectXTex load and metadata narrowing, replace `format.value() != supported_bc1_unorm` with `!supported_dds_writer_format(format.value())` so DirectXTex-derived `metadata.format` is carried through for every supported format per D-14.

    In `src/texture/dds_reconstruction.cpp`, make reconstruction support exactly the same set:
    - Replace `bc1_top_level_size` with `top_level_linear_size(dxgi_format format, std::uint32_t width, std::uint32_t height)`.
    - For BC1 return blocks * 8; for BC3/BC5/BC7 return blocks * 16; for R8G8B8A8 return `width * 4`; keep the field as the DDS top-level pitch/linear-size value multiplied by array size where existing code multiplies it.
    - Replace `supported_reconstruction_format` with the same five-format allowlist so extraction validation works for all supported writer formats.
    - Preserve existing comments and add a short WHY comment near the allowlist explaining Phase 10 intentionally supports the DDS formats that libbsa can reconstruct and validate without transcoding; broader format corpus tuning belongs to Phase 11.

    Do not expose DirectXTex, DXGI native headers, Windows headers, libdeflate, LZ4, or TES5Edit names in public headers. Do not edit `TES5Edit/`.
  </action>
  <verify>
    <automated>cmake --build build/windows-vs2026-vcpkg --config Debug --target libbsa_ba2_writer_tests; ctest --test-dir build/windows-vs2026-vcpkg --output-on-failure -C Debug -R libbsa_ba2_writer_tests</automated>
  </verify>
  <acceptance_criteria>
    - `src/texture/dds_analysis.cpp` contains `supported_dds_writer_format`.
    - `src/texture/dds_analysis.cpp` does not contain `supported_bc1_unorm`.
    - `src/texture/dds_reconstruction.cpp` contains `top_level_linear_size`.
    - `src/texture/dds_reconstruction.cpp` contains all supported values `28U`, `71U`, `77U`, `83U`, and `98U` in its reconstruction allowlist or adjacent constants.
    - `ctest --test-dir build/windows-vs2026-vcpkg --output-on-failure -C Debug -R libbsa_ba2_writer_tests` passes the new supported-format tests.
  </acceptance_criteria>
  <done>DDS planning and extraction validation support the explicit Phase 10 writer format set instead of BC1-only analysis, with DirectXTex metadata.format preserved into plan preview and read-back metadata.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Disambiguate DX10 chunk compression and document boundaries</name>
  <files>src/ba2_writer.cpp, tests/ba2_writer_tests.cpp, README.md</files>
  <read_first>src/ba2_writer.cpp, tests/ba2_writer_tests.cpp, README.md, .planning/phases/10-ba2-writers/10-VERIFICATION.md, .planning/phases/10-ba2-writers/10-06-SUMMARY.md</read_first>
  <behavior>
    - If a DX10 chunk is requested as compressed but the compressed byte count is greater than or equal to the uncompressed chunk byte count, the plan stores the original chunk bytes as raw with `compression_state::raw` and native `packed_size == unpacked_size`, preventing the BA2 reader from interpreting compressed bytes as raw.
    - If a DX10 chunk compresses smaller than the source chunk, existing deflate and Starfield method-3 raw LZ4-block routes remain compressed and read back correctly.
    - README names the supported DDS writer formats and explains that unsupported DDS formats fail structurally during planning instead of being transcoded.
  </behavior>
  <action>
    In `src/ba2_writer.cpp`, change DX10 chunk storage planning inside `plan_ba2_dds_write` so compressed chunks never produce a native compressed record where `packed_size == size`:
    - After `stored_dds_payload_for(...)` and size checks, if requested `compression != compression_state::raw` and `stored.value().size() >= source_chunk.payload.size()`, replace `stored` with a fresh vector copied from `source_chunk.payload` and set the effective chunk compression state to `compression_state::raw` before assigning `chunk.packed_size`, `chunk.compression`, `planned_ba2_data_region.compression`, and dedup comparisons.
    - Add a WHY comment near this branch: `BA2 DX10 readers use PackedSize == Size as the raw marker, so equal-size compressed bytes would be extracted as image data.`
    - Keep GNRL behavior unchanged; the raw marker for GNRL remains `PackedSize == 0` per existing comments.
    - Deduplication must compare the effective stored bytes after this decision so two chunks that become raw can still share data regions when `options.deduplicate` is true.

    In `tests/ba2_writer_tests.cpp`, update the new RED test from Task 1 so it passes by asserting the effective raw fallback behavior. Keep existing compression-route tests for larger compressible DDS inputs passing so real compressed deflate/LZ4 chunks still prove WRT-03.

    In `README.md` under `## BA2 writer support`, add a sentence naming `Supported DDS writer formats: R8G8B8A8_UNORM, BC1_UNORM, BC3_UNORM, BC5_UNORM, and BC7_UNORM.` Add another sentence explaining unsupported DDS/DX10 formats return structured `unsupported_format` during planning and texture transcoding/format conversion remains out of scope.
  </action>
  <verify>
    <automated>cmake --build build/windows-vs2026-vcpkg --config Debug --target libbsa_ba2_writer_tests libbsa_public_header_smoke; ctest --test-dir build/windows-vs2026-vcpkg --output-on-failure -C Debug -R "libbsa_ba2_writer_tests|libbsa_ba2_dds_reader_tests|libbsa.public_header_smoke"; rg -n "DirectXTex|DXGI_FORMAT|Windows\.h|libdeflate|TES5Edit|lz4\.h|lz4frame\.h|LZ4_" include/libbsa; if ($LASTEXITCODE -eq 0) { exit 1 } else { exit 0 }</automated>
  </verify>
  <acceptance_criteria>
    - `src/ba2_writer.cpp` contains `BA2 DX10 readers use PackedSize == Size as the raw marker`.
    - `src/ba2_writer.cpp` contains a branch matching `stored.value().size() >= source_chunk.payload.size()` or equivalent before `chunk.packed_size` is assigned.
    - `tests/ba2_writer_tests.cpp` contains `compression_state::raw` assertions in `BA2 DX10 forced compression stores equal or larger chunks raw to avoid PackedSize Size ambiguity`.
    - `README.md` contains `Supported DDS writer formats: R8G8B8A8_UNORM, BC1_UNORM, BC3_UNORM, BC5_UNORM, and BC7_UNORM.`
    - Focused BA2 writer/DDS reader/public-smoke CTest command passes.
    - Public header leakage grep returns no matches; the scripted command exits 0 only when no public leakage matches are found.
  </acceptance_criteria>
  <done>DX10 writer planning cannot emit ambiguous equal-size compressed chunks, non-BC1 supported DDS formats round-trip, and README support boundaries match implementation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller DDS bytes -> DirectXTex analysis | Untrusted DDS memory/disk bytes are parsed during planning. |
| planner -> BA2 reader read-back | Writer-emitted native chunk metadata is later trusted by the reader to route raw/deflate/LZ4 extraction. |
| implementation -> public documentation | Consumer-facing docs must not overclaim support beyond tested behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-07-01 | T | `src/texture/dds_analysis.cpp` | mitigate | Use an explicit supported-format allowlist and structured `unsupported_format` failures for unsupported DDS/DX10 formats before plan creation. |
| T-10-07-02 | T | `src/ba2_writer.cpp` DX10 chunk records | mitigate | Store equal-or-larger compressed chunks as raw so native `PackedSize == Size` cannot corrupt extraction. |
| T-10-07-03 | I | `README.md` | mitigate | Document exact supported DDS writer formats and unsupported-format planning failures. |
| T-10-07-04 | E | public headers | mitigate | Run public-header leakage grep to ensure DirectXTex/DXGI/Windows/codec/TES5Edit implementation details remain private. |
</threat_model>

<verification>
Run focused gap closure verification:

```powershell
cmake --build build/windows-vs2026-vcpkg --config Debug --target libbsa_ba2_writer_tests libbsa_public_header_smoke
ctest --test-dir build/windows-vs2026-vcpkg --output-on-failure -C Debug -R "libbsa_ba2_writer_tests|libbsa_ba2_dds_reader_tests|libbsa.public_header_smoke"
rg -n "DirectXTex|DXGI_FORMAT|Windows\.h|libdeflate|TES5Edit|lz4\.h|lz4frame\.h|LZ4_" include/libbsa
git status --short TES5Edit
```

The `rg` command must produce no matches; `git status --short TES5Edit` must produce no output.
</verification>

<success_criteria>
- `src/texture/dds_analysis.cpp` no longer rejects all non-BC1 DDS/DX10 inputs; it accepts exactly R8G8B8A8_UNORM, BC1_UNORM, BC3_UNORM, BC5_UNORM, and BC7_UNORM.
- `src/texture/dds_reconstruction.cpp` can reconstruct DDS headers for every supported writer format so read-after-write extraction validates through `detail::validate_dds`.
- `src/ba2_writer.cpp` never emits compressed DX10 chunks with native `PackedSize == Size`; equal-or-larger compressed results are stored as raw with a compatibility comment.
- `tests/ba2_writer_tests.cpp` proves the supported format set, unsupported-format failure, and equal-or-larger compressed chunk handling.
- `README.md` names supported DDS writer formats and does not overclaim unsupported transcoding or broad DDS format support.
</success_criteria>

<output>
After completion, create `.planning/phases/10-ba2-writers/10-07-SUMMARY.md`.
</output>
