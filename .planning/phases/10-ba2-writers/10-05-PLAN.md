---
phase: 10-ba2-writers
plan: 05
type: tdd
wave: 5
depends_on: [10-04]
files_modified:
  - tests/ba2_writer_tests.cpp
  - src/ba2_writer.cpp
  - src/texture/dds_analysis.cpp
autonomous: true
requirements: [WRT-03]
must_haves:
  truths:
    - "Consumer can create BA2 DDS/DX10 archives from disk DDS files and memory DDS bytes with equivalent read-back."
    - "DDS chunks support raw, deflate, and Starfield v3 method-3 raw LZ4-block compression without fallback."
    - "Opt-in DDS chunk dedup shares identical post-policy chunk payload offsets where BA2 layout permits it."
    - "Malformed, unsupported, or unrepresentable DDS inputs fail structurally during planning with no partial plan."
  artifacts:
    - path: "tests/ba2_writer_tests.cpp"
      provides: "DDS disk, compression, dedup, malformed-input, and sink-failure tests"
      contains: "disk-backed and memory-backed BA2 DDS inputs read back equivalently"
    - path: "src/ba2_writer.cpp"
      provides: "DX10 disk planning, compression, dedup, and failure behavior"
      contains: "plan_ba2_dds_write_from_disk"
  key_links:
    - from: "src/ba2_writer.cpp"
      to: "src/compression.cpp"
      via: "per-chunk compression route using archive format and Starfield method"
      pattern: "compression_method"
---

<objective>
TDD complete BA2 DDS/DX10 writer behavior for disk inputs, chunk compression, deduplication, malformed DDS rejection, and finalization failure safety per WRT-03.

Purpose: Close DDS writer requirements after memory layout and analyzer exist.
Output: Disk-backed DDS planning, raw/deflate/LZ4 chunk compression, chunk dedup preview, malformed/unsupported DDS failures, and sink failure coverage.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-ba2-writers/10-CONTEXT.md
@.planning/phases/10-ba2-writers/10-SPEC.md
@.planning/phases/10-ba2-writers/10-RESEARCH.md
@.planning/phases/10-ba2-writers/10-PATTERNS.md
@include/libbsa/ba2_writer.hpp
@src/ba2_writer.cpp
@src/texture/dds_analysis.cpp
@src/compression.cpp
@tests/ba2_writer_tests.cpp
@tests/bsa_writer_tests.cpp
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 RED: Add failing DDS disk, compression, dedup, and failure tests</name>
  <files>tests/ba2_writer_tests.cpp</files>
  <read_first>tests/ba2_writer_tests.cpp, src/ba2_writer.cpp, src/texture/dds_analysis.cpp, tests/bsa_writer_tests.cpp, tests/ba2_dds_reader_tests.cpp, .planning/phases/10-ba2-writers/10-SPEC.md</read_first>
  <behavior>
    - Disk-backed DDS inputs validate archive paths before host reads, read DDS bytes during planning, and remain unchanged if the host file mutates before finalization.
    - Raw DX10 chunks use `packed_size == size`; compressed chunks use `packed_size == compressed_size` and `size == uncompressed_chunk_size`.
    - Starfield DX10 v3 method 3 compressed chunks reopen as `lz4_block`; raw chunks remain raw even when archive method is 3.
    - Dedup enabled shares identical post-policy DDS chunk offsets; dedup disabled emits distinct chunk data regions.
    - Malformed DDS bytes, unsupported DXGI/layouts, impossible mip/chunk mapping, duplicate normalized paths, missing disk files, unsupported compression methods, layout overflow, and sink failures return structured errors.
  </behavior>
  <action>Add RED tests named `disk-backed and memory-backed BA2 DDS inputs read back equivalently`, `BA2 DX10 chunk compression routes raw deflate and Starfield LZ4-block`, `BA2 DX10 dedup shares exact post-policy chunks`, and `BA2 DDS writer rejects malformed unsupported and unsafe inputs`. Use custom failing sink coverage to prove `finalize_ba2_write` returns the original sink error. Do not implement behavior in this task.</action>
  <verify>
    <automated>cmake --build build/windows-vs2026-vcpkg --config Debug --target libbsa_ba2_writer_tests && ctest --test-dir build/windows-vs2026-vcpkg --output-on-failure -C Debug -R libbsa_ba2_writer_tests</automated>
  </verify>
  <acceptance_criteria>Focused CTest fails before GREEN on missing DDS disk/compression/dedup/failure behavior; `rg -n "disk-backed and memory-backed BA2 DDS|DX10 chunk compression|DX10 dedup|rejects malformed unsupported|sink failure" tests/ba2_writer_tests.cpp` finds all RED cases.</acceptance_criteria>
  <done>Failing tests lock DDS disk, codec, dedup, malformed input, and finalization failure behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 GREEN: Implement DDS disk planning, chunk codecs, dedup, and safety failures</name>
  <files>src/ba2_writer.cpp, src/texture/dds_analysis.cpp, tests/ba2_writer_tests.cpp</files>
  <read_first>src/ba2_writer.cpp, src/texture/dds_analysis.cpp, tests/ba2_writer_tests.cpp, include/libbsa/ba2_writer.hpp, src/compression.cpp, src/bsa_writer.cpp, .planning/phases/10-ba2-writers/10-RESEARCH.md</read_first>
  <action>Implement `plan_ba2_dds_write_from_disk` with pre-I/O archive-path validation and planning-time disk reads. Extend DX10 planning to resolve per-chunk compression using `resolve_write_compression`, `resolve_payload_codec`, and `compress_payload`; for raw chunks write native `packed_size == size`, for compressed chunks write native compressed size and original chunk size. Support Starfield v3 method 0 and method 3 only. Deduplicate exact post-policy stored chunk bytes when enabled and expose shared stable data-region IDs and archive-absolute offsets. Return structured failures for malformed or unsupported DDS from `detail::analyze_dds`, impossible mip/chunk mapping, overflow/narrowing, duplicate paths, missing files, unsupported methods, and sink write failures.</action>
  <verify>
    <automated>cmake --build build/windows-vs2026-vcpkg --config Debug --target libbsa_ba2_writer_tests && ctest --test-dir build/windows-vs2026-vcpkg --output-on-failure -C Debug -R "libbsa_ba2_writer_tests|libbsa_ba2_dds_reader_tests|libbsa_compression_policy_tests"</automated>
  </verify>
  <acceptance_criteria>Focused/regression CTest passes; `rg -n "plan_ba2_dds_write_from_disk|packed_size == size|resolve_payload_codec|lz4_block|deduplicate|DDS analysis failed|unsupported" src/ba2_writer.cpp src/texture/dds_analysis.cpp` finds implementation; `rg -n "BA2 DDS writer planning is not implemented" src/ba2_writer.cpp` returns no matches.</acceptance_criteria>
  <done>DDS writer fully satisfies WRT-03 for disk/memory inputs, chunk compression metadata, dedup offsets, malformed DDS rejection, and sink failure handling.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host DDS file → planner | Disk DDS data can be missing, malformed, or mutate after planning. |
| DDS chunks → codec dispatcher | Wrong packed-size semantics or codec route corrupts texture output. |
| malformed DDS → plan result | Unsupported inputs must fail without a partial plan or finalizer side effects. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-13 | Tampering | DDS disk planning | mitigate | Validate archive paths before host reads; read DDS bytes during planning; finalization uses plan-owned chunks only. |
| T-10-14 | Tampering / DoS | DX10 chunk codec routing | mitigate | Encode raw chunks with `packed_size == size`, compressed chunks with explicit deflate/LZ4-block state, and reject unsupported methods. |
| T-10-15 | Tampering / DoS | malformed DDS inputs | mitigate | Analyzer and planner return structured errors for malformed/unsupported/unrepresentable DDS with no partial plan. |
</threat_model>

<verification>
Run BA2 writer, BA2 DDS reader, compression policy, public-header private-token grep, and `git status --short TES5Edit`.
</verification>

<success_criteria>
Plan 05 is complete when DDS disk/memory, compression, dedup, malformed input, and finalization failure tests all pass and no DDS placeholder remains.
</success_criteria>

<output>
After completion, create `.planning/phases/10-ba2-writers/10-05-SUMMARY.md`.
</output>
