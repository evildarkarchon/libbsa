---
phase: 10-ba2-writers
plan: 06
type: execute
wave: 6
depends_on: [10-05]
files_modified:
  - tests/public_header_smoke.cpp
  - README.md
  - .planning/phases/10-ba2-writers/10-VALIDATION.md
autonomous: true
requirements: [WRT-02, WRT-03]
must_haves:
  truths:
    - "Public consumers can create, finalize, reopen, and extract one FO4 and one Starfield BA2 GNRL archive using only public headers."
    - "Public consumers can create, finalize, reopen, and extract one FO4 and one Starfield BA2 DDS archive using only public headers."
    - "Documentation claims BA2 writer support only within Phase 10 boundaries and leaves corpus/performance/CLI/in-place work deferred."
    - "Final gates prove tests, public-header leakage checks, and TES5Edit read-only boundary."
  artifacts:
    - path: "tests/public_header_smoke.cpp"
      provides: "Consumer-style BA2 writer public smoke"
      contains: "ba2_writer_smoke_ok"
    - path: "README.md"
      provides: "Phase 10 BA2 writer support documentation"
      contains: "BA2 writer"
    - path: ".planning/phases/10-ba2-writers/10-VALIDATION.md"
      provides: "Nyquist sign-off and gate evidence"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "tests/public_header_smoke.cpp"
      to: "include/libbsa/ba2_writer.hpp"
      via: "public-only include and function-pointer/runtime usage"
      pattern: "ba2_writer.hpp"
---

<objective>
Finalize Phase 10 public smoke, documentation, validation sign-off, and repository boundary gates.

Purpose: Prove BA2 writer APIs are usable by consumers through public headers and document exact support/deferred boundaries before verification.
Output: Public smoke extension, README BA2 writer section, validation metadata updated to green, and final automated gate evidence.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-ba2-writers/10-CONTEXT.md
@.planning/phases/10-ba2-writers/10-SPEC.md
@.planning/phases/10-ba2-writers/10-VALIDATION.md
@tests/public_header_smoke.cpp
@README.md
@include/libbsa/ba2_writer.hpp
@.planning/phases/09-bsa-writers/09-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend public-header smoke for BA2 writers</name>
  <files>tests/public_header_smoke.cpp</files>
  <read_first>tests/public_header_smoke.cpp, include/libbsa/ba2_writer.hpp, include/libbsa/ba2.hpp, tests/ba2_writer_tests.cpp, .planning/phases/10-ba2-writers/10-SPEC.md</read_first>
  <action>Add `#include <libbsa/ba2_writer.hpp>`. Add consumer-style helper(s) that create/finalize/reopen/extract one Fallout 4 GNRL archive, one Starfield GNRL archive, one Fallout 4 DX10 archive, and one Starfield DX10 archive using only public libbsa headers and `memory_source`/`memory_sink`. Construct real `ba2_gnrl_memory_entry` and `ba2_dds_memory_entry` values, call `plan_ba2_gnrl_write`, `plan_ba2_dds_write`, and `finalize_ba2_write`, reopen with `open_ba2`, and extract with `extract_ba2_entry`. Add public function-pointer references for `plan_ba2_gnrl_write`, `plan_ba2_gnrl_write_from_disk`, `plan_ba2_dds_write`, `plan_ba2_dds_write_from_disk`, and `finalize_ba2_write`.</action>
  <verify>
    <automated>cmake --build build/windows-vs2026-vcpkg --config Debug --target libbsa_public_header_smoke && ctest --test-dir build/windows-vs2026-vcpkg --output-on-failure -C Debug -R libbsa.public_header_smoke</automated>
  </verify>
  <acceptance_criteria>`rg -n "ba2_writer.hpp|plan_ba2_gnrl_write|plan_ba2_gnrl_write_from_disk|plan_ba2_dds_write|plan_ba2_dds_write_from_disk|finalize_ba2_write|ba2_writer_smoke" tests/public_header_smoke.cpp` finds public smoke coverage; public smoke CTest passes.</acceptance_criteria>
  <done>Consumer-style public smoke proves BA2 writer compile/link/runtime use without private headers.</done>
</task>

<task type="auto">
  <name>Task 2: Document BA2 writer support and update validation sign-off</name>
  <files>README.md, .planning/phases/10-ba2-writers/10-VALIDATION.md</files>
  <read_first>README.md, .planning/phases/10-ba2-writers/10-VALIDATION.md, .planning/phases/10-ba2-writers/10-SPEC.md, .planning/phases/10-ba2-writers/10-CONTEXT.md, .planning/phases/09-bsa-writers/09-06-SUMMARY.md</read_first>
  <action>Update README with a Phase 10 BA2 writer support section that names exact GNRL targets `fallout4_gnrl_v1/v7/v8`, `starfield_gnrl_v2/v3`, DX10 targets `fallout4_dx10_v1/v7/v8`, `starfield_dx10_v3`, memory/disk GNRL inputs, memory/disk DDS inputs, plan-owned bytes, caller-owned `byte_sink` finalization, raw/deflate/Starfield method-3 LZ4-block routes, opt-in dedup, and read-after-write validation. Explicitly state that external corpus comparison, broad malformed archive hardening, texture transcoding/optimization, CLI/GUI, performance/multithreading, and true in-place mutation are out of Phase 10. Update `10-VALIDATION.md` frontmatter to `nyquist_compliant: true` and `wave_0_complete: true`, mark task rows complete, and record exact gate commands/results after running them.</action>
  <verify>
    <automated>ctest --test-dir build/windows-vs2026-vcpkg --output-on-failure -C Debug -R "libbsa_ba2_writer_tests|libbsa_ba2_reader_tests|libbsa_ba2_dds_reader_tests|libbsa_writer_tests|libbsa_bsa_writer_tests|libbsa.public_header_smoke"</automated>
  </verify>
  <acceptance_criteria>`rg -n "BA2 writer|fallout4_gnrl_v1|starfield_gnrl_v3|fallout4_dx10_v1|starfield_dx10_v3|method-3|dedup|external corpus|in-place" README.md` finds support and boundary text; `rg -n "nyquist_compliant: true|wave_0_complete: true|complete|PASS" .planning/phases/10-ba2-writers/10-VALIDATION.md` finds sign-off updates.</acceptance_criteria>
  <done>README and validation accurately describe implemented BA2 writer support and Phase 10 boundaries.</done>
</task>

<task type="auto">
  <name>Task 3: Run final Phase 10 gates and boundary checks</name>
  <files>.planning/phases/10-ba2-writers/10-VALIDATION.md</files>
  <read_first>.planning/phases/10-ba2-writers/10-VALIDATION.md, .planning/phases/10-ba2-writers/10-SPEC.md, CMakeLists.txt, include/libbsa/ba2_writer.hpp</read_first>
  <action>Run and record final gates: focused BA2 writer/readers/smoke lane, full CTest, public-header private-token grep, CMake no-glob grep, stale placeholder grep for BA2 writer not-implemented strings, and `git status --short TES5Edit`. Update `10-VALIDATION.md` with PASS/FAIL evidence only after commands run; if any gate fails, fix the issue in the relevant source/test/docs file before marking PASS.</action>
  <verify>
    <automated>ctest --test-dir build/windows-vs2026-vcpkg --output-on-failure -C Debug; rg -n "DirectXTex|DXGI_FORMAT|Windows\.h|libdeflate|TES5Edit|lz4\.h|lz4frame\.h|LZ4_" include/libbsa; rg -n "^[^#]*\b(GLOB|GLOB_RECURSE)\b" CMakeLists.txt; git status --short TES5Edit</automated>
  </verify>
  <acceptance_criteria>Full CTest exits 0; public-header private-token grep has no matches; CMake glob grep has no matches; `rg -n "BA2 (GNRL|DDS) writer planning is not implemented|BA2 writer finalization is not implemented" src/ba2_writer.cpp tests/ba2_writer_tests.cpp tests/public_header_smoke.cpp` has no active placeholder matches; `git status --short TES5Edit` is empty.</acceptance_criteria>
  <done>Final Phase 10 validation evidence proves production BA2 writer support and repository boundaries are clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public consumers → BA2 writer API | Smoke must use only public libbsa types and avoid dependency leakage. |
| documentation → consumer expectations | README must not claim Phase 11/12 work or unsupported texture transforms. |
| repo → TES5Edit submodule | Final gates must prove no submodule mutation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-16 | Information Disclosure | public smoke/header boundary | mitigate | Compile smoke through public headers only and run private-token grep over `include/libbsa`. |
| T-10-17 | Repudiation | README/validation claims | mitigate | Document exact supported targets and exact deferred boundaries from SPEC; record command evidence in validation. |
| T-10-18 | Tampering | TES5Edit boundary | mitigate | Run `git status --short TES5Edit` and keep CMake/source lists free of TES5Edit paths. |
</threat_model>

<verification>
Focused regression lane, full CTest, public-header grep, CMake glob grep, placeholder grep, and TES5Edit status gate must pass.
</verification>

<success_criteria>
Plan 06 is complete when public smoke, README, validation metadata, and final gates prove Phase 10 BA2 writer support with no public dependency leakage or TES5Edit mutation.
</success_criteria>

<output>
After completion, create `.planning/phases/10-ba2-writers/10-06-SUMMARY.md`.
</output>
