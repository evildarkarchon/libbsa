---
phase: 10-ba2-writers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - include/libbsa/ba2_writer.hpp
  - src/ba2_writer.cpp
  - tests/ba2_writer_tests.cpp
  - CMakeLists.txt
autonomous: true
requirements: [WRT-02, WRT-03]
must_haves:
  truths:
    - "D-01: Consumer can discover BA2 writing through a dedicated public header."
    - "D-05: Consumer can name every required BA2 GNRL and DX10 target with variant-safe values."
    - "D-04: BA2 writer planning and finalization entry points exist behind structured placeholders for downstream TDD plans."
  artifacts:
    - path: "include/libbsa/ba2_writer.hpp"
      provides: "Public BA2 writer target, option, input, preview, plan, and finalize declarations"
      contains: "enum class ba2_write_target"
    - path: "src/ba2_writer.cpp"
      provides: "Linkable BA2 writer placeholder implementation"
      contains: "plan_ba2_gnrl_write"
    - path: "tests/ba2_writer_tests.cpp"
      provides: "Focused BA2 writer Catch2 target seed"
      contains: "[ba2-writer]"
  key_links:
    - from: "include/libbsa/ba2_writer.hpp"
      to: "src/ba2_writer.cpp"
      via: "declared public function names implemented with result<T> placeholders"
      pattern: "plan_ba2_(gnrl|dds)_write"
    - from: "CMakeLists.txt"
      to: "tests/ba2_writer_tests.cpp"
      via: "libbsa_ba2_writer_tests target"
      pattern: "libbsa_ba2_writer_tests"
---

<objective>
Create the BA2 writer public API seam and focused test/build target required by D-01 through D-12 without claiming native writer behavior yet.

Purpose: Downstream TDD plans need stable public type/function names, exact target values, and preview structures before implementing GNRL and DDS behavior.
Output: `ba2_writer.hpp`, linkable placeholder functions in `src/ba2_writer.cpp`, a focused `libbsa_ba2_writer_tests` target, and explicit CMake wiring.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-ba2-writers/10-CONTEXT.md
@.planning/phases/10-ba2-writers/10-SPEC.md
@.planning/phases/10-ba2-writers/10-RESEARCH.md
@.planning/phases/10-ba2-writers/10-PATTERNS.md
@include/libbsa/bsa_writer.hpp
@include/libbsa/ba2.hpp
@include/libbsa/compression.hpp
@include/libbsa/io.hpp
@src/bsa_writer.cpp
@CMakeLists.txt

<decision_coverage>
This foundation plan intentionally covers the public BA2 writer contract decisions: D-01: dedicated BA2 writer header; D-02: separate memory-backed and disk-backed input value types; D-03: subtype-specific GNRL and DDS input types; D-04: plan/finalize operation pairs; D-05: exact variant-safe target values; D-06: archive-level Starfield v3 compression method option; D-07: explicit compression policy controls; D-08: native target fields in write plans; D-09: subtype-specific preview sections; D-10: GNRL native preview detail; D-11: DDS/DX10 texture and chunk preview detail; D-12: digest-neutral dedup preview with stable region IDs and archive-absolute offsets.
</decision_coverage>

<interfaces>
Create public API names exactly as planned here so later plans do not invent competing seams:
- `enum class ba2_write_target { fallout4_gnrl_v1, fallout4_gnrl_v7, fallout4_gnrl_v8, starfield_gnrl_v2, starfield_gnrl_v3, fallout4_dx10_v1, fallout4_dx10_v7, fallout4_dx10_v8, starfield_dx10_v3 };`
- `struct ba2_write_options { bool archive_default_compressed{}; bool deduplicate{}; std::uint32_t starfield_v3_compression_method{0}; };`
- `struct ba2_gnrl_memory_entry { std::string path; std::vector<std::byte> payload; compression_policy compression{compression_policy::archive_default}; };`
- `struct ba2_gnrl_disk_entry { std::string host_path; std::string path; compression_policy compression{compression_policy::archive_default}; };`
- `struct ba2_dds_memory_entry { std::string path; std::vector<std::byte> dds_bytes; compression_policy compression{compression_policy::archive_default}; };`
- `struct ba2_dds_disk_entry { std::string host_path; std::string path; compression_policy compression{compression_policy::archive_default}; };`
- Plan preview structs must include native target fields (`subtype`, `version`, `header_size`, `compression_method`), subtype sections (`gnrl`, `dds`), table/data regions, stable `data_region_id`, archive-absolute offsets, and plan-owned `table_bytes`/`data_regions`.
- Functions: `plan_ba2_gnrl_write`, `plan_ba2_gnrl_write_from_disk`, `plan_ba2_dds_write`, `plan_ba2_dds_write_from_disk`, `finalize_ba2_write`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dedicated BA2 writer public contracts</name>
  <files>include/libbsa/ba2_writer.hpp</files>
  <read_first>include/libbsa/ba2_writer.hpp if it exists, include/libbsa/bsa_writer.hpp, include/libbsa/ba2.hpp, include/libbsa/compression.hpp, include/libbsa/io.hpp, .planning/phases/10-ba2-writers/10-CONTEXT.md, .planning/phases/10-ba2-writers/10-SPEC.md, .planning/phases/10-ba2-writers/10-PATTERNS.md</read_first>
  <action>Create `include/libbsa/ba2_writer.hpp` with Doxygen comments for all public types and functions. Implement D-01 with a dedicated header, D-02 with separate memory/disk GNRL input types, D-03 with separate DDS input types, D-04 with plan/finalize declarations, D-05 with exact `ba2_write_target` values listed in this plan, D-06 with archive-level `starfield_v3_compression_method`, D-07 with explicit `compression_policy` on each entry/chunk source, D-08 through D-12 with native target, GNRL preview, DDS preview, table/data region, stable region ID, and archive-absolute offset fields. Do not include DirectXTex, Windows, libdeflate, lz4, TES5Edit, or private headers.</action>
  <verify>
    <automated>cmake --build build/windows-vs2026-vcpkg --config Debug --target libbsa_public_header_smoke</automated>
  </verify>
  <acceptance_criteria>`rg -n "enum class ba2_write_target|ba2_gnrl_memory_entry|ba2_gnrl_disk_entry|ba2_dds_memory_entry|ba2_dds_disk_entry|ba2_write_plan|plan_ba2_gnrl_write|plan_ba2_dds_write|finalize_ba2_write" include/libbsa/ba2_writer.hpp` finds all names; `rg -n "DirectXTex|DXGI_FORMAT|Windows\.h|libdeflate|lz4|TES5Edit" include/libbsa/ba2_writer.hpp` returns no matches.</acceptance_criteria>
  <done>Public BA2 writer contracts compile and expose all locked decisions D-01 through D-12 without private dependency leakage.</done>
</task>

<task type="auto">
  <name>Task 2: Wire placeholder implementation and focused test target</name>
  <files>src/ba2_writer.cpp, tests/ba2_writer_tests.cpp, CMakeLists.txt</files>
  <read_first>src/ba2_writer.cpp if it exists, tests/ba2_writer_tests.cpp if it exists, CMakeLists.txt, include/libbsa/ba2_writer.hpp, src/bsa_writer.cpp, tests/bsa_writer_tests.cpp, .planning/phases/10-ba2-writers/10-VALIDATION.md</read_first>
  <action>Add `src/ba2_writer.cpp` implementing the five public functions as structured `error_code::unsupported_format` placeholders with messages containing `BA2 GNRL writer planning is not implemented`, `BA2 DDS writer planning is not implemented`, and `BA2 writer finalization is not implemented`. Add `tests/ba2_writer_tests.cpp` with Catch2 tests that include `<libbsa/ba2_writer.hpp>`, construct every target enum value, verify placeholder failures, and tag tests `[unit][ba2-writer]`. Update `CMakeLists.txt` to list `include/libbsa/ba2_writer.hpp` in the public header file set, `src/ba2_writer.cpp` in private sources, and create `libbsa_ba2_writer_tests` linked to `libbsa::libbsa` and `Catch2::Catch2WithMain` with labels `unit;fixture;codec;roundtrip`.</action>
  <verify>
    <automated>cmake --build build/windows-vs2026-vcpkg --config Debug --target libbsa_ba2_writer_tests && ctest --test-dir build/windows-vs2026-vcpkg --output-on-failure -C Debug -R libbsa_ba2_writer_tests</automated>
  </verify>
  <acceptance_criteria>`rg -n "src/ba2_writer.cpp|include/libbsa/ba2_writer.hpp|libbsa_ba2_writer_tests|tests/ba2_writer_tests.cpp" CMakeLists.txt` finds all wiring; `rg -n "BA2 GNRL writer planning is not implemented|BA2 DDS writer planning is not implemented|BA2 writer finalization is not implemented" src/ba2_writer.cpp tests/ba2_writer_tests.cpp` finds placeholder contract text.</acceptance_criteria>
  <done>BA2 writer symbols link, focused tests run, and later plans can replace placeholders behind stable public names.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public header → consumers | Consumer code can compile only libbsa-owned BA2 writer contracts; private dependency leakage would become an API boundary flaw. |
| caller inputs → planning placeholders | Entry values are accepted but not yet interpreted; placeholder functions must fail structurally without touching sinks or files. |
| repo → TES5Edit submodule | Reference material is read-only and must not enter source lists. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Information Disclosure | `include/libbsa/ba2_writer.hpp` | mitigate | Keep DirectXTex, DXGI, Windows, libdeflate, LZ4, Delphi, and TES5Edit tokens out of public headers; verify with grep. |
| T-10-02 | Tampering | `CMakeLists.txt` | mitigate | Add only `src/ba2_writer.cpp` and public header/test files; never add `TES5Edit/` paths. |
| T-10-03 | Repudiation | placeholder functions | mitigate | Return structured `unsupported_format` errors with stable messages until behavior is implemented by TDD plans. |
</threat_model>

<verification>
Run focused build/test for `libbsa_ba2_writer_tests`, public-header smoke build, public-header private-token grep, CMake glob grep, and `git status --short TES5Edit`.
</verification>

<success_criteria>
Plan 01 is complete when BA2 writer public contracts, placeholder implementation, focused test target, and CMake wiring exist and all placeholder tests pass without public dependency leakage.
</success_criteria>

<output>
After completion, create `.planning/phases/10-ba2-writers/10-01-SUMMARY.md`.
</output>
