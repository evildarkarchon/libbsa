---
phase: 10-ba2-writers
plan: 08
type: execute
wave: 8
depends_on: ["10-07"]
files_modified:
  - src/ba2_writer.cpp
  - src/texture/dds_analysis.cpp
  - tests/ba2_writer_tests.cpp
autonomous: true
gap_closure: true
requirements: [WRT-02, WRT-03]
must_haves:
  truths:
    - "Consumer can create BA2 GNRL and DDS archives whose native record extension fields omit the leading dot and use NUL padding/truncation."
    - "Multi-mip DDS arrays and multi-mip cubemaps no longer silently round-trip with reordered payload bytes."
    - "Cubemap arrays no longer silently collapse to a single-cube native DX10 field during BA2 DDS planning."
    - "BA2 writer tests cover native extension bytes, unsafe multi-mip array/cubemap layouts, and cubemap-array structural rejection."
  artifacts:
    - path: "src/ba2_writer.cpp"
      provides: "BA2 native GNRL/DX10 extension-field serialization without leading separators"
      contains: "dot + 1U"
    - path: "src/texture/dds_analysis.cpp"
      provides: "Planning-time structural rejection for DDS layouts that current BA2 writer/extractor cannot preserve"
      contains: "unsupported cubemap array"
    - path: "tests/ba2_writer_tests.cpp"
      provides: "Regression coverage for extension bytes and unsafe DDS layout rejection"
      contains: "BA2 native record extension fields omit the dot separator"
  key_links:
    - from: "src/ba2_writer.cpp"
      to: "tests/ba2_writer_tests.cpp"
      via: "native table byte assertions inspect the four extension bytes emitted by append_extension4"
      pattern: "nif\\0"
    - from: "src/texture/dds_analysis.cpp"
      to: "src/ba2_writer.cpp"
      via: "detail::analyze_dds fails before plan_ba2_dds_write can serialize unsupported DDS layouts"
      pattern: "unsupported.*cubemap"
    - from: "tests/ba2_writer_tests.cpp"
      to: "open_ba2/extract_ba2_entry"
      via: "existing round-trip tests remain passing after extension and DDS safety fixes"
      pattern: "extract_ba2_bytes"
---

<objective>
Close the remaining Phase 10 BA2 writer verification gaps after Plan 10-07.

Purpose: WRT-02 remains blocked by native BA2 extension fields serialized as `.nif`/`.dds` instead of `nif\0`/`dds\0`, and WRT-03 remains blocked because accepted multi-mip DDS arrays/cubemaps reorder image bytes while cubemap arrays collapse to a single-cube native field. This gap-closure plan fixes the native extension field and turns currently unsafe DDS layouts into structured planning failures instead of silent corruption.

Output: Focused BA2 writer source fixes and regression tests proving native extension bytes, multi-mip array/cubemap rejection, cubemap-array rejection, and unchanged successful read-after-write behavior for supported layouts.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-ba2-writers/10-CONTEXT.md
@.planning/phases/10-ba2-writers/10-RESEARCH.md
@.planning/phases/10-ba2-writers/10-VERIFICATION.md
@.planning/phases/10-ba2-writers/10-05-SUMMARY.md
@.planning/phases/10-ba2-writers/10-07-SUMMARY.md

<interfaces>
Existing contracts the executor must preserve:

From `src/ba2_writer.cpp`:
```cpp
void append_extension4(std::vector<std::byte>& bytes, std::string_view path);
result<ba2_write_plan> plan_ba2_gnrl_write(ba2_write_target target,
                                           std::span<const ba2_gnrl_memory_entry> entries,
                                           ba2_write_options options);
result<ba2_write_plan> plan_ba2_dds_write(ba2_write_target target,
                                          std::span<const ba2_dds_memory_entry> entries,
                                          ba2_write_options options);
```

From `src/texture/dds_analysis.cpp` / `src/texture/dds_analysis.hpp`:
```cpp
struct analyzed_dds_texture {
    dxgi_format format{};
    std::uint32_t width{};
    std::uint32_t height{};
    std::uint32_t mip_count{};
    std::uint32_t array_size{};
    bool is_cubemap{};
    std::vector<analyzed_dds_chunk> chunks;
};

[[nodiscard]] result<analyzed_dds_texture> analyze_dds(std::span<const std::byte> dds_bytes);
```

From `tests/ba2_writer_tests.cpp` helpers:
```cpp
std::vector<std::byte> generated_dx10_dds(std::uint32_t format,
                                          std::uint32_t width,
                                          std::uint32_t height,
                                          std::uint32_t mip_count,
                                          std::uint32_t array_size,
                                          bool cubemap,
                                          std::byte seed);
std::vector<std::byte> finalize_to_bytes(const libbsa::ba2_write_plan& plan);
std::vector<std::byte> extract_ba2_bytes(const std::vector<std::byte>& bytes, std::string path);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing BA2 native and DDS safety regression tests</name>
  <files>tests/ba2_writer_tests.cpp</files>
  <read_first>tests/ba2_writer_tests.cpp, .planning/phases/10-ba2-writers/10-VERIFICATION.md, .planning/phases/10-ba2-writers/10-07-SUMMARY.md</read_first>
  <behavior>
    - Test 1: `BA2 native record extension fields omit the dot separator` creates one GNRL archive containing `meshes/native-extension.nif` and one DX10 archive containing `textures/native-extension.dds`, finalizes both, and asserts the four extension bytes at each first native record are exactly `{'n','i','f','\0'}` and `{'d','d','s','\0'}`; add one longer-extension case such as `scripts/native-extension.pscbackup` and assert the four bytes are `{'p','s','c','b'}`.
    - Test 2: `BA2 DDS writer rejects multi-mip arrays and cubemaps until payload ordering is representable` passes generated DDS inputs with `mip_count = 2`, `array_size = 2`, `cubemap = false` and with `mip_count = 2`, `array_size = 6`, `cubemap = true`; each `plan_ba2_dds_write(fallout4_dx10_v8, ...)` returns `error_code::unsupported_format`.
    - Test 3: `BA2 DDS writer rejects cubemap arrays instead of collapsing cube count` passes generated cubemap-array DDS bytes with `mip_count = 1`, `array_size = 12`, `cubemap = true`; `plan_ba2_dds_write(fallout4_dx10_v8, ...)` returns `error_code::unsupported_format`.
  </behavior>
  <action>
    Add RED coverage for every remaining actionable gap in `10-VERIFICATION.md`:
    - For native extension bytes, inspect finalized table bytes directly rather than relying on `open_ba2`, because the current reader ignores the native extension field. Use current header sizes and record offsets: first GNRL record extension bytes start at offset `expected_header_size(target) + 4`, and first DX10 record extension bytes start at offset `expected_header_size(target) + 4`.
    - Assert exact byte values with `std::to_integer<unsigned char>(bytes[offset + n])`, not string comparisons over possibly NUL-padded memory.
    - For long-extension truncation, add a GNRL entry path ending in `.pscbackup` or equivalent and assert the native field contains only the first four extension text bytes after the dot.
    - For unsafe DDS layouts, use actual DDS bytes from `generated_dx10_dds` per D-13/D-14/D-16; do not add public native descriptors or manual BA2 texture records.
    - Keep existing successful one-mip array/cubemap and single-item multi-mip tests unchanged so the regression coverage proves only unsafe combinations are rejected.
  </action>
  <verify>
    <automated>cmake --build build/windows-vs2026-vcpkg --config Debug --target libbsa_ba2_writer_tests; ctest --test-dir build/windows-vs2026-vcpkg --output-on-failure -C Debug -R libbsa_ba2_writer_tests</automated>
  </verify>
  <acceptance_criteria>
    - `tests/ba2_writer_tests.cpp` contains test name `BA2 native record extension fields omit the dot separator`.
    - `tests/ba2_writer_tests.cpp` contains test name `BA2 DDS writer rejects multi-mip arrays and cubemaps until payload ordering is representable`.
    - `tests/ba2_writer_tests.cpp` contains test name `BA2 DDS writer rejects cubemap arrays instead of collapsing cube count`.
    - Before implementation tasks, the focused `libbsa_ba2_writer_tests` command fails on the new expectations; after implementation tasks, the same command passes.
  </acceptance_criteria>
  <done>Failing tests prove the current native extension-field corruption, multi-mip array/cubemap ordering hazard, and cubemap-array collapse hazard.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Correct BA2 native extension-field serialization</name>
  <files>src/ba2_writer.cpp, tests/ba2_writer_tests.cpp</files>
  <read_first>src/ba2_writer.cpp, tests/ba2_writer_tests.cpp, .planning/phases/10-ba2-writers/10-VERIFICATION.md</read_first>
  <behavior>
    - `append_extension4` writes extension text beginning after the final dot, never the dot itself.
    - Extension fields are exactly four bytes, NUL-padded when shorter than four bytes and truncated when longer than four bytes.
    - Both GNRL and DX10 serialization paths use the corrected helper because they already call `append_extension4`.
  </behavior>
  <action>
    In `src/ba2_writer.cpp`, update `append_extension4` only:
    - Current incorrect code derives `extension = path.substr(dot, min(4, path.size() - dot))` and therefore includes the dot.
    - Replace it with logic equivalent to: if there is no dot or the dot is the final character, use an empty extension; otherwise set `extension_start = dot + 1U` and take `path.substr(extension_start, std::min<std::size_t>(4U, path.size() - extension_start))`.
    - Preserve the loop that emits exactly four bytes and NUL-pads missing bytes.
    - Add a short WHY comment near the helper explaining that native BA2 records store extension text without the separator, while archive paths still keep normal dotted filenames.
    - Do not change GNRL `PackedSize == 0` raw semantics, DX10 `PackedSize == Size` raw semantics, hashes, offsets, file-table names, compression routing, or dedup behavior.
  </action>
  <verify>
    <automated>cmake --build build/windows-vs2026-vcpkg --config Debug --target libbsa_ba2_writer_tests; ctest --test-dir build/windows-vs2026-vcpkg --output-on-failure -C Debug -R libbsa_ba2_writer_tests</automated>
  </verify>
  <acceptance_criteria>
    - `src/ba2_writer.cpp` contains `dot + 1U` or `extension_start` inside `append_extension4`.
    - `src/ba2_writer.cpp` does not contain `path.substr(dot, std::min`.
    - `tests/ba2_writer_tests.cpp` native extension test asserts `nif`, `dds`, and a four-byte truncation case without leading `.`.
    - Focused BA2 writer tests pass the native extension-field regression while preserving existing GNRL and DX10 read-after-write tests.
  </acceptance_criteria>
  <done>Native BA2 GNRL and DX10 record extension fields serialize as separator-free four-byte extension text, closing the WRT-02 native table layout gap.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Reject unsafe DDS array and cubemap-array layouts during planning</name>
  <files>src/texture/dds_analysis.cpp, tests/ba2_writer_tests.cpp</files>
  <read_first>src/texture/dds_analysis.cpp, src/texture/dds_reconstruction.cpp, tests/ba2_writer_tests.cpp, .planning/phases/10-ba2-writers/10-VERIFICATION.md</read_first>
  <behavior>
    - `detail::analyze_dds` returns `unsupported_format` for any DDS texture where `metadata.arraySize > 1` and `metadata.mipLevels > 1`, because the current BA2 extraction/reconstruction surface cannot preserve item-major DDS payload order for multi-mip arrays/cubemaps without additional per-chunk mip-range metadata.
    - `detail::analyze_dds` returns `unsupported_format` for cubemap DDS inputs where DirectXTex reports an array size other than six faces, because current DX10 writer serialization cannot represent cubemap arrays without losing cube count.
    - Existing supported layouts remain accepted: single-item multi-mip textures, one-mip texture arrays, and one-cube cubemaps.
  </behavior>
  <action>
    In `src/texture/dds_analysis.cpp`, add structural DDS layout rejection after successful DirectXTex load and before chunk payload construction:
    - If `metadata.arraySize > 1 && metadata.mipLevels > 1`, return `failure<analyzed_dds_texture>({error_code::unsupported_format, "DDS analysis failed: unsupported multi-mip array layout"})`.
    - If `metadata.IsCubemap() && metadata.arraySize != 6`, return `failure<analyzed_dds_texture>({error_code::unsupported_format, "DDS analysis failed: unsupported cubemap array"})`.
    - Add a short WHY comment explaining that current BA2 extraction reconstructs DDS payloads by concatenating chunks, so multi-mip arrays/cubemaps would require explicit safe reordering support before they can be accepted without corruption.
    - Keep existing `supported_dds_writer_format` allowlist, DirectXTex-private boundary, metadata narrowing checks, one-mip array support, one-cube cubemap support, and single-item multi-mip chunking unchanged.

    In `tests/ba2_writer_tests.cpp`, ensure the RED tests from Task 1 pass and existing tests still prove successful read-after-write for `multi_mip.dds` with `array_size = 1`, `cubemap.dds` with `mip_count = 1` and `array_size = 6`, and `array.dds` with `mip_count = 1` and `array_size = 4`.
  </action>
  <verify>
    <automated>cmake --build build/windows-vs2026-vcpkg --config Debug --target libbsa_ba2_writer_tests libbsa_public_header_smoke; ctest --test-dir build/windows-vs2026-vcpkg --output-on-failure -C Debug -R "libbsa_ba2_writer_tests|libbsa_ba2_dds_reader_tests|libbsa.public_header_smoke"; rg -n "DirectXTex|DXGI_FORMAT|Windows\.h|libdeflate|TES5Edit|lz4\.h|lz4frame\.h|LZ4_" include/libbsa; if ($LASTEXITCODE -eq 0) { exit 1 } else { exit 0 }; git status --short TES5Edit</automated>
  </verify>
  <acceptance_criteria>
    - `src/texture/dds_analysis.cpp` contains `unsupported multi-mip array layout`.
    - `src/texture/dds_analysis.cpp` contains `unsupported cubemap array`.
    - `tests/ba2_writer_tests.cpp` contains assertions that the multi-mip array, multi-mip cubemap, and cubemap-array plans fail with `error_code::unsupported_format`.
    - Focused BA2 writer/DDS reader/public-smoke CTest command passes.
    - Public header leakage grep returns no matches; `git status --short TES5Edit` prints no output.
  </acceptance_criteria>
  <done>Unsafe DDS layouts fail structurally during planning with no partial plan, while currently supported one-axis DDS layouts continue to round-trip.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller archive paths -> native BA2 table bytes | User-provided paths are normalized then serialized into native record fields that external tools may inspect. |
| caller DDS bytes -> DirectXTex analysis | Untrusted DDS memory/disk bytes are parsed during planning and converted into writer-owned metadata/chunk bytes. |
| writer output -> reader/extractor | Emitted native BA2 metadata controls extraction offsets, sizes, and DDS reconstruction order. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-08-01 | T | `src/ba2_writer.cpp::append_extension4` | mitigate | Serialize separator-free four-byte native extension fields and assert exact native bytes in tests. |
| T-10-08-02 | T | `src/texture/dds_analysis.cpp` DDS layout acceptance | mitigate | Return structured `unsupported_format` for multi-mip array/cubemap layouts that current reconstruction cannot preserve. |
| T-10-08-03 | T | `src/ba2_writer.cpp` DX10 cubemap native field | mitigate | Reject cubemap arrays during DDS analysis before writer serialization can collapse cube count. |
| T-10-08-04 | E | public headers / TES5Edit boundary | mitigate | Run public-header dependency leakage grep and `git status --short TES5Edit` after implementation. |
</threat_model>

<verification>
Run focused gap-closure verification:

```powershell
cmake --build build/windows-vs2026-vcpkg --config Debug --target libbsa_ba2_writer_tests libbsa_public_header_smoke
ctest --test-dir build/windows-vs2026-vcpkg --output-on-failure -C Debug -R "libbsa_ba2_writer_tests|libbsa_ba2_dds_reader_tests|libbsa.public_header_smoke"
rg -n "DirectXTex|DXGI_FORMAT|Windows\.h|libdeflate|TES5Edit|lz4\.h|lz4frame\.h|LZ4_" include/libbsa
git status --short TES5Edit
```

The `rg` command must produce no matches; `git status --short TES5Edit` must produce no output.
</verification>

<success_criteria>
- Native BA2 GNRL and DX10 record extension fields omit the leading dot and are NUL-padded/truncated to exactly four bytes.
- Multi-mip DDS arrays and multi-mip cubemaps fail during planning with `unsupported_format` instead of silently reordering payload bytes.
- Cubemap arrays fail during planning with `unsupported_format` instead of serializing as a single cube.
- Existing supported DDS layouts still pass: single-item multi-mip textures, one-mip arrays, and one-cube cubemaps.
- Focused BA2 writer, BA2 DDS reader, and public-header smoke tests pass with no public dependency leakage and no TES5Edit changes.
</success_criteria>

<output>
After completion, create `.planning/phases/10-ba2-writers/10-08-SUMMARY.md`.
</output>
