---
phase: 10-ba2-writers
plan: 03
type: tdd
wave: 3
depends_on: [10-02]
files_modified:
  - tests/ba2_writer_tests.cpp
  - src/ba2_writer.cpp
autonomous: true
requirements: [WRT-02]
must_haves:
  truths:
    - "Consumer can create BA2 GNRL archives from disk-file mappings and memory payloads with equivalent read-back."
    - "GNRL entries support raw, deflate, and Starfield v3 method-3 raw LZ4-block compression without codec fallback."
    - "Opt-in GNRL dedup shares identical post-policy stored payload offsets and disabled dedup emits distinct regions."
    - "Invalid GNRL writer inputs fail structurally before sink writes or masked host-file errors."
  artifacts:
    - path: "tests/ba2_writer_tests.cpp"
      provides: "GNRL disk, compression, dedup, and failure tests"
      contains: "disk-backed and memory-backed BA2 GNRL inputs read back equivalently"
    - path: "src/ba2_writer.cpp"
      provides: "GNRL disk planning, codec routing, dedup, and failure behavior"
      contains: "plan_ba2_gnrl_write_from_disk"
  key_links:
    - from: "src/ba2_writer.cpp"
      to: "src/compression.cpp"
      via: "resolve_write_compression / resolve_payload_codec / compress_payload"
      pattern: "resolve_payload_codec"
---

<objective>
TDD complete BA2 GNRL writer behavior for disk inputs, compression routing, deduplication, and structured failures per WRT-02 and D-02/D-06/D-07/D-12.

Purpose: Close the remaining GNRL writer requirements after native memory layout exists.
Output: GNRL disk planning, raw/deflate/LZ4-block stored payloads, dedup preview/offset behavior, and negative tests.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-ba2-writers/10-CONTEXT.md
@.planning/phases/10-ba2-writers/10-SPEC.md
@.planning/phases/10-ba2-writers/10-RESEARCH.md
@.planning/phases/10-ba2-writers/10-PATTERNS.md
@include/libbsa/ba2_writer.hpp
@src/ba2_writer.cpp
@src/bsa_writer.cpp
@src/compression.cpp
@tests/bsa_writer_tests.cpp
@tests/ba2_writer_tests.cpp
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 RED: Add failing GNRL disk, compression, dedup, and failure tests</name>
  <files>tests/ba2_writer_tests.cpp</files>
  <read_first>tests/ba2_writer_tests.cpp, src/ba2_writer.cpp, include/libbsa/ba2_writer.hpp, tests/bsa_writer_tests.cpp, src/compression.cpp, .planning/phases/10-ba2-writers/10-SPEC.md</read_first>
  <behavior>
    - Disk-backed and memory-backed GNRL inputs with identical payloads produce read-back-equivalent archives, and modifying disk files after planning does not affect finalization output.
    - Force-raw writes raw GNRL records with `PackedSize == 0`; force-compressed and archive-default-compressed write nonzero packed sizes with reader-reported `deflate` or `lz4_block` as appropriate.
    - Starfield v3 method 0 and method 3 are supported archive-level options; unsupported nonzero methods fail with `unsupported_format` or existing codec-route error.
    - Dedup enabled shares archive-absolute offsets for identical post-policy GNRL stored payloads; dedup disabled emits distinct offsets.
    - Duplicate normalized paths, invalid archive paths, missing disk inputs, layout overflow, unsupported target/subtype misuse, and injected sink failure return structured failures.
  </behavior>
  <action>Add RED Catch2 cases named `disk-backed and memory-backed BA2 GNRL inputs read back equivalently`, `BA2 GNRL compression routes raw deflate and Starfield LZ4-block payloads`, `BA2 GNRL dedup shares exact post-policy stored payloads`, and `BA2 GNRL writer rejects invalid inputs and preserves sink failures`. Use temporary files and custom failing sink patterns from `tests/bsa_writer_tests.cpp`. Do not implement production behavior in this task.</action>
  <verify>
    <automated>cmake --build build/windows-vs2026-vcpkg --config Debug --target libbsa_ba2_writer_tests && ctest --test-dir build/windows-vs2026-vcpkg --output-on-failure -C Debug -R libbsa_ba2_writer_tests</automated>
  </verify>
  <acceptance_criteria>Focused CTest fails before GREEN on at least one missing disk/compression/dedup behavior; `rg -n "disk-backed and memory-backed BA2 GNRL|Starfield LZ4-block|GNRL dedup|rejects invalid inputs|first sink failure" tests/ba2_writer_tests.cpp` finds all RED cases.</acceptance_criteria>
  <done>Failing tests define GNRL disk, compression, dedup, and failure semantics with exact expected errors and metadata.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 GREEN: Implement GNRL disk planning, codecs, dedup, and failures</name>
  <files>src/ba2_writer.cpp, tests/ba2_writer_tests.cpp</files>
  <read_first>src/ba2_writer.cpp, tests/ba2_writer_tests.cpp, include/libbsa/ba2_writer.hpp, src/compression.cpp, src/bsa_writer.cpp, include/libbsa/archive_path.hpp, .planning/phases/10-ba2-writers/10-RESEARCH.md</read_first>
  <action>Implement `plan_ba2_gnrl_write_from_disk` by validating archive paths and duplicates before opening host files, reading all bytes during planning, and delegating to memory planning. Extend GNRL planning to resolve per-entry `compression_policy` against `ba2_write_options::archive_default_compressed`, use archive format plus Starfield v3 `starfield_v3_compression_method` in `payload_codec_request`, reject method values other than 0 and 3, and store compressed bytes in plan-owned data regions. Implement dedup by comparing exact post-policy stored bytes and reusing stable data-region IDs plus offsets only when `options.deduplicate` is true. Ensure finalization returns first sink failure unchanged and never reopens disk inputs.</action>
  <verify>
    <automated>cmake --build build/windows-vs2026-vcpkg --config Debug --target libbsa_ba2_writer_tests && ctest --test-dir build/windows-vs2026-vcpkg --output-on-failure -C Debug -R "libbsa_ba2_writer_tests|libbsa_ba2_reader_tests|libbsa_compression_policy_tests"</automated>
  </verify>
  <acceptance_criteria>Focused/regression CTest passes; `rg -n "plan_ba2_gnrl_write_from_disk|validate.*archive.*path|resolve_write_compression|resolve_payload_codec|compress_payload|deduplicate|starfield_v3_compression_method" src/ba2_writer.cpp` finds implementation; `rg -n "BA2 GNRL writer planning is not implemented" src/ba2_writer.cpp` returns no matches.</acceptance_criteria>
  <done>GNRL writer fully satisfies WRT-02 for disk/memory inputs, native compression metadata, dedup offsets, and structured failure behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host filesystem → planner | Disk bytes can be missing or mutate; archive paths must be validated before host reads. |
| compression policy → codec dispatcher | Incorrect archive format/method routing can produce unreadable or corrupted payloads. |
| dedup grouping → BA2 offsets | Shared offsets must only be used for exact post-policy stored bytes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-07 | Tampering | disk GNRL planning | mitigate | Validate archive paths and duplicates before filesystem I/O; read bytes during planning and retain no file handles. |
| T-10-08 | Tampering / DoS | compression routing | mitigate | Use existing dispatcher with explicit archive format, per-entry compression state, and Starfield method 0/3 only; test no fallback routes. |
| T-10-09 | Tampering | GNRL dedup | mitigate | Share offsets only for byte-identical post-policy stored payloads; expose stable data-region IDs and archive-absolute offsets, never content hashes. |
</threat_model>

<verification>
Run BA2 writer, BA2 reader, and compression policy focused tests. Also run `git status --short TES5Edit` to prove the read-only reference boundary remains clean.
</verification>

<success_criteria>
Plan 03 is complete when all GNRL disk/memory, compression, dedup, and failure tests pass and no GNRL placeholder behavior remains.
</success_criteria>

<output>
After completion, create `.planning/phases/10-ba2-writers/10-03-SUMMARY.md`.
</output>
