---
phase: 10-ba2-writers
plan: 04
type: tdd
wave: 4
depends_on: [10-03]
files_modified:
  - src/texture/dds_analysis.hpp
  - src/texture/dds_analysis.cpp
  - src/ba2_writer.cpp
  - tests/ba2_writer_tests.cpp
  - CMakeLists.txt
autonomous: true
requirements: [WRT-03]
must_haves:
  truths:
    - "D-13: Consumer can create BA2 DDS/DX10 archives from actual in-memory DDS bytes for FO4 v1/v7/v8 and Starfield v3."
    - "D-14: DDS inputs are analyzed during planning into libbsa-owned texture metadata and plan-owned target-rule mip/chunk payload bytes."
    - "D-11: Written DX10 archives reopen through `open_ba2`, expose matching `texture_metadata`, extract as valid DDS, and keep DirectXTex private."
  artifacts:
    - path: "src/texture/dds_analysis.hpp"
      provides: "Private DDS analysis contract returning libbsa-owned metadata and chunks"
      contains: "analyze_dds"
    - path: "src/texture/dds_analysis.cpp"
      provides: "DirectXTex-backed DDS analysis implementation"
      contains: "LoadFromDDSMemory"
    - path: "src/ba2_writer.cpp"
      provides: "DX10 memory planning and serialization"
      contains: "DX10"
  key_links:
    - from: "src/ba2_writer.cpp"
      to: "src/texture/dds_analysis.cpp"
      via: "plan_ba2_dds_write analyzes DDS before table layout"
      pattern: "analyze_dds"
---

<objective>
TDD private DDS analysis and native BA2 DX10 memory writer layout per WRT-03 and D-09/D-11/D-13/D-14/D-15/D-16.

Purpose: BA2 DDS writing must package actual DDS bytes, not caller-supplied native descriptors.
Output: Private DirectXTex-backed analyzer, DX10 texture/chunk records, automatic target-rule chunk planning, and read-after-write validation for memory DDS inputs.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-ba2-writers/10-CONTEXT.md
@.planning/phases/10-ba2-writers/10-SPEC.md
@.planning/phases/10-ba2-writers/10-RESEARCH.md
@.planning/phases/10-ba2-writers/10-PATTERNS.md
@include/libbsa/ba2.hpp
@include/libbsa/ba2_writer.hpp
@src/texture/dds_validation.cpp
@src/texture/dds_reconstruction.cpp
@tests/ba2_dds_fixture_helpers.cpp
@tests/ba2_dds_reader_tests.cpp
@src/ba2_writer.cpp
@tests/ba2_writer_tests.cpp
@CMakeLists.txt

<decision_coverage>
This DDS memory writer plan covers D-03: subtype-specific DDS input types, D-09: subtype-specific DDS/DX10 preview sections, D-11: texture and chunk preview detail, D-13: actual DDS bytes/files as normal inputs rather than BA2-native descriptors, D-14: planning-time DDS analysis with plan-owned derived bytes, D-15: automatic chunking, and D-16: structural planning failure for malformed or unrepresentable DDS inputs.
</decision_coverage>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 RED: Add failing DDS memory analysis and DX10 read-back tests</name>
  <files>tests/ba2_writer_tests.cpp, CMakeLists.txt</files>
  <read_first>tests/ba2_writer_tests.cpp, CMakeLists.txt, tests/ba2_dds_fixture_helpers.cpp, tests/ba2_dds_reader_tests.cpp, include/libbsa/ba2_writer.hpp, src/texture/dds_validation.cpp, .planning/phases/10-ba2-writers/10-SPEC.md</read_first>
  <behavior>
    - In-memory DDS bytes are accepted through `ba2_dds_memory_entry`, analyzed during planning, and not interpreted as BA2-native descriptors supplied by the caller.
    - One-mip, multi-mip, cubemap, and array DDS inputs produce DX10 texture metadata with matching format, dimensions, mip count, array/cubemap state, chunk-to-mip mapping, and archive-absolute chunk offsets.
    - FO4 DX10 v1/v7/v8 and Starfield DX10 v3 emit `BTDX` + `DX10` native headers, texture records, chunk records, name table bytes, and payload regions.
    - Extracted entries validate as DDS through the private validation helper before any sink write succeeds.
  </behavior>
  <action>Add RED tests named `plans BA2 DX10 archives from in-memory DDS inputs` and `DX10 write plans expose texture and chunk preview details`. Reuse or extend source-built DDS bytes from `tests/ba2_dds_fixture_helpers.cpp`; if test helpers do not expose raw DDS input builders, create local generated DDS byte helpers in `tests/ba2_writer_tests.cpp` without adding binary blobs. Update `CMakeLists.txt` only if the BA2 writer tests need `tests/ba2_dds_fixture_helpers.cpp` or private `src` include access. Do not implement analyzer or writer behavior in this RED task.</action>
  <verify>
    <automated>cmake --build build/windows-vs2026-vcpkg --config Debug --target libbsa_ba2_writer_tests && ctest --test-dir build/windows-vs2026-vcpkg --output-on-failure -C Debug -R libbsa_ba2_writer_tests</automated>
  </verify>
  <acceptance_criteria>Focused CTest fails before GREEN on missing `BA2 DDS writer planning is not implemented` or missing analyzer behavior; `rg -n "plans BA2 DX10 archives from in-memory DDS inputs|DX10 write plans expose texture and chunk preview details|cubemap|array|validate" tests/ba2_writer_tests.cpp` finds RED coverage.</acceptance_criteria>
  <done>Failing tests specify exact DDS memory analysis, DX10 native layout, preview, and validation behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 GREEN: Implement private DDS analyzer and DX10 memory writer</name>
  <files>src/texture/dds_analysis.hpp, src/texture/dds_analysis.cpp, src/ba2_writer.cpp, CMakeLists.txt, tests/ba2_writer_tests.cpp</files>
  <read_first>src/texture/dds_analysis.hpp if it exists, src/texture/dds_analysis.cpp if it exists, src/texture/dds_validation.cpp, src/texture/dds_reconstruction.cpp, src/ba2_writer.cpp, include/libbsa/ba2_writer.hpp, tests/ba2_writer_tests.cpp, CMakeLists.txt, .planning/phases/10-ba2-writers/10-RESEARCH.md</read_first>
  <action>Create private `detail::analyze_dds` in `src/texture/dds_analysis.hpp/.cpp` that includes `DirectXTex.h` only in the `.cpp`, calls `DirectX::LoadFromDDSMemory`, translates metadata into libbsa-owned width/height/mip_count/array_size/cubemap/`dxgi_format`, and copies image payload bytes from `ScratchImage` into target-rule chunks documented in `10-RESEARCH.md` Open Questions (RESOLVED): emit one chunk per mip while `max(width, height) > 256`, then group the first mip with `max(width, height) <= 256` and all remaining lower-resolution mips into one final contiguous chunk; a one-mip texture still emits one chunk, and arrays/cubemaps include every array/cube item for each chunk mip range in deterministic DirectXTex image order. Return `result<T>` failures for malformed DDS, unsupported layouts/formats, missing mip images, impossible target-rule ranges, and integer narrowing. Update `CMakeLists.txt` to compile `src/texture/dds_analysis.cpp`. Implement `plan_ba2_dds_write` for memory inputs by calling the analyzer during planning, deriving DX10 records/chunk records/name table/payload offsets, and owning chunk payload bytes before finalization. Preserve DirectXTex/private dependency boundaries and no public native chunk descriptor API.</action>
  <verify>
    <automated>cmake --build build/windows-vs2026-vcpkg --config Debug --target libbsa_ba2_writer_tests && ctest --test-dir build/windows-vs2026-vcpkg --output-on-failure -C Debug -R "libbsa_ba2_writer_tests|libbsa_ba2_dds_reader_tests"</automated>
  </verify>
  <acceptance_criteria>Focused/regression CTest passes; `rg -n "LoadFromDDSMemory|ScratchImage|analyze_dds|DDS analysis failed|mip" src/texture/dds_analysis.cpp src/texture/dds_analysis.hpp` finds analyzer; `rg -n "DirectXTex|DXGI_FORMAT|Windows\.h" include/libbsa` returns no matches; `rg -n "magic_dx10|chunk_header_size|plan_ba2_dds_write" src/ba2_writer.cpp` finds DX10 writer implementation.</acceptance_criteria>
  <done>Memory-backed DDS inputs produce native DX10 BA2 archives that reopen, expose texture metadata, extract valid DDS bytes, and keep DirectXTex private.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DDS bytes → DirectXTex analyzer | Malformed or unsupported image data enters private texture parser. |
| analyzer → native DX10 records | Metadata and chunk ranges are narrowed to BA2 native fields. |
| public API → private dependency boundary | DirectXTex and DXGI types must not leak into public headers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-10 | Tampering / DoS | `detail::analyze_dds` | mitigate | Return structured failures for malformed DDS, unsupported layouts, missing mip images, and integer narrowing; no partial plan on failure. |
| T-10-11 | Tampering | DX10 layout arithmetic | mitigate | Check texture record, chunk record, name table, mip range, chunk offset, packed size, unpacked size, and total archive arithmetic before success. |
| T-10-12 | Information Disclosure | public headers | mitigate | Keep `DirectXTex.h` in private `.cpp`; verify public headers for private dependency tokens. |
</threat_model>

<verification>
Run BA2 writer and BA2 DDS reader tests, public-header private-token grep, and `git status --short TES5Edit`.
</verification>

<success_criteria>
Plan 04 is complete when memory DDS inputs for one-mip, multi-mip, cubemap, and array cases produce read-back-valid BA2 DX10 archives with expected target-rule chunk ranges and preview metadata.
</success_criteria>

<output>
After completion, create `.planning/phases/10-ba2-writers/10-04-SUMMARY.md`.
</output>
