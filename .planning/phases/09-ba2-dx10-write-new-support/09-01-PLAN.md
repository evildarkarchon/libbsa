---
phase: 09-ba2-dx10-write-new-support
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - include/libbsa/writer.hpp
  - tests/unit/public_include_boundary_tests.cpp
requirements:
  - WBA2-06
  - WBA2-07
requirements_addressed:
  - WBA2-06
  - WBA2-07
autonomous: true
must_haves:
  truths:
    - Consumer code can name and construct a public BA2 DX10 writer for Fallout 4 and Starfield v3 targets.
    - "D-01: Public DX10 writer surface mirrors the established BA2 GNRL shape: target/options construction, explicit archive paths, DDS host-file add methods, and write_to-style host-path finalization."
    - "D-04: Public input remains DDS-host-file-only; no Phase 9 public memory-buffer DDS input is planned."
    - "D-05: Public BA2 DX10 writer output is compressed-only at archive level with no entry-specific or chunk-specific raw/compressed overrides."
    - "D-06: Public headers expose only libbsa-owned C++20 types and no DirectXTex, DXGI, Windows SDK, codec, TES5Edit, private implementation, or std::expected symbols."
    - "D-29: Public include-boundary tests remain green after adding the DX10 writer API."
    - Public DX10 writer contract is DDS-host-file-only and compressed-only at archive level.
  artifacts:
    - path: include/libbsa/writer.hpp
      provides: ba2_dx10_target, ba2_dx10_writer_options, ba2_dx10_writer declarations
      contains: ba2_dx10_writer
    - path: tests/unit/public_include_boundary_tests.cpp
      provides: compile-time public boundary proof for DX10 writer surface
      contains: ba2_dx10_target
  key_links:
    - from: tests/unit/public_include_boundary_tests.cpp
      to: include/libbsa/writer.hpp
      via: static_assert/requires public API checks
      pattern: ba2_dx10_writer.*add_file
---

<objective>
Define the public BA2 DX10 writer contract before implementation.

Purpose: Downstream Phase 9 implementation needs a stable dependency-light contract that satisfies D-01 through D-07 without importing the stale SPEC Requirement 7 raw/override API.
Output: Public declarations and boundary tests for a compressed-only, DDS-host-file BA2 DX10 writer.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
@C:/Users/evild/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-ba2-dx10-write-new-support/09-CONTEXT.md
@.planning/phases/09-ba2-dx10-write-new-support/09-SPEC.md
@.planning/phases/09-ba2-dx10-write-new-support/09-RESEARCH.md
@.planning/phases/09-ba2-dx10-write-new-support/09-PATTERNS.md
@include/libbsa/writer.hpp
@include/libbsa/libbsa.hpp
@tests/unit/public_include_boundary_tests.cpp

<interfaces>
Use these exact public names unless an existing compile conflict forces a spelling adjustment:

```cpp
enum class ba2_dx10_target { fallout4, starfield_v3 };

struct ba2_dx10_writer_options {
  bool overwrite_existing = false;
  bool deduplicate_payloads = false;
  std::uint32_t max_decoded_chunk_bytes = 0U; // 0 means reference-derived default chunking.
  std::uint32_t starfield_unknown1 = 1U;
  std::uint32_t starfield_unknown2 = 0U;
  std::uint32_t starfield_compression_method = 3U;
};

class ba2_dx10_writer {
 public:
  explicit ba2_dx10_writer(ba2_dx10_target target);
  explicit ba2_dx10_writer(ba2_dx10_target target, ba2_dx10_writer_options options);
  [[nodiscard]] ba2_dx10_target target() const noexcept;
  [[nodiscard]] const ba2_dx10_writer_options& options() const noexcept;
  result<void> add_file(std::string_view archive_path, std::string_view dds_host_path);
  result<void> write_to(std::string_view host_path) const;
 private:
  struct state;
  std::shared_ptr<state> state_;
};
```

Do not add public `add_bytes`, `entry_compression_policy`, per-entry options, or per-chunk compression override APIs for DX10; D-04, D-05, and D-07 supersede SPEC Requirement 7.
</interfaces>
</context>

<feature>
  <name>Public BA2 DX10 writer contract</name>
  <files>include/libbsa/writer.hpp, tests/unit/public_include_boundary_tests.cpp</files>
  <behavior>
    - `ba2_dx10_writer{ba2_dx10_target::fallout4}` and `ba2_dx10_writer{ba2_dx10_target::starfield_v3, options}` compile from public headers.
    - `add_file("Textures/Foo.dds", "source.dds")` returns `libbsa::result<void>` and no public memory-buffer overload is required.
    - Public forbidden-token scans still reject DirectXTex, DirectX::, DXGI, Windows.h, libdeflate, lz4, TES5Edit, std::expected, and private helper names.
    - No DX10 public compression override type appears in the public contract.
  </behavior>
  <implementation>Add declaration-only public API and compile-time boundary tests first; method definitions are implemented in later plans.</implementation>
</feature>

<tasks>
<task type="auto">
  <name>RED: Specify dependency-light DX10 writer API</name>
  <read_first>include/libbsa/writer.hpp; tests/unit/public_include_boundary_tests.cpp; .planning/phases/09-ba2-dx10-write-new-support/09-CONTEXT.md; .planning/phases/09-ba2-dx10-write-new-support/09-SPEC.md; .planning/phases/09-ba2-dx10-write-new-support/09-PATTERNS.md</read_first>
  <files>tests/unit/public_include_boundary_tests.cpp</files>
  <action>Add failing public-boundary assertions for `libbsa::ba2_dx10_target`, `libbsa::ba2_dx10_writer_options`, and `libbsa::ba2_dx10_writer`. Assert `target()`, `options()`, `add_file("Textures/Diffuse.dds", "source.dds")`, and `write_to("out.ba2")` signatures exactly. Add a negative public-surface scan that fails if `ba2_dx10_entry_options`, `entry_compression_policy` in a DX10 requires-expression, `add_bytes`, or `chunk_compression` appears in the DX10 public assertion block, per D-04/D-05/D-07.</action>
  <verify><automated>ctest --preset windows-msvc-debug-static -R public_include_boundary --output-on-failure</automated></verify>
  <acceptance_criteria>`tests/unit/public_include_boundary_tests.cpp` contains `ba2_dx10_writer`; contains `ba2_dx10_target::fallout4`; contains `ba2_dx10_target::starfield_v3`; contains `add_file("Textures/Diffuse.dds", "source.dds")`; does not contain `ba2_dx10_entry_options` outside a forbidden-token/negative assertion context.</acceptance_criteria>
  <done>RED gate fails because the DX10 public symbols do not exist yet, not because of syntax or missing test registration.</done>
</task>

<task type="auto">
  <name>GREEN: Add declaration-only public DX10 writer surface</name>
  <read_first>include/libbsa/writer.hpp; tests/unit/public_include_boundary_tests.cpp; .planning/phases/09-ba2-dx10-write-new-support/09-CONTEXT.md; .planning/phases/09-ba2-dx10-write-new-support/09-RESEARCH.md</read_first>
  <files>include/libbsa/writer.hpp</files>
  <action>Add Doxygen-documented `ba2_dx10_target`, `ba2_dx10_writer_options`, and `ba2_dx10_writer` declarations using the exact interface block above. Set option defaults to `overwrite_existing=false`, `deduplicate_payloads=false`, `max_decoded_chunk_bytes=0U`, `starfield_unknown1=1U`, `starfield_unknown2=0U`, `starfield_compression_method=3U`. Document that `max_decoded_chunk_bytes == 0` selects reference-derived default chunking per D-12/D-14 and that DX10 output is compressed-only with no public entry/chunk raw override per D-05/D-07. Do not include DirectXTex, DXGI, codec, TES5Edit, private headers, or C++23 types.</action>
  <verify><automated>ctest --preset windows-msvc-debug-static -R public_include_boundary --output-on-failure</automated></verify>
  <acceptance_criteria>`include/libbsa/writer.hpp` contains `enum class ba2_dx10_target`; contains `struct ba2_dx10_writer_options`; contains `class ba2_dx10_writer`; contains `std::uint32_t max_decoded_chunk_bytes = 0U`; contains `std::uint32_t starfield_compression_method = 3U`; contains no `ba2_dx10_entry_options`; contains no `add_bytes` in the `ba2_dx10_writer` declaration.</acceptance_criteria>
  <done>Public boundary tests compile and pass with declaration-only DX10 API.</done>
</task>

<task type="auto">
  <name>REFACTOR: Verify public boundary remains sealed</name>
  <read_first>include/libbsa/writer.hpp; include/libbsa/libbsa.hpp; tests/unit/public_include_boundary_tests.cpp; AGENTS.md</read_first>
  <files>include/libbsa/writer.hpp, tests/unit/public_include_boundary_tests.cpp</files>
  <action>Keep the DX10 declarations in `writer.hpp` so `include/libbsa/libbsa.hpp` needs no new include. Tighten comments only where needed to explain the SPEC Requirement 7 correction: public DX10 writer is compressed-only at archive level. Do not remove existing accurate comments. Ensure the forbidden-token list still includes `DirectXTex`, `DirectX::`, `DXGI`, `Windows.h`, `libdeflate`, `lz4`, `TES5Edit`, `std::expected`, `compression_router`, and `archive_path_key`.</action>
  <verify><automated>ctest --preset windows-msvc-debug-static -R public_include_boundary --output-on-failure</automated></verify>
  <acceptance_criteria>`tests/unit/public_include_boundary_tests.cpp` contains `DirectXTex`; contains `std::expected`; `include/libbsa/libbsa.hpp` still contains `#include <libbsa/writer.hpp>`; `include/libbsa/writer.hpp` contains `compressed-only` or `compressed only` in the DX10 writer documentation.</acceptance_criteria>
  <done>DX10 public API is documented, dependency-light, and intentionally excludes public compression overrides.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public header → consumer build | Installed consumers compile untrusted translation units against libbsa headers. |
| user decisions → stale SPEC | CONTEXT correction must override stale raw/compressed override requirement. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01-01 | Information Disclosure | include/libbsa/writer.hpp | mitigate | Public boundary tests scan for DirectXTex/DXGI/codec/TES5Edit/private/C++23 tokens. |
| T-09-01-02 | Tampering | ba2_dx10_writer public API | mitigate | Exclude raw/per-entry/per-chunk compression override APIs; document compressed-only behavior per D-05/D-07. |
| T-09-01-03 | Repudiation | tests/unit/public_include_boundary_tests.cpp | mitigate | Compile-time signature assertions make the public contract observable before implementation. |
</threat_model>

<verification>
Run `ctest --preset windows-msvc-debug-static -R public_include_boundary --output-on-failure`.
</verification>

<success_criteria>
- RED commit `test(09-01): add failing test for public DX10 writer contract` exists.
- GREEN commit `feat(09-01): declare public DX10 writer contract` exists.
- Public contract names compile and no public dependency leakage is introduced.
- No public DX10 raw/compressed override API is added.
</success_criteria>

<output>
After completion, create `.planning/phases/09-ba2-dx10-write-new-support/09-01-SUMMARY.md`.
</output>
