---
phase: 06-ba2-gnrl-read-and-extract
plan: 06
type: execute
wave: 6
depends_on: [06-05]
files_modified:
  - README.md
  - tests/public_header_smoke.cpp
autonomous: true
requirements: [BA2-01, BA2-02, BA2-03, BA2-04]
must_haves:
  truths:
    - "Consumer-facing docs describe BA2 GNRL read/extract support and boundaries."
    - "D-03: Public smoke and boundary gates prove ba2.hpp, ba2_archive, open_ba2, and extract_ba2_entry stay visible from consumer-style code."
    - "D-13: Documentation and final validation identify generated deterministic BA2 fixtures as the Phase 6 acceptance corpus."
    - "D-16: Maintainer can run final gates proving malformed coverage for truncated tables, impossible offsets, mismatched counts, and codec route confusion."
  artifacts:
    - path: "README.md"
      provides: "BA2 GNRL support and validation documentation"
      contains: "BA2 GNRL"
    - path: "tests/public_header_smoke.cpp"
      provides: "Final public BA2 smoke coverage"
      contains: "extract_ba2_entry_fn"
  key_links:
    - from: "README.md"
      to: "CTest"
      via: "documented BA2 validation commands"
      pattern: "libbsa_ba2_reader_tests"
---

<objective>
Finalize Phase 6 documentation, public smoke confirmation, and validation gates.

Purpose: Makes BA2 GNRL support adoptable and proves D-03, D-13, D-16, public-header boundaries, explicit CMake wiring, and TES5Edit cleanliness after implementation.
Output: README updates, final smoke validation, and documented gate results in the summary.
</objective>

<decision_coverage>
- D-03: Final smoke validation keeps `ba2.hpp`, `ba2_archive`, `open_ba2`, and `extract_ba2_entry` visible from consumer-style public code.
- D-13: README and final validation describe generated deterministic BA2 fixtures as the Phase 6 acceptance corpus, not external archives or BSArchPro execution.
- D-16: Final gates confirm the malformed matrix remains covered and documented for truncated tables, impossible offsets, mismatched counts, and codec route confusion.
</decision_coverage>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@README.md
@tests/public_header_smoke.cpp
@.planning/phases/06-ba2-gnrl-read-and-extract/06-SPEC.md
@.planning/phases/06-ba2-gnrl-read-and-extract/06-CONTEXT.md
@.planning/phases/06-ba2-gnrl-read-and-extract/06-VALIDATION.md
@.planning/phases/05-tes3-bsa-read-and-extract/05-04-SUMMARY.md
@.planning/phases/04-tes4-family-bsa-read-and-extract/04-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document BA2 GNRL support and boundaries</name>
  <read_first>README.md, .planning/phases/06-ba2-gnrl-read-and-extract/06-SPEC.md, .planning/phases/06-ba2-gnrl-read-and-extract/06-CONTEXT.md, .planning/phases/06-ba2-gnrl-read-and-extract/06-VALIDATION.md, AGENTS.md</read_first>
  <files>README.md</files>
  <action>Update README in the existing support/validation style to state that libbsa supports BA2 GNRL open/list/lookup/extract for Fallout 4 versions 1/7/8 and Starfield versions 2/3. Document that `include/libbsa/ba2.hpp` provides `ba2_archive`, `open_ba2`, and `extract_ba2_entry`; archive objects are metadata-only; callers pass a `byte_source` again to extraction; GNRL `.dds` files are treated as ordinary payloads; DX10 texture reconstruction is Phase 7; BA2 writers are later scope. Document compression behavior: raw when `PackedSize == 0`, deflate for compressed FO4/default Starfield, and raw LZ4 block for compressed Starfield v3 archives with `CompressionMethod == 3`. Add validation commands exactly: `ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug -R libbsa_ba2_reader_tests`, `ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug -L codec`, `ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug -R libbsa.public_header_smoke`, and `git status --short TES5Edit`.</action>
  <verify>
    <automated>ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug -R libbsa.public_header_smoke</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "BA2 GNRL|ba2_archive|open_ba2|extract_ba2_entry|CompressionMethod == 3|DX10 texture reconstruction is Phase 7|git status --short TES5Edit" README.md` prints all seven patterns.
    - `rg -n "safe disk extraction|bulk extraction|BA2 writer" README.md` must not describe those items as Phase 6 deliverables; if mentioned, they must be explicitly later/out-of-scope.
  </acceptance_criteria>
  <done>README accurately documents Phase 6 BA2 GNRL support, compression behavior, boundaries, and validation commands.</done>
</task>

<task type="auto">
  <name>Task 2: Run final BA2 validation and boundary gates</name>
  <read_first>tests/public_header_smoke.cpp, include/libbsa/ba2.hpp, CMakeLists.txt, .planning/phases/06-ba2-gnrl-read-and-extract/06-VALIDATION.md</read_first>
  <files>tests/public_header_smoke.cpp</files>
  <action>Inspect `tests/public_header_smoke.cpp` and only adjust it if prior implementation changed the BA2 public symbol names; the final required state is still `#include <libbsa/ba2.hpp>`, `libbsa::ba2_archive`, `open_ba2_fn`, and `extract_ba2_entry_fn`. Run final gates and record exact command results in the SUMMARY: `cmake --build build/local-vs2026-vcpkg --config Debug`, `ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug -R libbsa_ba2_reader_tests`, `ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug -L codec`, `ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug -R libbsa.public_header_smoke`, `ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug`, `rg -n "libdeflate|lz4\.h|lz4frame\.h|LZ4|DirectXTex|TES5Edit" include/libbsa`, `rg -n "^[^#]*\b(GLOB|GLOB_RECURSE)\b" CMakeLists.txt`, and `git status --short TES5Edit`. Public-header token gate should return no matches; CMake GLOB gate should return no matches; TES5Edit status must be empty.</action>
  <verify>
    <automated>ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "#include <libbsa/ba2.hpp>|libbsa::ba2_archive|open_ba2_fn|extract_ba2_entry_fn" tests/public_header_smoke.cpp` prints all four patterns.
    - `rg -n "libdeflate|lz4\.h|lz4frame\.h|LZ4|DirectXTex|TES5Edit" include/libbsa` prints no matches.
    - `rg -n "^[^#]*\b(GLOB|GLOB_RECURSE)\b" CMakeLists.txt` prints no matches.
    - `git status --short TES5Edit` prints no output.
  </acceptance_criteria>
  <done>All Phase 6 targeted and full validation gates pass, and the public-header/CMake/TES5Edit boundary gates are clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation -> consumers | README must not overpromise safe disk extraction, DX10 reconstruction, writer, or bulk extraction behavior. |
| public headers -> dependency boundary | Final smoke and grep gates verify public API does not leak private dependencies. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-16 | Information Disclosure | public headers | mitigate | Run public-header forbidden-token gate and smoke test using only public headers. |
| T-06-17 | Repudiation | validation evidence | mitigate | Record exact final command outputs in SUMMARY so verification can be audited. |
| T-06-18 | Tampering | `TES5Edit/` boundary | mitigate | Run `git status --short TES5Edit` and require empty output. |
</threat_model>

<verification>
Run the final command set from Task 2. The phase gate requires full CTest green, BA2 reader tests green, codec tests green, smoke green, public-header token gate empty, CMake GLOB gate empty, and `TES5Edit/` clean.
</verification>

<success_criteria>
- README documents exactly the Phase 6 support and out-of-scope boundaries.
- Public header smoke names BA2 API symbols.
- Full and targeted validation commands pass and are recorded in the summary.
</success_criteria>

<output>
After completion, create `.planning/phases/06-ba2-gnrl-read-and-extract/06-06-SUMMARY.md`.
</output>
