---
phase: 06-ba2-gnrl-read-and-extract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - include/libbsa/ba2.hpp
  - src/ba2_reader.cpp
  - tests/public_header_smoke.cpp
  - CMakeLists.txt
autonomous: true
requirements: [BA2-01, BA2-02, BA2-03, BA2-04]
must_haves:
  truths:
    - "D-01: Consumer code can include separate <libbsa/ba2.hpp> BA2 APIs without adding BA2 symbols to bsa.hpp or introducing a family-neutral archive API."
    - "D-02: Consumer code can use ba2_archive summary(), paths(), contains(), and entry() parallel to bsa_archive."
    - "D-03: Consumer-style smoke coverage names ba2_archive and takes addresses of open_ba2 and extract_ba2_entry from ba2.hpp."
    - "D-04: BA2 archive objects are metadata-only and use caller-owned byte_source objects for extraction."
  artifacts:
    - path: "include/libbsa/ba2.hpp"
      provides: "Public BA2 archive API"
      contains: "class ba2_archive"
    - path: "src/ba2_reader.cpp"
      provides: "BA2 API symbol definitions and metadata-only wrapper"
      contains: "open_ba2"
    - path: "tests/public_header_smoke.cpp"
      provides: "Consumer-style public API smoke coverage"
      contains: "libbsa::ba2_archive"
    - path: "CMakeLists.txt"
      provides: "Explicit BA2 header/source build wiring"
      contains: "include/libbsa/ba2.hpp"
  key_links:
    - from: "include/libbsa/ba2.hpp"
      to: "include/libbsa/archive_view.hpp"
      via: "ba2_archive owns archive_view"
      pattern: "archive_view view_"
    - from: "tests/public_header_smoke.cpp"
      to: "include/libbsa/ba2.hpp"
      via: "public include and symbol address checks"
      pattern: "open_ba2_fn"
---

<objective>
Create the public BA2 read/extract surface and build wiring required before behavior-driven BA2 parser work can proceed.

Purpose: Implements D-01 through D-04 and D-13/D-15 foundations without introducing a family-neutral archive API or changing BSA public headers.
Output: `include/libbsa/ba2.hpp`, metadata-only `ba2_archive` wrapper definitions, CMake source/header wiring, and public-header smoke coverage.
</objective>

<decision_coverage>
- D-01: Create the separate public BA2 header and API surface in `include/libbsa/ba2.hpp`; do not add BA2 APIs to `bsa.hpp` or create a family-neutral API.
- D-02: Keep `ba2_archive` parallel to `bsa_archive` with `summary()`, `paths()`, `contains()`, and `entry()` backed by existing summary/metadata types.
- D-03: Extend consumer-style public smoke coverage to include `ba2.hpp`, `ba2_archive`, `open_ba2`, and `extract_ba2_entry` symbols.
- D-04: Implement `ba2_archive` as copied metadata only; extraction takes a caller-owned `byte_source` again.
- D-13: Establish generated deterministic fixtures as the acceptance-corpus direction without external archives or BSArchPro execution.
- D-15: Keep initial BA2 fixture/test support test-local unless a later writer phase proves shared helpers are needed.
</decision_coverage>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-ba2-gnrl-read-and-extract/06-SPEC.md
@.planning/phases/06-ba2-gnrl-read-and-extract/06-CONTEXT.md
@.planning/phases/06-ba2-gnrl-read-and-extract/06-PATTERNS.md
@include/libbsa/bsa.hpp
@include/libbsa/archive.hpp
@include/libbsa/archive_view.hpp
@include/libbsa/io.hpp
@tests/public_header_smoke.cpp
@CMakeLists.txt

<interfaces>
Create `ba2_archive` parallel to `bsa_archive`:

```cpp
class ba2_archive {
public:
    ba2_archive(archive_summary summary, std::vector<entry_metadata> entries);
    [[nodiscard]] const archive_summary& summary() const noexcept;
    [[nodiscard]] std::vector<archive_path> paths() const;
    [[nodiscard]] bool contains(std::string path) const;
    [[nodiscard]] result<entry_metadata> entry(std::string path) const;
private:
    archive_view view_;
};

[[nodiscard]] result<ba2_archive> open_ba2(const byte_source& source);
[[nodiscard]] result<void> extract_ba2_entry(const ba2_archive& archive,
                                            const byte_source& source,
                                            std::string path,
                                            byte_sink& sink);
```

Initial `open_ba2` / `extract_ba2_entry` definitions may return structured `unsupported_format` until the TDD parser/extractor plans replace them. Do not add any BA2 declarations to `include/libbsa/bsa.hpp` per D-01.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add BA2 public header and metadata-only wrapper definitions</name>
  <read_first>include/libbsa/ba2.hpp (new), include/libbsa/bsa.hpp, include/libbsa/archive.hpp, include/libbsa/archive_view.hpp, include/libbsa/io.hpp, .planning/phases/06-ba2-gnrl-read-and-extract/06-CONTEXT.md</read_first>
  <files>include/libbsa/ba2.hpp, src/ba2_reader.cpp</files>
  <action>Create `include/libbsa/ba2.hpp` with the same include set and namespace style as `bsa.hpp`: `archive.hpp`, `archive_path.hpp`, `archive_view.hpp`, `io.hpp`, `result.hpp`, `<string>`, and `<vector>`. Add Doxygen comments documenting that `ba2_archive` owns copied metadata only and payload bytes remain in caller-owned `byte_source` objects per D-04. Add `class ba2_archive` with constructor and methods exactly: `summary()`, `paths()`, `contains(std::string)`, and `entry(std::string)`. Add declarations for `result<ba2_archive> open_ba2(const byte_source& source)` and `result<void> extract_ba2_entry(const ba2_archive& archive, const byte_source& source, std::string path, byte_sink& sink)`. Create `src/ba2_reader.cpp` implementing the wrapper methods by forwarding to `archive_view`; add temporary structured `unsupported_format` returns for `open_ba2` and `extract_ba2_entry` with messages `BA2 GNRL parsing is not implemented yet` and `BA2 GNRL extraction is not implemented yet`. Do not include or reference `TES5Edit/`, libdeflate, LZ4, DirectXTex, platform headers, or `bsa.hpp` from `ba2.hpp`.</action>
  <verify>
    <automated>cmake --build build/local-vs2026-vcpkg --config Debug</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "class ba2_archive|open_ba2|extract_ba2_entry|archive_view view_" include/libbsa/ba2.hpp` prints all four symbols.
    - `rg -n "ba2_archive::summary|ba2_archive::paths|ba2_archive::contains|ba2_archive::entry" src/ba2_reader.cpp` prints all four forwarding method definitions.
    - `rg -n "#include <libbsa/bsa.hpp>|libdeflate|lz4|DirectXTex|TES5Edit" include/libbsa/ba2.hpp` prints no matches.
  </acceptance_criteria>
  <done>`ba2.hpp` exists, compiles as a public header, and `ba2_archive` is a copied metadata view parallel to `bsa_archive` per D-01 through D-04.</done>
</task>

<task type="auto">
  <name>Task 2: Wire BA2 source/header and public smoke coverage</name>
  <read_first>CMakeLists.txt, tests/public_header_smoke.cpp, include/libbsa/ba2.hpp, .planning/phases/06-ba2-gnrl-read-and-extract/06-PATTERNS.md</read_first>
  <files>CMakeLists.txt, tests/public_header_smoke.cpp</files>
  <action>Add `src/ba2_reader.cpp` to the `target_sources(libbsa PRIVATE ...)` list and `include/libbsa/ba2.hpp` to the `FILE_SET public_headers` list. In `tests/public_header_smoke.cpp`, add `#include <libbsa/ba2.hpp>`, construct `libbsa::ba2_archive ba2{summary, std::vector{ba2_metadata}}` using a BA2-format `archive_summary` and copied `entry_metadata`, call `ba2.paths()`, `ba2.contains(...)`, and `ba2.entry(...)`, and take `const auto open_ba2_fn = &libbsa::open_ba2;` plus `const auto extract_ba2_entry_fn = &libbsa::extract_ba2_entry;` per D-03. Extend the smoke return expression to require BA2 path lookup success and both function pointers non-null. Keep the smoke executable using only public headers.</action>
  <verify>
    <automated>cmake --build build/local-vs2026-vcpkg --config Debug && ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug -R libbsa.public_header_smoke</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "src/ba2_reader.cpp|include/libbsa/ba2.hpp" CMakeLists.txt` prints both paths.
    - `rg -n "#include <libbsa/ba2.hpp>|libbsa::ba2_archive|open_ba2_fn|extract_ba2_entry_fn" tests/public_header_smoke.cpp` prints all four patterns.
    - `rg -n "libdeflate|lz4\.h|lz4frame\.h|LZ4|DirectXTex|TES5Edit" include/libbsa` prints no matches.
  </acceptance_criteria>
  <done>Consumer-style public smoke coverage names the BA2 API and CMake explicitly builds the BA2 header/source without touching `TES5Edit/`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer source -> BA2 public API | Untrusted archive bytes enter `open_ba2` and extraction through caller-owned `byte_source`. |
| public header -> consumer build | Public API must not leak private dependency or platform headers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | `include/libbsa/ba2.hpp` | mitigate | Public header includes only libbsa-owned/public headers and standard library headers; verify forbidden dependency tokens are absent. |
| T-06-02 | Tampering | `ba2_archive` lifetime model | mitigate | Archive object owns copied metadata only; extraction requires caller to pass the `byte_source` again, preventing stale borrowed source pointers. |
</threat_model>

<verification>
Run `cmake --build build/local-vs2026-vcpkg --config Debug`, `ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug -R libbsa.public_header_smoke`, and `git status --short TES5Edit` with empty output.
</verification>

<success_criteria>
- BA2 public API surface exists in `include/libbsa/ba2.hpp` and not in `include/libbsa/bsa.hpp`.
- Public smoke uses `ba2_archive`, `open_ba2`, and `extract_ba2_entry` from consumer-style code.
- Build wiring is explicit and does not use non-comment CMake `GLOB` or `GLOB_RECURSE`.
</success_criteria>

<output>
After completion, create `.planning/phases/06-ba2-gnrl-read-and-extract/06-01-SUMMARY.md`.
</output>
