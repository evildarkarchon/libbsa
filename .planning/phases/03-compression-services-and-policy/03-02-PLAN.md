---
phase: 03-compression-services-and-policy
plan: 02
type: tdd
wave: 2
depends_on: [03-01]
files_modified:
  - src/compression/deflate_codec.hpp
  - src/compression/deflate_codec.cpp
  - src/compression.cpp
  - tests/deflate_codec_tests.cpp
  - CMakeLists.txt
autonomous: true
requirements: [CMP-01, CMP-04]
must_haves:
  truths:
    - "Consumer-facing extraction code can decompress deflate payloads through libbsa-owned APIs."
    - "Maintainer can prove deflate rejects malformed data and exact-size mismatches."
  artifacts:
    - path: "src/compression/deflate_codec.hpp"
      provides: "Internal raw deflate wrapper declarations"
    - path: "src/compression/deflate_codec.cpp"
      provides: "libdeflate-backed raw deflate compression/decompression"
    - path: "tests/deflate_codec_tests.cpp"
      provides: "Deflate round-trip and failure tests"
  key_links:
    - from: "src/compression.cpp"
      to: "src/compression/deflate_codec.cpp"
      via: "dispatcher calls internal deflate helper for `compression_algorithm::deflate`"
      pattern: "compression_algorithm::deflate"
---

<objective>
Implement exact-size raw deflate compression and decompression behind the public compression dispatcher.

Purpose: Satisfy the deflate path needed by TES4-family BSA, Fallout 4 BA2, and Starfield deflate payloads while preserving a dependency-free public API.
Output: Internal deflate codec wrapper, dispatcher integration, and focused tests.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-compression-services-and-policy/03-RESEARCH.md
@include/libbsa/compression.hpp
@include/libbsa/result.hpp
@src/compression.cpp
@CMakeLists.txt

<interfaces>
From Plan 03-01 expected `include/libbsa/compression.hpp`:
```cpp
enum class compression_algorithm { none, deflate, lz4_frame, lz4_block };
result<std::vector<std::byte>> decompress_payload(compression_algorithm algorithm, std::span<const std::byte> packed, std::uint64_t expected_size);
result<std::vector<std::byte>> compress_payload(compression_algorithm algorithm, std::span<const std::byte> unpacked);
```
If Plan 03-01 only created routing declarations, extend the same public header with these two Doxygen-documented functions in this plan.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing deflate codec tests</name>
  <files>tests/deflate_codec_tests.cpp, CMakeLists.txt</files>
  <read_first>tests/compression_policy_tests.cpp, include/libbsa/compression.hpp, CMakeLists.txt</read_first>
  <behavior>
    - Compressing bytes `{0x41, 0x41, 0x41, 0x42, 0x43}` with `compression_algorithm::deflate`, then decompressing with expected size `5`, returns the original bytes.
    - Decompressing the same deflate bytes with expected size `6` fails with `error_code::decompression_failure` and message `deflate size mismatch`.
    - Decompressing bytes `{0xde, 0xad, 0xbe, 0xef}` as deflate fails with `error_code::decompression_failure` and message `deflate decompression failed`.
    - Compressing an empty span as deflate succeeds and decompresses back to an empty vector when expected size is `0`.
  </behavior>
  <action>Create `tests/deflate_codec_tests.cpp` using the public dispatcher functions from `include/libbsa/compression.hpp`. Register `libbsa_deflate_codec_tests` in `CMakeLists.txt`, link `libbsa::libbsa` and `Catch2::Catch2WithMain`, and discover it with `TEST_PREFIX "libbsa_deflate_codec_tests."` and labels `unit;codec`. Run the targeted build/test and confirm RED because the deflate payload functions are not implemented.</action>
  <verify>
    <automated>cmake --build build/local-vs2026-vcpkg --config Debug --target libbsa_deflate_codec_tests</automated>
  </verify>
  <acceptance_criteria>
    - `tests/deflate_codec_tests.cpp` contains `compression_algorithm::deflate`.
    - `tests/deflate_codec_tests.cpp` contains `deflate size mismatch`.
    - `CMakeLists.txt` contains `add_executable(libbsa_deflate_codec_tests tests/deflate_codec_tests.cpp)`.
    - The targeted build or test fails before implementation.
  </acceptance_criteria>
  <done>RED state captured for deflate round-trip and failure behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement libdeflate-backed exact-size deflate wrapper</name>
  <files>src/compression/deflate_codec.hpp, src/compression/deflate_codec.cpp, src/compression.cpp, include/libbsa/compression.hpp, CMakeLists.txt</files>
  <read_first>include/libbsa/compression.hpp, src/compression.cpp, include/libbsa/result.hpp, CMakeLists.txt, tests/deflate_codec_tests.cpp</read_first>
  <action>Create internal `src/compression/deflate_codec.hpp` and `src/compression/deflate_codec.cpp` that include `libdeflate.h` only in internal source/header files. Add Doxygen comments to public dispatcher declarations if they are added here. Implement `deflate_decompress(std::span<const std::byte> packed, std::uint64_t expected_size)` by allocating exactly `expected_size` bytes, calling `libdeflate_alloc_decompressor`, `libdeflate_deflate_decompress`, and `libdeflate_free_decompressor`, then requiring `LIBDEFLATE_SUCCESS` and `actual_out == expected_size`. Return `error_code::decompression_failure` with message `deflate decompression failed` for bad data/insufficient space and `deflate size mismatch` for a successful decode with a different output length. Implement `deflate_compress` using `libdeflate_alloc_compressor(6)`, `libdeflate_deflate_compress_bound`, `libdeflate_deflate_compress`, output resize to the returned byte count, and `libdeflate_free_compressor`; compression failure returns `error_code::decompression_failure` with message `deflate compression failed`. Wire public `compress_payload` and `decompress_payload` in `src/compression.cpp` so `compression_algorithm::deflate` calls these helpers and `compression_algorithm::none` returns the original bytes after exact-size validation for decompression. Add the new `.cpp` file to `target_sources(libbsa PRIVATE ...)`.</action>
  <verify>
    <automated>cmake --build build/local-vs2026-vcpkg --config Debug && ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug -R libbsa_deflate_codec_tests</automated>
  </verify>
  <acceptance_criteria>
    - `src/compression/deflate_codec.cpp` contains `libdeflate_deflate_decompress`.
    - `src/compression/deflate_codec.cpp` contains `libdeflate_deflate_compress_bound`.
    - `src/compression/deflate_codec.cpp` contains `actual_out` and `expected_size` comparison logic.
    - `include/libbsa/compression.hpp` does not contain `libdeflate`.
    - Targeted deflate codec tests pass.
  </acceptance_criteria>
  <done>Deflate payloads can round-trip through the public dispatcher, malformed data fails structurally, and exact output size is enforced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| archive payload→libdeflate | Untrusted compressed bytes are decoded by a native library into caller-requested output buffers. |
| archive metadata→allocation | Untrusted expected-size metadata controls output allocation size. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | T | `deflate_decompress` | mitigate | Require libdeflate success and exact `actual_out == expected_size`; return `decompression_failure` otherwise. |
| T-03-02-02 | D | `deflate_decompress` | accept | Phase 03 uses whole-buffer wrappers because archive metadata supplies payload sizes; streaming/bulk memory limits are handled in later reader/performance phases. |
</threat_model>

<verification>
Run `cmake --build build/local-vs2026-vcpkg --config Debug && ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug -R libbsa_deflate_codec_tests`.
</verification>

<success_criteria>
- Deflate round-trip through `compress_payload`/`decompress_payload` passes.
- Bad deflate input and exact-size mismatches return `error_code::decompression_failure`.
- `libdeflate` remains private to implementation files.
</success_criteria>

<output>
After completion, create `.planning/phases/03-compression-services-and-policy/03-02-SUMMARY.md`.
</output>
