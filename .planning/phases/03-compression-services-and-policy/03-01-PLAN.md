---
phase: 03-compression-services-and-policy
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - include/libbsa/compression.hpp
  - src/compression.cpp
  - tests/compression_policy_tests.cpp
  - CMakeLists.txt
autonomous: true
requirements: [CMP-04, CMP-05]
must_haves:
  truths:
    - "Maintainer can prove codec routing is explicit and format-aware."
    - "Writer code can request archive-default, force-compressed, or force-raw policy without choosing a third-party codec directly."
  artifacts:
    - path: "include/libbsa/compression.hpp"
      provides: "Public libbsa-owned compression policy and routing declarations"
      exports: ["compression_algorithm", "compression_policy", "payload_codec_request", "resolve_payload_codec", "resolve_write_compression"]
    - path: "src/compression.cpp"
      provides: "Initial routing and writer policy implementation"
    - path: "tests/compression_policy_tests.cpp"
      provides: "Routing and policy unit coverage"
  key_links:
    - from: "include/libbsa/compression.hpp"
      to: "include/libbsa/archive.hpp"
      via: "archive_format and compression_state inputs"
      pattern: "archive_format.*compression_state"
    - from: "tests/compression_policy_tests.cpp"
      to: "src/compression.cpp"
      via: "resolve_payload_codec / resolve_write_compression assertions"
      pattern: "resolve_(payload_codec|write_compression)"
---

<objective>
Define the libbsa-owned compression contracts that later archive readers and writers use to route payloads.

Purpose: Prevent codec confusion by making archive format, entry compression state, Starfield `CompressionMethod`, and writer policy explicit before codec wrappers are wired in.
Output: Public compression API, routing implementation, CMake test target, and routing/policy tests.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-compression-services-and-policy/03-RESEARCH.md
@include/libbsa/archive.hpp
@include/libbsa/result.hpp
@CMakeLists.txt

<interfaces>
From `include/libbsa/archive.hpp`:
```cpp
enum class archive_format { tes3_bsa, tes4_bsa, fo3_bsa, sse_bsa, fo4_ba2_gnrl, fo4_ba2_dds, starfield_ba2_gnrl, starfield_ba2_dds };
enum class compression_state { none, raw, deflate, lz4_frame, lz4_block, archive_default, unknown };
struct archive_summary { archive_format format{}; std::optional<std::uint32_t> version; std::optional<std::uint32_t> subtype; std::optional<std::uint32_t> flags; std::optional<std::uint32_t> folder_count; std::optional<std::uint32_t> file_count; std::optional<std::uint64_t> file_table_offset; std::optional<std::uint32_t> compression_method; };
```

From `include/libbsa/result.hpp`:
```cpp
enum class error_code { unsupported_format, malformed_archive, io_failure, decompression_failure };
template <class T> class result;
template <class T> result<T> success(T value);
template <class T> result<T> failure(libbsa::error err);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing routing and writer-policy tests</name>
  <files>tests/compression_policy_tests.cpp, CMakeLists.txt</files>
  <read_first>tests/detection_tests.cpp, CMakeLists.txt, include/libbsa/archive.hpp, include/libbsa/result.hpp</read_first>
  <behavior>
    - `resolve_payload_codec({sse_bsa, lz4_frame, nullopt})` returns `compression_algorithm::lz4_frame`.
    - `resolve_payload_codec({starfield_ba2_gnrl, archive_default, 3})` returns `compression_algorithm::lz4_block`.
    - `resolve_payload_codec({fo4_ba2_gnrl, deflate, nullopt})` returns `compression_algorithm::deflate`.
    - `resolve_payload_codec({starfield_ba2_gnrl, archive_default, 0})` returns `compression_algorithm::deflate`.
    - `resolve_payload_codec({fo4_ba2_gnrl, lz4_block, nullopt})` fails with `error_code::unsupported_format` and message `unsupported compression route`.
    - `resolve_write_compression(archive_format::sse_bsa, compression_policy::force_compressed, false)` returns `compression_state::lz4_frame`.
    - `resolve_write_compression(archive_format::fo4_ba2_gnrl, compression_policy::force_raw, true)` returns `compression_state::raw`.
  </behavior>
  <action>Create `tests/compression_policy_tests.cpp` using Catch2 and helper assertions matching the behavior list above. Register a `libbsa_compression_policy_tests` executable in `CMakeLists.txt`, link `libbsa::libbsa` and `Catch2::Catch2WithMain`, and discover it with `TEST_PREFIX "libbsa_compression_policy_tests."` and labels `unit;codec`. Run the targeted test command and confirm it fails because `include/libbsa/compression.hpp` and the routing functions do not exist yet.</action>
  <verify>
    <automated>cmake --build build/local-vs2026-vcpkg --config Debug --target libbsa_compression_policy_tests</automated>
  </verify>
  <acceptance_criteria>
    - `tests/compression_policy_tests.cpp` contains `resolve_payload_codec`.
    - `tests/compression_policy_tests.cpp` contains `CompressionMethod == 3` or `compression_method` assertions for raw LZ4 block routing.
    - `CMakeLists.txt` contains `add_executable(libbsa_compression_policy_tests tests/compression_policy_tests.cpp)`.
    - The targeted build fails before implementation because compression declarations are absent.
  </acceptance_criteria>
  <done>RED state captured: routing and writer-policy tests exist and fail for missing implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement public compression routing contracts</name>
  <files>include/libbsa/compression.hpp, src/compression.cpp, CMakeLists.txt</files>
  <read_first>include/libbsa/archive.hpp, include/libbsa/result.hpp, CMakeLists.txt, tests/compression_policy_tests.cpp</read_first>
  <action>Create `include/libbsa/compression.hpp` with Doxygen comments and only libbsa/std includes. Add `enum class compression_algorithm { none, deflate, lz4_frame, lz4_block };`, `enum class compression_policy { archive_default, force_compressed, force_raw };`, and `struct payload_codec_request { archive_format format{}; compression_state entry_state{compression_state::archive_default}; std::optional<std::uint32_t> compression_method; };`. Declare `result<compression_algorithm> resolve_payload_codec(const payload_codec_request& request);` and `result<compression_state> resolve_write_compression(archive_format format, compression_policy policy, bool archive_default_compressed);`. Implement `src/compression.cpp`: `none`/`raw` map to `compression_algorithm::none`; explicit `deflate`, `lz4_frame`, `lz4_block` map only where supported by the target format; Starfield BA2 with `entry_state == archive_default` and `compression_method == 3` maps to `lz4_block`; Starfield BA2 default with no method or method `0` maps to `deflate`; SSE default/compressed maps to `lz4_frame`; TES4/FO3/FO4 BA2 default/compressed maps to `deflate`; unsupported combinations fail with `error_code::unsupported_format` and message `unsupported compression route`. Add `src/compression.cpp` and `include/libbsa/compression.hpp` to the explicit `target_sources(libbsa ...)` lists.</action>
  <verify>
    <automated>cmake --build build/local-vs2026-vcpkg --config Debug && ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug -R libbsa_compression_policy_tests</automated>
  </verify>
  <acceptance_criteria>
    - `include/libbsa/compression.hpp` contains `enum class compression_algorithm`.
    - `include/libbsa/compression.hpp` contains `enum class compression_policy`.
    - `src/compression.cpp` contains `unsupported compression route`.
    - `src/compression.cpp` contains a branch that checks `compression_method` for value `3`.
    - Targeted compression policy tests pass.
  </acceptance_criteria>
  <done>Routing and writer policy APIs compile, pass tests, and expose no third-party codec headers publicly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| archive metadata→codec router | Untrusted archive header flags, entry states, and BA2 compression methods select a decompressor. |
| writer option→archive policy | Caller policy choices are converted to concrete archive compression states. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | T | `resolve_payload_codec` | mitigate | Reject unsupported format/state/method combinations with `error_code::unsupported_format`; do not infer from path or extension. |
| T-03-01-02 | D | `resolve_write_compression` | mitigate | Resolve only supported archive policies and return structured failure for unsupported targets. |
</threat_model>

<verification>
Run `cmake --build build/local-vs2026-vcpkg --config Debug && ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug -R libbsa_compression_policy_tests`.
</verification>

<success_criteria>
- Public compression routing contracts exist in `include/libbsa/compression.hpp`.
- Policy tests prove deflate, LZ4 frame, and raw LZ4 block routing decisions are explicit.
- Writer policy resolution covers archive-default, force-compressed, and force-raw choices where supported.
</success_criteria>

<output>
After completion, create `.planning/phases/03-compression-services-and-policy/03-01-SUMMARY.md`.
</output>
