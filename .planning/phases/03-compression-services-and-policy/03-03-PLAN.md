---
phase: 03-compression-services-and-policy
plan: 03
type: tdd
wave: 3
depends_on: [03-02]
files_modified:
  - src/compression/lz4_frame_codec.hpp
  - src/compression/lz4_frame_codec.cpp
  - src/compression/lz4_block_codec.hpp
  - src/compression/lz4_block_codec.cpp
  - src/compression.cpp
  - tests/lz4_codec_tests.cpp
  - CMakeLists.txt
autonomous: true
requirements: [CMP-02, CMP-03, CMP-04]
must_haves:
  truths:
    - "Consumer-facing extraction code can decompress Skyrim SE/AE LZ4 frame payloads."
    - "Consumer-facing extraction code can decompress Starfield BA2 v3 raw LZ4 block payloads when `CompressionMethod == 3`."
    - "Maintainer can prove LZ4 frame and raw block paths cannot be accidentally interchanged."
  artifacts:
    - path: "src/compression/lz4_frame_codec.cpp"
      provides: "Internal LZ4 frame wrapper"
    - path: "src/compression/lz4_block_codec.cpp"
      provides: "Internal raw LZ4 block wrapper"
    - path: "tests/lz4_codec_tests.cpp"
      provides: "LZ4 frame/block round-trip and cross-route rejection tests"
  key_links:
    - from: "src/compression.cpp"
      to: "src/compression/lz4_frame_codec.cpp"
      via: "dispatcher calls frame helper for `compression_algorithm::lz4_frame`"
      pattern: "compression_algorithm::lz4_frame"
    - from: "src/compression.cpp"
      to: "src/compression/lz4_block_codec.cpp"
      via: "dispatcher calls raw block helper for `compression_algorithm::lz4_block`"
      pattern: "compression_algorithm::lz4_block"
---

<objective>
Implement separate LZ4 frame and raw block codecs and wire them through the public compression dispatcher.

Purpose: Keep Skyrim SE/AE BSA frame payload handling distinct from Starfield BA2 v3 raw block handling so future readers cannot confuse the APIs.
Output: Internal LZ4 frame/block wrappers, dispatcher integration, and route-crossfeed tests.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-compression-services-and-policy/03-RESEARCH.md
@include/libbsa/compression.hpp
@src/compression.cpp
@src/compression/deflate_codec.cpp
@CMakeLists.txt

<interfaces>
From Plan 03-02 expected `include/libbsa/compression.hpp`:
```cpp
enum class compression_algorithm { none, deflate, lz4_frame, lz4_block };
result<std::vector<std::byte>> decompress_payload(compression_algorithm algorithm, std::span<const std::byte> packed, std::uint64_t expected_size);
result<std::vector<std::byte>> compress_payload(compression_algorithm algorithm, std::span<const std::byte> unpacked);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing LZ4 frame/block and cross-route tests</name>
  <files>tests/lz4_codec_tests.cpp, CMakeLists.txt</files>
  <read_first>tests/deflate_codec_tests.cpp, include/libbsa/compression.hpp, CMakeLists.txt</read_first>
  <behavior>
    - `compression_algorithm::lz4_frame` compresses bytes `{0x10, 0x20, 0x20, 0x30}` into an LZ4 frame and decompresses them back with expected size `4`.
    - LZ4 frame compressed bytes begin with frame magic bytes `04 22 4D 18`.
    - `compression_algorithm::lz4_block` compresses the same bytes into a raw LZ4 block and decompresses them back with expected size `4`.
    - Raw LZ4 block compressed bytes are not accepted by `compression_algorithm::lz4_frame` decompression.
    - LZ4 frame compressed bytes are not accepted by `compression_algorithm::lz4_block` decompression.
    - Decompressing a valid raw LZ4 block with expected size `5` fails with `error_code::decompression_failure` and message `lz4 block size mismatch`.
  </behavior>
  <action>Create `tests/lz4_codec_tests.cpp` using public `compress_payload` and `decompress_payload`. Register `libbsa_lz4_codec_tests` in `CMakeLists.txt`, link `libbsa::libbsa` and `Catch2::Catch2WithMain`, and discover it with `TEST_PREFIX "libbsa_lz4_codec_tests."` and labels `unit;codec`. Run the targeted build/test and confirm RED because LZ4 dispatcher cases or internal wrappers do not exist yet.</action>
  <verify>
    <automated>cmake --build build/local-vs2026-vcpkg --config Debug --target libbsa_lz4_codec_tests</automated>
  </verify>
  <acceptance_criteria>
    - `tests/lz4_codec_tests.cpp` contains `compression_algorithm::lz4_frame`.
    - `tests/lz4_codec_tests.cpp` contains `compression_algorithm::lz4_block`.
    - `tests/lz4_codec_tests.cpp` contains frame magic bytes `0x04`, `0x22`, `0x4d`, and `0x18`.
    - `CMakeLists.txt` contains `add_executable(libbsa_lz4_codec_tests tests/lz4_codec_tests.cpp)`.
    - The targeted build or test fails before implementation.
  </acceptance_criteria>
  <done>RED state captured for LZ4 frame, raw block, and cross-route rejection behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement LZ4 frame and raw block wrappers</name>
  <files>src/compression/lz4_frame_codec.hpp, src/compression/lz4_frame_codec.cpp, src/compression/lz4_block_codec.hpp, src/compression/lz4_block_codec.cpp, src/compression.cpp, CMakeLists.txt</files>
  <read_first>include/libbsa/compression.hpp, src/compression.cpp, tests/lz4_codec_tests.cpp, CMakeLists.txt</read_first>
  <action>Create internal frame wrapper files that include `lz4frame.h` only internally. Implement `lz4_frame_compress` using `LZ4F_compressFrameBound` and `LZ4F_compressFrame`, returning `error_code::decompression_failure` with message `lz4 frame compression failed` if `LZ4F_isError` reports failure. Implement `lz4_frame_decompress` with an `LZ4F_decompressionContext_t`, `LZ4F_createDecompressionContext`, `LZ4F_decompress`, and `LZ4F_freeDecompressionContext`, allocating exactly `expected_size` bytes and requiring all input consumed, `ret == 0`, and actual output equal to expected size; bad frame data returns `lz4 frame decompression failed`, exact-size mismatch returns `lz4 frame size mismatch`. Create internal raw block wrapper files that include `lz4.h` only internally. Implement `lz4_block_compress` using `LZ4_compressBound` and `LZ4_compress_default`, and implement `lz4_block_decompress` using `LZ4_decompress_safe`, requiring returned byte count equals `expected_size`; failures return `lz4 block decompression failed`, exact-size mismatch returns `lz4 block size mismatch`. Wire `compression_algorithm::lz4_frame` and `compression_algorithm::lz4_block` in `src/compression.cpp`. Add the new `.cpp` files to `target_sources(libbsa PRIVATE ...)`.</action>
  <verify>
    <automated>cmake --build build/local-vs2026-vcpkg --config Debug && ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug -R libbsa_lz4_codec_tests</automated>
  </verify>
  <acceptance_criteria>
    - `src/compression/lz4_frame_codec.cpp` contains `LZ4F_compressFrame` and `LZ4F_decompress`.
    - `src/compression/lz4_block_codec.cpp` contains `LZ4_compress_default` and `LZ4_decompress_safe`.
    - `include/libbsa/compression.hpp` does not contain `lz4` or `LZ4`.
    - `src/compression.cpp` contains branches for `compression_algorithm::lz4_frame` and `compression_algorithm::lz4_block`.
    - Targeted LZ4 codec tests pass.
  </acceptance_criteria>
  <done>LZ4 frame and raw block payloads round-trip through separate helpers and cross-route rejection tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| archive payload→LZ4 frame API | Untrusted bytes are decoded as an LZ4 frame. |
| archive payload→LZ4 raw block API | Untrusted bytes are decoded as a raw LZ4 block. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | T | `lz4_frame_decompress` / `lz4_block_decompress` | mitigate | Use safe LZ4 APIs, require exact output size, and test cross-route rejection. |
| T-03-03-02 | D | LZ4 decompression buffers | accept | Whole-buffer allocation follows archive metadata in this phase; later streaming extraction phases control payload chunking. |
</threat_model>

<verification>
Run `cmake --build build/local-vs2026-vcpkg --config Debug && ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug -R libbsa_lz4_codec_tests`.
</verification>

<success_criteria>
- LZ4 frame round-trip passes through `compression_algorithm::lz4_frame`.
- Raw LZ4 block round-trip passes through `compression_algorithm::lz4_block`.
- Cross-route tests prove frame and raw block bytes are rejected by the wrong decoder.
</success_criteria>

<output>
After completion, create `.planning/phases/03-compression-services-and-policy/03-03-SUMMARY.md`.
</output>
