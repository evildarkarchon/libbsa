---
phase: 03-compression-services-and-policy
plan: 04
type: execute
wave: 4
depends_on: [03-03]
files_modified:
  - tests/public_header_smoke.cpp
  - README.md
autonomous: true
requirements: [CMP-01, CMP-02, CMP-03, CMP-04, CMP-05]
must_haves:
  truths:
    - "Consumer can include the compression public header without third-party codec headers."
    - "Maintainer can run documented commands that prove all compression routes and boundary gates pass."
  artifacts:
    - path: "tests/public_header_smoke.cpp"
      provides: "Consumer-style include/link smoke coverage for compression API"
    - path: "README.md"
      provides: "Compression API, routing, and verification documentation"
  key_links:
    - from: "tests/public_header_smoke.cpp"
      to: "include/libbsa/compression.hpp"
      via: "consumer-style include and simple API use"
      pattern: "#include <libbsa/compression.hpp>"
    - from: "README.md"
      to: "CTest codec labels"
      via: "documented validation commands"
      pattern: "codec"
---

<objective>
Finalize Phase 03 consumer smoke coverage, documentation, and boundary gates.

Purpose: Prove compression services are usable through libbsa public headers while codec libraries and TES5Edit remain private/read-only.
Output: Updated smoke target, README section, and final verification commands.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-compression-services-and-policy/03-RESEARCH.md
@include/libbsa/compression.hpp
@tests/public_header_smoke.cpp
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand public-header smoke for compression API</name>
  <files>tests/public_header_smoke.cpp</files>
  <read_first>tests/public_header_smoke.cpp, include/libbsa/compression.hpp, include/libbsa/archive.hpp</read_first>
  <action>Add `#include <libbsa/compression.hpp>` to `tests/public_header_smoke.cpp`. In `main`, create a `libbsa::payload_codec_request` for `archive_format::starfield_ba2_gnrl`, `compression_state::archive_default`, and `compression_method = 3`, call `libbsa::resolve_payload_codec`, and require the result value to be `libbsa::compression_algorithm::lz4_block`. Also call `libbsa::resolve_write_compression(libbsa::archive_format::sse_bsa, libbsa::compression_policy::force_compressed, false)` and require `libbsa::compression_state::lz4_frame`. Keep the smoke executable consumer-style: it may include only public `libbsa/...` headers and standard library headers.</action>
  <verify>
    <automated>cmake --build build/local-vs2026-vcpkg --config Debug --target libbsa_public_header_smoke && ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug -R libbsa.public_header_smoke</automated>
  </verify>
  <acceptance_criteria>
    - `tests/public_header_smoke.cpp` contains `#include <libbsa/compression.hpp>`.
    - `tests/public_header_smoke.cpp` contains `compression_algorithm::lz4_block`.
    - `tests/public_header_smoke.cpp` contains `compression_policy::force_compressed`.
    - Public header smoke test passes.
  </acceptance_criteria>
  <done>Compression API is covered by the existing consumer-style smoke target.</done>
</task>

<task type="auto">
  <name>Task 2: Document compression routing and validation commands</name>
  <files>README.md</files>
  <read_first>README.md, include/libbsa/compression.hpp, .planning/phases/03-compression-services-and-policy/03-RESEARCH.md</read_first>
  <action>Update `README.md` with a `Compression services` section that states: deflate is used for TES4/FO3/FNV/FO4 BA2/Starfield deflate payloads; LZ4 frame is used for Skyrim SE/AE BSA payloads; raw LZ4 block is used for Starfield BA2 v3 when `CompressionMethod == 3`; writer policy supports `archive_default`, `force_compressed`, and `force_raw` where the target format supports that state. Add validation commands for the codec tests: `ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug -L codec`, full suite command, public-header forbidden-token gate, and `git status --short TES5Edit`. State that `libdeflate` and LZ4 headers remain private implementation details and must not appear in public headers.</action>
  <verify>
    <automated>cmake --build build/local-vs2026-vcpkg --config Debug && ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug -L codec</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` contains `Compression services`.
    - `README.md` contains `CompressionMethod == 3`.
    - `README.md` contains `ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug -L codec`.
    - `README.md` contains `git status --short TES5Edit`.
    - Codec-labeled tests pass.
  </acceptance_criteria>
  <done>README documents compression routing, policy choices, and exact verification commands.</done>
</task>

<task type="auto">
  <name>Task 3: Run final compression boundary gates</name>
  <files>include/libbsa/compression.hpp, tests/public_header_smoke.cpp, README.md</files>
  <read_first>include/libbsa/compression.hpp, tests/public_header_smoke.cpp, README.md, CMakeLists.txt</read_first>
  <action>Run final gates without editing `TES5Edit/`: full build and CTest; codec label CTest; smoke CTest; public-header forbidden-token gate for `libdeflate`, `lz4.h`, `lz4frame.h`, `LZ4`, and `DirectXTex` in `include/libbsa`; CMake glob gate excluding comments by searching for `GLOB`/`GLOB_RECURSE` in non-comment lines; and `git status --short TES5Edit`. If a gate fails because of code from this phase, fix the phase files and rerun all gates. Do not modify, format, stage, or commit anything under `TES5Edit/`.</action>
  <verify>
    <automated>cmake --build build/local-vs2026-vcpkg --config Debug && ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug && git status --short TES5Edit</automated>
  </verify>
  <acceptance_criteria>
    - Full CTest passes.
    - `ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug -L codec` passes.
    - Public-header forbidden-token gate finds no third-party codec tokens in `include/libbsa` outside comments/prose.
    - `git status --short TES5Edit` prints no modified files.
  </acceptance_criteria>
  <done>Phase 03 completes with passing codec, smoke, full-suite, public-header, CMake, and TES5Edit boundary gates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public API→implementation dependencies | Consumer includes public headers while implementation links native codec libraries privately. |
| repository→TES5Edit submodule | Planning/execution may inspect read-only reference files but must not mutate them. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | I | public headers | mitigate | Run forbidden-token gates so libdeflate/LZ4/DirectXTex implementation details do not leak into public headers. |
| T-03-04-02 | T | `TES5Edit/` | mitigate | Run `git status --short TES5Edit` and require no output. |
</threat_model>

<verification>
Run full build, full CTest, codec label tests, smoke tests, forbidden-token public-header gate, CMake glob gate, and `git status --short TES5Edit`.
</verification>

<success_criteria>
- Compression API appears in public-header smoke coverage.
- README documents the exact deflate/LZ4 frame/raw block routing rules and writer policy choices.
- All final verification gates pass with no `TES5Edit/` modifications.
</success_criteria>

<output>
After completion, create `.planning/phases/03-compression-services-and-policy/03-04-SUMMARY.md`.
</output>
