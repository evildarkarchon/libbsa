---
phase: 04-tes3-bsa-read-extract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/CMakeLists.txt
  - tests/fixtures/README.md
  - tests/fixtures/generated/generate_tes3_bsa_fixtures.cpp
  - tests/fixtures/generated/archives/tes3_success.bsa
  - tests/fixtures/generated/archives/tes3_success_manifest.json
  - tests/fixtures/generated/archives/tes3_malformed_manifest.json
  - tests/unit/tes3_bsa_reader_tests.cpp
autonomous: true
requirements: [BSA-04, BSA-08]
must_haves:
  truths:
    - "Maintainer can regenerate one rich legal TES3 success fixture from repository-owned code per D-22, D-23, and D-24."
    - "Maintainer can run TES3 reader tests that describe open, listing, lookup, extraction, and malformed behavior before parser implementation."
    - "Fixture manifests prove public archive-absolute payload offsets and raw TES3 data-section-relative offsets separately per D-01, D-02, D-03, and D-09."
    - "D-13: TES3 fixture manifests include the stored 64-bit archive hash plus low/high 32-bit halves for sort-order expectations."
    - "D-18: Malformed hash fixtures cover stored hash mismatch, duplicate hash collision, and unsorted hash/order records."
    - "D-19: Hash-validation tests assert stable error codes only and do not compare diagnostic text."
    - "D-25: Malformed fixture coverage includes the SPEC set plus strict hash and raw-offset regression cases."
  artifacts:
    - path: "tests/fixtures/generated/generate_tes3_bsa_fixtures.cpp"
      provides: "TES3 success and malformed fixture generator"
      contains: "raw_tes3_data_offset"
    - path: "tests/fixtures/generated/archives/tes3_success_manifest.json"
      provides: "Rich TES3 success fixture contract"
      contains: "hash_low32"
    - path: "tests/unit/tes3_bsa_reader_tests.cpp"
      provides: "RED behavior tests for Phase 4"
      contains: "tes3_bsa_extract"
  key_links:
    - from: "tests/unit/tes3_bsa_reader_tests.cpp"
      to: "tests/fixtures/generated/archives/tes3_success_manifest.json"
      via: "manifest-driven metadata, lookup, offset, hash, and payload assertions"
      pattern: "tes3_success_manifest"
    - from: "tests/CMakeLists.txt"
      to: "tests/fixtures/generated/generate_tes3_bsa_fixtures.cpp"
      via: "generate_tes3_bsa_fixtures_tool target"
      pattern: "generate_tes3_bsa_fixtures"
---

<objective>
Create the Phase 4 fixture and test contract for TES3/Morrowind BSA read/extract behavior.

Purpose: The parser and extraction tasks need concrete generated bytes and RED tests that encode locked TES3 offset, hash, path, and malformed-input decisions before implementation.
Output: A TES3 fixture generator, committed success/malformed outputs, CMake wiring, fixture documentation, and fixture-backed TES3 reader tests.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-tes3-bsa-read-extract/04-SPEC.md
@.planning/phases/04-tes3-bsa-read-extract/04-CONTEXT.md
@.planning/phases/04-tes3-bsa-read-extract/04-RESEARCH.md
@.planning/phases/04-tes3-bsa-read-extract/04-VALIDATION.md
@.planning/phases/04-tes3-bsa-read-extract/04-PATTERNS.md
@tests/fixtures/generated/generate_tes4_bsa_fixtures.cpp
@tests/unit/tes4_bsa_reader_tests.cpp
@tests/CMakeLists.txt
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add TES3 fixture generator and CMake target</name>
  <files>tests/fixtures/generated/generate_tes3_bsa_fixtures.cpp, tests/CMakeLists.txt, tests/fixtures/README.md</files>
  <read_first>tests/fixtures/generated/generate_tes3_bsa_fixtures.cpp; tests/CMakeLists.txt; tests/fixtures/README.md; tests/fixtures/generated/generate_tes4_bsa_fixtures.cpp; .planning/phases/04-tes3-bsa-read-extract/04-SPEC.md; .planning/phases/04-tes3-bsa-read-extract/04-CONTEXT.md; .planning/phases/04-tes3-bsa-read-extract/04-RESEARCH.md; .planning/phases/04-tes3-bsa-read-extract/04-PATTERNS.md</read_first>
  <behavior>
    - Generator writes one rich TES3 success archive plus TES3 malformed archive cases from synthetic repository-owned bytes only.
    - D-13: Success manifest includes `path`, `original_path`, `raw_tes3_data_offset`, `payload_offset`, `archive_hash`, `hash_low32`, `hash_high32`, `raw_size`, `stored_size`, `compression`, `has_embedded_name`, `embedded_name_prefix_size`, `lookup_variants`, and expected payload hex.
    - Generator uses `libbsa::detail::hash_tes3` for stored TES3 hashes; it does not copy or mutate TES5Edit bytes.
  </behavior>
  <action>Create `tests/fixtures/generated/generate_tes3_bsa_fixtures.cpp` following the TES4 generator byte-buffer/provenance style but using TES3 layout: 12-byte header with magic/version `0x00000100`, `hash_offset_minus_header`, `file_count`; `file_count` pairs of `{size, raw_data_section_offset}`; `file_count` name offsets; null-terminated names; `file_count` stored hashes sorted by TES3 low32 then high32; raw uncompressed payload bytes. Per D-22/D-23, generate exactly one rich success archive with representative Morrowind-like names such as `Meshes/Tiny/Probe.nif`, `textures/tx_probe.dds`, `Sound\\Fx\\Ping.wav`, and a zero-byte entry. Per D-24, add a separate CMake executable `generate_tes3_bsa_fixtures_tool` and custom target `generate_tes3_bsa_fixtures` with explicit BYPRODUCTS under `tests/fixtures/generated/archives`. Update `tests/fixtures/README.md` with the generator command, manifest schema, legal provenance, and the rule that `TES5Edit/` is reference-only.</action>
  <verify><automated>cmake --build --preset windows-msvc-debug-static --target generate_tes3_bsa_fixtures && pwsh -NoProfile -Command '$required = @("tests/fixtures/generated/archives/tes3_success.bsa", "tests/fixtures/generated/archives/tes3_success_manifest.json", "tests/fixtures/generated/archives/tes3_malformed_manifest.json"); foreach ($path in $required) { if (-not (Test-Path -LiteralPath $path)) { throw "Missing $path" } }; $json = Get-Content -Raw -LiteralPath "tests/fixtures/generated/archives/tes3_success_manifest.json"; foreach ($token in @("raw_tes3_data_offset", "payload_offset", "archive_hash", "hash_low32", "hash_high32")) { if ($json -notmatch $token) { throw "Manifest missing $token" } }'</automated></verify>
  <acceptance_criteria>`grep -R "generate_tes3_bsa_fixtures_tool" tests/CMakeLists.txt` finds the target; `grep -R "raw_tes3_data_offset" tests/fixtures/generated/generate_tes3_bsa_fixtures.cpp tests/fixtures/generated/archives/tes3_success_manifest.json` finds both generator and manifest fields; `grep -R "synthetic strings generated for libbsa tests; no game or TES5Edit bytes copied" tests/fixtures/generated/archives/tes3_success_manifest.json tests/fixtures/README.md` proves provenance is documented; `git -C TES5Edit status --short` produces no output.</acceptance_criteria>
  <done>TES3 fixture generation is wired, produces committed success/malformed contracts, and documents legal provenance without touching TES5Edit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add RED TES3 reader tests for open, entries, lookup, and extraction</name>
  <files>tests/unit/tes3_bsa_reader_tests.cpp, tests/CMakeLists.txt</files>
  <read_first>tests/unit/tes3_bsa_reader_tests.cpp; tests/CMakeLists.txt; tests/unit/tes4_bsa_reader_tests.cpp; tests/fixtures/generated/archives/tes3_success_manifest.json; .planning/phases/04-tes3-bsa-read-extract/04-SPEC.md; .planning/phases/04-tes3-bsa-read-extract/04-CONTEXT.md; .planning/phases/04-tes3-bsa-read-extract/04-VALIDATION.md; .planning/phases/04-tes3-bsa-read-extract/04-PATTERNS.md</read_first>
  <behavior>
    - Tests assert `archive_type::bsa`, `archive_variant::tes3`, version/header identity `0x00000100`, file count, and `entry_compression::none` for all TES3 entries.
    - Tests compare `entries()` against manifest canonical paths, original `/` paths, raw/stored sizes, archive-absolute `payload_offset`, stored `archive_hash`, embedded-name fields, and deterministic canonical sorting.
    - Tests verify `find()` and `contains()` with original spelling, case variation, slash/backslash variation, valid missing path, and invalid path syntax.
    - Tests verify `extract(path, sink)` and `extract_bytes(path)` byte-compare expected payloads and read archive bytes at asserted absolute offsets per D-09.
  </behavior>
  <action>Create `tests/unit/tes3_bsa_reader_tests.cpp` by mirroring `tests/unit/tes4_bsa_reader_tests.cpp` helper style: generated archive directory helper, JSON manifest reader, hex-to-byte helpers, `collecting_sink`, and stable error-code mapping. Register it in `tests/CMakeLists.txt`. Add Catch2 cases tagged exactly with `[unit][fixture][tes3_bsa_metadata]`, `[unit][fixture][tes3_bsa_entries]`, `[unit][fixture][tes3_bsa_lookup]`, and `[unit][fixture][tes3_bsa_extract]`. Use the existing public `archive_reader` API only: `archive_reader::open`, `metadata()`, `entries()`, `find()`, `contains()`, `extract()`, and `extract_bytes()`. Per D-01/D-02/D-03/D-09, assert public `payload_offset` equals manifest archive-absolute offset, `raw_tes3_data_offset` remains manifest-only, the raw offset `0` entry has public offset equal to `data_section_start`, and file bytes at `payload_offset` match the expected payload prefix.</action>
  <verify><automated>pwsh -NoProfile -Command 'cmake --build --preset windows-msvc-debug-static; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; ctest --preset windows-msvc-debug-static -L "tes3_bsa_metadata|tes3_bsa_entries|tes3_bsa_lookup|tes3_bsa_extract" --output-on-failure; if ($LASTEXITCODE -eq 0) { throw "TES3 RED tests unexpectedly passed before implementation" }; exit 0'</automated></verify>
  <acceptance_criteria>`grep -R "tes3_bsa_metadata" tests/unit/tes3_bsa_reader_tests.cpp` finds a Catch2 tag; `grep -R "raw_tes3_data_offset" tests/unit/tes3_bsa_reader_tests.cpp` finds offset proof assertions; `grep -R "archive_hash" tests/unit/tes3_bsa_reader_tests.cpp` finds neutral hash assertions; `grep -R "archive_reader::open" tests/unit/tes3_bsa_reader_tests.cpp` confirms public API usage; RED ctest exits non-zero before implementation and is treated as expected by the verify command.</acceptance_criteria>
  <done>Open/list/lookup/extract behavior is expressed as failing TES3 public API tests with manifest-backed assertions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add RED malformed TES3 tests</name>
  <files>tests/unit/tes3_bsa_reader_tests.cpp, tests/fixtures/generated/archives/tes3_malformed_manifest.json</files>
  <read_first>tests/unit/tes3_bsa_reader_tests.cpp; tests/fixtures/generated/archives/tes3_malformed_manifest.json; .planning/phases/04-tes3-bsa-read-extract/04-SPEC.md; .planning/phases/04-tes3-bsa-read-extract/04-CONTEXT.md; .planning/phases/04-tes3-bsa-read-extract/04-VALIDATION.md</read_first>
  <behavior>
    - D-19: Malformed tests assert stable `error_code::format_error` or `error_code::unsupported`, never diagnostic text.
    - D-18: Cases cover stored hash mismatch, duplicate hash collision, and unsorted hash/order records.
    - D-25: Cases cover truncated structures, invalid name spans, invalid payload spans, duplicate canonical paths, inconsistent counts/offsets, strict hash cases, and raw-offset-as-absolute regression.
    - Corrupted payload span extraction fails without reporting partial success to the caller sink.
  </behavior>
  <action>Extend `tests/unit/tes3_bsa_reader_tests.cpp` with a manifest-driven malformed test tagged `[unit][fixture][malformed][tes3_bsa_malformed]`. Read `tests/fixtures/generated/archives/tes3_malformed_manifest.json`; for each case open the named archive through `archive_reader::open` and assert only the expected `error_code`. Include malformed manifest case names exactly: `tes3_truncated_header`, `tes3_truncated_records`, `tes3_invalid_name_span`, `tes3_invalid_payload_span`, `tes3_duplicate_canonical_path`, `tes3_inconsistent_counts_offsets`, `tes3_stored_hash_mismatch`, `tes3_hash_collision`, `tes3_unsorted_hash_records`, and `tes3_raw_offset_absolute_regression`. Add a sink-failure guard test for any case that opens enough to call extraction only if the generator includes a deliberately corrupt extraction fixture; otherwise keep corrupt payload span as an open-time `format_error` per D-05.</action>
  <verify><automated>pwsh -NoProfile -Command 'cmake --build --preset windows-msvc-debug-static; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; ctest --preset windows-msvc-debug-static -L tes3_bsa_malformed --output-on-failure; if ($LASTEXITCODE -eq 0) { throw "TES3 malformed RED tests unexpectedly passed before implementation" }; exit 0'</automated></verify>
  <acceptance_criteria>`grep -R "tes3_stored_hash_mismatch" tests/unit/tes3_bsa_reader_tests.cpp tests/fixtures/generated/archives/tes3_malformed_manifest.json` finds both the test and manifest case; `grep -R "error_code_from_manifest" tests/unit/tes3_bsa_reader_tests.cpp` or equivalent stable-code mapping exists; a grep for `message` in TES3 malformed assertions does not show diagnostic-string comparisons; RED ctest exits non-zero before implementation and is treated as expected by the verify command.</acceptance_criteria>
  <done>All mandatory malformed TES3 cases are represented as failing tests with stable error-code expectations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated archive bytes→future parser | Synthetic but untrusted BSA bytes define every parser trust surface for Phase 4. |
| manifest JSON→tests | Test-only JSON drives expected offsets, hashes, paths, and payload bytes. |
| malformed fixture bytes→open/extract tests | Malformed inputs exercise fail-closed behavior before implementation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | TES3 fixture generator | mitigate | Generate hashes with `detail::hash_tes3`, include hash halves and raw/absolute offsets in manifests, and verify generated tokens by CLI. |
| T-04-02 | Repudiation | fixture provenance | mitigate | Manifest and README state synthetic repository-owned source and no TES5Edit/game bytes copied. |
| T-04-03 | Tampering / DoS | malformed fixture set | mitigate | Include explicit cases for truncation, invalid spans, duplicate paths, hash mismatch/collision, unsorted hashes, and raw-offset regression. |
| T-04-04 | Information Disclosure | TES5Edit reference boundary | mitigate | Fixtures are generated outside `TES5Edit/`; validation includes `git -C TES5Edit status --short` cleanliness. |
</threat_model>

<verification>
Quick command: `ctest --preset windows-msvc-debug-static -L unit --output-on-failure`.
Full command: `ctest --preset windows-msvc-debug-static --output-on-failure`.
Plan-specific checks build `generate_tes3_bsa_fixtures`, verify committed fixture outputs, and expect RED TES3 tests until implementation plans run.
</verification>

<success_criteria>
The Phase 4 fixture/test foundation exists, uses legal generated bytes, proves raw-vs-absolute TES3 offsets in manifests/tests, includes all mandatory malformed cases, and leaves TES5Edit untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/04-tes3-bsa-read-extract/04-01-SUMMARY.md`
</output>
