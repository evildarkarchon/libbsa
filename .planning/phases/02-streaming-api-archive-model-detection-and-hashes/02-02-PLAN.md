---
phase: 02-streaming-api-archive-model-detection-and-hashes
plan: 02
type: tdd
wave: 2
depends_on: [02-01]
files_modified:
  - include/libbsa/archive.hpp
  - include/libbsa/detect.hpp
  - src/detect.cpp
  - tests/detection_tests.cpp
  - CMakeLists.txt
autonomous: true
requirements: [BIO-04, BIO-05, DPH-01]
must_haves:
  truths:
    - "D-05: Recognized magic with unsupported version returns structured unsupported_format."
    - "D-06: Truncated required header fields return structured malformed_archive immediately."
    - "D-07: Successful detection returns identity plus safely available bounded header fields."
    - "D-08: BA2 subtype is decided only from explicit subtype/header fields."
  artifacts:
    - path: "include/libbsa/archive.hpp"
      provides: "Public archive identity, summary, entry metadata, and compression state types"
    - path: "include/libbsa/detect.hpp"
      provides: "Public bounded archive detection API"
      exports: ["detect_archive"]
    - path: "src/detect.cpp"
      provides: "Strict header detection implementation"
    - path: "tests/detection_tests.cpp"
      provides: "Positive and negative detection coverage"
  key_links:
    - from: "include/libbsa/detect.hpp"
      to: "include/libbsa/io.hpp"
      via: "byte_source parameter"
      pattern: "byte_source"
    - from: "src/detect.cpp"
      to: "include/libbsa/result.hpp"
      via: "unsupported and malformed failures"
      pattern: "error_code::(unsupported_format|malformed_archive)"
---

<objective>
Add public archive metadata types and strict bounded format detection.

Purpose: Consumers need to identify TES3 BSA, TES4-family BSA, BA2 GNRL, and BA2 DDS headers and inspect safe header fields without table parsing or payload extraction.
Output: Archive metadata headers, detection implementation, detection tests, and CMake wiring.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-streaming-api-archive-model-detection-and-hashes/02-SPEC.md
@.planning/phases/02-streaming-api-archive-model-detection-and-hashes/02-CONTEXT.md
@.planning/phases/02-streaming-api-archive-model-detection-and-hashes/02-RESEARCH.md
@TES5Edit/Core/wbBSArchive.pas
@include/libbsa/result.hpp
@include/libbsa/io.hpp

<interfaces>
Plan 01 creates:
```cpp
class byte_source {
public:
  virtual std::uint64_t size() const noexcept = 0;
  virtual result<void> read_at(std::uint64_t offset, std::span<std::byte> destination) const = 0;
};
```

Reference constants from `TES5Edit/Core/wbBSArchive.pas`:
```text
MAGIC_TES3 = 00 01 00 00
MAGIC_BSA  = "BSA\0"
MAGIC_BTDX = "BTDX"
MAGIC_GNRL = "GNRL"
MAGIC_DX10 = "DX10"
TES4 versions: 0x67, 0x68, 0x69
BA2 versions: 0x01, 0x02, 0x03, 0x07, 0x08
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify archive metadata and detection behavior with failing tests</name>
  <files>tests/detection_tests.cpp, CMakeLists.txt</files>
  <read_first>
    - `TES5Edit/Core/wbBSArchive.pas` lines 232-339 and 500-530 — copy identity/version/subtype constants without modifying the file.
    - `include/libbsa/io.hpp` — build detection tests around `memory_source`.
    - `tests/io_tests.cpp` — copy byte construction and Catch2 style.
    - `.planning/phases/02-streaming-api-archive-model-detection-and-hashes/02-CONTEXT.md` — implement D-05 through D-08.
  </read_first>
  <behavior>
    - Test 1: TES3 sample `{00,01,00,00, hash_offset, file_count}` detects identity `tes3_bsa` and exposes `file_count`.
    - Test 2: `BSA\0` version `0x67`, `0x68`, and `0x69` samples detect `tes4_bsa`, `fo3_bsa`, and `sse_bsa` identities and expose flags/folder/file counts.
    - Test 3: `BTDX` versions `0x01`, `0x07`, `0x08` with subtype `GNRL` detect `fo4_ba2_gnrl`; subtype `DX10` detects `fo4_ba2_dds`.
    - Test 4: `BTDX` versions `0x02` and `0x03` detect Starfield BA2 identities and v3 exposes `compression_method` when present.
    - Test 5: unknown magic returns `unsupported_format`; recognized magic with unsupported version returns `unsupported_format`; truncated recognized headers return `malformed_archive`.
    - Test 6: BA2 subtype is not inferred from filenames or extensions because detection accepts only header bytes.
  </behavior>
  <action>Create `tests/detection_tests.cpp` first. Define byte-sample helpers using little-endian appends for `std::uint32_t` and `std::uint64_t`. Add `libbsa_detection_tests` to `CMakeLists.txt` and label with `unit`. Reference `detect_archive(memory_source)` from `<libbsa/detect.hpp>` and metadata enums from `<libbsa/archive.hpp>`. Do not implement production headers or `src/detect.cpp` in this task; the build must fail because those files do not exist yet.</action>
  <verify>
    <automated>cmake --build build/local-vs2026-vcpkg --config Debug</automated>
  </verify>
  <acceptance_criteria>
    - `tests/detection_tests.cpp` contains `detect_archive`.
    - `tests/detection_tests.cpp` contains `unsupported_format` and `malformed_archive` assertions.
    - `tests/detection_tests.cpp` contains `BTDX`, `GNRL`, and `DX10` sample construction.
    - `CMakeLists.txt` contains `add_executable(libbsa_detection_tests tests/detection_tests.cpp)`.
    - The automated build fails before Task 2 because detection headers are missing.
  </acceptance_criteria>
  <done>Failing tests lock D-05 through D-08 before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement metadata types and strict bounded detection</name>
  <files>include/libbsa/archive.hpp, include/libbsa/detect.hpp, src/detect.cpp, CMakeLists.txt, tests/detection_tests.cpp</files>
  <read_first>
    - `tests/detection_tests.cpp` — satisfy the exact tests from Task 1 without weakening them.
    - `include/libbsa/io.hpp` — use `byte_source::read_at` for bounded header reads.
    - `include/libbsa/result.hpp` — return structured success/failure.
    - `TES5Edit/Core/wbBSArchive.pas` lines 242-302 and 1079-1198 — preserve reference identity/version/subtype behavior.
  </read_first>
  <action>Create `include/libbsa/archive.hpp` with Doxygen comments and libbsa-owned value types: `enum class archive_format { tes3_bsa, tes4_bsa, fo3_bsa, sse_bsa, fo4_ba2_gnrl, fo4_ba2_dds, starfield_ba2_gnrl, starfield_ba2_dds }`, `enum class compression_state { none, raw, deflate, lz4_frame, lz4_block, archive_default, unknown }`, `struct archive_summary` with optional-like numeric fields for `version`, `subtype`, `flags`, `folder_count`, `file_count`, `file_table_offset`, and `compression_method`, plus `struct entry_metadata` with normalized path string, size, packed size, offset, hash fields, and compression state. Create `include/libbsa/detect.hpp` declaring `result<archive_summary> detect_archive(const byte_source& source)`. Implement `src/detect.cpp` using only bounded little-endian reads from `byte_source`; read the minimum required bytes for each recognized header. Return `unsupported_format` with message `unknown archive magic` for unknown magic, `unsupported archive version` for recognized magic unsupported version, `unsupported BA2 subtype` for `BTDX` with unsupported subtype, and `malformed_archive` with message `truncated archive header` when required fields are missing. Add new files to explicit CMake source/header lists.</action>
  <verify>
    <automated>cmake --build build/local-vs2026-vcpkg --config Debug && ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug -R libbsa_detection_tests</automated>
  </verify>
  <acceptance_criteria>
    - `include/libbsa/archive.hpp` contains `enum class archive_format`, `enum class compression_state`, `struct archive_summary`, and `struct entry_metadata`.
    - `include/libbsa/detect.hpp` contains `result<archive_summary> detect_archive(const byte_source& source)`.
    - `src/detect.cpp` contains `truncated archive header`, `unsupported archive version`, and `unsupported BA2 subtype`.
    - `src/detect.cpp` contains `GNRL` and `DX10` subtype checks.
    - `libbsa_detection_tests` passes.
  </acceptance_criteria>
  <done>Consumers can inspect archive identity and safe header summary fields without parsing archive tables or extracting payload data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Archive bytes → detector | Untrusted magic/version/subtype/header fields are parsed into public metadata. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | T/D | `detect_archive` | mitigate | Use bounded source reads and return `malformed_archive` on missing required fields before reading numeric values. |
| T-02-04 | I | `archive_summary` | mitigate | Report only bounded header fields that were safely available; do not imply table parsing or payload validity. |
</threat_model>

<verification>
Run `cmake --build build/local-vs2026-vcpkg --config Debug && ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug -R libbsa_detection_tests`.
</verification>

<success_criteria>
- Supported header samples detect the correct archive identity.
- Unsupported versions and malformed truncation produce structured failures per D-05 and D-06.
- BA2 GNRL/DDS distinction is made only from explicit subtype bytes per D-08.
</success_criteria>

<output>
After completion, create `.planning/phases/02-streaming-api-archive-model-detection-and-hashes/02-02-SUMMARY.md`.
</output>
