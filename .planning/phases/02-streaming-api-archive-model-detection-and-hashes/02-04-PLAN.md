---
phase: 02-streaming-api-archive-model-detection-and-hashes
plan: 04
type: tdd
wave: 4
depends_on: [02-03]
files_modified:
  - include/libbsa/archive_view.hpp
  - src/archive_view.cpp
  - tests/archive_view_tests.cpp
  - CMakeLists.txt
autonomous: true
requirements: [BIO-04, DPH-02, DPH-03, DPH-04]
must_haves:
  truths:
    - "D-02: Metadata archive views own copied metadata and do not retain borrowed source/sink lifetimes."
    - "D-04: Consumers get boolean contains(path) and result-returning entry(path) metadata lookup."
    - "D-09/D-10: Lookup uses public normalized archive paths, independent of host path rules."
  artifacts:
    - path: "include/libbsa/archive_view.hpp"
      provides: "Public metadata-only archive view API"
      exports: ["archive_view"]
    - path: "src/archive_view.cpp"
      provides: "Owned metadata lookup implementation"
    - path: "tests/archive_view_tests.cpp"
      provides: "Listing, contains, and metadata lookup tests"
  key_links:
    - from: "archive_view"
      to: "archive_path"
      via: "normalization before lookup"
      pattern: "normalize_archive_path"
    - from: "archive_view"
      to: "entry_metadata"
      via: "copied metadata storage"
      pattern: "std::vector<entry_metadata>"
---

<objective>
Create a metadata-only archive view for listing paths, existence checks, and entry metadata lookup.

Purpose: Consumers need lookup behavior over copied metadata before full archive-family parsers populate those metadata values.
Output: Public archive_view API, implementation, tests, and CMake wiring.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-streaming-api-archive-model-detection-and-hashes/02-SPEC.md
@.planning/phases/02-streaming-api-archive-model-detection-and-hashes/02-CONTEXT.md
@include/libbsa/archive.hpp
@include/libbsa/archive_path.hpp
@include/libbsa/result.hpp

<interfaces>
Plan 02 creates `entry_metadata` in `include/libbsa/archive.hpp`.
Plan 03 creates:
```cpp
class archive_path { const std::string& string() const noexcept; };
result<archive_path> normalize_archive_path(std::string input);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify metadata-only view lookup behavior</name>
  <files>tests/archive_view_tests.cpp, CMakeLists.txt</files>
  <read_first>
    - `include/libbsa/archive.hpp` — use real `entry_metadata` and `archive_summary` fields from Plan 02.
    - `include/libbsa/archive_path.hpp` — use real `archive_path` normalization from Plan 03.
    - `tests/path_hash_tests.cpp` — mirror normalization expectations.
    - `.planning/phases/02-streaming-api-archive-model-detection-and-hashes/02-CONTEXT.md` — implement D-02, D-04, D-09, and D-10.
  </read_first>
  <behavior>
    - Test 1: constructing `archive_view` from an `archive_summary` and entries with mixed path spellings lists normalized paths in deterministic sorted order.
    - Test 2: `contains("Textures\\Actors\\Hero.DDS")` returns true for a stored normalized path `textures/actors/hero.dds`.
    - Test 3: `entry("textures/actors/hero.dds")` returns copied `entry_metadata` with expected size, packed size, offset, hash, and compression state.
    - Test 4: missing path makes `contains` return false and `entry` return `error_code::malformed_archive` with message `archive entry not found`.
    - Test 5: invalid path input makes `contains` return false and `entry` return the normalization failure.
    - Test 6: mutating the source vector after construction does not change the view's listed paths or metadata.
  </behavior>
  <action>Create `tests/archive_view_tests.cpp` first. Add `libbsa_archive_view_tests` to `CMakeLists.txt`, link to `libbsa::libbsa` and `Catch2::Catch2WithMain`, and label with `unit`. Reference `archive_view` from `<libbsa/archive_view.hpp>`. Do not implement production files in this task; the build must fail because `archive_view.hpp` does not exist yet.</action>
  <verify>
    <automated>cmake --build build/local-vs2026-vcpkg --config Debug</automated>
  </verify>
  <acceptance_criteria>
    - `tests/archive_view_tests.cpp` contains `archive_view`.
    - `tests/archive_view_tests.cpp` contains `contains("Textures\\Actors\\Hero.DDS")`.
    - `tests/archive_view_tests.cpp` contains `archive entry not found`.
    - `tests/archive_view_tests.cpp` contains a mutation-after-construction test.
    - `CMakeLists.txt` contains `add_executable(libbsa_archive_view_tests tests/archive_view_tests.cpp)`.
    - The automated build fails before Task 2 because `archive_view.hpp` is missing.
  </acceptance_criteria>
  <done>Failing tests lock metadata-only listing and lookup behavior before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement owned metadata archive_view</name>
  <files>include/libbsa/archive_view.hpp, src/archive_view.cpp, CMakeLists.txt, tests/archive_view_tests.cpp</files>
  <read_first>
    - `tests/archive_view_tests.cpp` — satisfy exact tests from Task 1.
    - `include/libbsa/archive.hpp` — use `archive_summary` and `entry_metadata` without changing their semantics.
    - `include/libbsa/archive_path.hpp` — normalize query paths before lookup.
    - `include/libbsa/result.hpp` — return structured lookup failures.
  </read_first>
  <action>Create `include/libbsa/archive_view.hpp` with Doxygen comments. Define `class archive_view` with constructor `archive_view(archive_summary summary, std::vector<entry_metadata> entries)`, `const archive_summary& summary() const noexcept`, `std::vector<archive_path> paths() const`, `bool contains(std::string path) const`, and `result<entry_metadata> entry(std::string path) const`. Implement `src/archive_view.cpp` so the constructor normalizes each entry path, stores copied metadata keyed by normalized path string, and preserves deterministic ordering by normalized path. `contains` returns false for invalid normalization or missing key. `entry` returns normalization failure unchanged for invalid input, returns `failure<entry_metadata>({error_code::malformed_archive, "archive entry not found"})` when not present, and returns a copied metadata value on success. Do not read from `byte_source`, write to `byte_sink`, retain spans, or retain references to the caller's vector.</action>
  <verify>
    <automated>cmake --build build/local-vs2026-vcpkg --config Debug && ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug -R libbsa_archive_view_tests</automated>
  </verify>
  <acceptance_criteria>
    - `include/libbsa/archive_view.hpp` contains `class archive_view`.
    - `include/libbsa/archive_view.hpp` contains `bool contains(std::string path) const`.
    - `include/libbsa/archive_view.hpp` contains `result<entry_metadata> entry(std::string path) const`.
    - `src/archive_view.cpp` contains `archive entry not found`.
    - `src/archive_view.cpp` contains `normalize_archive_path`.
    - `libbsa_archive_view_tests` passes.
  </acceptance_criteria>
  <done>Consumers can list normalized paths, check existence, and retrieve copied metadata without reading payload bytes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Consumer path → archive view lookup | Untrusted query strings are normalized before metadata lookup. |
| Caller metadata vector → archive view | Caller-owned metadata crosses into owned view storage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | T/I | `archive_view` constructor | mitigate | Copy metadata and normalized keys; do not retain caller references or source/sink lifetimes. |
| T-02-08 | T/D | `archive_view::entry` | mitigate | Normalize query paths and return structured failures for invalid or missing entries. |
</threat_model>

<verification>
Run `cmake --build build/local-vs2026-vcpkg --config Debug && ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug -R libbsa_archive_view_tests`.
</verification>

<success_criteria>
- Archive view owns copied metadata.
- `paths`, `contains`, and `entry` operate on normalized archive paths.
- Missing and invalid path lookups produce distinct structured behavior per D-04 and D-12.
</success_criteria>

<output>
After completion, create `.planning/phases/02-streaming-api-archive-model-detection-and-hashes/02-04-SUMMARY.md`.
</output>
