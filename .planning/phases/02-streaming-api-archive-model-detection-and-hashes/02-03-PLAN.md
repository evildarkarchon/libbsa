---
phase: 02-streaming-api-archive-model-detection-and-hashes
plan: 03
type: tdd
wave: 3
depends_on: [02-02]
files_modified:
  - include/libbsa/archive_path.hpp
  - src/archive_path.cpp
  - src/hash.hpp
  - src/hash.cpp
  - tests/path_hash_tests.cpp
  - CMakeLists.txt
autonomous: true
requirements: [DPH-02, DPH-04, DPH-05, BIO-05]
must_haves:
  truths:
    - "D-09: Consumers can normalize archive paths through public API before lookup."
    - "D-10: Public archive paths are libbsa-owned normalized UTF-8 string values, not std::filesystem::path."
    - "D-11: Exact hash functions remain internal or test-visible while golden-vector tests lock behavior."
    - "D-12: Invalid archive path input returns result<archive_path> failure instead of throwing for data-shaped invalid input."
    - "D-13/D-14/D-15/D-16: Golden vectors live under tests with inline provenance and block uncertain hash behavior."
  artifacts:
    - path: "include/libbsa/archive_path.hpp"
      provides: "Public normalized archive_path value and normalization API"
      exports: ["archive_path", "normalize_archive_path"]
    - path: "src/archive_path.cpp"
      provides: "Archive path validation and normalization implementation"
    - path: "src/hash.hpp"
      provides: "Internal/test-visible Bethesda hash declarations"
    - path: "src/hash.cpp"
      provides: "TES3, TES4-family, and FO4/BA2 hash implementation"
    - path: "tests/path_hash_tests.cpp"
      provides: "Path normalization and hash golden vectors"
  key_links:
    - from: "tests/path_hash_tests.cpp"
      to: "TES5Edit/Core/wbBSArchive.pas"
      via: "inline vector provenance comments"
      pattern: "CreateHash(TES3|TES4|FO4)"
    - from: "include/libbsa/archive_path.hpp"
      to: "src/archive_path.cpp"
      via: "normalize_archive_path implementation"
      pattern: "normalize_archive_path"
---

<objective>
Add public archive-path normalization and internal Bethesda hash primitives with golden-vector coverage.

Purpose: Later metadata lookup and archive readers require stable virtual path normalization plus proven TES3, TES4-family, and FO4/BA2 hash behavior traced from the reference.
Output: Archive path API, internal hash files, golden-vector tests, and CMake wiring.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-streaming-api-archive-model-detection-and-hashes/02-SPEC.md
@.planning/phases/02-streaming-api-archive-model-detection-and-hashes/02-CONTEXT.md
@.planning/phases/02-streaming-api-archive-model-detection-and-hashes/02-RESEARCH.md
@TES5Edit/Core/wbBSArchive.pas
@include/libbsa/result.hpp

<interfaces>
Reference hash routines to port behavior from `TES5Edit/Core/wbBSArchive.pas`:
```text
CreateHashTES3(const aFileName: string): UInt64
CreateHashTES4(const aName, aExt: string): UInt64
CreateHashTES4(const aFileName: string): UInt64
CreateHashFO4(const aFileName: string): Cardinal
LowerByte lowercases ASCII A-Z only.
FO4 hash maps '/' to '\\' and ignores bytes > 127.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify path normalization and hash golden-vector behavior</name>
  <files>tests/path_hash_tests.cpp, CMakeLists.txt</files>
  <read_first>
    - `TES5Edit/Core/wbBSArchive.pas` lines 646-800 — trace `LowerByte`, split helpers, and hash routines.
    - `tests/foundation_tests.cpp` — copy Catch2 style.
    - `.planning/phases/02-streaming-api-archive-model-detection-and-hashes/02-CONTEXT.md` — implement D-09 through D-16.
    - `.planning/phases/02-streaming-api-archive-model-detection-and-hashes/02-SPEC.md` — follow path/hash acceptance criteria exactly.
  </read_first>
  <behavior>
    - Test 1: `normalize_archive_path("Textures//Actors\\Hero.DDS")` returns an owned `archive_path` whose `string()` is `textures/actors/hero.dds`.
    - Test 2: repeated separators and leading `./` segments are removed while archive-virtual relative path semantics are preserved.
    - Test 3: empty paths, absolute drive paths such as `C:\\Data\\x.dds`, UNC-like paths, and traversal segments `..` return `error_code::malformed_archive`.
    - Test 4: TES3 hash vectors include at least `meshes\\marker.nif`, `textures\\tx_boulder_01.dds`, and one mixed-case input.
    - Test 5: TES4 folder/file hash vectors include folder `textures\\actors`, file `hero.dds`, plus extension special cases `.nif`, `.dds`, `.wav`, and `.kf`.
    - Test 6: FO4/BA2 hash vectors include directory `textures\\actors`, basename `hero`, extension magic `dds`, slash normalization, and mixed case.
  </behavior>
  <action>Create `tests/path_hash_tests.cpp` first with inline comments above each vector group: `// Provenance: TES5Edit/Core/wbBSArchive.pas CreateHashTES3`, `CreateHashTES4`, and `CreateHashFO4`. Add `libbsa_path_hash_tests` to `CMakeLists.txt`, link it to `libbsa::libbsa` and `Catch2::Catch2WithMain`, and label it with both `unit` and `golden` using `catch_discover_tests(libbsa_path_hash_tests PROPERTIES LABELS "unit;golden")`. Reference `normalize_archive_path` from `<libbsa/archive_path.hpp>` and internal test-visible functions from `src/hash.hpp`. Do not implement production files in this task; the build must fail because these files are missing.</action>
  <verify>
    <automated>cmake --build build/local-vs2026-vcpkg --config Debug</automated>
  </verify>
  <acceptance_criteria>
    - `tests/path_hash_tests.cpp` contains `normalize_archive_path("Textures//Actors\\Hero.DDS")`.
    - `tests/path_hash_tests.cpp` contains `Provenance: TES5Edit/Core/wbBSArchive.pas CreateHashTES3`.
    - `tests/path_hash_tests.cpp` contains `Provenance: TES5Edit/Core/wbBSArchive.pas CreateHashTES4`.
    - `tests/path_hash_tests.cpp` contains `Provenance: TES5Edit/Core/wbBSArchive.pas CreateHashFO4`.
    - `CMakeLists.txt` contains `LABELS "unit;golden"` for `libbsa_path_hash_tests`.
    - The automated build fails before Task 2 because path/hash files are missing.
  </acceptance_criteria>
  <done>Failing tests lock D-09 through D-16 and include committed vector provenance in test source.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement archive_path normalization and internal hashes</name>
  <files>include/libbsa/archive_path.hpp, src/archive_path.cpp, src/hash.hpp, src/hash.cpp, CMakeLists.txt, tests/path_hash_tests.cpp</files>
  <read_first>
    - `tests/path_hash_tests.cpp` — satisfy the exact tests from Task 1 without weakening them.
    - `include/libbsa/result.hpp` — use `result<archive_path>` for invalid path reporting.
    - `TES5Edit/Core/wbBSArchive.pas` lines 646-800 — preserve byte-lowercase and hash algorithms.
    - `CMakeLists.txt` — add source/header entries explicitly.
  </read_first>
  <action>Create `include/libbsa/archive_path.hpp` with Doxygen comments. Define `class archive_path` as an owning normalized string value with `const std::string& string() const noexcept`, equality, and ordering. Define `result<archive_path> normalize_archive_path(std::string input)`. Implement `src/archive_path.cpp` to replace backslashes with `/`, lowercase ASCII A-Z, collapse repeated separators, remove `.` segments, reject empty results, reject drive-letter absolute paths, reject paths starting with `/`, reject UNC-like `//` after normalization checks, and reject `..` segments with `failure<archive_path>({error_code::malformed_archive, "invalid archive path"})`. Create `src/hash.hpp` and `src/hash.cpp` as internal/test-visible files declaring and defining `std::uint64_t hash_tes3_path(std::string_view)`, `std::uint64_t hash_tes4_name(std::string_view name, std::string_view ext)`, `std::uint64_t hash_tes4_path(std::string_view)`, `std::uint32_t hash_fo4_path_part(std::string_view)`, and `std::uint32_t fo4_extension_magic(std::string_view extension_without_dot)`. Port the reference behavior from `TES5Edit/Core/wbBSArchive.pas`; add a short WHY comment above the ASCII-only lowercasing helper explaining compatibility with `LowerByte`. Add all new files to explicit CMake lists.</action>
  <verify>
    <automated>cmake --build build/local-vs2026-vcpkg --config Debug && ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug -R libbsa_path_hash_tests</automated>
  </verify>
  <acceptance_criteria>
    - `include/libbsa/archive_path.hpp` contains `class archive_path` and `result<archive_path> normalize_archive_path`.
    - `include/libbsa/archive_path.hpp` contains no `std::filesystem` token.
    - `src/hash.cpp` contains `hash_tes3_path`, `hash_tes4_name`, `hash_tes4_path`, and `hash_fo4_path_part`.
    - `src/hash.cpp` contains a comment mentioning compatibility with `LowerByte`.
    - `tests/path_hash_tests.cpp` retains all `Provenance:` comments.
    - `libbsa_path_hash_tests` passes.
  </acceptance_criteria>
  <done>Archive path normalization is public and owned; compatibility hashes are locked by golden vectors without making them public API.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Consumer string → `normalize_archive_path` | Untrusted path text crosses into lookup normalization. |
| Reference vectors → hash implementation | Compatibility-sensitive bytes are transformed into lookup hashes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | T/I | `normalize_archive_path` | mitigate | Reject absolute, empty, and traversal paths with `malformed_archive`; do not use host filesystem APIs. |
| T-02-06 | R/T | `src/hash.cpp` | mitigate | Keep inline provenance comments in tests and add golden-vector coverage for each hash family. |
</threat_model>

<verification>
Run `cmake --build build/local-vs2026-vcpkg --config Debug && ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug -R libbsa_path_hash_tests` and `ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug -L golden`.
</verification>

<success_criteria>
- Public archive paths are normalized owned strings and do not use `std::filesystem::path`.
- Invalid path data returns structured failure.
- TES3, TES4-family, and FO4/BA2 hash golden-vector tests pass with provenance comments.
</success_criteria>

<output>
After completion, create `.planning/phases/02-streaming-api-archive-model-detection-and-hashes/02-03-SUMMARY.md`.
</output>
