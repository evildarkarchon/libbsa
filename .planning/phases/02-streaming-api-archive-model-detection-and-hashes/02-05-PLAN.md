---
phase: 02-streaming-api-archive-model-detection-and-hashes
plan: 05
type: execute
wave: 5
depends_on: [02-04]
files_modified:
  - tests/public_header_smoke.cpp
  - README.md
  - CMakeLists.txt
autonomous: true
requirements: [BIO-05, DPH-01, DPH-02, DPH-03, DPH-04, DPH-05]
must_haves:
  truths:
    - "Consumer can include all Phase 02 public headers without dependency, platform, Delphi, UI, DirectXTex, or TES5Edit leakage."
    - "Maintainer can run unit, golden, smoke, and full CTest lanes for Phase 02."
    - "Maintainer can verify `TES5Edit/` remains read-only and untouched after Phase 02."
  artifacts:
    - path: "tests/public_header_smoke.cpp"
      provides: "Consumer-style public header compile smoke across Phase 02 headers"
    - path: "README.md"
      provides: "Documented Phase 02 APIs, test labels, and TES5Edit boundary"
    - path: "CMakeLists.txt"
      provides: "No recursive globbing and all tests explicitly registered"
  key_links:
    - from: "tests/public_header_smoke.cpp"
      to: "include/libbsa/*.hpp"
      via: "public includes only"
      pattern: "#include <libbsa/(result|io|archive|detect|archive_path|archive_view)\.hpp>"
    - from: "README.md"
      to: "CTest labels"
      via: "documented commands"
      pattern: "golden"
---

<objective>
Finalize Phase 02 boundary validation, public-header smoke coverage, and developer documentation.

Purpose: The phase is only complete if its public API remains reusable, dependency-free, tested, and visibly separated from the read-only TES5Edit reference.
Output: Expanded smoke test, README updates, and final automated boundary gates.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-streaming-api-archive-model-detection-and-hashes/02-SPEC.md
@.planning/phases/02-streaming-api-archive-model-detection-and-hashes/02-CONTEXT.md
@.planning/phases/02-streaming-api-archive-model-detection-and-hashes/02-RESEARCH.md
@tests/public_header_smoke.cpp
@README.md
@CMakeLists.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand public-header smoke coverage for Phase 02 headers</name>
  <files>tests/public_header_smoke.cpp, CMakeLists.txt</files>
  <read_first>
    - `tests/public_header_smoke.cpp` — preserve existing result smoke behavior while adding Phase 02 headers.
    - `include/libbsa/io.hpp` — instantiate memory helper types without leaking source ownership.
    - `include/libbsa/archive.hpp` — instantiate public metadata values.
    - `include/libbsa/archive_path.hpp` — normalize a representative path.
    - `include/libbsa/archive_view.hpp` — instantiate a metadata-only view.
    - `CMakeLists.txt` — confirm smoke target remains linked only to `libbsa::libbsa`.
  </read_first>
  <action>Update `tests/public_header_smoke.cpp` to include `<libbsa/result.hpp>`, `<libbsa/io.hpp>`, `<libbsa/archive.hpp>`, `<libbsa/detect.hpp>`, `<libbsa/archive_path.hpp>`, and `<libbsa/archive_view.hpp>`. In `main`, construct a `memory_source`, `memory_sink`, `archive_summary`, one `entry_metadata`, normalize `textures/actors/hero.dds`, construct `archive_view`, and return 0 only when result success, source size, sink bytes, path string, and `view.contains("textures\\actors\\hero.dds")` all match expected values. Keep `libbsa_public_header_smoke` linked only to `libbsa::libbsa`; do not link Catch2 or private dependency targets to the smoke executable.</action>
  <verify>
    <automated>cmake --build build/local-vs2026-vcpkg --config Debug && ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug -L smoke</automated>
  </verify>
  <acceptance_criteria>
    - `tests/public_header_smoke.cpp` contains all six public includes: `result.hpp`, `io.hpp`, `archive.hpp`, `detect.hpp`, `archive_path.hpp`, `archive_view.hpp`.
    - `tests/public_header_smoke.cpp` contains `view.contains("textures\\actors\\hero.dds")`.
    - `CMakeLists.txt` still contains `target_link_libraries(libbsa_public_header_smoke` and `libbsa::libbsa` in that target block.
    - `CMakeLists.txt` does not add `Catch2::Catch2WithMain` to `libbsa_public_header_smoke`.
    - The smoke CTest label passes.
  </acceptance_criteria>
  <done>Consumer-style smoke coverage proves Phase 02 public headers compile and link through the public library target only.</done>
</task>

<task type="auto">
  <name>Task 2: Document Phase 02 APIs, labels, and boundary gates</name>
  <files>README.md</files>
  <read_first>
    - `README.md` — preserve existing build/test/TES5Edit boundary text and extend it.
    - `.planning/phases/02-streaming-api-archive-model-detection-and-hashes/02-SPEC.md` — reflect exact in-scope and out-of-scope boundaries.
    - `.planning/phases/02-streaming-api-archive-model-detection-and-hashes/02-RESEARCH.md` — document commands and reference-provenance policy.
  </read_first>
  <action>Update README with a concise Phase 02 section that names the public headers and APIs: `byte_source`, `byte_sink`, `memory_source`, `memory_sink`, `archive_summary`, `entry_metadata`, `detect_archive`, `archive_path`, `normalize_archive_path`, and `archive_view`. Document CTest commands for full, `unit`, `golden`, and `smoke` labels using `build/local-vs2026-vcpkg` as the local fallback build directory already used by Phase 1 verification. State that exact hash functions are internal/test-visible and locked by golden-vector tests with inline provenance comments near vectors. State that Phase 02 does not implement full table parsing, payload extraction, decompression, DDS reconstruction, writers, CLI, GUI, or public file source. Preserve the existing TES5Edit read-only boundary text; do not delete accurate comments or documentation.</action>
  <verify>
    <automated>cmake --build build/local-vs2026-vcpkg --config Debug && ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` contains `byte_source`, `byte_sink`, `memory_source`, `memory_sink`, `detect_archive`, `normalize_archive_path`, and `archive_view`.
    - `README.md` contains `ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug -L golden`.
    - `README.md` contains `TES5Edit/` and `read-only` in the same section.
    - `README.md` contains `does not implement full table parsing`.
    - Full CTest passes.
  </acceptance_criteria>
  <done>README describes Phase 02 public surface, verification commands, and compatibility boundary without adding implementation leakage.</done>
</task>

<task type="auto">
  <name>Task 3: Run final dependency-leakage and read-only boundary gates</name>
  <files>CMakeLists.txt, tests/public_header_smoke.cpp, README.md</files>
  <read_first>
    - `CMakeLists.txt` — verify explicit source lists and test targets.
    - `tests/public_header_smoke.cpp` — verify public include set.
    - `README.md` — verify documented commands and boundaries.
    - `AGENTS.md` — enforce TES5Edit read-only and comment/documentation policy.
  </read_first>
  <action>Run final gates and fix only issues revealed by the commands. Public header leakage gate: search only `include/libbsa/*.hpp` for forbidden implementation tokens `std::filesystem`, `libdeflate`, `LZ4`, `DirectX`, `Windows.h`, `TES5Edit`, `Delphi`, and UI terminology. CMake gate: confirm no `GLOB` or `GLOB_RECURSE` source discovery exists. TES5Edit gate: confirm `git status --short TES5Edit` prints nothing. Test gates: run full CTest plus `unit`, `golden`, and `smoke` labels. If a grep gate searches prose-bearing files, filter comments or narrow to public headers so the gate does not invalidate itself by matching documentation text.</action>
  <verify>
    <automated>cmake --build build/local-vs2026-vcpkg --config Debug && ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug && ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug -L unit && ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug -L golden && ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug -L smoke && git status --short TES5Edit</automated>
  </verify>
  <acceptance_criteria>
    - Full CTest passes.
    - Unit, golden, and smoke label commands pass.
    - `git status --short TES5Edit` prints no output.
    - `CMakeLists.txt` has no `GLOB` or `GLOB_RECURSE` token outside comments; if a command checks this, use a comment-filtered grep.
    - Public headers under `include/libbsa/` do not contain forbidden dependency/platform/reference tokens.
  </acceptance_criteria>
  <done>Phase 02 finishes with tests green, public boundary intact, and TES5Edit untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public headers → consumers | Public API must not expose private dependencies, platform APIs, or reference implementation types. |
| Repository worktree → TES5Edit submodule | Planning and implementation must not mutate the read-only reference. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | I | `include/libbsa/*.hpp` | mitigate | Smoke compile and forbidden-token gates verify no implementation dependency leakage. |
| T-02-10 | T/R | `TES5Edit/` | mitigate | Run `git status --short TES5Edit`; require empty output before completion. |
</threat_model>

<verification>
Run full, `unit`, `golden`, and `smoke` CTest lanes plus `git status --short TES5Edit`.
</verification>

<success_criteria>
- Phase 02 public headers compile together in a consumer-style executable.
- README includes Phase 02 API and verification commands.
- Full test suite and labels pass.
- TES5Edit remains untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/02-streaming-api-archive-model-detection-and-hashes/02-05-SUMMARY.md`.
</output>
