---
phase: 17-writer-hotspot-hardening-and-ship-gate
plan: 04
type: execute
wave: 3
depends_on:
  - 17-02
  - 17-03
files_modified:
  - include/libbsa/writer.hpp
  - docs/target-format-guide.md
  - docs/integration-examples.md
  - tests/unit/writer_hotspot_policy_tests.cpp
autonomous: true
requirements:
  - DX10-02
must_haves:
  truths:
    - "D-01: Maintainer-facing documentation states BA2 DX10 write_to is terminal after any ordinary write_to attempt, whether success or result error."
    - "D-02: Public writer documentation describes BA2 DX10 consumed-after-write invalid_argument behavior for later add_file and write_to without changing public signatures."
    - "D-03: Documentation keeps the consumed-writer rule specific to BA2 DX10 and does not standardize one-shot behavior across other writer families."
    - "D-04: Documentation explains cleanup priority over retryability for BA2 DX10 snapshot temp data."
    - "D-15: Maintainer-facing documentation distinguishes BA2 DX10 normal completion cleanup, ordinary failure cleanup, destructor safety-net cleanup, and residual abnormal-termination risk."
    - "Policy tests prevent docs from claiming crash-proof or hard-termination cleanup."
    - "Policy tests prove Phase 17 documentation-only public BA2 DX10 header edits do not add new ba2_dx10_writer methods, ba2_dx10_writer_options fields, or public signatures."
  artifacts:
    - path: "include/libbsa/writer.hpp"
      provides: "Public BA2 DX10 writer lifecycle documentation"
      contains: "ba2_dx10_writer"
    - path: "docs/target-format-guide.md"
      provides: "Maintainer-facing temp lifecycle contract and residual risk"
      contains: "BA2 DX10"
    - path: "docs/integration-examples.md"
      provides: "Consumer-oriented BA2 DX10 snapshot lifecycle note"
      contains: "example_create_ba2_dx10"
    - path: "tests/unit/writer_hotspot_policy_tests.cpp"
      provides: "Docs/source policy verification for DX10 lifecycle truthfulness and public BA2 DX10 API stability"
      contains: "ba2_dx10_writer_options"
  key_links:
    - from: "include/libbsa/writer.hpp"
      to: "docs/target-format-guide.md"
      via: "matching BA2 DX10 lifecycle terms"
      pattern: "ordinary failure"
    - from: "tests/unit/writer_hotspot_policy_tests.cpp"
      to: "docs/target-format-guide.md"
      via: "required lifecycle and residual-risk tokens"
      pattern: "abnormal-termination"
    - from: "tests/unit/writer_hotspot_policy_tests.cpp"
      to: "include/libbsa/writer.hpp"
      via: "public BA2 DX10 declaration-shape assertions"
      pattern: "ba2_dx10_writer_options"
---

<objective>
Document and guard the BA2 DX10 temporary-data lifecycle so DX10-02 is verifiable and truthful before milestone closure.

Purpose: Satisfies D-15 and DX10-02 by making cleanup guarantees and residual abnormal-termination risk explicit in public or maintainer-facing surfaces without adding public API signatures.
Output: Doxygen/public docs updates plus writer-hotspot policy assertions for lifecycle truthfulness.
</objective>

<execution_context>
@J:/libbsa/.opencode/get-shit-done/workflows/execute-plan.md
@J:/libbsa/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/17-writer-hotspot-hardening-and-ship-gate/17-SPEC.md
@.planning/phases/17-writer-hotspot-hardening-and-ship-gate/17-CONTEXT.md
@.planning/phases/17-writer-hotspot-hardening-and-ship-gate/17-RESEARCH.md
@.planning/phases/17-writer-hotspot-hardening-and-ship-gate/17-VALIDATION.md
@.planning/phases/17-writer-hotspot-hardening-and-ship-gate/17-PATTERNS.md
@.planning/phases/17-writer-hotspot-hardening-and-ship-gate/17-02-SUMMARY.md
@.planning/phases/17-writer-hotspot-hardening-and-ship-gate/17-03-SUMMARY.md
@include/libbsa/writer.hpp
@docs/target-format-guide.md
@docs/integration-examples.md
@tests/unit/writer_hotspot_policy_tests.cpp
@tests/unit/validation_policy_tests.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update BA2 DX10 public and maintainer lifecycle docs</name>
  <read_first>
    - include/libbsa/writer.hpp
    - docs/target-format-guide.md
    - docs/integration-examples.md
    - .planning/phases/17-writer-hotspot-hardening-and-ship-gate/17-CONTEXT.md
    - .planning/phases/17-writer-hotspot-hardening-and-ship-gate/17-SPEC.md
    - .planning/phases/17-writer-hotspot-hardening-and-ship-gate/17-PATTERNS.md
    - .planning/phases/17-writer-hotspot-hardening-and-ship-gate/17-03-SUMMARY.md
  </read_first>
  <files>include/libbsa/writer.hpp, docs/target-format-guide.md, docs/integration-examples.md</files>
  <action>Document the BA2 DX10 lifecycle contract per D-01 through D-04 and D-15. In include/libbsa/writer.hpp, update ba2_dx10_writer Doxygen comments for add_file and write_to to state that DDS data is snapshotted at add time, write_to is a consuming operation after any ordinary attempt, successful and result-returning failing write_to paths run best-effort snapshot cleanup, and later add_file/write_to return invalid_argument through result. In docs/target-format-guide.md, add a BA2 DX10 temporary snapshot lifecycle subsection that names four states: successful write_to completion, ordinary result-returning failure unwinding, destructor safety-net cleanup, and residual abnormal-termination risk. In docs/integration-examples.md, add a short note under example_create_ba2_dx10 that callers should not reuse a BA2 DX10 writer after write_to and that crash, forced termination, OS shutdown, or external temp-directory interference can leave residual temp artifacts. Do not change any public function signatures.</action>
  <verify>
    <automated>cmake --build --preset windows-msvc-debug-static && ctest --preset windows-msvc-debug-static --output-on-failure -L writer_hotspot_policy</automated>
  </verify>
  <acceptance_criteria>
    - include/libbsa/writer.hpp still declares the same ba2_dx10_writer public method signatures and contains text for consuming write_to and invalid_argument after consumption.
    - docs/target-format-guide.md contains the exact concepts successful write_to completion, ordinary result-returning failure, destructor safety-net cleanup, and abnormal-termination risk.
    - docs/integration-examples.md contains a BA2 DX10 note about not reusing the writer after write_to.
    - No docs claim crash-proof, hard-termination-proof, or OS-shutdown-proof cleanup.
  </acceptance_criteria>
  <done>Docs and public Doxygen comments truthfully describe BA2 DX10 temp lifecycle guarantees and residual risks.</done>
</task>

<task type="auto">
  <name>Task 2: Extend writer-hotspot policy tests for lifecycle docs and source truthfulness</name>
  <read_first>
    - tests/unit/writer_hotspot_policy_tests.cpp
    - tests/unit/validation_policy_tests.cpp
    - include/libbsa/writer.hpp
    - docs/target-format-guide.md
    - docs/integration-examples.md
    - src/formats/ba2/ba2_dx10_writer.cpp
    - .planning/phases/17-writer-hotspot-hardening-and-ship-gate/17-CONTEXT.md
    - .planning/phases/17-writer-hotspot-hardening-and-ship-gate/17-VALIDATION.md
  </read_first>
  <files>tests/unit/writer_hotspot_policy_tests.cpp</files>
  <action>Add [unit][writer_hotspot_policy][doc_structure] assertions that read include/libbsa/writer.hpp, docs/target-format-guide.md, docs/integration-examples.md, and src/formats/ba2/ba2_dx10_writer.cpp. Require tokens for BA2 DX10, snapshot cleanup, successful write_to completion, ordinary result-returning failure, destructor safety-net cleanup, abnormal-termination risk, consumed write_to, and invalid_argument. Add negative assertions that forbidden overclaims such as crash-proof cleanup and hard-termination cleanup do not appear. Add a public API stability/source-policy assertion for include/libbsa/writer.hpp per D-02, D-03, and D-15: parse or otherwise inspect the public ba2_dx10_writer_options field declarations and ba2_dx10_writer public method declarations and require the existing public declaration shape only, so documentation/comment edits for lifecycle truthfulness are allowed but new public BA2 DX10 methods, options, overloads, or signature expansions fail the writer_hotspot_policy label. Keep assertions role-based and avoid coupling to incidental helper names except consumed, snapshot_dir, invalid_argument, ba2_dx10_writer_options, and ba2_dx10_writer declaration tokens needed to prove D-01, D-02, and public API stability are implemented.</action>
  <verify>
    <automated>cmake --build --preset windows-msvc-debug-static && ctest --preset windows-msvc-debug-static --output-on-failure -L writer_hotspot_policy</automated>
  </verify>
  <acceptance_criteria>
    - writer_hotspot_policy label passes in Debug.
    - tests/unit/writer_hotspot_policy_tests.cpp reads all four surfaces: include/libbsa/writer.hpp, docs/target-format-guide.md, docs/integration-examples.md, and src/formats/ba2/ba2_dx10_writer.cpp.
    - Policy tests require residual abnormal-termination risk language and reject crash-proof or hard-termination cleanup claims.
    - Policy tests fail if include/libbsa/writer.hpp introduces public ba2_dx10_writer methods, ba2_dx10_writer_options fields, or BA2 DX10 public signature expansions beyond documentation/comment changes.
    - Policy tests still include DEDU-01 and DEDU-02 guardrails from Plans 17-01 and 17-02.
  </acceptance_criteria>
  <done>DX10-02 lifecycle truthfulness is enforced by committed policy tests.</done>
</task>

<task type="auto">
  <name>Task 3: Run focused lifecycle verification and record evidence</name>
  <read_first>
    - .planning/phases/17-writer-hotspot-hardening-and-ship-gate/17-VALIDATION.md
    - tests/unit/writer_hotspot_policy_tests.cpp
    - tests/unit/ba2_dx10_writer_tests.cpp
    - include/libbsa/writer.hpp
    - docs/target-format-guide.md
  </read_first>
  <files>tests/unit/writer_hotspot_policy_tests.cpp, docs/target-format-guide.md, include/libbsa/writer.hpp</files>
  <action>Run the focused policy and BA2 DX10 writer cleanup labels after documentation updates. If failures reveal wording drift, fix only the docs or policy assertions needed to restore the explicit target states in Task 1 and Task 2. Do not change implementation behavior in this plan except to correct a docs-policy mismatch caused by missing lifecycle terms.</action>
  <verify>
    <automated>cmake --build --preset windows-msvc-debug-static && ctest --preset windows-msvc-debug-static --output-on-failure -L "writer_hotspot_policy|ba2_dx10_writer"</automated>
  </verify>
  <acceptance_criteria>
    - Focused writer_hotspot_policy and ba2_dx10_writer labels pass in Debug.
    - SUMMARY records exact command output status for DX10-02.
    - include/libbsa/writer.hpp public signatures are unchanged compared with the start of this plan.
  </acceptance_criteria>
  <done>DX10 lifecycle documentation is verified by focused Debug policy/runtime evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| implementation behavior -> public documentation | Consumers and maintainers rely on docs to understand temp-data cleanup guarantees and residual risk. |
| source-policy tests -> repository text | Tests read docs and source files as policy inputs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-11 | R | docs/target-format-guide.md | mitigate | Require explicit docs tokens for normal cleanup, ordinary failure cleanup, destructor safety net, and residual abnormal-termination risk. |
| T-17-12 | I | include/libbsa/writer.hpp | mitigate | Avoid overclaiming crash-proof cleanup; document residual temp artifact risk for hard termination. |
| T-17-13 | T | tests/unit/writer_hotspot_policy_tests.cpp | mitigate | Use policy assertions to prevent docs/source drift that would misrepresent cleanup behavior before shipping. |
</threat_model>

<verification>
Focused plan gate: `cmake --build --preset windows-msvc-debug-static && ctest --preset windows-msvc-debug-static --output-on-failure -L "writer_hotspot_policy|ba2_dx10_writer"`.
Wave gate after Wave 3 completion: `cmake --build --preset windows-msvc-debug-static && ctest --preset windows-msvc-debug-static --output-on-failure`.
</verification>

<success_criteria>
- DX10-02 is satisfied by public or maintainer-facing docs plus policy evidence.
- Documentation distinguishes successful completion, ordinary failure unwinding, destructor safety-net cleanup, and abnormal-termination risk.
- No public signatures change and no crash-proof cleanup claim is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/17-writer-hotspot-hardening-and-ship-gate/17-04-SUMMARY.md` with documentation diffs, policy evidence, and DX10-02 traceability.
</output>
