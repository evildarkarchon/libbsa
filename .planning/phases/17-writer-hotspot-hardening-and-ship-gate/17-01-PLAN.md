---
phase: 17-writer-hotspot-hardening-and-ship-gate
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/formats/bsa/tes4_bsa_layout.cpp
  - tests/unit/tes4_bsa_writer_tests.cpp
  - tests/unit/writer_hotspot_policy_tests.cpp
  - tests/CMakeLists.txt
autonomous: true
requirements:
  - DEDU-01
must_haves:
  truths:
    - "D-05: TES4-family BSA writer dedupe combines runtime regression tests with source/policy guardrails for candidate narrowing when deduplicate_payloads is true."
    - "D-06: TES4-family BSA writer dedupe considers keyed or bounded candidate groups before exact stored-byte comparison, producing algorithmic non-flaky proof instead of timing benchmarks."
    - "D-07: TES4-family BSA writer dedupe narrowing stays format-local near existing TES4 layout code unless a very small internal helper is justified."
    - "D-08: TES4 shared payload offsets are still assigned only after exact final stored-byte equality succeeds; candidate filters are never authoritative."
    - "TES4 writer regression coverage still proves identical stored bytes share offsets and compression/embedded-name mismatches remain distinct."
  artifacts:
    - path: "src/formats/bsa/tes4_bsa_layout.cpp"
      provides: "Format-local TES4 dedupe candidate narrowing before exact equality"
      contains: "tes4_stored_payloads_equal"
    - path: "tests/unit/tes4_bsa_writer_tests.cpp"
      provides: "Runtime TES4 dedupe behavior proof"
      contains: "[tes4_bsa_writer]"
    - path: "tests/unit/writer_hotspot_policy_tests.cpp"
      provides: "Source-policy guardrail against TES4 all-prior unkeyed scans"
      contains: "[writer_hotspot_policy]"
  key_links:
    - from: "src/formats/bsa/tes4_bsa_layout.cpp"
      to: "tes4_stored_payloads_equal"
      via: "dedupe bucket candidate loop"
      pattern: "tes4_stored_payloads_equal"
    - from: "tests/unit/writer_hotspot_policy_tests.cpp"
      to: "src/formats/bsa/tes4_bsa_layout.cpp"
      via: "source token assertions"
      pattern: "writer_hotspot_policy"
---

<objective>
Harden TES4-family BSA dedupe by adding format-local candidate narrowing before the existing exact stored-byte equality authority.

Purpose: Satisfies DEDU-01, D-05, D-06, D-07, and D-08 without changing public writer APIs, dedupe defaults, or final equality semantics.
Output: TES4 layout narrowing implementation, focused runtime proof, and the initial Phase 17 writer-hotspot policy suite registration.
</objective>

<execution_context>
@J:/libbsa/.opencode/get-shit-done/workflows/execute-plan.md
@J:/libbsa/.opencode/get-shit-done/templates/summary.md
@J:/libbsa/.opencode/get-shit-done/references/tdd.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/17-writer-hotspot-hardening-and-ship-gate/17-SPEC.md
@.planning/phases/17-writer-hotspot-hardening-and-ship-gate/17-CONTEXT.md
@.planning/phases/17-writer-hotspot-hardening-and-ship-gate/17-RESEARCH.md
@.planning/phases/17-writer-hotspot-hardening-and-ship-gate/17-VALIDATION.md
@.planning/phases/17-writer-hotspot-hardening-and-ship-gate/17-PATTERNS.md
@.planning/codebase/TESTING.md
@src/formats/bsa/tes4_bsa_layout.cpp
@tests/unit/tes4_bsa_writer_tests.cpp
@tests/unit/parser_preparer_seam_policy_tests.cpp
@tests/CMakeLists.txt
</context>

<feature>
  <name>TES4 dedupe candidate narrowing</name>
  <files>src/formats/bsa/tes4_bsa_layout.cpp, tests/unit/tes4_bsa_writer_tests.cpp, tests/unit/writer_hotspot_policy_tests.cpp, tests/CMakeLists.txt</files>
  <behavior>
    - With tes4_bsa_writer_options::deduplicate_payloads = true, entries whose final stored bytes are identical still share payload_offset.
    - Entries with the same source bytes but different final stored bytes, including compression encoding or embedded-name prefix differences, do not share payload_offset.
    - The layout path narrows candidates through a keyed or bounded structure before calling tes4_stored_payloads_equal.
    - tes4_stored_payloads_equal remains the final authority before any shared payload_offset assignment.
  </behavior>
  <implementation>Keep the change format-local in src/formats/bsa/tes4_bsa_layout.cpp per D-07. Introduce a private TES4 final-stored-payload key or bounded bucket that uses cheap identity facts such as stored_size and available stored-payload fingerprint/prefix facts, then call tes4_stored_payloads_equal only for candidates in that bucket. Do not add dependencies, do not change public headers, do not change dedupe defaults, and do not replace exact equality with hash-only sharing.</implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED - add TES4 dedupe narrowing runtime and policy tests</name>
  <read_first>
    - .planning/phases/17-writer-hotspot-hardening-and-ship-gate/17-SPEC.md
    - .planning/phases/17-writer-hotspot-hardening-and-ship-gate/17-CONTEXT.md
    - .planning/phases/17-writer-hotspot-hardening-and-ship-gate/17-VALIDATION.md
    - .planning/phases/17-writer-hotspot-hardening-and-ship-gate/17-PATTERNS.md
    - tests/unit/tes4_bsa_writer_tests.cpp
    - tests/unit/parser_preparer_seam_policy_tests.cpp
    - tests/CMakeLists.txt
    - src/formats/bsa/tes4_bsa_layout.cpp
  </read_first>
  <files>tests/unit/tes4_bsa_writer_tests.cpp, tests/unit/writer_hotspot_policy_tests.cpp, tests/CMakeLists.txt</files>
  <action>Create the Phase 17 writer-hotspot policy suite and TES4 RED tests per D-05 and D-06. Register tests/unit/writer_hotspot_policy_tests.cpp in the libbsa_tests source list in tests/CMakeLists.txt. Add policy tests tagged [unit][writer_hotspot_policy] that read src/formats/bsa/tes4_bsa_layout.cpp and assert: a keyed or bounded candidate container is present in the dedupe assignment path, tes4_stored_payloads_equal is still present in that path, and the old unkeyed all-prior std::vector assigned-payload scan pattern no longer appears as the dedupe candidate selection mechanism. Add or extend TES4 runtime coverage tagged [unit][tes4_bsa_writer] to keep proving identical final stored bytes share offsets and mismatched compression/embedded-name final stored bytes stay distinct. The RED gate must fail before implementation because TES4 currently still uses the unkeyed candidate scan.</action>
  <verify>
    <automated>cmake --build --preset windows-msvc-debug-static && ctest --preset windows-msvc-debug-static --output-on-failure -L "tes4_bsa_writer|writer_hotspot_policy"</automated>
  </verify>
  <acceptance_criteria>
    - tests/unit/writer_hotspot_policy_tests.cpp exists and contains the tag string [writer_hotspot_policy].
    - tests/CMakeLists.txt contains unit/writer_hotspot_policy_tests.cpp in the libbsa_tests source list.
    - RED run fails on a policy or runtime assertion tied to TES4 dedupe narrowing, not on compile errors, missing test registration, or fixture setup failure.
    - Existing TES4 assertions for shared offsets, compression mismatch, and embedded-name mismatch remain present in tests/unit/tes4_bsa_writer_tests.cpp.
  </acceptance_criteria>
  <done>RED gate is committed as test(17-01): add failing test for TES4 dedupe narrowing, with failure evidence recorded in the summary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN - implement TES4 candidate narrowing with exact equality fallback</name>
  <read_first>
    - src/formats/bsa/tes4_bsa_layout.cpp
    - tests/unit/tes4_bsa_writer_tests.cpp
    - tests/unit/writer_hotspot_policy_tests.cpp
    - .planning/phases/17-writer-hotspot-hardening-and-ship-gate/17-CONTEXT.md
    - .planning/phases/17-writer-hotspot-hardening-and-ship-gate/17-PATTERNS.md
  </read_first>
  <files>src/formats/bsa/tes4_bsa_layout.cpp</files>
  <action>Replace the TES4 dedupe candidate list in tes4_assign_offsets with a format-local keyed or bounded candidate map before exact comparisons. The key must be derived from final stored-payload facts available at layout time, at minimum stored_size plus a cheap deterministic fingerprint or prefix for in-memory stored_payload data, while disk-backed candidates still route through tes4_stored_payloads_equal before sharing offsets. Keep assigned_payload or an equivalent candidate record carrying the prior tes4_prepared_entry pointer and assigned offset. For every shared offset assignment, call tes4_stored_payloads_equal(entry, prior_entry) first and only assign entry.payload_offset, entry.stored_size, and entry.owns_payload_bytes = false when it returns true. Add a short comment explaining that candidate keys narrow comparisons only and exact stored-byte equality remains mandatory per D-08.</action>
  <verify>
    <automated>cmake --build --preset windows-msvc-debug-static && ctest --preset windows-msvc-debug-static --output-on-failure -L "tes4_bsa_writer|writer_hotspot_policy"</automated>
  </verify>
  <acceptance_criteria>
    - src/formats/bsa/tes4_bsa_layout.cpp still contains tes4_stored_payloads_equal and every dedupe shared-offset branch reaches it before setting owns_payload_bytes to false.
    - src/formats/bsa/tes4_bsa_layout.cpp contains a keyed or bounded candidate structure used for deduplicate_payloads before exact equality calls.
    - The focused tes4_bsa_writer and writer_hotspot_policy labels pass in Debug.
    - No public header under include/libbsa is modified by this plan.
  </acceptance_criteria>
  <done>GREEN gate is committed as feat(17-01): implement TES4 dedupe candidate narrowing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: REFACTOR - tighten TES4 guardrails without changing behavior</name>
  <read_first>
    - src/formats/bsa/tes4_bsa_layout.cpp
    - tests/unit/writer_hotspot_policy_tests.cpp
    - tests/unit/tes4_bsa_writer_tests.cpp
  </read_first>
  <files>src/formats/bsa/tes4_bsa_layout.cpp, tests/unit/writer_hotspot_policy_tests.cpp</files>
  <action>Refactor only if the GREEN implementation leaves duplicated key-building or brittle policy-test assertions. Keep helper scope private to src/formats/bsa/tes4_bsa_layout.cpp and keep policy assertions role-based rather than frozen to incidental helper names. Do not weaken tests to pass the implementation; the source policy must still detect unkeyed all-prior candidate scanning and missing exact equality fallback.</action>
  <verify>
    <automated>cmake --build --preset windows-msvc-debug-static && ctest --preset windows-msvc-debug-static --output-on-failure -L "tes4_bsa_writer|writer_hotspot_policy"</automated>
  </verify>
  <acceptance_criteria>
    - Focused Debug labels pass after any refactor.
    - writer_hotspot_policy assertions remain tied to semantic tokens: keyed/bounded narrowing, tes4_stored_payloads_equal, and absence of unkeyed all-prior scanning.
    - No new dependency appears in vcpkg.json or CMake package discovery.
  </acceptance_criteria>
  <done>Optional REFACTOR gate is committed as refactor(17-01): clean up TES4 dedupe narrowing if source changes are made; otherwise summary records no refactor needed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| staged writer entry -> archive layout | Caller-controlled payload bytes and archive paths affect stored offsets and dedupe decisions. |
| disk source -> dedupe equality | Disk-backed payloads may change between preparation and layout comparison. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | T | src/formats/bsa/tes4_bsa_layout.cpp | mitigate | Preserve tes4_stored_payloads_equal as the required final gate before shared payload_offset assignment so keyed narrowing cannot corrupt offsets on fingerprint collision. |
| T-17-02 | D | src/formats/bsa/tes4_bsa_layout.cpp | mitigate | Replace unkeyed all-prior dedupe scan with keyed/bounded candidate buckets to reduce expensive comparisons without timing-sensitive benchmarks. |
| T-17-03 | I | tests/unit/writer_hotspot_policy_tests.cpp | accept | Source-policy tests read repository files only and expose no archive payload data beyond committed source text. |
</threat_model>

<verification>
Focused plan gate: `cmake --build --preset windows-msvc-debug-static && ctest --preset windows-msvc-debug-static --output-on-failure -L "tes4_bsa_writer|writer_hotspot_policy"`.
Wave gate after Wave 1 completion: `cmake --build --preset windows-msvc-debug-static && ctest --preset windows-msvc-debug-static --output-on-failure`.
</verification>

<success_criteria>
- DEDU-01 is covered by runtime and source-policy evidence.
- TES4 dedupe uses keyed or bounded candidate narrowing before exact equality.
- Exact stored-byte equality remains mandatory before shared offset assignment.
- Public API signatures and TES5Edit/ remain untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/17-writer-hotspot-hardening-and-ship-gate/17-01-SUMMARY.md` with RED/GREEN/REFACTOR evidence, commands run, and DEDU-01 traceability.
</output>
