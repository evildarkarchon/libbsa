---
phase: 08-ba2-gnrl-write-new-support
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - include/libbsa/writer.hpp
  - tests/unit/public_include_boundary_tests.cpp
requirements: [WBA2-01, WBA2-02, WBA2-05]
autonomous: true
must_haves:
  truths:
    - "D-01: Expose a dedicated BA2 GNRL writer object, not a generic BA2 writer shell."
    - "D-02: Mirror the Phase 7 writer-object flow with target/options, explicit paths, copied memory entries, and host-path finalization."
    - "D-03: Keep Phase 8 output host-path only through a write_to-style operation."
    - "D-04: Reuse archive_compression_policy and entry_compression_policy in the public API."
    - "D-06: Preserve dependency-light public headers with no private codec, DirectXTex, TES5Edit, or std::expected leakage."
    - "Consumer code can name and construct a dedicated BA2 GNRL writer for Fallout 4 v1, Starfield v2, and Starfield v3."
    - "Consumer code can add disk-file and copied memory entries and finalize only to a host-path archive."
    - "Public headers expose only libbsa-owned C++20 types and no private codec/reference dependencies."
  artifacts:
    - path: "include/libbsa/writer.hpp"
      provides: "ba2_gnrl_target, ba2_gnrl_writer_options, ba2_gnrl_entry_options, ba2_gnrl_writer declarations"
      contains: "class ba2_gnrl_writer"
    - path: "tests/unit/public_include_boundary_tests.cpp"
      provides: "compile-time public API and forbidden-token boundary coverage"
      contains: "ba2_gnrl_writer"
  key_links:
    - from: "tests/unit/public_include_boundary_tests.cpp"
      to: "include/libbsa/writer.hpp"
      via: "static_assert and requires expressions"
      pattern: "ba2_gnrl_writer"
---

<objective>
Define the dependency-light public BA2 GNRL writer contract before private implementation.

Purpose: Phase 8 requires a dedicated GNRL writer surface per D-01, mirroring Phase 7's object flow per D-02 while preserving host-path-only output per D-03 and public dependency boundaries per D-06.
Output: Public API declarations and RED/GREEN public include-boundary assertions.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-ba2-gnrl-write-new-support/08-SPEC.md
@.planning/phases/08-ba2-gnrl-write-new-support/08-CONTEXT.md
@.planning/phases/08-ba2-gnrl-write-new-support/08-RESEARCH.md
@.planning/phases/08-ba2-gnrl-write-new-support/08-PATTERNS.md
@.planning/phases/07-tes4-family-bsa-write-new-support/07-01-SUMMARY.md
@include/libbsa/writer.hpp
@tests/unit/public_include_boundary_tests.cpp
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1 RED: Add public BA2 GNRL writer boundary expectations</name>
  <files>tests/unit/public_include_boundary_tests.cpp</files>
  <read_first>
    - tests/unit/public_include_boundary_tests.cpp
    - include/libbsa/writer.hpp
    - .planning/phases/08-ba2-gnrl-write-new-support/08-SPEC.md
    - .planning/phases/08-ba2-gnrl-write-new-support/08-CONTEXT.md
    - .planning/phases/08-ba2-gnrl-write-new-support/08-PATTERNS.md
  </read_first>
  <behavior>
    - Assert `std::is_enum_v&lt;libbsa::ba2_gnrl_target&gt;` and `std::is_class_v&lt;libbsa::ba2_gnrl_writer&gt;`.
    - Assert `libbsa::ba2_gnrl_writer` is constructible from `libbsa::ba2_gnrl_target` and from `libbsa::ba2_gnrl_target, libbsa::ba2_gnrl_writer_options`.
    - Assert `add_file`, `add_bytes`, and `write_to` return `libbsa::result&lt;void&gt;`.
  </behavior>
  <action>Add failing public-boundary static assertions and requires expressions for concrete names: `ba2_gnrl_target`, `ba2_gnrl_target::fallout4`, `ba2_gnrl_target::starfield_v2`, `ba2_gnrl_target::starfield_v3`, `ba2_gnrl_writer_options`, `ba2_gnrl_entry_options`, and `ba2_gnrl_writer`. Include method checks for `add_file("Meshes/Disk.nif", "source.nif")`, `add_bytes("Meshes/Memory.nif", std::span&lt;const std::byte&gt;{})`, overloads taking `ba2_gnrl_entry_options`, `target()`, `options()`, and `write_to("out.ba2")`. Keep the existing forbidden-token scan and add no production declarations in this task.</action>
  <verify>
    <automated>cmake --build --preset windows-msvc-debug-static --target libbsa_tests</automated>
  </verify>
  <acceptance_criteria>
    - `tests/unit/public_include_boundary_tests.cpp` contains `std::is_enum_v&lt;libbsa::ba2_gnrl_target&gt;`.
    - `tests/unit/public_include_boundary_tests.cpp` contains `ba2_gnrl_target::starfield_v3`.
    - `tests/unit/public_include_boundary_tests.cpp` contains `writer.add_bytes("Meshes/Memory.nif", bytes)`.
    - Build fails before Task 2 because `ba2_gnrl_writer` API names are missing.
  </acceptance_criteria>
  <done>RED boundary expectations exist and fail for missing public BA2 GNRL writer declarations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 GREEN: Add dependency-light public BA2 GNRL writer declarations</name>
  <files>include/libbsa/writer.hpp</files>
  <read_first>
    - include/libbsa/writer.hpp
    - tests/unit/public_include_boundary_tests.cpp
    - .planning/phases/08-ba2-gnrl-write-new-support/08-CONTEXT.md
    - .planning/phases/08-ba2-gnrl-write-new-support/08-PATTERNS.md
    - .planning/phases/07-tes4-family-bsa-write-new-support/07-01-SUMMARY.md
  </read_first>
  <action>Add Doxygen-documented public declarations beside the TES4 writer: `enum class ba2_gnrl_target { fallout4, starfield_v2, starfield_v3 };`, `struct ba2_gnrl_writer_options { archive_compression_policy compression = archive_compression_policy::target_default; bool overwrite_existing = false; bool deduplicate_payloads = false; std::uint32_t starfield_unknown1 = 1U; std::uint32_t starfield_unknown2 = 0U; std::uint32_t starfield_compression_method = 3U; };`, `struct ba2_gnrl_entry_options { entry_compression_policy compression = entry_compression_policy::inherit; std::optional&lt;std::uint32_t&gt; record_flags = std::nullopt; };`, and `class ba2_gnrl_writer` with constructors, `target()`, `options()`, `add_file`/`add_bytes` overloads taking either `entry_compression_policy` or `ba2_gnrl_entry_options`, and host-path-only `write_to(std::string_view host_path) const`. Reuse existing `archive_compression_policy` and `entry_compression_policy` per D-04. Do not expose libdeflate, lz4, DirectXTex, Windows SDK, TES5Edit, private detail types, or `std::expected` per D-06.</action>
  <verify>
    <automated>cmake --build --preset windows-msvc-debug-static --target libbsa_tests &amp;&amp; ctest --preset windows-msvc-debug-static -R public_include_boundary --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `include/libbsa/writer.hpp` contains `enum class ba2_gnrl_target`.
    - `include/libbsa/writer.hpp` contains `starfield_unknown1 = 1U`.
    - `include/libbsa/writer.hpp` contains `starfield_compression_method = 3U`.
    - `include/libbsa/writer.hpp` contains `std::optional&lt;std::uint32_t&gt; record_flags`.
    - `include/libbsa/writer.hpp` contains `result&lt;void&gt; write_to(std::string_view host_path) const`.
    - Public boundary tests pass after declarations are added.
  </acceptance_criteria>
  <done>Public BA2 GNRL writer contract compiles through installed public headers without dependency leakage.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Consumer code→public writer API | Untrusted archive paths, host paths, options, and memory spans enter libbsa-owned writer state. |
| Public headers→consumer build | Public declarations can leak private dependencies or C++23 types into consumers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-01 | Information Disclosure | `include/libbsa/writer.hpp` | mitigate | Public boundary forbidden-token test rejects `libdeflate`, `lz4::`, `DirectXTex`, `DXGI`, `Windows.h`, `TES5Edit`, `std::expected`, `bethesda_hash`, `compression_router`, and `archive_path_key`. |
| T-08-01-02 | Tampering | `ba2_gnrl_writer_options` | mitigate | Expose only target/profile options and record-flags override; keep hashes, offsets, packed/raw sizes, and `BAADF00D` writer-owned per D-15. |
</threat_model>

<verification>
Run `cmake --build --preset windows-msvc-debug-static --target libbsa_tests` and `ctest --preset windows-msvc-debug-static -R public_include_boundary --output-on-failure`.
</verification>

<success_criteria>
`ba2_gnrl_writer` public declarations exist, boundary tests pass, and no private dependency tokens appear in public headers outside comments.
</success_criteria>

<output>
After completion, create `.planning/phases/08-ba2-gnrl-write-new-support/08-01-SUMMARY.md`.
</output>
