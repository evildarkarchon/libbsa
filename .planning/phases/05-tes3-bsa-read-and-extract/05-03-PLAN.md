---
phase: 05-tes3-bsa-read-and-extract
plan: 03
type: execute
wave: 3
depends_on: [05-02]
files_modified: [src/bsa_reader.cpp, tests/bsa_reader_tests.cpp]
autonomous: true
requirements: [BSA-04]
must_haves:
  truths:
    - "Malformed TES3 tables fail with structured errors"
    - "Impossible TES3 data-section-relative payload ranges are rejected before reading"
    - "TES3 name/hash table bounds cannot associate entries with out-of-range names"
  artifacts:
    - path: "tests/bsa_reader_tests.cpp"
      provides: "TES3 malformed fixture tests"
      contains: "open_bsa rejects truncated TES3 tables"
    - path: "src/bsa_reader.cpp"
      provides: "Bounded TES3 table validation"
      contains: "malformed_archive"
  key_links:
    - from: "TES3 parser"
      to: "result/error API"
      via: "failure<bsa_archive>({error_code::malformed_archive, ...})"
      pattern: "malformed_archive"
---

<objective>
Harden TES3 parsing and extraction against malformed archive bytes.

Purpose: BSA-04 requires correct TES3 offsets, and untrusted archive tables must not cause unchecked reads, allocations, or silent metadata corruption.
Output: Malformed TES3 tests and parser bounds checks.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-tes3-bsa-read-and-extract/05-RESEARCH.md
@.planning/phases/05-tes3-bsa-read-and-extract/05-02-SUMMARY.md
@src/bsa_reader.cpp
@tests/bsa_reader_tests.cpp
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add malformed TES3 table and payload tests</name>
  <files>tests/bsa_reader_tests.cpp</files>
  <read_first>tests/bsa_reader_tests.cpp; src/bsa_reader.cpp; .planning/phases/05-tes3-bsa-read-and-extract/05-RESEARCH.md</read_first>
  <behavior>
    - Test 1: Truncating before the complete size/offset table returns `error_code::malformed_archive`.
    - Test 2: A name offset outside the name block returns `error_code::malformed_archive`.
    - Test 3: A `hashOffset` that points before the end of the name block returns `error_code::malformed_archive`.
    - Test 4: A TES3 relative payload offset plus size exceeding `source.size()` causes `extract_bsa_entry` to return `error_code::malformed_archive` and message `BSA payload range exceeds source size`.
  </behavior>
  <action>Add tests named exactly `open_bsa rejects truncated TES3 tables`, `open_bsa rejects invalid TES3 name offsets`, `open_bsa rejects invalid TES3 hash offsets`, and `extract_bsa_entry rejects impossible TES3 payload ranges`. Use the generated TES3 fixture helper, then mutate bytes to create each malformed condition. Keep assertions on `error_code::malformed_archive`; for impossible payload range also assert the existing message `BSA payload range exceeds source size`.</action>
  <verify>
    <automated>ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug -R libbsa_bsa_reader_tests</automated>
  </verify>
  <acceptance_criteria>
    - `tests/bsa_reader_tests.cpp` contains all four test names listed in the action.
    - `tests/bsa_reader_tests.cpp` contains `BSA payload range exceeds source size`.
  </acceptance_criteria>
  <done>Malformed TES3 behavior is specified by automated tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tighten TES3 bounds checks until malformed tests pass</name>
  <files>src/bsa_reader.cpp</files>
  <read_first>src/bsa_reader.cpp; tests/bsa_reader_tests.cpp; .planning/phases/05-tes3-bsa-read-and-extract/05-RESEARCH.md</read_first>
  <behavior>
    - All TES3 table offsets and counts are checked with overflow-safe arithmetic.
    - Null-terminated name reads stop before `hash_table_start` and fail if no terminator exists in the name block.
    - Data payload ranges are checked before reading through existing `read_payload_bytes` behavior.
  </behavior>
  <action>Update the TES3 parser in `src/bsa_reader.cpp` to use existing `checked_add`, `range_fits`, and `read_bytes` helpers for all table boundaries. Add a bounded null-terminated string reader for TES3 names that accepts `offset` and `limit` and returns `truncated BSA table` on missing terminator or out-of-range reads. Ensure `fileCount * recordSize`, `fileCount * 4`, and `fileCount * 8` cannot overflow. Reject `hashOffset` values that place the hash table before `name_block_start` or beyond `source.size()`. Reject name offsets where `name_block_start + nameOffset >= hash_table_start`. Do not catch exceptions or allocate based on unchecked counts.</action>
  <verify>
    <automated>ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug -R libbsa_bsa_reader_tests</automated>
  </verify>
  <acceptance_criteria>
    - `src/bsa_reader.cpp` contains `hash_table_start < name_block_start` or equivalent explicit check.
    - `src/bsa_reader.cpp` contains `name_block_start`.
    - `src/bsa_reader.cpp` contains a bounded TES3 name-reading helper or loop that checks against `hash_table_start`.
    - Targeted CTest exits 0.
  </acceptance_criteria>
  <done>Malformed TES3 archives fail safely with structured errors and no unchecked table or payload reads.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| archive tables → parser allocation/range checks | Untrusted counts and offsets influence vector reserves and read ranges. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | D/T | TES3 table parser | mitigate | Use overflow-safe arithmetic, bounded reads, bounded name parsing, and structured `malformed_archive` failures. |
</threat_model>

<verification>
Run `ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug -R libbsa_bsa_reader_tests`.
</verification>

<success_criteria>
- Truncated TES3 tables fail structured.
- Invalid TES3 name/hash offsets fail structured.
- Impossible TES3 payload ranges fail before sink writes.
</success_criteria>

<output>
After completion, create `.planning/phases/05-tes3-bsa-read-and-extract/05-03-SUMMARY.md`
</output>
