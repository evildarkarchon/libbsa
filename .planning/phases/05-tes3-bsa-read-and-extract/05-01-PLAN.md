---
phase: 05-tes3-bsa-read-and-extract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [include/libbsa/bsa.hpp, src/bsa_reader.hpp, src/bsa_reader.cpp, tests/bsa_reader_tests.cpp]
autonomous: true
requirements: [BSA-04]
must_haves:
  truths:
    - "Consumer can open a generated TES3 Morrowind BSA archive through open_bsa"
    - "Consumer can list normalized TES3 entry paths"
    - "Consumer can inspect TES3 sizes, hashes, and data-section-relative offsets represented as absolute payload offsets"
  artifacts:
    - path: "src/bsa_reader.cpp"
      provides: "TES3 metadata parser branch in open_bsa"
      contains: "archive_format::tes3_bsa"
    - path: "tests/bsa_reader_tests.cpp"
      provides: "Generated TES3 metadata fixture tests"
      contains: "open_bsa lists TES3 metadata with data-section-relative offsets"
  key_links:
    - from: "src/bsa_reader.cpp"
      to: "src/hash.cpp"
      via: "detail::hash_tes3_path compatibility check/fallback"
      pattern: "hash_tes3_path"
    - from: "tests/bsa_reader_tests.cpp"
      to: "src/bsa_reader.cpp"
      via: "libbsa::open_bsa(memory_source)"
      pattern: "open_bsa.*TES3"
---

<objective>
Add TES3 BSA metadata parsing to the existing BSA reader surface.

Purpose: BSA-04 requires Morrowind archives to open/list/inspect with TES3-specific data-section-relative offsets while preserving the existing `bsa_archive` public API.
Output: TES3 branch in `open_bsa`, generated TES3 fixture helpers, and failing-then-passing metadata coverage.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-tes3-bsa-read-and-extract/05-RESEARCH.md
@TES5Edit/Core/wbBSArchive.pas
@include/libbsa/bsa.hpp
@src/bsa_reader.hpp
@src/bsa_reader.cpp
@tests/bsa_reader_tests.cpp

<interfaces>
Existing public API to preserve:
```cpp
class bsa_archive {
public:
    bsa_archive(archive_summary summary, std::vector<entry_metadata> entries);
    [[nodiscard]] const archive_summary& summary() const noexcept;
    [[nodiscard]] std::vector<archive_path> paths() const;
    [[nodiscard]] bool contains(std::string path) const;
    [[nodiscard]] result<entry_metadata> entry(std::string path) const;
};

[[nodiscard]] result<bsa_archive> open_bsa(const byte_source& source);
```

Existing internal hash contract:
```cpp
namespace libbsa::detail {
[[nodiscard]] std::uint64_t hash_tes3_path(std::string_view path);
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add generated TES3 metadata fixture tests</name>
  <files>tests/bsa_reader_tests.cpp</files>
  <read_first>tests/bsa_reader_tests.cpp; .planning/phases/05-tes3-bsa-read-and-extract/05-RESEARCH.md; TES5Edit/Core/wbBSArchive.pas lines 242-252 and 1112-1130</read_first>
  <behavior>
    - Test 1: A generated TES3 archive with magic `0x00000100`, one file named `meshes\marker.nif`, and payload bytes `{0x4d,0x57}` opens successfully and reports `archive_format::tes3_bsa`.
    - Test 2: The listed path is normalized to `meshes/marker.nif`.
    - Test 3: The entry reports `size == 2`, `packed_size == 2`, `stored_size == 2`, `compression == libbsa::compression_state::raw`, `directory_hash == 0`, and `offset == data_section_offset + relative_record_offset`.
    - Test 4: The stored `name_hash` equals `libbsa::detail::hash_tes3_path("meshes\\marker.nif")`.
  </behavior>
  <action>Create TES3-specific fixture helpers in `tests/bsa_reader_tests.cpp`: `VERSION_TES3 = 0x00000100`, a small `tes3_entry` struct, and `tes3_archive_bytes(...)` that writes header `{magic, hashOffset, fileCount}`, size/relative-offset pairs, name-offset table, null-terminated names, UInt64 hashes, and payload data. Add a test named exactly `open_bsa lists TES3 metadata with data-section-relative offsets`. Keep helpers local to the test file. Do not modify `TES5Edit/`.</action>
  <verify>
    <automated>ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug -R libbsa_bsa_reader_tests</automated>
  </verify>
  <acceptance_criteria>
    - `tests/bsa_reader_tests.cpp` contains `VERSION_TES3 = 0x00000100`.
    - `tests/bsa_reader_tests.cpp` contains `tes3_archive_bytes`.
    - `tests/bsa_reader_tests.cpp` contains `open_bsa lists TES3 metadata with data-section-relative offsets`.
    - The targeted CTest command initially fails before implementation and passes after Task 2.
  </acceptance_criteria>
  <done>TES3 metadata tests exist and encode the exact data-section-relative offset behavior required by BSA-04.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement TES3 metadata parsing in open_bsa</name>
  <files>include/libbsa/bsa.hpp, src/bsa_reader.hpp, src/bsa_reader.cpp</files>
  <read_first>include/libbsa/bsa.hpp; src/bsa_reader.hpp; src/bsa_reader.cpp; src/hash.hpp; .planning/phases/05-tes3-bsa-read-and-extract/05-RESEARCH.md</read_first>
  <behavior>
    - `open_bsa` routes first magic `0x00000100` to TES3 parsing and first magic `0x00415342` to the existing TES4-family path.
    - TES3 parser reads size/relative-offset records, validates name offsets, reads null-terminated names, reads UInt64 hashes, computes the data-section start after the hash table, and stores absolute payload offsets in `entry_metadata.offset`.
    - TES4 v103/v104/v105 tests remain green.
  </behavior>
  <action>Add TES3 constants to `src/bsa_reader.hpp`: `magic_tes3 = 0x00000100`, `tes3_header_size = 12`, `tes3_file_record_size = 8`, `tes3_name_offset_size = 4`, and `tes3_hash_size = 8`. Refactor `open_bsa` so it first reads 4 bytes, dispatches TES3 magic to a new internal parser, dispatches `detail::bsa_magic` to the existing parser logic, and preserves the existing unsupported error text `unsupported BSA magic` for unknown magic. In the TES3 parser, read `hashOffset` at byte 4 and `fileCount` at byte 8; validate all table ranges with checked arithmetic; parse each record as little-endian `{size, relativeOffset}`; parse each name offset as a little-endian u32 into the name block; derive `name_block_start = 12 + fileCount * 8 + fileCount * 4`; derive `hash_table_start = 12 + hashOffset`; derive `data_offset = hash_table_start + fileCount * 8`; validate `hash_table_start >= name_block_start` and `data_offset <= source.size()`. For each entry, read the null-terminated name starting at `name_block_start + nameOffset` and before `hash_table_start`, read the matching UInt64 hash, set `metadata.path` to the name, `metadata.size`, `packed_size`, and `stored_size` to the record size, `metadata.offset` to `data_offset + relativeOffset`, `metadata.name_hash` to the stored hash, `metadata.directory_hash = 0`, and `metadata.compression = compression_state::raw`. Update `include/libbsa/bsa.hpp` doc comments from TES4-family-only wording to BSA-family wording because the same API now opens TES3 and TES4-family BSA files.</action>
  <verify>
    <automated>ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug -R libbsa_bsa_reader_tests</automated>
  </verify>
  <acceptance_criteria>
    - `src/bsa_reader.hpp` contains `magic_tes3` and `tes3_header_size`.
    - `src/bsa_reader.cpp` contains `archive_format::tes3_bsa`.
    - `src/bsa_reader.cpp` contains `hash_table_start` and `data_offset`.
    - `include/libbsa/bsa.hpp` no longer describes `bsa_archive` as TES4-family only.
    - `ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug -R libbsa_bsa_reader_tests` exits 0.
  </acceptance_criteria>
  <done>`open_bsa` opens generated TES3 archives, lists normalized paths, and exposes correct metadata without regressing TES4-family behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| archive bytes → TES3 parser | Untrusted BSA bytes control counts, offsets, sizes, names, and hashes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | T/D | `open_bsa` TES3 parser | mitigate | Validate table arithmetic and ranges before allocation or reads; reject malformed name/hash/data ranges with `error_code::malformed_archive`. |
| T-05-02 | I | `include/libbsa/bsa.hpp` | mitigate | Keep public headers limited to libbsa-owned types; do not expose TES5Edit, libdeflate, LZ4, or platform headers. |
</threat_model>

<verification>
Run `ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug -R libbsa_bsa_reader_tests` and confirm TES3 metadata and existing TES4-family BSA tests pass.
</verification>

<success_criteria>
- `open_bsa` accepts TES3 magic `0x00000100`.
- TES3 entry metadata uses normalized paths and absolute payload offsets derived from TES3 data-section-relative records.
- Existing TES4-family open/list tests remain passing.
</success_criteria>

<output>
After completion, create `.planning/phases/05-tes3-bsa-read-and-extract/05-01-SUMMARY.md`
</output>
