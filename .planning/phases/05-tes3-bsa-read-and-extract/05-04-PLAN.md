---
phase: 05-tes3-bsa-read-and-extract
plan: 04
type: execute
wave: 4
depends_on: [05-03]
files_modified: [tests/public_header_smoke.cpp, README.md]
autonomous: true
requirements: [BSA-04]
must_haves:
  truths:
    - "Consumer-facing docs state TES3 BSA read/extract support"
    - "Public header smoke still compiles without dependency or TES5Edit leakage"
    - "Final validation proves TES3 and prior BSA-family behavior remain green"
  artifacts:
    - path: "README.md"
      provides: "TES3 BSA read/extract documentation"
      contains: "TES3"
    - path: "tests/public_header_smoke.cpp"
      provides: "Consumer compile smoke for BSA API"
      contains: "open_bsa"
  key_links:
    - from: "README.md"
      to: "include/libbsa/bsa.hpp"
      via: "documented open/list/extract API"
      pattern: "open_bsa"
---

<objective>
Finalize Phase 05 public documentation, smoke coverage, and full validation gates.

Purpose: BSA-04 should be visible to consumers and maintainers, with boundary checks proving TES5Edit and dependency implementation details remain private.
Output: README update, public smoke adjustment if needed, and final verification commands.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-tes3-bsa-read-and-extract/05-RESEARCH.md
@.planning/phases/05-tes3-bsa-read-and-extract/05-03-SUMMARY.md
@README.md
@tests/public_header_smoke.cpp
@include/libbsa/bsa.hpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document TES3 BSA support and compatibility constraints</name>
  <files>README.md</files>
  <read_first>README.md; .planning/phases/05-tes3-bsa-read-and-extract/05-RESEARCH.md; .planning/phases/05-tes3-bsa-read-and-extract/05-03-SUMMARY.md</read_first>
  <action>Update README BSA read/extract documentation to state that libbsa supports TES3/Morrowind BSA open, list, inspect, lookup, and raw extraction through `open_bsa` and `extract_bsa_entry`. Include the exact compatibility note: `TES3 file records store offsets relative to the data section; libbsa exposes absolute payload offsets in entry_metadata after validating the table range.` Also state TES3 payloads are raw and do not use TES4-family compression flags or embedded-name prefixes. Keep TES5Edit described as read-only reference material only.</action>
  <verify>
    <automated>ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug -R libbsa_bsa_reader_tests</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` contains `TES3/Morrowind BSA`.
    - `README.md` contains `offsets relative to the data section`.
    - `README.md` contains `entry_metadata`.
    - `README.md` contains `raw` in the TES3 support note.
  </acceptance_criteria>
  <done>README communicates TES3 support and the offset semantic that matters for consumers.</done>
</task>

<task type="auto">
  <name>Task 2: Expand public smoke and run final boundary gates</name>
  <files>tests/public_header_smoke.cpp</files>
  <read_first>tests/public_header_smoke.cpp; include/libbsa/bsa.hpp; README.md</read_first>
  <action>Update `tests/public_header_smoke.cpp` only if needed so consumer-style code includes `libbsa/bsa.hpp`, constructs or references `libbsa::bsa_archive`, and references `libbsa::open_bsa` / `libbsa::extract_bsa_entry` without including private headers. Then run final validation commands: targeted BSA reader tests, TES3 hash tests, public-header smoke, full CTest, public-header dependency token gate, CMake GLOB gate, and TES5Edit clean-status gate. Use grep gates that do not count comments when checking CMake GLOB patterns: `rg -n "^[^#]*\b(GLOB|GLOB_RECURSE)\b" CMakeLists.txt`.</action>
  <verify>
    <automated>ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug -R libbsa.public_header_smoke</automated>
    <automated>ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug</automated>
    <automated>rg -n "libdeflate|lz4\.h|lz4frame\.h|LZ4|DirectXTex|TES5Edit" include/libbsa; if ($LASTEXITCODE -eq 1) { exit 0 } else { exit $LASTEXITCODE }</automated>
    <automated>rg -n "^[^#]*\b(GLOB|GLOB_RECURSE)\b" CMakeLists.txt; if ($LASTEXITCODE -eq 1) { exit 0 } else { exit $LASTEXITCODE }</automated>
    <automated>git status --short TES5Edit</automated>
  </verify>
  <acceptance_criteria>
    - `tests/public_header_smoke.cpp` contains `libbsa/bsa.hpp`.
    - `tests/public_header_smoke.cpp` contains `open_bsa` or `extract_bsa_entry`.
    - Full CTest exits 0.
    - Public-header token gate returns no matches.
    - CMake GLOB gate returns no matches.
    - `git status --short TES5Edit` returns no output.
  </acceptance_criteria>
  <done>Phase 05 passes consumer smoke, full automated tests, and project boundary gates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public API → consumer build | Public headers must not expose private dependencies or TES5Edit implementation details. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | I | public headers/docs | mitigate | Run dependency-token and TES5Edit status gates; document reference behavior without compiling or modifying TES5Edit. |
</threat_model>

<verification>
Run targeted BSA tests, hash tests, public smoke, full CTest, public-header token gate, CMake GLOB gate, and TES5Edit clean-status gate.
</verification>

<success_criteria>
- README documents TES3/Morrowind BSA support and data-section-relative offset semantics.
- Public smoke compiles against libbsa-owned headers.
- Full validation is green and `TES5Edit/` remains unmodified.
</success_criteria>

<output>
After completion, create `.planning/phases/05-tes3-bsa-read-and-extract/05-04-SUMMARY.md`
</output>
