---
phase: 05-tes3-bsa-read-and-extract
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified: [src/bsa_reader.cpp, tests/bsa_reader_tests.cpp]
autonomous: true
requirements: [BSA-04]
must_haves:
  truths:
    - "Consumer can extract TES3 entry bytes through the streaming sink API"
    - "Consumer can look up TES3 entries by normalized slash or backslash paths"
    - "TES3 extraction preserves raw payload bytes without compression or embedded-name handling"
  artifacts:
    - path: "src/bsa_reader.cpp"
      provides: "TES3-compatible extraction using existing extract_bsa_entry path"
      contains: "compression_state::raw"
    - path: "tests/bsa_reader_tests.cpp"
      provides: "TES3 extraction and lookup tests"
      contains: "extract_bsa_entry writes raw TES3 bytes"
  key_links:
    - from: "extract_bsa_entry"
      to: "entry_metadata.offset"
      via: "absolute offset produced from TES3 data section"
      pattern: "read_payload_bytes.*metadata.value\(\).offset"
---

<objective>
Prove TES3 extraction works through the existing streaming sink API.

Purpose: BSA-04 is not satisfied by metadata alone; callers must extract Morrowind payload bytes without corruption.
Output: TES3 extraction tests and any needed extraction-path adjustments.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-tes3-bsa-read-and-extract/05-RESEARCH.md
@.planning/phases/05-tes3-bsa-read-and-extract/05-01-SUMMARY.md
@src/bsa_reader.cpp
@tests/bsa_reader_tests.cpp
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add TES3 extraction and lookup behavior tests</name>
  <files>tests/bsa_reader_tests.cpp</files>
  <read_first>tests/bsa_reader_tests.cpp; .planning/phases/05-tes3-bsa-read-and-extract/05-01-SUMMARY.md; .planning/phases/05-tes3-bsa-read-and-extract/05-RESEARCH.md</read_first>
  <behavior>
    - Test 1: `extract_bsa_entry` writes the exact TES3 payload bytes `{0x4d, 0x57, 0x21}` to `memory_sink`.
    - Test 2: Lookup succeeds when the caller uses `Meshes/Marker.NIF` even though the TES3 table stores `meshes\marker.nif`.
    - Test 3: Multiple TES3 entries with different relative offsets extract the correct bytes for each path.
  </behavior>
  <action>Add tests named exactly `extract_bsa_entry writes raw TES3 bytes` and `TES3 lookup uses normalized archive paths`. Reuse the TES3 fixture helper from Plan 05-01 and extend it to support at least two entries with distinct names, sizes, relative offsets, hashes, and payloads. Assert sink bytes exactly match the fixture payload vectors. Do not add compression headers or embedded-name prefixes for TES3 because TES3 payloads are raw.</action>
  <verify>
    <automated>ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug -R libbsa_bsa_reader_tests</automated>
  </verify>
  <acceptance_criteria>
    - `tests/bsa_reader_tests.cpp` contains `extract_bsa_entry writes raw TES3 bytes`.
    - `tests/bsa_reader_tests.cpp` contains `TES3 lookup uses normalized archive paths`.
    - `tests/bsa_reader_tests.cpp` contains `Meshes/Marker.NIF`.
  </acceptance_criteria>
  <done>Tests define exact TES3 extraction and normalized lookup behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Adjust extraction path only if TES3 tests expose a gap</name>
  <files>src/bsa_reader.cpp</files>
  <read_first>src/bsa_reader.cpp; tests/bsa_reader_tests.cpp; .planning/phases/05-tes3-bsa-read-and-extract/05-RESEARCH.md</read_first>
  <behavior>
    - For TES3 metadata with `compression_state::raw`, `extract_bsa_entry` reads exactly `stored_size` bytes from absolute `metadata.offset` and writes them unchanged.
    - The compression dispatcher is not invoked with deflate or LZ4 for TES3 entries.
  </behavior>
  <action>Run the targeted tests after Task 1. If they fail because extraction assumes TES4-family state, update `extract_bsa_entry` to treat `archive.summary().format == archive_format::tes3_bsa` as raw-only: read `metadata.stored_size` bytes from `metadata.offset`, skip no embedded name, read no uncompressed-size prefix, and write bytes through the existing `decompress_payload` path with `compression_algorithm::none` or directly through the sink. Preserve existing TES4-family embedded-name and compression behavior exactly. If the tests already pass because Plan 05-01 stored absolute offsets and raw state correctly, make no production-code change and record that in the summary.</action>
  <verify>
    <automated>ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug -R libbsa_bsa_reader_tests</automated>
  </verify>
  <acceptance_criteria>
    - Targeted CTest exits 0.
    - `src/bsa_reader.cpp` still contains `archive_embed_name` for TES4-family embedded-name handling.
    - `src/bsa_reader.cpp` still contains `resolve_payload_codec` for compressed TES4-family entries.
  </acceptance_criteria>
  <done>TES3 payload bytes extract exactly and existing TES4-family extraction behavior remains intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TES3 metadata → sink write | Untrusted offsets and sizes select bytes written to caller-owned sink. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02 | T/D | `extract_bsa_entry` | mitigate | Reuse `read_payload_bytes` range validation before sink writes; preserve structured failure on impossible payload ranges. |
</threat_model>

<verification>
Run `ctest --test-dir build/local-vs2026-vcpkg --output-on-failure -C Debug -R libbsa_bsa_reader_tests`.
</verification>

<success_criteria>
- TES3 entries extract byte-identical raw payloads.
- TES3 lookup accepts normalized caller paths.
- TES4-family extraction tests continue to pass.
</success_criteria>

<output>
After completion, create `.planning/phases/05-tes3-bsa-read-and-extract/05-02-SUMMARY.md`
</output>
