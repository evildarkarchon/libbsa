---
phase: 01-foundation-api-boundary-and-test-harness
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - CMakeLists.txt
  - tests/CMakeLists.txt
  - tests/unit/result_tests.cpp
  - tests/unit/archive_reader_tests.cpp
  - tests/unit/public_include_boundary_tests.cpp
  - tests/unit/local_game_fixture_tests.cpp
autonomous: true
requirements: [FND-06, FND-03, FND-05]
must_haves:
  truths:
    - "Maintainer can run Catch2 tests through CTest and select `[unit]` tests with `ctest -L unit` per D-25."
    - "Tests requiring local game archives are discovered but skipped by default with setup hints per D-31."
    - "Required future label taxonomy is represented in test/CMake wiring or fixture docs per D-34."
  artifacts:
    - path: "tests/CMakeLists.txt"
      provides: "Catch2/CTest integration"
      contains: "ADD_TAGS_AS_LABELS"
    - path: "tests/unit/local_game_fixture_tests.cpp"
      provides: "Discovered skip-by-default local fixture test"
      contains: "LIBBSA_GAME_FIXTURES"
  key_links:
    - from: "tests/CMakeLists.txt"
      to: "tests/unit/*.cpp"
      via: "libbsa_tests executable"
      pattern: "catch_discover_tests\(libbsa_tests"
---

<objective>
Wire the Catch2/CTest harness and label taxonomy around the public boundary tests created by Plan 02.

Purpose: Phase 1 requires maintainers to run labeled tests immediately and gives later fixture/compatibility phases a stable test taxonomy.
Output: Test CMake integration, unit target, label discovery, and skipped local-game-fixture smoke test.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-api-boundary-and-test-harness/01-SPEC.md
@.planning/phases/01-foundation-api-boundary-and-test-harness/01-CONTEXT.md
@.planning/phases/01-foundation-api-boundary-and-test-harness/01-RESEARCH.md
@CMakeLists.txt
@tests/unit/result_tests.cpp
@tests/unit/archive_reader_tests.cpp
@tests/unit/public_include_boundary_tests.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Catch2 test target and CTest discovery labels</name>
  <files>CMakeLists.txt, tests/CMakeLists.txt</files>
  <read_first>CMakeLists.txt, tests/CMakeLists.txt if it exists, vcpkg.json, .planning/phases/01-foundation-api-boundary-and-test-harness/01-CONTEXT.md</read_first>
  <action>Create `tests/CMakeLists.txt` with `find_package(Catch2 CONFIG REQUIRED)`, `add_executable(libbsa_tests tests source list relative to the tests directory or current source directory)`, `target_link_libraries(libbsa_tests PRIVATE libbsa::libbsa Catch2::Catch2WithMain)`, `target_compile_features(libbsa_tests PRIVATE cxx_std_20)`, `include(Catch)`, and `catch_discover_tests(libbsa_tests ADD_TAGS_AS_LABELS DISCOVERY_MODE PRE_TEST)`. Ensure root `CMakeLists.txt` enables this file only under `if(LIBBSA_BUILD_TESTS) include(CTest) enable_testing() add_subdirectory(tests) endif()`. Keep Catch2 test-only; do not link Catch2 into `libbsa`.</action>
  <verify>
    <automated>cmake --preset windows-msvc-debug-static</automated>
  </verify>
  <acceptance_criteria>
    - `tests/CMakeLists.txt` contains `find_package(Catch2 CONFIG REQUIRED)`.
    - `tests/CMakeLists.txt` contains `Catch2::Catch2WithMain`.
    - `tests/CMakeLists.txt` contains `catch_discover_tests(libbsa_tests`.
    - `tests/CMakeLists.txt` contains `ADD_TAGS_AS_LABELS`.
    - `CMakeLists.txt` contains `enable_testing()`.
  </acceptance_criteria>
  <done>CMake configures a Catch2 test executable and maps Catch2 tags to CTest labels.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add local game fixture discovery skip test</name>
  <files>tests/unit/local_game_fixture_tests.cpp, tests/CMakeLists.txt</files>
  <read_first>tests/unit/local_game_fixture_tests.cpp if it exists, tests/CMakeLists.txt, .planning/phases/01-foundation-api-boundary-and-test-harness/01-CONTEXT.md</read_first>
  <behavior>
    - Test is discovered by CTest because it is compiled into `libbsa_tests`.
    - Test has tags `[requires-game-fixture][unit]`.
    - If `LIBBSA_GAME_FIXTURES` is unset or empty, test calls `SKIP` with text containing `tests/fixtures/local` and `LIBBSA_GAME_FIXTURES`.
    - If `LIBBSA_GAME_FIXTURES` is set, Phase 1 does not assert real archives; it only checks the path string is non-empty.
  </behavior>
  <action>Create `tests/unit/local_game_fixture_tests.cpp` using Catch2. Use `std::getenv("LIBBSA_GAME_FIXTURES")`. When absent or empty, call `SKIP("Set LIBBSA_GAME_FIXTURES or place local game archives under tests/fixtures/local; these files are not committed.")`. Add the file to `libbsa_tests` in `tests/CMakeLists.txt`. Do not read game files or `TES5Edit/`; this is a discovery/skip behavior test only per D-31.</action>
  <verify>
    <automated>ctest --preset windows-msvc-debug-static -L requires-game-fixture --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `tests/unit/local_game_fixture_tests.cpp` contains `[requires-game-fixture]`.
    - `tests/unit/local_game_fixture_tests.cpp` contains `LIBBSA_GAME_FIXTURES`.
    - `tests/unit/local_game_fixture_tests.cpp` contains `tests/fixtures/local`.
    - `tests/CMakeLists.txt` contains `local_game_fixture_tests.cpp`.
  </acceptance_criteria>
  <done>Local game fixture tests are discoverable by label and skipped by default with actionable setup hints.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test runner→environment | Tests observe `LIBBSA_GAME_FIXTURES` from the local environment. |
| Tests→library target | Test code links against public `libbsa::libbsa`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Information Disclosure | Local fixture tests | mitigate | Default behavior skips when local fixtures are absent and never commits or reads copyrighted data in Phase 1. |
| T-01-05 | Tampering | Test linkage | mitigate | Link Catch2 only to `libbsa_tests`; keep runtime library target independent. |
</threat_model>

<verification>
Run `ctest --preset windows-msvc-debug-static -L unit --output-on-failure` and `ctest --preset windows-msvc-debug-static -N` to confirm discovered labels.
</verification>

<success_criteria>
FND-06 is satisfied when Catch2 tests are discoverable through CTest, `ctest -L unit` selects public-boundary tests, and the local fixture test is discoverable but skipped by default.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-api-boundary-and-test-harness/01-03-SUMMARY.md`
</output>
