---
phase: 07-tes4-family-bsa-write-new-support
plan: 06
type: tdd
wave: 6
depends_on: [05]
files_modified:
  - src/formats/bsa/tes4_bsa_writer.cpp
  - tests/unit/tes4_bsa_writer_tests.cpp
  - tests/unit/public_include_boundary_tests.cpp
autonomous: true
requirements: [WBSA-01, WBSA-02, WBSA-03, WBSA-05, WBSA-06, WBSA-07, WBSA-08, WBSA-09, WBSA-10]
requirements_addressed: [WBSA-01, WBSA-02, WBSA-03, WBSA-05, WBSA-06, WBSA-07, WBSA-08, WBSA-09, WBSA-10]
must_haves:
  truths:
    - "Deduplication is disabled by default and enabled only by explicit option."
    - "Deduplication shares offsets only for identical final stored bytes after compression, embedded-name, and size-prefix encoding."
    - "Complete Phase 7 writer output passes public API, writer, reader, and TES5Edit read-only gates."
    - "D-19: Opt-in deduplication shares payload offsets only when final stored bytes are identical after compression policy, embedded-name policy, and size-prefix encoding."
    - "D-22: Final writer acceptance uses archive_reader metadata, lookup behavior, extraction bytes, and stable errors."
    - "D-23: Final tests preserve generated legal fixture policy and never use TES5Edit as a mutable workspace."
  artifacts:
    - path: "src/formats/bsa/tes4_bsa_writer.cpp"
      provides: "Final stored-byte dedupe and phase hardening"
    - path: "tests/unit/tes4_bsa_writer_tests.cpp"
      provides: "Dedupe enabled/disabled and full round-trip coverage"
    - path: "tests/unit/public_include_boundary_tests.cpp"
      provides: "Final public boundary smoke with writer API"
  key_links:
    - from: "src/formats/bsa/tes4_bsa_writer.cpp"
      to: "archive_reader metadata tests"
      via: "shared payload offsets observed through reopened entry metadata"
      pattern: "deduplicate_payloads"
---

<objective>
Finish Phase 7 by adding opt-in final-stored-byte deduplication and full regression gates.

Purpose: Completes WBSA-09 and closes WBSA-10 across all previously planned writer behavior while preserving D-19, D-22, and D-23.

Decision coverage: D-19: opt-in deduplication shares offsets only when final stored bytes are identical after compression policy, embedded-name policy, and size-prefix encoding. D-22: final acceptance uses public `archive_reader` metadata, lookup behavior, extraction bytes, and stable errors. D-23: final writer-output coverage uses generated legal synthetic data and verifies the `TES5Edit/` submodule remains untouched.
Output: Deduplicating writer option plus final round-trip and boundary verification.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-tes4-family-bsa-write-new-support/07-SPEC.md
@.planning/phases/07-tes4-family-bsa-write-new-support/07-CONTEXT.md
@.planning/phases/07-tes4-family-bsa-write-new-support/07-RESEARCH.md
@.planning/phases/07-tes4-family-bsa-write-new-support/07-PATTERNS.md
@src/formats/bsa/tes4_bsa_writer.cpp
@tests/unit/tes4_bsa_writer_tests.cpp
@tests/unit/public_include_boundary_tests.cpp
@.planning/phases/03-format-detection-and-tes4-family-bsa-read-extract/03-05-SUMMARY.md
@.planning/phases/03-format-detection-and-tes4-family-bsa-read-extract/03-06-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 RED: Add final-stored-byte dedupe tests</name>
  <read_first>
    - `.planning/phases/07-tes4-family-bsa-write-new-support/07-CONTEXT.md` decisions D-19, D-22, D-23
    - `.planning/phases/07-tes4-family-bsa-write-new-support/07-SPEC.md` requirements 7 and 8
    - `.planning/phases/07-tes4-family-bsa-write-new-support/07-RESEARCH.md` Pitfall 5
    - `tests/unit/tes4_bsa_writer_tests.cpp`
  </read_first>
  <files>tests/unit/tes4_bsa_writer_tests.cpp</files>
  <action>Add RED tests that write duplicate-content entries twice: first with `tes4_bsa_writer_options{.deduplicate_payloads = false}` and assert reopened entries have distinct `payload_offset` values while extracting equal source bytes; second with `.deduplicate_payloads = true` and assert entries with identical final stored bytes share `payload_offset` and `stored_size`. Include a negative dedupe case where source bytes are identical but final stored bytes differ because one entry is `entry_compression_policy::raw` and another is `entry_compression_policy::compressed`, and assert offsets remain distinct. Include an embedded-name-enabled negative case where identical source bytes with different file names do not dedupe because final stored bytes include different embedded-name prefixes. Use only synthetic repository-generated temp data and public `archive_reader` metadata/extraction APIs.</action>
  <verify>
    <automated>ctest --preset windows-msvc-debug-static -L tes4_bsa_writer --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `tests/unit/tes4_bsa_writer_tests.cpp` contains `deduplicate_payloads = false` and `deduplicate_payloads = true`.
    - `tests/unit/tes4_bsa_writer_tests.cpp` asserts both shared and distinct `payload_offset` cases.
    - `tests/unit/tes4_bsa_writer_tests.cpp` contains negative cases for compression-policy mismatch and embedded-name prefix mismatch.
    - The automated command fails before Task 2 because final-stored-byte dedupe is incomplete.
  </acceptance_criteria>
  <done>RED tests prove dedupe default, opt-in sharing, and D-19 stored-encoding eligibility rules.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 GREEN: Implement final-stored-byte dedupe and full phase gates</name>
  <read_first>
    - `src/formats/bsa/tes4_bsa_writer.cpp`
    - `tests/unit/tes4_bsa_writer_tests.cpp`
    - `tests/unit/public_include_boundary_tests.cpp`
    - `.planning/phases/07-tes4-family-bsa-write-new-support/07-SPEC.md`
  </read_first>
  <files>src/formats/bsa/tes4_bsa_writer.cpp, tests/unit/public_include_boundary_tests.cpp</files>
  <action>After each entry's complete stored payload vector is built, including embedded-name prefix, compression raw-size prefix, and compressed/raw bytes, implement dedupe only when `options.deduplicate_payloads == true`. Use a deterministic in-memory key based on final stored bytes and stored size; a standard `std::map<std::vector<std::byte>, payload_assignment>` or equivalent byte-exact lookup is acceptable because Phase 7 does not require cryptographic hashing or performance tuning. Assign the same archive-absolute payload offset only when final stored bytes are byte-identical; otherwise allocate the next payload offset. Leave dedupe disabled by default. Extend public boundary smoke if needed so writer construction, options, `add_bytes`, and `write_to` remain visible from installed headers without private tokens. Run the full writer/read/public boundary gates and fix only Phase 7 regressions.</action>
  <verify>
    <automated>ctest --preset windows-msvc-debug-static -L tes4_bsa_writer --output-on-failure</automated>
    <automated>ctest --preset windows-msvc-debug-static -R "tes4_bsa_reader|public_include_boundary" --output-on-failure</automated>
    <automated>pwsh -NoProfile -Command '$status = git -C TES5Edit status --short; if ($status) { throw "TES5Edit has unexpected changes: $status" }'</automated>
  </verify>
  <acceptance_criteria>
    - `src/formats/bsa/tes4_bsa_writer.cpp` contains `deduplicate_payloads` and performs lookup after final stored payload construction, not before compression or embedded-name prefixing.
    - Dedupe-disabled tests observe distinct offsets for duplicate source bytes.
    - Dedupe-enabled tests observe shared offsets only for identical final stored bytes.
    - Compression-mismatch and embedded-name-mismatch negative cases observe distinct offsets.
    - All automated verification commands pass.
  </acceptance_criteria>
  <done>Final Phase 7 writer behavior passes dedupe, round-trip, public boundary, reader regression, and TES5Edit read-only gates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| encoded payloads→dedupe map | Final stored bytes determine shared archive offsets. |
| phase output→full test suite | Writer output is validated against reader and public API boundaries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06-01 | Tampering | dedupe eligibility | mitigate | Compare final stored bytes after all encoding steps; never dedupe source bytes alone. |
| T-07-06-02 | Repudiation | test oracle | mitigate | Validate offsets, metadata, lookup, and extraction through public `archive_reader` APIs per D-22. |
| T-07-06-03 | Denial of Service | dedupe map memory | accept | Phase 7 single-threaded correctness uses small synthetic tests; performance/bounded-memory tuning is Phase 12. |
| T-07-06-04 | Tampering | TES5Edit boundary | mitigate | Automated `git -C TES5Edit status --short` gate must produce no output. |
</threat_model>

<verification>
Run the full automated commands listed in Task 2. If local compiler/vcpkg environment is unavailable, record the environment blocker exactly and do not mark the plan complete.
</verification>

<success_criteria>
All Phase 7 requirements WBSA-01, WBSA-02, WBSA-03, WBSA-05, WBSA-06, WBSA-07, WBSA-08, WBSA-09, and WBSA-10 are covered by passing writer tests, public boundary tests, reader regression tests, and an empty TES5Edit status gate.
</success_criteria>

<output>
After completion, create `.planning/phases/07-tes4-family-bsa-write-new-support/07-06-SUMMARY.md`.
</output>
