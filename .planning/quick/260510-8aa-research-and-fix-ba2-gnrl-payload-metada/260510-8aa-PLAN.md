---
status: ready
created: 2026-05-10
quick_id: 260510-8aa
slug: research-and-fix-ba2-gnrl-payload-metada
---

# Quick Task 260510-8aa: Research and fix BA2 GNRL payload metadata-overlap rejection

## Goal

Verify the BA2 GNRL metadata-overlap review finding against TES5Edit/BSArchPro behavior, then make libbsa reject non-empty GNRL payload spans that intersect the fixed header or record table before materializing entries.

## Tasks

1. Trace TES5Edit/BSArchPro BA2 GNRL read/write behavior and confirm whether offsets into metadata are compatible or malformed.
2. Add a focused failing BA2 GNRL regression test for a non-empty payload span at offset zero.
3. Pass the computed record-table end into GNRL entry materialization and reject payload spans intersecting `[0, records_end)`.
4. Run focused BA2 GNRL tests, fixture validation if needed, and confirm `TES5Edit/` remains untouched.
