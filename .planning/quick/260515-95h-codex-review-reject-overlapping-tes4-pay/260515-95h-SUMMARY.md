---
phase: 260515-95h-codex-review-reject-overlapping-tes4-pay
status: complete
plan: 01
subsystem: parser-hardening
tags: [cpp20, tes4-bsa, host-path, validation, catch2]

requires:
  - phase: 17.1
    provides: Shared host-file path seam and TES4 parser hardening baseline
provides:
  - TES4-family BSA partial stored-payload overlap rejection before entry metadata publication
  - Exact duplicate TES4 stored-payload spans remain accepted for writer dedupe compatibility
  - Embedded-NUL rejection at the public UTF-8 host path boundary
affects: [tes4-bsa-parser, host-file-path, malformed-fixtures]

tech-stack:
  added: []
  patterns:
    - Track accepted non-empty stored payload spans and reject partial overlaps while allowing exact duplicates
    - Validate public host path text before Windows UTF-8 to native path conversion

key-files:
  created: []
  modified:
    - tests/unit/tes4_bsa_reader_tests.cpp
    - tests/unit/host_file_path_tests.cpp
    - src/formats/bsa/tes4_bsa_parser.cpp
    - src/detail/host_file_path.cpp

key-decisions:
  - "TES4 payload span validation mirrors the existing BA2 GNRL policy: skip zero-length spans, allow exact duplicates for dedupe, reject partial overlaps."
  - "Embedded NUL bytes are invalid public host path text and are rejected before UTF-8 decoding or native path construction."
patterns-established:
  - "TES4 span validation uses parser-local stored_payload_span tracking before entry_metadata publication."
requirements-completed: [QUICK-260515-95h]

duration: 18min
completed: 2026-05-15
---

# Quick Task 260515-95h: Codex Review Hardening Summary

**TES4-family BSA stored-payload span ambiguity rejection plus embedded-NUL host path boundary validation.**

## Performance

- **Duration:** 18 min
- **Started:** 2026-05-15T13:42:11Z
- **Completed:** 2026-05-15T14:00:00Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments

- Added focused regressions proving TES4 partial payload overlaps fail through direct open and validation surfaces.
- Preserved exact duplicate TES4 non-empty payload spans as valid dedupe-compatible metadata.
- Rejected embedded NUL bytes in public UTF-8 host path text before Windows native path conversion.

## Task Commits

1. **Task 1: Add focused failing regressions for TES4 payload overlaps and embedded-NUL host paths** - `c5400e1` (test)
2. **Task 2: Implement TES4 span tracking and embedded-NUL rejection** - `de7bccc` (fix)

## Files Created/Modified

- `tests/unit/tes4_bsa_reader_tests.cpp` - Added TES4 partial-overlap rejection and exact-duplicate acceptance regressions.
- `tests/unit/host_file_path_tests.cpp` - Added embedded-NUL host path rejection regression.
- `src/formats/bsa/tes4_bsa_parser.cpp` - Tracks accepted stored payload spans during entry materialization and rejects partial overlaps.
- `src/detail/host_file_path.cpp` - Rejects embedded NUL bytes before copying, decoding, or constructing native paths.

## Decisions Made

- TES4 stored-payload span validation follows BA2 GNRL semantics for consistency: zero-length spans are ignored, exact duplicates are allowed for writer dedupe, and any other overlap is a format error.
- Host path embedded NUL detection is done on the original `std::string_view` so explicit-length caller text cannot be truncated by later NUL-terminated Windows APIs.

## Deviations from Plan

None - plan executed exactly as written.

## Threat Flags

None - the task mitigated the two threat-model items from the plan and introduced no new endpoints, file access surfaces, or schema boundaries.

## Known Stubs

None.

## Issues Encountered

- The requested preset `windows-msvc-debug` was not present locally. Used the equivalent `windows-msvc-debug-static` preset listed by CMake.

## Validation

- RED verification: `cmake --build --preset windows-msvc-debug-static --target libbsa_tests && ctest --preset windows-msvc-debug-static -R "tes4_bsa_malformed_open|host_file_path" --output-on-failure` failed as expected for the new TES4 partial-overlap and embedded-NUL tests before production changes.
- Final verification: `cmake --build --preset windows-msvc-debug-static --target libbsa_tests && ctest --preset windows-msvc-debug-static -R "tes4_bsa_malformed_open|host_file_path" --output-on-failure` passed: 21/21 focused tests.

## User Setup Required

None - no external service configuration required.

## Self-Check: PASSED

- Confirmed task commits exist: `c5400e1`, `de7bccc`.
- Confirmed summary created at `.planning/quick/260515-95h-codex-review-reject-overlapping-tes4-pay/260515-95h-SUMMARY.md`.
- Confirmed no files under `TES5Edit/` were staged or committed.

---
*Quick task: 260515-95h-codex-review-reject-overlapping-tes4-pay*
*Completed: 2026-05-15*
