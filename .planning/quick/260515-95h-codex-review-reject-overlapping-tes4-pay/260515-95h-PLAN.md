---
phase: 260515-95h-codex-review-reject-overlapping-tes4-pay
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/tes4_bsa_reader_tests.cpp
  - src/formats/bsa/tes4_bsa_parser.cpp
  - tests/unit/host_file_path_tests.cpp
  - src/detail/host_file_path.cpp
autonomous: true
requirements:
  - QUICK-260515-95h
must_haves:
  truths:
    - "TES4-family BSA parsing rejects non-empty partially overlapping payload ranges before exposing entry metadata."
    - "TES4-family BSA parsing still accepts exact duplicate non-empty payload ranges as valid writer dedupe."
    - "Public UTF-8 host path resolution rejects embedded NUL bytes before native Windows path conversion or file I/O."
  artifacts:
    - path: "src/formats/bsa/tes4_bsa_parser.cpp"
      provides: "TES4 stored-payload span tracking in materialize_entries"
      contains: "TES4 BSA entry payload spans partially overlap"
    - path: "tests/unit/tes4_bsa_reader_tests.cpp"
      provides: "Malformed partial-overlap and exact-duplicate TES4 reader regressions"
      contains: "tes4_bsa_malformed_open rejects partially overlapping payload spans"
    - path: "src/detail/host_file_path.cpp"
      provides: "Embedded-NUL rejection at public host-file path boundary"
      contains: "embedded NUL"
    - path: "tests/unit/host_file_path_tests.cpp"
      provides: "Embedded-NUL host path regression"
      contains: "rejects embedded NUL"
  key_links:
    - from: "src/formats/bsa/tes4_bsa_parser.cpp"
      to: "src/formats/bsa/tes4_bsa_payload_descriptor.cpp"
      via: "make_tes4_bsa_payload_descriptor output payload_offset/stored_size"
      pattern: "make_tes4_bsa_payload_descriptor"
    - from: "src/formats/bsa/tes4_bsa_parser.cpp"
      to: "src/formats/ba2/ba2_gnrl_parser.cpp"
      via: "Same exact-duplicate-allowed, partial-overlap-rejected span semantics"
      pattern: "accepted_payload_spans"
    - from: "tests/unit/host_file_path_tests.cpp"
      to: "src/detail/host_file_path.cpp"
      via: "resolve_host_file_path(std::string_view{..., size}) returns invalid_argument before conversion"
      pattern: "resolve_host_file_path"
---

<objective>
Reject two Codex review hardening gaps in one focused change: malformed TES4-family BSA archives with ambiguous partially overlapping payload ranges, and public host paths containing embedded NUL bytes that Windows APIs could truncate at use.

Purpose: Preserve strict archive metadata publication and host-file boundary correctness without changing public API shape or adding dependencies.
Output: Parser/path validation changes plus focused Catch2 regressions for malformed TES4 spans, exact duplicate TES4 dedupe spans, and embedded-NUL host paths.
</objective>

<execution_context>
@J:/libbsa/.opencode/get-shit-done/workflows/execute-plan.md
@J:/libbsa/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260515-95h-codex-review-reject-overlapping-tes4-pay/260515-95h-RESEARCH.md
@AGENTS.md
@src/formats/bsa/tes4_bsa_parser.cpp
@src/formats/bsa/tes4_bsa_payload_descriptor.cpp
@src/formats/ba2/ba2_gnrl_parser.cpp
@src/formats/ba2/ba2_dx10_parser.cpp
@src/detail/host_file_path.cpp
@tests/unit/tes4_bsa_reader_tests.cpp
@tests/unit/host_file_path_tests.cpp

<interfaces>
Existing TES4 parser seam:
- `make_tes4_bsa_payload_descriptor(table.header, record, archive_size, table.metadata_table_size, read_payload_bytes)` returns descriptor fields used by `entry_metadata`: `raw_size`, `stored_size`, `payload_offset`, `compression`, `embedded_prefix_size`.
- `materialize_entries` currently validates canonical paths and TES4 file record hashes before calling the descriptor seam; preserve this error precedence.

Existing BA2 overlap policy to mirror:
- Store accepted non-empty spans as `{offset, size}`.
- Skip zero-length spans.
- Accept exact duplicate non-empty `{offset, size}` pairs for writer dedupe.
- Reject every other overlap as `error_code::format_error` before pushing `entry_metadata`.

Existing host path seam:
- `libbsa::detail::resolve_host_file_path(std::string_view host_path)` is the shared public UTF-8-to-native boundary for reader/writer/validation host paths.
- It returns `error_code::invalid_argument` for malformed public path text.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add focused failing regressions for TES4 payload overlaps and embedded-NUL host paths</name>
  <files>tests/unit/tes4_bsa_reader_tests.cpp, tests/unit/host_file_path_tests.cpp</files>
  <behavior>
    - TES4 partial overlap: mutate `tes4_v103.bsa` so two non-empty file records have stored payload spans that overlap but are not identical; `archive_reader::open` returns `format_error`, and `validate_archive` reports invalid with one `format_error`.
    - TES4 exact duplicate: mutate `tes4_v103.bsa` so the same two non-empty file records have identical payload offset and stored size; `archive_reader::open` succeeds and the affected entries expose the same `payload_offset` and `stored_size`.
    - Host path embedded NUL: call `resolve_host_file_path(std::string_view{"prefix\0suffix", 13})`; it returns `invalid_argument`.
  </behavior>
  <action>Add tests before production changes. In `tests/unit/tes4_bsa_reader_tests.cpp`, place the partial-overlap and exact-duplicate cases beside the existing payload metadata malformed tests. Reuse existing helpers such as `read_binary_file`, `write_binary_file`, `read_u32_le`, `overwrite_u32_le`, `generated_archive_path`, and `std::filesystem::temp_directory_path`; derive file record offsets from the TES4 v103 fixture metadata the same way the existing payload-inside-metadata test does, then edit the second file record offset/size fields rather than creating new fixtures. In `tests/unit/host_file_path_tests.cpp`, add the embedded-NUL regression beside the malformed UTF-8 test and pass an explicit-length `std::string_view` so the suffix remains part of caller-provided text. Do not modify `TES5Edit/` or add dependencies.</action>
  <verify>
    <automated>cmake --build --preset windows-msvc-debug --target libbsa_tests &amp;&amp; ctest --preset windows-msvc-debug -R "tes4_bsa_malformed_open|host_file_path" --output-on-failure</automated>
  </verify>
  <done>The new tests fail against the current implementation for TES4 partial overlap and embedded NUL, while the exact duplicate test documents the allowed dedupe behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement TES4 span tracking and embedded-NUL rejection</name>
  <files>src/formats/bsa/tes4_bsa_parser.cpp, src/detail/host_file_path.cpp</files>
  <behavior>
    - TES4 non-empty partial span overlap returns `error_code::format_error` before `entry_metadata` is pushed.
    - TES4 exact duplicate non-empty span is accepted and tracked as dedupe-compatible.
    - Zero-length TES4 spans do not participate in overlap rejection.
    - Host paths containing `\0` return `error_code::invalid_argument` before `MultiByteToWideChar` or `std::filesystem::path` construction.
  </behavior>
  <action>In `src/formats/bsa/tes4_bsa_parser.cpp`, add a private stored-span helper and `spans_overlap_u64` equivalent near the anonymous-namespace helpers, using `std::uint64_t` offsets and sizes. In `materialize_entries`, reserve a `std::vector` for `table.header.file_count` spans with `detail::reserve_metadata_vector(..., "TES4 BSA stored payload spans")`. After `make_tes4_bsa_payload_descriptor` succeeds and before `entries.push_back`, inspect `payload.value().payload_offset` plus `payload.value().stored_size`: skip zero stored size, allow exact duplicate offset+size, and reject any other overlap with `error{error_code::format_error, "TES4 BSA entry payload spans partially overlap"}`. Add a short comment explaining that exact duplicates are intentional writer dedupe while partial sharing makes extraction ambiguous. In `src/detail/host_file_path.cpp`, reject `host_path.find('\0') != std::string_view::npos` before copying/converting path text, returning `invalid_argument` with a message that names embedded NUL bytes; keep the existing empty-path behavior and UTF-8 validation behavior unchanged. Do not introduce public API changes or external dependencies.</action>
  <verify>
    <automated>cmake --build --preset windows-msvc-debug --target libbsa_tests &amp;&amp; ctest --preset windows-msvc-debug -R "tes4_bsa_malformed_open|host_file_path" --output-on-failure</automated>
  </verify>
  <done>Focused tests pass; TES4 parser rejects partial overlaps but not exact duplicate dedupe spans; embedded-NUL host paths are rejected before Windows-native path conversion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Archive bytes -> TES4 parser | Untrusted BSA file records become public `entry_metadata` and later extraction offsets. |
| Public UTF-8 host path -> Windows native path | Caller-provided `std::string_view` becomes a `std::filesystem::path` used by Windows file APIs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260515-95h-01 | Tampering | `tes4_bsa_parser.cpp::materialize_entries` | mitigate | Track accepted non-empty TES4 payload spans and reject partial overlaps before publishing metadata; allow only exact duplicate dedupe spans. |
| T-260515-95h-02 | Tampering / Elevation of privilege | `detail::resolve_host_file_path` | mitigate | Reject embedded NUL bytes before UTF-8 decoding or native path construction so later NUL-terminated Windows APIs cannot operate on a caller-hidden prefix. |
</threat_model>

<verification>
Run the focused debug lane:
`cmake --build --preset windows-msvc-debug --target libbsa_tests && ctest --preset windows-msvc-debug -R "tes4_bsa_malformed_open|host_file_path" --output-on-failure`

If the preset name differs locally, inspect `CMakePresets.json` and run the equivalent Windows MSVC debug test preset for `libbsa_tests` with the same CTest regex.
</verification>

<success_criteria>
- TES4 partial payload overlaps are rejected with `format_error` through both direct open and validation surfaces.
- TES4 exact duplicate non-empty payload spans remain accepted for dedupe-compatible archives.
- Embedded NUL bytes in public host path text return `invalid_argument` before conversion/opening.
- No changes are made under `TES5Edit/` and no new dependencies are introduced.
</success_criteria>

<output>
After completion, create `.planning/quick/260515-95h-codex-review-reject-overlapping-tes4-pay/260515-95h-SUMMARY.md`.
</output>
