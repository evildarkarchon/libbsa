---
quick_id: 260510-6by
status: complete
created: 2026-05-10
description: "Fix vcpkg registry baseline CI failure"
---

# Quick Task 260510-6by: Fix vcpkg Registry Baseline CI Failure

## Goal

Fix CI configure failures where vcpkg cannot read `versions/baseline.json` for the pinned registry commit before the Windows static/shared builds start.

## Tasks

1. Inspect the vcpkg manifest, vcpkg registry configuration, and CI preset path.
2. Remove duplicate baseline ownership so the project has one authoritative registry baseline.
3. Make the default vcpkg registry explicit enough for CI to fetch the pinned registry commit instead of relying on the hosted image's local `C:\vcpkg` Git history.
4. Validate JSON parsing, vcpkg dependency resolution, and both Windows MSVC configure presets.

## Constraints

- Do not edit, format, stage, or commit anything under `TES5Edit/`.
- Keep the existing pinned baseline unless validation shows it is invalid.
- Do not widen this into dependency-version or package-feature changes.
