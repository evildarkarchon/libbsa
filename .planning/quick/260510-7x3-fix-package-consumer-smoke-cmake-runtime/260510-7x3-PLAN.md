---
status: ready
created: 2026-05-10
quick_id: 260510-7x3
slug: fix-package-consumer-smoke-cmake-runtime
---

# Quick Task 260510-7x3: Fix package consumer smoke CMake runtime DLL copy syntax

## Goal

Fix the `package_consumer_smoke` consumer build so its post-build runtime DLL copy command uses CMake's documented `copy <file>... destination` syntax and remains runnable through the installed-package smoke path.

## Tasks

1. Update `tests/package-consumer/CMakeLists.txt` to remove the unsupported `cmake -E copy -t` invocation and pass the runtime DLL list before the target directory.
2. Preserve the concrete LZ4 runtime dependency in installed shared-package exports so the fixed copy command produces a runnable consumer executable.
3. Verify the package consumer smoke test through the repo's Windows MSVC debug static and shared presets.
