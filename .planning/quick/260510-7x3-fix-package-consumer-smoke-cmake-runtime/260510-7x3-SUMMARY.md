---
status: complete
quick_id: 260510-7x3
completed: 2026-05-10
---

# Quick Task 260510-7x3 Summary

## Result

Fixed the package consumer smoke build so its runtime DLL copy command uses the documented `cmake -E copy <file>... destination` shape instead of the unsupported `-t` option.

## Changes

- Updated `tests/package-consumer/CMakeLists.txt` to pass `$<TARGET_RUNTIME_DLLS:libbsa_package_consumer>` before `$<TARGET_FILE_DIR:libbsa_package_consumer>`.
- Updated root `CMakeLists.txt` to prefer concrete vcpkg `LZ4::lz4_static` / `LZ4::lz4_shared` targets when present, so installed shared exports preserve the LZ4 runtime DLL for consumer smoke builds.

## Verification

- `cmake --preset windows-msvc-debug-static`
- `cmake --preset windows-msvc-debug-shared`
- `cmake --build --preset windows-msvc-debug-static`
- `cmake --build --preset windows-msvc-debug-shared`
- `ctest --preset windows-msvc-debug-static -R package_consumer_smoke --output-on-failure`
- `ctest --preset windows-msvc-debug-shared -R package_consumer_smoke --output-on-failure`

Both package consumer smoke tests passed.
