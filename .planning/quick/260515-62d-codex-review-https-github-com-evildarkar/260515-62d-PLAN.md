---
phase: quick-260515-62d-codex-review
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/formats/ba2/ba2_gnrl_parser.cpp
  - tests/unit/ba2_gnrl_reader_tests.cpp
autonomous: true
requirements:
  - QUICK-260515-62D
must_haves:
  truths:
    - "Malformed BA2 GNRL archives with two non-empty, partially overlapping stored payload spans fail during open/validation."
    - "BA2 GNRL records that share the exact same non-empty stored span remain accepted so writer-created dedupe is preserved."
    - "Zero-length BA2 GNRL records are not treated as conflicting payload ranges."
  artifacts:
    - path: "src/formats/ba2/ba2_gnrl_parser.cpp"
      provides: "Accepted stored-payload span tracking inside materialize_entries before metadata publication"
      contains: "BA2 GNRL entry payload spans partially overlap"
    - path: "tests/unit/ba2_gnrl_reader_tests.cpp"
      provides: "Synthetic malformed partial-overlap regression and exact-duplicate positive regression"
      contains: "partially overlapping payload spans"
  key_links:
    - from: "src/formats/ba2/ba2_gnrl_parser.cpp::materialize_entries"
      to: "entry_metadata payload_offset/stored_size publication"
      via: "accepted non-empty span validation before entries.push_back"
      pattern: "accepted.*payload.*spans|stored.*span"
    - from: "tests/unit/ba2_gnrl_reader_tests.cpp"
      to: "libbsa::archive_reader::open"
      via: "synthetic temp-file BA2 GNRL fixtures"
      pattern: "archive_reader::open.*format_error"
---

<objective>
Reject malformed BA2 GNRL archives whose non-empty records point to partially overlapping stored payload ranges while preserving exact duplicate stored spans used by libbsa's dedupe writer path.

Purpose: Codex review identified a parser validation gap where independently valid spans can overlap each other and then publish ambiguous entries; this hardens open()/validate_archive() before extraction can read bytes from another payload.
Output: Focused parser validation plus Catch2 regressions for rejection and dedupe-compatible acceptance.
</objective>

<execution_context>
@J:/libbsa/.opencode/get-shit-done/workflows/execute-plan.md
@J:/libbsa/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@AGENTS.md
@.planning/quick/260515-62d-codex-review-https-github-com-evildarkar/260515-62d-RESEARCH.md
@src/formats/ba2/ba2_gnrl_parser.cpp
@tests/unit/ba2_gnrl_reader_tests.cpp

Key constraints from sources:
- Do not modify TES5Edit; all work stays in src/ and tests/.
- Preserve exact duplicate non-empty stored spans while rejecting partial overlaps.
- Use stored-size semantics: packed_size when nonzero, otherwise raw size.
- Check overlap only after span_fits_u64 proves offset + stored_size is inside archive bounds.
- Keep the change in materialize_entries so both memory-backed parsing and host-file open paths are covered.
- Use existing synthetic temp-file patterns in tests/unit/ba2_gnrl_reader_tests.cpp; no new dependency or hand-authored binary fixture is needed.

<interfaces>
Existing implementation contracts the executor should use directly:

From src/formats/ba2/ba2_gnrl_parser.cpp:
- `bool spans_overlap_u64(std::uint64_t first_start, std::uint64_t first_length, std::uint64_t second_start, std::uint64_t second_length) noexcept` returns false for zero-length spans and assumes callers have already guarded overflow with span_fits_u64.
- `result<std::vector<entry_metadata>> materialize_entries(std::uint64_t archive_size, std::uint64_t records_end, std::uint64_t name_table_offset, std::uint64_t name_table_end, std::span<const gnrl_record> records, std::span<const std::string> names, detected_ba2_format detected)` validates records and pushes `entry_metadata`.
- Current insertion point is immediately after existing payload span checks against archive bounds, fixed metadata, and filename table, before `entries.push_back(...)`.

From tests/unit/ba2_gnrl_reader_tests.cpp:
- Helpers already available: `append_u16_le`, `append_u32_le`, `append_u64_le`, `append_ascii`, `write_binary_file`, `temp_file_cleanup`.
- Existing synthetic tests build BA2 GNRL bytes with `BTDX`, version 1, subtype `GNRL`, file_count, FileTableOffset, one or more 36-byte records, length-prefixed names, and payload bytes.
- Use `libbsa::detail::hash_fo4("file.ext")` and `libbsa::detail::hash_fo4("directory")` to write matching record hashes.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add BA2 GNRL payload-overlap regressions</name>
  <files>tests/unit/ba2_gnrl_reader_tests.cpp</files>
  <behavior>
    - Test 1: A two-record BA2 GNRL archive with stored spans [payload_base, payload_base + 8) and [payload_base + 4, payload_base + 12) fails `libbsa::archive_reader::open(...)` with `libbsa::error_code::format_error`.
    - Test 2: A two-record BA2 GNRL archive with different canonical paths but the exact same non-empty stored span opens successfully, lists both entries with the same payload_offset/stored_size, and `extract_bytes(...)` returns identical payload bytes for both paths.
  </behavior>
  <action>Create focused synthetic temp-file tests near the existing BA2 GNRL malformed/end-table tests. Build version-1 GNRL bytes using existing append/hash helpers. Place payload bytes outside the fixed header/record table and outside the filename table so the new failure can only be inter-record partial overlap. Include a positive exact-duplicate case to lock the required dedupe-compatible boundary; do not require payload coalescing or add production-only test shims.</action>
  <verify>
    <automated>ctest --preset windows-msvc-debug-static -R ba2_gnrl_reader_tests --output-on-failure</automated>
  </verify>
  <done>Partial-overlap synthetic archive fails open with format_error; exact duplicate stored-span synthetic archive opens and extracts both entries successfully.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reject partial stored-payload overlaps during materialization</name>
  <files>src/formats/ba2/ba2_gnrl_parser.cpp</files>
  <behavior>
    - Existing independently valid payload-bound checks still reject spans outside the archive, fixed metadata, or filename table.
    - Non-empty stored spans are compared against previously accepted non-empty spans before entry_metadata is published.
    - Exact duplicate `{offset, stored_size}` pairs are allowed; all other overlaps are rejected with format_error.
    - Zero-length spans are ignored by the accepted-span tracker.
  </behavior>
  <action>Add a small local stored-span representation and `std::vector` inside `materialize_entries(...)` after canonical path reservation. Reserve it for `records.size()` with the existing metadata allocation pattern if practical. After computing `stored_size` and after existing span_fits_u64/header/name-table checks, compare the current non-empty `{records[index].offset, stored_size}` against accepted spans. If a prior span has the same offset and size, allow it; if it overlaps but is not exact, return `error{error_code::format_error, "BA2 GNRL entry payload spans partially overlap"}`. Push the current non-empty span only after it passes validation. Add a concise comment explaining that exact duplicates are allowed because the writer can intentionally dedupe entries onto the same stored payload, while partial overlap is malformed. Do not add dependencies, do not change public APIs, and do not touch TES5Edit.</action>
  <verify>
    <automated>ctest --preset windows-msvc-debug-static -R "ba2_gnrl_(reader|writer)_tests" --output-on-failure</automated>
  </verify>
  <done>Both host-file open and validate_archive reject partial non-empty overlaps through the shared parser path, while exact duplicate non-empty spans remain valid and existing BA2 GNRL writer dedupe tests continue to pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BA2 file bytes -> parser metadata | Untrusted archive headers/records/names become entry metadata used by extraction. |
| Parsed entry metadata -> extraction I/O | Stored offsets and sizes drive host-file reads for payload extraction. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260515-62d-01 | T/I | `materialize_entries` payload span validation | mitigate | Track accepted non-empty stored spans and reject partial overlaps before publishing `entry_metadata`. |
| T-260515-62d-02 | D | BA2 GNRL parser allocation path | accept | The vector is bounded by already validated `file_count`/metadata limits and allocation failures are translated by existing bad_alloc/length_error handling. |
| T-260515-62d-03 | R | Exact duplicate span handling | mitigate | Add a positive regression proving exact duplicate spans are intentionally accepted rather than accidentally rejected. |
</threat_model>

<verification>
Run focused tests after implementation:

<automated>ctest --preset windows-msvc-debug-static -R "ba2_gnrl_(reader|writer)_tests" --output-on-failure</automated>

If the static debug preset is unavailable locally, use another configured Windows/MSVC debug CTest preset from CMakePresets.json and record the exact command in the summary.
</verification>

<success_criteria>
- A malformed BA2 GNRL with two non-empty, partially overlapping stored payload spans fails during open/validation with format_error.
- A BA2 GNRL with two different entries sharing the exact same non-empty stored span still opens and extracts both entries.
- Empty records remain non-conflicting.
- No public API changes, no new dependencies, and no TES5Edit modifications.
</success_criteria>

<source_audit>
- GOAL: Reject partially overlapping BA2 GNRL payload ranges outside metadata while preserving exact duplicate dedupe spans — covered by Tasks 1 and 2.
- RESEARCH: Implement in `materialize_entries(...)`, use stored-size semantics, skip empty spans, call overlap after `span_fits_u64`, synthetic tests preferred — covered by Tasks 1 and 2.
- CONTEXT: User constraint to preserve exact duplicate non-empty stored spans and reject partial overlaps — covered by both tasks.
- REQ: Quick task `QUICK-260515-62D` — covered by this single plan.
</source_audit>

<output>
After completion, create `.planning/quick/260515-62d-codex-review-https-github-com-evildarkar/260515-62d-SUMMARY.md` with files changed, tests run, and whether any comments were added or rewritten.
</output>
