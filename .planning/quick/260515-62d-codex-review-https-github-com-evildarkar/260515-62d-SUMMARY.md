---
phase: quick-260515-62d-codex-review
plan: 01
subsystem: ba2-gnrl-parser
tags: [quick-task, ba2, gnrl, parser-hardening, tests]
requirements: [QUICK-260515-62D]
key-files:
  created: []
  modified:
    - tests/unit/ba2_gnrl_reader_tests.cpp
    - src/formats/ba2/ba2_gnrl_parser.cpp
commits:
  - 8053c60
  - 8931efc
completed: 2026-05-15
---

# Quick Task 260515-62d Summary

Rejected malformed BA2 GNRL archives with partially overlapping non-empty stored payload spans while preserving exact duplicate stored spans used by writer dedupe.

## Tasks Completed

| Task | Name | Commit | Files |
| ---- | ---- | ------ | ----- |
| 1 | Add BA2 GNRL payload-overlap regressions | 8053c60 | `tests/unit/ba2_gnrl_reader_tests.cpp` |
| 2 | Reject partial stored-payload overlaps during materialization | 8931efc | `src/formats/ba2/ba2_gnrl_parser.cpp`, `tests/unit/ba2_gnrl_reader_tests.cpp` |

## Changes

- Added synthetic BA2 GNRL tests for partial payload-span overlap rejection and exact duplicate stored-span acceptance.
- Added `materialize_entries` accepted-span tracking for non-empty stored payload ranges.
- Kept exact duplicate `{offset, stored_size}` spans valid for dedupe compatibility.
- Added `validate_archive` coverage for malformed payload-span rejection.

## Verification

- RED: `ctest --preset windows-msvc-debug-static -R "partially overlapping payload spans|exact duplicate non-empty payload spans" --output-on-failure` failed before implementation because the partial-overlap archive opened successfully.
- GREEN: `cmake --build --preset windows-msvc-debug-static --target libbsa_tests && ctest --preset windows-msvc-debug-static -R "BA2 GNRL writer|BA2 GNRL disk|ba2_gnrl_" --output-on-failure` passed: 43/43 tests.

Note: the plan's literal `-R ba2_gnrl_reader_tests` selector matched no CTest test names in this configured build, so focused BA2 GNRL test-name patterns were used instead.

## Deviations from Plan

### Auto-fixed Issues

None.

### Execution Adjustments

- Task 2 also staged the validation assertion added to `tests/unit/ba2_gnrl_reader_tests.cpp` so `validate_archive` rejection is explicitly covered with the same synthetic malformed overlap fixture.

## Comments Added or Rewritten

- Added Doxygen-style helper comments for the synthetic BA2 GNRL archive test builder.
- Added a parser comment documenting why exact duplicate stored spans are allowed while partial overlaps are rejected.
- No comments were removed.

## Known Stubs

None.

## Threat Flags

None beyond the plan threat model; the change hardens existing BA2 GNRL parser trust-boundary validation without adding new public API, network, file-access, or schema surfaces.

## Self-Check: PASSED

- Verified modified files exist: `src/formats/ba2/ba2_gnrl_parser.cpp`, `tests/unit/ba2_gnrl_reader_tests.cpp`.
- Verified commits exist: `8053c60`, `8931efc`.
- Verified no staged or committed paths touched `TES5Edit/` via the required submodule commit guard before each commit.
