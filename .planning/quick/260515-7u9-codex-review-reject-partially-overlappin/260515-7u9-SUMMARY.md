---
status: complete
task: 260515-7u9
commits:
  - e5d895d
  - 55c534f
created: 2026-05-15
---

# Quick Task 260515-7u9 Summary

Rejected partially overlapping BA2 DX10 chunk payload spans while preserving exact duplicate spans for writer dedupe.

## Changes

- Added focused BA2 DX10 parser tests for:
  - partial cross-entry raw chunk span overlap rejected as `format_error`
  - exact duplicate non-empty raw chunk span accepted, with matching metadata and identical `extract_bytes` output
- Added parser-side accepted-span tracking in `first_payload_offset_for`:
  - validates bounds before overlap comparisons
  - allows exact duplicate `(offset, stored_size)` spans
  - rejects non-exact overlapping spans with `BA2 DX10 chunk payload spans partially overlap`

## Commits

- `e5d895d` — `test(quick-260515-7u9): add BA2 DX10 chunk overlap coverage`
- `55c534f` — `fix(quick-260515-7u9): reject overlapping BA2 DX10 chunks`

## Verification

- `cmake --build --preset windows-msvc-debug-static --target libbsa_tests`
- `ctest --preset windows-msvc-debug-static -R "ba2_dx10|ba2_gnrl"`

Result: 51/51 focused tests passed.

## Caveats

- No files under `TES5Edit/` were modified or staged.
- Documentation/state artifacts were intentionally left uncommitted for orchestrator handling.
- Added comments/doc comments only; no accurate comments were removed or rewritten.
