---
phase: quick-260515-7u9-codex-review-reject-partially-overlappin
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/formats/ba2/ba2_dx10_parser.cpp
  - tests/unit/ba2_dx10_parser_tests.cpp
autonomous: true
requirements:
  - quick-260515-7u9
must_haves:
  truths:
    - "BA2 DX10 archives with partially overlapping non-empty chunk payload byte ranges fail during open/validation with format_error."
    - "BA2 DX10 archives with exact duplicate non-empty chunk payload spans remain readable so writer dedupe stays valid."
    - "Existing BA2 DX10 metadata, sparse-open, malformed, layout, and writer dedupe behavior continues to pass."
  artifacts:
    - path: "src/formats/ba2/ba2_dx10_parser.cpp"
      provides: "DX10 raw chunk-span validation before public metadata materialization"
      contains: "BA2 DX10 chunk payload spans partially overlap"
    - path: "tests/unit/ba2_dx10_parser_tests.cpp"
      provides: "Focused partial-overlap rejection and exact-duplicate acceptance coverage"
      contains: "ba2_dx10_detector rejects partially overlapping chunk payload spans"
  key_links:
    - from: "src/formats/ba2/ba2_dx10_parser.cpp:first_payload_offset_for"
      to: "src/formats/ba2/ba2_dx10_parser.cpp:parse_ba2_dx10_archive_file"
      via: "raw dx10_record chunk scan before filename-table parsing/materialization"
      pattern: "first_payload_offset_for\(records"
    - from: "tests/unit/ba2_dx10_parser_tests.cpp"
      to: "libbsa::archive_reader::open / libbsa::validate_archive"
      via: "synthetic BA2 DX10 fixtures written to temporary paths"
      pattern: "ba2_dx10.*overlap"
---

<objective>
Reject corrupt BA2 DX10 archives whose chunk payload byte ranges partially overlap while preserving exact duplicate spans used by writer payload deduplication.

Purpose: Close the Codex review finding by matching the accepted-span policy already used by BA2 GNRL: exact duplicate physical spans are valid dedupe evidence, but partial sharing makes extraction ambiguous and must be rejected as malformed input.
Output: A parser-side overlap guard in `ba2_dx10_parser.cpp` plus focused Catch2 coverage in `ba2_dx10_parser_tests.cpp`.
</objective>

<execution_context>
@J:/libbsa/.opencode/get-shit-done/workflows/execute-plan.md
@J:/libbsa/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260515-7u9-codex-review-reject-partially-overlappin/260515-7u9-RESEARCH.md
@AGENTS.md
@src/formats/ba2/ba2_dx10_parser.cpp
@src/formats/ba2/ba2_gnrl_parser.cpp
@tests/unit/ba2_dx10_parser_tests.cpp
@tests/unit/ba2_gnrl_reader_tests.cpp

<interfaces>
Key existing contracts and patterns:
- `ba2_dx10_parser.cpp` has `dx10_chunk_record { offset, packed_size, raw_size, start_mip, end_mip }` and `first_payload_offset_for(std::span<const dx10_record>, std::uint64_t archive_size)` currently validating each stored span with `span_fits_u64`.
- `ba2_gnrl_parser.cpp` uses local `stored_payload_span { std::uint64_t offset; std::uint64_t size; }` and `spans_overlap_u64(...)`, then allows exact duplicate `(offset,size)` spans while returning `format_error` for partial overlap.
- `ba2_dx10_parser_tests.cpp` already provides little-endian append/write helpers, temp-file mutation helpers, `archive_reader::open`, `validate_archive`, and generated DX10 fixture checks.
- `ba2_gnrl_reader_tests.cpp` lines 607-675 show the exact reject/accept policy shape for overlap tests.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add BA2 DX10 overlap policy tests</name>
  <files>tests/unit/ba2_dx10_parser_tests.cpp</files>
  <behavior>
    - Test 1: a synthetic BA2 DX10 archive with two valid chunk records whose physical stored spans overlap partially, such as [payload_base, payload_base + 8) and [payload_base + 4, payload_base + 12), causes `archive_reader::open(temp_path.string())` to fail with `libbsa::error_code::format_error` and `validate_archive(temp_path.string())` to return an invalid report whose first error is `format_error`.
    - Test 2: a synthetic BA2 DX10 archive with two distinct canonical texture paths whose non-empty chunk records have the exact same `(offset, stored_size)` opens successfully; both entries expose the duplicate chunk `payload_offset` and `stored_size`, and both `extract_bytes(path)` results succeed with identical reconstructed DDS bytes.
  </behavior>
  <action>Add focused Catch2 tests in `tests/unit/ba2_dx10_parser_tests.cpp`, near existing malformed DX10 parser tests, using the file's existing helper style. Prefer a small synthetic DX10 fixture helper over brittle generated-fixture mutation if practical. The fixture must produce valid hashes, filename table entries, `chunk_header_size == 24`, valid mip ranges for `texture::validate_and_order_chunks`, and uncompressed raw chunk bytes so extraction does not require adding compression dependencies or test shims. Add tight Doxygen-style comments for any non-trivial helper introduced. Do not modify `TES5Edit/`.</action>
  <verify>
    <automated>cmake --build --preset windows-msvc-debug-static --target libbsa_tests &amp;&amp; ctest --preset windows-msvc-debug-static -R "ba2_dx10"</automated>
  </verify>
  <done>DX10 partial-overlap and exact-duplicate tests exist; before parser implementation, the partial-overlap case demonstrates the missing guard, and after implementation both new tests pass with the existing DX10 suite.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce BA2 DX10 accepted chunk-span policy</name>
  <files>src/formats/ba2/ba2_dx10_parser.cpp</files>
  <behavior>
    - Non-empty DX10 chunk spans are recorded after their existing size and `span_fits_u64` checks pass.
    - Exact duplicate `(offset, stored_size)` spans are accepted.
    - Any non-exact overlap against a previously accepted non-empty span returns `format_error` with a DX10-specific message.
  </behavior>
  <action>In `ba2_dx10_parser.cpp`, add local span tracking matching the GNRL accepted-span pattern: define a small local span struct near `dx10_chunk_record`, add/copy `spans_overlap_u64` locally, and update `first_payload_offset_for` to maintain `accepted_payload_spans` across every chunk in every record. Compare only after the current existing `stored_size` consistency and `span_fits_u64` validation has succeeded so end arithmetic cannot wrap. Allow exact duplicates, reject only non-exact overlap with message `BA2 DX10 chunk payload spans partially overlap`, and push accepted spans for later comparisons. Add a short compatibility comment explaining that writer dedupe may intentionally publish exact duplicate chunks while partial sharing would make texture chunk extraction ambiguous. Do not refactor shared BA2 parser primitives in this quick fix.</action>
  <verify>
    <automated>cmake --build --preset windows-msvc-debug-static --target libbsa_tests &amp;&amp; ctest --preset windows-msvc-debug-static -R "ba2_dx10|ba2_gnrl"</automated>
  </verify>
  <done>`first_payload_offset_for` rejects partial DX10 chunk span overlap, exact duplicate spans continue through metadata materialization/extraction, and BA2 GNRL overlap behavior remains unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| archive bytes -> BA2 DX10 parser | Untrusted archive metadata declares offsets and sizes used for later payload reads. |
| parser metadata -> extraction | Accepted chunk spans become public metadata that drives host-file reads and DDS reconstruction. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260515-7u9-01 | T | `first_payload_offset_for` | mitigate | Validate every DX10 chunk span against archive size and reject non-exact overlap before materialization. |
| T-260515-7u9-02 | I | `archive_reader::open` / extraction metadata | mitigate | Preserve exact duplicate spans only when offset and stored size both match; reject partial sharing that could expose ambiguous bytes from another chunk. |
| T-260515-7u9-03 | D | synthetic overlap tests | mitigate | Keep tests bounded with small in-memory fixtures and existing temp-file cleanup style. |
</threat_model>

<verification>
Run the focused Debug static verification after implementation:

`cmake --build --preset windows-msvc-debug-static --target libbsa_tests && ctest --preset windows-msvc-debug-static -R "ba2_dx10|ba2_gnrl"`

If the build directory is not configured, first run:

`cmake --preset windows-msvc-debug-static`
</verification>

<success_criteria>
- BA2 DX10 partial chunk payload overlap is rejected with `format_error` during `archive_reader::open` and reported invalid by `validate_archive`.
- BA2 DX10 exact duplicate non-empty chunk spans still open and extract identical reconstructed DDS bytes.
- Existing BA2 DX10 writer dedupe coverage remains compatible; no changes touch `TES5Edit/` or add dependencies.
</success_criteria>

<output>
After completion, create `.planning/quick/260515-7u9-codex-review-reject-partially-overlappin/260515-7u9-SUMMARY.md`.
</output>
