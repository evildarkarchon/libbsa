---
phase: quick-260514-5h7-resolve-tes3-disk-source-paths-and-bsa-b
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/formats/bsa/tes3_bsa_prepare.hpp
  - src/formats/bsa/tes3_bsa_prepare.cpp
  - src/formats/bsa/tes3_bsa_serialize.cpp
  - src/formats/bsa/tes3_bsa_writer.cpp
  - src/formats/bsa/tes4_bsa_writer.cpp
  - src/formats/ba2/ba2_gnrl_writer.cpp
  - src/formats/ba2/ba2_dx10_writer.cpp
  - tests/unit/host_file_writer_name_tests.cpp
  - tests/unit/tes3_bsa_writer_tests.cpp
  - tests/unit/tes4_bsa_writer_tests.cpp
  - tests/unit/ba2_gnrl_writer_tests.cpp
  - tests/unit/ba2_dx10_writer_tests.cpp
autonomous: true
requirements:
  - QUICK-260514-5H7
must_haves:
  truths:
    - "TES3 BSA disk source paths supplied as public UTF-8 are decoded through the shared host_file_path boundary before size inspection and final serialization."
    - "TES3, TES4, BA2 GNRL, and BA2 DX10 writer output paths supplied as public UTF-8 are decoded once before publish validation and final publish."
    - "Non-ASCII Windows writer source/output regressions are covered by focused tests and source-policy gates."
  artifacts:
    - path: "src/formats/bsa/tes3_bsa_prepare.hpp"
      provides: "TES3 prepared entry carries resolved detail::host_file_path for later I/O"
      contains: "detail::host_file_path"
    - path: "src/formats/bsa/tes3_bsa_prepare.cpp"
      provides: "TES3 disk source size inspection through resolve_host_file_path and inspect_host_file_size"
      contains: "inspect_host_file_size"
    - path: "src/formats/bsa/tes3_bsa_serialize.cpp"
      provides: "TES3 disk payload streaming through resolved host-file helpers"
      contains: "open_host_file"
    - path: "src/formats/bsa/tes3_bsa_writer.cpp"
      provides: "TES3 writer output publish uses resolved UTF-8 host path"
      contains: "resolve_host_file_path"
    - path: "src/formats/bsa/tes4_bsa_writer.cpp"
      provides: "TES4 writer output publish uses resolved UTF-8 host path"
      contains: "resolve_host_file_path"
    - path: "src/formats/ba2/ba2_gnrl_writer.cpp"
      provides: "BA2 GNRL writer output publish uses resolved UTF-8 host path"
      contains: "resolve_host_file_path"
    - path: "src/formats/ba2/ba2_dx10_writer.cpp"
      provides: "BA2 DX10 writer output publish uses resolved UTF-8 host path"
      contains: "resolve_host_file_path"
    - path: "tests/unit/host_file_writer_name_tests.cpp"
      provides: "source-policy regression gate for writer host-path seams"
      contains: "std::filesystem::path{output_host_path}"
  key_links:
    - from: "src/formats/bsa/tes3_bsa_prepare.cpp"
      to: "src/formats/bsa/tes3_bsa_serialize.cpp"
      via: "tes3_prepared_entry::resolved_host_path"
      pattern: "resolved_host_path"
    - from: "public writer write_to/write_* functions"
      to: "src/detail/writer_publish.hpp"
      via: "detail::resolve_host_file_path(output_host_path).resolved passed to publish_writer_output"
      pattern: "publish_writer_output.*resolved"
    - from: "tests/unit/*writer_tests.cpp"
      to: "public writer APIs"
      via: "native non-ASCII path converted to explicit UTF-8 using u8string bytes"
      pattern: "u8string"
---

<objective>
Resolve the Codex review findings by moving TES3 disk-source I/O and all BSA/BA2 writer output publish paths onto the existing shared UTF-8 host-path boundary.

Purpose: Public host paths are UTF-8 by project convention; MSVC narrow std::filesystem::path construction uses the active ANSI code page and can size, read, or publish the wrong file for non-ASCII paths.
Output: TES3 prepared entries carry resolved host paths, writer outputs publish through resolved native paths, and focused tests/source-policy gates prevent regressions.
</objective>

<execution_context>
@J:/libbsa/.opencode/get-shit-done/workflows/execute-plan.md
@J:/libbsa/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@AGENTS.md
@.planning/quick/260514-5h7-resolve-tes3-disk-source-paths-and-bsa-b/260514-5h7-RESEARCH.md
@src/detail/host_file_path.hpp
@src/detail/host_file.hpp
@src/detail/writer_publish.hpp
@src/formats/bsa/tes3_bsa_prepare.hpp
@src/formats/bsa/tes3_bsa_prepare.cpp
@src/formats/bsa/tes3_bsa_serialize.cpp
@src/formats/bsa/tes3_bsa_writer.cpp
@src/formats/bsa/tes4_bsa_writer.cpp
@src/formats/ba2/ba2_gnrl_writer.cpp
@src/formats/ba2/ba2_dx10_writer.cpp
@tests/unit/host_file_writer_name_tests.cpp
@tests/unit/host_path_correctness_boundary_tests.cpp
@tests/unit/tes3_bsa_writer_tests.cpp
@tests/unit/tes4_bsa_writer_tests.cpp
@tests/unit/ba2_gnrl_writer_tests.cpp
@tests/unit/ba2_dx10_writer_tests.cpp

<interfaces>
Use the existing internal host-path contract from the research findings:
- detail::resolve_host_file_path(std::string_view) returns libbsa::result<detail::host_file_path> with original_utf8 for diagnostics and resolved std::filesystem::path for I/O.
- detail::inspect_host_file_size(const detail::host_file_path&) sizes resolved host files without reconstructing a path from raw caller text.
- detail::open_host_file(const detail::host_file_path&) and detail::for_each_host_file_chunk(const detail::host_file_path&, ...) perform read/streaming I/O through resolved paths.
- detail::publish_writer_output(const std::filesystem::path&) remains a native-path publish primitive; do not change it to accept std::string_view.
</interfaces>

<source_coverage_audit>
- GOAL: Resolve TES3 disk source paths and BSA/BA2 writer output paths through shared UTF-8 host path helpers — covered by Tasks 1-3.
- REVIEW-1: TES3 disk sources currently convert raw std::string to std::filesystem::path for size/read — covered by Task 2.
- REVIEW-2: BA2 DX10 and analogous writers directly construct std::filesystem::path from public output text — covered by Task 3.
- RESEARCH: Use resolve_host_file_path, host_file_path, inspect_host_file_size, open_host_file/for_each_host_file_chunk, and leave writer_publish native — covered by Tasks 2-3.
- CONTEXT: TES5Edit remains read-only, public headers remain minimal, no new dependencies — enforced across all tasks.
</source_coverage_audit>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add host-path regression coverage before production changes</name>
  <files>tests/unit/host_file_writer_name_tests.cpp, tests/unit/tes3_bsa_writer_tests.cpp, tests/unit/tes4_bsa_writer_tests.cpp, tests/unit/ba2_gnrl_writer_tests.cpp, tests/unit/ba2_dx10_writer_tests.cpp</files>
  <behavior>
    - Test 1: source-policy assertions fail while TES3 prepare/serialize can still size or stream disk sources from raw host-path text.
    - Test 2: source-policy assertions fail while any writer file contains direct std::filesystem::path construction from output_host_path.
    - Test 3: TES3 writer accepts a disk source and output destination under native non-ASCII Windows paths converted to explicit UTF-8 bytes, then produces a readable archive with expected payload bytes.
    - Test 4: TES3, TES4, BA2 GNRL, and BA2 DX10 writer output-path regressions cover non-ASCII UTF-8 destinations without relying on std::filesystem::path::string().
  </behavior>
  <action>Extend the existing source-policy checks in host_file_writer_name_tests.cpp to include TES3 prepare/serialize seams and to reject direct writer output conversions using std::filesystem::path constructed from output_host_path. Add focused writer regressions using the established native-wide-path to u8string byte-preserving pattern from host_path_correctness_boundary_tests.cpp and existing writer tests. Keep fixtures legal and generated in test temp directories; do not use TES5Edit/ as a mutable fixture.</action>
  <verify>
    <automated>ctest --test-dir build/windows-msvc-debug-static --output-on-failure -R "host_file_writer_name|tes3_bsa_writer|tes4_bsa_writer|ba2_gnrl_writer|ba2_dx10_writer"</automated>
  </verify>
  <done>Tests express the review failures before implementation and use explicit UTF-8 public API inputs for non-ASCII paths.</done>
</task>

<task type="auto">
  <name>Task 2: Carry resolved TES3 disk source paths through preparation and serialization</name>
  <files>src/formats/bsa/tes3_bsa_prepare.hpp, src/formats/bsa/tes3_bsa_prepare.cpp, src/formats/bsa/tes3_bsa_serialize.cpp</files>
  <action>Add detail::host_file_path to tes3_prepared_entry for disk-backed entries, preserving existing diagnostic strings where they are still used. Replace TES3 disk source sizing with resolve_host_file_path followed by inspect_host_file_size, matching the migrated TES4/BA2 GNRL prepare patterns and keeping add_file() error timing unchanged. Update TES3 serialization so disk payload streaming uses the prepared resolved host path through open_host_file or for_each_host_file_chunk, never by reconstructing a std::filesystem::path from original UTF-8 text. Preserve the existing changed-during-finalization diagnostic behavior, including the appended-byte/change detection semantics.</action>
  <verify>
    <automated>ctest --test-dir build/windows-msvc-debug-static --output-on-failure -R "host_file_writer_name|tes3_bsa_writer"</automated>
  </verify>
  <done>TES3 disk-backed entries are resolved once during preparation, size/read I/O uses the resolved native path, and TES3 writer regressions pass.</done>
</task>

<task type="auto">
  <name>Task 3: Resolve all public writer output paths before publish</name>
  <files>src/formats/bsa/tes3_bsa_writer.cpp, src/formats/bsa/tes4_bsa_writer.cpp, src/formats/ba2/ba2_gnrl_writer.cpp, src/formats/ba2/ba2_dx10_writer.cpp</files>
  <action>In each public writer output path, keep existing empty-path, NUL, target-option, and worker-count validation order unless the current file already resolves source paths earlier. After those validations, call detail::resolve_host_file_path(output_host_path) exactly once and pass the returned resolved native path into detail::publish_writer_output. Do not change writer_publish to accept public UTF-8 text, do not leak detail::host_file_path into public headers, and remove direct std::filesystem::path construction from output_host_path in TES3, TES4, BA2 GNRL, and BA2 DX10 writers.</action>
  <verify>
    <automated>ctest --test-dir build/windows-msvc-debug-static --output-on-failure -R "host_file|tes3_bsa_writer|tes4_bsa_writer|ba2_gnrl_writer|ba2_dx10_writer"</automated>
  </verify>
  <done>All BSA/BA2 writer output publish paths decode public UTF-8 once through resolve_host_file_path, no direct output_host_path filesystem conversion remains, and focused writer tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public writer API -> internal host filesystem I/O | Caller-provided UTF-8 strings cross into native Windows filesystem operations for disk source reads and output publish. |
| disk source file -> archive serializer | Host file contents and size metadata are consumed while writing archive bytes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260514-5H7-01 | Tampering | TES3 disk source finalization | mitigate | Resolve source paths once into detail::host_file_path and preserve existing changed-during-finalization size/appended-byte checks during serialization. |
| T-260514-5H7-02 | Information Disclosure | writer output path diagnostics | mitigate | Preserve caller-owned diagnostic strings and avoid publishing mojibake paths by using resolve_host_file_path before publish_writer_output. |
| T-260514-5H7-03 | Denial of Service | malformed UTF-8 host paths | mitigate | Reuse resolve_host_file_path malformed UTF-8 rejection and keep filesystem validation before expensive write work. |
</threat_model>

<verification>
Run the focused CTest command after all tasks:

ctest --test-dir build/windows-msvc-debug-static --output-on-failure -R "host_file|tes3_bsa_writer|tes4_bsa_writer|ba2_gnrl_writer|ba2_dx10_writer"

If the local configured build directory differs, use the current Windows MSVC debug-static build directory and the same test regex.
</verification>

<success_criteria>
- TES3 prepare/serialize never performs disk source size/read I/O by constructing a native path from raw public UTF-8 text.
- TES3, TES4, BA2 GNRL, and BA2 DX10 output publish paths never directly construct std::filesystem::path from output_host_path.
- Focused non-ASCII path regressions and source-policy gates pass.
- No files under TES5Edit/ are modified.
</success_criteria>

<output>
After completion, create `.planning/quick/260514-5h7-resolve-tes3-disk-source-paths-and-bsa-b/260514-5h7-SUMMARY.md`.
</output>
