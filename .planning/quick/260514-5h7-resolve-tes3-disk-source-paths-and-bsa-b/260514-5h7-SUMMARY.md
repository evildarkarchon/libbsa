---
phase: quick-260514-5h7-resolve-tes3-disk-source-paths-and-bsa-b
plan: 01
subsystem: writer-host-path-boundary
tags: [quick, windows, utf8, writer, bsa, ba2]
key-files:
  created: []
  modified:
    - src/formats/bsa/tes3_bsa_prepare.hpp
    - src/formats/bsa/tes3_bsa_prepare.cpp
    - src/formats/bsa/tes3_bsa_serialize.cpp
    - src/formats/bsa/tes3_bsa_writer.cpp
    - src/formats/bsa/tes4_bsa_writer.cpp
    - src/formats/ba2/ba2_gnrl_writer.cpp
    - src/formats/ba2/ba2_dx10_writer.cpp
    - tests/unit/host_file_writer_name_tests.cpp
    - tests/unit/tes3_bsa_writer_tests.cpp
    - tests/unit/tes4_bsa_writer_tests.cpp
    - tests/unit/ba2_gnrl_writer_tests.cpp
    - tests/unit/ba2_dx10_writer_tests.cpp
decisions:
  - "TES3 disk entries now carry detail::host_file_path from preparation into serialization."
  - "Public BSA/BA2 writer outputs resolve UTF-8 text once before native publish validation."
metrics:
  completed_date: 2026-05-14
  tasks_completed: 3
  commits: 3
---

# Quick Task 260514-5h7 Summary

Resolved UTF-8 host-path handling for TES3 disk sources and BSA/BA2 writer output destinations.

## Completed Tasks

| Task | Result | Commit |
|---|---|---|
| 1. Add host-path regression coverage | Added source-policy checks and non-ASCII UTF-8 source/output regressions across TES3, TES4, BA2 GNRL, and BA2 DX10 writers. | b9b19d8 |
| 2. Carry resolved TES3 disk source paths | TES3 preparation resolves disk sources once, sizes through `inspect_host_file_size`, and serialization streams through `open_host_file` using `tes3_prepared_entry::resolved_host_path`. | d6ebc64 |
| 3. Resolve writer output paths before publish | TES3, TES4, BA2 GNRL, and BA2 DX10 writers call `detail::resolve_host_file_path(output_host_path)` before `publish_writer_output`. | 152306a |

## Verification

- `cmake --build "build/windows-msvc-debug-static" --target libbsa_tests`
- `ctest --test-dir "build/windows-msvc-debug-static" -C Debug --output-on-failure -R "host_file|tes3_bsa_writer|tes4_bsa_writer|ba2_gnrl_writer|ba2_dx10_writer"` — 50/50 passed.
- `build/windows-msvc-debug-static/tests/Debug/libbsa_tests.exe "[host_file],[tes3_bsa_writer],[tes4_bsa_writer],[ba2_gnrl_writer],[ba2_dx10_writer]"` — 138 test cases passed.

## Deviations from Plan

None — plan executed as written.

## Known Stubs

None.

## Threat Flags

None — changes stayed within the planned public writer API to internal host filesystem I/O trust boundary.

## Notes

- Existing unrelated `.planning/config.json` modification was preserved and not staged.
- No files under `TES5Edit/` were modified, staged, or committed.
- No comments were removed or rewritten.

## Self-Check: PASSED

- Modified source and test files exist.
- Commits found: `b9b19d8`, `d6ebc64`, `152306a`.
