---
status: ready
created: 2026-05-14
quick_id: 260514-11b
slug: fix-ba2-gnrl-and-tes4-raw-streaming-to-use-resolved-host-paths
type: execute
wave: 1
depends_on: []
files_modified:
  - src/formats/ba2/ba2_gnrl_prepare.hpp
  - src/formats/ba2/ba2_gnrl_prepare.cpp
  - src/formats/ba2/ba2_gnrl_serialize.cpp
  - src/formats/bsa/tes4_bsa_prepare.hpp
  - src/formats/bsa/tes4_bsa_prepare.cpp
  - src/formats/bsa/tes4_bsa_serialize.cpp
  - tests/unit/host_file_writer_name_tests.cpp
  - tests/unit/ba2_gnrl_writer_tests.cpp
  - tests/unit/tes4_bsa_writer_tests.cpp
autonomous: true
requirements:
  - quick-260514-11b
must_haves:
  truths:
    - "BA2 GNRL raw disk finalization reuses the resolved Windows host path that preparation already validated, so non-ASCII UTF-8 source paths still write a readable archive."
    - "TES4 BSA raw disk finalization follows the same resolved host-file seam instead of reopening a narrow UTF-8 string path."
    - "Focused writer regressions fail if either serializer drifts back to direct `std::ifstream` opens for raw disk streaming."
  artifacts:
    - path: "src/formats/ba2/ba2_gnrl_prepare.hpp"
      provides: "BA2 prepared-entry state that can carry a resolved raw-disk host path alongside existing diagnostics text"
    - path: "src/formats/ba2/ba2_gnrl_serialize.cpp"
      provides: "BA2 raw finalization that opens disk payloads through `detail::open_host_file(...)`"
    - path: "src/formats/bsa/tes4_bsa_prepare.hpp"
      provides: "TES4 prepared-entry state that preserves the resolved host path for raw streaming"
    - path: "src/formats/bsa/tes4_bsa_serialize.cpp"
      provides: "TES4 raw finalization that streams from the shared host-file seam"
    - path: "tests/unit/ba2_gnrl_writer_tests.cpp"
      provides: "Black-box BA2 raw writer coverage for non-ASCII UTF-8 disk paths"
    - path: "tests/unit/tes4_bsa_writer_tests.cpp"
      provides: "Black-box TES4 raw writer coverage for non-ASCII UTF-8 disk paths"
    - path: "tests/unit/host_file_writer_name_tests.cpp"
      provides: "Source-policy checks that lock serializer raw streaming to the host_file seam"
  key_links:
    - from: "src/formats/ba2/ba2_gnrl_prepare.cpp"
      to: "src/formats/ba2/ba2_gnrl_serialize.cpp"
      via: "stored `detail::host_file_path` on `ba2_gnrl_prepared_entry`"
      pattern: "stream_disk_payload\(|open_host_file\("
    - from: "src/formats/bsa/tes4_bsa_prepare.cpp"
      to: "src/formats/bsa/tes4_bsa_serialize.cpp"
      via: "stored `detail::host_file_path` on `tes4_prepared_entry`"
      pattern: "stream_disk_payload_to_output\(|open_host_file\("
    - from: "tests/unit/host_file_writer_name_tests.cpp"
      to: "src/formats/ba2/ba2_gnrl_serialize.cpp"
      via: "source-policy assertion"
      pattern: "open_host_file\("
    - from: "tests/unit/host_file_writer_name_tests.cpp"
      to: "src/formats/bsa/tes4_bsa_serialize.cpp"
      via: "source-policy assertion"
      pattern: "open_host_file\("
---

# Quick Task 260514-11b: Fix BA2 GNRL and TES4 raw streaming to use resolved host paths

<objective>
Keep the existing host-file seam intact while fixing the two remaining raw-writer reopen paths that still bypass it during archive finalization.

Purpose: Preparation already resolves UTF-8 Windows disk paths once; this quick fix closes the review gap so BA2 GNRL and TES4 raw streaming do not regress on non-ASCII host paths.
Output: One internal seam-plumbing change across BA2/TES4 raw prepared-entry state and serializer helpers, plus focused regression coverage.
</objective>

<context>
@.planning/STATE.md
@.planning/quick/260514-11b-fix-ba2-gnrl-and-tes4-raw-streaming-to-u/260514-11b-RESEARCH.md
@src/detail/host_file_path.hpp
@src/detail/host_file.hpp
@src/formats/ba2/ba2_gnrl_prepare.hpp
@src/formats/ba2/ba2_gnrl_prepare.cpp
@src/formats/ba2/ba2_gnrl_serialize.cpp
@src/formats/bsa/tes4_bsa_prepare.hpp
@src/formats/bsa/tes4_bsa_prepare.cpp
@src/formats/bsa/tes4_bsa_serialize.cpp
@tests/unit/host_file_writer_name_tests.cpp
@tests/unit/ba2_gnrl_writer_tests.cpp
@tests/unit/tes4_bsa_writer_tests.cpp

<interfaces>
From src/detail/host_file_path.hpp:
- `struct host_file_path { std::string original_utf8; std::filesystem::path resolved; };`
- `result<host_file_path> resolve_host_file_path(std::string_view host_path);`

From src/detail/host_file.hpp:
- `result<std::ifstream> open_host_file(const host_file_path& host_path, const host_file_context& context);`
- `result<void> for_each_host_file_chunk(const host_file_path& host_path, std::uint64_t expected_size, const host_file_context& context, ...)`

From src/formats/ba2/ba2_gnrl_prepare.hpp:
- `struct ba2_gnrl_prepared_entry` currently keeps `source_path`, `stream_from_disk`, `raw_size`, and `stored_payload`.

From src/formats/bsa/tes4_bsa_prepare.hpp:
- `struct tes4_prepared_entry` currently keeps `raw_disk_host_path`, `stream_raw_disk`, `raw_disk_size`, and `stored_payload`.

Research/review lock:
- Keep the existing diagnostic strings; add the resolved `detail::host_file_path` alongside them rather than widening this quick fix into dedupe/layout refactors.
- BA2 and TES4 raw finalization helpers must consume `detail::host_file_path` and `detail::open_host_file(...)`, not `std::ifstream{host_path, ...}`.
- Tests should stay black-box through the public writer/reader APIs, with a cheap source-policy guard in `host_file_writer_name_tests.cpp`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add raw-writer UTF-8 regression and source-policy coverage</name>
  <files>tests/unit/ba2_gnrl_writer_tests.cpp, tests/unit/tes4_bsa_writer_tests.cpp, tests/unit/host_file_writer_name_tests.cpp</files>
  <behavior>
    - Test 1: BA2 GNRL `all_raw` disk entries written from a native non-ASCII directory and passed through explicit UTF-8 text still round-trip through `archive_reader::open(...).extract_bytes(...)`.
    - Test 2: TES4 BSA `all_raw` disk entries for each target profile do the same without changing the public writer API.
    - Test 3: Serializer source-policy tests require `open_host_file(` in the BA2/TES4 raw streaming sources and reject direct `std::ifstream` host-path opens there.
  </behavior>
  <action>Extend the existing raw-disk writer suites around the current Fallout 4 BA2 and multi-target TES4 raw round-trip coverage so they create source files under a native non-ASCII Windows path, convert that path to explicit UTF-8 via `path.u8string()`, and then call the unchanged public `add_file(...)`/`write_to(...)` APIs per the research guidance and review findings 1-3. Keep payloads tiny and raw so the test exercises finalization streaming, not compression. In `host_file_writer_name_tests.cpp`, add focused serializer-policy assertions for `src/formats/ba2/ba2_gnrl_serialize.cpp` and `src/formats/bsa/tes4_bsa_serialize.cpp` that require `open_host_file(` and forbid the reviewed narrow reopen pattern. Reference the resolved-path policy from D-53 through D-57 in task comments where the why is non-obvious.</action>
  <verify>
    <automated>cmake --build --preset windows-msvc-debug-static --target libbsa_tests && ctest --preset windows-msvc-debug-static --output-on-failure --tests-regex "host_file_writer_name|ba2_gnrl_writer|tes4_bsa_writer"</automated>
  </verify>
  <done>The new tests prove both archive families can serialize raw disk entries from non-ASCII UTF-8 host paths and the source-policy suite fails if finalization falls back to direct narrow-path `std::ifstream` opens.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve resolved host paths through BA2 and TES4 raw finalization</name>
  <files>src/formats/ba2/ba2_gnrl_prepare.hpp, src/formats/ba2/ba2_gnrl_prepare.cpp, src/formats/ba2/ba2_gnrl_serialize.cpp, src/formats/bsa/tes4_bsa_prepare.hpp, src/formats/bsa/tes4_bsa_prepare.cpp, src/formats/bsa/tes4_bsa_serialize.cpp</files>
  <behavior>
    - Test 1: BA2 prepared raw disk entries carry the already-resolved host path from prepare into finalization, and streaming reopens through `detail::open_host_file(...)`.
    - Test 2: TES4 prepared raw disk entries do the same while preserving the existing raw-size and embedded-prefix behavior.
    - Test 3: Diagnostics continue to use caller-owned UTF-8 text while Windows filesystem I/O reuses the resolved native path.
  </behavior>
  <action>Add a `detail::host_file_path` field to the internal BA2 and TES4 prepared-entry structs specifically for raw-disk streaming state, keeping the existing string members in place so this quick fix stays within the current host-file seam and does not spill into dedupe/layout work. In both prepare paths, resolve the disk source once when selecting raw streaming and store that resolved object on the prepared entry per review finding 3. Then change `stream_disk_payload(...)` and `stream_disk_payload_to_output(...)` to accept `const detail::host_file_path&`, open the file with `detail::open_host_file(...)`, and keep the existing bounded read / extra-byte mutation checks unchanged. Add a short why-comment near the new resolved-path field or helper usage explaining that raw finalization must reuse the prepare-time resolved host path because Windows UTF-8 correctness is locked at that seam.</action>
  <verify>
    <automated>cmake --build --preset windows-msvc-debug-static --target libbsa_tests && ctest --preset windows-msvc-debug-static --output-on-failure --tests-regex "host_file_writer_name|ba2_gnrl_writer|tes4_bsa_writer"</automated>
  </verify>
  <done>BA2 GNRL and TES4 raw finalization no longer reopen UTF-8 text with narrow `std::ifstream`, prepared raw entries carry the resolved `host_file_path`, and the focused writer/source-policy suites pass on Windows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller UTF-8 disk path -> writer prepare/finalize seam | Untrusted host-path text crosses from public writer input into Windows filesystem access |
| prepared raw entry -> serializer reopen | Internal state transfers the validated host path from preparation into finalization |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260514-11b-01 | T | BA2/TES4 raw serializer reopen helpers | mitigate | Replace direct narrow `std::ifstream` opens with `detail::open_host_file(resolved_path, context)` so finalization cannot reinterpret caller UTF-8 bytes through the active ANSI code page. |
| T-260514-11b-02 | D | raw disk source mutation during finalization | mitigate | Preserve the existing bounded chunk reads plus trailing-byte detection so a grown/shrunk source still fails archive finalization instead of silently publishing shifted payload bytes. |
| T-260514-11b-03 | R | writer seam regressions | mitigate | Add source-policy tests that assert serializer sources use `open_host_file(` and reject the reviewed raw narrow reopen pattern. |
</threat_model>

<verification>
- `cmake --build --preset windows-msvc-debug-static --target libbsa_tests`
- `ctest --preset windows-msvc-debug-static --output-on-failure --tests-regex "host_file_writer_name|ba2_gnrl_writer|tes4_bsa_writer"`
</verification>

<success_criteria>
- BA2 GNRL raw disk streaming writes and reopens correctly when the source file lives under a non-ASCII Windows path supplied through UTF-8 public API text.
- TES4 raw disk streaming does the same for all supported TES4 writer targets.
- Both serializer implementations reuse a prepared `detail::host_file_path` instead of reopening raw path text directly.
- Focused tests lock both behavioral round-trip success and the serializer source-policy seam.
</success_criteria>

<output>
After completion, update this quick-task directory with the execution summary or completion notes expected by the quick-task workflow.
</output>
