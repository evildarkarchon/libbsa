---
id: 260510-7if
description: Declare libbsa Windows-only and remove Linux CMake presets
status: planned
date: 2026-05-10
---

# Quick Task 260510-7if Plan

## Task 1: Declare Windows-only project policy

Files: `README.md`, `AGENTS.md`, `CLAUDE.md`, `.planning/PROJECT.md`, `.planning/research/STACK.md`, `docs/PRD.md`

Action: Add or update agent-facing and project-facing documentation so libbsa is explicitly Windows-only. Remove stale wording that asks reviewers to preserve a future Linux/macOS path.

Verify: Source search shows the root agent/readme docs contain Windows-only policy and no current project policy contradicts it.

Done: Windows-only support boundary is clear to maintainers and review agents.

## Task 2: Remove Linux preset contract

Files: `CMakePresets.json`, `tests/fixtures/README.md`, `tests/unit/validation_policy_tests.cpp`

Action: Remove the `linux-clang-asan-ubsan` configure/build/test presets and adjust policy docs/tests so Windows MSVC static/shared presets are the supported configured profiles.

Verify: `cmake --list-presets=all`, focused policy CTest, and source search for the removed preset name.

Done: No active build/test profile requires Linux or the removed sanitizer preset.
