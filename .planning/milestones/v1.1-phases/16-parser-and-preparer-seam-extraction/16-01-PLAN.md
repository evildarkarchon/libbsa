---
phase: 16-parser-and-preparer-seam-extraction
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/formats/bsa/tes4_bsa_parser.cpp
  - src/formats/bsa/tes4_bsa_table.hpp
  - src/formats/bsa/tes4_bsa_table.cpp
  - src/formats/bsa/tes4_bsa_payload_descriptor.hpp
  - src/formats/bsa/tes4_bsa_payload_descriptor.cpp
  - CMakeLists.txt
  - tests/CMakeLists.txt
  - tests/unit/tes4_bsa_parser_seam_tests.cpp
  - tests/unit/tes4_bsa_reader_tests.cpp
autonomous: true
requirements: [REFA-01]
must_haves:
  truths:
    - "Maintainer can change TES4 table/header parsing without editing payload descriptor rules."
    - "Maintainer can change TES4 embedded-name/raw-size/payload-span rules without editing raw table parsing."
    - "Existing TES4 success and malformed fixture behavior remains unchanged."
  artifacts:
    - path: "src/formats/bsa/tes4_bsa_table.hpp"
      provides: "Private raw table bundle seam for checked header/table state, folder records or blocks, and file-name strings"
      contains: "raw table"
    - path: "src/formats/bsa/tes4_bsa_payload_descriptor.hpp"
      provides: "Private payload descriptor seam for embedded-name prefix sizing, raw-size calculation, compression interpretation, and payload-span validation"
      contains: "payload descriptor"
    - path: "tests/unit/tes4_bsa_parser_seam_tests.cpp"
      provides: "Focused TES4 table and payload seam regression coverage"
      contains: "[parser_preparer_seam]"
  key_links:
    - from: "src/formats/bsa/tes4_bsa_parser.cpp"
      to: "src/formats/bsa/tes4_bsa_table.hpp"
      via: "thin parser coordinator calls raw table seam before materializing entries"
      pattern: "tes4_bsa_table"
    - from: "src/formats/bsa/tes4_bsa_parser.cpp"
      to: "src/formats/bsa/tes4_bsa_payload_descriptor.hpp"
      via: "entry materialization asks payload descriptor seam for compression/raw-size/span facts"
      pattern: "payload_descriptor"
---

<objective>
Extract the targeted TES4-family BSA parser hotspot into private raw-table and payload-descriptor seams with focused regression coverage.

Purpose: Satisfy REFA-01 and locked decisions D-01 through D-04 and D-10 by making TES4 table parsing and payload metadata rules independently reviewable while preserving current parser entrypoints and fixture behavior.
Output: Private TES4 table/payload modules, direct seam tests, and unchanged public TES4 parser behavior.
</objective>

<execution_context>
@J:/libbsa/.opencode/get-shit-done/workflows/execute-plan.md
@J:/libbsa/.opencode/get-shit-done/templates/summary.md
@J:/libbsa/.opencode/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/16-parser-and-preparer-seam-extraction/16-SPEC.md
@.planning/phases/16-parser-and-preparer-seam-extraction/16-CONTEXT.md
@.planning/phases/16-parser-and-preparer-seam-extraction/16-RESEARCH.md
@.planning/phases/16-parser-and-preparer-seam-extraction/16-PATTERNS.md
@.planning/phases/16-parser-and-preparer-seam-extraction/16-VALIDATION.md
@src/formats/bsa/tes4_bsa_parser.cpp
@src/formats/bsa/tes4_bsa_parser.hpp
@src/formats/bsa/tes4_bsa_constants.hpp
@tests/unit/tes4_bsa_reader_tests.cpp
@CMakeLists.txt
@tests/CMakeLists.txt

<interfaces>
Existing stable TES4 parser entrypoints that must not change:
- result<tes4_bsa_archive> parse_tes4_bsa_archive(std::span<const std::byte> bytes, detected_bsa_format detected)
- result<tes4_bsa_archive> parse_tes4_bsa_archive_file(const detail::host_file_path& host_path, std::uint64_t archive_size, detected_bsa_format detected)
- result<archive_metadata> parse_tes4_bsa_metadata(std::span<const std::byte> bytes, detected_bsa_format detected)

New private seam roles required by locked decisions:
- D-02 raw table bundle returns checked header/table state, folder records or folder blocks, and file-name strings only; it must not return partially normalized entry candidates or final entry_metadata values.
- D-03 payload descriptor helper owns embedded-name prefix sizing, raw-size calculation, compression interpretation, and payload-span validation from table records plus the payload reader.
</interfaces>
</context>

<feature>
  <name>TES4 raw table and payload descriptor seams</name>
  <files>src/formats/bsa/tes4_bsa_table.hpp, src/formats/bsa/tes4_bsa_table.cpp, src/formats/bsa/tes4_bsa_payload_descriptor.hpp, src/formats/bsa/tes4_bsa_payload_descriptor.cpp, src/formats/bsa/tes4_bsa_parser.cpp, tests/unit/tes4_bsa_parser_seam_tests.cpp, tests/unit/tes4_bsa_reader_tests.cpp, CMakeLists.txt, tests/CMakeLists.txt</files>
  <behavior>
    - A raw table seam reads and validates TES4 header/table bytes, folder records or folder blocks, and file-name strings while leaving canonical path construction, duplicate canonical path rejection, and entry_metadata construction outside the raw table bundle.
    - A payload descriptor seam derives entry_compression, embedded-name prefix size, raw uncompressed size, stored payload size, and payload spans, rejecting payload spans outside the archive or overlapping metadata with error_code::format_error.
    - Public parser entrypoints keep the exact signatures in src/formats/bsa/tes4_bsa_parser.hpp and existing TES4 reader/malformed tests still pass.
  </behavior>
  <implementation>Use adjacent private modules under src/formats/bsa per D-01. Keep parse_tes4_bsa_archive_impl as a thin coordinator that reads the raw table bundle, materializes entries, rejects duplicate canonical paths, validates hashes, and delegates payload facts to the payload descriptor helper. Do not add headers under include/libbsa/ and do not edit TES5Edit/.</implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — add focused TES4 seam regression tests</name>
  <read_first>
    - .planning/phases/16-parser-and-preparer-seam-extraction/16-SPEC.md
    - .planning/phases/16-parser-and-preparer-seam-extraction/16-CONTEXT.md
    - .planning/phases/16-parser-and-preparer-seam-extraction/16-PATTERNS.md
    - src/formats/bsa/tes4_bsa_parser.cpp
    - src/formats/bsa/tes4_bsa_parser.hpp
    - src/formats/bsa/tes4_bsa_constants.hpp
    - tests/unit/tes4_bsa_reader_tests.cpp
    - CMakeLists.txt
    - tests/CMakeLists.txt
  </read_first>
  <files>tests/unit/tes4_bsa_parser_seam_tests.cpp, tests/unit/tes4_bsa_reader_tests.cpp, tests/CMakeLists.txt</files>
  <action>Create tests/unit/tes4_bsa_parser_seam_tests.cpp with Catch2 tags [unit][fixture][tes4_bsa][parser_preparer_seam]. Test the D-02 raw table seam against generated tes4_v103.bsa metadata bytes for table sizing, folder/file-name offset handling, and filename consumption. Test the D-03 payload descriptor seam with the existing tes4_v103.bsa mutation pattern for embedded-name prefix sizing, raw-size calculation, compression interpretation for TES4 v103/v104/v105, and payload-span-over-metadata rejection. Extend tests/unit/tes4_bsa_reader_tests.cpp only if an existing helper must be shared or a public malformed case needs an assertion name update; do not weaken existing expected error_code assertions. Register the new test file in tests/CMakeLists.txt. RED gate must fail before implementation because the private seam behavior is not yet available, and the failure must be either an assertion against current monolithic behavior or unresolved private seam symbols, not a syntax error.</action>
  <verify>
    <automated>cmake --build --preset windows-msvc-debug-static --target libbsa_tests && ctest --preset windows-msvc-debug-static --output-on-failure -L "parser_preparer_seam"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: tests/unit/tes4_bsa_parser_seam_tests.cpp contains the exact tag string [parser_preparer_seam].
    - Source assertion: tests/unit/tes4_bsa_parser_seam_tests.cpp names raw table coverage for table sizing and folder/file-name offset handling.
    - Source assertion: tests/unit/tes4_bsa_parser_seam_tests.cpp names payload descriptor coverage for embedded-name prefix sizing, raw-size calculation, compression interpretation, and payload-span-over-metadata rejection.
    - Behavior assertion: the RED command runs and does not pass before the TES4 private seams are implemented.
  </acceptance_criteria>
  <done>Focused TES4 seam tests are committed in a RED state and registered for CTest discovery.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — extract raw table and payload descriptor seams</name>
  <read_first>
    - src/formats/bsa/tes4_bsa_parser.cpp
    - src/formats/bsa/tes4_bsa_parser.hpp
    - src/formats/bsa/tes4_bsa_constants.hpp
    - src/detail/parser_primitives.hpp
    - src/detail/binary_io.hpp
    - src/detail/host_file.hpp
    - tests/unit/tes4_bsa_parser_seam_tests.cpp
    - tests/unit/tes4_bsa_reader_tests.cpp
    - CMakeLists.txt
  </read_first>
  <files>src/formats/bsa/tes4_bsa_parser.cpp, src/formats/bsa/tes4_bsa_table.hpp, src/formats/bsa/tes4_bsa_table.cpp, src/formats/bsa/tes4_bsa_payload_descriptor.hpp, src/formats/bsa/tes4_bsa_payload_descriptor.cpp, CMakeLists.txt</files>
  <action>Implement D-01 through D-03 by moving TES4 table/header parsing responsibilities into adjacent private src/formats/bsa/tes4_bsa_table.hpp and .cpp and moving payload descriptor responsibilities into src/formats/bsa/tes4_bsa_payload_descriptor.hpp and .cpp. The raw table bundle must expose checked header fields, metadata table size, folder records or folder blocks, file records, and file-name strings only; it must not expose entry_metadata, canonical path strings, duplicate rejection, or normalized entry candidates. The payload descriptor helper must compute compression_for, embedded_prefix_size, raw_size_for, stored payload size, and payload span checks using the existing result<T> error propagation and the existing PayloadReader callback shape. Keep parse_tes4_bsa_archive, parse_tes4_bsa_archive_file, and parse_tes4_bsa_metadata signatures unchanged. Add both new .cpp files to libbsa_library_sources in CMakeLists.txt. Preserve Phase 13 host-file behavior in parse_tes4_bsa_archive_file by continuing to use detail::open_host_file and read_file_bytes_at from the resolved detail::host_file_path.</action>
  <verify>
    <automated>cmake --build --preset windows-msvc-debug-static --target libbsa_tests && ctest --preset windows-msvc-debug-static --output-on-failure -L "parser_preparer_seam"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: src/formats/bsa/tes4_bsa_table.hpp exists and does not include include/libbsa/archive.hpp unless needed only for existing result/error types through libbsa/result.hpp; it must not declare entry_metadata-returning table APIs.
    - Source assertion: src/formats/bsa/tes4_bsa_payload_descriptor.hpp exists and exposes a payload descriptor role that references embedded prefix, raw size, compression, and payload span semantics.
    - Source assertion: src/formats/bsa/tes4_bsa_parser.hpp public/internal parser entrypoint signatures remain byte-for-byte compatible for parse_tes4_bsa_archive, parse_tes4_bsa_archive_file, and parse_tes4_bsa_metadata.
    - Behavior assertion: focused TES4 seam tests and existing tes4_bsa tests pass under the verify command.
  </acceptance_criteria>
  <done>TES4 parser behavior passes with table/header and payload descriptor responsibilities split into private modules.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: REFACTOR — tighten TES4 coordinator and preserve public behavior</name>
  <read_first>
    - src/formats/bsa/tes4_bsa_parser.cpp
    - src/formats/bsa/tes4_bsa_table.hpp
    - src/formats/bsa/tes4_bsa_payload_descriptor.hpp
    - tests/unit/tes4_bsa_parser_seam_tests.cpp
    - tests/unit/tes4_bsa_reader_tests.cpp
    - .planning/phases/15-reader-backend-dispatch-cleanup/15-01-SUMMARY.md
  </read_first>
  <files>src/formats/bsa/tes4_bsa_parser.cpp, src/formats/bsa/tes4_bsa_table.hpp, src/formats/bsa/tes4_bsa_payload_descriptor.hpp, tests/unit/tes4_bsa_parser_seam_tests.cpp</files>
  <action>Refactor only if the GREEN implementation leaves duplicated checks or broad mixed-concern logic in parse_tes4_bsa_archive_impl. Keep the coordinator responsible for orchestration, archive_metadata construction, canonical archive path normalization, duplicate canonical path rejection, hash validation, and final sorting. Keep raw table bundle and payload descriptor helpers private to src/formats/bsa. Add short comments only for non-obvious format compatibility constraints such as why the raw table seam must not normalize entries or why payload spans must reject metadata overlap before decompression. Do not change public headers under include/libbsa/.</action>
  <verify>
    <automated>ctest --preset windows-msvc-debug-static --output-on-failure -L "parser_preparer_seam"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: parse_tes4_bsa_archive_impl no longer directly owns raw table reading functions and payload descriptor calculations in the same broad body.
    - Source assertion: any comments added explain a TES4 compatibility constraint or error-ordering reason; no accurate existing comments are removed as cleanup.
    - Behavior assertion: the verify command passes after refactor.
  </acceptance_criteria>
  <done>TES4 coordinator is thinner, behavior-preserving, and covered by direct seam plus existing public parser tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Archive bytes -> TES4 table seam | Untrusted metadata counts, offsets, and string table sizes cross into allocation and parsing code. |
| TES4 table records -> payload descriptor seam | Untrusted payload offsets and size flags cross into span validation and compression interpretation. |
| Parser internals -> public archive metadata | Internal parser results become consumer-visible entry_metadata and error_code behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Tampering/Denial of Service | tes4_bsa_table raw table parsing | mitigate | Preserve validate_metadata_count, multiply_fits, add_fits, and span_fits checks; add tests for table sizing and folder/file-name offsets. |
| T-16-02 | Tampering | tes4_bsa_payload_descriptor payload spans | mitigate | Reject payload spans outside archive or overlapping metadata with error_code::format_error before entry materialization. |
| T-16-03 | Repudiation | TES4 malformed behavior | mitigate | Keep existing public malformed fixture tests and focused seam tests checking stable error_code values. |
| T-16-04 | Information Disclosure | TES5Edit reference boundary | accept | TES5Edit is read-only reference material and no Phase 16 plan modifies or stages files under TES5Edit/. |
</threat_model>

<verification>
- RED: cmake --build --preset windows-msvc-debug-static --target libbsa_tests && ctest --preset windows-msvc-debug-static --output-on-failure -L "parser_preparer_seam" fails before seam implementation for the planned TES4 seam behavior.
- GREEN/REFACTOR: the same focused command passes.
- Wave gate: ctest --preset windows-msvc-debug-static --output-on-failure -L "tes4_bsa|parser_preparer_seam" after all Wave 1 work completes; full debug suite remains a phase gate in Plan 16-03.
</verification>

<success_criteria>
- REFA-01 is satisfied for TES4 parser seams.
- D-01, D-02, D-03, D-04, and D-10 are implemented or directly covered.
- No public headers under include/libbsa/ are added or changed for TES4 seam extraction.
- Existing TES4 reader fixture and malformed behavior remains unchanged.
</success_criteria>

<source_audit>
- GOAL: Targeted TES4 parser hotspot becomes a smaller internal seam with focused regression coverage — COVERED by this plan.
- REQ: REFA-01 — COVERED by this plan.
- RESEARCH: Raw table bundle, payload descriptor helper, stable parser entrypoints, host-file boundary, no public API expansion — COVERED by this plan.
- CONTEXT: D-01, D-02, D-03, D-04, D-10, D-12 — COVERED by this plan.
- DEFERRED/OUT OF SCOPE: Dedupe optimization, BA2 DX10 temp lifecycle cleanup proof, public API redesign, TES5Edit edits — EXCLUDED.
</source_audit>

<output>
After completion, create `.planning/phases/16-parser-and-preparer-seam-extraction/16-01-SUMMARY.md` with RED/GREEN/REFACTOR details and commits.
</output>
