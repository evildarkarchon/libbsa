---
phase: 17-writer-hotspot-hardening-and-ship-gate
plan: 02
type: tdd
wave: 2
depends_on:
  - 17-01
files_modified:
  - src/formats/ba2/ba2_gnrl_prepare.hpp
  - src/formats/ba2/ba2_gnrl_prepare.cpp
  - src/formats/ba2/ba2_gnrl_layout.cpp
  - tests/unit/ba2_gnrl_writer_tests.cpp
  - tests/unit/writer_hotspot_policy_tests.cpp
autonomous: true
requirements:
  - DEDU-02
must_haves:
  truths:
    - "D-05: BA2 GNRL writer dedupe combines runtime archive behavior proof with source/policy guardrails for staged identity or digest hardening."
    - "D-06: BA2 GNRL writer dedupe uses explicit staged identity or digest evidence for final stored bytes, proving bounded exact comparisons without timing-sensitive benchmarks."
    - "D-07: BA2 GNRL dedupe narrowing stays format-local near existing BA2 GNRL preparation/layout code unless a very small internal helper is justified."
    - "D-08: BA2 GNRL shared offsets still require ba2_gnrl_payloads_equal exact fallback before assignment; identity or digest values are filters only."
    - "BA2 GNRL disk-source growth and truncation are still rejected before malformed offsets can be published."
  artifacts:
    - path: "src/formats/ba2/ba2_gnrl_prepare.hpp"
      provides: "Prepared-entry staged identity or digest contract"
      contains: "ba2_gnrl_prepared_entry"
    - path: "src/formats/ba2/ba2_gnrl_layout.cpp"
      provides: "BA2 GNRL dedupe candidate narrowing and exact equality fallback"
      contains: "ba2_gnrl_payloads_equal"
    - path: "tests/unit/ba2_gnrl_writer_tests.cpp"
      provides: "Runtime BA2 GNRL dedupe and disk-source regression proof"
      contains: "[ba2_gnrl_writer]"
    - path: "tests/unit/writer_hotspot_policy_tests.cpp"
      provides: "Source-policy guardrail for staged identity/digest plus exact fallback"
      contains: "ba2_gnrl_payloads_equal"
  key_links:
    - from: "src/formats/ba2/ba2_gnrl_prepare.cpp"
      to: "src/formats/ba2/ba2_gnrl_layout.cpp"
      via: "ba2_gnrl_prepared_entry staged identity/digest field"
      pattern: "ba2_gnrl_prepared_entry"
    - from: "src/formats/ba2/ba2_gnrl_layout.cpp"
      to: "ba2_gnrl_payloads_equal"
      via: "dedupe candidate branch"
      pattern: "ba2_gnrl_payloads_equal"
---

<objective>
Harden BA2 GNRL dedupe with explicit staged final-stored identity or digest narrowing while preserving exact equality and disk-source change rejection.

Purpose: Satisfies DEDU-02, D-05, D-06, D-07, and D-08 without adding dependencies or changing the public BA2 GNRL writer surface.
Output: BA2 GNRL prepared identity/digest evidence, layout candidate narrowing, runtime tests, and writer-hotspot policy extensions.
</objective>

<execution_context>
@J:/libbsa/.opencode/get-shit-done/workflows/execute-plan.md
@J:/libbsa/.opencode/get-shit-done/templates/summary.md
@J:/libbsa/.opencode/get-shit-done/references/tdd.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/17-writer-hotspot-hardening-and-ship-gate/17-SPEC.md
@.planning/phases/17-writer-hotspot-hardening-and-ship-gate/17-CONTEXT.md
@.planning/phases/17-writer-hotspot-hardening-and-ship-gate/17-RESEARCH.md
@.planning/phases/17-writer-hotspot-hardening-and-ship-gate/17-VALIDATION.md
@.planning/phases/17-writer-hotspot-hardening-and-ship-gate/17-PATTERNS.md
@.planning/phases/17-writer-hotspot-hardening-and-ship-gate/17-01-SUMMARY.md
@src/formats/ba2/ba2_gnrl_prepare.hpp
@src/formats/ba2/ba2_gnrl_prepare.cpp
@src/formats/ba2/ba2_gnrl_layout.cpp
@tests/unit/ba2_gnrl_writer_tests.cpp
@tests/unit/writer_hotspot_policy_tests.cpp
</context>

<feature>
  <name>BA2 GNRL staged identity/digest dedupe hardening</name>
  <files>src/formats/ba2/ba2_gnrl_prepare.hpp, src/formats/ba2/ba2_gnrl_prepare.cpp, src/formats/ba2/ba2_gnrl_layout.cpp, tests/unit/ba2_gnrl_writer_tests.cpp, tests/unit/writer_hotspot_policy_tests.cpp</files>
  <behavior>
    - BA2 GNRL preparation records explicit final-stored identity or digest evidence for each prepared entry without adding external dependencies.
    - ba2_gnrl_assign_payload_offsets uses that staged evidence as the dedupe candidate key when deduplicate_payloads is true.
    - Every shared payload offset remains gated by ba2_gnrl_payloads_equal.
    - Disk-backed sources that grow or truncate after preparation still return io_error during equality fallback.
  </behavior>
  <implementation>Add a named field or small private key type for final-stored identity/digest evidence in the BA2 GNRL prepared path, then use it in layout candidate selection. Keep ba2_gnrl_payloads_equal as the only correctness authority for sharing offsets. Do not introduce a generic dedupe framework, do not add dependencies, and do not change raw/compressed routing.</implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED - add BA2 GNRL staged identity and fallback tests</name>
  <read_first>
    - .planning/phases/17-writer-hotspot-hardening-and-ship-gate/17-SPEC.md
    - .planning/phases/17-writer-hotspot-hardening-and-ship-gate/17-CONTEXT.md
    - .planning/phases/17-writer-hotspot-hardening-and-ship-gate/17-VALIDATION.md
    - .planning/phases/17-writer-hotspot-hardening-and-ship-gate/17-PATTERNS.md
    - .planning/phases/17-writer-hotspot-hardening-and-ship-gate/17-01-SUMMARY.md
    - src/formats/ba2/ba2_gnrl_prepare.hpp
    - src/formats/ba2/ba2_gnrl_prepare.cpp
    - src/formats/ba2/ba2_gnrl_layout.cpp
    - tests/unit/ba2_gnrl_writer_tests.cpp
    - tests/unit/writer_hotspot_policy_tests.cpp
  </read_first>
  <files>tests/unit/ba2_gnrl_writer_tests.cpp, tests/unit/writer_hotspot_policy_tests.cpp</files>
  <action>Extend runtime and policy coverage for DEDU-02. Add policy assertions tagged [unit][writer_hotspot_policy] that read ba2_gnrl_prepare.hpp, ba2_gnrl_prepare.cpp, and ba2_gnrl_layout.cpp and require an explicit staged final-stored identity or digest field/key in ba2_gnrl_prepared_entry or adjacent private layout code; require ba2_gnrl_payloads_equal in the sharing path; require disk-source changed diagnostics to remain present. Extend tests/unit/ba2_gnrl_writer_tests.cpp only where current tests do not prove the new guard, keeping existing cases for byte-identical stored payload sharing, raw-vs-compressed separation, and disk-source growth/truncation rejection. The RED gate must fail before implementation because the explicit staged identity/digest evidence is not yet present.</action>
  <verify>
    <automated>cmake --build --preset windows-msvc-debug-static && ctest --preset windows-msvc-debug-static --output-on-failure -L "ba2_gnrl_writer|writer_hotspot_policy"</automated>
  </verify>
  <acceptance_criteria>
    - tests/unit/writer_hotspot_policy_tests.cpp contains BA2 GNRL assertions against ba2_gnrl_prepare.hpp or ba2_gnrl_prepare.cpp and ba2_gnrl_layout.cpp.
    - RED run fails on missing explicit BA2 GNRL staged identity/digest evidence or equivalent BA2 GNRL policy proof, not on build or discovery errors.
    - Existing BA2 GNRL disk-source size-change rejection tests still assert error_code::io_error.
    - Existing raw-vs-compressed dedupe separation coverage remains tagged [ba2_gnrl_writer].
  </acceptance_criteria>
  <done>RED gate is committed as test(17-02): add failing test for BA2 GNRL dedupe identity hardening.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN - add staged identity/digest narrowing with exact equality fallback</name>
  <read_first>
    - src/formats/ba2/ba2_gnrl_prepare.hpp
    - src/formats/ba2/ba2_gnrl_prepare.cpp
    - src/formats/ba2/ba2_gnrl_layout.cpp
    - tests/unit/ba2_gnrl_writer_tests.cpp
    - tests/unit/writer_hotspot_policy_tests.cpp
    - .planning/phases/17-writer-hotspot-hardening-and-ship-gate/17-CONTEXT.md
    - .planning/phases/17-writer-hotspot-hardening-and-ship-gate/17-PATTERNS.md
  </read_first>
  <files>src/formats/ba2/ba2_gnrl_prepare.hpp, src/formats/ba2/ba2_gnrl_prepare.cpp, src/formats/ba2/ba2_gnrl_layout.cpp</files>
  <action>Add explicit BA2 GNRL final-stored candidate evidence without changing public APIs. Preferred target state: ba2_gnrl_prepared_entry carries a named staged identity or digest value derived from the final stored byte stream after raw-vs-compressed routing; ba2_gnrl_prepare.cpp fills it for both memory-backed and disk-backed entries using existing in-memory bytes or existing chunked host-file hashing; ba2_gnrl_layout.cpp uses a private dedupe key containing stored_size and the staged identity/digest for candidate lookup. Keep ba2_gnrl_payloads_equal as the required final comparison before setting payload_offset and owns_payload_bytes = false. Keep compare_disk_payloads and compare_disk_payload_to_bytes growth/truncation checks and their io_error behavior unchanged.</action>
  <verify>
    <automated>cmake --build --preset windows-msvc-debug-static && ctest --preset windows-msvc-debug-static --output-on-failure -L "ba2_gnrl_writer|writer_hotspot_policy"</automated>
  </verify>
  <acceptance_criteria>
    - ba2_gnrl_prepared_entry or adjacent private BA2 GNRL layout code exposes a named staged identity/digest used by ba2_gnrl_assign_payload_offsets candidate lookup.
    - ba2_gnrl_layout.cpp still calls ba2_gnrl_payloads_equal before any shared payload_offset assignment.
    - BA2 GNRL disk-source growth and truncation tests still return error_code::io_error.
    - Focused ba2_gnrl_writer and writer_hotspot_policy labels pass in Debug.
    - vcpkg.json and public include/libbsa headers are unchanged by this plan.
  </acceptance_criteria>
  <done>GREEN gate is committed as feat(17-02): implement BA2 GNRL staged dedupe identity narrowing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: REFACTOR - keep BA2 GNRL narrowing local and policy resilient</name>
  <read_first>
    - src/formats/ba2/ba2_gnrl_prepare.hpp
    - src/formats/ba2/ba2_gnrl_prepare.cpp
    - src/formats/ba2/ba2_gnrl_layout.cpp
    - tests/unit/writer_hotspot_policy_tests.cpp
  </read_first>
  <files>src/formats/ba2/ba2_gnrl_prepare.hpp, src/formats/ba2/ba2_gnrl_prepare.cpp, src/formats/ba2/ba2_gnrl_layout.cpp, tests/unit/writer_hotspot_policy_tests.cpp</files>
  <action>Refactor only if needed to avoid duplicated hashing or brittle source-policy assertions. Keep helpers private under src/formats/ba2 and keep policy assertions focused on final-stored identity/digest evidence, ba2_gnrl_payloads_equal fallback, and disk-source change rejection. Do not create a shared dedupe framework and do not weaken collision-safety by allowing identity/digest-only offset sharing.</action>
  <verify>
    <automated>cmake --build --preset windows-msvc-debug-static && ctest --preset windows-msvc-debug-static --output-on-failure -L "ba2_gnrl_writer|writer_hotspot_policy"</automated>
  </verify>
  <acceptance_criteria>
    - Focused Debug labels pass after any refactor.
    - Source-policy tests do not require incidental helper names beyond deliberate staged identity/digest contract identifiers introduced by this plan.
    - ba2_gnrl_payloads_equal and disk-source changed error text remain present in source.
  </acceptance_criteria>
  <done>Optional REFACTOR gate is committed as refactor(17-02): clean up BA2 GNRL dedupe identity hardening if source changes are made; otherwise summary records no refactor needed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| staged BA2 GNRL entries -> layout offsets | Caller-controlled payload data influences dedupe candidate identity and final offsets. |
| host disk source -> exact equality fallback | Disk-backed files may grow, truncate, or become unreadable after preparation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-04 | T | src/formats/ba2/ba2_gnrl_layout.cpp | mitigate | Keep ba2_gnrl_payloads_equal as final authority before shared offsets so staged identity/digest collisions cannot corrupt archives. |
| T-17-05 | T | src/formats/ba2/ba2_gnrl_layout.cpp | mitigate | Preserve disk-source growth/truncation io_error checks in compare_disk_payloads and compare_disk_payload_to_bytes. |
| T-17-06 | D | src/formats/ba2/ba2_gnrl_prepare.cpp | mitigate | Use explicit staged identity/digest evidence to reduce repeated expensive equality scans without timing-sensitive benchmarks. |
</threat_model>

<verification>
Focused plan gate: `cmake --build --preset windows-msvc-debug-static && ctest --preset windows-msvc-debug-static --output-on-failure -L "ba2_gnrl_writer|writer_hotspot_policy"`.
Wave gate after Wave 2 completion: `cmake --build --preset windows-msvc-debug-static && ctest --preset windows-msvc-debug-static --output-on-failure`.
</verification>

<success_criteria>
- DEDU-02 is covered by runtime and source-policy evidence.
- BA2 GNRL dedupe uses explicit final-stored identity or digest candidate narrowing.
- Exact equality and disk-source change rejection remain mandatory before shared offset assignment.
- No public API expansion, dependency change, or TES5Edit/ modification occurs.
</success_criteria>

<output>
After completion, create `.planning/phases/17-writer-hotspot-hardening-and-ship-gate/17-02-SUMMARY.md` with RED/GREEN/REFACTOR evidence, commands run, and DEDU-02 traceability.
</output>
