---
phase: 17.1-address-tech-debt-in-planning-v1-1-milestone-audit-md
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/host_path_correctness_boundary_tests.cpp
autonomous: true
requirements: [SPEC-1, D-05, D-06, D-07, D-08, D-13, D-14]
must_haves:
  truths:
    - "A copied non-ASCII host-path archive opens through libbsa::archive_reader::open."
    - "The opened reader exercises entries, find, contains, and extract_entries through the public API from the non-ASCII copied path."
    - "The regression is a reader matrix reusing representative Phase 15 families where practical without duplicating negative dispatch cases."
    - "D-05: The combined regression uses a reader matrix, not a single-fixture smoke test, reusing representative Phase 15 families where practical."
    - "D-06: The combined regression covers only entries, find, contains, and extract_entries through the public API from copied non-ASCII Windows host paths."
    - "D-07: The combined regression lives in tests/unit/host_path_correctness_boundary_tests.cpp and reuses its non-ASCII path setup, UTF-8 conversion, fixture-copy, and manifest patterns."
    - "D-08: The regression remains success-path focused on manifest count/path, canonical find/contains, and extract_entries payload bytes without duplicating negative dispatch matrices."
    - "D-13: Runtime proof is required here for public reader behavior under non-ASCII host paths, with source-policy proof handled by the cleanup/writer plans."
    - "D-14: Host-path and reader-dispatch verification evidence targets focused Debug and MSVC AddressSanitizer lanes rather than Release package proof."
  artifacts:
    - path: "tests/unit/host_path_correctness_boundary_tests.cpp"
      provides: "Combined non-ASCII host-path plus reader-surface regression"
      contains: "entries, find, contains, extract_entries"
  key_links:
    - from: "tests/unit/host_path_correctness_boundary_tests.cpp"
      to: "tests/fixtures/generated/archives"
      via: "manifest-backed copied archive cases"
      pattern: "copy_archive_under_test.*archive_reader::open"
    - from: "tests/unit/host_path_correctness_boundary_tests.cpp"
      to: "libbsa::archive_reader public API"
      via: "entries/find/contains/extract_entries assertions"
      pattern: "reader\.extract_entries"
---

<objective>
Add the combined public-API regression that closes the audit warning where non-ASCII host-path coverage and full reader-surface dispatch coverage were split across separate suites.

Purpose: Prove the Phase 13 host-file boundary and Phase 15 reader backend seam work together for copied archives under non-ASCII Windows host paths, per SPEC-1 and D-05 through D-08.
Output: One focused Catch2 regression in `tests/unit/host_path_correctness_boundary_tests.cpp`.
</objective>

<execution_context>
@J:/libbsa/.opencode/get-shit-done/workflows/execute-plan.md
@J:/libbsa/.opencode/get-shit-done/templates/summary.md
@J:/libbsa/.opencode/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/v1.1-MILESTONE-AUDIT.md
@.planning/phases/17.1-address-tech-debt-in-planning-v1-1-milestone-audit-md/17.1-SPEC.md
@.planning/phases/17.1-address-tech-debt-in-planning-v1-1-milestone-audit-md/17.1-CONTEXT.md
@.planning/phases/17.1-address-tech-debt-in-planning-v1-1-milestone-audit-md/17.1-RESEARCH.md
@.planning/phases/17.1-address-tech-debt-in-planning-v1-1-milestone-audit-md/17.1-VALIDATION.md
@.planning/phases/17.1-address-tech-debt-in-planning-v1-1-milestone-audit-md/17.1-PATTERNS.md
@.planning/codebase/TESTING.md
@.planning/phases/15-reader-backend-dispatch-cleanup/15-01-SUMMARY.md
@.planning/phases/15-reader-backend-dispatch-cleanup/15-LEARNINGS.md
@tests/unit/host_path_correctness_boundary_tests.cpp
@tests/unit/archive_reader_dispatch_tests.cpp
@tests/fixtures/generated/archives/

<interfaces>
From `tests/unit/host_path_correctness_boundary_tests.cpp` pattern map:
- Reuse `non_ascii_path_token`, `unique_non_ascii_root()`, `copy_archive_under_test(...)`, `utf8_string_from_path(...)`, `canonical_entry_from_manifest(...)`, and `expected_bytes_from_manifest_entry(...)` if present.
- The public open call is `libbsa::archive_reader::open(utf8_string_from_path(copied_archive))`.

From `tests/unit/archive_reader_dispatch_tests.cpp` pattern map:
- Adapt the concrete `recording_sink_factory`/capturing sink pattern for `extract_entries` payload assertions.
- Do not copy missing-path or invalid-path negative matrix cases into this host-path suite per D-08.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add combined non-ASCII reader-surface matrix regression</name>
  <files>tests/unit/host_path_correctness_boundary_tests.cpp</files>
  <read_first>
    - .planning/phases/17.1-address-tech-debt-in-planning-v1-1-milestone-audit-md/17.1-SPEC.md
    - .planning/phases/17.1-address-tech-debt-in-planning-v1-1-milestone-audit-md/17.1-CONTEXT.md
    - .planning/phases/17.1-address-tech-debt-in-planning-v1-1-milestone-audit-md/17.1-RESEARCH.md
    - .planning/phases/17.1-address-tech-debt-in-planning-v1-1-milestone-audit-md/17.1-PATTERNS.md
    - .planning/v1.1-MILESTONE-AUDIT.md
    - .planning/codebase/TESTING.md
    - .planning/phases/15-reader-backend-dispatch-cleanup/15-01-SUMMARY.md
    - .planning/phases/15-reader-backend-dispatch-cleanup/15-LEARNINGS.md
    - tests/unit/host_path_correctness_boundary_tests.cpp
    - tests/unit/archive_reader_dispatch_tests.cpp
  </read_first>
  <behavior>
    - For each selected representative reader case, copying the archive under a path containing `libbsa-Ångström-日本語` and opening via `archive_reader::open` returns `has_value()`.
    - `entries()` returns a manifest-backed count and includes the canonical manifest path.
    - `find(canonical_path)` returns the canonical entry and `contains(canonical_path)` returns true.
    - `extract_entries` for the canonical path writes exactly the manifest expected payload bytes to the recording sink.
  </behavior>
  <action>Add one success-path-focused Catch2 test case in `tests/unit/host_path_correctness_boundary_tests.cpp` per D-05, D-06, D-07, and D-08. Reuse the existing non-ASCII temp-root/copy/UTF-8 helpers in that file. Adapt only the success-path reader-surface pieces from `tests/unit/archive_reader_dispatch_tests.cpp`: manifest entry count/path expectations, canonical `find`, canonical `contains`, and a concrete `bulk_extract_sink_factory` capture for `extract_entries`. Use a reader matrix rather than a single fixture; include the Phase 15 representative families where practical. If adding TES3 requires changing a host-path suite source-policy allowlist, update only the policy assertion in this same file so `tes3_success.bsa` is allowed for the combined matrix. Do not add `extract`, `extract_bytes`, missing-path, invalid-path, or negative lookup loops unless the new test exposes a real implementation gap. Record focused Debug proof for this reader regression and leave ASan evidence to the final verification plan per D-13 and D-14.</action>
  <acceptance_criteria>
    - Source assertion: `tests/unit/host_path_correctness_boundary_tests.cpp` contains one test whose name includes both `non-ASCII` (or `non-ASCII host path`) and `reader` or `dispatch`.
    - Source assertion: the new test body contains calls or equivalent helper calls for `.entries(`, `.find(`, `.contains(`, and `.extract_entries(`.
    - Source assertion: the new test uses `copy_archive_under_test` or the existing copied non-ASCII fixture helper, not direct fixture paths from `tests/fixtures/generated/archives`.
    - Source assertion: the new test does not introduce new missing-path or invalid-path negative dispatch loops.
    - Behavior assertion: at least one copied non-ASCII archive fixture is opened through `libbsa::archive_reader::open` using a UTF-8 string derived from the copied path.
  </acceptance_criteria>
  <verify>
    <automated>ctest --preset windows-msvc-debug-static --output-on-failure -R host_path_correctness_boundary</automated>
    <automated>ctest --preset windows-msvc-debug-static --output-on-failure -R "host_path_correctness_boundary|archive_reader_dispatch|host_file|host_file_path"</automated>
  </verify>
  <done>The focused host-path suite proves the SPEC-1 public reader surface from copied non-ASCII host paths, and the broader focused Debug command passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public UTF-8 host path -> Windows native filesystem path | Untrusted caller UTF-8 text is decoded and later reused for filesystem I/O. |
| Copied fixture archive -> reader public API | Archive bytes and archive-internal paths flow through parser, lookup, and extraction. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17.1-01 | Tampering/DoS | `archive_reader::open` from non-ASCII copied path | mitigate | Regression must open copied archives via UTF-8 text and then exercise public reader operations without raw-string reopen drift. |
| T-17.1-02 | Tampering | `extract_entries` payload routing | mitigate | Assert extracted bytes equal manifest expected bytes from the copied non-ASCII archive path. |
</threat_model>

<verification>
Run focused Debug CTest after the task:
- `ctest --preset windows-msvc-debug-static --output-on-failure -R host_path_correctness_boundary`
- `ctest --preset windows-msvc-debug-static --output-on-failure -R "host_path_correctness_boundary|archive_reader_dispatch|host_file|host_file_path"`
</verification>

<success_criteria>
- SPEC-1 and D-05 through D-08 are implemented exactly.
- No public API headers under `include/libbsa/` are modified.
- `.planning/v1.1-MILESTONE-AUDIT.md` is not modified.
- No files under `TES5Edit/` are modified.
</success_criteria>

<output>
After completion, create `.planning/phases/17.1-address-tech-debt-in-planning-v1-1-milestone-audit-md/17.1-01-SUMMARY.md`.
</output>
