---
phase: 17.1-address-tech-debt-in-planning-v1-1-milestone-audit-md
verified: 2026-05-15T05:00:05Z
status: passed
score: 13/13 must-haves verified
overrides_applied: 0
---

# Phase 17.1: Address Tech Debt in v1.1 Milestone Audit Verification Report

**Phase Goal:** close urgent v1.1 audit tech debt from `.planning/v1.1-MILESTONE-AUDIT.md` without modifying the audit source file, preserving the TES5Edit boundary, and proving host-path/reader/writer cleanup with focused Debug and MSVC ASan evidence.
**Verified:** 2026-05-15T05:00:05Z
**Status:** passed
**Re-verification:** No — previous verification existed, but it had no structured `gaps:` section, so this pass re-established must-haves from the phase spec and plan frontmatter.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | A copied non-ASCII host-path archive opens through `libbsa::archive_reader::open`. | ✓ VERIFIED | `tests/unit/host_path_correctness_boundary_tests.cpp` copies representative fixtures under `libbsa-Ångström-日本語` and opens with `archive_reader::open(utf8_string_from_path(copied_archive))`. Focused Debug and ASan CTest runs passed the regression. |
| 2 | The opened reader exercises `entries`, `find`, `contains`, and `extract_entries` through the public API from the non-ASCII copied path. | ✓ VERIFIED | The `host_path_correctness_boundary non-ASCII host path exercises reader dispatch surface` test calls `entries`, `find`, `contains`, and `extract_entries`, then checks manifest path/count and payload bytes. |
| 3 | The regression is a reader matrix reusing representative Phase 15 families without duplicating negative dispatch cases. | ✓ VERIFIED | `representative_archive_cases()` includes TES3 BSA, TES4 v103/v104/v105 BSA, BA2 GNRL FO4, BA2 DX10 FO4, and BA2 GNRL SFv3. No new missing-path or invalid-path negative dispatch loops were added in the host-path suite. |
| 4 | `detail::host_file_path` no longer stores `original_utf8` or claims diagnostics-only preservation. | ✓ VERIFIED | `src/detail/host_file_path.hpp` contains only `std::filesystem::path resolved`; source grep found no `original_utf8` under `src`. `host_file_path contract does not preserve caller UTF-8 text as dead diagnostics state` passed in both focused lanes. |
| 5 | `archive_reader::state` no longer stores `backend_identity` beside `backend_table`. | ✓ VERIFIED | `src/archive.cpp` state contains metadata, entries, resolved host path, and `const reader_backend* backend_table`; `backend_identity` remains only file-local/open-time selector plumbing. The state-slice policy test passed in both focused lanes. |
| 6 | Public reader methods continue to use the open-time selected `backend_table` and do not regain repeated family dispatch. | ✓ VERIFIED | Public methods in `src/archive.cpp` call `state_->backend_table` or helpers that receive it; `archive_reader_dispatch_policy forbids repeated family dispatch in public reader methods` passed in Debug and ASan. |
| 7 | Writer host-path disposition covers TES3 BSA, TES4-family BSA, BA2 GNRL, and BA2 DX10 source, output, finalization, and dedupe paths. | ✓ VERIFIED | `tests/unit/host_file_writer_name_tests.cpp` defines `writer_host_path_inventory_cases` for all four families and asserts source/output/finalization/dedupe disposition tokens. Actual source grep confirms shared host-file/publish seams in corresponding writer files. |
| 8 | Runtime and source-policy proof close the audit warning without broad writer redesign or public API expansion. | ✓ VERIFIED | Focused runtime tests and source-policy tests passed; git diff checks found no changes under `include/libbsa`, no dependency manifest changes, and no broad writer API expansion. |
| 9 | Focused Debug and MSVC AddressSanitizer host-path/reader/writer I/O gates pass. | ✓ VERIFIED | Ran `ctest --preset windows-msvc-debug-static --output-on-failure -R "host_path_correctness_boundary|archive_reader_dispatch|host_file|host_file_path"`: 30/30 passed. Ran the same ASan preset/filter: 30/30 passed. |
| 10 | `.planning/v1.1-MILESTONE-AUDIT.md` remains unmodified during Phase 17.1 implementation. | ✓ VERIFIED | `git diff --name-only -- .planning/v1.1-MILESTONE-AUDIT.md` and `git diff --name-only HEAD -- .planning/v1.1-MILESTONE-AUDIT.md` produced no output before this verification report was rewritten. |
| 11 | BA2 DX10 abnormal-termination cleanup remains explicitly accepted residual risk and out of scope. | ✓ VERIFIED | `17.1-VERIFICATION.md` and `17.1-VALIDATION.md` record this as residual risk; no Phase 17.1 source changes attempt crash/OS-shutdown-proof cleanup. |
| 12 | TES5Edit read-only boundary is preserved. | ✓ VERIFIED | `git diff --name-only -- TES5Edit` and `git diff --name-only HEAD -- TES5Edit` produced no output. |
| 13 | No new external dependencies or public API surfaces were introduced. | ✓ VERIFIED | Forbidden-scope git diff checks found no changes under `include/libbsa`, `vcpkg.json`, or `vcpkg-configuration.json`. |

**Score:** 13/13 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `tests/unit/host_path_correctness_boundary_tests.cpp` | Combined non-ASCII host-path plus reader-surface regression | ✓ VERIFIED | Substantive matrix test exists and is wired into CTest; Debug/ASan focused lanes execute it. |
| `src/detail/host_file_path.hpp` | Internal host-file path contract containing only operational resolved path state | ✓ VERIFIED | Struct contains `std::filesystem::path resolved`; no `original_utf8`. |
| `src/detail/host_file_path.cpp` | Strict UTF-8 decode into resolved Windows-native path | ✓ VERIFIED | Uses `MultiByteToWideChar(CP_UTF8, MB_ERR_INVALID_CHARS)` and returns `host_file_path{std::filesystem::path{...}}`. |
| `src/archive.cpp` | Reader state without stored backend identity and public methods on selected backend table | ✓ VERIFIED | `state` has no backend identity member; open-time selector remains file-local and public methods route through `backend_table`. |
| `tests/unit/host_file_path_tests.cpp` | UTF-8 conversion and removed-state policy proof | ✓ VERIFIED | Tests strict decode, malformed UTF-8 rejection, and absence of removed diagnostics state. |
| `tests/unit/host_file_tests.cpp` | Host-file helper behavior without `.original_utf8` access | ✓ VERIFIED | Tests exact/prefix/chunk/missing/change/allocation behavior; no `.original_utf8` access. |
| `tests/unit/archive_reader_dispatch_policy_tests.cpp` | Policy proof for no repeated public dispatch and no stored backend identity | ✓ VERIFIED | Slices public method bodies and reader state; both policy tests pass. |
| `tests/unit/host_file_writer_name_tests.cpp` | Writer host-path inventory and source-policy evidence | ✓ VERIFIED | Explicit matrix covers TES3 BSA, TES4 BSA, BA2 GNRL, BA2 DX10; asserts shared host-file/publish seams and negative invariants. |
| `17.1-VALIDATION.md` | Completed validation status | ✓ VERIFIED | Frontmatter has `wave_0_complete: true`; task rows are green. |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `host_path_correctness_boundary_tests.cpp` | generated archive fixtures | `copy_archive_under_test` and manifest-backed matrix | ✓ WIRED | Test copies fixture archives under non-ASCII paths before opening and asserting manifest expectations. |
| `host_path_correctness_boundary_tests.cpp` | `libbsa::archive_reader` public API | `open`, `entries`, `find`, `contains`, `extract_entries` | ✓ WIRED | Public API calls are in the regression that passed in Debug and ASan. |
| `host_file_path.cpp` | `host_file_path.hpp` | `resolve_host_file_path` returns resolved-only contract | ✓ WIRED | Source returns the single-member contract; tests verify strict UTF-8 conversion and absent diagnostics state. |
| `archive.cpp` | reader backend table | open-time `reader_backend_table(backend_identity)` selection | ✓ WIRED | Backend identity is a function parameter/local selector, not stored state; public methods use `backend_table`. |
| `host_file_writer_name_tests.cpp` | BSA/BA2 writer source files | source-policy matrix assertions | ✓ WIRED | Test reads actual writer sources and asserts resolve/open/publish/dedupe disposition tokens. |
| `17.1-VERIFICATION.md` | SPEC/CONTEXT decisions | requirement and decision evidence | ✓ WIRED | This report records the phase outcomes, residual risk, forbidden-scope checks, and focused lane evidence. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|---|---|---|---|---|
| `host_path_correctness_boundary_tests.cpp` | copied archive path and manifest expected bytes | `copy_archive_under_test`, generated archive manifests, `archive_reader::open` | Yes | ✓ FLOWING |
| `src/archive.cpp` | `state_->host_path`, `state_->backend_table` | `resolve_host_file_path(host_path)` plus open-time format detection/parser | Yes | ✓ FLOWING |
| `host_file_writer_name_tests.cpp` | writer inventory case matrix | actual checked-in writer source files read by test | Yes | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| Focused Debug host-path/reader/writer proof | `ctest --preset windows-msvc-debug-static --output-on-failure -R "host_path_correctness_boundary|archive_reader_dispatch|host_file|host_file_path"` | 30/30 tests passed, total test time 0.74 sec | ✓ PASS |
| Focused MSVC ASan host-path/reader/writer proof | `ctest --preset windows-msvc-asan-static --output-on-failure -R "host_path_correctness_boundary|archive_reader_dispatch|host_file|host_file_path"` | 30/30 tests passed, total test time 2.06 sec | ✓ PASS |
| Forbidden scope: audit/TES5Edit/public API/dependency files | `git diff --name-only -- .planning/v1.1-MILESTONE-AUDIT.md TES5Edit include/libbsa vcpkg.json vcpkg-configuration.json` and `git diff --name-only HEAD -- ...` | no output before verification rewrite | ✓ PASS |
| Working tree before report rewrite | `git status --short --untracked-files=all` | no output before verification rewrite | ✓ PASS |

### Probe Execution

| Probe | Command | Result | Status |
|---|---|---|---|
| Conventional probes | N/A | No phase-declared probe scripts were identified in the required plan/summary set; this is a CTest/policy-test phase. | SKIPPED |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|---|---|---|---|---|
| SPEC-1 | 17.1-01 | Combined non-ASCII reader-surface regression | ✓ SATISFIED | Matrix test opens copied non-ASCII fixtures and exercises `entries/find/contains/extract_entries`; Debug/ASan passed. |
| SPEC-2 | 17.1-02 | `host_file_path::original_utf8` disposition | ✓ SATISFIED | Member removed; no `original_utf8` under `src`; source-policy tests passed. |
| SPEC-3 | 17.1-02 | `archive_reader::state::backend_identity` disposition without repeated dispatch | ✓ SATISFIED | Stored state removed; public method policy and runtime focused tests passed. |
| SPEC-4 | 17.1-03 | Writer host-path audit disposition | ✓ SATISFIED | Writer inventory matrix and source-policy assertions cover all required families/categories; focused lanes passed. |
| D-01 through D-16 | 17.1-01/02/03 | Locked implementation decisions | ✓ SATISFIED | All decision outcomes are reflected in source/tests/report: removed state, success-path reader matrix, bounded writer inventory, Debug/ASan proof, no audit-source edit. |
| `.planning/REQUIREMENTS.md` traceability | N/A | Phase requirement IDs were not mapped to v1.1 requirements; phase-specific plans use SPEC/D decision IDs. | ✓ SATISFIED | No Phase 17.1 orphaned requirements are listed in `.planning/REQUIREMENTS.md`; the base v1.1 requirement table maps only Phases 13-17. |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---:|---|---|---|
| Modified source/test files | N/A | `TODO`, `FIXME`, `XXX`, placeholder/not-implemented markers | None | Targeted searches found no debt-marker blockers in the modified source/test files. |
| Modified source files | N/A | `return {}` matches from generic scan | Info | Matches are normal `result<void>` success returns and callback completions, not empty implementations or stubs. |

### Human Verification Required

None.

### Gaps Summary

No blocking gaps found. The phase goal is achieved: the audit tech-debt items are closed with committed source/test evidence, the audit source file and TES5Edit boundary were preserved, the dead private state was removed, writer host-path disposition is explicit and policy-tested, and focused Debug plus MSVC ASan verification passed.

---

_Verified: 2026-05-15T05:00:05Z_
_Verifier: the agent (gsd-verifier)_
