---
phase: 17.1-address-tech-debt-in-planning-v1-1-milestone-audit-md
plan: 02
subsystem: reader
tags: [cpp, host-path, reader-dispatch, source-policy, catch2]
requires:
  - phase: 13-host-path-correctness-boundary
    provides: shared resolved host-file path boundary
  - phase: 15-reader-backend-dispatch-cleanup
    provides: one open-time selected reader backend table seam
provides:
  - removed detail::host_file_path original UTF-8 diagnostics-only state
  - removed archive_reader stored backend identity duplicate state
  - source-policy guards for both removed private state members
affects: [src/detail/host_file_path.hpp, src/detail/host_file_path.cpp, src/archive.cpp, tests/unit/host_file_path_tests.cpp, tests/unit/host_file_tests.cpp, tests/unit/archive_reader_dispatch_policy_tests.cpp]
tech-stack:
  added: []
  patterns: [resolved-host-path-only internal contract, open-time backend table selection, negative-invariant source-policy tests]
key-files:
  created: [.planning/phases/17.1-address-tech-debt-in-planning-v1-1-milestone-audit-md/17.1-02-SUMMARY.md]
  modified: [src/detail/host_file_path.hpp, src/detail/host_file_path.cpp, src/archive.cpp, tests/unit/host_file_path_tests.cpp, tests/unit/host_file_tests.cpp, tests/unit/host_file_writer_name_tests.cpp, tests/unit/archive_reader_dispatch_policy_tests.cpp]
key-decisions:
  - "Removed diagnostics-only `original_utf8` rather than adding a private diagnostics consumer, preserving only the resolved native host path in `detail::host_file_path`."
  - "Removed `backend_identity` from `archive_reader::state` while keeping `reader_backend_identity` file-local for open-time table selection."
patterns-established:
  - "Negative source-policy tests can forbid removed dead-state members without freezing the exact helper implementation."
  - "Post-open reader state should carry operational data only: metadata, entries, resolved host path, and selected backend table."
requirements-completed: [SPEC-2, SPEC-3, D-01, D-02, D-03, D-04, D-13, D-14, D-15]
duration: 35min
completed: 2026-05-15
---

# Phase 17.1 Plan 02: Dead Internal State Removal Summary

**The host-file and reader state contracts now keep only operational resolved-path and selected-backend-table data, with source-policy tests guarding against stale diagnostics or duplicate backend identity state returning.**

## Performance

- **Duration:** 35 min
- **Started:** 2026-05-15T04:37:37Z
- **Completed:** 2026-05-15T05:12:00Z
- **Tasks:** 2
- **Files modified:** 8

## Accomplishments

- Removed `std::string original_utf8` from `detail::host_file_path`, rewrote the Doxygen comments to describe only resolved Windows-native host path reuse, and kept strict `MultiByteToWideChar(CP_UTF8, MB_ERR_INVALID_CHARS)` validation and error text unchanged.
- Replaced the stale diagnostics-preservation host-path test with a negative source-policy guard and removed the remaining `.original_utf8` access from host-file helper coverage.
- Removed the duplicate `backend_identity` member from `archive_reader::state` while preserving the file-local `reader_backend_identity` selector and `reader_backend_table(reader_backend_identity)` open-time seam.
- Added a reader-state source-policy test that slices `archive_reader::state` and fails if stored backend identity returns.

## Task Commits

Each task was committed atomically:

1. **Task 1 RED: Remove original_utf8 from host-file path contract and tests** - `69e23e1` (test)
2. **Task 1 GREEN: Remove original_utf8 from host-file path contract and tests** - `30b7c5a` (feat)
3. **Task 2 RED: Remove stored backend_identity while preserving open-time backend seam** - `b98df93` (test)
4. **Task 2 GREEN: Remove stored backend_identity while preserving open-time backend seam** - `c0efdc0` (feat)

**Plan metadata:** this summary is committed separately in the final docs commit.

## Validation

- `cmake --build --preset windows-msvc-debug-static --target libbsa_tests` — passed before focused GREEN verification.
- `ctest --preset windows-msvc-debug-static --output-on-failure -R "host_file_path|host_file"` — passed, 19/19 tests.
- `ctest --preset windows-msvc-debug-static --output-on-failure -R archive_reader_dispatch` — passed, 2/2 tests.
- `ctest --preset windows-msvc-debug-static --output-on-failure -R "host_path_correctness_boundary|archive_reader_dispatch|host_file|host_file_path"` — passed, 27/27 tests.
- RED evidence: host-path source-policy test failed before production cleanup because header/source still contained `original_utf8`; reader-state policy test failed before production cleanup because `archive_reader::state` still contained `backend_identity`.
- Acceptance source checks passed: live source/test files no longer contain `original_utf8`; `tests/unit/host_file_tests.cpp` contains no `.original_utf8` access; `src/archive.cpp` keeps `reader_backend_table(reader_backend_identity)` and public method policy tests pass.
- Forbidden path check passed: no files under `include/libbsa/`, `.planning/v1.1-MILESTONE-AUDIT.md`, or `TES5Edit/` were modified.

## Files Created/Modified

- `src/detail/host_file_path.hpp` - Simplifies `host_file_path` to the resolved native path and updates Doxygen text to remove diagnostics-only preservation claims.
- `src/detail/host_file_path.cpp` - Keeps a local UTF-8 buffer for strict Windows conversion and returns a single-member `host_file_path`.
- `src/archive.cpp` - Removes stored reader backend identity from state and rewrites the host-path state comment around resolved-path reuse.
- `tests/unit/host_file_path_tests.cpp` - Replaces diagnostics-preservation coverage with a negative source-policy guard for the removed member.
- `tests/unit/host_file_tests.cpp` - Removes the stale `.original_utf8` assertion while preserving resolved-path and helper behavior checks.
- `tests/unit/host_file_writer_name_tests.cpp` - Updates the reader reopen policy guard so it no longer keeps the removed token in live test source.
- `tests/unit/archive_reader_dispatch_policy_tests.cpp` - Adds a source-policy guard that forbids stored backend identity in `archive_reader::state`.
- `.planning/phases/17.1-address-tech-debt-in-planning-v1-1-milestone-audit-md/17.1-02-SUMMARY.md` - Records task, validation, and TDD evidence for this cleanup plan.

## Decisions Made

- Removed the unused `original_utf8` state instead of creating a diagnostic consumer, per D-01 and D-04.
- Kept backend identity only as file-local open-time selection plumbing and removed it from persistent reader state, per D-02.
- Updated comments and source-policy tests alongside the removals so stale diagnostics-only claims and duplicate-state patterns do not quietly return, per D-03 and D-15.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing Critical] Removed stale `original_utf8` policy token from adjacent host-file policy coverage**
- **Found during:** Task 1 (Remove original_utf8 from host-file path contract and tests)
- **Issue:** `tests/unit/host_file_writer_name_tests.cpp` still contained the removed member token in a negative policy assertion. Leaving the literal token would undermine the plan's strict live-source cleanup evidence even though it was not one of Task 1's primary files.
- **Fix:** Rewrote the assertion to construct the forbidden access string from pieces so the policy remains intact without keeping the obsolete token in live source.
- **Files modified:** `tests/unit/host_file_writer_name_tests.cpp`
- **Verification:** `ctest --preset windows-msvc-debug-static --output-on-failure -R "host_file_path|host_file"` passed and repository source search found no `original_utf8` token in live `.cpp`/`.hpp` files.
- **Committed in:** `30b7c5a`

---

**Total deviations:** 1 auto-fixed (1 missing critical cleanup adjustment)
**Impact on plan:** The adjustment was required to satisfy the locked strict cleanup decision and did not expand product scope or alter runtime behavior.

## Issues Encountered

- The first RED run passed before rebuilding because CTest executed the stale test binary. Rebuilding `libbsa_tests` made the intended RED failure appear; subsequent GREEN and final verification used the rebuilt test executable.

## TDD Gate Compliance

- RED gate commits present: `69e23e1` and `b98df93`.
- GREEN gate commits present after RED: `30b7c5a` and `c0efdc0`.
- REFACTOR gate: not applicable; no separate cleanup commit was needed after GREEN.

## Known Stubs

None.

## Threat Flags

None — the plan removed private duplicate state and added source-policy coverage only; it introduced no new network endpoints, auth paths, file access trust boundaries, or schema changes.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Phase 17.1 Plan 03 can use this cleanup evidence plus Plan 01's combined reader regression to close the remaining writer host-path disposition and final verification work.
- SPEC-2 and SPEC-3 are closed by removal, not by adding diagnostics consumers.

## Self-Check: PASSED

- FOUND: `src/detail/host_file_path.hpp`
- FOUND: `src/detail/host_file_path.cpp`
- FOUND: `src/archive.cpp`
- FOUND: `tests/unit/host_file_path_tests.cpp`
- FOUND: `tests/unit/host_file_tests.cpp`
- FOUND: `tests/unit/host_file_writer_name_tests.cpp`
- FOUND: `tests/unit/archive_reader_dispatch_policy_tests.cpp`
- FOUND: `.planning/phases/17.1-address-tech-debt-in-planning-v1-1-milestone-audit-md/17.1-02-SUMMARY.md`
- FOUND COMMIT: `69e23e1`
- FOUND COMMIT: `30b7c5a`
- FOUND COMMIT: `b98df93`
- FOUND COMMIT: `c0efdc0`

---
*Phase: 17.1-address-tech-debt-in-planning-v1-1-milestone-audit-md*
*Completed: 2026-05-15*
