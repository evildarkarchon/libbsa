---
phase: 17.1-address-tech-debt-in-planning-v1-1-milestone-audit-md
plan: 01
subsystem: testing
tags: [catch2, ctest, host-path, reader-dispatch, non-ascii]
requires:
  - phase: 13-host-path-correctness-boundary
    provides: non-ASCII Windows host-path reader boundary helpers and fixture-copy patterns
  - phase: 15-reader-backend-dispatch-cleanup
    provides: representative reader dispatch matrix and bulk extraction sink pattern
provides:
  - combined non-ASCII host-path and public reader-surface regression
  - TES3-inclusive representative host-path reader matrix coverage
affects: [tests/unit/host_path_correctness_boundary_tests.cpp, phase-17.1-verification]
tech-stack:
  added: []
  patterns: [Catch2 fixture matrix, manifest-backed extraction assertions, concrete bulk_extract_sink_factory capture]
key-files:
  created: [.planning/phases/17.1-address-tech-debt-in-planning-v1-1-milestone-audit-md/17.1-01-SUMMARY.md]
  modified: [tests/unit/host_path_correctness_boundary_tests.cpp]
key-decisions:
  - "Extended the existing host-path suite instead of adding a new CTest source so the Phase 13 and Phase 15 seam proof remains colocated."
  - "Included TES3 in the representative copied-archive matrix and updated the suite policy to allow that fixture only for the combined reader regression."
patterns-established:
  - "Host-path reader regressions should copy fixtures under the locked non-ASCII token before invoking public archive_reader APIs."
  - "Bulk extraction payload assertions use a concrete sink factory that records bytes by archive path without mocking frameworks."
requirements-completed: [SPEC-1, D-05, D-06, D-07, D-08, D-13, D-14]
duration: 18min
completed: 2026-05-15
---

# Phase 17.1 Plan 01: Combined Non-ASCII Reader-Surface Regression Summary

**Manifest-backed reader matrix now proves copied non-ASCII Windows host paths exercise `entries`, `find`, `contains`, and `extract_entries` through the public API.**

## Performance

- **Duration:** 18 min
- **Started:** 2026-05-15T04:32:31Z
- **Completed:** 2026-05-15T04:50:00Z
- **Tasks:** 1
- **Files modified:** 2

## Accomplishments

- Added a focused Catch2 regression in `tests/unit/host_path_correctness_boundary_tests.cpp` that copies each representative archive under `libbsa-Ångström-日本語`, opens it with `archive_reader::open(utf8_string_from_path(copied_archive))`, and exercises the required reader surface.
- Adapted the concrete `bulk_extract_sink_factory` capture pattern so `extract_entries` payload bytes are asserted against manifest-derived expected bytes from the non-ASCII copied archive path.
- Expanded the host-path representative matrix to include the TES3 fixture while keeping the new regression success-path focused and avoiding missing-path or invalid-path dispatch loops.

## Task Commits

Each task was committed atomically:

1. **Task 1: Add combined non-ASCII reader-surface matrix regression** - `3bee64b` (test)

**Plan metadata:** committed separately in the final docs commit for this summary and state update.

## Validation

- `cmake --build --preset windows-msvc-debug-static --target libbsa_tests` — passed.
- `ctest --preset windows-msvc-debug-static --output-on-failure -R host_path_correctness_boundary` — passed, 6/6 tests.
- `ctest --preset windows-msvc-debug-static --output-on-failure -R "host_path_correctness_boundary|archive_reader_dispatch|host_file|host_file_path"` — passed, 26/26 tests.
- Acceptance source checks passed: the new test name includes `non-ASCII` and `reader`; the body calls `.entries(`, `.find(`, `.contains(`, and `.extract_entries(`; it uses `copy_archive_under_test`; and it opens via `archive_reader::open(utf8_string_from_path(copied_archive))`.
- Forbidden path check passed: no modifications under `include/libbsa/`, `.planning/v1.1-MILESTONE-AUDIT.md`, or `TES5Edit/`.

## Files Created/Modified

- `tests/unit/host_path_correctness_boundary_tests.cpp` - Adds the combined non-ASCII host-path plus reader dispatch surface regression, TES3 matrix coverage, sorted manifest path assertion helper, and recording bulk-extract sink factory.
- `.planning/phases/17.1-address-tech-debt-in-planning-v1-1-milestone-audit-md/17.1-01-SUMMARY.md` - Records execution evidence and closure context for this plan.

## Decisions Made

- Reused the existing host-path boundary suite so the regression shares the non-ASCII path token, fixture-copy helper, UTF-8 path conversion, and manifest byte helpers.
- Kept the combined regression to the SPEC-required success surface only: entries, canonical find/contains, and `extract_entries` bytes.
- Allowed TES3 in the host-path suite matrix to match Phase 15 representative reader-family coverage where practical.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Updated the smoke setup after adding TES3 to the matrix**
- **Found during:** Task 1 validation
- **Issue:** The existing smoke setup used the first representative archive and assumed the canonical manifest entry had non-empty `expected.bytes_hex`; after adding TES3 as the first matrix case, that smoke assertion became a false negative.
- **Fix:** Pointed the smoke setup at the TES4 v103 case while leaving the new combined reader matrix to cover TES3 and the other representative families.
- **Files modified:** `tests/unit/host_path_correctness_boundary_tests.cpp`
- **Verification:** `ctest --preset windows-msvc-debug-static --output-on-failure -R host_path_correctness_boundary` passed after the fix.
- **Committed in:** `3bee64b`

---

**Total deviations:** 1 auto-fixed (1 bug)
**Impact on plan:** The fix removed a test-suite false negative caused by the intended TES3 matrix expansion. It did not expand product scope or add negative dispatch coverage.

## Issues Encountered

- TDD RED/GREEN was not meaningfully separable because the requested work is a regression for already-implemented public reader behavior. The added regression passed once compiled; only the pre-existing smoke assumption failed after the TES3 matrix expansion and was fixed in the task commit.

## TDD Gate Compliance

- RED gate commit present: `3bee64b` (`test(17.1-01): add non-ASCII reader surface regression`).
- GREEN gate commit absent: no production implementation was required because this plan adds coverage for existing behavior rather than changing library code.
- REFACTOR gate: not applicable.

## Known Stubs

None.

## Threat Flags

None — the plan added test coverage only and introduced no new runtime network endpoints, auth paths, file access behavior, or schema trust boundaries.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Phase 17.1 Plan 02 can remove or disposition unused host-path and reader state with this combined reader/host-path runtime proof in place.
- The focused Debug evidence requested by D-13 and D-14 is recorded; ASan evidence remains for the later verification plan as scoped.

## Self-Check: PASSED

- FOUND: `tests/unit/host_path_correctness_boundary_tests.cpp`
- FOUND: `.planning/phases/17.1-address-tech-debt-in-planning-v1-1-milestone-audit-md/17.1-01-SUMMARY.md`
- FOUND COMMIT: `3bee64b`

---
*Phase: 17.1-address-tech-debt-in-planning-v1-1-milestone-audit-md*
*Completed: 2026-05-15*
