---
phase: 14-verification-lane-truthfulness
plan: 02
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - .github/workflows/ci.yml
  - tests/unit/validation_policy_tests.cpp
autonomous: true
requirements:
  - VER-01
  - VER-02
  - VER-03
must_haves:
  truths:
    - "CI runs one main Windows matrix for debug and release lanes plus one separate parallel ASan hardening job per D-21 through D-24."
    - "Main-matrix jobs and the ASan job invoke the same checked-in presets maintainers use locally."
    - "CI job names expose both the lane role and the concrete preset so the supported matrix is obvious in workflow output."
  artifacts:
    - "`.github/workflows/ci.yml` contains debug static/shared plus release static/shared in one matrix with `fail-fast: false`."
    - "`.github/workflows/ci.yml` contains a separate `windows-msvc-asan-static` hardening job that runs in parallel with the main matrix."
  key_links:
    - "workflow matrix preset -> `cmake --preset` / `cmake --build --preset` / `ctest --preset`"
    - "separate ASan job -> `windows-msvc-asan-static`"
---

<objective>
Make CI truthful about the supported verification matrix by executing the same checked-in debug, release, and ASan lanes the repo now supports locally, with the exact role split locked in Context.

Purpose: Implement D-02 and D-21 through D-24 so CI stops understating or misnaming the supported verification contract.
Output: A role-aware GitHub Actions workflow with one debug/release matrix and one separate ASan hardening job.
</objective>

<execution_context>
@J:/libbsa/.opencode/get-shit-done/workflows/execute-plan.md
@J:/libbsa/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-verification-lane-truthfulness/14-CONTEXT.md
@.planning/phases/14-verification-lane-truthfulness/14-SPEC.md
@.planning/phases/14-verification-lane-truthfulness/14-RESEARCH.md
@.planning/phases/14-verification-lane-truthfulness/14-PATTERNS.md
@.planning/phases/14-verification-lane-truthfulness/14-01-PLAN.md
@.github/workflows/ci.yml
@CMakePresets.json
@tests/unit/validation_policy_tests.cpp

<interfaces>
Supported presets from Plan 01:
- `windows-msvc-debug-static`
- `windows-msvc-debug-shared`
- `windows-msvc-release-static`
- `windows-msvc-release-shared`
- `windows-msvc-asan-static`

Existing workflow step sequence to preserve:
- checkout with submodules
- install CMake 4.3.2
- verify CMake version
- configure / build / test via preset
- verify `TES5Edit` stayed read-only

Mapped analogs from `14-PATTERNS.md` to follow directly:
- `.github/workflows/ci.yml`: preserve the existing matrix step sequence and `fail-fast: false` pattern while expanding lane rows.
- `tests/unit/validation_policy_tests.cpp`: keep repo-reading, independent workflow assertions tied to the shared contract helper from Plan 01.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand the main Windows matrix and lock its topology in policy tests</name>
  <files>.github/workflows/ci.yml, tests/unit/validation_policy_tests.cpp</files>
  <read_first>
    - .github/workflows/ci.yml
    - CMakePresets.json
    - .planning/phases/14-verification-lane-truthfulness/14-CONTEXT.md
    - .planning/phases/14-verification-lane-truthfulness/14-SPEC.md
    - tests/unit/validation_policy_tests.cpp
  </read_first>
  <action>Update the `windows-msvc` matrix job so it covers `windows-msvc-debug-static`, `windows-msvc-debug-shared`, `windows-msvc-release-static`, and `windows-msvc-release-shared` per D-21, following the existing matrix and step-sequence analogs mapped in `14-PATTERNS.md`. Keep `fail-fast: false` per D-22, keep the current checkout/CMake install/CMake verify sequence, and keep the job using `cmake --preset`, `cmake --build --preset`, and `ctest --preset` so CI exactly mirrors the supported local commands. Rename the job output so each row shows both role and preset, not only one or the other, per D-24. In `tests/unit/validation_policy_tests.cpp`, extend the shared verification-matrix helper from 14-01 with deterministic workflow-topology assertions that read `.github/workflows/ci.yml` directly and prove the main matrix contains exactly the four debug/release presets, retains `fail-fast: false`, and runs the preset triad through `matrix.preset` instead of ad hoc commands.</action>
  <verify>
    <automated>cmake --build --preset windows-msvc-debug-static --target libbsa_tests &amp;&amp; ctest --preset windows-msvc-debug-static -R "validation_policy" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - The main workflow matrix lists all four debug/release presets.
    - `fail-fast: false` remains present.
    - The job name exposes role plus concrete preset.
    - `validation_policy_tests.cpp` proves the matrix topology and preset-command wiring structurally, not just by token presence.
  </acceptance_criteria>
  <done>The main Windows CI matrix truthfully covers the supported debug and release lanes.</done>
</task>

<task type="auto">
  <name>Task 2: Add the separate parallel MSVC AddressSanitizer hardening job</name>
  <files>.github/workflows/ci.yml, tests/unit/validation_policy_tests.cpp</files>
  <read_first>
    - .github/workflows/ci.yml
    - CMakePresets.json
    - .planning/phases/14-verification-lane-truthfulness/14-CONTEXT.md
    - .planning/phases/14-verification-lane-truthfulness/14-VALIDATION.md
    - tests/unit/validation_policy_tests.cpp
  </read_first>
  <action>Add a dedicated ASan job that uses `windows-msvc-asan-static` and runs in parallel with the main matrix per D-23, following the same workflow step-sequence analog mapped in `14-PATTERNS.md` instead of inventing a second topology. Name it explicitly as the MSVC AddressSanitizer hardening lane per D-01 and D-02, preserve the existing CMake 4.3.2 and TES5Edit read-only checks, and keep it separate from the main matrix rather than adding a fifth row. In `tests/unit/validation_policy_tests.cpp`, finish the workflow-topology assertions so they also prove the ASan job is a distinct job, uses the exact `windows-msvc-asan-static` preset triad, and is not nested under the main matrix. Do not add Linux, WSL, or extra sanitizer jobs.</action>
  <verify>
    <automated>cmake --build --preset windows-msvc-debug-static --target libbsa_tests &amp;&amp; ctest --preset windows-msvc-debug-static -R "validation_policy" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` defines a separate ASan job instead of a matrix row.
    - The ASan job uses `windows-msvc-asan-static` for configure, build, and test.
    - The ASan job keeps the same read-only TES5Edit guard and Windows runner boundary.
    - `validation_policy_tests.cpp` proves the ASan hardening job stays separate from the main matrix and preserves the supported topology facts.
    - No out-of-scope sanitizer or non-Windows jobs are added.
  </acceptance_criteria>
  <done>The workflow contains a separate parallel MSVC AddressSanitizer hardening job that matches the supported local lane.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workflow YAML -> GitHub-hosted execution | stale or misnamed jobs can misrepresent supported verification coverage |
| CI preset invocation -> local maintainer contract | CI can drift if it stops using the same preset names maintainers run locally |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-04 | T/R | `.github/workflows/ci.yml` main matrix | mitigate | Expand the matrix to the exact debug/release preset set with role-aware names and `fail-fast: false` per D-21, D-22, and D-24. |
| T-14-05 | R/I | `.github/workflows/ci.yml` ASan job | mitigate | Keep ASan as a separate named hardening job using `windows-msvc-asan-static` so the workflow cannot imply hardening coverage without executing the real preset per D-01, D-02, and D-23. |
</threat_model>

<verification>
- Source assertion: `rg -n "windows-msvc-release-static|windows-msvc-release-shared|windows-msvc-asan-static|fail-fast: false|AddressSanitizer hardening" .github/workflows/ci.yml`
- Manual-only GitHub execution from `14-VALIDATION.md`: push or open a PR and confirm one debug/release matrix plus one separate ASan job all pass.
</verification>

<success_criteria>
- CI runs the same supported presets maintainers run locally.
- Release lanes live in the main matrix and ASan lives in its own parallel hardening job.
- Workflow names expose lane role and preset clearly.
</success_criteria>

<output>
After completion, create `.planning/phases/14-verification-lane-truthfulness/14-02-SUMMARY.md`
</output>
