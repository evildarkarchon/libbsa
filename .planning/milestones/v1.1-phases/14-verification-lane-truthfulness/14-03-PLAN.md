---
phase: 14-verification-lane-truthfulness
plan: 03
type: execute
wave: 3
depends_on:
  - 14-01
  - 14-02
files_modified:
  - README.md
  - tests/fixtures/README.md
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - tests/unit/validation_policy_tests.cpp
autonomous: true
requirements:
  - VER-03
must_haves:
  truths:
    - "README presents `windows-msvc-debug-static` as the quick path and groups the rest of the supported lanes by role per D-09 through D-12."
    - "Fixture policy, README, `.planning`, presets, and CI all describe the same Windows-only supported matrix and keep `requires-game-fixture` opt-in per D-13 through D-20."
    - "Policy tests independently validate docs, workflow, presets, and planning summaries against the shared contract so one stale file cannot validate another."
  artifacts:
    - "README.md documents the quick path, Debug inner-loop lanes, Release package-proof lanes, and the MSVC AddressSanitizer hardening lane."
    - "tests/fixtures/README.md preserves Windows-only scope and opt-in `requires-game-fixture` rules while updating the supported matrix facts."
    - "`.planning/PROJECT.md`, `.planning/ROADMAP.md`, and `.planning/STATE.md` contain role-aware summaries at the locked detail level."
    - "tests/unit/validation_policy_tests.cpp` checks README, fixture policy, workflow, presets, and planning summaries independently against the shared matrix contract."
  key_links:
    - "shared contract helper -> README / fixture / workflow / presets / planning assertions"
    - "README quick path -> `windows-msvc-debug-static`"
    - "planning summaries -> v1.1 Phase 14 outcome, not rewritten v1.0 history"
---

<objective>
Align maintainer docs and planning summaries with the now-real verification matrix, then close the truthfulness loop by making docs/planning drift a machine-checked failure.

Purpose: Implement D-09 through D-20 and complete VER-03 without moving detailed lane-contract prose out of `14-CONTEXT.md`.
Output: Updated README / fixture / planning summaries and final verification-matrix policy coverage over every named surface.
</objective>

<execution_context>
@J:/libbsa/.opencode/get-shit-done/workflows/execute-plan.md
@J:/libbsa/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-verification-lane-truthfulness/14-CONTEXT.md
@.planning/phases/14-verification-lane-truthfulness/14-SPEC.md
@.planning/phases/14-verification-lane-truthfulness/14-RESEARCH.md
@.planning/phases/14-verification-lane-truthfulness/14-VALIDATION.md
@.planning/phases/14-verification-lane-truthfulness/14-PATTERNS.md
@.planning/phases/13-host-path-correctness-boundary/13-CONTEXT.md
@.planning/phases/13-host-path-correctness-boundary/13-VERIFICATION.md
@README.md
@tests/fixtures/README.md
@tests/unit/validation_policy_tests.cpp
@.github/workflows/ci.yml
@CMakePresets.json

<interfaces>
Locked communication rules to apply verbatim in intent:
- Quick path: `windows-msvc-debug-static` per D-10.
- Role groups: Debug inner-loop lanes, Release package-proof lanes, MSVC AddressSanitizer hardening lane per D-11.
- Required exact facts per D-13: lane names, lane roles, release smoke ownership, Windows-only scope, opt-in local-corpus rule.
- Planning layering per D-17 through D-20: `PROJECT.md` and `ROADMAP.md` summarize, `STATE.md` stays terse, `14-CONTEXT.md` stays the detailed contract.

Mapped analogs from `14-PATTERNS.md` to follow directly:
- `README.md`: concrete quick-path command block first, then concise supported-lane summary.
- `tests/fixtures/README.md`: preserve Windows-only and opt-in `requires-game-fixture` language while updating supported-lane facts.
- `.planning/PROJECT.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`: keep summary-level planning layering rather than moving detailed lane commands into project-wide files.
- `tests/unit/validation_policy_tests.cpp`: use the shared contract helper to validate each surface independently.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update maintainer-facing docs to the quick-path plus role-matrix contract</name>
  <files>README.md, tests/fixtures/README.md</files>
  <read_first>
    - README.md
    - tests/fixtures/README.md
    - .planning/phases/14-verification-lane-truthfulness/14-CONTEXT.md
    - CMakePresets.json
    - .github/workflows/ci.yml
  </read_first>
  <action>Rewrite the supported-lane sections in `README.md` and `tests/fixtures/README.md` to match D-09 through D-16 exactly, following the quick-path-first and fixture-policy analogs mapped in `14-PATTERNS.md`. Keep `windows-msvc-debug-static` as the explicit quick path per D-10. After that, group the supported lanes by role: debug inner-loop lanes, release package-proof lanes, and the MSVC AddressSanitizer hardening lane. State that release lanes own install/export plus downstream package-consumer smoke per D-05 through D-08, and preserve the Windows-only plus opt-in `requires-game-fixture` / local corpus rule per D-16. Do not claim Linux, WSL, or extra sanitizer support.</action>
  <verify>
    <automated>rg -n "windows-msvc-debug-static|windows-msvc-debug-shared|windows-msvc-release-static|windows-msvc-release-shared|windows-msvc-asan-static|AddressSanitizer hardening|package_consumer_smoke|requires-game-fixture|Windows-only" README.md tests/fixtures/README.md</automated>
  </verify>
  <acceptance_criteria>
    - README keeps a concrete quick-path command block for `windows-msvc-debug-static`.
    - README and fixture docs both name the same supported lane set and role split.
    - Fixture docs still say local corpus checks are opt-in and skipped by default.
    - Neither file reopens non-Windows or extra sanitizer scope.
  </acceptance_criteria>
  <done>Maintainer docs are role-aware, Windows-only, and aligned with the actual supported matrix.</done>
</task>

<task type="auto">
  <name>Task 2: Update planning summaries without moving detailed contract prose out of the phase context</name>
  <files>.planning/PROJECT.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - .planning/PROJECT.md
    - .planning/ROADMAP.md
    - .planning/STATE.md
    - .planning/phases/14-verification-lane-truthfulness/14-CONTEXT.md
    - .planning/phases/14-verification-lane-truthfulness/14-SPEC.md
    - .planning/phases/13-host-path-correctness-boundary/13-VERIFICATION.md
  </read_first>
  <action>Update the planning summaries per D-17 through D-20, following the summary-layering analogs mapped in `14-PATTERNS.md`. In `.planning/PROJECT.md`, correct the chronology so v1.0 stays historically accurate and the truthful Release/ASan verification matrix is clearly described as a v1.1 Phase 14 outcome per D-19. In `.planning/ROADMAP.md`, keep the existing Phase 14 plan count and plan bullets intact, and update only the real remaining summary-level delta: role-aware Phase 14 wording or progress text that still understates the supported Release/ASan matrix. In `.planning/STATE.md`, keep the note terse and session-oriented; replace the stale repo-dirty emphasis with a short note that Phase 14 is about the truthful supported matrix, without copying full lane detail from `14-CONTEXT.md`.</action>
  <verify>
    <automated>rg -n "Phase 14|verification matrix|windows-msvc-release-static|windows-msvc-release-shared|windows-msvc-asan-static|v1\.1" .planning/PROJECT.md .planning/ROADMAP.md .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `PROJECT.md` treats the truthful supported matrix as a Phase 14 hardening outcome, not as shipped v1.0 history.
    - `ROADMAP.md` preserves the existing actual Phase 14 plan list while updating only the remaining summary-level matrix wording.
    - `STATE.md` stays concise and does not duplicate full lane-by-lane contract detail.
  </acceptance_criteria>
  <done>Planning surfaces are truthful, summary-scoped, and layered correctly.</done>
</task>

<task type="auto">
  <name>Task 3: Finalize independent truthfulness checks across docs and planning surfaces</name>
  <files>tests/unit/validation_policy_tests.cpp</files>
  <read_first>
    - tests/unit/validation_policy_tests.cpp
    - README.md
    - tests/fixtures/README.md
    - .planning/PROJECT.md
    - .planning/ROADMAP.md
    - .planning/STATE.md
    - .github/workflows/ci.yml
    - CMakePresets.json
    - .planning/phases/14-verification-lane-truthfulness/14-CONTEXT.md
  </read_first>
  <action>Finish the shared verification-matrix policy section so each remaining listed surface is checked independently against the same contract per D-27, using the repo-reading helper and independent-surface assertion patterns mapped in `14-PATTERNS.md`. Preserve the workflow and preset topology assertions landed in 14-01 and 14-02, then add or tighten README, fixture policy, and planning-summary assertions for exact lane names, quick-path debug role per D-10, release package-proof ownership per D-05 through D-08, the ASan hardening-lane name per D-01 through D-04, Windows-only scope, and opt-in local corpus rules per D-16. Keep the checks fact-based and tolerant of prose style variation per D-13; do not make one surface validate another by copying text blindly.</action>
  <verify>
    <automated>cmake --build --preset windows-msvc-debug-static --target libbsa_tests &amp;&amp; ctest --preset windows-msvc-debug-static -R "validation_policy" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `validation_policy_tests.cpp` checks README, fixture policy, workflow, presets, and planning summaries independently against the shared contract.
    - Focused `validation_policy` execution passes from the debug quick path.
    - Future drift in README, fixture docs, presets, workflow, or planning summaries would fail the policy suite.
  </acceptance_criteria>
  <done>The repo has one machine-checked truthfulness gate over docs, workflow, presets, and planning summaries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer docs / planning summaries -> human expectations | stale prose can create false confidence even when build surfaces changed |
| policy tests -> repo text surfaces | weak or coupled assertions can let one stale file validate another stale file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-06 | T/R | README / fixture / planning surfaces | mitigate | Update every named surface in the same phase and keep exact lane facts, Windows-only scope, and opt-in corpus wording aligned per D-13 through D-20. |
| T-14-07 | T/R | `tests/unit/validation_policy_tests.cpp` | mitigate | Check each surface independently against the shared contract helper so stale prose in one file cannot validate another per D-27. |
</threat_model>

<verification>
- Focused truthfulness gate: `cmake --build --preset windows-msvc-debug-static --target libbsa_tests && ctest --preset windows-msvc-debug-static -R "validation_policy" --output-on-failure`
- Source assertions: `rg -n "windows-msvc-debug-static|windows-msvc-release-static|windows-msvc-release-shared|windows-msvc-asan-static|AddressSanitizer hardening|requires-game-fixture|Windows-only" README.md tests/fixtures/README.md .planning/PROJECT.md .planning/ROADMAP.md .planning/STATE.md`
</verification>

<success_criteria>
- README uses quick path plus role-grouped matrix wording.
- Fixture docs keep Windows-only and opt-in local corpus rules while naming the new supported lanes.
- `PROJECT.md`, `ROADMAP.md`, and `STATE.md` stay layered and truthful.
- `validation_policy` independently checks every named surface against the shared contract.
</success_criteria>

<output>
After completion, create `.planning/phases/14-verification-lane-truthfulness/14-03-SUMMARY.md`
</output>
