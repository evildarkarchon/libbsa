---
phase: 13-host-path-correctness-boundary
plan: 05
type: execute
wave: 5
depends_on:
  - 13-04
files_modified:
  - tests/unit/host_path_correctness_boundary_tests.cpp
  - tests/CMakeLists.txt
autonomous: true
requirements:
  - HOST-01
  - HOST-02
  - HOST-03
must_haves:
  truths:
    - "Consumers can open the locked representative TES4 BSA and BA2 archives from Windows host paths containing non-ASCII directory and filename segments."
    - "Consumers can validate those same archives with `validate_entry_extractability = true` from those non-ASCII paths."
    - "Each representative archive can perform one canonical manifest-backed extraction after the non-ASCII-path open succeeds."
    - "Maintainers have one dedicated deterministic cross-family regression suite proving the whole Phase 13 host-path story from the public API."
  artifacts:
    - "tests/unit/host_path_correctness_boundary_tests.cpp contains the dedicated cross-family regression suite."
    - "tests/CMakeLists.txt` registers the suite for the `host_path_correctness_boundary` quick-run filter and smoke subset."
    - "The suite uses the exact token `libbsa-Ångström-日本語` in both the temp directory and copied archive filename."
  key_links:
    - "copied non-ASCII archive path -> archive_reader::open"
    - "copied non-ASCII archive path -> validate_archive(..., {.validate_entry_extractability = true})"
    - "manifest-backed canonical entry -> extract_bytes / sink assertions"
---

<objective>
Land the locked black-box proof: one dedicated deterministic cross-family regression suite that demonstrates non-ASCII Windows host-path correctness for open, validation, and one canonical extraction per representative archive.

Purpose: Implement D-21 through D-27 and close HOST-01, HOST-02, and HOST-03 with public-API evidence.
Output: `host_path_correctness_boundary_tests.cpp` plus CMake registration and focused quick-run coverage.
</objective>

<execution_context>
@J:/libbsa/.opencode/get-shit-done/workflows/execute-plan.md
@J:/libbsa/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-host-path-correctness-boundary/13-CONTEXT.md
@.planning/phases/13-host-path-correctness-boundary/13-SPEC.md
@.planning/phases/13-host-path-correctness-boundary/13-RESEARCH.md
@.planning/phases/13-host-path-correctness-boundary/13-PATTERNS.md
@.planning/phases/13-host-path-correctness-boundary/13-VALIDATION.md
@.planning/codebase/TESTING.md

<interfaces>
Test patterns to reuse:
- `archive_reader::open(copied_path.string())`
- `validate_archive(copied_path.string(), {.validate_entry_extractability = true})`
- `opened.value().extract_bytes(expected_path)` or `opened.value().extract(expected_path, sink)`
- existing manifest-backed JSON helpers from `tests/unit/validation_api_tests.cpp`, `tests/unit/tes4_bsa_reader_tests.cpp`, `tests/unit/ba2_gnrl_reader_tests.cpp`, and `tests/unit/ba2_dx10_extraction_tests.cpp`

Locked fixture scope:
- TES4 BSA fixtures: v103, v104, v105
- Fallout 4 BA2 GNRL
- Fallout 4 BA2 DX10
- Starfield BA2 GNRL representative archive `ba2_gnrl_sfv3.ba2`

Locked path token: `libbsa-Ångström-日本語` must appear in both the parent directory and copied archive filename.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the dedicated cross-family non-ASCII host-path suite and register its focused filters</name>
  <files>tests/unit/host_path_correctness_boundary_tests.cpp, tests/CMakeLists.txt</files>
  <behavior>
    - Test 1: CTest discovery lists the new suite through the `host_path_correctness_boundary` selector.
    - Test 2: the exact token `libbsa-Ångström-日本語` is used in both directory and filename helpers.
    - Test 3: the suite stays black-box and uses committed fixture manifests rather than copied expected payload trees.
  </behavior>
  <action>Per D-21, D-23, D-24, D-25, and D-27, create `tests/unit/host_path_correctness_boundary_tests.cpp` and register it in `tests/CMakeLists.txt`. Keep the suite deterministic and black-box. Copy only the archive-under-test into a temp directory whose parent directory name contains `libbsa-Ångström-日本語`, rename the copied archive file so its filename also contains the same token, and add file-local helpers for temp setup, archive copy, manifest loading, canonical entry lookup, and expected-byte materialization. Also register the suite so both the broad `host_path_correctness_boundary` filter and the smoke subset from `13-VALIDATION.md` remain runnable.</action>
  <verify>
    <automated>cmake --build --preset windows-msvc-debug-static --target libbsa_tests &amp;&amp; ctest --preset windows-msvc-debug-static -N -R "host_path_correctness_boundary"</automated>
  </verify>
  <done>The dedicated Phase 13 suite exists, is discoverable by the focused CTest selectors, and encodes the locked non-ASCII directory+filename setup.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cover the full locked representative matrix for open, validate, and one canonical extraction each</name>
  <files>tests/unit/host_path_correctness_boundary_tests.cpp</files>
  <behavior>
    - Test 1: `archive_reader::open` succeeds for TES4 v103/v104/v105, FO4 BA2 GNRL, FO4 BA2 DX10, and Starfield `ba2_gnrl_sfv3.ba2` from non-ASCII directory+filename paths.
    - Test 2: `validate_archive(..., {.validate_entry_extractability = true})` succeeds for the same representative set.
    - Test 3: one canonical manifest-backed extraction per representative archive returns the expected bytes.
  </behavior>
  <action>Per D-22, D-26, and D-27, implement the locked public proof exactly: TES4 v103/v104/v105, Fallout 4 BA2 GNRL, Fallout 4 BA2 DX10, and the fixed Starfield BA2 GNRL representative `ba2_gnrl_sfv3.ba2`. For each representative archive, prove `archive_reader::open` succeeds from the non-ASCII directory+filename path, prove `validate_archive(non_ascii_path, {.validate_entry_extractability = true})` succeeds, and prove one canonical manifest-backed extraction target returns the expected bytes. Do not substitute a different Starfield fixture or add executor-time discretion.</action>
  <verify>
    <automated>cmake --build --preset windows-msvc-debug-static --target libbsa_tests &amp;&amp; ctest --preset windows-msvc-debug-static -R "host_path_correctness_boundary" --output-on-failure</automated>
  </verify>
  <done>The locked representative matrix is covered end-to-end from the public API with deterministic manifest-backed expectations.</done>
</task>

<task type="auto">
  <name>Task 3: Keep the suite phase-scoped, deterministic, and aligned with the validation contract</name>
  <files>tests/unit/host_path_correctness_boundary_tests.cpp, tests/CMakeLists.txt</files>
  <action>Finalize the suite so it proves only the public behavior required by Phase 13. Do not add white-box hooks, internal open-count assertions, writer-side host-path coverage, TES3 non-ASCII regression requirements, archive-internal path behavior checks, or Phase 14 verification-lane work. Keep concise WHY comments where the non-ASCII temp naming or manifest-only copy rule is non-obvious, and mention any rewritten comments in the execution summary.</action>
  <verify>
    <automated>cmake --build --preset windows-msvc-debug-static --target libbsa_tests &amp;&amp; ctest --preset windows-msvc-debug-static -R "host_path_correctness_boundary|host_path_correctness_boundary_smoke" --output-on-failure</automated>
  </verify>
  <done>The Phase 13 regression suite is deterministic, black-box, phase-scoped, and runnable through the documented focused CTest filters.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated fixture archive copy → non-ASCII temp path | the test recreates the Windows host-path boundary here |
| public API result/report/extraction output → assertions | false assurance risk exists if the suite misses locked behaviors |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-05-01 | R | `tests/unit/host_path_correctness_boundary_tests.cpp` | mitigate | Use the exact deterministic token in both parent directory and filename per D-24 and D-25 so the suite cannot pass while missing half the host-path boundary. |
| T-13-05-02 | R | focused regression coverage | mitigate | Keep the suite black-box and require public `archive_reader::open`, `validate_archive(..., {.validate_entry_extractability = true})`, and canonical extraction checks per D-27 so the evidence matches consumer-visible behavior. |
| T-13-05-03 | T | fixture handling | mitigate | Copy only the archive-under-test into the non-ASCII temp location and use committed manifest-backed expected bytes per D-23 and D-26 so expected data is not duplicated or drift-prone. |
</threat_model>

<verification>
- Focused smoke gate: `cmake --build --preset windows-msvc-debug-static --target libbsa_tests && ctest --preset windows-msvc-debug-static -R "host_path_correctness_boundary|host_path_correctness_boundary_smoke" --output-on-failure`
- Full dedicated suite gate: `ctest --preset windows-msvc-debug-static -R "host_path_correctness_boundary" --output-on-failure`
</verification>

<success_criteria>
- The dedicated suite proves non-ASCII directory and filename behavior for the locked representative archive set.
- Validation with `validate_entry_extractability = true` succeeds from those copied paths.
- One canonical extraction per representative archive matches committed expected bytes.
</success_criteria>

<output>
After completion, create `.planning/phases/13-host-path-correctness-boundary/13-05-SUMMARY.md`
</output>
