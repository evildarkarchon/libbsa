---
phase: 13-host-path-correctness-boundary
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/detail/host_file.hpp
  - src/detail/host_file.cpp
  - src/formats/bsa/tes4_bsa_prepare.cpp
  - src/formats/bsa/tes4_bsa_layout.cpp
  - src/formats/ba2/ba2_gnrl_prepare.cpp
  - src/formats/ba2/ba2_dx10_prepare.cpp
  - CMakeLists.txt
  - tests/CMakeLists.txt
  - tests/unit/host_file_tests.cpp
autonomous: true
requirements:
  - HOST-01
must_haves:
  truths:
    - "Existing disk-backed writer flows still read host files successfully after the helper rename, so Phase 13 hardening starts from unchanged real file-I/O behavior instead of rename fallout."
    - "Missing, changed, and allocation-related host-file failures still surface the same caller-owned diagnostics after the rename, preserving the observable error contract on the already-migrated writer paths."
    - "Focused helper and writer regression gates prove the renamed shared boundary is a safe base for the later non-ASCII open and validation migration work."
  artifacts:
    - "src/detail/host_file.hpp and src/detail/host_file.cpp define the renamed shared host-file helper family."
    - "tests/unit/host_file_tests.cpp proves the renamed helper preserves the prior exact/prefix/chunk/error behavior matrix."
    - "Writer prepare/layout files include and call `host_file` helper names rather than `writer_disk_source` names."
  key_links:
    - "tes4_bsa_prepare.cpp -> detail::host_file_* helpers"
    - "tes4_bsa_layout.cpp -> detail::host_file_* helpers"
    - "ba2_gnrl_prepare.cpp / ba2_dx10_prepare.cpp -> detail::host_file_* helpers"
---

<objective>
Rename and generalize the existing writer-only helper into the neutral shared `host_file` seam while keeping the current writer paths buildable and test-backed.

Purpose: Satisfy D-01 through D-06 before the shared internal path contract is introduced.
Output: Renamed `host_file` helper files, migrated helper tests/build registration, and writer call sites compiled against the neutral helper names.
</objective>

<execution_context>
@J:/libbsa/.opencode/get-shit-done/workflows/execute-plan.md
@J:/libbsa/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-host-path-correctness-boundary/13-CONTEXT.md
@.planning/phases/13-host-path-correctness-boundary/13-SPEC.md
@.planning/phases/13-host-path-correctness-boundary/13-RESEARCH.md
@.planning/phases/13-host-path-correctness-boundary/13-PATTERNS.md
@.planning/phases/13-host-path-correctness-boundary/13-VALIDATION.md

<interfaces>
From the existing helper family and patterns map:
- `struct writer_disk_source_context { open_error, inspect_error, read_error, changed_error, allocation_description }`
- `result<std::ifstream> open_disk_source(...)`
- `result<std::uint64_t> inspect_disk_source_size(...)`
- `result<std::vector<std::byte>> read_disk_source_prefix(...)`
- `result<std::vector<std::byte>> read_disk_source_exact(...)`
- `result<void> for_each_disk_source_chunk(...)`

This plan keeps the layered behavior and caller-owned diagnostics, but lands the neutral `host_file` naming in active code per D-01, D-02, D-04, D-05, and D-06.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rename the helper family and move its regression matrix first</name>
  <files>src/detail/host_file.hpp, src/detail/host_file.cpp, tests/unit/host_file_tests.cpp, tests/CMakeLists.txt, CMakeLists.txt</files>
  <behavior>
    - Test 1: exact reads, prefix reads, and chunk iteration still succeed through the renamed helper.
    - Test 2: missing source, changed source, and allocation-related failures still surface the caller-owned diagnostics.
    - Test 3: build registration references `host_file` sources/tests and no longer references `writer_disk_source` sources/tests.
  </behavior>
  <action>Per D-01, D-02, D-04, D-05, and D-06, rename `writer_disk_source` into the neutral shared `host_file` family in `src/detail/` and move the focused helper tests/build registration onto the new names before broader path-contract work begins. Preserve the layered helper shape exactly: open stream, inspect size, prefix read, exact read, and bounded chunk iteration. Keep returning `std::ifstream` and keep caller-supplied diagnostic text as the stable reporting contract. Add or update Doxygen-style doc comments on renamed helper declarations and any substantially rewritten helper functions so the shared read/write role and changed-source semantics stay documented.</action>
  <verify>
    <automated>cmake --build --preset windows-msvc-debug-static --target libbsa_tests &amp;&amp; ctest --preset windows-msvc-debug-static -R "host_file" --output-on-failure</automated>
  </verify>
  <done>The helper family and its regression matrix live under `host_file` names, the build registers those names, and the old file/test names are gone from active build inputs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewire current writer callers onto the neutral helper names without changing their path contract yet</name>
  <files>src/formats/bsa/tes4_bsa_prepare.cpp, src/formats/bsa/tes4_bsa_layout.cpp, src/formats/ba2/ba2_gnrl_prepare.cpp, src/formats/ba2/ba2_dx10_prepare.cpp</files>
  <behavior>
    - Test 1: TES4 and BA2 writer-focused tests still pass after the helper rename.
    - Test 2: each touched writer file keeps its file-specific diagnostic strings.
    - Test 3: no active writer source still references `writer_disk_source` identifiers after the rename.
  </behavior>
  <action>Per D-02 and D-06, update all active writer-side includes, context objects, helper identifiers, and comments to the renamed `host_file` surface in the four touched prepare/layout files. Keep this task strictly as rename-and-rewire work: preserve current raw host-path inputs, preserve each file's caller-owned diagnostics, and do not introduce compatibility shims, alias typedefs, or behavioral rewrites. If a comment or doc comment still describes a writer-only helper boundary, rewrite it to explain the new shared host-file role and note the rewrite in the execution summary.</action>
  <verify>
    <automated>cmake --build --preset windows-msvc-debug-static --target libbsa_tests &amp;&amp; ctest --preset windows-msvc-debug-static -R "host_file|tes4_bsa_writer|ba2_gnrl_writer|ba2_dx10_writer" --output-on-failure</automated>
  </verify>
  <done>All active writer prepare/layout callers compile against neutral `host_file` names with unchanged diagnostics and no leftover writer-only identifier usage.</done>
</task>

<task type="auto">
  <name>Task 3: Sweep old helper names from the active build and document the shared seam</name>
  <files>src/detail/host_file.hpp, src/detail/host_file.cpp, src/formats/bsa/tes4_bsa_prepare.cpp, src/formats/bsa/tes4_bsa_layout.cpp, src/formats/ba2/ba2_gnrl_prepare.cpp, src/formats/ba2/ba2_dx10_prepare.cpp, tests/unit/host_file_tests.cpp, tests/CMakeLists.txt, CMakeLists.txt</files>
  <action>Finish Wave 1 by removing any lingering active-code references to the old helper family per D-02, while leaving accurate comments intact and adding short WHY comments where the changed-source or allocation semantics remain non-obvious. Do not broaden into the shared internal path contract yet; that belongs to Plan 13-02.</action>
  <verify>
    <automated>cmake --build --preset windows-msvc-debug-static --target libbsa_tests &amp;&amp; ctest --preset windows-msvc-debug-static -R "host_file|tes4_bsa_writer|ba2_gnrl_writer|ba2_dx10_writer" --output-on-failure &amp;&amp; pwsh -NoProfile -Command "rg -n 'writer_disk_source|open_disk_source|inspect_disk_source_size|read_disk_source_|for_each_disk_source_chunk' src tests CMakeLists.txt; if ($LASTEXITCODE -eq 0) { exit 1 } elseif ($LASTEXITCODE -eq 1) { exit 0 } else { exit $LASTEXITCODE }"</automated>
  </verify>
  <done>Wave 1 ends with one neutral `host_file` seam in active code, no old helper identifiers in build inputs, and a green focused helper/writer verification set.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| writer prepare/layout code → shared host-file helper | shared I/O policy and caller diagnostics cross here |
| host-file helper → Windows filesystem | mutable host-file contents and path bytes cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01-01 | T/D | `src/detail/host_file.*` | mitigate | Preserve exact-read, changed-source, and non-zero chunk-size checks while renaming the helper per D-04 and D-05 so the shared seam does not regress on mutable-file safety. |
| T-13-01-02 | R | writer caller diagnostics | mitigate | Keep caller-owned context strings per D-06 so writer-specific failures remain attributable after the helper rename. |
| T-13-01-03 | T | helper-name drift | mitigate | Remove old identifiers from active build/test inputs in the same plan per D-02 so later path-contract work cannot bind to the wrong helper family. |
</threat_model>

<verification>
- Focused helper/writer gate: `cmake --build --preset windows-msvc-debug-static --target libbsa_tests && ctest --preset windows-msvc-debug-static -R "host_file|tes4_bsa_writer|ba2_gnrl_writer|ba2_dx10_writer" --output-on-failure`
- Symbol sweep gate: `rg -n "writer_disk_source|open_disk_source|inspect_disk_source_size|read_disk_source_|for_each_disk_source_chunk" src tests CMakeLists.txt`
</verification>

<success_criteria>
- Active code has one neutral `host_file` helper family.
- Existing writer callers compile against that family with unchanged behavior and diagnostics.
- Old helper names are removed from active source/test/build registration.
</success_criteria>

<output>
After completion, create `.planning/phases/13-host-path-correctness-boundary/13-01-SUMMARY.md`
</output>
