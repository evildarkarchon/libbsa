---
phase: 08-ba2-gnrl-write-new-support
plan: 04
type: tdd
wave: 3
depends_on: [08-02, 08-03]
files_modified:
  - src/formats/ba2/ba2_gnrl_writer.cpp
  - tests/unit/ba2_gnrl_writer_tests.cpp
requirements: [WBA2-01, WBA2-02, WBA2-03, WBA2-05]
autonomous: true
must_haves:
  truths:
    - "D-07: Starfield writer options use deterministic defaults plus caller overrides for Unknown1, Unknown2, and CompressionMethod."
    - "D-08: Starfield v3 remains one target profile with CompressionMethod controlled by writer options."
    - "D-09: Default Starfield Unknown1 and Unknown2 values are reference-derived and documented near implementation."
    - "D-12: Derive and validate the BA2 GNRL extension field from archive path extension without silent truncation."
    - "D-13: Non-default per-entry record_flags overrides reopen through entry_metadata.record_flags."
    - "D-14: The writer computes BA2 name and directory hash fields from archive paths using libbsa helpers."
    - "D-15: The writer owns payload offsets, packed/raw sizes, and the BAADF00D sentinel."
    - "D-16: Existing entry_metadata fields are sufficient for writer-output verification."
    - "D-17: Prefer reference-compatible record/name ordering, with deterministic canonical fallback if evidence is inconclusive."
    - "D-18: Records and filename-table entries stay paired by index in the selected order."
    - "D-19: Filename-table paths preserve caller spelling with separators normalized to slash."
    - "D-20: Stored payload bytes come before the final length-prefixed filename table."
    - "D-22: Ordering tests assert structure plus reader-backed round trip, not full byte-golden archives."
    - "D-24: Preserve BA2 reader path semantics for canonical lookup keys and original spelling."
    - "D-26: Writer validation reopens output through archive_reader and asserts public metadata, lookup, extraction, and layout facts."
    - "Writer output for FO4 v1, Starfield v2, and Starfield v3 reopens through archive_reader."
    - "Memory-buffer entries reopen with the bytes copied at add time even if caller memory is mutated before write_to."
    - "Writer output stores payloads before the final UInt16-prefixed filename table and sets FileTableOffset to that table."
    - "Reopened metadata exposes expected target version, variant, default and caller-overridden Starfield fields, raw sizes, stored sizes, offsets, caller-overridden record flags, original paths, and canonical paths."
  artifacts:
    - path: "src/formats/ba2/ba2_gnrl_writer.cpp"
      provides: "raw BA2 GNRL header, record, payload, and end-name-table serialization"
      contains: "BAADF00D"
    - path: "tests/unit/ba2_gnrl_writer_tests.cpp"
      provides: "reader-backed raw serialization and metadata tests"
      contains: "FileTableOffset"
  key_links:
    - from: "ba2_gnrl_writer::write_to"
      to: "archive_reader::open"
      via: "writer-produced archive reopened from host path"
      pattern: "archive_reader::open"
---

<objective>
Replace validation-only output with reader-reopenable raw BA2 GNRL serialization for all locked target profiles.

Purpose: This delivers the physical BA2 layout required by D-14 through D-20 and Starfield profile fields required by D-07 through D-10, using the parser support from Plan 08-02.
Output: Raw writer archives that reopen, list, find, contain, and extract through public reader APIs.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-ba2-gnrl-write-new-support/08-SPEC.md
@.planning/phases/08-ba2-gnrl-write-new-support/08-CONTEXT.md
@.planning/phases/08-ba2-gnrl-write-new-support/08-RESEARCH.md
@.planning/phases/08-ba2-gnrl-write-new-support/08-PATTERNS.md
@.planning/phases/07-tes4-family-bsa-write-new-support/07-03-SUMMARY.md
@src/formats/ba2/ba2_gnrl_writer.cpp
@src/formats/ba2/ba2_gnrl_parser.cpp
@tests/unit/ba2_gnrl_writer_tests.cpp
@src/detail/binary_io.hpp
@src/detail/bethesda_hash.hpp
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1 RED: Add raw target-profile round-trip and layout tests</name>
  <files>tests/unit/ba2_gnrl_writer_tests.cpp</files>
  <read_first>
    - tests/unit/ba2_gnrl_writer_tests.cpp
    - src/formats/ba2/ba2_gnrl_writer.cpp
    - src/formats/ba2/ba2_gnrl_parser.cpp
    - .planning/phases/08-ba2-gnrl-write-new-support/08-SPEC.md
    - .planning/phases/08-ba2-gnrl-write-new-support/08-CONTEXT.md
    - .planning/phases/08-ba2-gnrl-write-new-support/08-PATTERNS.md
  </read_first>
  <behavior>
    - FO4 v1, Starfield v2, and Starfield v3 all-raw writer outputs reopen with `archive_type::ba2`.
    - A memory entry added from a mutable caller buffer extracts the original add-time bytes after the caller mutates that buffer and `write_to` serializes the archive.
    - `metadata().version` is `1`, `2`, or `3`; `file_count` equals input entry count.
    - Starfield v2/v3 metadata reports default `starfield_unknown1 == 1`, `starfield_unknown2 == 0`, and v3 `compression_method == 3` when options do not override them per D-07/D-09.
    - Starfield v2 and Starfield v3 writer outputs created with non-default `ba2_gnrl_writer_options::starfield_unknown1` and `ba2_gnrl_writer_options::starfield_unknown2` reopen through `archive_reader::open` with those exact public `metadata().ba2.starfield_unknown1` and `metadata().ba2.starfield_unknown2` values per WBA2-05/D-07.
    - At least one entry added with non-zero `ba2_gnrl_entry_options::record_flags` reopens with `entry_metadata.record_flags` equal to that override per D-13/D-16.
    - `FileTableOffset` is greater than every raw payload offset and the filename table contains original spellings with `/` separators.
  </behavior>
  <action>Add failing tests for raw BA2 GNRL serialization. Create disk and memory entries including `Meshes/MixedCase/Alpha.nif`, `Scripts/Quest.psc`, and a zero-byte path. For at least one `add_bytes` entry, keep an `expected_original` byte vector, add it from a mutable caller buffer, mutate the caller buffer before `write_to`, then reopen and assert `extract_bytes` returns `expected_original` to prove copied memory ownership through public APIs per D-02. Reopen outputs with `archive_reader::open`; assert `entries()`, `find`, `contains`, `extract_bytes`, `path`, `original_path`, `archive_hash`, `record_flags`, `raw_size`, `stored_size`, `offset`, and `entry_compression::none`. Add explicit WBA2-05/D-07 tests that construct Starfield v2 and Starfield v3 writers with non-default `ba2_gnrl_writer_options::starfield_unknown1` and `ba2_gnrl_writer_options::starfield_unknown2`, write archives, reopen them, and assert public metadata reports the override values instead of the defaults. Add an advanced D-13 test that supplies a non-zero `ba2_gnrl_entry_options::record_flags` override for one entry and asserts the reopened `entry_metadata.record_flags` equals that value. Add a small physical-layout helper that reads `BTDX`, version, `GNRL`, file count, `FileTableOffset`, record offsets/sizes, and final names to assert D-18, D-19, D-20, and D-22 without byte-golden comparison.</action>
  <verify>
    <automated>cmake --build --preset windows-msvc-debug-static --target libbsa_tests &amp;&amp; ctest --preset windows-msvc-debug-static -L ba2_gnrl_writer --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `tests/unit/ba2_gnrl_writer_tests.cpp` contains `FileTableOffset` or `file_table_offset`.
    - `tests/unit/ba2_gnrl_writer_tests.cpp` contains `ba2_gnrl_target::starfield_v2`.
    - `tests/unit/ba2_gnrl_writer_tests.cpp` contains `ba2_gnrl_target::starfield_v3`.
    - `tests/unit/ba2_gnrl_writer_tests.cpp` contains `starfield_unknown1`.
    - `tests/unit/ba2_gnrl_writer_tests.cpp` contains non-default Starfield v2 and Starfield v3 `starfield_unknown1`/`starfield_unknown2` override assertions against reopened public metadata.
    - `tests/unit/ba2_gnrl_writer_tests.cpp` contains a non-zero `record_flags` override assertion against reopened `entry_metadata.record_flags`.
    - `tests/unit/ba2_gnrl_writer_tests.cpp` contains a caller-buffer mutation assertion that compares reopened `extract_bytes` against add-time bytes.
    - Tests fail before real BA2 serialization replaces marker output.
  </acceptance_criteria>
  <done>RED tests specify raw target-profile serialization, copied-memory round-trip proof, end-table layout, Starfield default and override metadata proof, and non-zero record-flags override metadata proof.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 GREEN: Serialize raw BA2 GNRL archives with end filename tables</name>
  <files>src/formats/ba2/ba2_gnrl_writer.cpp</files>
  <read_first>
    - src/formats/ba2/ba2_gnrl_writer.cpp
    - tests/unit/ba2_gnrl_writer_tests.cpp
    - src/detail/binary_io.hpp
    - src/detail/bethesda_hash.hpp
    - src/formats/ba2/ba2_gnrl_parser.cpp
    - .planning/phases/08-ba2-gnrl-write-new-support/08-RESEARCH.md
    - .planning/phases/08-ba2-gnrl-write-new-support/08-PATTERNS.md
  </read_first>
  <action>Implement checked BA2 GNRL serialization using `detail::binary_writer`: write magic `BTDX`, version `1/2/3`, subtype `GNRL`, file count, `FileTableOffset`, Starfield `Unknown1`/`Unknown2` for version >=2 from `ba2_gnrl_writer_options::starfield_unknown1` and `ba2_gnrl_writer_options::starfield_unknown2` with defaults `1` and `0`, Starfield `CompressionMethod` for version >=3, then one 36-byte GNRL record per ordered entry. Use deterministic canonical-path order as the D-17 fallback documented by research; keep record/name entries paired by index per D-18. Derive extension FourCC from the original archive path extension without dot, padding to 4 bytes and rejecting empty or >4-byte extensions per D-12. Compute name and directory hash fields with libbsa FO4/BA2 hash helpers per D-14; write caller-visible record flags from `ba2_gnrl_entry_options::record_flags.value_or(0U)` per D-13 so non-zero advanced overrides reopen through `entry_metadata.record_flags`; write archive-absolute payload offsets, `packed_size == 0` for raw entries, raw size, and sentinel `0xBAADF00D` per D-15. Emit payload bytes first and final UInt16-prefixed original spellings with `/` separators at `FileTableOffset` per D-19/D-20. Add comments documenting Starfield defaults `Unknown1=1`, `Unknown2=0`, option override behavior, and v3 structural support uncertainty per D-09.</action>
  <verify>
    <automated>cmake --build --preset windows-msvc-debug-static --target libbsa_tests &amp;&amp; ctest --preset windows-msvc-debug-static -L ba2_gnrl_writer --output-on-failure &amp;&amp; ctest --preset windows-msvc-debug-static -R ba2_gnrl_reader --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `src/formats/ba2/ba2_gnrl_writer.cpp` contains `BTDX`.
    - `src/formats/ba2/ba2_gnrl_writer.cpp` contains `GNRL`.
    - `src/formats/ba2/ba2_gnrl_writer.cpp` contains `0xBAADF00D`.
    - `src/formats/ba2/ba2_gnrl_writer.cpp` contains `starfield_unknown1` or `Unknown1`.
    - Starfield v2 and Starfield v3 archives written with non-default `ba2_gnrl_writer_options::starfield_unknown1`/`starfield_unknown2` reopen with those exact public metadata values.
    - An entry written with non-zero `ba2_gnrl_entry_options::record_flags` reopens with `entry_metadata.record_flags` equal to that override.
    - `src/formats/ba2/ba2_gnrl_writer.cpp` contains `hash_fo4`.
    - BA2 writer and BA2 reader tests pass.
  </acceptance_criteria>
  <done>Raw FO4 v1, Starfield v2, and Starfield v3 BA2 GNRL archives are reader-reopenable, physically match the Phase 8 layout, preserve caller-provided Starfield unknown-field overrides, and preserve caller-provided record-flags overrides through public metadata.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Writer state→binary archive | Internal entries become untrusted-on-reopen archive bytes with offsets, sizes, hashes, names, and sentinels. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04-01 | Tampering/DoS | BA2 offset/size serialization | mitigate | Use 64-bit checked arithmetic and u32/u16 conversion guards before writing file count, name lengths, raw sizes, packed sizes, and payload offsets. |
| T-08-04-02 | Tampering | BA2 extension FourCC | mitigate | Reject empty or >4-byte extensions rather than silently truncating. |
| T-08-04-03 | Information Disclosure | Starfield defaults | mitigate | Document reference-derived `Unknown1=1`, `Unknown2=0`, and structural v3 GNRL uncertainty in comments; expose only libbsa-owned numeric options. |
</threat_model>

<verification>
Run targeted BA2 writer/reader tests, public include boundary tests, and `git -C TES5Edit status --short`.
</verification>

<success_criteria>
Raw writer outputs for all target profiles reopen and extract through public reader APIs, with end filename tables and Starfield metadata visible through public metadata.
</success_criteria>

<output>
After completion, create `.planning/phases/08-ba2-gnrl-write-new-support/08-04-SUMMARY.md`.
</output>
