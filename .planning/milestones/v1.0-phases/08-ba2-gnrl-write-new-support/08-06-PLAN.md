---
phase: 08-ba2-gnrl-write-new-support
plan: 06
type: tdd
wave: 5
depends_on: [08-05]
files_modified:
  - src/formats/ba2/ba2_gnrl_writer.cpp
  - tests/unit/ba2_gnrl_writer_tests.cpp
requirements: [WBA2-01, WBA2-02, WBA2-03, WBA2-04, WBA2-05]
autonomous: true
must_haves:
  truths:
    - "D-21: With dedupe enabled, the first eligible byte-identical final stored payload owns the payload bytes and later duplicates share that offset."
    - "D-22: Ordering and layout verification uses structure plus reader-backed round trip instead of full byte-golden archives."
    - "D-23: Deduplication is opt-in and compares final stored payload bytes after compression and metadata-affecting encoding decisions."
    - "D-26: Writer validation reopens output through archive_reader and asserts public metadata, lookup, extraction, and layout facts."
    - "D-27: Generated/legal tests must not mutate or use TES5Edit as a fixture workspace."
    - "D-28: Tests assert stable error_code values and public metadata rather than diagnostic message text."
    - "BA2 GNRL deduplication is disabled by default and enabled only by `deduplicate_payloads = true`."
    - "Eligible duplicates share payload offsets only when final stored payload bytes are identical."
    - "Full Phase 8 writer output passes reader-backed round trips, public boundary checks, existing BA2/BSA regressions, and TES5Edit cleanliness."
  artifacts:
    - path: "src/formats/ba2/ba2_gnrl_writer.cpp"
      provides: "opt-in final stored-byte deduplication and final phase gates"
      contains: "deduplicate_payloads"
    - path: "tests/unit/ba2_gnrl_writer_tests.cpp"
      provides: "dedupe enabled/disabled and full writer regression tests"
      contains: "deduplicate_payloads"
  key_links:
    - from: "writer stored payload preparation"
      to: "entry_metadata.offset after reopen"
      via: "reader-backed shared/distinct offset assertions"
      pattern: "offset"
---

<objective>
Add opt-in final-stored-byte deduplication and run full Phase 8 closure gates.

Purpose: This completes D-21, D-22, D-23, D-26, D-27, and D-28 by proving dedupe and all writer behavior through public reader APIs and regression tests.
Output: Deduplicating BA2 GNRL writer plus final verification evidence.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-ba2-gnrl-write-new-support/08-SPEC.md
@.planning/phases/08-ba2-gnrl-write-new-support/08-CONTEXT.md
@.planning/phases/08-ba2-gnrl-write-new-support/08-VALIDATION.md
@.planning/phases/08-ba2-gnrl-write-new-support/08-PATTERNS.md
@.planning/phases/07-tes4-family-bsa-write-new-support/07-06-SUMMARY.md
@src/formats/ba2/ba2_gnrl_writer.cpp
@tests/unit/ba2_gnrl_writer_tests.cpp
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1 RED: Add BA2 final-stored-byte dedupe tests</name>
  <files>tests/unit/ba2_gnrl_writer_tests.cpp</files>
  <read_first>
    - tests/unit/ba2_gnrl_writer_tests.cpp
    - src/formats/ba2/ba2_gnrl_writer.cpp
    - .planning/phases/08-ba2-gnrl-write-new-support/08-SPEC.md
    - .planning/phases/08-ba2-gnrl-write-new-support/08-CONTEXT.md
    - .planning/phases/07-tes4-family-bsa-write-new-support/07-06-SUMMARY.md
  </read_first>
  <behavior>
    - Duplicate source bytes with default options have distinct reopened `entry_metadata.offset` values.
    - Duplicate source bytes with `deduplicate_payloads = true` and identical final stored bytes share reopened offsets.
    - Raw and compressed entries with equal source bytes do not share offsets because final stored bytes differ.
    - Dedupe preserves distinct records and filename-table entries and every entry extracts byte-identical source data.
  </behavior>
  <action>Add failing tests for disabled dedupe, enabled dedupe, and non-eligible duplicates. Use `ba2_gnrl_writer_options options; options.deduplicate_payloads = true;` for enabled mode. Assert shared/distinct offsets through reopened `archive_reader::find`, not writer internals, and assert all duplicate paths remain separately listed with byte-identical extraction. Include at least one compressed duplicate case so final stored bytes after codec output are the dedupe key per D-23.</action>
  <verify>
    <automated>cmake --build --preset windows-msvc-debug-static --target libbsa_tests &amp;&amp; ctest --preset windows-msvc-debug-static -L ba2_gnrl_writer --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `tests/unit/ba2_gnrl_writer_tests.cpp` contains `deduplicate_payloads = true`.
    - `tests/unit/ba2_gnrl_writer_tests.cpp` contains `CHECK(` assertions comparing `.offset` values.
    - `tests/unit/ba2_gnrl_writer_tests.cpp` contains `entry_compression_policy::compressed` in a dedupe test.
    - Tests fail before dedupe implementation because enabled duplicates still have distinct offsets.
  </acceptance_criteria>
  <done>RED tests lock opt-in final-stored-byte deduplication semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 GREEN: Implement BA2 final-stored-byte deduplication</name>
  <files>src/formats/ba2/ba2_gnrl_writer.cpp</files>
  <read_first>
    - src/formats/ba2/ba2_gnrl_writer.cpp
    - tests/unit/ba2_gnrl_writer_tests.cpp
    - src/formats/bsa/tes4_bsa_writer.cpp
    - .planning/phases/08-ba2-gnrl-write-new-support/08-CONTEXT.md
    - .planning/phases/08-ba2-gnrl-write-new-support/08-PATTERNS.md
  </read_first>
  <action>After each entry's final stored payload vector is built, implement opt-in dedupe controlled only by `ba2_gnrl_writer_options::deduplicate_payloads`. Use the complete stored payload vector as the key after raw/compressed routing. Entries in selected record order own payload bytes first; later byte-identical duplicates copy the owner's archive-absolute payload offset and set an internal ownership flag so duplicate bytes are not emitted. Preserve one record and one filename-table name for every entry. Keep dedupe disabled by default.</action>
  <verify>
    <automated>cmake --build --preset windows-msvc-debug-static --target libbsa_tests &amp;&amp; ctest --preset windows-msvc-debug-static -L ba2_gnrl_writer --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `src/formats/ba2/ba2_gnrl_writer.cpp` contains `deduplicate_payloads`.
    - `src/formats/ba2/ba2_gnrl_writer.cpp` contains `owns_payload` or `owns_payload_bytes`.
    - `src/formats/ba2/ba2_gnrl_writer.cpp` contains `std::map&lt;std::vector&lt;std::byte&gt;` or `std::unordered_map` keyed by stored payload bytes.
    - BA2 writer dedupe tests pass.
  </acceptance_criteria>
  <done>BA2 GNRL writer deduplicates eligible stored payloads only when explicitly requested and remains reader-correct.</done>
</task>

<task type="auto">
  <name>Task 3: Run final Phase 8 regression gates</name>
  <files>tests/unit/ba2_gnrl_writer_tests.cpp, src/formats/ba2/ba2_gnrl_writer.cpp</files>
  <read_first>
    - tests/unit/ba2_gnrl_writer_tests.cpp
    - src/formats/ba2/ba2_gnrl_writer.cpp
    - .planning/phases/08-ba2-gnrl-write-new-support/08-VALIDATION.md
    - .planning/phases/08-ba2-gnrl-write-new-support/08-SPEC.md
  </read_first>
  <action>Run the final phase gates exactly: `ctest --preset windows-msvc-debug-static -L ba2_gnrl_writer --output-on-failure`, `ctest --preset windows-msvc-debug-static -R "ba2_gnrl_reader|ba2_dx10|tes4_bsa_writer|public_include_boundary" --output-on-failure`, full `ctest --preset windows-msvc-debug-static --output-on-failure`, and `git -C TES5Edit status --short`. If a CTest filter has zero matches, record the exact CTest output in the summary and still run the full suite. Do not modify `TES5Edit/`.</action>
  <verify>
    <automated>ctest --preset windows-msvc-debug-static -L ba2_gnrl_writer --output-on-failure &amp;&amp; ctest --preset windows-msvc-debug-static -R "ba2_gnrl_reader|ba2_dx10|tes4_bsa_writer|public_include_boundary" --output-on-failure &amp;&amp; ctest --preset windows-msvc-debug-static --output-on-failure &amp;&amp; git -C TES5Edit status --short</automated>
  </verify>
  <acceptance_criteria>
    - Targeted BA2 writer CTest exits with code 0.
    - Regression CTest command exits with code 0 or reports zero matching tests only for a documented filter while the full suite exits with code 0.
    - `git -C TES5Edit status --short` produces no output.
    - Summary records all commands and pass/fail outputs.
  </acceptance_criteria>
  <done>Full Phase 8 writer, reader, public boundary, regression, and TES5Edit cleanliness gates pass.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stored payload cache→record offsets | Internal dedupe state controls whether multiple records share one archive payload region. |
| Phase output→future phases | Completed BA2 writer becomes a dependency for DX10 writer and later validation/hardening. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06-01 | Tampering | Dedupe offset assignment | mitigate | Deduplicate only byte-identical final stored payload vectors after compression; reader-backed tests assert offsets and extraction for every entry. |
| T-08-06-02 | DoS | Regression coverage gaps | mitigate | Run targeted writer tests, BA2 reader/DX10/TES4 writer/public boundary regressions, full CTest, and TES5Edit cleanliness before closeout. |
</threat_model>

<verification>
Run final gates listed in Task 3 and create the summary with exact verification evidence.
</verification>

<success_criteria>
All WBA2-01 through WBA2-05 acceptance behavior passes through public reader-backed tests; dedupe is correct; existing regressions stay green; TES5Edit remains clean.
</success_criteria>

<output>
After completion, create `.planning/phases/08-ba2-gnrl-write-new-support/08-06-SUMMARY.md`.
</output>
