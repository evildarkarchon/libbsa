---
phase: 08-ba2-gnrl-write-new-support
plan: 03
type: tdd
wave: 2
depends_on: [08-01]
files_modified:
  - include/libbsa/writer.hpp
  - src/formats/ba2/ba2_gnrl_writer.hpp
  - src/formats/ba2/ba2_gnrl_writer.cpp
  - CMakeLists.txt
  - tests/CMakeLists.txt
  - tests/unit/ba2_gnrl_writer_tests.cpp
requirements: [WBA2-01]
autonomous: true
must_haves:
  truths:
    - "D-02: Memory-buffer entries are copied into writer-owned state at add time."
    - "D-05: Duplicate canonical BA2 paths are rejected at write time with a structured error."
    - "D-12: Derive and validate the BA2 GNRL 4-byte extension field without silent truncation."
    - "D-13: Use deterministic record-flags defaults while allowing an optional advanced per-entry override."
    - "D-27: Generated/legal tests must not mutate or use TES5Edit as a fixture workspace."
    - "D-28: Tests assert stable error_code values and public metadata rather than diagnostic message text."
    - "BA2 writer entries copy memory input into writer-owned state at add time and keep disk source paths for write-time validation."
    - "Duplicate canonical archive paths are rejected at write time with `error_code::format_error`."
    - "Invalid archive paths and missing disk sources return stable structured errors."
  artifacts:
    - path: "src/formats/ba2/ba2_gnrl_writer.hpp"
      provides: "private BA2 GNRL writer entry model and write function contract"
      contains: "ba2_gnrl_writer_entry"
    - path: "src/formats/ba2/ba2_gnrl_writer.cpp"
      provides: "public writer method bodies and validation foundation"
      contains: "write_ba2_gnrl_archive"
    - path: "tests/unit/ba2_gnrl_writer_tests.cpp"
      provides: "writer validation tests"
      contains: "[ba2_gnrl_writer]"
  key_links:
    - from: "include/libbsa/writer.hpp"
      to: "src/formats/ba2/ba2_gnrl_writer.cpp"
      via: "ba2_gnrl_writer::state shared state implementation"
      pattern: "ba2_gnrl_writer::state"
---

<objective>
Implement BA2 GNRL writer-owned state, validation, and build wiring behind the public contract.

Purpose: This establishes disk/memory entry ownership and write-time validation per D-02, D-05, D-12, D-13, and D-27 before real BA2 serialization.
Output: Private writer files, CMake/test registration, and validation tests.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-ba2-gnrl-write-new-support/08-SPEC.md
@.planning/phases/08-ba2-gnrl-write-new-support/08-CONTEXT.md
@.planning/phases/08-ba2-gnrl-write-new-support/08-RESEARCH.md
@.planning/phases/08-ba2-gnrl-write-new-support/08-PATTERNS.md
@.planning/phases/07-tes4-family-bsa-write-new-support/07-02-SUMMARY.md
@include/libbsa/writer.hpp
@src/formats/bsa/tes4_bsa_writer.hpp
@src/formats/bsa/tes4_bsa_writer.cpp
@tests/unit/tes4_bsa_writer_tests.cpp
@CMakeLists.txt
@tests/CMakeLists.txt
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1 RED: Add BA2 writer validation foundation tests</name>
  <files>tests/unit/ba2_gnrl_writer_tests.cpp, tests/CMakeLists.txt</files>
  <read_first>
    - tests/unit/ba2_gnrl_writer_tests.cpp
    - tests/CMakeLists.txt
    - tests/unit/tes4_bsa_writer_tests.cpp
    - include/libbsa/writer.hpp
    - .planning/phases/08-ba2-gnrl-write-new-support/08-CONTEXT.md
    - .planning/phases/08-ba2-gnrl-write-new-support/08-PATTERNS.md
  </read_first>
  <behavior>
    - `add_file` rejects empty host paths with `error_code::invalid_argument`.
    - `write_to` rejects duplicate canonical paths with `error_code::format_error`.
    - Invalid archive paths fail at add time with `error_code::invalid_argument`.
    - `write_to` refuses existing output unless `overwrite_existing == true`.
  </behavior>
  <action>Create `tests/unit/ba2_gnrl_writer_tests.cpp` tagged `[ba2_gnrl_writer]` and register it in `tests/CMakeLists.txt`. Add failing tests for disk-source host path validation, invalid archive paths, write-time duplicate canonical rejection, default overwrite guard, and missing disk source errors. Use public API only: `libbsa::ba2_gnrl_writer`, `libbsa::ba2_gnrl_target::fallout4`, `libbsa::ba2_gnrl_writer_options`, `add_file`, `add_bytes`, and `write_to`. Do not attempt reader-observable copied-memory byte proof in this plan because successful output is validation-only until Plan 08-04; Plan 08-04 owns the public round-trip proof after real BA2 serialization exists. Assert `error_code` values only per D-28.</action>
  <verify>
    <automated>cmake --build --preset windows-msvc-debug-static --target libbsa_tests &amp;&amp; ctest --preset windows-msvc-debug-static -L ba2_gnrl_writer --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `tests/unit/ba2_gnrl_writer_tests.cpp` contains `[ba2_gnrl_writer]`.
    - `tests/unit/ba2_gnrl_writer_tests.cpp` contains `ba2_gnrl_target::fallout4`.
    - `tests/unit/ba2_gnrl_writer_tests.cpp` contains `error_code::format_error` for duplicate canonical paths.
    - `tests/CMakeLists.txt` contains `unit/ba2_gnrl_writer_tests.cpp`.
    - Tests fail before production implementation exists.
  </acceptance_criteria>
  <done>RED writer validation suite is registered and failing for missing implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 GREEN: Implement BA2 writer state, validation, and marker write</name>
  <files>include/libbsa/writer.hpp, src/formats/ba2/ba2_gnrl_writer.hpp, src/formats/ba2/ba2_gnrl_writer.cpp, CMakeLists.txt</files>
  <read_first>
    - include/libbsa/writer.hpp
    - src/formats/ba2/ba2_gnrl_writer.hpp
    - src/formats/ba2/ba2_gnrl_writer.cpp
    - CMakeLists.txt
    - src/formats/bsa/tes4_bsa_writer.hpp
    - src/formats/bsa/tes4_bsa_writer.cpp
    - src/detail/archive_path.hpp
    - .planning/phases/08-ba2-gnrl-write-new-support/08-CONTEXT.md
    - .planning/phases/08-ba2-gnrl-write-new-support/08-PATTERNS.md
  </read_first>
  <action>Add `ba2_gnrl_writer::state` containing `ba2_gnrl_target target`, `ba2_gnrl_writer_options options`, and `std::vector&lt;formats::ba2::ba2_gnrl_writer_entry&gt; entries`. Create private `ba2_gnrl_writer_entry` with `archive_path_original`, `archive_path_canonical`, `host_path`, `memory_bytes`, `from_memory`, and `ba2_gnrl_entry_options options`. Normalize paths with `detail::normalize_archive_path`, preserve original spelling with `/`, copy memory spans into writer-owned state, and reject duplicate canonical paths only inside `write_ba2_gnrl_archive` per D-05. Validate extension FourCC in the writer path per D-12: extension without dot must be 1-4 ASCII bytes and must not be silently truncated. Implement overwrite and temp-file publish pattern matching Phase 7; successful validation may write a `BTDX` marker archive only until Plan 08-04 replaces it with reader-reopenable serialization. Register `src/formats/ba2/ba2_gnrl_writer.cpp` in root `CMakeLists.txt`.</action>
  <verify>
    <automated>cmake --build --preset windows-msvc-debug-static --target libbsa_tests &amp;&amp; ctest --preset windows-msvc-debug-static -L ba2_gnrl_writer --output-on-failure &amp;&amp; ctest --preset windows-msvc-debug-static -R public_include_boundary --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `src/formats/ba2/ba2_gnrl_writer.hpp` contains `struct ba2_gnrl_writer_entry`.
    - `src/formats/ba2/ba2_gnrl_writer.cpp` contains `struct ba2_gnrl_writer::state`.
    - `src/formats/ba2/ba2_gnrl_writer.cpp` contains `normalize_archive_path`.
    - `src/formats/ba2/ba2_gnrl_writer.cpp` contains `write_ba2_gnrl_archive`.
    - `CMakeLists.txt` contains `src/formats/ba2/ba2_gnrl_writer.cpp`.
    - BA2 writer validation and public boundary tests pass.
  </acceptance_criteria>
  <done>BA2 writer validates inputs, copies memory entries into writer-owned state for later serialization, rejects duplicates at write time, and is built into libbsa.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Consumer inputs→writer state | Archive paths, host paths, memory buffers, compression flags, and record flags enter mutable writer state. |
| Writer→host filesystem | `write_to` creates or replaces a host archive path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03-01 | Tampering | Archive paths | mitigate | Use `detail::normalize_archive_path`, preserve original spelling separately, and reject duplicate canonical paths at write time with `format_error`. |
| T-08-03-02 | Tampering | Output path publishing | mitigate | Use a sibling temporary file and publish only after successful validation/write; refuse existing output unless `overwrite_existing` is true. |
| T-08-03-03 | Repudiation | Stable error assertions | mitigate | Tests assert `error_code` values, not diagnostic message text, per D-28. |
</threat_model>

<verification>
Run `ctest --preset windows-msvc-debug-static -L ba2_gnrl_writer --output-on-failure`, public boundary tests, and `git -C TES5Edit status --short`.
</verification>

<success_criteria>
Writer state and validation behavior are green; no TES5Edit changes exist; real serialization is ready for Plan 08-04.
</success_criteria>

<output>
After completion, create `.planning/phases/08-ba2-gnrl-write-new-support/08-03-SUMMARY.md`.
</output>
