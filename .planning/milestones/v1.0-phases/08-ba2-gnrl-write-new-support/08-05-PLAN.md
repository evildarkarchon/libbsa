---
phase: 08-ba2-gnrl-write-new-support
plan: 05
type: tdd
wave: 4
depends_on: [08-04]
files_modified:
  - src/formats/ba2/ba2_gnrl_writer.cpp
  - tests/unit/ba2_gnrl_writer_tests.cpp
requirements: [WBA2-02, WBA2-04, WBA2-05]
autonomous: true
must_haves:
  truths:
    - "D-04: Reuse public compression policy enums while target/profile maps compressed entries to the internal codec."
    - "D-10: Starfield v3 CompressionMethod is archive-wide and per-entry overrides choose raw versus compressed only."
    - "D-11: Method 3 uses raw LZ4 block, method 0 uses deflate, and unsupported methods remain unsupported."
    - "D-25: Compression routing comes from explicit target/options metadata, raw-vs-packed state, and Starfield CompressionMethod, never extension guessing."
    - "D-28: Tests assert stable error_code values and public metadata rather than diagnostic message text."
    - "Compressed BA2 GNRL entries round-trip for FO4 v1, Starfield v2, Starfield v3 method 0, and Starfield v3 method 3."
    - "Per-entry raw/compressed overrides choose only raw versus compressed; Starfield v3 compressed entries use the archive-wide CompressionMethod."
    - "Compression routing is explicit target/options metadata, never filename-extension inference."
  artifacts:
    - path: "src/formats/ba2/ba2_gnrl_writer.cpp"
      provides: "BA2 GNRL compression policy and deflate/raw-LZ4 payload encoding"
      contains: "compression_method::lz4_block"
    - path: "tests/unit/ba2_gnrl_writer_tests.cpp"
      provides: "reader-backed compression and override tests"
      contains: "entry_compression::lz4_block"
  key_links:
    - from: "ba2_gnrl_writer options"
      to: "detail::compress_payload"
      via: "target profile + archive-wide Starfield CompressionMethod"
      pattern: "compress_payload"
---

<objective>
Add BA2 GNRL compression defaults and per-entry overrides using existing deflate/raw-LZ4 router behavior.

Purpose: This satisfies D-04, D-10, D-11, and D-25 by mapping compressed entries from explicit target/options metadata to deflate or raw LZ4 block.
Output: Compressed writer payloads that reopen and extract byte-identical source data.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-ba2-gnrl-write-new-support/08-SPEC.md
@.planning/phases/08-ba2-gnrl-write-new-support/08-CONTEXT.md
@.planning/phases/08-ba2-gnrl-write-new-support/08-RESEARCH.md
@.planning/phases/08-ba2-gnrl-write-new-support/08-PATTERNS.md
@.planning/phases/07-tes4-family-bsa-write-new-support/07-04-SUMMARY.md
@src/formats/ba2/ba2_gnrl_writer.cpp
@src/formats/ba2/ba2_gnrl_reader.cpp
@src/detail/compression_router.hpp
@src/detail/compression_router.cpp
@tests/unit/ba2_gnrl_writer_tests.cpp
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1 RED: Add BA2 compression policy and override tests</name>
  <files>tests/unit/ba2_gnrl_writer_tests.cpp</files>
  <read_first>
    - tests/unit/ba2_gnrl_writer_tests.cpp
    - src/formats/ba2/ba2_gnrl_writer.cpp
    - src/formats/ba2/ba2_gnrl_reader.cpp
    - src/detail/compression_router.hpp
    - .planning/phases/08-ba2-gnrl-write-new-support/08-SPEC.md
    - .planning/phases/08-ba2-gnrl-write-new-support/08-CONTEXT.md
  </read_first>
  <behavior>
    - `archive_compression_policy::all_compressed` creates deflate entries for FO4 v1, Starfield v2, and Starfield v3 `starfield_compression_method = 0`.
    - `archive_compression_policy::all_compressed` creates raw LZ4 block entries for Starfield v3 `starfield_compression_method = 3`.
    - `entry_compression_policy::raw` stores `PackedSize == 0`; `entry_compression_policy::compressed` stores nonzero packed size for non-empty payloads.
    - Zero-byte entries remain raw and extract as empty bytes.
  </behavior>
  <action>Add failing tests that produce compressed and raw-override BA2 GNRL outputs for FO4 v1, Starfield v2, Starfield v3 method `0`, and Starfield v3 method `3`. Reopen with `archive_reader`, assert `entry_metadata.compression` is `entry_compression::deflate`, `entry_compression::lz4_block`, or `entry_compression::none` as appropriate, assert `stored_size`, `raw_size`, `metadata.ba2->compression_method`, and byte-compare `extract_bytes`. Include an input whose extension would suggest nothing about compression to prove no extension inference.</action>
  <verify>
    <automated>cmake --build --preset windows-msvc-debug-static --target libbsa_tests &amp;&amp; ctest --preset windows-msvc-debug-static -L ba2_gnrl_writer --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `tests/unit/ba2_gnrl_writer_tests.cpp` contains `archive_compression_policy::all_compressed`.
    - `tests/unit/ba2_gnrl_writer_tests.cpp` contains `entry_compression::deflate`.
    - `tests/unit/ba2_gnrl_writer_tests.cpp` contains `entry_compression::lz4_block`.
    - `tests/unit/ba2_gnrl_writer_tests.cpp` contains `starfield_compression_method = 0U`.
    - Tests fail before compression implementation because compressed entries are still raw.
  </acceptance_criteria>
  <done>RED tests lock BA2 deflate/raw-LZ4 routing, overrides, zero-byte raw behavior, and reader-backed extraction.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 GREEN: Route compressed BA2 payloads through deflate or raw LZ4 block</name>
  <files>src/formats/ba2/ba2_gnrl_writer.cpp</files>
  <read_first>
    - src/formats/ba2/ba2_gnrl_writer.cpp
    - tests/unit/ba2_gnrl_writer_tests.cpp
    - src/detail/compression_router.hpp
    - src/detail/compression_router.cpp
    - src/formats/ba2/ba2_format_detector.cpp
    - .planning/phases/08-ba2-gnrl-write-new-support/08-RESEARCH.md
  </read_first>
  <action>Implement BA2 compression policy resolution: `archive_compression_policy::target_default` and `all_compressed` request compression for non-empty entries, `all_raw` requests raw, and per-entry `entry_compression_policy::raw`/`compressed` overrides the archive policy. For compressed entries, map FO4 v1, Starfield v2, and Starfield v3 with `starfield_compression_method == 0U` to `detail::compression_method::deflate`; map Starfield v3 with `starfield_compression_method == 3U` to `detail::compression_method::lz4_block`; reject other v3 methods with `error_code::unsupported`. Store raw entries with `packed_size == 0`, compressed entries with packed size equal to compressed byte length, raw size equal to source byte length, and no BSA-style raw-size prefix. Preserve zero-byte entries as raw. Do not use LZ4 frame APIs and do not infer compression from archive path extensions.</action>
  <verify>
    <automated>cmake --build --preset windows-msvc-debug-static --target libbsa_tests &amp;&amp; ctest --preset windows-msvc-debug-static -L ba2_gnrl_writer --output-on-failure &amp;&amp; ctest --preset windows-msvc-debug-static -R "compression_router|deflate_codec|lz4_codec|ba2_gnrl_reader" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `src/formats/ba2/ba2_gnrl_writer.cpp` contains `compression_method::deflate`.
    - `src/formats/ba2/ba2_gnrl_writer.cpp` contains `compression_method::lz4_block`.
    - `src/formats/ba2/ba2_gnrl_writer.cpp` contains `compress_payload`.
    - `src/formats/ba2/ba2_gnrl_writer.cpp` does not contain `lz4_frame` in BA2 GNRL writer compression routing code.
    - BA2 writer compression tests and codec/router regression tests pass.
  </acceptance_criteria>
  <done>BA2 GNRL writer compressed entries round-trip through existing exact-size deflate and raw-LZ4 reader extraction paths.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Writer policy→compression router | Target/options metadata selects codec and determines compressed archive bytes. |
| Writer archive→reader decompressor | Produced compressed bytes are later treated as untrusted compressed payloads. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05-01 | Tampering | Compression method routing | mitigate | Route only from explicit target/options and `starfield_compression_method`; reject unsupported methods and never infer from extension. |
| T-08-05-02 | DoS | Compressed payloads | mitigate | Reopen tests exercise existing exact-size deflate/raw-LZ4 extraction paths to reject malformed size mismatches. |
| T-08-05-03 | Tampering | LZ4 API selection | mitigate | Use `detail::compression_method::lz4_block` for method 3 and forbid `lz4_frame` in BA2 GNRL writer routing. |
</threat_model>

<verification>
Run BA2 writer tests, compression router/codec tests, BA2 reader tests, and `git -C TES5Edit status --short`.
</verification>

<success_criteria>
All required BA2 GNRL compression profiles and overrides reopen and extract source bytes exactly through public reader APIs.
</success_criteria>

<output>
After completion, create `.planning/phases/08-ba2-gnrl-write-new-support/08-05-SUMMARY.md`.
</output>
