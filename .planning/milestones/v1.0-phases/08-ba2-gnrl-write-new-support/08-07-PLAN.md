---
phase: 08-ba2-gnrl-write-new-support
plan: 07
type: execute
wave: 6
depends_on: [08-06]
files_modified:
  - src/formats/ba2/ba2_gnrl_writer.cpp
  - tests/unit/ba2_gnrl_writer_tests.cpp
autonomous: true
gap_closure: true
requirements: [WBA2-01, WBA2-02, WBA2-03, WBA2-04, WBA2-05]
must_haves:
  truths:
    - "Public BA2 GNRL writer finalization does not delete unrelated sibling temp files."
    - "Public BA2 GNRL writer overwrite failure paths preserve the existing destination or reject unsafe destination shapes before publish."
    - "BA2 GNRL writer filesystem publish checks return libbsa::result errors instead of throwing std::filesystem exceptions for caller-controlled paths."
  artifacts:
    - path: "src/formats/ba2/ba2_gnrl_writer.cpp"
      provides: "Safe BA2 GNRL temporary publish and overwrite rollback behavior"
      contains: "write_ba2_gnrl_archive"
    - path: "tests/unit/ba2_gnrl_writer_tests.cpp"
      provides: "Regression coverage for temp-name collision and overwrite publish safety"
      contains: "BA2 GNRL writer"
  key_links:
    - from: "ba2_gnrl_writer::write_to"
      to: "formats::ba2::write_ba2_gnrl_archive"
      via: "public writer finalization path"
      pattern: "write_ba2_gnrl_archive"
    - from: "src/formats/ba2/ba2_gnrl_writer.cpp"
      to: "host filesystem"
      via: "unique temporary publish and backup/rollback sequence"
      pattern: "std::filesystem::rename"
---

<objective>
Close the Phase 8 verification gap by making BA2 GNRL writer finalization filesystem-safe.

Purpose: `ba2_gnrl_writer::write_to` is a public write-new API. It must not delete unrelated caller files via deterministic temp-path collisions, and overwrite mode must not lose an existing destination if publishing the replacement fails.
Output: Safe BA2 GNRL publish logic plus automated regression tests covering the verification gap.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-ba2-gnrl-write-new-support/08-CONTEXT.md
@.planning/phases/08-ba2-gnrl-write-new-support/08-VERIFICATION.md
@.planning/phases/08-ba2-gnrl-write-new-support/08-06-SUMMARY.md
@src/formats/ba2/ba2_gnrl_writer.cpp
@tests/unit/ba2_gnrl_writer_tests.cpp

<interfaces>
Existing public finalization path:
```cpp
result<void> ba2_gnrl_writer::write_to(std::string_view host_path) const;
```

Existing private writer entry point to preserve:
```cpp
result<void> write_ba2_gnrl_archive(ba2_gnrl_target target,
                                    const ba2_gnrl_writer_options& options,
                                    std::span<const ba2_gnrl_writer_entry> entries,
                                    std::string_view output_host_path);
```

Verification gap to close from `08-VERIFICATION.md`:
- Allocate a unique temporary file in the destination directory without deleting pre-existing unrelated temp-name collisions.
- Publish replacements without deleting the existing destination until replacement can succeed or be rolled back.
- Add public writer tests covering temp-name collision and overwrite publish failure behavior.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add publish-safety regression tests</name>
  <files>tests/unit/ba2_gnrl_writer_tests.cpp</files>
  <read_first>
    - tests/unit/ba2_gnrl_writer_tests.cpp
    - .planning/phases/08-ba2-gnrl-write-new-support/08-VERIFICATION.md
  </read_first>
  <behavior>
    - Test 1: When `output_path("safe-temp-collision.ba2.tmp")` already contains sentinel bytes and `ba2_gnrl_writer::write_to(output_path("safe-temp-collision.ba2"))` succeeds, the pre-existing `.tmp` sentinel file still exists and its bytes are unchanged.
    - Test 2: When `overwrite_existing = true` and the destination path is an existing directory named `output_path("overwrite-directory.ba2")`, `write_to` returns `libbsa::error_code::io_error` and the directory still exists afterward. This proves the writer rejects an unsafe destination shape instead of deleting it before publish.
    - Test 3: When `overwrite_existing = false` and the destination path already exists, the existing file bytes remain unchanged after the expected `io_error` result.
  </behavior>
  <action>
    Add Catch2 tests near the existing overwrite/missing-source writer tests. Use the existing helpers `output_path`, `write_binary_file`, `read_binary_file`, `sample_bytes`, and `overwriting_raw_options`.

    Concrete assertions:
    - For temp collision, write sentinel bytes `{0x54, 0x4D, 0x50}` to `output_path("safe-temp-collision.ba2.tmp")`, create a Fallout 4 writer with `overwriting_raw_options()`, add `Meshes/SafeTemp.nif` using `sample_bytes()`, write to `output_path("safe-temp-collision.ba2")`, then assert `written.has_value()`, `std::filesystem::exists(collision)`, and `read_binary_file(collision) == sentinel`.
    - For unsafe overwrite destination, create a directory at `output_path("overwrite-directory.ba2")`, create a Fallout 4 writer with `overwriting_raw_options()`, add `Meshes/DirectoryTarget.nif`, call `write_to(directory.string())`, then assert `!written.has_value()`, `written.error().code == libbsa::error_code::io_error`, and `std::filesystem::is_directory(directory)`.
    - For no-overwrite preservation, strengthen the existing `BA2 GNRL writer refuses to overwrite existing output when overwrite_existing is false` test by reading the destination after failure and asserting it still equals `{std::byte{0x01}}`.

    These tests intentionally target public writer behavior and stable `error_code` values per D-26 and D-28. Do not assert diagnostic message text.
  </action>
  <verify>
    <automated>cmake --build --preset windows-msvc-debug-static --target libbsa_tests; ctest --preset windows-msvc-debug-static -L ba2_gnrl_writer --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `tests/unit/ba2_gnrl_writer_tests.cpp` contains `safe-temp-collision.ba2.tmp`.
    - `tests/unit/ba2_gnrl_writer_tests.cpp` contains `overwrite-directory.ba2`.
    - `tests/unit/ba2_gnrl_writer_tests.cpp` contains `read_binary_file(existing)` in the no-overwrite test.
    - Targeted BA2 writer tests compile and run through the command in `<verify>`.
  </acceptance_criteria>
  <done>Regression tests prove deterministic `.tmp` collision preservation, unsafe overwrite destination preservation, and existing destination preservation in no-overwrite mode.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace deterministic delete-then-rename publish with unique temp and rollback</name>
  <files>src/formats/ba2/ba2_gnrl_writer.cpp, tests/unit/ba2_gnrl_writer_tests.cpp</files>
  <read_first>
    - src/formats/ba2/ba2_gnrl_writer.cpp
    - tests/unit/ba2_gnrl_writer_tests.cpp
    - .planning/phases/08-ba2-gnrl-write-new-support/08-VERIFICATION.md
  </read_first>
  <behavior>
    - Existing sibling files named `{output}.tmp` are never removed or modified by BA2 GNRL writer finalization.
    - `std::filesystem` probes on caller-controlled output paths use `std::error_code` overloads and return `libbsa::error_code::io_error` instead of throwing.
    - Overwrite mode rejects non-regular existing destinations before creating a replacement.
    - Overwrite mode preserves the previous destination by renaming it to a unique backup first, publishing the temp archive second, and rolling the backup back if publish fails.
  </behavior>
  <action>
    Refactor only the BA2 GNRL publish section in `write_ba2_gnrl_archive`; do not change BA2 record serialization, compression routing, dedupe semantics, or public APIs.

    Implement concrete private helpers in `src/formats/ba2/ba2_gnrl_writer.cpp` near the existing filesystem helpers:
    - `result<bool> path_exists_noexcept(const std::filesystem::path& path)` that calls `std::filesystem::exists(path, fs_error)` and returns `io_error` if `fs_error` is set.
    - `result<std::filesystem::path> make_unique_publish_directory(const std::filesystem::path& output_path)` that tries up to 64 sibling directory names using the output filename plus a suffix such as `.libbsa-tmp-<counter>`; use `std::filesystem::create_directory(candidate, fs_error)` so a collision is not deleted. Return `io_error` if all candidates fail or filesystem errors occur. The temp archive file should be `candidate / output_path.filename()`.
    - `void cleanup_publish_directory(const std::filesystem::path& temp_dir) noexcept` using `std::filesystem::remove_all(temp_dir, fs_error)` with a comment explaining cleanup is best-effort because the public result already reflects the primary write/publish operation.
    - `result<std::filesystem::path> reserve_backup_path(const std::filesystem::path& output_path)` that finds a non-existing sibling backup path such as `<filename>.libbsa-bak-<counter>` without deleting anything.

    Replace the current lines that append `.tmp`, remove it, remove `output_path`, and rename with this sequence:
    1. Convert `output_host_path` to `output_path` as today.
    2. Use `path_exists_noexcept(output_path)` for the overwrite guard; if `overwrite_existing` is false and the path exists, return `io_error` without writing anything.
    3. Create a unique temp directory in the destination directory and write archive bytes to `temp_dir / output_path.filename()`.
    4. If `overwrite_existing` is false, rename the temp archive to `output_path`; on failure, cleanup temp directory and return `io_error`.
    5. If `overwrite_existing` is true and `output_path` exists, require `std::filesystem::is_regular_file(output_path, fs_error)` to be true; otherwise cleanup temp directory and return `io_error`. This prevents deleting/replacing directories or special paths.
    6. For a regular existing destination, reserve a unique backup path, rename `output_path` to the backup, then rename temp archive to `output_path`. If publishing temp fails, rename the backup back to `output_path` before returning `io_error`; if rollback also fails, still return `io_error` and leave both paths untouched as much as the filesystem allows.
    7. After a successful replacement, remove the backup with `std::filesystem::remove(backup, fs_error)` as best-effort cleanup and cleanup the temp directory.

    Add short comments for the non-obvious WHY: unique temp directories avoid deleting caller-owned `.tmp` files, and the backup/rollback sequence avoids removing the last good archive before the replacement is publishable. Preserve D-27 by not touching `TES5Edit/`.
  </action>
  <verify>
    <automated>cmake --build --preset windows-msvc-debug-static --target libbsa_tests; ctest --preset windows-msvc-debug-static -L ba2_gnrl_writer --output-on-failure; ctest --preset windows-msvc-debug-static -R "ba2_gnrl_reader|ba2_dx10|tes4_bsa_writer|public_include_boundary" --output-on-failure; git -C TES5Edit status --short</automated>
  </verify>
  <acceptance_criteria>
    - `src/formats/ba2/ba2_gnrl_writer.cpp` no longer contains `temp_path += ".tmp"`.
    - `src/formats/ba2/ba2_gnrl_writer.cpp` no longer contains `std::filesystem::remove(output_path, fs_error)` in the publish sequence.
    - `src/formats/ba2/ba2_gnrl_writer.cpp` contains `create_directory` for unique temp reservation.
    - `src/formats/ba2/ba2_gnrl_writer.cpp` contains `is_regular_file` before overwrite replacement.
    - Targeted BA2 writer tests and listed regression tests pass.
    - `git -C TES5Edit status --short` prints no output.
  </acceptance_criteria>
  <done>BA2 GNRL publish no longer deletes deterministic temp collisions, rejects unsafe overwrite destinations, preserves rollback path for existing archives, and returns structured filesystem errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller→writer output path | Caller-controlled host path reaches filesystem publish logic. |
| writer→filesystem | Archive bytes, temp paths, backup paths, and destination paths are created, renamed, or cleaned up on the host filesystem. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07-01 | Tampering | `write_ba2_gnrl_archive` publish path | mitigate | Use unique sibling temp directories created with `std::filesystem::create_directory`; never remove pre-existing deterministic `.tmp` paths. |
| T-08-07-02 | Tampering/DoS | overwrite replacement | mitigate | Reject non-regular existing destinations and use backup+rollback before publishing replacement bytes. |
| T-08-07-03 | DoS | filesystem probes | mitigate | Use `std::error_code` overloads for caller-controlled `exists`, `is_regular_file`, `rename`, and cleanup paths; return `libbsa::error_code::io_error` instead of throwing. |
</threat_model>

<verification>
Run targeted writer tests, BA2/TES4/public-boundary regressions, and TES5Edit cleanliness:

```powershell
cmake --build --preset windows-msvc-debug-static --target libbsa_tests
ctest --preset windows-msvc-debug-static -L ba2_gnrl_writer --output-on-failure
ctest --preset windows-msvc-debug-static -R "ba2_gnrl_reader|ba2_dx10|tes4_bsa_writer|public_include_boundary" --output-on-failure
```
</verification>

<success_criteria>
- Phase 8 verification gap truth is closed: public BA2 GNRL writer finalization does not delete unrelated caller files or lose existing destinations during failed publish paths.
- BA2 GNRL writer tests include explicit coverage for `.tmp` collision preservation and unsafe overwrite target preservation.
- BA2 GNRL writer output still reopens/extracts through `archive_reader` after publish refactor.
- `TES5Edit/` remains read-only and clean.
</success_criteria>

<output>
After completion, create `.planning/phases/08-ba2-gnrl-write-new-support/08-07-SUMMARY.md`.
</output>
