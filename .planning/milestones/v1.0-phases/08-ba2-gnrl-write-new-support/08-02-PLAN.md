---
phase: 08-ba2-gnrl-write-new-support
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/formats/ba2/ba2_gnrl_parser.cpp
  - tests/unit/ba2_gnrl_reader_tests.cpp
requirements: [WBA2-03]
autonomous: true
must_haves:
  truths:
    - "D-20: Writer output must place stored payload bytes before the final length-prefixed filename table and set FileTableOffset to that table."
    - "D-24: Preserve BA2 reader path semantics for canonical lookup keys, original spelling, duplicate rejection, and not_found behavior."
    - "D-28: Tests assert stable error_code values and public metadata rather than diagnostic message text."
    - "archive_reader can reopen BA2 GNRL archives whose filename table is after payload bytes."
    - "BA2 path semantics remain canonical lowercase lookup keys with preserved original spelling."
  artifacts:
    - path: "src/formats/ba2/ba2_gnrl_parser.cpp"
      provides: "bounded host-file filename-table parsing independent from first payload offset"
      contains: "read_names"
    - path: "tests/unit/ba2_gnrl_reader_tests.cpp"
      provides: "reader regression for payload-before-filename-table layout"
      contains: "end filename table"
  key_links:
    - from: "src/formats/ba2/ba2_gnrl_parser.cpp"
      to: "archive_reader::open"
      via: "BA2 GNRL host-file parser path"
      pattern: "file_table_offset"
---

<objective>
Teach the BA2 GNRL reader to accept end-of-archive filename tables before writer output depends on reopening.

Purpose: Phase 8 locks payloads before names in D-20, but current parser rejects `FileTableOffset > first_payload_offset`; this plan removes that blocker while preserving D-24 reader path semantics.
Output: Parser regression and bounded host-file parsing update.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-ba2-gnrl-write-new-support/08-SPEC.md
@.planning/phases/08-ba2-gnrl-write-new-support/08-CONTEXT.md
@.planning/phases/08-ba2-gnrl-write-new-support/08-RESEARCH.md
@.planning/phases/08-ba2-gnrl-write-new-support/08-PATTERNS.md
@src/formats/ba2/ba2_gnrl_parser.cpp
@tests/unit/ba2_gnrl_reader_tests.cpp
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1 RED: Add end filename-table reader regression</name>
  <files>tests/unit/ba2_gnrl_reader_tests.cpp</files>
  <read_first>
    - tests/unit/ba2_gnrl_reader_tests.cpp
    - src/formats/ba2/ba2_gnrl_parser.cpp
    - .planning/phases/08-ba2-gnrl-write-new-support/08-SPEC.md
    - .planning/phases/08-ba2-gnrl-write-new-support/08-CONTEXT.md
    - .planning/phases/08-ba2-gnrl-write-new-support/08-PATTERNS.md
  </read_first>
  <behavior>
    - Generated test bytes use `BTDX`, version `1`, subtype `GNRL`, one or more records, payload bytes, then UInt16 length-prefixed names at the end.
    - `archive_reader::open` succeeds, `metadata().type == archive_type::ba2`, `contains` succeeds for mixed-case lookup, and `extract_bytes` returns the original payload.
  </behavior>
  <action>Add a failing Catch2 test tagged `[ba2_gnrl_reader][ba2_gnrl_end_table]` that writes a synthetic BA2 GNRL file with header+records first, raw payload bytes next, and the length-prefixed filename table last. Set `FileTableOffset` to the first byte of the final name table. Use a path such as `Meshes/EndTable/Alpha.nif`, set packed size `0`, raw size equal to the payload length, and sentinel `0xBAADF00D`. Assert public reopen/list/find/contains/extract behavior only, not diagnostic messages, per D-28.</action>
  <verify>
    <automated>cmake --build --preset windows-msvc-debug-static --target libbsa_tests &amp;&amp; ctest --preset windows-msvc-debug-static -R ba2_gnrl_end_table --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `tests/unit/ba2_gnrl_reader_tests.cpp` contains `[ba2_gnrl_end_table]`.
    - `tests/unit/ba2_gnrl_reader_tests.cpp` contains `0xBAADF00D`.
    - `tests/unit/ba2_gnrl_reader_tests.cpp` contains `FileTableOffset` or `file_table_offset`.
    - The new test fails before parser changes because the current parser rejects end filename tables.
  </acceptance_criteria>
  <done>RED regression captures the required end-of-archive filename table behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 GREEN: Parse exactly file_count names from FileTableOffset</name>
  <files>src/formats/ba2/ba2_gnrl_parser.cpp</files>
  <read_first>
    - src/formats/ba2/ba2_gnrl_parser.cpp
    - tests/unit/ba2_gnrl_reader_tests.cpp
    - .planning/phases/08-ba2-gnrl-write-new-support/08-RESEARCH.md
    - .planning/phases/08-ba2-gnrl-write-new-support/08-PATTERNS.md
  </read_first>
  <action>Replace the host-file parsing assumption that computes filename-table size as `first_payload_offset - FileTableOffset`. Keep record-span and payload-span validation, but seek/read from `FileTableOffset` and parse exactly `file_count` UInt16-prefixed names with existing `read_names`. Bound reads by archive size and stop after `read_names` reports consumed bytes. Preserve duplicate canonical path rejection, original path preservation with `/` separators, and `not_found` behavior per D-24. Do not read payload bytes as metadata and do not allow `FileTableOffset` beyond archive size.</action>
  <verify>
    <automated>cmake --build --preset windows-msvc-debug-static --target libbsa_tests &amp;&amp; ctest --preset windows-msvc-debug-static -R "ba2_gnrl_end_table|ba2_gnrl_reader" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `src/formats/ba2/ba2_gnrl_parser.cpp` no longer contains the exact diagnostic string `BA2 GNRL filename table overlaps payload data` on the host-file end-table path.
    - `src/formats/ba2/ba2_gnrl_parser.cpp` contains `read_names` and validates `file_table_offset` against archive size.
    - The `[ba2_gnrl_end_table]` test passes.
    - Existing `ba2_gnrl_reader` tests pass.
  </acceptance_criteria>
  <done>BA2 GNRL reader accepts both existing generated fixtures and writer-required end filename tables safely.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Archive bytes→BA2 parser | Untrusted offsets, counts, sizes, names, and payload spans are parsed during open. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02-01 | Tampering/DoS | `ba2_gnrl_parser.cpp` offset math | mitigate | Validate `FileTableOffset`, record spans, name-table consumed bytes, and payload spans using checked 64-bit bounds before allocation or metadata creation. |
| T-08-02-02 | Tampering | BA2 names | mitigate | Continue using existing archive path normalization and duplicate canonical path rejection for parsed names. |
</threat_model>

<verification>
Run `ctest --preset windows-msvc-debug-static -R "ba2_gnrl_end_table|ba2_gnrl_reader" --output-on-failure` and `git -C TES5Edit status --short`.
</verification>

<success_criteria>
End filename-table BA2 GNRL archives reopen and extract through public reader APIs without weakening existing BA2 reader coverage.
</success_criteria>

<output>
After completion, create `.planning/phases/08-ba2-gnrl-write-new-support/08-02-SUMMARY.md`.
</output>
