---
phase: 01-foundation-api-boundary-and-test-harness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CMakeLists.txt
  - cmake/libbsaConfig.cmake.in
  - CMakePresets.json
  - vcpkg-configuration.json
  - include/libbsa/version.hpp
  - src/libbsa.cpp
autonomous: true
requirements: [FND-01, FND-02, DOC-04]
must_haves:
  truths:
    - "Maintainer can configure and build a C++20 libbsa target as static or shared per D-19, D-20, and D-21."
    - "Consumer package metadata can be installed/exported without compiling TES5Edit per D-09."
    - "D-22: Build enables high compiler warnings but does not make warnings errors in Phase 1."
    - "D-26: vcpkg dependencies remain directly listed rather than split into manifest features."
  artifacts:
    - path: "CMakeLists.txt"
      provides: "Root CMake project, libbsa target, install/export, and testing entry point"
      contains: "project(libbsa VERSION 0.1.0 LANGUAGES CXX)"
    - path: "CMakePresets.json"
      provides: "Static and shared MSVC Debug presets"
      contains: "windows-msvc-debug-static"
    - path: "cmake/libbsaConfig.cmake.in"
      provides: "Installed package config template"
      contains: "@PACKAGE_INIT@"
  key_links:
    - from: "CMakeLists.txt"
      to: "include/libbsa/version.hpp"
      via: "target_sources FILE_SET HEADERS"
      pattern: "FILE_SET HEADERS"
---

<objective>
Create the CMake/vcpkg build and package foundation for a reusable C++20 `libbsa` library target.

Purpose: Later public API, tests, fixtures, and CI need a real target, static/shared presets, and install/export support before implementation can grow.
Output: Root CMake project, package config template, presets, vcpkg configuration companion, and minimal version/header/source anchors.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-api-boundary-and-test-harness/01-SPEC.md
@.planning/phases/01-foundation-api-boundary-and-test-harness/01-CONTEXT.md
@.planning/phases/01-foundation-api-boundary-and-test-harness/01-RESEARCH.md
@.planning/phases/01-foundation-api-boundary-and-test-harness/01-PATTERNS.md
@vcpkg.json
@.clangd

<decision_traceability>
- D-19: Primary local developer preset targets MSVC Debug static builds.
- D-20: Static and shared configure presets are explicit.
- D-21: Preset names are `windows-msvc-debug-static` and `windows-msvc-debug-shared`.
- D-22: High compiler warnings are enabled, but warnings are not errors in Phase 1.
- D-26: vcpkg dependencies remain directly listed in `vcpkg.json`; no manifest features are introduced for tests.
</decision_traceability>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create root CMake project and install/export package skeleton</name>
  <files>CMakeLists.txt, cmake/libbsaConfig.cmake.in, include/libbsa/version.hpp, src/libbsa.cpp</files>
  <read_first>CMakeLists.txt if it exists, cmake/libbsaConfig.cmake.in if it exists, include/libbsa/version.hpp if it exists, src/libbsa.cpp if it exists, .planning/phases/01-foundation-api-boundary-and-test-harness/01-CONTEXT.md, .planning/phases/01-foundation-api-boundary-and-test-harness/01-SPEC.md</read_first>
  <action>Create `CMakeLists.txt` with `cmake_minimum_required(VERSION 4.0)`, `project(libbsa VERSION 0.1.0 LANGUAGES CXX)`, `include(GNUInstallDirs)`, `include(CMakePackageConfigHelpers)`, `option(LIBBSA_BUILD_TESTS "Build libbsa tests" ON)`, `add_library(libbsa)`, `add_library(libbsa::libbsa ALIAS libbsa)`, `target_compile_features(libbsa PUBLIC cxx_std_20)`, public include directories for build/install interface, warning options for MSVC `/W4` and non-MSVC `-Wall -Wextra -Wpedantic` without warnings-as-errors per D-22, `target_sources(libbsa PUBLIC FILE_SET HEADERS BASE_DIRS include FILES include/libbsa/version.hpp PRIVATE src/libbsa.cpp)`, install/export rules for `libbsaTargets` with namespace `libbsa::`, package version file generation, and `if(LIBBSA_BUILD_TESTS) include(CTest) add_subdirectory(tests) endif()` guarded so the plan can build before tests exist by creating an empty `tests/CMakeLists.txt` only in Plan 03. Create `cmake/libbsaConfig.cmake.in` containing `@PACKAGE_INIT@` and `include("${CMAKE_CURRENT_LIST_DIR}/libbsaTargets.cmake")`. Create `include/libbsa/version.hpp` with Doxygen comments and `namespace libbsa { inline constexpr int version_major = 0; inline constexpr int version_minor = 1; inline constexpr int version_patch = 0; }`. Create `src/libbsa.cpp` with `#include <libbsa/version.hpp>` only. Do not add writer APIs, archive parsing, compression adapters, or `TES5Edit/` references.</action>
  <verify>
    <automated>cmake -S . -B build/phase01-static -DLIBBSA_BUILD_TESTS=OFF -DBUILD_SHARED_LIBS=OFF && cmake --build build/phase01-static --config Debug</automated>
  </verify>
  <acceptance_criteria>
    - `CMakeLists.txt` contains `project(libbsa VERSION 0.1.0 LANGUAGES CXX)`.
    - `CMakeLists.txt` contains `add_library(libbsa)` and `add_library(libbsa::libbsa ALIAS libbsa)`.
    - `CMakeLists.txt` contains `FILE_SET HEADERS` and `install(EXPORT libbsaTargets`.
    - `cmake/libbsaConfig.cmake.in` contains `@PACKAGE_INIT@`.
    - `include/libbsa/version.hpp` contains `inline constexpr int version_major = 0`.
    - `grep -R "TES5Edit" CMakeLists.txt cmake include src` returns no matches.
  </acceptance_criteria>
  <done>Static build can compile a minimal `libbsa` target with tests disabled, install/export package files are defined, and no `TES5Edit/` file is referenced.</done>
</task>

<task type="auto">
  <name>Task 2: Add explicit static/shared presets and vcpkg configuration</name>
  <files>CMakePresets.json, vcpkg-configuration.json</files>
  <read_first>CMakePresets.json if it exists, vcpkg-configuration.json if it exists, vcpkg.json, .planning/phases/01-foundation-api-boundary-and-test-harness/01-CONTEXT.md</read_first>
  <action>Create `CMakePresets.json` schema version 6 with configure presets `windows-msvc-debug-static` and `windows-msvc-debug-shared`. Both presets must set `generator` to `Ninja`, `binaryDir` to `${sourceDir}/build/${presetName}`, `CMAKE_BUILD_TYPE=Debug`, `CMAKE_CXX_STANDARD=20`, `CMAKE_CXX_STANDARD_REQUIRED=ON`, `LIBBSA_BUILD_TESTS=ON`, and `CMAKE_TOOLCHAIN_FILE=$env{VCPKG_ROOT}/scripts/buildsystems/vcpkg.cmake`. The static preset sets `BUILD_SHARED_LIBS=OFF`; the shared preset sets `BUILD_SHARED_LIBS=ON`. Add matching build presets and test presets with the same names. Create `vcpkg-configuration.json` with `default-registry.kind` set to `builtin` and `default-registry.baseline` equal to the existing `vcpkg.json` baseline `12dcccadfe573d0eaa6c67a968413ded7805d256`. Preserve `vcpkg.json` unchanged per D-26.</action>
  <verify>
    <automated>cmake --list-presets</automated>
  </verify>
  <acceptance_criteria>
    - `CMakePresets.json` contains `windows-msvc-debug-static`.
    - `CMakePresets.json` contains `windows-msvc-debug-shared`.
    - `CMakePresets.json` contains `BUILD_SHARED_LIBS` with both `OFF` and `ON` values.
    - `vcpkg-configuration.json` contains `12dcccadfe573d0eaa6c67a968413ded7805d256`.
    - `vcpkg.json` still contains `libdeflate`, `lz4`, `directxtex`, and `catch2`.
  </acceptance_criteria>
  <done>Maintainer can list and invoke explicit static/shared presets, and vcpkg baseline configuration remains reproducible.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer shell→CMake configure | Local environment variables and preset values influence build output. |
| Source tree→install package | Public headers and exported targets become consumer-facing artifacts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | `CMakeLists.txt` / install rules | mitigate | Do not reference `TES5Edit/`; acceptance criteria grep CMake/include/src for `TES5Edit`. |
| T-01-02 | Information Disclosure | Public export package | mitigate | Install only `include/libbsa/*.hpp` file-set headers; no private dependency include directories are public. |
</threat_model>

<verification>
Run `cmake --list-presets`, then run static and shared configure/build after Plan 03 creates test files: `cmake --preset windows-msvc-debug-static && cmake --build --preset windows-msvc-debug-static` and `cmake --preset windows-msvc-debug-shared && cmake --build --preset windows-msvc-debug-shared`.
</verification>

<success_criteria>
FND-01, FND-02, and DOC-04 build-foundation portions are satisfied when static/shared presets exist, `libbsa` builds as C++20, install/export metadata exists, and no `TES5Edit/` files are compiled or referenced.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-api-boundary-and-test-harness/01-01-SUMMARY.md`
</output>
