---
phase: 01-foundation-api-boundary-and-test-harness
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - tests/fixtures/README.md
  - tests/fixtures/generated/source/.gitkeep
  - tests/fixtures/generated/archives/.gitkeep
  - tests/fixtures/local/.gitkeep
autonomous: true
requirements: [FND-07]
must_haves:
  truths:
    - "Maintainer can add legal generated fixtures under committed generated directories per D-27 and D-28."
    - "Local game-derived fixtures are ignored by default and can be supplied through `tests/fixtures/local` or `LIBBSA_GAME_FIXTURES` per D-29 and D-31."
    - "Fixture policy explicitly prohibits using `TES5Edit/` as fixture workspace or committed fixture source per D-33."
    - "D-30: Phase 1 adds fixture README/policy documentation without a detailed manifest format."
    - "D-32: Fixture README requires generator/source recipe, legal provenance, and behavior-proven documentation for committed generated fixtures."
    - "D-35: Tiny legal generated binary fixtures may be committed later only when provenance/recipe is documented."
  artifacts:
    - path: "tests/fixtures/README.md"
      provides: "Fixture legal provenance policy and label taxonomy"
      contains: "TES5Edit/ must not be used as a fixture workspace"
    - path: ".gitignore"
      provides: "Local fixture ignore rule"
      contains: "/tests/fixtures/local/*"
  key_links:
    - from: ".gitignore"
      to: "tests/fixtures/local/.gitkeep"
      via: "ignore exception"
      pattern: "!tests/fixtures/local/.gitkeep"
---

<objective>
Establish the legal fixture directory layout, documentation policy, and local-only fixture ignore boundary.

Purpose: Later archive-format phases need a safe place for tiny generated fixtures and a clear separation from copyrighted local game data and the read-only `TES5Edit/` submodule.
Output: Fixture README, generated fixture directories, ignored local fixture directory, and `.gitignore` rules.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-api-boundary-and-test-harness/01-SPEC.md
@.planning/phases/01-foundation-api-boundary-and-test-harness/01-CONTEXT.md
@.planning/phases/01-foundation-api-boundary-and-test-harness/01-RESEARCH.md
@.gitignore

<decision_traceability>
- D-27: Committed legal fixtures live under `tests/fixtures/generated`.
- D-28: Generated fixture source inputs and archive outputs use `tests/fixtures/generated/source` and `tests/fixtures/generated/archives`.
- D-29: Local-only game-derived fixtures live under ignored `tests/fixtures/local` or outside the repo via `LIBBSA_GAME_FIXTURES`.
- D-30: Phase 1 adds fixture README/policy documentation only, not a detailed manifest format.
- D-31: Tests requiring local game archives are discovered but skipped by default with setup hints.
- D-32: Fixture README requires each committed generated fixture to document generator/source recipe, legal provenance, and behavior it proves.
- D-33: Fixture docs explicitly prohibit using `TES5Edit/` as fixture workspace or source of committed fixture files.
- D-34: README placeholders/taxonomy include `unit`, `fixture`, `roundtrip`, `compat`, `malformed`, `slow`, and `requires-game-fixture`.
- D-35: Tiny legal generated binary fixtures may be committed later only when documented by provenance/recipe.
</decision_traceability>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create fixture directory skeleton with local-only ignore boundary</name>
  <files>.gitignore, tests/fixtures/generated/source/.gitkeep, tests/fixtures/generated/archives/.gitkeep, tests/fixtures/local/.gitkeep</files>
  <read_first>.gitignore, tests/fixtures/generated/source/.gitkeep if it exists, tests/fixtures/generated/archives/.gitkeep if it exists, tests/fixtures/local/.gitkeep if it exists, .planning/phases/01-foundation-api-boundary-and-test-harness/01-CONTEXT.md</read_first>
  <action>Create directories `tests/fixtures/generated/source`, `tests/fixtures/generated/archives`, and `tests/fixtures/local`. Add `.gitkeep` files to all three directories so the intended layout is visible. Append a new `.gitignore` section without deleting existing comments: `# Local game-derived fixtures are not committed`, `/tests/fixtures/local/*`, and `!/tests/fixtures/local/.gitkeep`. This implements D-27 through D-29. Do not place any generated binary fixture data in Phase 1; D-35 says tiny legal generated binaries may be committed later when documented by provenance/recipe.</action>
  <verify>
    <automated>git check-ignore tests/fixtures/local/example.bsa</automated>
  </verify>
  <acceptance_criteria>
    - `.gitignore` contains `/tests/fixtures/local/*`.
    - `.gitignore` contains `!/tests/fixtures/local/.gitkeep`.
    - `tests/fixtures/generated/source/.gitkeep` exists.
    - `tests/fixtures/generated/archives/.gitkeep` exists.
    - `tests/fixtures/local/.gitkeep` exists.
  </acceptance_criteria>
  <done>Committed generated fixture directories and ignored local fixture directory exist without adding copyrighted data.</done>
</task>

<task type="auto">
  <name>Task 2: Document fixture provenance policy and label taxonomy</name>
  <files>tests/fixtures/README.md</files>
  <read_first>tests/fixtures/README.md if it exists, .planning/phases/01-foundation-api-boundary-and-test-harness/01-CONTEXT.md, .planning/phases/01-foundation-api-boundary-and-test-harness/01-SPEC.md, AGENTS.md</read_first>
  <action>Create `tests/fixtures/README.md` with sections `## Committed generated fixtures`, `## Local game-derived fixtures`, `## Provenance requirements`, `## TES5Edit boundary`, and `## Test labels`. Specify that committed legal fixtures live under `tests/fixtures/generated/source` and `tests/fixtures/generated/archives`; local game-derived archives live under ignored `tests/fixtures/local` or outside the repo via `LIBBSA_GAME_FIXTURES`; each committed generated fixture must document generator/source recipe, legal provenance, and behavior it proves per D-32; `TES5Edit/` must not be used as a fixture workspace, mutable test data location, or source of committed fixture files per D-33; labels are exactly `unit`, `fixture`, `roundtrip`, `compat`, `malformed`, `slow`, and `requires-game-fixture` per D-34.</action>
  <verify>
    <automated>python -c "from pathlib import Path; p=Path('tests/fixtures/README.md').read_text(); required=['generated/source','generated/archives','LIBBSA_GAME_FIXTURES','TES5Edit/','requires-game-fixture']; missing=[s for s in required if s not in p]; raise SystemExit('missing '+str(missing) if missing else 0)"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/fixtures/README.md` contains `tests/fixtures/generated/source`.
    - `tests/fixtures/README.md` contains `tests/fixtures/generated/archives`.
    - `tests/fixtures/README.md` contains `LIBBSA_GAME_FIXTURES`.
    - `tests/fixtures/README.md` contains `TES5Edit/ must not be used as a fixture workspace`.
    - `tests/fixtures/README.md` contains all labels: `unit`, `fixture`, `roundtrip`, `compat`, `malformed`, `slow`, `requires-game-fixture`.
  </acceptance_criteria>
  <done>Fixture policy is explicit, grep-verifiable, and prevents accidental copyrighted or TES5Edit-derived fixture commits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local game archives→repo | Copyrighted local fixture data must not cross into committed source. |
| TES5Edit submodule→tests | Read-only reference material must not become mutable fixtures. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | Information Disclosure | `tests/fixtures/local` | mitigate | `.gitignore` ignores all local fixture content except `.gitkeep`. |
| T-01-07 | Tampering | `TES5Edit/` reference boundary | mitigate | README explicitly prohibits using `TES5Edit/` as fixture workspace or committed fixture source. |
</threat_model>

<verification>
Run `git check-ignore tests/fixtures/local/example.bsa` and grep `tests/fixtures/README.md` for provenance, `TES5Edit/`, and label taxonomy strings.
</verification>

<success_criteria>
FND-07 is satisfied when generated fixture locations are committed, local fixture content is ignored, and README policy defines provenance, local data separation, required labels, and the `TES5Edit/` prohibition.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-api-boundary-and-test-harness/01-04-SUMMARY.md`
</output>
