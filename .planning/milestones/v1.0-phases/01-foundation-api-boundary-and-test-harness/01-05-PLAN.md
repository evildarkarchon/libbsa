---
phase: 01-foundation-api-boundary-and-test-harness
plan: 05
type: execute
wave: 4
depends_on: [03, 04]
files_modified:
  - .github/workflows/ci.yml
  - tests/package-consumer/CMakeLists.txt
  - tests/package-consumer/main.cpp
  - tests/CMakeLists.txt
autonomous: true
requirements: [DOC-04, FND-01, FND-02, FND-03, FND-06]
must_haves:
  truths:
    - "Maintainer can run CI for Windows/MSVC static and shared configure/build/test flows per D-23."
    - "Installed-package consumer smoke test proves `find_package(libbsa CONFIG REQUIRED)` and `libbsa::libbsa` consumption per D-09 and D-24."
    - "CI and package consumer do not compile, format, stage, or mutate `TES5Edit/`."
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Windows/MSVC static/shared CI validation"
      contains: "windows-msvc-debug-static"
    - path: "tests/package-consumer/CMakeLists.txt"
      provides: "Installed package consumer smoke project"
      contains: "find_package(libbsa CONFIG REQUIRED)"
    - path: "tests/package-consumer/main.cpp"
      provides: "Consumer include/link smoke test"
      contains: "#include <libbsa/libbsa.hpp>"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "CMakePresets.json"
      via: "preset names"
      pattern: "cmake --preset windows-msvc-debug-static"
    - from: "tests/package-consumer/CMakeLists.txt"
      to: "cmake/libbsaConfig.cmake.in"
      via: "find_package(libbsa CONFIG REQUIRED)"
      pattern: "target_link_libraries\(libbsa_package_consumer PRIVATE libbsa::libbsa\)"
---

<objective>
Add CI and installed-package smoke validation for the completed Phase 1 foundation.

Purpose: The phase is only complete if the reusable library boundary builds in automation and a consumer can use the installed package instead of relying on source-tree internals.
Output: GitHub Actions workflow plus package-consumer CMake smoke test wired into CTest.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-api-boundary-and-test-harness/01-SPEC.md
@.planning/phases/01-foundation-api-boundary-and-test-harness/01-CONTEXT.md
@.planning/phases/01-foundation-api-boundary-and-test-harness/01-RESEARCH.md
@CMakeLists.txt
@CMakePresets.json
@tests/CMakeLists.txt
@tests/fixtures/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add installed-package consumer smoke project</name>
  <files>tests/package-consumer/CMakeLists.txt, tests/package-consumer/main.cpp, tests/CMakeLists.txt</files>
  <read_first>tests/package-consumer/CMakeLists.txt if it exists, tests/package-consumer/main.cpp if it exists, tests/CMakeLists.txt, CMakeLists.txt, include/libbsa/libbsa.hpp</read_first>
  <action>Create `tests/package-consumer/CMakeLists.txt` with `cmake_minimum_required(VERSION 4.0)`, `project(libbsa_package_consumer LANGUAGES CXX)`, `find_package(libbsa CONFIG REQUIRED)`, `add_executable(libbsa_package_consumer main.cpp)`, `target_compile_features(libbsa_package_consumer PRIVATE cxx_std_20)`, and `target_link_libraries(libbsa_package_consumer PRIVATE libbsa::libbsa)`. Create `tests/package-consumer/main.cpp` that includes `<libbsa/libbsa.hpp>`, calls `auto result = libbsa::archive_reader::open("consumer-smoke.bsa");`, and returns 0 only when `!result` and `result.error().code == libbsa::error_code::unsupported`. Add a CTest named `package_consumer_smoke` in `tests/CMakeLists.txt` that installs libbsa to a build-local prefix, configures the consumer with `-DCMAKE_PREFIX_PATH=<install-prefix>`, builds it, and runs it. Use CMake/CTest scripting or separate custom test commands; do not rely on source-tree include paths for the consumer.</action>
  <verify>
    <automated>ctest --preset windows-msvc-debug-static -R package_consumer_smoke --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `tests/package-consumer/CMakeLists.txt` contains `find_package(libbsa CONFIG REQUIRED)`.
    - `tests/package-consumer/CMakeLists.txt` contains `target_link_libraries(libbsa_package_consumer PRIVATE libbsa::libbsa)`.
    - `tests/package-consumer/main.cpp` contains `#include <libbsa/libbsa.hpp>`.
    - `tests/package-consumer/main.cpp` contains `error_code::unsupported`.
    - `tests/CMakeLists.txt` contains `package_consumer_smoke`.
  </acceptance_criteria>
  <done>Installed-package consumer smoke test proves the exported package and public headers work without source-tree internals.</done>
</task>

<task type="auto">
  <name>Task 2: Add Windows/MSVC static and shared CI workflow</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>.github/workflows/ci.yml if it exists, CMakePresets.json, vcpkg.json, .planning/phases/01-foundation-api-boundary-and-test-harness/01-CONTEXT.md</read_first>
  <action>Create `.github/workflows/ci.yml` named `CI` with triggers `push` and `pull_request`. Add one Windows job on `windows-latest` with a matrix containing `windows-msvc-debug-static` and `windows-msvc-debug-shared`. Steps: checkout with submodules enabled but no mutation, set `VCPKG_ROOT` using the runner's vcpkg path or checkout-compatible setup, run `cmake --preset ${{ matrix.preset }}`, `cmake --build --preset ${{ matrix.preset }}`, and `ctest --preset ${{ matrix.preset }} --output-on-failure`. Add a final guard step that runs `git status --short TES5Edit` and fails if output is non-empty. Do not add formatting, staging, or any command that compiles files under `TES5Edit/`.</action>
  <verify>
    <automated>python -c "from pathlib import Path; p=Path('.github/workflows/ci.yml').read_text(); required=['windows-msvc-debug-static','windows-msvc-debug-shared','cmake --preset','cmake --build --preset','ctest --preset','git status --short TES5Edit']; missing=[s for s in required if s not in p]; raise SystemExit('missing '+str(missing) if missing else 0)"</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` contains `windows-msvc-debug-static`.
    - `.github/workflows/ci.yml` contains `windows-msvc-debug-shared`.
    - `.github/workflows/ci.yml` contains `cmake --preset`.
    - `.github/workflows/ci.yml` contains `ctest --preset`.
    - `.github/workflows/ci.yml` contains `git status --short TES5Edit`.
  </acceptance_criteria>
  <done>CI validates static/shared configure, build, and test flows while guarding the read-only `TES5Edit/` boundary.</done>
</task>

<task type="auto">
  <name>Task 3: Run final static/shared foundation verification</name>
  <files>.github/workflows/ci.yml, tests/package-consumer/CMakeLists.txt, tests/package-consumer/main.cpp, tests/CMakeLists.txt</files>
  <read_first>.github/workflows/ci.yml, tests/package-consumer/CMakeLists.txt, tests/package-consumer/main.cpp, tests/CMakeLists.txt, CMakePresets.json</read_first>
  <action>Run the full Phase 1 local validation commands: configure/build/test `windows-msvc-debug-static`; configure/build/test `windows-msvc-debug-shared`; run `ctest -L unit`; run `ctest -R package_consumer_smoke`; run grep gates against public headers for forbidden public dependencies; run `git status --short TES5Edit` and confirm no output. If a command fails because `VCPKG_ROOT` is unset, report the missing environment variable in the plan summary and still leave all files in the correct state; do not edit around it by hardcoding a machine-specific path.</action>
  <verify>
    <automated>cmake --preset windows-msvc-debug-static && cmake --build --preset windows-msvc-debug-static && ctest --preset windows-msvc-debug-static --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - Static preset configure/build/test command exits 0 when vcpkg is available.
    - Shared preset configure/build/test command exits 0 when vcpkg is available.
    - `ctest --preset windows-msvc-debug-static -L unit --output-on-failure` exits 0.
    - `git status --short TES5Edit` prints no changed files.
  </acceptance_criteria>
  <done>DOC-04 and final Phase 1 validation are complete when CI file and local commands prove static/shared build and test behavior without mutating `TES5Edit/`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner→build scripts | CI executes repository-defined commands on hosted infrastructure. |
| Installed package→consumer project | Consumer smoke test uses exported CMake package rather than source-tree internals. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-08 | Tampering | CI checkout and submodule | mitigate | CI includes `git status --short TES5Edit` guard and no formatting/staging commands. |
| T-01-09 | Spoofing | Installed package consumer | mitigate | Consumer uses `find_package(libbsa CONFIG REQUIRED)` and links `libbsa::libbsa`, proving exported target identity. |
</threat_model>

<verification>
Run static and shared preset configure/build/test commands, `ctest -L unit`, `ctest -R package_consumer_smoke`, public-header grep gates, and `git status --short TES5Edit`.
</verification>

<success_criteria>
DOC-04 and remaining build-validation criteria are satisfied when CI and local package-consumer smoke tests prove the library can be built, tested, installed, and consumed in static/shared configurations.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-api-boundary-and-test-harness/01-05-SUMMARY.md`
</output>
