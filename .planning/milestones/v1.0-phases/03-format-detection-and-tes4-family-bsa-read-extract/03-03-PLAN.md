---
phase: 03-format-detection-and-tes4-family-bsa-read-extract
plan: 03
type: execute
wave: 2
depends_on: [03-02]
files_modified:
  - tests/fixtures/generated/archives/malformed_unsupported_version.bsa
  - tests/fixtures/generated/archives/malformed_truncated_header.bsa
  - tests/fixtures/generated/archives/malformed_truncated_table.bsa
  - tests/fixtures/generated/archives/malformed_duplicate_canonical_path.bsa
  - tests/fixtures/generated/archives/malformed_corrupt_compressed_payload.bsa
  - tests/fixtures/generated/archives/malformed_size_mismatch.bsa
  - tests/fixtures/generated/archives/malformed_non_bsa_bytes.bsa
  - tests/fixtures/generated/archives/malformed_manifest.json
  - tests/fixtures/generated/validate_fixture_manifests.py
autonomous: true
requirements: [FMT-01, FMT-06, BSA-07]
must_haves:
  truths:
    - "Default CI has malformed fixtures for unsupported version, truncated header/table, duplicate canonical path, corrupt compressed payload, size mismatch, and non-BSA `.bsa` bytes per D-27."
    - "Manifest validation tests prove malformed case names and expected error categories are machine-readable before parser behavior runs."
    - "Malformed fixtures are generated legal data and do not rely on TES5Edit or game archives per D-23/D-26/D-28."
  artifacts:
    - path: "tests/fixtures/generated/archives/malformed_manifest.json"
      provides: "Malformed fixture case index"
      contains: "duplicate_canonical_path"
    - path: "tests/fixtures/generated/validate_fixture_manifests.py"
      provides: "Manifest schema/case validation script"
  key_links:
    - from: "tests/fixtures/generated/validate_fixture_manifests.py"
      to: "tests/fixtures/generated/archives/malformed_manifest.json"
      via: "manifest schema validation"
      pattern: "malformed_manifest"
---

<objective>
Generate the malformed fixture set and validate manifest shape before parser behavior depends on it.

Purpose: Phase 3 must fail closed on malformed bytes; these cases need committed, legal, deterministic proof inputs independent of implementation behavior.
Output: Malformed `.bsa` files, malformed manifest, and passing manifest validation tests.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-format-detection-and-tes4-family-bsa-read-extract/03-SPEC.md
@.planning/phases/03-format-detection-and-tes4-family-bsa-read-extract/03-CONTEXT.md
@.planning/phases/03-format-detection-and-tes4-family-bsa-read-extract/03-RESEARCH.md
@.planning/phases/03-format-detection-and-tes4-family-bsa-read-extract/03-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate malformed outputs with the C++ fixture generator</name>
  <files>tests/fixtures/generated/archives/malformed_unsupported_version.bsa, tests/fixtures/generated/archives/malformed_truncated_header.bsa, tests/fixtures/generated/archives/malformed_truncated_table.bsa, tests/fixtures/generated/archives/malformed_duplicate_canonical_path.bsa, tests/fixtures/generated/archives/malformed_corrupt_compressed_payload.bsa, tests/fixtures/generated/archives/malformed_size_mismatch.bsa, tests/fixtures/generated/archives/malformed_non_bsa_bytes.bsa, tests/fixtures/generated/archives/malformed_manifest.json</files>
  <read_first>tests/fixtures/generated/generate_tes4_bsa_fixtures.cpp; .planning/phases/03-format-detection-and-tes4-family-bsa-read-extract/03-CONTEXT.md; .planning/phases/03-format-detection-and-tes4-family-bsa-read-extract/03-VALIDATION.md; .planning/phases/03-format-detection-and-tes4-family-bsa-read-extract/03-RESEARCH.md</read_first>
  <action>Run the C++ fixture generator malformed mode created by Plan 03-02 to emit exactly these malformed outputs: unsupported version, truncated header, truncated table, duplicate canonical path, corrupt compressed payload, size mismatch, and non-BSA bytes with a `.bsa` host filename. Emit `malformed_manifest.json` with case IDs, archive filename, requirement/threat references, and expected stable error code (`unsupported`, `format_error`, or extraction-time `format_error` as appropriate). Include duplicate canonical path data that normalizes to one public key per D-06 and corrupt/size mismatch cases that later exercise exact-size decompression per D-17/BSA-07. Use the single C++ generator path per the research-resolved generator language and Plan 03-02 malformed-mode contract; do not create a separate Python malformed generator.</action>
  <verify><automated>cmake --build --preset windows-msvc-debug-static --target generate_tes4_bsa_fixtures; pwsh -NoProfile -Command "$root='tests/fixtures/generated/archives'; $files=@('malformed_unsupported_version.bsa','malformed_truncated_header.bsa','malformed_truncated_table.bsa','malformed_duplicate_canonical_path.bsa','malformed_corrupt_compressed_payload.bsa','malformed_size_mismatch.bsa','malformed_non_bsa_bytes.bsa','malformed_manifest.json'); foreach($f in $files){ if(-not (Test-Path -LiteralPath (Join-Path $root $f))){ throw \"missing $f\" } }; $m=Get-Content -Raw -LiteralPath (Join-Path $root 'malformed_manifest.json'); foreach($token in @('unsupported_version','truncated_header','truncated_table','duplicate_canonical_path','corrupt_compressed_payload','size_mismatch','non_bsa_bytes','format_error')){ if($m -notmatch $token){ throw \"missing token $token\" } }; if((git -C TES5Edit status --short)){ throw 'TES5Edit modified' }"</automated></verify>
  <acceptance_criteria>All malformed `.bsa` files and malformed manifest exist; manifest lists every required malformed case and expected error category; `TES5Edit/` status is empty.</acceptance_criteria>
  <done>Malformed fixture bytes and case metadata are generated deterministically.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add manifest validation script for success and malformed fixtures</name>
  <files>tests/fixtures/generated/validate_fixture_manifests.py</files>
  <read_first>tests/fixtures/generated/archives/tes4_v103_manifest.json; tests/fixtures/generated/archives/tes4_v104_manifest.json; tests/fixtures/generated/archives/tes4_v105_manifest.json; tests/fixtures/generated/archives/malformed_manifest.json; .planning/phases/03-format-detection-and-tes4-family-bsa-read-extract/03-VALIDATION.md</read_first>
  <action>Create `tests/fixtures/generated/validate_fixture_manifests.py` to parse success and malformed manifests with Python's standard `json` module and assert required fields/case IDs are present. This validation script checks fixture data only; do not call `archive_reader::open` or any parser behavior here. Keep CTest registration for API-aware behavior in later plans so this Wave 1 plan does not modify `tests/CMakeLists.txt` owned by Plan 03-01.</action>
  <verify><automated>python tests/fixtures/generated/validate_fixture_manifests.py tests/fixtures/generated/archives</automated></verify>
  <acceptance_criteria>The validation script exits 0; it parses all four manifests and asserts every malformed case ID plus required success metadata fields.</acceptance_criteria>
  <done>Fixture manifests are validated by passing tests without requiring parser implementation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| malformed bytes→future parser | Intentionally invalid archives enter parser tests later. |
| manifest→test expectations | Manifest fields define expected failure modes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | malformed fixtures | mitigate | Include required malformed cases and validate their manifest IDs. |
| T-03-02 | Denial of Service | truncated/size-mismatch cases | mitigate | Fixtures target checked bounds and exact-size validation in later parser/extractor plans. |
| T-03-03 | Tampering | duplicate canonical path case | mitigate | Generate path spellings that normalize to the same canonical key for D-06 testing. |
| T-03-04 | Information Disclosure | fixture provenance | mitigate | Keep generated data outside TES5Edit and verify TES5Edit status. |
</threat_model>

<verification>
Run generator target and `fixture_manifest` tests; verify exact malformed files and case IDs.
</verification>

<success_criteria>
Malformed fixtures and manifest validation tests exist and pass, covering all D-27 cases before implementation selectors use them.
</success_criteria>

<output>
After completion, create `.planning/phases/03-format-detection-and-tes4-family-bsa-read-extract/03-03-SUMMARY.md`
</output>
