---
phase: 03-format-detection-and-tes4-family-bsa-read-extract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - include/libbsa/archive.hpp
  - include/libbsa/result.hpp
  - vcpkg.json
  - tests/CMakeLists.txt
  - tests/unit/archive_reader_tests.cpp
  - tests/unit/public_include_boundary_tests.cpp
autonomous: true
requirements: [FMT-02, FMT-03, FMT-04, FMT-05, FMT-06, BSA-05, BSA-06, BSA-07]
must_haves:
  truths:
    - "archive_reader owns the public Phase 3 surface: metadata, entries, find, contains, extract, and extract_bytes per D-01/D-22."
    - "Public metadata exposes only Phase 3 archive fields and stable entry records with canonical/original paths, sizes, offset, hash, compression, and embedded-name fields per D-02/D-03/D-05/D-18."
    - "Valid missing extraction paths have error_code::not_found while invalid path syntax remains invalid_argument per D-04/D-11/D-20."
    - "The public sink is synchronous, sink-first, and dependency-light per D-12/D-15/D-19/D-21/D-29."
  artifacts:
    - path: "include/libbsa/archive.hpp"
      provides: "Public reader API contracts and value types"
      contains: "class payload_sink"
    - path: "include/libbsa/result.hpp"
      provides: "Stable missing-entry error category"
      contains: "not_found"
    - path: "tests/unit/archive_reader_tests.cpp"
      provides: "Contract/public-boundary tests that compile without parser behavior"
  key_links:
    - from: "include/libbsa/archive.hpp"
      to: "tests/unit/public_include_boundary_tests.cpp"
      via: "public include dependency boundary"
      pattern: "payload_sink"
---

<objective>
Define the Phase 3 public reader contracts and test-only JSON manifest dependency wiring before parser behavior exists.

Purpose: Later plans need a compile-stable public boundary so detector, parser, lookup, and extraction work can target explicit contracts instead of inventing APIs.
Output: Public value types, reader method declarations, `not_found`, synchronous sink contract, compile-only contract tests, and private test-only JSON linkage.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-format-detection-and-tes4-family-bsa-read-extract/03-SPEC.md
@.planning/phases/03-format-detection-and-tes4-family-bsa-read-extract/03-CONTEXT.md
@.planning/phases/03-format-detection-and-tes4-family-bsa-read-extract/03-RESEARCH.md
@.planning/phases/03-format-detection-and-tes4-family-bsa-read-extract/03-PATTERNS.md
@include/libbsa/archive.hpp
@include/libbsa/result.hpp
@tests/CMakeLists.txt
@tests/unit/public_include_boundary_tests.cpp

<interfaces>
Current `error_code` has `unsupported`, `invalid_argument`, `io_error`, and `format_error`; add `not_found` per D-20.
Current `archive_reader` only has `open(std::string_view)`; extend the same class with methods per D-01 rather than adding separate public view/extractor objects.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add public reader value and sink contracts</name>
  <files>include/libbsa/archive.hpp, include/libbsa/result.hpp, tests/unit/public_include_boundary_tests.cpp</files>
  <read_first>include/libbsa/archive.hpp; include/libbsa/result.hpp; tests/unit/public_include_boundary_tests.cpp; .planning/phases/03-format-detection-and-tes4-family-bsa-read-extract/03-CONTEXT.md; .planning/phases/03-format-detection-and-tes4-family-bsa-read-extract/03-PATTERNS.md</read_first>
  <behavior>
    - `archive_metadata` exposes archive type/variant, version, archive flags, file count, and supported compression behavior only per D-02.
    - `entry_metadata` exposes `path`, `original_path`, raw size, stored size, payload offset, TES4 hash, compression, record flags, `has_embedded_name`, and `embedded_name_prefix_size` per D-03/D-05/D-18.
    - `payload_sink::write(std::span<const std::byte>)` is synchronous and returns accepted byte count per D-12/D-15/D-21.
  </behavior>
  <action>In `include/libbsa/archive.hpp`, add Doxygen-documented public enums and value structs for Phase 3 metadata: `archive_type`, `archive_variant`, `entry_compression`, `archive_metadata`, `entry_metadata`, and an abstract `payload_sink` interface. Use only standard C++20 headers plus `libbsa/result.hpp`; do not expose `detail::`, libdeflate, lz4, DirectXTex, Windows SDK, TES5Edit, `std::filesystem::path` for archive-internal paths, or `std::expected`. In `include/libbsa/result.hpp`, add `error_code::not_found` per D-20. Update `tests/unit/public_include_boundary_tests.cpp` so boundary checks still reject private dependency tokens after the new declarations.</action>
  <verify><automated>cmake --build --preset windows-msvc-debug-static && ctest --preset windows-msvc-debug-static -R "public_include_boundary" --output-on-failure</automated></verify>
  <acceptance_criteria>Public contracts compile; boundary test passes; `archive.hpp` contains `archive_metadata`, `entry_metadata`, and `payload_sink`; `result.hpp` contains `not_found`; no private dependency or TES5Edit tokens leak through installed public headers.</acceptance_criteria>
  <done>Public metadata, sink, and missing-entry error contracts exist and preserve the dependency-light C++20 boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Declare archive_reader methods and compile-only contract tests</name>
  <files>include/libbsa/archive.hpp, tests/CMakeLists.txt, tests/unit/archive_reader_tests.cpp, vcpkg.json</files>
  <read_first>include/libbsa/archive.hpp; tests/CMakeLists.txt; tests/unit/archive_reader_tests.cpp; vcpkg.json; .planning/phases/03-format-detection-and-tes4-family-bsa-read-extract/03-VALIDATION.md</read_first>
  <behavior>
    - `archive_reader` declares `metadata()`, `entries()`, `find(std::string_view)`, `contains(std::string_view)`, `extract(std::string_view, payload_sink&)`, and `extract_bytes(std::string_view)` per D-01/D-04/D-12/D-13/D-14/D-22.
    - Contract tests prove declarations and public includes compile without requiring a detector/parser implementation.
    - `nlohmann-json` is wired only to test targets as the documented D-29 exception and never to `libbsa` runtime/public linkage.
  </behavior>
  <action>Extend `archive_reader` declarations in `include/libbsa/archive.hpp` with the Phase 3 methods. If implementation stubs are needed for linking, keep them returning `unsupported` until Plan 03-04 wires real state; do not run behavior selectors in this plan. Add `nlohmann-json` to `vcpkg.json` as a test/tool-only dependency per D-29, then update `tests/CMakeLists.txt` with `find_package(nlohmann_json CONFIG REQUIRED)` and a PRIVATE link from `libbsa_tests` only. Update `tests/unit/archive_reader_tests.cpp` with compile-only/contract tests for method signatures, `not_found`, and sink type shape; preserve the empty-host-path `invalid_argument` test and do not assert public TES4 detection behavior yet.</action>
  <verify><automated>cmake --build --preset windows-msvc-debug-static --target libbsa_tests && ctest --preset windows-msvc-debug-static -R "archive_reader|public_include_boundary" --output-on-failure</automated></verify>
  <acceptance_criteria>Build succeeds; archive_reader contract tests pass without parser behavior; `nlohmann_json::nlohmann_json` is linked PRIVATE to `libbsa_tests`; `libbsa` target is not linked to nlohmann-json; existing empty path validation still passes.</acceptance_criteria>
  <done>The public reader API and test-only manifest dependency are ready for fixture and implementation plans without intentional RED failures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public header→consumer | Installed headers must not leak private dependencies or implementation details. |
| caller→archive_reader | Callers provide host paths, archive virtual paths, and sink implementations. |
| tests→JSON manifests | Test tooling parses generated manifests with a test-only dependency. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | public headers | mitigate | Keep public API to standard C++20/libbsa-owned types and run public include boundary tests. |
| T-03-02 | Tampering | path-taking method contracts | mitigate | Declare result-returning `find`/`contains`/`extract`; later plans must normalize and reject invalid paths. |
| T-03-03 | Denial of Service | `extract_bytes` contract | mitigate | Document bounded per-entry helper; implementation validates metadata before allocation in Plan 03-06. |
| T-03-04 | Tampering / DoS | `payload_sink` | mitigate | Sink reports accepted count so Plan 03-06 can fail partial writes as `io_error`. |
| T-03-05 | Information Disclosure | nlohmann-json dependency | mitigate | Link JSON only to `libbsa_tests` PRIVATE and keep it out of public headers/runtime target. |
</threat_model>

<verification>
Run build, public include boundary, and contract-only archive_reader tests. Do not run TES4 behavior selectors until fixtures and usable reader state exist.
</verification>

<success_criteria>
The public Phase 3 contracts compile, are documented, remain dependency-light, include `not_found`, and provide the exact reader-centric API required by D-01 through D-22.
</success_criteria>

<output>
After completion, create `.planning/phases/03-format-detection-and-tes4-family-bsa-read-extract/03-01-SUMMARY.md`
</output>
