---
phase: 03-format-detection-and-tes4-family-bsa-read-extract
plan: 06
type: tdd
wave: 5
depends_on: [03-05]
files_modified:
  - src/archive.cpp
  - src/formats/bsa/tes4_bsa_reader.hpp
  - src/formats/bsa/tes4_bsa_reader.cpp
  - tests/unit/tes4_bsa_reader_tests.cpp
autonomous: true
requirements: [BSA-01, BSA-02, BSA-03, BSA-06, BSA-07]
must_haves:
  truths:
    - "Consumers extract by path string first through `extract(path, sink)` or equivalent, without requiring entry handles, per D-14."
    - "Consumers can extract v103 raw and deflate entries through sink-first API and bounded extract_bytes per D-13."
    - "Consumers can extract v104 raw and deflate entries, including embedded-name entries whose prefix is skipped."
    - "Consumers can extract v105 raw and LZ4-frame entries."
    - "Compressed extraction uses one-entry bounded buffers around exact-size decompression and never whole-archive loading per D-16."
    - "Corrupt compressed payloads and exact-size mismatches fail with format_error per D-17; partial sink writes fail with io_error; valid missing paths fail with not_found."
  artifacts:
    - path: "src/formats/bsa/tes4_bsa_reader.cpp"
      provides: "TES4-family extraction routing"
      contains: "decompress_payload_exact"
    - path: "src/archive.cpp"
      provides: "Public extract/extract_bytes delegation"
      contains: "extract_bytes"
  key_links:
    - from: "tes4_bsa_reader.cpp"
      to: "src/detail/compression_router.hpp"
      via: "exact-size deflate/LZ4-frame routing"
      pattern: "decompress_payload_exact"
    - from: "archive_reader::extract"
      to: "payload_sink::write"
      via: "synchronous full-chunk sink writes"
      pattern: "write\("
---

<objective>
Implement TES4-family extraction routing for raw, deflate, LZ4-frame, embedded-name, sink errors, `extract_bytes`, and missing paths.

Purpose: This completes the Phase 3 consumer-visible read/extract slice for TES4/Oblivion, FO3/FNV/Skyrim LE, and Skyrim SE/AE BSA archives.
Output: Green extraction, compression routing, embedded-name, sink error, not_found, and full Phase 3 verification tests.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-format-detection-and-tes4-family-bsa-read-extract/03-SPEC.md
@.planning/phases/03-format-detection-and-tes4-family-bsa-read-extract/03-CONTEXT.md
@.planning/phases/03-format-detection-and-tes4-family-bsa-read-extract/03-RESEARCH.md
@.planning/phases/03-format-detection-and-tes4-family-bsa-read-extract/03-PATTERNS.md
@include/libbsa/archive.hpp
@src/detail/compression_router.hpp
@src/detail/payload_stream.hpp
@src/formats/bsa/tes4_bsa_reader.cpp
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement sink extraction for raw, deflate, and LZ4-frame payloads</name>
  <files>src/formats/bsa/tes4_bsa_reader.hpp, src/formats/bsa/tes4_bsa_reader.cpp, src/archive.cpp, tests/unit/tes4_bsa_reader_tests.cpp</files>
  <read_first>src/formats/bsa/tes4_bsa_reader.cpp; src/detail/compression_router.hpp; src/detail/payload_stream.hpp; include/libbsa/archive.hpp; tests/unit/tes4_bsa_reader_tests.cpp; .planning/phases/03-format-detection-and-tes4-family-bsa-read-extract/03-RESEARCH.md</read_first>
  <behavior>
    - Raw entries write exactly consumer-visible payload bytes to the sink.
    - v103/v104 compressed entries read the 4-byte uncompressed-size prefix and route remaining bytes to `compression_method::deflate`.
    - v105 compressed entries read the 4-byte uncompressed-size prefix and route remaining bytes to `compression_method::lz4_frame`.
    - Partial sink acceptance returns `error_code::io_error` per D-15.
  </behavior>
  <action>Implement `archive_reader::extract(path, payload_sink&)` delegation and private TES4 extraction in `tes4_bsa_reader.*`; extraction identifies entries by path string first and must not require callers to obtain an entry handle per D-14. Reuse Plan 03-05 lookup normalization: invalid path -> `invalid_argument`, valid missing path -> `not_found` per D-20. Read only the selected entry payload range from archive bytes/file. For compressed entries, consume the little-endian expected-size prefix, subtract it from the stored span, and call `detail::decompress_payload_exact` using one-entry bounded buffers, never whole-archive loading, per D-16; use `deflate` for v103/v104 and `lz4_frame` for v105 per BSA-07. Corrupt compressed payloads or exact-size decompression mismatches must return `format_error` per D-17. For raw entries, write bounded chunks directly. Check every sink write result and return `io_error` if accepted count differs from requested bytes. Do not add filesystem extraction, async callbacks, progress, cancellation, or retained sink lifetimes per D-19/D-21.</action>
  <verify><automated>cmake --build --preset windows-msvc-debug-static && ctest --preset windows-msvc-debug-static -R "tes4_bsa_v103_extract|tes4_bsa_v104_extract|tes4_bsa_v105_extract|tes4_bsa_compression_routing|tes4_bsa_sink_errors" --output-on-failure</automated></verify>
  <acceptance_criteria>v103/v104/v105 extraction selectors pass for raw and compressed entries; extraction is addressed by path string without entry handles per D-14; compressed extraction uses per-entry bounded buffering only per D-16; corrupt compressed and exact-size mismatch cases return `format_error` per D-17; partial sink test returns `io_error`; missing valid path returns `not_found`.</acceptance_criteria>
  <done>Sink-first extraction works for raw, deflate, and LZ4-frame payloads with required error handling.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement embedded-name skipping and bounded extract_bytes</name>
  <files>src/formats/bsa/tes4_bsa_reader.cpp, src/archive.cpp, tests/unit/tes4_bsa_reader_tests.cpp</files>
  <read_first>src/formats/bsa/tes4_bsa_reader.cpp; include/libbsa/archive.hpp; tests/unit/tes4_bsa_reader_tests.cpp; .planning/phases/03-format-detection-and-tes4-family-bsa-read-extract/03-CONTEXT.md; .planning/phases/03-format-detection-and-tes4-family-bsa-read-extract/03-RESEARCH.md</read_first>
  <behavior>
    - Embedded-name extraction skips the already-materialized prefix size and returns only consumer-visible file bytes per D-18/BSA-06.
    - `extract_bytes(path)` is implemented through the sink extraction path with a bounded vector sink per D-13.
    - Missing/invalid path semantics match `extract`.
  </behavior>
  <action>Use the `has_embedded_name` and `embedded_name_prefix_size` materialized in Plan 03-05 to advance the payload cursor and reduce the stored payload span before raw copy or compressed-prefix handling. Do not rediscover embedded-name metadata only during extraction. Implement `extract_bytes(path)` as a convenience helper backed by a local public-sink adapter that appends to `std::vector<std::byte>` after metadata-derived per-entry size validation; keep it synchronous and per-entry bounded. Add tests asserting `entries()`/`find()` expose embedded metadata before extraction and extracted bytes omit the prefix.</action>
  <verify><automated>cmake --build --preset windows-msvc-debug-static && ctest --preset windows-msvc-debug-static -R "tes4_bsa_embedded_name|tes4_bsa_entry_metadata|tes4_bsa_v103_extract|tes4_bsa_v104_extract|tes4_bsa_v105_extract|tes4_bsa_lookup" --output-on-failure</automated></verify>
  <acceptance_criteria>Embedded-name tests pass; entry metadata assertions occur before extraction; bounded `extract_bytes(path)` byte-compares expected fixture bytes per D-13 and reuses path-string extraction semantics per D-14; no public filesystem extraction helper exists.</acceptance_criteria>
  <done>Embedded-name extraction and bounded byte convenience extraction meet the locked contract.</done>
</task>

<task type="auto">
  <name>Task 3: Run final Phase 3 verification</name>
  <files>tests/unit/tes4_bsa_reader_tests.cpp, tests/unit/public_include_boundary_tests.cpp</files>
  <read_first>tests/unit/tes4_bsa_reader_tests.cpp; tests/unit/public_include_boundary_tests.cpp; .planning/phases/03-format-detection-and-tes4-family-bsa-read-extract/03-VALIDATION.md; .planning/phases/03-format-detection-and-tes4-family-bsa-read-extract/03-SPEC.md</read_first>
  <action>Run the full Phase 3 validation suite and fix only Phase 3-owned failures without relaxing tests to hide missing behavior. Verify every 03-SPEC acceptance item is covered by passing tests: detection, archive metadata, listing, lookup, entry metadata, v103/v104/v105 extraction, embedded-name behavior, malformed/corrupt/size mismatch handling, public boundary, and TES5Edit read-only status. If a generated fixture expectation is wrong, update generator and manifest together with a provenance note.</action>
  <verify><automated>cmake --build --preset windows-msvc-debug-static && ctest --preset windows-msvc-debug-static --output-on-failure && pwsh -NoProfile -Command "$status = git -C TES5Edit status --short; if ($status) { throw \"TES5Edit has unexpected changes: $status\" }"</automated></verify>
  <acceptance_criteria>Full CTest exits 0; the PowerShell TES5Edit status gate fails on any non-empty `git -C TES5Edit status --short` output; public include boundary remains green; all Phase 3 selectors from `03-VALIDATION.md` pass.</acceptance_criteria>
  <done>Phase 3 reader/extraction slice is fully green under automated validation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| parsed payload metadata→payload reader | Untrusted offsets/sizes select archive byte ranges. |
| compressed bytes→codec router | Untrusted compressed payloads enter libdeflate/LZ4-frame adapters. |
| extraction→caller sink | Caller-controlled sink receives extracted bytes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | payload cursor | mitigate | Validate payload spans and embedded-name prefix size before reading. |
| T-03-02 | Denial of Service | `extract_bytes` / decompression buffers | mitigate | Allocate only per-entry expected size after parser-validated metadata; reject mismatches. |
| T-03-03 | Tampering | compression decode | mitigate | Use `decompress_payload_exact`; corrupt streams and exact-size mismatches return `format_error`. |
| T-03-04 | Tampering / DoS | caller sink | mitigate | Treat partial accepted writes as `io_error` and never report partial success. |
| T-03-05 | Tampering | extraction path input | mitigate | Reuse normalized lookup; invalid path -> `invalid_argument`, valid missing path -> `not_found`. |
</threat_model>

<verification>
Run extraction, embedded-name, malformed compression, sink error, public boundary, full CTest, and TES5Edit status checks.
</verification>

<success_criteria>
Consumers can extract raw, deflate, LZ4-frame, and embedded-name TES4-family BSA entries by path through sink-first API and bounded `extract_bytes`, with all malformed/partial/missing cases failing closed.
</success_criteria>

<output>
After completion, create `.planning/phases/03-format-detection-and-tes4-family-bsa-read-extract/03-06-SUMMARY.md`
</output>
