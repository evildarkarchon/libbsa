---
phase: 03-format-detection-and-tes4-family-bsa-read-extract
plan: 04
type: tdd
wave: 3
depends_on: [03-01, 03-02, 03-03]
files_modified:
  - CMakeLists.txt
  - src/archive.cpp
  - src/formats/bsa/bsa_format_detector.hpp
  - src/formats/bsa/bsa_format_detector.cpp
  - src/formats/bsa/tes4_bsa_parser.hpp
  - src/formats/bsa/tes4_bsa_parser.cpp
  - tests/unit/tes4_bsa_reader_tests.cpp
autonomous: true
requirements: [FMT-01, FMT-02, FMT-06]
must_haves:
  truths:
    - "archive_reader::open returns a usable reader state for valid v103/v104/v105 BSA bytes before public detection selectors run."
    - "Detection is byte-driven and ignores host filename extension per FMT-01."
    - "The private detector/parser seam supports future archive families without public dependency leakage per FMT-06."
  artifacts:
    - path: "src/formats/bsa/bsa_format_detector.cpp"
      provides: "BSA magic/version classifier"
      contains: "0x69"
    - path: "src/formats/bsa/tes4_bsa_parser.cpp"
      provides: "Minimal checked open-state parser"
      contains: "binary_reader"
    - path: "src/archive.cpp"
      provides: "archive_reader::open detector/parser delegation and owned state"
      contains: "archive_reader::open"
  key_links:
    - from: "src/archive.cpp"
      to: "src/formats/bsa/bsa_format_detector.cpp"
      via: "byte-driven open classification"
      pattern: "detect.*bsa"
    - from: "src/archive.cpp"
      to: "archive_reader::metadata"
      via: "usable reader state returned from open"
      pattern: "metadata\(\) const"
---

<objective>
Implement byte-driven TES4-family detection plus a usable public reader state that exposes variant/version/file-count metadata.

Purpose: Public detection selectors must run only after `archive_reader::open` can return a reader with enough state for metadata inspection, avoiding the checker-blocked order where tests verified selectors before open state existed.
Output: Private detector, parser skeleton, CMake source registration, and green detection/metadata-open tests.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-format-detection-and-tes4-family-bsa-read-extract/03-SPEC.md
@.planning/phases/03-format-detection-and-tes4-family-bsa-read-extract/03-CONTEXT.md
@.planning/phases/03-format-detection-and-tes4-family-bsa-read-extract/03-RESEARCH.md
@.planning/phases/03-format-detection-and-tes4-family-bsa-read-extract/03-PATTERNS.md
@include/libbsa/archive.hpp
@src/archive.cpp
@src/detail/binary_io.hpp
@CMakeLists.txt
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create private byte-driven detector and open-state skeleton</name>
  <files>CMakeLists.txt, src/formats/bsa/bsa_format_detector.hpp, src/formats/bsa/bsa_format_detector.cpp, src/formats/bsa/tes4_bsa_parser.hpp, src/formats/bsa/tes4_bsa_parser.cpp, src/archive.cpp, tests/unit/tes4_bsa_reader_tests.cpp</files>
  <read_first>CMakeLists.txt; src/archive.cpp; include/libbsa/archive.hpp; src/detail/binary_io.hpp; tests/unit/tes4_bsa_reader_tests.cpp; .planning/phases/03-format-detection-and-tes4-family-bsa-read-extract/03-RESEARCH.md; .planning/phases/03-format-detection-and-tes4-family-bsa-read-extract/03-PATTERNS.md</read_first>
  <behavior>
    - `BSA\0` + `0x67` maps to TES4/Oblivion v103, `0x68` to FO3/FNV/Skyrim LE v104, and `0x69` to Skyrim SE/AE v105.
    - Non-BSA bytes fail `format_error`; recognizable unsupported BSA versions fail `unsupported`.
    - `archive_reader::open` constructs state with archive variant, version, flags, and file count before any public detection selector is executed.
  </behavior>
  <action>First create/update `tests/unit/tes4_bsa_reader_tests.cpp` with failing tests registered under the selectors `tes4_bsa_detection`, `tes4_bsa_metadata`, and `unsupported_future_bsa` for v103/v104/v105 success fixtures plus unsupported/non-BSA malformed cases. Then create private `src/formats/bsa/bsa_format_detector.hpp/.cpp` and `src/formats/bsa/tes4_bsa_parser.hpp/.cpp` using the namespaces and source-registration patterns from `03-PATTERNS.md`. The detector reads bytes, not filename extension, and returns a small detected variant/version struct. The parser skeleton should use `detail::binary_reader` to validate the TES4-family header and enough table count fields to produce `archive_metadata` with variant, version, flags, file_count, and supported compression behavior per D-02. In `src/archive.cpp`, replace the unconditional non-empty `unsupported` stub with file-byte read, detector call, parser skeleton call, and owned reader state used by `metadata()`. Preserve empty host path `invalid_argument`; do not implement entries/lookup/extraction yet.</action>
  <verify><automated>cmake --build --preset windows-msvc-debug-static && ctest --preset windows-msvc-debug-static -R "tes4_bsa_detection|tes4_bsa_metadata|unsupported_future_bsa|public_include_boundary" --output-on-failure</automated></verify>
  <acceptance_criteria>Detection and metadata-open tests pass for v103/v104/v105 success fixtures and malformed unsupported/non-BSA cases; `src/archive.cpp` has no extension-based format truth; detector and parser sources are registered PRIVATE in CMake.</acceptance_criteria>
  <done>Valid fixtures open into usable reader state and report archive-level detection metadata.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harden skeleton bounds checks for malformed open cases</name>
  <files>src/formats/bsa/tes4_bsa_parser.cpp, tests/unit/tes4_bsa_reader_tests.cpp</files>
  <read_first>src/formats/bsa/tes4_bsa_parser.cpp; tests/unit/tes4_bsa_reader_tests.cpp; tests/fixtures/generated/archives/malformed_manifest.json; .planning/phases/03-format-detection-and-tes4-family-bsa-read-extract/03-VALIDATION.md</read_first>
  <behavior>
    - Truncated header/table fixtures fail with `error_code::format_error` without out-of-bounds reads.
    - Count/offset fields that cannot fit within archive bytes fail before allocation.
    - Missing-name/hash-only behavior is deferred to full table parsing in Plan 03-05 but must not be exposed as usable entries here.
  </behavior>
  <action>Add malformed open tests to `tests/unit/tes4_bsa_reader_tests.cpp` for unsupported version, non-BSA bytes, truncated header, and truncated table using the Plan 03-03 manifest. Harden `tes4_bsa_parser.cpp` so checked reads and count-derived span validation fail closed with structured errors. Keep the public reader state limited to metadata until Plan 03-05 builds entries and duplicate validation; do not run `tes4_bsa_entry_metadata` yet.</action>
  <verify><automated>cmake --build --preset windows-msvc-debug-static && ctest --preset windows-msvc-debug-static -R "tes4_bsa_detection|tes4_bsa_metadata|unsupported_future_bsa|tes4_bsa_malformed_open|public_include_boundary" --output-on-failure</automated></verify>
  <acceptance_criteria>Malformed open selectors pass; parser uses `binary_reader`; tests assert stable error codes rather than diagnostic strings; `git -C TES5Edit status --short` prints nothing.</acceptance_criteria>
  <done>Detector/open-state behavior passes success and malformed-open validation without exposing incomplete entry metadata.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host file bytes→detector/parser | Untrusted archive bytes determine format and header fields. |
| parser state→public metadata | Parsed fields become consumer-visible values. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | detector | mitigate | Validate `BSA\0` magic/version from bytes and reject non-BSA/unsupported versions with stable errors. |
| T-03-02 | Denial of Service | parser skeleton | mitigate | Use checked reads and validate count/offset-derived spans before allocation. |
| T-03-03 | Information Disclosure | public boundary | mitigate | Keep detector/parser private and run public include boundary tests. |
| T-03-04 | Tampering | compression metadata | mitigate | Report explicit supported compression behavior from version, never infer from extension. |
</threat_model>

<verification>
Run only detection, archive metadata, unsupported future, malformed-open, and public boundary selectors after usable open state exists.
</verification>

<success_criteria>
`archive_reader::open` returns a usable reader for v103/v104/v105 fixture bytes and reports detected metadata; unsupported/truncated/non-BSA inputs fail closed with structured errors.
</success_criteria>

<output>
After completion, create `.planning/phases/03-format-detection-and-tes4-family-bsa-read-extract/03-04-SUMMARY.md`
</output>
