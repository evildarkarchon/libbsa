---
phase: 10-tes3-write-support-and-bsa-format-completeness
plan: 02
type: tdd
wave: 2
depends_on: [10-01]
files_modified:
  - src/formats/bsa/tes3_bsa_writer.hpp
  - src/formats/bsa/tes3_bsa_writer.cpp
  - tests/unit/tes3_bsa_writer_tests.cpp
  - tests/CMakeLists.txt
autonomous: true
requirements: [WBSA-04]
requirements_addressed: [WBSA-04]
must_haves:
  truths:
    - "D-05, D-07: TES3 writer copies memory entries at add time, keeps disk entries path-backed until write_to, and leaves write_to logically non-consuming so disk entries may be reread."
    - "D-06, D-17: TES3 writer validates archive paths and non-empty host paths at add_file, reports missing disk sources from write_to, and uses stable result error codes."
    - "D-08: TES3 writer never publishes a partial successful archive when source preparation or output publishing fails."
  artifacts:
    - path: "tests/unit/tes3_bsa_writer_tests.cpp"
      provides: "source ownership, validation, and safe publish behavior tests"
      contains: "[tes3_bsa_writer]"
    - path: "src/formats/bsa/tes3_bsa_writer.cpp"
      provides: "finalization validation, late disk read, duplicate detection, and safe publish"
      contains: "validate_entries"
  key_links:
    - from: "tes3_bsa_writer::write_to"
      to: "write_tes3_bsa_archive"
      via: "const state entries span"
      pattern: "write_tes3_bsa_archive\(state_->options, state_->entries"
    - from: "src/formats/bsa/tes3_bsa_writer.cpp"
      to: "filesystem output"
      via: "unique temp publish directory and overwrite option"
      pattern: "libbsa-tmp"
---

<objective>
Implement TES3 writer source ownership, finalization validation, and safe publish behavior before byte-level TES3 serialization is added.

Purpose: Satisfies D-05 through D-08 and D-17 while preventing partial output publication and matching established writer result/error style.
Output: Focused TES3 writer tests, test registration, late disk source reading, duplicate detection, overwrite handling, and safe output publishing behavior.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-tes3-write-support-and-bsa-format-completeness/10-SPEC.md
@.planning/phases/10-tes3-write-support-and-bsa-format-completeness/10-CONTEXT.md
@.planning/phases/10-tes3-write-support-and-bsa-format-completeness/10-RESEARCH.md
@.planning/phases/10-tes3-write-support-and-bsa-format-completeness/10-PATTERNS.md
@src/formats/bsa/tes3_bsa_writer.hpp
@src/formats/bsa/tes3_bsa_writer.cpp
@src/formats/bsa/tes4_bsa_writer.cpp
@src/formats/ba2/ba2_gnrl_writer.cpp
@src/formats/ba2/ba2_publish.hpp
@tests/unit/tes4_bsa_writer_tests.cpp
@tests/unit/ba2_dx10_writer_tests.cpp
@tests/CMakeLists.txt
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED validation, ownership, and publish tests</name>
  <files>tests/unit/tes3_bsa_writer_tests.cpp, tests/CMakeLists.txt</files>
  <read_first>
    - `.planning/phases/10-tes3-write-support-and-bsa-format-completeness/10-SPEC.md`
    - `.planning/phases/10-tes3-write-support-and-bsa-format-completeness/10-CONTEXT.md`
    - `tests/unit/tes4_bsa_writer_tests.cpp`
    - `tests/unit/ba2_dx10_writer_tests.cpp`
    - `tests/CMakeLists.txt`
  </read_first>
  <behavior>
    - `add_bytes` copies caller memory so mutating the original vector before `write_to` does not alter output.
    - `add_file` accepts a missing non-empty host path at add time and reports `error_code::io_error` from `write_to` per D-06.
    - Invalid archive paths return `error_code::invalid_argument` from add methods.
    - Duplicate canonical paths return `error_code::format_error` from `write_to`.
    - Empty writer finalization returns `error_code::invalid_argument`.
    - Existing output is preserved when `overwrite_existing == false` and replaced when `overwrite_existing == true`.
    - Caller-owned `<output>.tmp` sibling files are preserved.
  </behavior>
  <action>Create `tests/unit/tes3_bsa_writer_tests.cpp` with Catch2 tag `[unit][tes3_bsa_writer]` and register it in `tests/CMakeLists.txt`. Use temp directory `libbsa_tes3_bsa_writer_tests`. Add helper functions `write_binary_file`, `read_binary_file`, and `sample_bytes`. Write the RED tests listed in the behavior block using stable `error_code` assertions only; do not assert diagnostic text per D-17. Run the focused TES3 writer test and confirm it fails because finalization/publish behavior is not implemented.</action>
  <verify>
    <automated>cmake --build build --target libbsa_tests &amp;&amp; ctest --test-dir build -R tes3_bsa_writer --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `tests/unit/tes3_bsa_writer_tests.cpp` contains `[unit][tes3_bsa_writer]`.
    - The test file contains `error_code::invalid_argument`, `error_code::format_error`, and `error_code::io_error` assertions.
    - The test file contains an overwrite-disabled preservation assertion comparing existing output bytes to a sentinel vector.
    - The test file contains a `<output>.tmp` sibling preservation assertion.
    - RED run fails before production validation/publish behavior is implemented.
  </acceptance_criteria>
  <done>Failing tests specify Phase 10 source ownership, validation, and safe publish behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN finalization validation and safe publish behavior</name>
  <files>src/formats/bsa/tes3_bsa_writer.hpp, src/formats/bsa/tes3_bsa_writer.cpp</files>
  <read_first>
    - `src/formats/bsa/tes3_bsa_writer.hpp`
    - `src/formats/bsa/tes3_bsa_writer.cpp`
    - `src/formats/ba2/ba2_gnrl_writer.cpp`
    - `src/formats/ba2/ba2_publish.hpp`
    - `src/detail/archive_path.hpp`
    - `.planning/phases/10-tes3-write-support-and-bsa-format-completeness/10-RESEARCH.md`
  </read_first>
  <action>Implement `validate_entries` to reject empty entry lists and duplicate `archive_path_canonical` values with stable `error_code` values. Implement `read_source_bytes` so memory entries return copied bytes and disk entries open/read `entry.host_path` only during `write_to`; open/read failures return `error_code::io_error`. Implement safe output publishing in `write_tes3_bsa_archive`: reject empty output host path, reject existing non-regular destinations, refuse existing output unless `options.overwrite_existing` is true, write only to a unique sibling temp directory named with `.libbsa-tmp-N`, preserve caller-owned `<output>.tmp`, and when overwriting move the existing output to a unique backup and restore it if publish fails. For this plan, serialize the valid TES3 archive structure needed by the validation tests to observe output replacement; Plan 03 owns the full byte-level hash/name/offset/payload proof. Do not publish anything until all entries have been validated and source bytes have been prepared per D-08.</action>
  <verify>
    <automated>cmake --build build --target libbsa_tests &amp;&amp; ctest --test-dir build -R tes3_bsa_writer --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `src/formats/bsa/tes3_bsa_writer.cpp` contains `validate_entries`.
    - `src/formats/bsa/tes3_bsa_writer.cpp` contains `.libbsa-tmp-` and does not rely on deleting deterministic `<output>.tmp`.
    - Missing disk source test returns `libbsa::error_code::io_error` from `write_to`, not `add_file`.
    - Duplicate canonical path test returns `libbsa::error_code::format_error` from `write_to`.
    - Overwrite-disabled test preserves existing bytes; overwrite-enabled test replaces existing bytes.
  </acceptance_criteria>
  <done>TES3 writer finalization is fail-closed and publish-safe before full serializer work proceeds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: REFACTOR publish cleanup and comments</name>
  <files>src/formats/bsa/tes3_bsa_writer.cpp, tests/unit/tes3_bsa_writer_tests.cpp</files>
  <read_first>
    - `src/formats/bsa/tes3_bsa_writer.cpp`
    - `tests/unit/tes3_bsa_writer_tests.cpp`
    - `AGENTS.md`
  </read_first>
  <action>Refactor duplicated publish cleanup into small local helpers only if it improves clarity. Add comments for non-obvious WHY: unique temp directory avoids deleting caller-owned deterministic siblings; backup restore preserves previous output on overwrite publish failure; disk sources are reread at `write_to` per D-05/D-07. Do not remove accurate existing comments.</action>
  <verify>
    <automated>ctest --test-dir build -R "tes3_bsa_writer|public_include_boundary" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - Focused TES3 writer and public boundary tests pass.
    - Comments explain unique temp directory and overwrite rollback behavior.
    - No test checks diagnostic strings.
  </acceptance_criteria>
  <done>Safe publish implementation is readable, documented, and covered by focused tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller→writer finalization | Untrusted entry list, host source paths, and output host path enter file I/O code. |
| temp output→destination archive | Temporary bytes become published archive bytes only after all validation and source reads succeed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | Tampering | archive path validation | mitigate | Use canonical paths from `detail::normalize_archive_path` and reject duplicate canonical keys during finalization. |
| T-10-05 | Tampering | output publishing | mitigate | Prepare all source bytes before publishing; write to unique temp directory; preserve existing output unless overwrite succeeds. |
| T-10-06 | DoS | source file reads | accept | Phase 12 owns bounded-memory packing; Phase 10 follows existing writer conventions and fails cleanly on I/O errors. |
| T-10-07 | Repudiation | validation tests | mitigate | Assert stable `error_code` values for caller-data and I/O failures rather than mutable diagnostic text. |
</threat_model>

<verification>
Run `cmake --build build --target libbsa_tests`, then `ctest --test-dir build -R "tes3_bsa_writer|public_include_boundary" --output-on-failure`.
</verification>

<success_criteria>
- D-05 through D-08 and D-17 are implemented.
- Missing disk source failures do not publish partial archives.
- Existing output and caller-owned temp-name siblings are preserved unless explicit overwrite succeeds.
</success_criteria>

<output>
After completion, create `.planning/phases/10-tes3-write-support-and-bsa-format-completeness/10-02-SUMMARY.md` with RED/GREEN/REFACTOR notes and commits.
</output>
