---
phase: 10-tes3-write-support-and-bsa-format-completeness
plan: 06
type: execute
wave: 6
depends_on: [10-05]
files_modified:
  - src/detail/archive_path.cpp
  - tests/unit/archive_path_tests.cpp
  - src/detail/atomic_file_ops.hpp
  - src/formats/bsa/tes3_bsa_writer.cpp
  - tests/unit/tes3_bsa_writer_tests.cpp
autonomous: true
gap_closure: true
requirements: [WBSA-04]
must_haves:
  truths:
    - "TES3 writer rejects embedded-NUL archive paths before they can be serialized into a NUL-terminated TES3 name table."
    - "The shared archive path validator rejects embedded-NUL archive paths for all callers that rely on normalized archive keys."
    - "TES3 writer with overwrite_existing=false cannot replace an output path that appears between initial validation and final publish."
  artifacts:
    - path: "src/detail/archive_path.cpp"
      provides: "Shared embedded-NUL archive path rejection"
      contains: "raw == '\\0'"
    - path: "tests/unit/archive_path_tests.cpp"
      provides: "Shared archive path embedded-NUL regression coverage"
      contains: "embedded"
    - path: "src/detail/atomic_file_ops.hpp"
      provides: "Portable no-replace publish primitive"
      contains: "publish_file_without_replace"
    - path: "src/formats/bsa/tes3_bsa_writer.cpp"
      provides: "TES3 writer boundary and final non-overwrite publish enforcement"
      contains: "publish_file_without_replace"
    - path: "tests/unit/tes3_bsa_writer_tests.cpp"
      provides: "TES3 writer NUL path and no-replace publish regression coverage"
      contains: "NUL bytes"
  key_links:
    - from: "src/detail/archive_path.cpp"
      to: "src/formats/bsa/tes3_bsa_writer.cpp"
      via: "make_entry calls detail::normalize_archive_path before preserving/hash-sorting TES3 names"
      pattern: "normalize_archive_path\(archive_path\)"
    - from: "src/formats/bsa/tes3_bsa_writer.cpp"
      to: "src/detail/atomic_file_ops.hpp"
      via: "non-overwrite final publish uses publish_file_without_replace after final output existence check"
      pattern: "publish_file_without_replace\(temp_path, output_path\)"
---

<objective>
Close Phase 10 verification gaps for TES3 writer path safety and non-overwrite publish semantics.

Purpose: Phase 10 cannot satisfy WBSA-04 while accepted archive paths can create reader-unopenable TES3 archives or while `overwrite_existing=false` can replace a destination that appears during finalization.
Output: Shared embedded-NUL path rejection, TES3 writer regression tests, portable no-replace publish enforcement, and focused/full verification commands proving both gaps are closed.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-tes3-write-support-and-bsa-format-completeness/10-VERIFICATION.md
@.planning/phases/10-tes3-write-support-and-bsa-format-completeness/10-SPEC.md
@.planning/phases/10-tes3-write-support-and-bsa-format-completeness/10-CONTEXT.md
@.planning/phases/10-tes3-write-support-and-bsa-format-completeness/10-02-SUMMARY.md
@.planning/phases/10-tes3-write-support-and-bsa-format-completeness/10-05-SUMMARY.md
@src/detail/archive_path.cpp
@src/detail/atomic_file_ops.hpp
@src/formats/bsa/tes3_bsa_writer.cpp
@tests/unit/archive_path_tests.cpp
@tests/unit/tes3_bsa_writer_tests.cpp

<interfaces>
From `src/detail/archive_path.cpp`:
```cpp
result<archive_path_key> normalize_archive_path(std::string_view input);
```

From `src/detail/atomic_file_ops.hpp`:
```cpp
inline result<void> publish_file_without_replace(const std::filesystem::path& temp_path,
                                                  const std::filesystem::path& output_path);
inline result<void> replace_file_atomically(const std::filesystem::path& temp_path,
                                            const std::filesystem::path& output_path);
```

From `src/formats/bsa/tes3_bsa_writer.cpp`:
```cpp
result<formats::bsa::tes3_writer_entry> make_entry(std::string_view archive_path);
result<void> write_tes3_bsa_archive(const tes3_bsa_writer_options& options,
                                    std::span<const tes3_writer_entry> entries,
                                    std::string_view output_host_path);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reject embedded-NUL archive paths at the shared validator and TES3 writer boundary</name>
  <files>src/detail/archive_path.cpp, tests/unit/archive_path_tests.cpp, tests/unit/tes3_bsa_writer_tests.cpp</files>
  <read_first>
    - src/detail/archive_path.cpp
    - tests/unit/archive_path_tests.cpp
    - src/formats/bsa/tes3_bsa_writer.cpp
    - tests/unit/tes3_bsa_writer_tests.cpp
    - .planning/phases/10-tes3-write-support-and-bsa-format-completeness/10-VERIFICATION.md
  </read_first>
  <behavior>
    - `detail::normalize_archive_path(std::string_view{"Meshes/A.nif\0Suffix", 19})` returns `error_code::invalid_argument`.
    - `tes3_bsa_writer::add_bytes` with `std::string{"Meshes/A.nif\0Suffix", 19}` returns `error_code::invalid_argument`.
    - `tes3_bsa_writer::add_file` with the same archive path returns `error_code::invalid_argument` before any disk source probing.
  </behavior>
  <action>Add an embedded-NUL check to `normalize_archive_path`: during the existing `for (const char raw : input)` loop, return `invalid_path_error()` when `raw == '\0'` before separator normalization or segment appending. Keep the existing root, drive, empty-segment, `.`, and `..` behavior unchanged. Add an `archive_path` unit test using an owning `std::string` with an embedded NUL, because string literals through `std::string_view` would otherwise truncate at the first NUL. Add TES3 writer coverage proving both `add_bytes` and `add_file` reject `std::string{"Meshes/A.nif\0Suffix", 19}` with `libbsa::error_code::invalid_argument`, closing the verification gap for accepted paths that serialize into self-inconsistent NUL-terminated TES3 names.</action>
  <verify>
    <automated>cmake --build "build/windows-msvc-debug-static" --target libbsa_tests --config Debug &amp;&amp; ctest --test-dir "build/windows-msvc-debug-static" -C Debug -R "archive_path|tes3_bsa_writer" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `src/detail/archive_path.cpp` contains `raw == '\0'` inside `normalize_archive_path`.
    - `tests/unit/archive_path_tests.cpp` contains a test name with `embedded-NUL` or `NUL` and asserts `error_code::invalid_argument`.
    - `tests/unit/tes3_bsa_writer_tests.cpp` contains `tes3_bsa_writer rejects archive paths containing NUL bytes`.
    - The focused CTest command exits 0.
  </acceptance_criteria>
  <done>Shared archive path normalization and TES3 writer add APIs reject embedded-NUL archive paths with stable invalid_argument errors, preventing writer output that the TES3 reader rejects due to truncated NUL-terminated names.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce final no-replace publish semantics for overwrite_existing=false</name>
  <files>src/detail/atomic_file_ops.hpp, src/formats/bsa/tes3_bsa_writer.cpp, tests/unit/tes3_bsa_writer_tests.cpp</files>
  <read_first>
    - src/detail/atomic_file_ops.hpp
    - src/formats/bsa/tes3_bsa_writer.cpp
    - tests/unit/tes3_bsa_writer_tests.cpp
    - .planning/phases/10-tes3-write-support-and-bsa-format-completeness/10-VERIFICATION.md
    - .planning/phases/10-tes3-write-support-and-bsa-format-completeness/10-02-SUMMARY.md
  </read_first>
  <behavior>
    - `detail::publish_file_without_replace(temp, archive)` fails with `error_code::io_error` and preserves both files when `archive` already exists.
    - `detail::publish_file_without_replace(temp, archive)` succeeds and removes `temp` when `archive` does not exist.
    - `tes3_bsa_writer::write_to` with `overwrite_existing=false` performs a final output existence check after temp archive creation and before final publish, then uses the no-replace primitive rather than a replacing rename.
  </behavior>
  <action>Ensure `src/detail/atomic_file_ops.hpp` exposes `publish_file_without_replace(temp_path, output_path)` that uses Windows `MoveFileExW` without `MOVEFILE_REPLACE_EXISTING`, and POSIX hard-link creation followed by temp removal, so the destination name is never replaced when it already exists. In `write_tes3_bsa_archive`, keep the initial preflight existence check, keep the post-temp final `path_exists_noexcept(output_path)` check for clearer errors, and publish the non-overwrite path only through `detail::publish_file_without_replace(temp_path, output_path)`. Keep overwrite-enabled replacement routed through `detail::replace_file_atomically`. Add tests for the no-replace helper preserving an existing sentinel destination and for the free-destination success path.</action>
  <verify>
    <automated>cmake --build "build/windows-msvc-debug-static" --target libbsa_tests --config Debug &amp;&amp; ctest --test-dir "build/windows-msvc-debug-static" -C Debug -R "tes3_bsa_writer" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `src/detail/atomic_file_ops.hpp` contains `publish_file_without_replace` and does not pass `MOVEFILE_REPLACE_EXISTING` in that function.
    - `src/formats/bsa/tes3_bsa_writer.cpp` contains `path_exists_noexcept(output_path)` after temporary archive writing and before `detail::publish_file_without_replace(temp_path, output_path)`.
    - `tests/unit/tes3_bsa_writer_tests.cpp` contains `publish helper never replaces an existing destination` and asserts the destination sentinel bytes remain unchanged.
    - The focused TES3 writer CTest command exits 0.
  </acceptance_criteria>
  <done>TES3 writer non-overwrite finalization cannot replace an output path that appears during finalization, and helper-level regression tests prove both failure-preserve and success-move behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller archive path → shared archive path validator | Untrusted archive-internal path bytes enter normalization and writer serialization. |
| caller host paths → local filesystem publish | Untrusted output/source path strings drive local file inspection, temporary publication, and final destination ownership. |
| temporary output → final archive path | Completed archive bytes cross from writer-owned temp storage into caller-visible output location. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-06-01 | Tampering | `normalize_archive_path`, `tes3_bsa_writer::add_file/add_bytes` | mitigate | Reject embedded NUL archive paths with `invalid_argument` before TES3 NUL-terminated name serialization and hash computation. |
| T-10-06-02 | Tampering / DoS | `write_tes3_bsa_archive` non-overwrite publish path | mitigate | Perform final destination check and use `publish_file_without_replace`, preserving existing destinations when `overwrite_existing=false`. |
| T-10-06-03 | Repudiation / Supply-chain | `TES5Edit/` reference boundary | mitigate | Run `git -C "TES5Edit" status --short` and require empty output in final verification. |
</threat_model>

<verification>
Run these after both tasks:

<automated>ctest --test-dir "build/windows-msvc-debug-static" -C Debug -R "archive_path|tes3_bsa_writer|tes3_bsa_reader|TES4 BSA writer|public_include_boundary" --output-on-failure</automated>
<automated>ctest --test-dir "build/windows-msvc-debug-static" -C Debug --output-on-failure</automated>
<automated>git -C "TES5Edit" status --short</automated>

The first two commands must exit 0. The `TES5Edit` command must print no output.
</verification>

<success_criteria>
- Phase 10 verification gap 1 is closed: embedded-NUL archive paths are rejected by the shared validator and by TES3 writer `add_file`/`add_bytes` tests.
- Phase 10 verification gap 2 is closed: non-overwrite TES3 publish uses a no-replace primitive after finalization and has tests preserving a pre-existing output.
- WBSA-04 remains covered by TES3 writer, TES3 reader, TES4 writer, and public include boundary focused regressions.
- `TES5Edit/` remains read-only and clean.
</success_criteria>

<output>
After completion, create `.planning/phases/10-tes3-write-support-and-bsa-format-completeness/10-06-SUMMARY.md`.
</output>
