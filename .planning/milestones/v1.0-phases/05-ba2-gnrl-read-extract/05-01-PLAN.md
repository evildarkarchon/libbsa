---
phase: 05-ba2-gnrl-read-extract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/fixtures/generated/generate_ba2_gnrl_fixtures.cpp
  - tests/CMakeLists.txt
  - tests/fixtures/generated/archives/ba2_gnrl_fo4.ba2
  - tests/fixtures/generated/archives/ba2_gnrl_fo4_manifest.json
  - tests/fixtures/generated/archives/ba2_gnrl_sfv2.ba2
  - tests/fixtures/generated/archives/ba2_gnrl_sfv2_manifest.json
  - tests/fixtures/generated/archives/ba2_gnrl_sfv3.ba2
  - tests/fixtures/generated/archives/ba2_gnrl_sfv3_manifest.json
  - tests/fixtures/generated/archives/ba2_gnrl_malformed_manifest.json
autonomous: true
requirements: [GNRL-01, GNRL-02, GNRL-03, GNRL-04, GNRL-05, GNRL-06, GNRL-07, GNRL-08]
user_setup: []
must_haves:
  truths:
    - "Maintainer can generate legal BA2 GNRL fixtures for FO4, Starfield v2, and Starfield v3."
    - "Generated manifests describe filenames, lookup variants, sizes, offsets, hashes, compression routes, Starfield fields, and expected bytes."
    - "Malformed and unsupported BA2 cases exist before parser implementation."
  artifacts:
    - path: "tests/fixtures/generated/generate_ba2_gnrl_fixtures.cpp"
      provides: "Synthetic BA2 GNRL fixture generator"
      contains: "generate_ba2_gnrl_fixtures"
    - path: "tests/CMakeLists.txt"
      provides: "CMake target generate_ba2_gnrl_fixtures"
      contains: "generate_ba2_gnrl_fixtures"
  key_links:
    - from: "tests/CMakeLists.txt"
      to: "tests/fixtures/generated/generate_ba2_gnrl_fixtures.cpp"
      via: "add_executable and add_custom_target"
      pattern: "generate_ba2_gnrl_fixtures_tool"
---

<objective>
Create the legal, generated BA2 GNRL fixture corpus that every later Phase 5 task consumes.

Purpose: Phase 5 acceptance is generated-only per D-05 through D-11 and must not require copyrighted game archives.
Output: A dedicated BA2 GNRL generator, success fixtures/manifests for FO4/SFv2/SFv3, malformed/unsupported fixtures, and CMake target wiring.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-ba2-gnrl-read-extract/05-SPEC.md
@.planning/phases/05-ba2-gnrl-read-extract/05-CONTEXT.md
@.planning/phases/05-ba2-gnrl-read-extract/05-RESEARCH.md
@tests/fixtures/generated/generate_tes3_bsa_fixtures.cpp
@tests/fixtures/generated/generate_tes4_bsa_fixtures.cpp
@tests/CMakeLists.txt
@src/detail/bethesda_hash.hpp
@src/detail/compression_router.hpp

<interfaces>
From `src/detail/compression_router.hpp`:
```cpp
enum class compression_method { none, deflate, lz4_frame, lz4_block };
result<std::vector<std::byte>> compress_payload(compression_method method, std::span<const std::byte> input);
```

BA2 GNRL reference layout from `TES5Edit/Core/wbBSArchive.pas`:
```text
BTDX magic, uint32 version, header: Magic(GNRL/DX10), FileCount, FileTableOffset.
Starfield v2 extra: Unknown1, Unknown2. Starfield v3 extra: Unknown1, Unknown2, CompressionMethod.
GNRL record: NameHash, Ext, DirHash, Unknown, Offset, PackedSize, Size, BAADF00D.
Filename table: one uint16 length-prefixed name per record at FileTableOffset.
```
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add BA2 GNRL fixture generator source</name>
  <files>tests/fixtures/generated/generate_ba2_gnrl_fixtures.cpp</files>
  <read_first>tests/fixtures/generated/generate_tes3_bsa_fixtures.cpp, tests/fixtures/generated/generate_tes4_bsa_fixtures.cpp, src/detail/compression_router.hpp, src/detail/bethesda_hash.hpp, .planning/phases/05-ba2-gnrl-read-extract/05-CONTEXT.md</read_first>
  <behavior>
    - Generator writes `BTDX` BA2 GNRL archives for FO4, Starfield v2, and Starfield v3 per D-05.
    - Across success fixtures, entries cover mixed case, slash/backslash source names normalized in manifests, nested folders, zero-byte raw payload, raw payload, deflate payload, and v3 raw LZ4 block payload per D-06/D-12.
    - Manifest records `path`, `original_path`, `lookup_variants`, `raw_size`, `stored_size`, `payload_offset`, `archive_hash`, `record_flags`, `compression`, `starfield_unknown1`, `starfield_unknown2`, `compression_method`, and `expected.bytes_hex` per D-09.
  </behavior>
  <action>Create `generate_ba2_gnrl_fixtures.cpp` as a standalone C++20 generator outside `TES5Edit/` per D-07/D-24. Implement little-endian byte writer helpers, UInt16 length-prefixed filename table writing at `FileTableOffset`, FO4/BA2 hash manifest fields using `libbsa::detail::hash_fo4`, and compression payload creation via `detail::compress_payload(detail::compression_method::deflate, ...)` and `detail::compress_payload(detail::compression_method::lz4_block, ...)`. Use `PackedSize == 0` for raw entries and non-zero packed size for compressed entries; never infer compression from path or extension per D-15. Add a provenance block stating synthetic strings generated for libbsa tests and no game/TES5Edit bytes copied.</action>
  <verify><automated>cmake --build --preset windows-msvc-debug-static --target generate_ba2_gnrl_fixtures_tool</automated></verify>
  <acceptance_criteria>
    - `tests/fixtures/generated/generate_ba2_gnrl_fixtures.cpp` contains `BTDX`, `GNRL`, `FileTableOffset`, `BAADF00D`, `compression_method`, and `no game or TES5Edit bytes copied`.
    - `tests/fixtures/generated/generate_ba2_gnrl_fixtures.cpp` contains `detail::compression_method::deflate` and `detail::compression_method::lz4_block`.
    - The build command exits 0.
  </acceptance_criteria>
  <done>Generator source compiles as a C++20 tool and contains all success/malformed fixture generation logic.</done>
</task>

<task type="auto">
  <name>Task 2: Wire generator into CMake and generate fixture artifacts</name>
  <files>tests/CMakeLists.txt, tests/fixtures/generated/archives/ba2_gnrl_fo4.ba2, tests/fixtures/generated/archives/ba2_gnrl_fo4_manifest.json, tests/fixtures/generated/archives/ba2_gnrl_sfv2.ba2, tests/fixtures/generated/archives/ba2_gnrl_sfv2_manifest.json, tests/fixtures/generated/archives/ba2_gnrl_sfv3.ba2, tests/fixtures/generated/archives/ba2_gnrl_sfv3_manifest.json, tests/fixtures/generated/archives/ba2_gnrl_malformed_manifest.json</files>
  <read_first>tests/CMakeLists.txt, tests/fixtures/generated/generate_ba2_gnrl_fixtures.cpp</read_first>
  <action>Update `tests/CMakeLists.txt` with `add_executable(generate_ba2_gnrl_fixtures_tool fixtures/generated/generate_ba2_gnrl_fixtures.cpp)`, link it PRIVATE to `libbsa::libbsa`, set C++20, add `${CMAKE_SOURCE_DIR}/src` as a private include, and add custom target `generate_ba2_gnrl_fixtures` with BYPRODUCTS for the three success BA2 files/manifests plus malformed/unsupported BA2 fixtures. Run the target so generated artifacts are created in `tests/fixtures/generated/archives`.</action>
  <verify><automated>cmake --build --preset windows-msvc-debug-static --target generate_ba2_gnrl_fixtures</automated></verify>
  <acceptance_criteria>
    - `tests/CMakeLists.txt` contains `generate_ba2_gnrl_fixtures_tool` and `generate_ba2_gnrl_fixtures`.
    - `tests/fixtures/generated/archives/ba2_gnrl_fo4_manifest.json` contains `"variant": "fallout4"`.
    - `tests/fixtures/generated/archives/ba2_gnrl_sfv2_manifest.json` contains `"starfield_unknown1"` and `"starfield_unknown2"`.
    - `tests/fixtures/generated/archives/ba2_gnrl_sfv3_manifest.json` contains `"compression_method": 3` and `"compression": "lz4_block"`.
    - `tests/fixtures/generated/archives/ba2_gnrl_malformed_manifest.json` contains cases `ba2_dx10_unsupported`, `ba2_duplicate_canonical_path`, `ba2_corrupt_compressed_payload`, `ba2_exact_size_mismatch`, and `ba2_unsupported_v3_compression_method`.
  </acceptance_criteria>
  <done>CMake can regenerate all required Phase 5 BA2 fixtures and manifests from repository-owned generator code.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| generator→repo fixtures | Synthetic fixture bytes become trusted parser tests. |
| compression helper→fixture bytes | Generated compressed payloads must match metadata exactly. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | T | `generate_ba2_gnrl_fixtures.cpp` | mitigate | Record provenance and generate all bytes from synthetic strings outside `TES5Edit/`. |
| T-05-02 | I | fixture manifests | mitigate | Store expected bytes as hex and explicit metadata fields so tests do not infer behavior from filenames. |
</threat_model>

<verification>
Run `cmake --build --preset windows-msvc-debug-static --target generate_ba2_gnrl_fixtures` and confirm `git -C TES5Edit status --short` has no output.
</verification>

<success_criteria>
Generated BA2 fixture target exists, emits all required success/malformed artifacts, and manifests cover D-05 through D-11 without copyrighted input.
</success_criteria>

<output>
After completion, create `.planning/phases/05-ba2-gnrl-read-extract/05-01-SUMMARY.md`.
</output>
