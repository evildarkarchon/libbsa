---
phase: 05-ba2-gnrl-read-extract
plan: 04
type: execute
wave: 4
depends_on: [05-03]
files_modified:
  - src/formats/ba2/ba2_gnrl_reader.hpp
  - src/formats/ba2/ba2_gnrl_reader.cpp
  - src/archive.cpp
  - tests/unit/ba2_gnrl_reader_tests.cpp
autonomous: true
requirements: [GNRL-05, GNRL-06, GNRL-07]
user_setup: []
must_haves:
  truths:
    - "Consumer can extract raw BA2 GNRL entries through sink and extract_bytes."
    - "Consumer can extract deflate BA2 GNRL entries with exact-size validation."
    - "Consumer can extract Starfield v3 CompressionMethod 3 entries through raw LZ4 block."
  artifacts:
    - path: "src/formats/ba2/ba2_gnrl_reader.cpp"
      provides: "BA2 GNRL extraction helper"
      contains: "extract_ba2_gnrl_payload"
    - path: "src/archive.cpp"
      provides: "archive_reader BA2 extraction dispatch"
      contains: "extract_ba2_gnrl_payload"
  key_links:
    - from: "src/archive.cpp"
      to: "src/formats/ba2/ba2_gnrl_reader.cpp"
      via: "extract dispatch for BA2 selected entries"
      pattern: "archive_type::ba2.*extract_ba2_gnrl_payload"
    - from: "src/formats/ba2/ba2_gnrl_reader.cpp"
      to: "src/detail/compression_router.cpp"
      via: "detail::decompress_payload_exact"
      pattern: "decompress_payload_exact"
---

<objective>
Implement BA2 GNRL raw, deflate, and Starfield raw-LZ4-block extraction through the established public reader API.

Purpose: GNRL-06/GNRL-07 require sink-first extraction with metadata-driven compression routing and exact-size validation per D-12 through D-15 and D-22/D-23.
Output: BA2 extraction helper, archive_reader extraction dispatch, and manifest-backed byte comparisons.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-ba2-gnrl-read-extract/05-SPEC.md
@.planning/phases/05-ba2-gnrl-read-extract/05-CONTEXT.md
@.planning/phases/05-ba2-gnrl-read-extract/05-RESEARCH.md
@.planning/phases/05-ba2-gnrl-read-extract/05-03-SUMMARY.md
@src/archive.cpp
@src/formats/ba2/ba2_gnrl_reader.hpp
@src/formats/ba2/ba2_gnrl_reader.cpp
@src/detail/compression_router.hpp
@src/formats/bsa/tes4_bsa_reader.cpp
@src/formats/bsa/tes3_bsa_reader.cpp
@tests/unit/ba2_gnrl_reader_tests.cpp

<interfaces>
Add BA2 extraction helper:
```cpp
result<void> extract_ba2_gnrl_payload(std::string_view host_path, const entry_metadata& entry, payload_sink& sink);
```
It must route by `entry.compression` only: `none`, `deflate`, or `lz4_block`; never by path/extension.
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add BA2 extraction RED tests</name>
  <files>tests/unit/ba2_gnrl_reader_tests.cpp</files>
  <read_first>tests/unit/ba2_gnrl_reader_tests.cpp, tests/unit/tes3_bsa_reader_tests.cpp, tests/unit/tes4_bsa_reader_tests.cpp, tests/fixtures/generated/archives/ba2_gnrl_fo4_manifest.json, tests/fixtures/generated/archives/ba2_gnrl_sfv2_manifest.json, tests/fixtures/generated/archives/ba2_gnrl_sfv3_manifest.json</read_first>
  <behavior>
    - For every manifest entry, `extract(path, sink)` and `extract_bytes(path)` equal `expected.bytes_hex`.
    - Tests include at least one raw, one zero-byte raw, one deflate, and one Starfield v3 `lz4_block` entry.
    - Partial sink writes return `error_code::io_error` without reporting success.
  </behavior>
  <action>Extend `ba2_gnrl_reader_tests.cpp` with tests labeled `[ba2_gnrl_extract]`. Reuse manifest helpers for hex decoding and collecting/partial sinks. Assert `extract` and `extract_bytes` match exact bytes for FO4/SFv2/SFv3 fixtures. Add a direct helper contract test for `extract_ba2_gnrl_payload` requiring sink partial write detection and metadata-driven routing. Do not assert diagnostic message text.</action>
  <verify><automated>cmake --build --preset windows-msvc-debug-static && ctest --preset windows-msvc-debug-static -L ba2_gnrl_extract --output-on-failure</automated></verify>
  <acceptance_criteria>
    - `tests/unit/ba2_gnrl_reader_tests.cpp` contains `[ba2_gnrl_extract]`, `extract_bytes`, `partial_sink`, `lz4_block`, and `bytes_hex`.
    - RED state fails before extraction implementation; GREEN state passes after Task 2.
  </acceptance_criteria>
  <done>Extraction tests prove expected raw/deflate/LZ4 bytes and sink failure behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement BA2 extraction helper and archive_reader dispatch</name>
  <files>src/formats/ba2/ba2_gnrl_reader.hpp, src/formats/ba2/ba2_gnrl_reader.cpp, src/archive.cpp</files>
  <read_first>src/formats/ba2/ba2_gnrl_reader.hpp, src/formats/ba2/ba2_gnrl_reader.cpp, src/archive.cpp, src/detail/compression_router.hpp, src/formats/bsa/tes4_bsa_reader.cpp, src/formats/bsa/tes3_bsa_reader.cpp</read_first>
  <action>Add `extract_ba2_gnrl_payload(std::string_view host_path, const entry_metadata& entry, payload_sink& sink)`. Open the host archive, seek to archive-absolute `entry.payload_offset`, read exactly `entry.stored_size`, and reject short reads as `format_error`. If `entry.compression == entry_compression::none`, stream the stored bytes to the sink in chunks no larger than 64 KiB. If `deflate` or `lz4_block`, call `detail::decompress_payload_exact(detail::compression_method::deflate/lz4_block, stored, entry.raw_size)` and stream decoded bytes in 64 KiB chunks. Treat partial sink acceptance as `error_code::io_error`. Do not support `lz4_frame` for BA2 GNRL. Update `archive_reader::extract` so BA2 entries call this helper before generic BSA stored-payload buffering; keep `extract_bytes` unchanged as a sink-backed convenience adapter per D-22.</action>
  <verify><automated>cmake --build --preset windows-msvc-debug-static && ctest --preset windows-msvc-debug-static -L "ba2_gnrl_extract|ba2_gnrl_metadata" --output-on-failure</automated></verify>
  <acceptance_criteria>
    - `src/formats/ba2/ba2_gnrl_reader.cpp` contains `extract_ba2_gnrl_payload`, `decompress_payload_exact`, `compression_method::deflate`, `compression_method::lz4_block`, and a partial sink `io_error` path.
    - `src/formats/ba2/ba2_gnrl_reader.cpp` does not contain `lz4_frame` except in an explicit unsupported/fail-closed branch if needed.
    - `src/archive.cpp` dispatches BA2 extraction to `extract_ba2_gnrl_payload`.
    - Focused extraction and metadata labels pass.
  </acceptance_criteria>
  <done>BA2 raw, deflate, and v3 raw-LZ4-block entries extract byte-for-byte through `extract` and `extract_bytes`.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| archive payload→codec | Untrusted compressed bytes enter deflate/LZ4 decoders. |
| decoded bytes→caller sink | Extracted bytes cross into caller-owned sink. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | T | `extract_ba2_gnrl_payload` | mitigate | Exact-size decompression and stored-span validation before sink writes. |
| T-05-08 | D | `payload_sink` writes | mitigate | Treat partial sink acceptance as `io_error`; chunk large outputs. |
</threat_model>

<verification>
Run `ctest --preset windows-msvc-debug-static -L "ba2_gnrl_extract|ba2_gnrl_metadata|ba2_gnrl_lookup" --output-on-failure`.
</verification>

<success_criteria>
Every generated BA2 success fixture extracts manifest bytes exactly through both public extraction APIs, including Starfield v3 raw LZ4 block entries.
</success_criteria>

<output>
After completion, create `.planning/phases/05-ba2-gnrl-read-extract/05-04-SUMMARY.md`.
</output>
