---
phase: 05-ba2-gnrl-read-extract
plan: 03
type: execute
wave: 3
depends_on: [05-02]
files_modified:
  - src/formats/ba2/ba2_gnrl_parser.hpp
  - src/formats/ba2/ba2_gnrl_parser.cpp
  - src/formats/ba2/ba2_gnrl_reader.hpp
  - src/formats/ba2/ba2_gnrl_reader.cpp
  - src/archive.cpp
  - CMakeLists.txt
  - tests/unit/ba2_gnrl_reader_tests.cpp
autonomous: true
requirements: [GNRL-01, GNRL-02, GNRL-03, GNRL-04, GNRL-05, GNRL-08]
user_setup: []
must_haves:
  truths:
    - "Consumer can list BA2 GNRL entries parsed from the filename table."
    - "Consumer can find and contains BA2 entries using normalized archive paths."
    - "BA2 entry metadata reports sizes, archive-absolute offsets, hash, flags, and compression route."
  artifacts:
    - path: "src/formats/ba2/ba2_gnrl_parser.cpp"
      provides: "Checked BA2 GNRL header, record, filename table parsing"
      contains: "parse_ba2_gnrl_archive_file"
    - path: "src/formats/ba2/ba2_gnrl_reader.cpp"
      provides: "BA2 entry list/find/contains helpers"
      contains: "find_ba2_gnrl_entry"
  key_links:
    - from: "src/archive.cpp"
      to: "src/formats/ba2/ba2_gnrl_parser.cpp"
      via: "open routes detected BA2 GNRL to parser"
      pattern: "parse_ba2_gnrl_archive_file"
    - from: "src/archive.cpp"
      to: "src/formats/ba2/ba2_gnrl_reader.cpp"
      via: "entries/find/contains dispatch when metadata.type == ba2"
      pattern: "find_ba2_gnrl_entry"
---

<objective>
Implement BA2 GNRL record and filename-table parsing plus deterministic list/lookup metadata behavior.

Purpose: GNRL-04/GNRL-05/GNRL-08 require parser-owned BA2 metadata rather than the detector skeleton from Plan 02.
Output: Private BA2 parser/reader helpers, archive_reader dispatch, and manifest-backed metadata/lookup tests.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-ba2-gnrl-read-extract/05-SPEC.md
@.planning/phases/05-ba2-gnrl-read-extract/05-CONTEXT.md
@.planning/phases/05-ba2-gnrl-read-extract/05-RESEARCH.md
@.planning/phases/05-ba2-gnrl-read-extract/05-02-SUMMARY.md
@include/libbsa/archive.hpp
@src/archive.cpp
@src/detail/binary_io.hpp
@src/detail/archive_path.hpp
@src/detail/bethesda_hash.hpp
@src/formats/bsa/tes3_bsa_parser.hpp
@src/formats/bsa/tes3_bsa_reader.hpp
@tests/unit/ba2_gnrl_reader_tests.cpp

<interfaces>
Create parser contract:
```cpp
namespace libbsa::formats::ba2 {
struct ba2_gnrl_archive { archive_metadata metadata; std::vector<entry_metadata> entries; };
result<ba2_gnrl_archive> parse_ba2_gnrl_archive_file(std::string_view host_path, std::uint64_t archive_size, detected_ba2_format detected);
result<std::vector<entry_metadata>> ba2_gnrl_entries(std::span<const entry_metadata> entries);
result<std::optional<entry_metadata>> find_ba2_gnrl_entry(std::span<const entry_metadata> entries, std::string_view path);
result<bool> contains_ba2_gnrl_entry(std::span<const entry_metadata> entries, std::string_view path);
}
```
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add BA2 metadata/listing/lookup RED tests</name>
  <files>tests/unit/ba2_gnrl_reader_tests.cpp</files>
  <read_first>tests/unit/ba2_gnrl_reader_tests.cpp, tests/unit/tes3_bsa_reader_tests.cpp, tests/fixtures/generated/archives/ba2_gnrl_fo4_manifest.json, tests/fixtures/generated/archives/ba2_gnrl_sfv2_manifest.json, tests/fixtures/generated/archives/ba2_gnrl_sfv3_manifest.json</read_first>
  <behavior>
    - `entries()` returns canonical paths sorted lexicographically and `original_path` with `/` separators per D-20/D-21.
    - `find()` and `contains()` accept manifest lookup variants and return empty optional/false for valid missing paths.
    - Metadata reports manifest `raw_size`, `stored_size`, `payload_offset`, `archive_hash`, `record_flags`, `compression`, and non-embedded-name state per D-04/D-09.
  </behavior>
  <action>Extend `ba2_gnrl_reader_tests.cpp` with manifest-driven tests labeled `[ba2_gnrl_metadata]` and `[ba2_gnrl_lookup]`. Tests must iterate all three success manifests and assert every entry field from manifest to public `entry_metadata`. Include invalid caller path checks for `""`, `"/rooted/file.txt"`, `"..\\escape.txt"`, and `"folder//file.txt"` returning `error_code::invalid_argument`; do not assert diagnostic message text per D-11.</action>
  <verify><automated>cmake --build --preset windows-msvc-debug-static && ctest --preset windows-msvc-debug-static -L "ba2_gnrl_metadata|ba2_gnrl_lookup" --output-on-failure</automated></verify>
  <acceptance_criteria>
    - `tests/unit/ba2_gnrl_reader_tests.cpp` contains `[ba2_gnrl_metadata]`, `[ba2_gnrl_lookup]`, `lookup_variants`, `payload_offset`, and `record_flags`.
    - Tests compare `compression` against manifest values `raw`, `deflate`, and `lz4_block`.
    - RED state fails before parser implementation; GREEN state passes after Task 2.
  </acceptance_criteria>
  <done>Tests define BA2 GNRL parser/list/lookup behavior against generated manifests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement BA2 GNRL parser and reader dispatch</name>
  <files>src/formats/ba2/ba2_gnrl_parser.hpp, src/formats/ba2/ba2_gnrl_parser.cpp, src/formats/ba2/ba2_gnrl_reader.hpp, src/formats/ba2/ba2_gnrl_reader.cpp, src/archive.cpp, CMakeLists.txt</files>
  <read_first>src/archive.cpp, src/detail/binary_io.hpp, src/detail/archive_path.hpp, src/detail/bethesda_hash.hpp, src/formats/bsa/tes3_bsa_parser.hpp, src/formats/bsa/tes3_bsa_reader.hpp, src/formats/ba2/ba2_format_detector.hpp, CMakeLists.txt</read_first>
  <action>Create BA2 parser/reader files under `src/formats/ba2/`. Parser must read the full BA2 header shape selected by `detected_ba2_format`, validate `FileTableOffset`, record table span, payload spans, UInt16 filename table spans, and file count arithmetic with structured `format_error`. Materialize `entry_metadata` using canonical path from `detail::normalize_archive_path`, `original_path` with `/`, archive-absolute `payload_offset`, `raw_size = Size`, `stored_size = PackedSize != 0 ? PackedSize : Size`, `archive_hash` combining or preserving FO4 hash data in the existing 64-bit field, `record_flags = Unknown`, `compression = none/deflate/lz4_block` from `PackedSize` and detected metadata, `has_embedded_name=false`, and `embedded_name_prefix_size=0`. Reject duplicate canonical paths during open per D-08/D-21. Add BA2 entries/find/contains helpers mirroring TES3/TES4 lower_bound semantics. Update `archive_reader::open`, `entries`, `find`, and `contains` to dispatch BA2 by `metadata.type == archive_type::ba2`. Register new sources in `CMakeLists.txt`.</action>
  <verify><automated>cmake --build --preset windows-msvc-debug-static && ctest --preset windows-msvc-debug-static -L "ba2_gnrl_detector|ba2_gnrl_metadata|ba2_gnrl_lookup" --output-on-failure</automated></verify>
  <acceptance_criteria>
    - `src/formats/ba2/ba2_gnrl_parser.cpp` contains `FileTableOffset`, `PackedSize`, `BAADF00D`, `normalize_archive_path`, and duplicate path rejection logic.
    - `src/formats/ba2/ba2_gnrl_reader.cpp` contains `ba2_gnrl_entries`, `find_ba2_gnrl_entry`, and `contains_ba2_gnrl_entry`.
    - `src/archive.cpp` dispatches `entries`, `find`, and `contains` for `archive_type::ba2`.
    - Focused BA2 detector/metadata/lookup labels pass.
  </acceptance_criteria>
  <done>BA2 GNRL archives open with full manifest-backed metadata, deterministic listing, normalized lookup, and duplicate canonical path rejection.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| BA2 bytes→parser | Untrusted headers, records, and filename table become public metadata. |
| caller path→lookup | Untrusted user path input is normalized before search. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | T | `ba2_gnrl_parser.cpp` | mitigate | Checked arithmetic/span validation for records, filename table, and payload offsets. |
| T-05-06 | D | `ba2_gnrl_reader.cpp` | mitigate | Sorted entries with lower_bound lookup and duplicate canonical path rejection at open. |
</threat_model>

<verification>
Run `ctest --preset windows-msvc-debug-static -L "ba2_gnrl_detector|ba2_gnrl_metadata|ba2_gnrl_lookup" --output-on-failure`.
</verification>

<success_criteria>
All generated BA2 success fixtures list, find, and contain expected entries with public metadata matching manifests.
</success_criteria>

<output>
After completion, create `.planning/phases/05-ba2-gnrl-read-extract/05-03-SUMMARY.md`.
</output>
