---
phase: 06-dds-boundary-and-ba2-dx10-read-reconstruction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/fixtures/generated/generate_ba2_dx10_fixtures.cpp
  - tests/CMakeLists.txt
  - tests/unit/ba2_dx10_metadata_tests.cpp
  - tests/unit/dds_layout_tests.cpp
  - tests/unit/ba2_dx10_parser_tests.cpp
  - tests/unit/ba2_dx10_extraction_tests.cpp
  - tests/unit/ba2_dx10_malformed_tests.cpp
autonomous: true
requirements: [DDS-01, DDS-02, DDS-03, DDS-04, DDS-05, DDS-06, DDS-07]
must_haves:
  truths:
    - "Maintainer can generate legal Fallout 4 and Starfield v3 BA2 DX10 texture fixtures outside TES5Edit."
    - "Generated manifests describe public texture metadata, DirectXTex validation expectations, chunk layout, compression routes, DDS payload order, and malformed expected error codes."
    - "CTest has named ba2_dx10 test labels ready for RED/GREEN plans without requiring local game archives."
  artifacts:
    - path: "tests/fixtures/generated/generate_ba2_dx10_fixtures.cpp"
      provides: "Legal BA2 DX10 fixture generator and rich manifests"
      contains: "generate_ba2_dx10_fixtures"
    - path: "tests/CMakeLists.txt"
      provides: "DX10 fixture generator target and DX10 test source registration"
      contains: "generate_ba2_dx10_fixtures_tool"
    - path: "tests/unit/ba2_dx10_metadata_tests.cpp"
      provides: "RED test landing zone for texture metadata"
      contains: "[ba2_dx10_metadata]"
  key_links:
    - from: "tests/CMakeLists.txt"
      to: "tests/fixtures/generated/generate_ba2_dx10_fixtures.cpp"
      via: "add_executable(generate_ba2_dx10_fixtures_tool ...)"
      pattern: "generate_ba2_dx10_fixtures_tool"
    - from: "tests/unit/*ba2_dx10*.cpp"
      to: "tests/fixtures/generated/archives/*dx10*manifest.json"
      via: "manifest-backed tests"
      pattern: "ba2_dx10_.*manifest"
---

<objective>
Create the Phase 6 legal fixture and test-target foundation before production DX10 code is added.

Purpose: Later TDD plans need stable generated FO4/Starfield DX10 inputs, malformed cases, and CTest labels that prove DDS-01 through DDS-07 without copyrighted game archives or TES5Edit mutation.
Output: DX10 fixture generator, CMake wiring, placeholder/red-ready test files.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-dds-boundary-and-ba2-dx10-read-reconstruction/06-SPEC.md
@.planning/phases/06-dds-boundary-and-ba2-dx10-read-reconstruction/06-CONTEXT.md
@.planning/phases/06-dds-boundary-and-ba2-dx10-read-reconstruction/06-RESEARCH.md
@.planning/phases/06-dds-boundary-and-ba2-dx10-read-reconstruction/06-VALIDATION.md
@.planning/phases/06-dds-boundary-and-ba2-dx10-read-reconstruction/06-PATTERNS.md
@tests/fixtures/generated/generate_ba2_gnrl_fixtures.cpp
@tests/CMakeLists.txt
</context>

<decision_coverage>
- D-20: The fixture generator creates a compact success matrix for FO4, Starfield v3, raw, deflate, raw-LZ4-block, cubemap, array, and mip/chunk layout coverage.
- D-21: Generated texture payloads are tiny, deterministic, legal, and DirectXTex-loadable after DDS reconstruction.
- D-22: Malformed fixture manifests cover truncated DX10 headers/records/chunks, invalid spans, inconsistent sizes/counts, bad compressed chunks, decoded-size mismatches, and mip/face gaps or duplicates.
- D-23: DX10 fixture manifests record archive chunks, expected public texture metadata, DirectXTex-loaded metadata, compression route, payload ordering, and decoded payload bytes or hashes.
- D-24: Fixture tooling and generated outputs stay under project test fixture directories with legal provenance documented and no TES5Edit workspace usage.
- D-33: Malformed manifest expectations use stable `error_code` names only, not diagnostic message text.
- D-34: The fixture plan preserves the TES5Edit boundary by documenting no copied game/TES5Edit bytes and never writing under `TES5Edit/`.
</decision_coverage>

<tasks>

<task type="auto">
  <name>Task 1: Generate legal BA2 DX10 success and malformed fixtures</name>
  <files>tests/fixtures/generated/generate_ba2_dx10_fixtures.cpp</files>
  <read_first>
    - tests/fixtures/generated/generate_ba2_gnrl_fixtures.cpp
    - .planning/phases/06-dds-boundary-and-ba2-dx10-read-reconstruction/06-CONTEXT.md
    - .planning/phases/06-dds-boundary-and-ba2-dx10-read-reconstruction/06-SPEC.md
    - .planning/phases/06-dds-boundary-and-ba2-dx10-read-reconstruction/06-RESEARCH.md
    - .planning/phases/06-dds-boundary-and-ba2-dx10-read-reconstruction/06-PATTERNS.md
  </read_first>
  <action>Create `generate_ba2_dx10_fixtures.cpp` by mirroring the BA2 GNRL generator style, but write BA2 subtype `DX10` records: common `BTDX` header, version `1` Fallout 4 fixture, version `3` Starfield fixture with `compression_method == 3`, per-entry fields `name_hash`, 4-byte extension, `directory_hash`, `unknown_tex`, `chunk_count`, `chunk_header_size == 24`, `height`, `width`, `num_mips`, byte `dxgi_format`, `cube_maps_raw`, and 24-byte chunk records containing archive-absolute `offset`, `packed_size`, `raw_size`, `start_mip`, `end_mip`, and sentinel `0xBAADF00D`. Per D-20 through D-24, generate a compact success matrix covering: FO4 raw 2D texture, FO4 deflate texture, Starfield v3 raw-LZ4-block texture, one cubemap (`cube_maps_raw == 2049`, `is_cubemap == true`), one array texture (`array_size > 1`), and representative multi-mip chunk layout. Manifest JSON must include `requirements: ["DDS-01","DDS-02","DDS-03","DDS-04","DDS-05","DDS-06","DDS-07"]`, `provenance.source` saying no game or TES5Edit bytes copied, archive-side records/chunks, expected public texture metadata, expected DirectXTex-loaded metadata, compression route strings (`raw`, `deflate`, `lz4_block`), expected decoded DDS payload bytes or hashes in validated DDS order, and for every source chunk an expected logical segment identity derived from BA2 format-defined chunk order plus texture metadata, shaped like `{array_index, face_index, start_mip, end_mip, source_chunk_index}` so DDS-07/D-15 can distinguish repeated mip ranges across cubemap faces or array slices from true duplicates. Do not create a success fixture that intentionally shuffles cubemap/array chunks out of researched BA2 format-defined order; manifests may record `logical_texture_segment` expectations, but those expectations are derived from parsed BA2 record order plus texture metadata, not arbitrary manifest-only source truth. Malformed cases must include truncated header, truncated records/chunks, invalid payload span, inconsistent size/count, corrupt compressed chunk, decoded-size mismatch, mip/face coverage gap, duplicate coverage within the same `(array_index, face_index)` identity, and repeated mip ranges across different faces/slices that remain valid. Do not write under `TES5Edit/` per D-34.</action>
  <verify>
    <automated>python -c "from pathlib import Path; p=Path('tests/fixtures/generated/generate_ba2_dx10_fixtures.cpp'); s=p.read_text(encoding='utf-8'); required=['BTDX','DX10','BAADF00D','compression_method','no game or TES5Edit bytes copied']; missing=[token for token in required if token not in s]; raise SystemExit('missing DX10 generator tokens: '+', '.join(missing) if missing else 0)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "BTDX" tests/fixtures/generated/generate_ba2_dx10_fixtures.cpp` finds the BA2 magic writer.
    - `grep -n "DX10" tests/fixtures/generated/generate_ba2_dx10_fixtures.cpp` finds DX10 subtype generation.
    - `grep -n "BAADF00D" tests/fixtures/generated/generate_ba2_dx10_fixtures.cpp` finds the chunk sentinel writer.
    - `grep -n "compression_method" tests/fixtures/generated/generate_ba2_dx10_fixtures.cpp` finds Starfield v3 compression metadata.
    - `grep -n "logical_texture_segment\|array_index\|face_index\|source_chunk_index" tests/fixtures/generated/generate_ba2_dx10_fixtures.cpp` finds per-source-chunk logical segment manifest fields.
    - `grep -n "format-defined.*order\|BA2.*order" tests/fixtures/generated/generate_ba2_dx10_fixtures.cpp` finds the manifest rule that logical segment identity is derived from researched BA2 format-defined order.
    - `grep -n "no game or TES5Edit bytes copied" tests/fixtures/generated/generate_ba2_dx10_fixtures.cpp` finds legal provenance text.
  </acceptance_criteria>
  <done>DX10 generator source emits manifest-backed legal success and malformed fixtures covering D-20 through D-24 and DDS-01 through DDS-07; CMake build verification happens after Task 2 registers the target.</done>
</task>

<task type="auto">
  <name>Task 2: Register DX10 fixture and test scaffolding in CMake</name>
  <files>tests/CMakeLists.txt, tests/unit/ba2_dx10_metadata_tests.cpp, tests/unit/dds_layout_tests.cpp, tests/unit/ba2_dx10_parser_tests.cpp, tests/unit/ba2_dx10_extraction_tests.cpp, tests/unit/ba2_dx10_malformed_tests.cpp</files>
  <read_first>
    - tests/CMakeLists.txt
    - tests/unit/ba2_gnrl_reader_tests.cpp
    - tests/unit/public_include_boundary_tests.cpp
    - .planning/phases/06-dds-boundary-and-ba2-dx10-read-reconstruction/06-VALIDATION.md
    - .planning/phases/06-dds-boundary-and-ba2-dx10-read-reconstruction/06-PATTERNS.md
  </read_first>
  <action>Update `tests/CMakeLists.txt` to add `unit/ba2_dx10_metadata_tests.cpp`, `unit/dds_layout_tests.cpp`, `unit/ba2_dx10_parser_tests.cpp`, `unit/ba2_dx10_extraction_tests.cpp`, and `unit/ba2_dx10_malformed_tests.cpp` to `libbsa_tests`; add `generate_ba2_dx10_fixtures_tool` built from `fixtures/generated/generate_ba2_dx10_fixtures.cpp`; add custom target `generate_ba2_dx10_fixtures` with BYPRODUCTS for `ba2_dx10_fo4.ba2`, `ba2_dx10_fo4_manifest.json`, `ba2_dx10_sfv3.ba2`, `ba2_dx10_sfv3_manifest.json`, `ba2_dx10_malformed_manifest.json`, and named malformed `.ba2` cases. Create the listed unit files with minimal Catch2 test cases tagged `[unit][fixture][ba2_dx10_metadata]`, `[unit][dds_layout]`, `[unit][fixture][ba2_dx10_detector]`, `[unit][fixture][ba2_dx10_extract]`, `[unit][fixture][ba2_dx10_compression]`, `[unit][fixture][ba2_dx10_directxtex]`, `[unit][fixture][ba2_dx10_layout]`, and `[unit][fixture][ba2_dx10_malformed]`; these placeholder tests should fail with `FAIL("Phase 6 RED test not implemented yet")` or equivalent until downstream TDD plans replace them. Follow the split-test analog mapping in `06-PATTERNS.md`: each split DX10 test file mirrors the relevant helper/assertion patterns from `tests/unit/ba2_gnrl_reader_tests.cpp` plus the CTest/Catch tag-discovery pattern from `tests/CMakeLists.txt`. This establishes Wave 0 test files without implementing production behavior.</action>
  <verify>
    <automated>cmake --build --preset windows-msvc-debug-static --target generate_ba2_dx10_fixtures</automated>
    <automated>cmake --build --preset windows-msvc-debug-static --target libbsa_tests</automated>
    <automated>ctest --preset windows-msvc-debug-static -N -R "ba2_dx10|dds_layout"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "unit/ba2_dx10_metadata_tests.cpp" tests/CMakeLists.txt` finds the test source registration.
    - `grep -n "generate_ba2_dx10_fixtures_tool" tests/CMakeLists.txt` finds the generator executable.
    - `grep -n "generate_ba2_dx10_fixtures" tests/CMakeLists.txt` finds the custom target.
    - `grep -n "ba2_dx10_fo4_manifest.json" tests/CMakeLists.txt` finds a success manifest byproduct.
    - `grep -n "\[unit\].*\[fixture\].*\[ba2_dx10_metadata\]" tests/unit/ba2_dx10_metadata_tests.cpp` finds the metadata test tag.
  </acceptance_criteria>
  <done>CMake can build generated DX10 fixtures and `libbsa_tests`, and all planned DX10 RED test files are discoverable through CTest labels without running placeholder failures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generator input → committed fixtures | Synthetic fixture specs become binary archive bytes consumed by parser tests. |
| manifest → assertions | JSON manifests drive expected public metadata and error-code assertions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering/DoS | `generate_ba2_dx10_fixtures.cpp` record/chunk counts | mitigate | Generate explicit tiny counts and sizes, use checked integer conversion helpers, and include malformed overflow/span cases. |
| T-06-02 | Tampering | fixture paths | mitigate | Generate archive virtual paths only; never write output under `TES5Edit/`; manifests document legal provenance per D-24/D-34. |
| T-06-03 | Repudiation | malformed expected errors | mitigate | Manifest stores stable `error_code` names only, not diagnostic message text, per D-33. |
| T-06-04 | Information Disclosure | fixture provenance | mitigate | Manifest explicitly states synthetic bytes and no game/TES5Edit data copied. |
</threat_model>

<verification>
Run `cmake --build --preset windows-msvc-debug-static --target generate_ba2_dx10_fixtures`, `cmake --build --preset windows-msvc-debug-static --target libbsa_tests`, then `ctest --preset windows-msvc-debug-static -N -R "ba2_dx10|dds_layout"` to confirm DX10/texture tests are compile-ready and discoverable before later RED/GREEN plans replace placeholder failures.
</verification>

<success_criteria>
- DX10 fixture generator exists outside `TES5Edit/`.
- Success manifests cover FO4, Starfield v3, raw, deflate, raw-LZ4-block, cubemap, array, and mip/chunk layouts.
- Success manifests encode `logical_texture_segment` identity for each source chunk as derived from researched BA2 format-defined order plus texture metadata; they do not rely on arbitrary manifest-only truth to reorder shuffled cubemap/array chunks.
- Malformed manifest covers SPEC-required fail-closed cases.
- CMake contains generator target, byproducts, and all DX10 test files.
</success_criteria>

<output>
After completion, create `.planning/phases/06-dds-boundary-and-ba2-dx10-read-reconstruction/06-01-SUMMARY.md`.
</output>
