---
phase: 06-dds-boundary-and-ba2-dx10-read-reconstruction
plan: 01
subsystem: testing
tags: [cpp, cmake, catch2, ba2, dx10, dds, fixtures]

requires:
  - phase: 05-ba2-gnrl-read-extract
    provides: BA2 GNRL fixture generation, compression routing, and CMake/Catch2 patterns
provides:
  - Legal generated BA2 DX10 FO4 and Starfield v3 success fixture corpus
  - BA2 DX10 malformed fixture corpus with stable expected error-code manifests
  - CMake generator target and placeholder CTest/Catch labels for downstream DX10 RED plans
affects: [phase-06-dx10-parser, phase-06-dds-layout, phase-06-dx10-extraction]

tech-stack:
  added: []
  patterns: [generated-fixture-tooling, manifest-backed-dx10-tests, catch2-placeholder-red-labels]

key-files:
  created:
    - tests/fixtures/generated/generate_ba2_dx10_fixtures.cpp
    - tests/unit/ba2_dx10_metadata_tests.cpp
    - tests/unit/dds_layout_tests.cpp
    - tests/unit/ba2_dx10_parser_tests.cpp
    - tests/unit/ba2_dx10_extraction_tests.cpp
    - tests/unit/ba2_dx10_malformed_tests.cpp
    - tests/fixtures/generated/archives/ba2_dx10_fo4.ba2
    - tests/fixtures/generated/archives/ba2_dx10_fo4_manifest.json
    - tests/fixtures/generated/archives/ba2_dx10_sfv3.ba2
    - tests/fixtures/generated/archives/ba2_dx10_sfv3_manifest.json
    - tests/fixtures/generated/archives/ba2_dx10_malformed_manifest.json
  modified:
    - tests/CMakeLists.txt

key-decisions:
  - "BA2 DX10 Wave 0 fixtures are committed generated artifacts, matching prior GNRL fixture policy and enabling tests without local game archives."
  - "Placeholder DX10 Catch2 tests intentionally fail when run, but are discoverable via `ctest -N` for downstream RED/GREEN plans."

patterns-established:
  - "DX10 fixture manifests record archive chunks, expected texture metadata, DirectXTex metadata expectations, and logical texture segments."
  - "CMake target `generate_ba2_dx10_fixtures` owns all DX10 success and malformed fixture byproducts."

requirements-completed: [DDS-01, DDS-02, DDS-03, DDS-04, DDS-05, DDS-06, DDS-07]

duration: 4min
completed: 2026-05-09
---

# Phase 06 Plan 01: DX10 Fixture and Test Foundation Summary

**Legal BA2 DX10 fixture generator with FO4/Starfield manifests, malformed cases, CMake wiring, and discoverable placeholder Catch2 labels**

## Performance

- **Duration:** 4 min
- **Started:** 2026-05-09T01:35:55Z
- **Completed:** 2026-05-09T01:39:45Z
- **Tasks:** 2
- **Files modified:** 22

## Accomplishments

- Added a deterministic BA2 `DX10` fixture generator that emits FO4 and Starfield v3 success archives with raw, deflate, LZ4-block, cubemap, array, and multi-mip coverage.
- Added rich JSON manifests documenting legal provenance, public texture metadata expectations, DirectXTex validation expectations, compression routes, payload bytes, and logical texture segment identity.
- Registered the DX10 generator, generated byproducts, and split placeholder Catch2 test files so CTest can discover Phase 6 DX10 labels before downstream production implementation lands.

## Task Commits

Each task was committed atomically:

1. **Task 1: Generate legal BA2 DX10 success and malformed fixtures** - `f358889` (feat)
2. **Task 2: Register DX10 fixture and test scaffolding in CMake** - `2c16bc0` (test)

**Plan metadata:** pending final docs commit

## Files Created/Modified

- `tests/fixtures/generated/generate_ba2_dx10_fixtures.cpp` - Generates legal DX10 BA2 archives, manifests, and malformed cases from synthetic bytes.
- `tests/CMakeLists.txt` - Registers DX10 test sources, generator executable, custom target, and fixture byproducts.
- `tests/unit/ba2_dx10_metadata_tests.cpp` - Placeholder metadata RED landing zone tagged `[ba2_dx10_metadata]`.
- `tests/unit/dds_layout_tests.cpp` - Placeholder DDS layout and DirectXTex/order RED landing zones.
- `tests/unit/ba2_dx10_parser_tests.cpp` - Placeholder parser/detector RED landing zones.
- `tests/unit/ba2_dx10_extraction_tests.cpp` - Placeholder extraction/compression/DirectXTex RED landing zones.
- `tests/unit/ba2_dx10_malformed_tests.cpp` - Placeholder malformed-manifest RED landing zone.
- `tests/fixtures/generated/archives/ba2_dx10_*` - Generated legal success and malformed BA2 DX10 archive/manifest byproducts.

## Decisions Made

- Committed generated DX10 byproducts because existing generated BA2 fixtures are tracked and downstream tests need stable archive bytes without rerunning the generator first.
- Kept placeholder tests as explicit `FAIL("Phase 6 RED test not implemented yet")` cases so downstream TDD plans can replace them while CTest labels are available immediately.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Corrected DX10 record table size accounting**
- **Found during:** Task 2 (Register DX10 fixture and test scaffolding in CMake)
- **Issue:** The generator initially counted the fixed DX10 texture record as 28 bytes, but the emitted fixed record fields are 24 bytes before chunk records, causing fixture generation to fail with a size-accounting mismatch.
- **Fix:** Updated record-table and malformed-case offset calculations to use the emitted 24-byte fixed DX10 texture record size.
- **Files modified:** `tests/fixtures/generated/generate_ba2_dx10_fixtures.cpp`
- **Verification:** `cmake --build --preset windows-msvc-debug-static --target generate_ba2_dx10_fixtures` succeeds.
- **Committed in:** `2c16bc0` (part of Task 2 commit)

---

**Total deviations:** 1 auto-fixed (1 Rule 1 bug)
**Impact on plan:** The fix was necessary for the planned generator target to produce valid byproducts. No scope expansion.

## Known Stubs

- `tests/unit/ba2_dx10_metadata_tests.cpp:4` - Intentional placeholder `FAIL("Phase 6 RED test not implemented yet")` for downstream metadata TDD work.
- `tests/unit/dds_layout_tests.cpp:4` and `:8` - Intentional placeholder failures for downstream DDS layout/order TDD work.
- `tests/unit/ba2_dx10_parser_tests.cpp:4` and `:8` - Intentional placeholder failures for downstream parser/detector TDD work.
- `tests/unit/ba2_dx10_extraction_tests.cpp:4`, `:8`, and `:12` - Intentional placeholder failures for downstream extraction/compression/DirectXTex TDD work.
- `tests/unit/ba2_dx10_malformed_tests.cpp:4` - Intentional placeholder failure for downstream malformed-case TDD work.

## Issues Encountered

- Generator verification initially failed due to the record-size accounting bug described above; fixed and re-ran the required build/CTest discovery commands successfully.

## User Setup Required

None - no external service configuration required.

## Verification

- `python -c "...required=['BTDX','DX10','BAADF00D','compression_method','no game or TES5Edit bytes copied']..."` — passed.
- `rg -n "BTDX|DX10|BAADF00D|compression_method|logical_texture_segment|array_index|face_index|source_chunk_index|format-defined.*order|BA2.*order|no game or TES5Edit bytes copied" tests/fixtures/generated/generate_ba2_dx10_fixtures.cpp` — passed.
- `cmake --build --preset windows-msvc-debug-static --target generate_ba2_dx10_fixtures` — passed.
- `cmake --build --preset windows-msvc-debug-static --target libbsa_tests` — passed.
- `ctest --preset windows-msvc-debug-static -N -R "ba2_dx10|dds_layout"` — passed, listing 9 tests.
- `git -C TES5Edit status --short` — passed with no output.

## Next Phase Readiness

Ready for Plan 06-02 and later TDD plans to replace placeholder DX10 tests with real RED assertions and production DX10/DDS implementation.

## Self-Check: PASSED

- Verified summary, generator, CMake registration, and all split DX10 test files exist.
- Verified task commits `f358889` and `2c16bc0` are present in git history.

---
*Phase: 06-dds-boundary-and-ba2-dx10-read-reconstruction*
*Completed: 2026-05-09*
