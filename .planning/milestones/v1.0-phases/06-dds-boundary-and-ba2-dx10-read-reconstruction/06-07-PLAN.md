---
phase: 06-dds-boundary-and-ba2-dx10-read-reconstruction
plan: 07
type: execute
wave: 7
depends_on: [06-06]
files_modified:
  - src/formats/ba2/ba2_dx10_parser.cpp
  - tests/unit/ba2_dx10_parser_tests.cpp
autonomous: true
gap_closure: true
requirements: [DDS-01, DDS-02, DDS-03]
must_haves:
  truths:
    - "BA2 DX10 open-time parsing is bounded and parses exactly file_count length-prefixed names without allocating the FileTableOffset-to-first-payload gap."
    - "Sparse BA2 DX10 archives with a large filename-table-to-payload gap can be opened/listed without reading payload-gap bytes."
  artifacts:
    - path: "src/formats/ba2/ba2_dx10_parser.cpp"
      provides: "Incremental length-prefixed DX10 filename parsing from host file"
      contains: "parse_ba2_dx10_names_from_file"
    - path: "tests/unit/ba2_dx10_parser_tests.cpp"
      provides: "Regression coverage for sparse DX10 bounded open/list behavior"
      contains: "sparse"
  key_links:
    - from: "src/formats/ba2/ba2_dx10_parser.cpp"
      to: "parse_ba2_dx10_archive_impl"
      via: "metadata bytes plus exactly parsed length-prefixed names"
      pattern: "parse_ba2_dx10_names_from_file.*parse_ba2_dx10_archive_impl"
---

<objective>
Close the verification gap where BA2 DX10 open/list parsing reads the entire `FileTableOffset` to first-payload interval into memory.

Purpose: Preserve Phase 6's bounded metadata-read contract for untrusted sparse or malicious BA2 DX10 archives.
Output: Incremental DX10 filename parsing and a regression test proving open/list does not require allocating the whole payload gap.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-dds-boundary-and-ba2-dx10-read-reconstruction/06-VERIFICATION.md
@.planning/phases/06-dds-boundary-and-ba2-dx10-read-reconstruction/06-04-SUMMARY.md
@src/formats/ba2/ba2_dx10_parser.cpp
@tests/unit/ba2_dx10_parser_tests.cpp

<interfaces>
From `src/formats/ba2/ba2_dx10_parser.hpp`:
```cpp
struct ba2_dx10_archive {
  archive_metadata metadata;
  std::vector<entry_metadata> entries;
};

result<ba2_dx10_archive> parse_ba2_dx10_archive(std::span<const std::byte> bytes, detected_ba2_format detected);
result<ba2_dx10_archive> parse_ba2_dx10_archive_file(std::string_view host_path, std::uint64_t archive_size,
                                                     detected_ba2_format detected);
```

Current failing implementation in `parse_ba2_dx10_archive_file` computes:
```cpp
const auto name_table_size_u64 = first_payload_offset.value() - header.value().file_table_offset;
auto name_table_bytes = read_file_bytes_at(input, header.value().file_table_offset,
                                           static_cast<std::size_t>(name_table_size_u64),
                                           "BA2 DX10 filename table");
```

Replace this with exact incremental parsing of `file_count` UInt16-prefixed names from `FileTableOffset`, bounded by `first_payload_offset`, then append only those encoded name bytes to `metadata_bytes` before calling `parse_ba2_dx10_archive_impl`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add bounded sparse DX10 open regression</name>
  <files>tests/unit/ba2_dx10_parser_tests.cpp</files>
  <read_first>
    - tests/unit/ba2_dx10_parser_tests.cpp
    - tests/fixtures/generated/generate_ba2_dx10_fixtures.cpp
    - .planning/phases/06-dds-boundary-and-ba2-dx10-read-reconstruction/06-VERIFICATION.md
  </read_first>
  <behavior>
    - Test opens a synthetic BA2 DX10 archive whose `FileTableOffset` names end immediately after `file_count` UInt16-prefixed names but whose first payload offset is far beyond the names.
    - Test proves `archive_reader::open`, `entries`, `find`, and `contains` succeed without requiring the entire sparse gap to be read into a vector.
    - Test preserves generated legal fixture policy: create the sparse archive inside the test temp/build area or mutate repository-owned generated fixture bytes; do not use `TES5Edit/` or real game data.
  </behavior>
  <action>
    Add a focused regression test to `tests/unit/ba2_dx10_parser_tests.cpp` for the verification gap: `BA2 DX10 open-time parsing is bounded and does not allocate the entire filename-table-to-payload gap`. Construct or mutate a minimal repository-owned DX10 archive so `first_payload_offset - FileTableOffset` is deliberately large while the valid filename table contains exactly `file_count` length-prefixed names. The test must fail against the current implementation because the parser reads the whole gap. Keep assertions stable: expected success after the fix is `archive_reader::open` has value, `entries().value().size()` equals the fixture file count, and lookup for the known canonical path succeeds. Do not add copyrighted fixtures and do not touch `TES5Edit/` per D-34.</action>
  <verify>
    <automated>cmake --build --preset windows-msvc-debug-static --target libbsa_tests &amp;&amp; ctest --preset windows-msvc-debug-static -R ba2_dx10_detector --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `tests/unit/ba2_dx10_parser_tests.cpp` contains the substring `sparse` in a BA2 DX10 test case name or helper name.
    - The new test references `archive_reader::open` and at least one of `entries`, `find`, or `contains`.
    - The test setup does not reference `TES5Edit/` and does not require `LIBBSA_GAME_FIXTURES`.
  </acceptance_criteria>
  <done>Regression test exists and fails before the parser fix, then passes once Task 2 is complete.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Parse exactly file_count DX10 names incrementally</name>
  <files>src/formats/ba2/ba2_dx10_parser.cpp</files>
  <read_first>
    - src/formats/ba2/ba2_dx10_parser.cpp
    - src/formats/ba2/ba2_gnrl_parser.cpp
    - src/detail/binary_io.hpp
    - .planning/phases/06-dds-boundary-and-ba2-dx10-read-reconstruction/06-VERIFICATION.md
  </read_first>
  <behavior>
    - Parser reads exactly `file_count` UInt16 length prefixes and name byte spans from `FileTableOffset`.
    - Parser rejects a length-prefixed name that would cross `first_payload_offset` with `error_code::format_error`.
    - Parser no longer allocates `first_payload_offset - FileTableOffset` as the filename table buffer.
  </behavior>
  <action>
    In `parse_ba2_dx10_archive_file`, remove the full-gap `name_table_size_u64` vector read. Add a private helper such as `parse_ba2_dx10_names_from_file(std::ifstream&amp; input, std::uint64_t file_table_offset, std::uint64_t first_payload_offset, std::uint32_t file_count)` that seeks to `file_table_offset`, reads a 2-byte little-endian length for each of `file_count` names, validates `cursor + 2 + length &lt;= first_payload_offset`, reads only that name's bytes, and appends the exact encoded UInt16 length plus bytes to a compact `std::vector&lt;std::byte&gt;`. Return `error_code::format_error` for truncated names, oversized length spans, or names crossing payload data; return `io_error` only for host-file seek/read failures. Preserve D-19 canonical path behavior by still delegating final name validation to `parse_ba2_dx10_archive_impl`. Add a short WHY comment explaining that BA2 filename tables are count-delimited, so the parser must not allocate sparse padding between the last name and the first payload.</action>
  <verify>
    <automated>cmake --build --preset windows-msvc-debug-static --target libbsa_tests &amp;&amp; ctest --preset windows-msvc-debug-static -R "ba2_dx10_detector|ba2_dx10_metadata|ba2_dx10_layout" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `src/formats/ba2/ba2_dx10_parser.cpp` no longer contains `name_table_size_u64 = first_payload_offset.value() - header.value().file_table_offset`.
    - `src/formats/ba2/ba2_dx10_parser.cpp` contains a helper or code path named with `file_count`/`names` semantics, such as `parse_ba2_dx10_names_from_file`.
    - `src/formats/ba2/ba2_dx10_parser.cpp` contains a comment mentioning sparse padding or sparse BA2 archives near the incremental name parsing logic.
  </acceptance_criteria>
  <done>DX10 open/list parsing consumes only exactly encoded name bytes and rejects malformed name spans without reading the whole gap.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Archive file -&gt; BA2 DX10 parser | Untrusted local archive bytes control offsets, name lengths, counts, and payload spans. |
| Parser -&gt; heap allocation | Untrusted metadata could otherwise trigger oversized allocations during open/list. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07-01 | D | `parse_ba2_dx10_archive_file` | mitigate | Parse exactly `file_count` length-prefixed names and reject names crossing `first_payload_offset` instead of allocating sparse gaps. |
| T-06-07-02 | T | `parse_ba2_dx10_archive_file` | mitigate | Preserve checked cursor/span validation and return `format_error` for malformed name table metadata. |
</threat_model>

<verification>
Run `ctest --preset windows-msvc-debug-static -R "ba2_dx10_detector|ba2_dx10_metadata|ba2_dx10_layout" --output-on-failure` and `git -C TES5Edit status --short`.
</verification>

<success_criteria>
The verifier's bounded-open truth is closed: sparse or malicious BA2 DX10 archives cannot force open-time allocation of the filename-table-to-payload gap, and focused DX10 parser tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/06-dds-boundary-and-ba2-dx10-read-reconstruction/06-07-SUMMARY.md`.
</output>
