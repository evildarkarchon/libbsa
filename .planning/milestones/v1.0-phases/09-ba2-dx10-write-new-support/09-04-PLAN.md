---
phase: 09-ba2-dx10-write-new-support
plan: 04
type: tdd
wave: 2
depends_on:
  - 09-01
  - 09-02
files_modified:
  - src/formats/ba2/ba2_dx10_writer.hpp
  - src/formats/ba2/ba2_dx10_writer.cpp
  - include/libbsa/writer.hpp
  - CMakeLists.txt
  - tests/unit/ba2_dx10_writer_tests.cpp
requirements:
  - WBA2-08
requirements_addressed:
  - WBA2-08
autonomous: true
must_haves:
  truths:
    - "D-01: Implemented writer object follows the public BA2 DX10 target/options/add_file/write_to shape established in Plan 09-01."
    - "D-02: add_file validates archive paths and DDS host files at add time, not only during write_to."
    - "D-03: Successfully added DDS entries are snapshotted into writer-owned bytes so later source mutations do not affect output."
    - "D-04: add_file accepts DDS host file paths only and does not add a public memory-buffer path."
    - "D-28: Empty source paths, missing files, malformed DDS files, and unsupported DDS formats return structured errors from add_file before any archive finalization."
    - `add_file` validates archive paths and DDS host files at add time.
    - Successfully added DDS entries are snapshotted into writer-owned bytes so later source mutations do not affect output.
    - Empty source paths, missing files, malformed DDS files, and unsupported DDS formats return structured errors from `add_file`.
  artifacts:
    - path: src/formats/ba2/ba2_dx10_writer.hpp
      provides: private DX10 writer entry state handoff
      contains: ba2_dx10_writer_entry
    - path: src/formats/ba2/ba2_dx10_writer.cpp
      provides: public method definitions and add-time validation/snapshotting
      contains: ba2_dx10_writer::add_file
    - path: CMakeLists.txt
      provides: production DX10 writer source registration
      contains: ba2_dx10_writer.cpp
  key_links:
    - from: src/formats/ba2/ba2_dx10_writer.cpp
      to: src/texture/directxtex_analyzer.cpp
      via: `texture::analyze_dds_source` during add_file
      pattern: analyze_dds_source
---

<objective>
Implement the public DX10 writer object state and add-time DDS validation/snapshot workflow.

Purpose: D-02 and D-03 require validation and ownership at add time, not deferred path reads during `write_to`.
Output: Build-registered private writer files with constructors, target/options accessors, `add_file`, and a placeholder `write_to` handoff ready for serialization.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
@C:/Users/evild/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/09-ba2-dx10-write-new-support/09-CONTEXT.md
@.planning/phases/09-ba2-dx10-write-new-support/09-RESEARCH.md
@.planning/phases/09-ba2-dx10-write-new-support/09-PATTERNS.md
@include/libbsa/writer.hpp
@src/formats/ba2/ba2_gnrl_writer.hpp
@src/formats/ba2/ba2_gnrl_writer.cpp
@src/texture/directxtex_analyzer.hpp
@src/detail/archive_path.hpp
@CMakeLists.txt
@tests/unit/ba2_dx10_writer_tests.cpp

<interfaces>
Create `src/formats/ba2/ba2_dx10_writer.hpp`:

```cpp
namespace libbsa::formats::ba2 {
struct ba2_dx10_writer_entry {
  std::string archive_path_original;
  std::string archive_path_canonical;
  std::vector<std::byte> dds_bytes;
  texture::dds_source_analysis source;
};

result<void> write_ba2_dx10_archive(ba2_dx10_target target,
                                    const ba2_dx10_writer_options& options,
                                    std::span<const ba2_dx10_writer_entry> entries,
                                    std::string_view output_host_path);
}
```

`write_ba2_dx10_archive` may return `error_code::unsupported` until Plan 09-05 implements serialization, but `add_file` behavior must be complete.
</interfaces>
</context>

<feature>
  <name>DX10 writer add-time validation and snapshot state</name>
  <files>src/formats/ba2/ba2_dx10_writer.*, include/libbsa/writer.hpp, CMakeLists.txt, tests/unit/ba2_dx10_writer_tests.cpp</files>
  <behavior>
    - Empty archive paths fail through existing archive path normalization.
    - Empty DDS host paths return `error_code::invalid_argument`.
    - Missing/unreadable DDS source paths return structured I/O errors.
    - Malformed/unsupported DDS bytes return `error_code::format_error` from add_file.
    - A valid DDS added, then modified or deleted on disk, remains represented by original writer-owned bytes.
  </behavior>
  <implementation>Mirror BA2 GNRL public state pattern, but replace path-only entry storage with validated DDS bytes and source analysis.</implementation>
</feature>

<tasks>
<task type="auto">
  <name>RED: Specify add-time validation and snapshot semantics</name>
  <read_first>tests/unit/ba2_dx10_writer_tests.cpp; include/libbsa/writer.hpp; src/formats/ba2/ba2_gnrl_writer.cpp; .planning/phases/09-ba2-dx10-write-new-support/09-CONTEXT.md</read_first>
  <files>tests/unit/ba2_dx10_writer_tests.cpp</files>
  <action>Add failing tests for `ba2_dx10_writer::add_file`: valid source returns success; empty DDS host path returns `error_code::invalid_argument`; missing DDS host path returns a stable non-success structured error; malformed and unsupported DDS fixtures from the writer source manifest return `error_code::format_error`; adding a valid DDS then overwriting/deleting the host file before a future write does not make `add_file` state invalid. For the snapshot test, assert add succeeds before mutation and leave final archive proof to Plan 09-05.</action>
  <verify><automated>ctest --preset windows-msvc-debug-static -R ba2_dx10_writer.*add --output-on-failure</automated></verify>
  <acceptance_criteria>`tests/unit/ba2_dx10_writer_tests.cpp` contains `empty DDS`; contains `missing`; contains `malformed`; contains `unsupported`; contains `snapshot`; contains `ba2_dx10_writer::add_file` or `.add_file(`.</acceptance_criteria>
  <done>RED gate fails because `ba2_dx10_writer` methods are declaration-only or unregistered.</done>
</task>

<task type="auto">
  <name>GREEN: Implement writer state, add_file, and build registration</name>
  <read_first>src/formats/ba2/ba2_dx10_writer.hpp; src/formats/ba2/ba2_dx10_writer.cpp; include/libbsa/writer.hpp; src/formats/ba2/ba2_gnrl_writer.cpp; src/texture/directxtex_analyzer.hpp; src/detail/archive_path.hpp; CMakeLists.txt</read_first>
  <files>src/formats/ba2/ba2_dx10_writer.hpp, src/formats/ba2/ba2_dx10_writer.cpp, include/libbsa/writer.hpp, CMakeLists.txt</files>
  <action>Create private writer header/source. Define `ba2_dx10_writer::state` with `ba2_dx10_target target`, `ba2_dx10_writer_options options`, and `std::vector<formats::ba2::ba2_dx10_writer_entry> entries`. Implement constructors and accessors. Implement `add_file` by normalizing `archive_path`, preserving original slash spelling, rejecting empty `dds_host_path` with `invalid_argument`, reading the entire DDS host file into `std::vector<std::byte>`, calling `texture::analyze_dds_source`, and pushing writer-owned `dds_bytes` plus analysis into state. Register `src/formats/ba2/ba2_dx10_writer.cpp` in root `CMakeLists.txt`. Implement `write_to` to call private `write_ba2_dx10_archive`, which may return `unsupported` until Plan 09-05. Add Doxygen comments for any substantially defined public methods if the declarations lack sufficient comments.</action>
  <verify><automated>ctest --preset windows-msvc-debug-static -R ba2_dx10_writer.*add --output-on-failure</automated></verify>
  <acceptance_criteria>`src/formats/ba2/ba2_dx10_writer.cpp` contains `struct ba2_dx10_writer::state`; contains `normalize_archive_path`; contains `analyze_dds_source`; contains `dds_bytes`; `CMakeLists.txt` contains `src/formats/ba2/ba2_dx10_writer.cpp`; `include/libbsa/writer.hpp` contains no DirectXTex token.</acceptance_criteria>
  <done>add_file validation/snapshot tests pass; write_to serialization remains intentionally unsupported until Plan 09-05.</done>
</task>

<task type="auto">
  <name>REFACTOR: Tighten ownership and duplicate-path preconditions</name>
  <read_first>src/formats/ba2/ba2_dx10_writer.cpp; src/formats/ba2/ba2_gnrl_writer.cpp; tests/unit/ba2_dx10_writer_tests.cpp; AGENTS.md</read_first>
  <files>src/formats/ba2/ba2_dx10_writer.cpp, tests/unit/ba2_dx10_writer_tests.cpp</files>
  <action>Ensure duplicate canonical archive paths are accepted during `add_file` and deferred to `write_to` validation, matching prior writer decisions. Add a focused test that two entries with the same canonical archive path can be added successfully for now. Add a short comment explaining DDS bytes are copied at add time because D-03 requires later source file changes/deletion not to affect output.</action>
  <verify><automated>ctest --preset windows-msvc-debug-static -R ba2_dx10_writer.*add --output-on-failure</automated></verify>
  <acceptance_criteria>`src/formats/ba2/ba2_dx10_writer.cpp` contains `D-03` or `source file changes`; `tests/unit/ba2_dx10_writer_tests.cpp` contains `duplicate`; add-time duplicate test asserts success, not `format_error`.</acceptance_criteria>
  <done>Writer state mirrors established add-time/deferred-write validation boundaries and owns DDS bytes.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host path → writer.add_file | Caller-controlled filesystem path is read and validated. |
| DDS bytes → writer state | Caller-controlled bytes become writer-owned archive source data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04-01 | Tampering/DoS | ba2_dx10_writer::add_file | mitigate | Reject empty paths, read with checked file state, validate through `analyze_dds_source` before storing. |
| T-09-04-02 | Tampering | writer-owned DDS snapshot | mitigate | Copy bytes into state at add time so later filesystem changes cannot alter output. |
| T-09-04-03 | Repudiation | duplicate archive paths | mitigate | Defer duplicate canonical validation to write-time tests to match prior writer semantics and produce deterministic failure point. |
</threat_model>

<verification>
Run `ctest --preset windows-msvc-debug-static -R "ba2_dx10_writer.*add|public_include_boundary" --output-on-failure`.
</verification>

<success_criteria>
- RED/GREEN TDD commits exist for `09-04`.
- DX10 writer production source is registered in CMake.
- add_file validates and snapshots DDS host files.
- Public boundary tests remain green.
</success_criteria>

<output>
After completion, create `.planning/phases/09-ba2-dx10-write-new-support/09-04-SUMMARY.md`.
</output>
