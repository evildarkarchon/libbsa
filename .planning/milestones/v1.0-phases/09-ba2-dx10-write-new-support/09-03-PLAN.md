---
phase: 09-ba2-dx10-write-new-support
plan: 03
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/texture/dds_layout.hpp
  - src/texture/dds_layout.cpp
  - tests/unit/dds_layout_tests.cpp
requirements:
  - WBA2-09
requirements_addressed:
  - WBA2-09
autonomous: true
must_haves:
  truths:
    - Writer code can compute raw mip byte sizes for every locked DDS format without DirectXTex types.
    - "D-12: Chunk planning exposes one archive-wide max decoded chunk-byte control rather than max-mips or named policy controls."
    - "D-13: Chunk planning configuration is archive-wide only, with no per-entry chunk-limit override in Phase 9."
    - "D-14: Default chunking is reference-derived; if reference evidence is inconclusive, conservative contiguous mip groups are documented."
    - "D-15: Array textures and cubemaps use a uniform mip-range split pattern across every slice or face."
    - "D-16: Chunk planning preserves the no-transform rule: no resizing, transcoding, mip generation, repair, or image-data reordering beyond BA2-required serialization."
    - Writer code can plan BA2-compatible contiguous mip chunks with reference-derived defaults or an archive-wide decoded-byte cap.
    - Array and cubemap textures use a uniform mip split pattern across every slice or face.
  artifacts:
    - path: src/texture/dds_layout.hpp
      provides: chunk planning contracts and planner result structs
      contains: plan_dx10_chunks
    - path: src/texture/dds_layout.cpp
      provides: locked-format size table and checked chunk planner
      contains: max_decoded_chunk_bytes
    - path: tests/unit/dds_layout_tests.cpp
      provides: table-driven format size and planner behavior tests
      contains: BC7
  key_links:
    - from: src/texture/dds_layout.cpp
      to: src/texture/dds_layout.hpp
      via: `plan_dx10_chunks` implementation
      pattern: result<std::vector<planned_texture_chunk>>
---

<objective>
Implement the DX10 DDS format-size and chunk-planning logic as a tested private texture utility.

Purpose: Serialization must not guess mip sizes or reorder data; it needs a deterministic, checked planner satisfying D-12 through D-16.
Output: Planner contracts and tests for all locked formats, reference defaults, explicit byte caps, arrays, cubemaps, and fail-closed cases.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
@C:/Users/evild/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/09-ba2-dx10-write-new-support/09-CONTEXT.md
@.planning/phases/09-ba2-dx10-write-new-support/09-RESEARCH.md
@.planning/phases/09-ba2-dx10-write-new-support/09-PATTERNS.md
@src/texture/dds_layout.hpp
@src/texture/dds_layout.cpp
@tests/unit/dds_layout_tests.cpp

<interfaces>
Add libbsa-owned internal planner types, with exact names unless existing code conflicts:

```cpp
struct planned_texture_chunk {
  std::uint32_t array_index;
  std::uint32_t face_index;
  std::uint32_t start_mip;
  std::uint32_t end_mip;
  std::uint64_t raw_size;
};

result<std::uint64_t> mip_size_for_format(const dds_texture_layout& layout, std::uint32_t mip);
result<std::uint64_t> mip_range_size(const dds_texture_layout& layout, std::uint32_t start_mip, std::uint32_t end_mip);
result<std::vector<planned_texture_chunk>> plan_dx10_chunks(const dds_texture_layout& layout,
                                                            std::uint32_t max_decoded_chunk_bytes);
```

`max_decoded_chunk_bytes == 0` means reference default: up to four chunks, one mip per chunk while dimensions are at least 512x512, remaining mips grouped in the final chunk.
</interfaces>
</context>

<feature>
  <name>DX10 mip sizing and chunk planning</name>
  <files>src/texture/dds_layout.*, tests/unit/dds_layout_tests.cpp</files>
  <behavior>
    - BC1/BC4 formats use 4x4 blocks of 8 bytes with minimum one block per dimension.
    - BC3/BC5/BC6H/BC7 formats use 4x4 blocks of 16 bytes with minimum one block per dimension.
    - R8 uses 1 byte per pixel; RGBA/BGRA formats use 4 bytes per pixel.
    - Explicit byte caps split only at mip boundaries and fail with `format_error` when a single mip cannot fit.
    - Cubemap and array plans repeat the same mip ranges for every face/slice.
  </behavior>
  <implementation>Extend existing checked arithmetic helpers and validation style; do not add DirectXTex includes.</implementation>
</feature>

<tasks>
<task type="auto">
  <name>RED: Specify format sizing and chunk planner behavior</name>
  <read_first>tests/unit/dds_layout_tests.cpp; src/texture/dds_layout.hpp; src/texture/dds_layout.cpp; .planning/phases/09-ba2-dx10-write-new-support/09-RESEARCH.md</read_first>
  <files>tests/unit/dds_layout_tests.cpp</files>
  <action>Add failing tests for `mip_size_for_format`, `mip_range_size`, and `plan_dx10_chunks`. Include exact DXGI format IDs and names in table comments: `BC1_UNORM=71`, `BC1_UNORM_SRGB=72`, `BC3_UNORM=77`, `BC4_UNORM=80`, `BC5_UNORM=83`, `BC5_SNORM=84`, `BC6H_UF16=95`, `BC7_UNORM=98`, `R8G8B8A8_UNORM_SRGB=29`, `B8G8R8A8_UNORM=87`, `R8_UNORM=61`, `R8G8B8A8_SNORM=31`. Test a 4x4 BC1 mip is 8 bytes, 4x4 BC7 is 16 bytes, 2x2 BC1 is 8 bytes, 4x4 R8 is 16 bytes, and 4x4 RGBA/BGRA is 64 bytes. Add planner tests for default 1024x1024 5-mip layout, cap-forced splits, impossible cap, array_size=2, and cubemap=true.</action>
  <verify><automated>ctest --preset windows-msvc-debug-static -R dds_layout --output-on-failure</automated></verify>
  <acceptance_criteria>`tests/unit/dds_layout_tests.cpp` contains `plan_dx10_chunks`; contains `BC6H_UF16`; contains `BC7_UNORM`; contains `max_decoded_chunk_bytes`; contains `array_size = 2`; contains `is_cubemap = true`.</acceptance_criteria>
  <done>RED gate fails because planner APIs or locked-format support are absent.</done>
</task>

<task type="auto">
  <name>GREEN: Implement checked locked-format sizing and planner</name>
  <read_first>src/texture/dds_layout.hpp; src/texture/dds_layout.cpp; tests/unit/dds_layout_tests.cpp; .planning/phases/09-ba2-dx10-write-new-support/09-CONTEXT.md; .planning/phases/09-ba2-dx10-write-new-support/09-PATTERNS.md</read_first>
  <files>src/texture/dds_layout.hpp, src/texture/dds_layout.cpp</files>
  <action>Add the planner types/functions from the interface block. Replace the narrow format switch with a descriptor table for the locked formats. Compute BC sizes as `max(1, (width+3)/4) * max(1, (height+3)/4) * bytes_per_block` with checked uint64 multiplication. Compute uncompressed sizes as `width * height * bytes_per_pixel` with checked multiplication. Implement reference default chunking for `max_decoded_chunk_bytes == 0`: up to four chunks, split one mip per chunk while width and height are both at least 512, then group all remaining mips. Implement nonzero cap by greedily grouping contiguous mip ranges without exceeding the cap; if one mip exceeds the cap, return `error_code::format_error`. Repeat the same mip ranges for each array slice or cubemap face per D-15.</action>
  <verify><automated>ctest --preset windows-msvc-debug-static -R dds_layout --output-on-failure</automated></verify>
  <acceptance_criteria>`src/texture/dds_layout.hpp` contains `struct planned_texture_chunk`; contains `plan_dx10_chunks`; `src/texture/dds_layout.cpp` contains `bytes_per_block`; contains `max_decoded_chunk_bytes == 0`; contains `512U`; contains `95U`; contains `98U`.</acceptance_criteria>
  <done>Format sizing and chunk planner tests pass.</done>
</task>

<task type="auto">
  <name>REFACTOR: Reuse planner in existing chunk validation safely</name>
  <read_first>src/texture/dds_layout.hpp; src/texture/dds_layout.cpp; tests/unit/dds_layout_tests.cpp; .planning/phases/09-ba2-dx10-write-new-support/09-RESEARCH.md</read_first>
  <files>src/texture/dds_layout.cpp, tests/unit/dds_layout_tests.cpp</files>
  <action>Deduplicate sizing logic used by existing `validate_and_order_chunks` and new planner so both use the same `mip_size_for_format`/`mip_range_size` path. Add comments only for non-obvious block-compressed minimum-block sizing and reference-derived default chunking. Preserve existing extraction tests by keeping previous fixture-supported format behavior unchanged.</action>
  <verify><automated>ctest --preset windows-msvc-debug-static -R "dds_layout|ba2_dx10" --output-on-failure</automated></verify>
  <acceptance_criteria>`src/texture/dds_layout.cpp` contains `block-compressed` or `block compressed`; contains `reference-derived` or `reference derived`; `tests/unit/dds_layout_tests.cpp` contains a case where `REQUIRE_FALSE` checks an impossible cap returns `format_error`.</acceptance_criteria>
  <done>Planner and existing DX10 extraction layout validation share checked sizing logic.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DDS metadata → chunk planner | Caller-controlled dimensions/mips/formats are converted to BA2 record sizes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03-01 | DoS/Tampering | mip_size_for_format | mitigate | Use uint64 checked multiplication and format whitelist; return `format_error` on overflow/unsupported formats. |
| T-09-03-02 | Tampering | plan_dx10_chunks | mitigate | Split only contiguous mip ranges; repeat identical ranges for arrays/cubemaps; existing validator proves no gaps/duplicates. |
| T-09-03-03 | DoS | max_decoded_chunk_bytes | mitigate | Reject impossible caps instead of producing zero-sized or overflowing records. |
</threat_model>

<verification>
Run `ctest --preset windows-msvc-debug-static -R "dds_layout|ba2_dx10" --output-on-failure`.
</verification>

<success_criteria>
- RED/GREEN TDD commits exist for `09-03`.
- All locked formats have tested size math.
- Planner supports default and explicit chunk caps without gaps or overlaps.
- Array/cubemap plans repeat a uniform mip pattern.
</success_criteria>

<output>
After completion, create `.planning/phases/09-ba2-dx10-write-new-support/09-03-SUMMARY.md`.
</output>
