---
phase: 09-ba2-dx10-write-new-support
plan: 06
type: tdd
wave: 4
depends_on:
  - 09-05
files_modified:
  - src/formats/ba2/ba2_dx10_writer.cpp
  - tests/unit/ba2_dx10_writer_tests.cpp
requirements:
  - WBA2-10
  - WBA2-11
requirements_addressed:
  - WBA2-10
  - WBA2-11
autonomous: true
must_haves:
  truths:
    - "D-17: DX10 deduplication is an archive-level boolean option, disabled by default, matching TES4-family and BA2 GNRL writer patterns."
    - "D-18: When enabled, dedupe is archive-wide; eligible chunks may share payload offsets while each texture record, chunk record, and filename-table entry remains distinct."
    - "D-19: Dedupe runs after chunk planning and archive-level compression; chunk splitting does not change to improve dedupe opportunities."
    - "D-20: Dedupe eligibility requires matching final stored bytes plus raw size, stored size, and compression route, not matching texture dimensions, format, cubemap/array state, or mip range."
    - "D-21: The first eligible chunk in deterministic record/chunk order owns the physical payload bytes and later duplicates reuse that offset."
    - "D-22: Complete duplicate DDS entries may share every eligible chunk offset when dedupe is enabled while preserving separate texture records and names."
    - "D-23: Tests prove disabled output has distinct eligible offsets, enabled output shares eligible offsets, and both modes reopen and extract valid DDS bytes."
    - Deduplication is disabled by default and keeps eligible chunk offsets distinct.
    - When enabled, byte-identical final stored chunks with matching raw size, stored size, and compression route share payload offsets deterministically.
    - DX10 writer finalization uses collision-safe temporary directories and preserves caller data on overwrite failures.
    - The full Phase 9 writer suite, public boundary suite, and TES5Edit cleanliness gate pass.
  artifacts:
    - path: src/formats/ba2/ba2_dx10_writer.cpp
      provides: DX10 chunk dedupe and safe publish finalization
      contains: deduplicate_payloads
    - path: tests/unit/ba2_dx10_writer_tests.cpp
      provides: dedupe, safe publish, and final regression tests
      contains: temp-name
  key_links:
    - from: src/formats/ba2/ba2_dx10_writer.cpp
      to: tests/unit/ba2_dx10_writer_tests.cpp
      via: reader-backed offset/dedupe assertions
      pattern: deduplicate_payloads
---

<objective>
Finish DX10 writer deduplication, filesystem-safe publish behavior, and full phase regression gates.

Purpose: D-17 through D-23 and Requirement 11 require deterministic optional chunk dedupe and Phase 8-grade filesystem safety before Phase 9 can be considered shippable.
Output: Dedupe implementation, safe publish hardening, tests, and final verification commands.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
@C:/Users/evild/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-ba2-dx10-write-new-support/09-CONTEXT.md
@.planning/phases/09-ba2-dx10-write-new-support/09-SPEC.md
@.planning/phases/09-ba2-dx10-write-new-support/09-RESEARCH.md
@.planning/phases/09-ba2-dx10-write-new-support/09-PATTERNS.md
@src/formats/ba2/ba2_dx10_writer.cpp
@src/formats/ba2/ba2_gnrl_writer.cpp
@tests/unit/ba2_dx10_writer_tests.cpp
@tests/unit/ba2_gnrl_writer_tests.cpp

<interfaces>
Deduplication key must include:
- final stored bytes
- raw size
- packed/stored size
- compression route (`deflate` vs `lz4_block`)

It must not include texture dimensions, DXGI format, cubemap state, array state, or mip range unless needed by the four required key fields above; D-20 explicitly allows otherwise different textures to share eligible chunks.
</interfaces>
</context>

<feature>
  <name>DX10 dedupe and safe publish completion</name>
  <files>src/formats/ba2/ba2_dx10_writer.cpp, tests/unit/ba2_dx10_writer_tests.cpp</files>
  <behavior>
    - Duplicate DDS entries produce distinct offsets when `deduplicate_payloads=false`.
    - Duplicate DDS entries share all eligible chunk offsets when `deduplicate_payloads=true`.
    - First eligible chunk in deterministic record/chunk order owns payload bytes.
    - Existing output is preserved when overwrite is false.
    - Non-regular overwrite targets fail safely.
    - Caller-owned temp-name sibling files are not deleted.
  </behavior>
  <implementation>Adapt Phase 8 safe publish pattern and use a DX10-specific dedupe key that includes final stored bytes and required chunk metadata.</implementation>
</feature>

<tasks>
<task type="auto">
  <name>RED: Specify DX10 dedupe and safe publish behavior</name>
  <read_first>tests/unit/ba2_dx10_writer_tests.cpp; tests/unit/ba2_gnrl_writer_tests.cpp; src/formats/ba2/ba2_dx10_writer.cpp; src/formats/ba2/ba2_gnrl_writer.cpp; .planning/phases/09-ba2-dx10-write-new-support/09-CONTEXT.md</read_first>
  <files>tests/unit/ba2_dx10_writer_tests.cpp</files>
  <action>Add failing tests for D-17 through D-23: two identical DDS entries with default options reopen with distinct eligible chunk offsets; the same inputs with `deduplicate_payloads=true` reopen with shared eligible offsets while both entries extract valid DDS bytes; first deterministic entry owns the physical offset. Add safe publish tests mirroring BA2 GNRL: no-overwrite preserves existing output, overwrite rejects non-regular targets, unique temp publish does not delete an existing sibling named like `<output>.tmp` or similar temp-name collision bait, and overwrite rollback preserves old output on a forced replacement failure if the existing GNRL pattern has a test hook.</action>
  <verify><automated>ctest --preset windows-msvc-debug-static -R "ba2_dx10_writer.*(dedupe|publish|overwrite|temp)" --output-on-failure</automated></verify>
  <acceptance_criteria>`tests/unit/ba2_dx10_writer_tests.cpp` contains `deduplicate_payloads = true`; contains `deduplicate_payloads = false`; contains `temp`; contains `overwrite`; contains `non-regular` or `non regular`; contains `distinct` and `shared`.</acceptance_criteria>
  <done>RED gate fails because DX10 dedupe/safe publish behavior is incomplete or not hardened.</done>
</task>

<task type="auto">
  <name>GREEN: Implement chunk dedupe key and Phase 8-grade safe publish</name>
  <read_first>src/formats/ba2/ba2_dx10_writer.cpp; src/formats/ba2/ba2_gnrl_writer.cpp; tests/unit/ba2_dx10_writer_tests.cpp; .planning/phases/09-ba2-dx10-write-new-support/09-PATTERNS.md</read_first>
  <files>src/formats/ba2/ba2_dx10_writer.cpp</files>
  <action>Implement optional DX10 chunk dedupe after chunk planning and compression, before final payload offset assignment. Use a deterministic ordered map keyed by final stored bytes plus raw size, packed size, and compression route; first record/chunk order owns bytes, later duplicates reuse offset and do not write duplicate payload bytes. Preserve distinct texture records, chunk records, and filename-table names. Replace any direct output write with the Phase 8 unique sibling temporary directory publish flow, no-overwrite guard, non-regular target rejection, overwrite backup/rollback, and cleanup that deletes only owned temporary directories/files. Add comments explaining why deterministic `<output>.tmp` is not used and why DX10 dedupe key includes chunk metadata per D-20.</action>
  <verify><automated>ctest --preset windows-msvc-debug-static -R "ba2_dx10_writer.*(dedupe|publish|overwrite|temp)" --output-on-failure</automated></verify>
  <acceptance_criteria>`src/formats/ba2/ba2_dx10_writer.cpp` contains `deduplicate_payloads`; contains `raw_size`; contains `packed_size`; contains `compression`; contains `make_unique_publish_directory` or `unique_publish`; contains `backup`; contains `rollback` or `roll back`.</acceptance_criteria>
  <done>Dedupe and safe publish tests pass.</done>
</task>

<task type="auto">
  <name>REFACTOR: Run final Phase 9 regression and cleanliness gates</name>
  <read_first>src/formats/ba2/ba2_dx10_writer.cpp; tests/unit/ba2_dx10_writer_tests.cpp; tests/unit/public_include_boundary_tests.cpp; CMakeLists.txt; tests/CMakeLists.txt; AGENTS.md</read_first>
  <files>src/formats/ba2/ba2_dx10_writer.cpp, tests/unit/ba2_dx10_writer_tests.cpp</files>
  <action>Refactor only if needed to remove duplicated publish/dedupe helper code while preserving comments and behavior. Run focused and broad tests. Fix any Phase 9 regressions in writer, public boundary, BA2 DX10 reader/extraction, BA2 GNRL writer/reader, TES4-family writer, or cross-format tests. Do not edit or stage anything under `TES5Edit/`.</action>
  <verify><automated>cmake --build --preset windows-msvc-debug-static && ctest --preset windows-msvc-debug-static -R "ba2_dx10_writer|ba2_dx10|ba2_gnrl_writer|public_include_boundary|tes4" --output-on-failure && git -C TES5Edit status --short</automated></verify>
  <acceptance_criteria>`git -C TES5Edit status --short` output is empty; `ctest --preset windows-msvc-debug-static -R ba2_dx10_writer --output-on-failure` exits 0; `ctest --preset windows-msvc-debug-static -R public_include_boundary --output-on-failure` exits 0.</acceptance_criteria>
  <done>Full Phase 9 writer behavior is complete, regression-tested, and TES5Edit remains untouched.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| final stored chunks → dedupe map | Compressed chunk bytes and metadata decide physical payload sharing. |
| writer output path → filesystem publish | Caller-controlled destination may already exist or collide with temp names. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-06-01 | Tampering | DX10 dedupe key | mitigate | Include stored bytes, raw size, packed size, and compression route; preserve distinct records/names. |
| T-09-06-02 | Tampering | overwrite finalization | mitigate | Reject non-regular targets and use backup/rollback before replacement. |
| T-09-06-03 | Tampering/DoS | temp path cleanup | mitigate | Reserve unique owned temporary directory; cleanup only owned paths so caller siblings are not deleted. |
| T-09-06-04 | Repudiation | phase verification | mitigate | Final command includes writer/read/public-boundary regression tests and TES5Edit cleanliness output. |
</threat_model>

<verification>
Run `cmake --build --preset windows-msvc-debug-static && ctest --preset windows-msvc-debug-static -R "ba2_dx10_writer|ba2_dx10|ba2_gnrl_writer|public_include_boundary|tes4" --output-on-failure && git -C TES5Edit status --short`.
</verification>

<success_criteria>
- RED/GREEN TDD commits exist for `09-06`.
- Dedupe disabled/enabled offset behavior is proven through reopened archives.
- Safe publish behavior matches Phase 8 expectations.
- Public dependency boundary and TES5Edit cleanliness gates pass.
</success_criteria>

<output>
After completion, create `.planning/phases/09-ba2-dx10-write-new-support/09-06-SUMMARY.md`.
</output>
