---
phase: 07-tes4-family-bsa-write-new-support
plan: 03
type: tdd
wave: 3
depends_on: [02]
files_modified:
  - src/formats/bsa/tes4_bsa_writer.hpp
  - src/formats/bsa/tes4_bsa_writer.cpp
  - tests/unit/tes4_bsa_writer_tests.cpp
autonomous: true
requirements: [WBSA-01, WBSA-02, WBSA-03, WBSA-05, WBSA-06, WBSA-10]
requirements_addressed: [WBSA-01, WBSA-02, WBSA-03, WBSA-05, WBSA-06, WBSA-10]
must_haves:
  truths:
    - "Writer output for v103, v104, and v105 reopens with archive_reader."
    - "Multi-folder indexes are hash sorted and list all entries exactly once by canonical path."
    - "Archive and file flags are derived from profile, content extensions, names, and raw archive policy."
    - "D-06: Serialized folder/file name tables preserve caller-provided path spelling."
    - "D-08: Serialized archives contain file entries only."
    - "D-16: Archive flags are derived by writer logic through safe named options."
    - "D-17: File category flags are the union of known category bits derived from entry extensions across the archive."
    - "D-22: Writer output is reopened with archive_reader to verify public metadata, lookup behavior, extraction bytes, and stable errors."
    - "D-23: Round-trip tests use generated legal writer-output data and keep TES5Edit read-only."
  artifacts:
    - path: "src/formats/bsa/tes4_bsa_writer.cpp"
      provides: "TES4-family BSA header, folder table, file table, file-name table, and raw payload serialization"
    - path: "tests/unit/tes4_bsa_writer_tests.cpp"
      provides: "Round-trip tests for raw v103/v104/v105 archives, multi-folder sorting, flags, disk+memory+zero-byte entries"
  key_links:
    - from: "src/formats/bsa/tes4_bsa_writer.cpp"
      to: "src/detail/bethesda_hash.cpp"
      via: "detail::hash_tes4 for folder/file record hashes and sort keys"
      pattern: "hash_tes4"
    - from: "tests/unit/tes4_bsa_writer_tests.cpp"
      to: "src/archive.cpp"
      via: "archive_reader::open writer output"
      pattern: "archive_reader::open"
---

<objective>
Turn validated writer entries into real TES4-family BSA bytes for all target profiles using raw payloads first.

Purpose: Delivers the core write-new table serialization needed for WBSA-01/WBSA-02/WBSA-03/WBSA-05/WBSA-06/WBSA-10 and implements D-06, D-08, D-16, D-17, D-22, D-23 with reader-backed proof.

Decision coverage: D-06: serialized folder/file name tables preserve caller spelling while canonical keys drive lookup validation. D-08: serialization covers file entries only, with no empty-directory surface. D-16: archive flags are derived by writer logic rather than caller-supplied raw bits. D-17: file category flags are derived as the union of traced extension-to-flag mappings across entries. D-22: writer acceptance reopens produced archives with `archive_reader` and verifies metadata, lookup, extraction, and stable errors. D-23: writer-output tests use repository-owned generated synthetic data and keep `TES5Edit/` read-only.
Output: Reopenable raw v103/v104/v105 BSA archives from disk and memory entries.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-tes4-family-bsa-write-new-support/07-SPEC.md
@.planning/phases/07-tes4-family-bsa-write-new-support/07-CONTEXT.md
@.planning/phases/07-tes4-family-bsa-write-new-support/07-RESEARCH.md
@.planning/phases/07-tes4-family-bsa-write-new-support/07-PATTERNS.md
@src/formats/bsa/tes4_bsa_parser.cpp
@src/formats/bsa/tes4_bsa_reader.cpp
@src/detail/bethesda_hash.hpp
@src/detail/binary_io.hpp
@tests/fixtures/generated/generate_tes4_bsa_fixtures.cpp
@tests/unit/tes4_bsa_reader_tests.cpp
@tests/unit/tes4_bsa_writer_tests.cpp

<interfaces>
Use these constants in private writer code, matching parser/generator expectations from `07-PATTERNS.md`:
```cpp
constexpr std::uint32_t oblivion_version = 0x67U;   // v103
constexpr std::uint32_t fallout3_version = 0x68U;   // v104
constexpr std::uint32_t skyrim_se_version = 0x69U;  // v105
constexpr std::uint32_t header_size = 36U;
constexpr std::uint32_t include_directory_names = 0x0001U;
constexpr std::uint32_t include_file_names = 0x0002U;
constexpr std::uint32_t archive_compress_by_default = 0x0004U;
constexpr std::uint32_t archive_embed_names = 0x0100U;
constexpr std::uint32_t file_size_compression_toggle = 0x40000000U;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 RED: Add raw profile round-trip, sorting, and flag tests</name>
  <read_first>
    - `.planning/phases/07-tes4-family-bsa-write-new-support/07-SPEC.md` requirements 2, 3, 4, and 8
    - `.planning/phases/07-tes4-family-bsa-write-new-support/07-CONTEXT.md` decisions D-06, D-08, D-16, D-17, D-22, D-23
    - `.planning/phases/07-tes4-family-bsa-write-new-support/07-PATTERNS.md` sections for table/offset serialization and writer tests
    - `tests/unit/tes4_bsa_writer_tests.cpp`
  </read_first>
  <files>tests/unit/tes4_bsa_writer_tests.cpp</files>
  <action>Add RED round-trip tests that create temporary synthetic source files and memory byte vectors, write one all-raw archive for each target `tes4_bsa_target::oblivion`, `tes4_bsa_target::fallout3`, and `tes4_bsa_target::skyrim_se`, reopen with `archive_reader::open`, and assert: metadata `type == archive_type::bsa`, `variant == archive_variant::tes4`, versions `103`, `104`, `105`, expected file count, default compression for `archive_compression_policy::all_raw`, and successful `contains`, `find`, `entries`, `extract_bytes`, and sink `extract` for mixed-case lookup variants. Include entries `Meshes/Upper/Model.NIF`, `textures/lower/diffuse.dds`, `Scripts/Quest/Stage.PEX`, `Interface/Menu.XML`, and `Empty/Zero.txt` to cover disk source, memory source, multi-folder, category flags, preserved original spelling, and zero-byte payloads. Add the copied-memory proof deferred from Plan 02: add `Meshes/Copy.nif` from a byte vector, mutate or clear that caller vector before `write_to()`, reopen the archive, and assert `extract_bytes("Meshes/Copy.nif")` returns the original pre-mutation bytes. Assert no TES3/BA2 writer API is used.</action>
  <verify>
    <automated>ctest --preset windows-msvc-debug-static -L tes4_bsa_writer --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `tests/unit/tes4_bsa_writer_tests.cpp` contains `tes4_bsa_target::oblivion`, `tes4_bsa_target::fallout3`, and `tes4_bsa_target::skyrim_se`.
    - `tests/unit/tes4_bsa_writer_tests.cpp` contains `Meshes/Upper/Model.NIF`, `textures/lower/diffuse.dds`, `Scripts/Quest/Stage.PEX`, and `Empty/Zero.txt`.
    - `tests/unit/tes4_bsa_writer_tests.cpp` contains `archive_reader::open`, `extract_bytes`, `contains`, and `find`.
    - `tests/unit/tes4_bsa_writer_tests.cpp` contains a `Meshes/Copy.nif` round-trip where the caller vector is mutated after `add_bytes` and extracted bytes equal the original pre-mutation payload.
    - The automated command fails before Task 2 because raw table serialization is incomplete.
  </acceptance_criteria>
  <done>RED tests define raw write-new behavior for all target profiles and table/flag acceptance.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 GREEN: Serialize hash-sorted raw TES4-family BSA archives</name>
  <read_first>
    - `src/formats/bsa/tes4_bsa_writer.hpp`
    - `src/formats/bsa/tes4_bsa_writer.cpp`
    - `src/detail/bethesda_hash.hpp`
    - `src/detail/binary_io.hpp`
    - `src/formats/bsa/tes4_bsa_parser.cpp`
    - `tests/fixtures/generated/generate_tes4_bsa_fixtures.cpp`
    - `tests/unit/tes4_bsa_writer_tests.cpp`
  </read_first>
  <files>src/formats/bsa/tes4_bsa_writer.hpp, src/formats/bsa/tes4_bsa_writer.cpp</files>
  <action>Implement raw BSA serialization in `write_tes4_bsa_archive`: map targets to versions 103/104/105, group entries by preserved folder spelling after converting `\` to `/`, compute folder hash with `detail::hash_tes4(folder, {})`, compute file hash with `detail::hash_tes4(file_name_without_extension, extension_with_dot)`, sort folders by folder hash and files by file hash, and derive archive flags as `0x0001 | 0x0002` with additional bits controlled by the same option fields that Plan 04/05 tests exercise (`0x0004` for compressed archive default and `0x0100` for compatible embedded names). Derive file category flags per D-17 using traced mapping: `.nif/.kf` mesh bit, `.dds` texture bit, `.wav` sound bit, `.xml` menu bit only for v103, `.txt/.html/.bat/.scc` misc bit except strip misc for v105, and script bit for `.pex/.psc`. Serialize header size 36, v103/v104 16-byte folder records, v105 24-byte folder records with the extra zero/reserved field, folder blocks, NUL-terminated file-name table, and payload bytes. Preserve the non-obvious TES5Edit folder offset rule with a short comment: folder record offsets include the later file-name-table length. Use checked `uint32_t`/`uint64_t` arithmetic and return `format_error` on overflow before writing. Write to a temporary sibling file and rename/replace according to `overwrite_existing` to avoid partial destination success.</action>
  <verify>
    <automated>ctest --preset windows-msvc-debug-static -L tes4_bsa_writer --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `src/formats/bsa/tes4_bsa_writer.cpp` contains `hash_tes4`, `folder offset`, `0x0001U`, `0x0002U`, and `0x69U`.
    - `src/formats/bsa/tes4_bsa_writer.cpp` contains checked conversion paths returning `error_code::format_error` for size overflow.
    - Reopened test archives report versions 103, 104, and 105 through public metadata.
    - Writer tests extract `Meshes/Upper/Model.NIF`, `textures/lower/diffuse.dds`, `Scripts/Quest/Stage.PEX`, and `Empty/Zero.txt` with bytes equal to source bytes.
    - The automated command passes for raw profile round-trip tests.
  </acceptance_criteria>
  <done>Raw TES4-family writer output is accepted by the existing reader for all target profiles and proves hash-sorted tables, flags, disk entries, memory entries, and zero-byte entries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| writer model→binary archive | Validated entry metadata is transformed into on-disk offsets, sizes, hashes, flags, and payload bytes. |
| generated archive→archive_reader | Writer output becomes parser input and must not exploit unchecked reader assumptions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03-01 | Tampering | folder/file index serialization | mitigate | Hash-sort folders/files with `detail::hash_tes4` and verify reopen/list/find/extract through public reader. |
| T-07-03-02 | Denial of Service | size and offset accounting | mitigate | Use checked arithmetic before serializing `uint32_t`/`uint64_t`; return `format_error` on overflow. |
| T-07-03-03 | Tampering | file category flags | mitigate | Derive flags from extension/root mapping in private code; callers cannot set raw bits per D-16/D-17. |
| T-07-03-04 | Tampering | partial output writes | mitigate | Write to a temporary sibling file and only publish final archive after all bytes are serialized successfully. |
</threat_model>

<verification>
Run `ctest --preset windows-msvc-debug-static -L tes4_bsa_writer --output-on-failure`, then `ctest --preset windows-msvc-debug-static -R "tes4_bsa_reader|public_include_boundary" --output-on-failure`, and verify `git -C TES5Edit status --short` is empty.
</verification>

<success_criteria>
All-raw writer output for v103, v104, and v105 reopens, lists, finds, contains, extracts, and byte-compares through public APIs, with correct table ordering and content-derived flags.
</success_criteria>

<output>
After completion, create `.planning/phases/07-tes4-family-bsa-write-new-support/07-03-SUMMARY.md`.
</output>
