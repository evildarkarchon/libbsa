---
phase: 07-tes4-family-bsa-write-new-support
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - include/libbsa/writer.hpp
  - include/libbsa/libbsa.hpp
  - CMakeLists.txt
  - tests/unit/public_include_boundary_tests.cpp
autonomous: true
requirements: [WBSA-01, WBSA-02, WBSA-03, WBSA-10]
requirements_addressed: [WBSA-01, WBSA-02, WBSA-03, WBSA-10]
must_haves:
  truths:
    - "Consumer can include a dependency-light public writer API for TES4-family write-new archives."
    - "Consumer can construct a writer for explicit v103, v104, and v105 target profiles."
    - "Consumer can add disk-file and copied memory-buffer entries with explicit archive-internal paths."
    - "D-01: Public write-new uses a writer object that holds options, accepts entries, and finalizes to a host-path archive."
    - "D-02: Memory-buffer entries are copied by add_bytes-style APIs into writer-owned storage."
    - "D-03: Phase 7 exposes host-path output only, with no memory-output or generic sink-output destination."
    - "D-04: Existing destination archive paths fail by default unless overwrite is explicit."
    - "D-05: Disk-file entries require explicit archive-internal paths."
    - "D-09: Per-entry compression selection is inherit/raw/compressed."
    - "D-10: Archive-wide compression uses named all-raw/all-compressed/default policy options."
    - "D-11: Zero-byte entries are represented for later raw-storage enforcement."
    - "D-12: Compression failures return structured errors instead of warning-and-continue fallback."
    - "D-13: Compression-level tuning stays internal and out of the public API."
    - "D-16: Public API exposes safe named compatibility options, not raw archive flag bits."
    - "D-20: Public writer headers remain dependency-light and expose no private codec, DirectXTex, Windows SDK, std::expected, or TES5Edit types."
    - "D-21: Fallible writer APIs preserve the C++20 result/error style."
  artifacts:
    - path: "include/libbsa/writer.hpp"
      provides: "Public TES4-family writer enums, options, and writer object"
      exports: ["tes4_bsa_target", "archive_compression_policy", "entry_compression_policy", "tes4_bsa_writer"]
    - path: "include/libbsa/libbsa.hpp"
      provides: "Umbrella include exposes writer API"
      contains: "#include <libbsa/writer.hpp>"
    - path: "CMakeLists.txt"
      provides: "Installed header file set includes writer.hpp"
      contains: "include/libbsa/writer.hpp"
  key_links:
    - from: "include/libbsa/libbsa.hpp"
      to: "include/libbsa/writer.hpp"
      via: "public include"
      pattern: "#include <libbsa/writer.hpp>"
    - from: "tests/unit/public_include_boundary_tests.cpp"
      to: "include/libbsa/writer.hpp"
      via: "compile-time public boundary smoke"
      pattern: "tes4_bsa_writer"
---

<objective>
Create the public Phase 7 writer contract first so downstream implementation plans build against stable C++20 API blueprints instead of inventing signatures.

Purpose: Satisfies D-01 through D-05, D-09 through D-13, D-16, D-20, D-21, and the public API portion of WBSA-01/WBSA-02/WBSA-03/WBSA-10 without exposing private codec, DirectXTex, Windows, `std::expected`, or TES5Edit types.

Decision coverage: D-01: public write-new is a writer object, not a single one-shot function. D-02: `add_bytes`-style memory-buffer entries are copied into writer-owned storage. D-03: Phase 7 exposes host-path output only through `write_to(std::string_view host_path)`. D-04: existing destination paths fail by default unless `overwrite_existing` is explicitly true. D-05: disk-file entries require explicit archive-internal paths. D-09: per-entry compression uses `inherit`, `raw`, or `compressed`. D-10: archive-wide compression policy is expressed through named options. D-11: zero-byte entries must be representable without public special-case knobs and are stored raw by implementation. D-12: compression failures surface through `result<void>` rather than warning-and-continue fallback. D-13: codec compression-level tuning stays internal. D-16: callers receive safe named options instead of raw archive flag bits. D-20: the public boundary remains dependency-light with no private codec, DirectXTex, Windows, `std::expected`, or TES5Edit types. D-21: fallible writer APIs use the established C++20 result/error style.
Output: Public writer header, umbrella include, CMake header registration, and RED/GREEN public boundary coverage.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-tes4-family-bsa-write-new-support/07-SPEC.md
@.planning/phases/07-tes4-family-bsa-write-new-support/07-CONTEXT.md
@.planning/phases/07-tes4-family-bsa-write-new-support/07-RESEARCH.md
@.planning/phases/07-tes4-family-bsa-write-new-support/07-PATTERNS.md
@include/libbsa/archive.hpp
@include/libbsa/libbsa.hpp
@include/libbsa/result.hpp
@tests/unit/public_include_boundary_tests.cpp
@CMakeLists.txt

<interfaces>
Create these exact public names in `include/libbsa/writer.hpp`:
```cpp
namespace libbsa {
enum class tes4_bsa_target { oblivion, fallout3, skyrim_se };
enum class archive_compression_policy { target_default, all_raw, all_compressed };
enum class entry_compression_policy { inherit, raw, compressed };

struct tes4_bsa_writer_options {
  archive_compression_policy compression_policy{archive_compression_policy::target_default};
  bool embed_file_names{false};
  bool deduplicate_payloads{false};
  bool overwrite_existing{false};
};

class tes4_bsa_writer {
 public:
  explicit tes4_bsa_writer(tes4_bsa_target target);
  explicit tes4_bsa_writer(tes4_bsa_target target, tes4_bsa_writer_options options);
  [[nodiscard]] tes4_bsa_target target() const noexcept;
  [[nodiscard]] const tes4_bsa_writer_options& options() const noexcept;
  result<void> add_file(std::string_view archive_path, std::string_view host_path,
                        entry_compression_policy compression = entry_compression_policy::inherit);
  result<void> add_bytes(std::string_view archive_path, std::span<const std::byte> bytes,
                         entry_compression_policy compression = entry_compression_policy::inherit);
  result<void> write_to(std::string_view host_path) const;
};
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 RED: Add failing public writer boundary expectations</name>
  <read_first>
    - `.planning/phases/07-tes4-family-bsa-write-new-support/07-CONTEXT.md` decisions D-01 through D-05, D-09 through D-13, D-16, D-20, D-21
    - `.planning/phases/07-tes4-family-bsa-write-new-support/07-SPEC.md` requirements 1, 2, and 8
    - `.planning/phases/07-tes4-family-bsa-write-new-support/07-PATTERNS.md` sections for `writer.hpp`, `libbsa.hpp`, `public_include_boundary_tests.cpp`, and `CMakeLists.txt`
    - `tests/unit/public_include_boundary_tests.cpp`
  </read_first>
  <files>tests/unit/public_include_boundary_tests.cpp</files>
  <action>Add RED public boundary assertions that include `<libbsa/libbsa.hpp>`, `static_assert(std::is_enum_v<libbsa::tes4_bsa_target>)`, `static_assert(std::is_enum_v<libbsa::archive_compression_policy>)`, `static_assert(std::is_enum_v<libbsa::entry_compression_policy>)`, `static_assert(std::is_constructible_v<libbsa::tes4_bsa_writer, libbsa::tes4_bsa_target>)`, `static_assert(std::is_constructible_v<libbsa::tes4_bsa_writer, libbsa::tes4_bsa_target, libbsa::tes4_bsa_writer_options>)`, and C++20 `requires` expressions proving `add_bytes("Meshes/Memory.nif", std::span<const std::byte>{})`, `add_file("Textures/Disk.dds", "source.dds")`, and `write_to("out.bsa")` return `libbsa::result<void>` without executing or linking method bodies. Extend the forbidden-token scan to include `libdeflate`, `lz4::`, `DirectXTex`, `DirectX::`, `DXGI`, `Windows.h`, `TES5Edit`, `std::expected`, `bethesda_hash`, `compression_router`, and `archive_path_key` for the new public header. This implements D-01/D-02/D-05/D-09/D-13/D-16/D-20/D-21 at the public contract level.</action>
  <verify>
    <automated>ctest --preset windows-msvc-debug-static -R public_include_boundary --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `tests/unit/public_include_boundary_tests.cpp` contains `tes4_bsa_writer`.
    - `tests/unit/public_include_boundary_tests.cpp` contains `tes4_bsa_target::oblivion`.
    - `tests/unit/public_include_boundary_tests.cpp` contains `entry_compression_policy::inherit` or a `requires` expression that references `add_bytes` with its default compression argument.
    - The verification command fails before Task 2 because `include/libbsa/writer.hpp` and symbols do not exist yet.
  </acceptance_criteria>
  <done>Public API expectations are encoded as failing compile/test coverage before adding the writer header.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 GREEN: Add dependency-light writer API contracts</name>
  <read_first>
    - `include/libbsa/archive.hpp`
    - `include/libbsa/libbsa.hpp`
    - `include/libbsa/result.hpp`
    - `CMakeLists.txt`
    - `tests/unit/public_include_boundary_tests.cpp`
  </read_first>
  <files>include/libbsa/writer.hpp, include/libbsa/libbsa.hpp, CMakeLists.txt</files>
  <action>Create `include/libbsa/writer.hpp` with the exact public names and signatures shown in the `<interfaces>` block. Add Doxygen `///` comments for every enum, options struct field, constructor, add method, accessor, and `write_to`. Keep method declarations in the public header and leave method definitions to the private implementation introduced by Plan 02; public boundary tests in this plan must use type traits and `requires` expressions rather than executing writer methods. Add `#include <cstddef>`, `<span>`, `<string>`, `<string_view>`, `<vector>`, and `<libbsa/result.hpp>` as needed; do not include codec, DirectX, Windows, filesystem, or TES5Edit headers. Add `#include <libbsa/writer.hpp>` to `include/libbsa/libbsa.hpp`. Add `include/libbsa/writer.hpp` to the `target_sources(libbsa PUBLIC FILE_SET HEADERS ...)` list in `CMakeLists.txt`.</action>
  <verify>
    <automated>ctest --preset windows-msvc-debug-static -R public_include_boundary --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `include/libbsa/writer.hpp` exists and contains `enum class tes4_bsa_target`, `enum class archive_compression_policy`, `enum class entry_compression_policy`, `struct tes4_bsa_writer_options`, and `class tes4_bsa_writer`.
    - `include/libbsa/libbsa.hpp` contains `#include <libbsa/writer.hpp>`.
    - `CMakeLists.txt` contains `include/libbsa/writer.hpp` in the public header file set.
    - `grep -v '^#' include/libbsa/writer.hpp | grep -E "libdeflate|lz4::|DirectXTex|DirectX::|DXGI|Windows.h|TES5Edit|std::expected|bethesda_hash|compression_router|archive_path_key"` returns no matches.
    - The automated verification command passes.
  </acceptance_criteria>
  <done>Public writer contracts compile through the umbrella header and preserve dependency-light C++20 boundaries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer→public writer API | Untrusted archive-internal path strings, host path strings, and memory spans enter libbsa-owned writer state. |
| public headers→consumer builds | Public API must not leak private dependencies or implementation-only types. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-01 | Tampering | `tes4_bsa_writer::add_file/add_bytes` contracts | mitigate | Require explicit archive paths and dependency-light policies in public signatures per D-05/D-09; implementation validation lands in Plan 02. |
| T-07-01-02 | Information Disclosure | `include/libbsa/writer.hpp` | mitigate | Public boundary forbidden-token test blocks codec, DirectX, Windows, TES5Edit, and private helper leakage per D-20. |
| T-07-01-03 | Repudiation | public result API | mitigate | Every fallible method returns `result<void>` with stable `error_code` style per D-21; no exceptions for expected I/O/format failures. |
</threat_model>

<verification>
Run `ctest --preset windows-msvc-debug-static -R public_include_boundary --output-on-failure` and confirm `git -C TES5Edit status --short` emits no output.
</verification>

<success_criteria>
The writer public contract exists, is exported by the umbrella header and CMake install file set, compiles in public boundary tests, and exposes no forbidden private dependency tokens.
</success_criteria>

<output>
After completion, create `.planning/phases/07-tes4-family-bsa-write-new-support/07-01-SUMMARY.md`.
</output>
