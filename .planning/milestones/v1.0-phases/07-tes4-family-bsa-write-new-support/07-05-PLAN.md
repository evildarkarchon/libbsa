---
phase: 07-tes4-family-bsa-write-new-support
plan: 05
type: tdd
wave: 5
depends_on: [04]
files_modified:
  - src/formats/bsa/tes4_bsa_writer.cpp
  - tests/unit/tes4_bsa_writer_tests.cpp
autonomous: true
requirements: [WBSA-02, WBSA-03, WBSA-08, WBSA-10]
requirements_addressed: [WBSA-02, WBSA-03, WBSA-08, WBSA-10]
must_haves:
  truths:
    - "Embedded names are absent by default."
    - "Embedded names are emitted only when the global option is enabled and target-compatible."
    - "Embedded-name prefixes never leak into extracted consumer-visible bytes."
    - "D-06: Embedded-name payload prefixes use preserved file-name spelling from serialized name tables."
    - "D-16: Embedded-name behavior is exposed as a safe named option rather than raw flag bits."
    - "D-18: Embedded names are off by default, controlled by one global option, and emitted only where target-compatible."
    - "D-22: Embedded-name acceptance is proven by reopened archive_reader metadata and extraction bytes."
  artifacts:
    - path: "src/formats/bsa/tes4_bsa_writer.cpp"
      provides: "Global embedded-name option handling and target-compatible prefix encoding"
    - path: "tests/unit/tes4_bsa_writer_tests.cpp"
      provides: "Round-trip embedded-name enabled/disabled tests"
  key_links:
    - from: "src/formats/bsa/tes4_bsa_writer.cpp"
      to: "src/formats/bsa/tes4_bsa_reader.cpp"
      via: "length-prefixed embedded name prefix stripped during extraction"
      pattern: "embed_file_names"
---

<objective>
Add explicit opt-in embedded-name writing without changing extracted file bytes.

Purpose: Completes WBSA-08 and implements D-18 while preserving D-06, D-16, D-22, and target compatibility guidance from research.

Decision coverage: D-06: embedded-name prefixes use the same preserved file-name spelling serialized in the name tables. D-16: embedded-name behavior is exposed as one safe named option, not raw archive flag control. D-18: embedded names are off by default, controlled globally, emitted only where target-compatible, and not exposed as per-entry opt-in. D-22: acceptance uses reopened `archive_reader` metadata and extraction proof.
Output: Global embedded-name option for v104/v105 writer output with reader-backed metadata and extraction proof.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-tes4-family-bsa-write-new-support/07-SPEC.md
@.planning/phases/07-tes4-family-bsa-write-new-support/07-CONTEXT.md
@.planning/phases/07-tes4-family-bsa-write-new-support/07-RESEARCH.md
@.planning/phases/07-tes4-family-bsa-write-new-support/07-PATTERNS.md
@src/formats/bsa/tes4_bsa_parser.cpp
@src/formats/bsa/tes4_bsa_reader.cpp
@src/formats/bsa/tes4_bsa_writer.cpp
@tests/unit/tes4_bsa_writer_tests.cpp
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 RED: Add embedded-name enabled/disabled tests</name>
  <read_first>
    - `.planning/phases/07-tes4-family-bsa-write-new-support/07-CONTEXT.md` decision D-18
    - `.planning/phases/07-tes4-family-bsa-write-new-support/07-RESEARCH.md` Pitfall 3
    - `tests/unit/tes4_bsa_writer_tests.cpp`
  </read_first>
  <files>tests/unit/tes4_bsa_writer_tests.cpp</files>
  <action>Add RED tests that write one v104 archive and one v105 archive with `tes4_bsa_writer_options{.embed_file_names = false}` and assert reopened entries have `has_embedded_name == false` and `embedded_name_prefix_size == 0`. Add matching v104/v105 tests with `embed_file_names = true` and entries `Meshes/Embedded/Model.nif` and `Scripts/Embedded/Quest.pex`; assert reopened metadata has `has_embedded_name == true`, `embedded_name_prefix_size == file_name.length() + 1`, and `extract_bytes()` returns source bytes without the embedded name prefix. Add a v103 `embed_file_names = true` test that documents target compatibility by asserting reopened metadata remains `has_embedded_name == false` and extracted bytes equal source bytes.</action>
  <verify>
    <automated>ctest --preset windows-msvc-debug-static -L tes4_bsa_writer --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `tests/unit/tes4_bsa_writer_tests.cpp` contains `embed_file_names = true`, `has_embedded_name`, and `embedded_name_prefix_size`.
    - `tests/unit/tes4_bsa_writer_tests.cpp` contains v104 and v105 enabled cases and a v103 compatibility case.
    - The automated command fails before Task 2 because embedded-name prefix emission is incomplete.
  </acceptance_criteria>
  <done>RED tests pin absent-by-default and opt-in embedded-name behavior for compatible targets.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 GREEN: Emit target-compatible embedded-name prefixes</name>
  <read_first>
    - `src/formats/bsa/tes4_bsa_writer.cpp`
    - `src/formats/bsa/tes4_bsa_parser.cpp`
    - `src/formats/bsa/tes4_bsa_reader.cpp`
    - `tests/unit/tes4_bsa_writer_tests.cpp`
  </read_first>
  <files>src/formats/bsa/tes4_bsa_writer.cpp</files>
  <action>When `options.embed_file_names == true` and target version is v104 or v105, set archive flag `0x0100U` and prefix each stored payload with one unsigned byte containing the preserved file-name length followed by the preserved file-name bytes. Use the file name portion only, not the folder path, and use the same spelling serialized in the file-name table after separator normalization per D-06. Do not expose per-entry embedded-name options. When target is v103, do not emit prefixes and do not set `0x0100U`, because the existing parser treats embedded names as active only when `version != 0x67`. Ensure the embedded prefix is included before compression size-prefix/payload layout exactly as expected by the reader: embedded-name prefix is part of stored payload and is stripped before decompression/consumer bytes by existing extraction semantics.</action>
  <verify>
    <automated>ctest --preset windows-msvc-debug-static -L tes4_bsa_writer --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `src/formats/bsa/tes4_bsa_writer.cpp` contains `embed_file_names`, `0x0100U`, and a target-version check excluding `0x67U` or v103.
    - Reopened v104/v105 embedded-name-enabled entries report `has_embedded_name == true` and nonzero prefix size.
    - Reopened v103 embedded-name-enabled output reports no embedded name and extracts source bytes.
    - The automated command passes.
  </acceptance_criteria>
  <done>Embedded names are controlled by one global option, target-compatible, metadata-visible, and invisible to extracted consumer bytes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| writer option→stored payload bytes | A compatibility-sensitive option changes on-disk payload prefixes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05-01 | Tampering | embedded-name prefix emission | mitigate | Default off; emit only for v104/v105 global option, never per-entry raw bits. |
| T-07-05-02 | Information Disclosure | embedded file names | accept | Embedded names intentionally serialize archive file names only when caller opts in globally. |
| T-07-05-03 | Tampering | extraction payload visibility | mitigate | Reader-backed tests assert prefixes do not leak into extracted bytes. |
</threat_model>

<verification>
Run `ctest --preset windows-msvc-debug-static -L tes4_bsa_writer --output-on-failure`, then `ctest --preset windows-msvc-debug-static -R tes4_bsa_reader --output-on-failure`, and verify `git -C TES5Edit status --short` is empty.
</verification>

<success_criteria>
Embedded names are absent by default, opt-in for v104/v105, target-safe for v103, visible in reopened metadata, and stripped from extracted bytes.
</success_criteria>

<output>
After completion, create `.planning/phases/07-tes4-family-bsa-write-new-support/07-05-SUMMARY.md`.
</output>
