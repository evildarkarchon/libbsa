---
phase: 07-tes4-family-bsa-write-new-support
plan: 02
type: tdd
wave: 2
depends_on: [01]
files_modified:
  - include/libbsa/writer.hpp
  - src/formats/bsa/tes4_bsa_writer.hpp
  - src/formats/bsa/tes4_bsa_writer.cpp
  - CMakeLists.txt
  - tests/unit/tes4_bsa_writer_tests.cpp
  - tests/CMakeLists.txt
autonomous: true
requirements: [WBSA-01, WBSA-02, WBSA-03, WBSA-10]
requirements_addressed: [WBSA-01, WBSA-02, WBSA-03, WBSA-10]
must_haves:
  truths:
    - "Writer stores copied memory-buffer entries and disk-source descriptors without relying on caller buffer lifetime."
    - "Writer rejects invalid or duplicate canonical archive paths with structured errors before serialization."
    - "Writer refuses to overwrite existing destination files unless overwrite_existing is explicitly true."
    - "D-01: Writer state backs the public write-new object."
    - "D-02: Memory-buffer entries are copied into writer-owned storage when added."
    - "D-03: The implemented destination remains host-path output only."
    - "D-04: Existing destination archive paths fail by default unless overwrite_existing is explicit."
    - "D-05: Disk-file entries require explicit archive-internal paths."
    - "D-06: Original archive path spelling is preserved while canonical normalization drives validation and duplicate detection."
    - "D-07: Duplicate canonical archive paths fail with a structured error at write time."
    - "D-08: Phase 7 writer state represents file entries only, not empty directories."
    - "D-20: Public boundary remains dependency-light while private implementation is added."
    - "D-21: Writer failures use structured result/error returns."
    - "D-23: Tests use generated legal synthetic data and do not mutate TES5Edit."
  artifacts:
    - path: "src/formats/bsa/tes4_bsa_writer.hpp"
      provides: "Private writer entry/options model and write entry point"
      exports: ["write_tes4_bsa_archive"]
    - path: "src/formats/bsa/tes4_bsa_writer.cpp"
      provides: "Entry validation, memory copy semantics, source loading, overwrite guard"
    - path: "tests/unit/tes4_bsa_writer_tests.cpp"
      provides: "TDD tests for add semantics, duplicate paths, invalid paths, overwrite default, missing source"
  key_links:
    - from: "include/libbsa/writer.hpp"
      to: "src/formats/bsa/tes4_bsa_writer.cpp"
      via: "public methods delegate to private writer implementation"
      pattern: "write_tes4_bsa_archive"
    - from: "src/formats/bsa/tes4_bsa_writer.cpp"
      to: "src/detail/archive_path.cpp"
      via: "detail::normalize_archive_path"
      pattern: "normalize_archive_path"
---

<objective>
Implement the writer state and validation foundation that every later serialization feature depends on.

Purpose: Satisfies D-01 through D-08, D-20, D-21, D-23 and prevents unsafe path, overwrite, and lifetime behavior before table serialization exists.

Decision coverage: D-01: writer state backs the public writer object. D-02: memory-buffer entries are copied into writer-owned storage. D-03: only host-path output is implemented. D-04: existing destination archives fail unless overwrite is explicit. D-05: disk entries require explicit archive-internal paths. D-06: original archive path spelling is preserved for serialization while canonical paths drive validation and lookup keys. D-07: duplicate canonical archive paths fail with structured errors at write time. D-08: Phase 7 writes file entries only and does not introduce empty directory representation. D-20: implementation preserves the dependency-light public boundary. D-21: validation, I/O, and format failures use structured result/error returns. D-23: tests use generated legal synthetic data and never mutate `TES5Edit/`.
Output: Private writer model, public method definitions, CMake/test wiring, and behavior tests for validation and entry ownership.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-tes4-family-bsa-write-new-support/07-SPEC.md
@.planning/phases/07-tes4-family-bsa-write-new-support/07-CONTEXT.md
@.planning/phases/07-tes4-family-bsa-write-new-support/07-RESEARCH.md
@.planning/phases/07-tes4-family-bsa-write-new-support/07-PATTERNS.md
@include/libbsa/writer.hpp
@src/detail/archive_path.hpp
@src/detail/binary_io.hpp
@src/archive.cpp
@tests/unit/tes4_bsa_reader_tests.cpp
@tests/CMakeLists.txt
@CMakeLists.txt

<interfaces>
Private implementation contract to create in `src/formats/bsa/tes4_bsa_writer.hpp`:
```cpp
namespace libbsa::formats::bsa {
struct tes4_writer_entry {
  std::string archive_path_original;
  std::string archive_path_canonical;
  std::string host_path;
  std::vector<std::byte> memory_bytes;
  bool from_memory{false};
  entry_compression_policy compression{entry_compression_policy::inherit};
};

result<void> write_tes4_bsa_archive(tes4_bsa_target target,
                                    const tes4_bsa_writer_options& options,
                                    std::span<const tes4_writer_entry> entries,
                                    std::string_view output_host_path);
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 RED: Add writer validation and ownership tests</name>
  <read_first>
    - `.planning/phases/07-tes4-family-bsa-write-new-support/07-CONTEXT.md` decisions D-01 through D-08, D-20, D-21, D-23
    - `.planning/phases/07-tes4-family-bsa-write-new-support/07-PATTERNS.md` sections for writer tests, path validation, public API error style, and CMake test registration
    - `include/libbsa/writer.hpp`
    - `tests/CMakeLists.txt`
  </read_first>
  <files>tests/unit/tes4_bsa_writer_tests.cpp, tests/CMakeLists.txt</files>
  <action>Create `tests/unit/tes4_bsa_writer_tests.cpp` with Catch2 tag `[tes4_bsa_writer]`. Add tests that: (1) `add_bytes("Meshes/Copy.nif", bytes)` accepts a memory span, then mutating/clearing the caller vector after add does not invalidate writer-owned state or change validation outcome; prove this in Plan 02 without archive extraction by combining the copied memory entry with a duplicate canonical path check or missing disk-source check that still reports the expected structured error after caller mutation; Plan 03 owns the first reader-reopen extraction assertion for the copied bytes; (2) duplicate canonical paths `Meshes/Foo.nif` and `meshes/foo.nif` make `write_to()` return `error_code::format_error`; (3) invalid archive paths `"/rooted/file.txt"`, `"C:/drive/file.txt"`, `"folder/../file.txt"`, and empty string return `error_code::invalid_argument` from add or write; (4) `write_to(existing_path)` returns `error_code::io_error` when `overwrite_existing == false`; (5) missing disk source returns `error_code::io_error`; and (6) disk entries require explicit archive paths by exercising `add_file("Textures/Disk.dds", host_path)`, not any host-directory inference. Add `unit/tes4_bsa_writer_tests.cpp` to `tests/CMakeLists.txt`.</action>
  <verify>
    <automated>ctest --preset windows-msvc-debug-static -L tes4_bsa_writer --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `tests/unit/tes4_bsa_writer_tests.cpp` exists and contains `[tes4_bsa_writer]`.
    - `tests/unit/tes4_bsa_writer_tests.cpp` contains `Meshes/Copy.nif`, `meshes/foo.nif`, `/rooted/file.txt`, `overwrite_existing`, and `error_code::format_error`.
    - `tests/CMakeLists.txt` contains `unit/tes4_bsa_writer_tests.cpp`.
    - The automated command fails before Task 2 because writer validation/storage implementation is incomplete.
    - No Plan 02 assertion requires `archive_reader::open`, `extract_bytes`, or reader-reopenable BSA bytes; copied-memory extraction proof is assigned to Plan 03.
  </acceptance_criteria>
  <done>RED tests pin Phase 7 writer validation, copied-memory, explicit-path, overwrite, and stable-error behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 GREEN: Implement writer state, validation, and overwrite guard</name>
  <read_first>
    - `include/libbsa/writer.hpp`
    - `src/detail/archive_path.hpp`
    - `src/detail/binary_io.hpp`
    - `src/archive.cpp`
    - `tests/unit/tes4_bsa_writer_tests.cpp`
    - `CMakeLists.txt`
  </read_first>
  <files>include/libbsa/writer.hpp, src/formats/bsa/tes4_bsa_writer.hpp, src/formats/bsa/tes4_bsa_writer.cpp, CMakeLists.txt</files>
  <action>Back `tes4_bsa_writer` with `std::shared_ptr<state>` or equivalent writer-owned state containing `tes4_bsa_target`, `tes4_bsa_writer_options`, and `std::vector<formats::bsa::tes4_writer_entry>`. Implement `add_bytes` to validate archive path with `detail::normalize_archive_path`, store caller spelling with `\` converted to `/` for serialized names per D-06, store canonical key, copy `std::span<const std::byte>` into writer-owned `std::vector<std::byte>` per D-02, and record `from_memory=true`. Implement `add_file` with the same archive-path validation and store the explicit host path string per D-05. Allow duplicate canonical paths to be detected in `write_to()` rather than requiring incremental rejection, but return `error_code::format_error` if duplicates exist per D-07. Implement output host path validation: empty output returns `invalid_argument`; existing output returns `io_error` unless `options.overwrite_existing` is true per D-04; missing disk source returns `io_error`; no entries returns `invalid_argument`. Register `src/formats/bsa/tes4_bsa_writer.cpp` in `CMakeLists.txt`. Route validated serialization requests to `write_tes4_bsa_archive`; Plan 03 owns the first reader-reopenable BSA byte stream and this plan must not mark round-trip acceptance complete.</action>
  <verify>
    <automated>ctest --preset windows-msvc-debug-static -L tes4_bsa_writer --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `src/formats/bsa/tes4_bsa_writer.hpp` exists and contains `struct tes4_writer_entry` and `write_tes4_bsa_archive`.
    - `src/formats/bsa/tes4_bsa_writer.cpp` contains `normalize_archive_path` and `overwrite_existing`.
    - `include/libbsa/writer.hpp` contains a private writer state pointer or equivalent writer-owned entry storage; no public codec/TES5Edit types are introduced.
    - `CMakeLists.txt` contains `src/formats/bsa/tes4_bsa_writer.cpp`.
    - `ctest --preset windows-msvc-debug-static -L tes4_bsa_writer --output-on-failure` passes validation/error tests that do not require reader-reopenable BSA bytes; copied-memory and reader-reopen round-trip assertions remain assigned to Plan 03.
  </acceptance_criteria>
  <done>Writer object owns entries, validates archive paths, rejects duplicates at write time, protects existing destinations by default, and reports stable structured errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller→writer entry model | Archive paths, host paths, and bytes cross into writer-owned state. |
| writer→host filesystem | Destination archive path may overwrite local files if not guarded. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | Tampering | archive path handling | mitigate | Use `detail::normalize_archive_path`, reject invalid paths, and reject duplicate canonical keys with `format_error`. |
| T-07-02-02 | Tampering | output host path | mitigate | Fail existing destination by default and require `overwrite_existing=true` for overwrite per D-04. |
| T-07-02-03 | Tampering | memory-buffer entries | mitigate | Copy spans into writer-owned storage at add time per D-02 so caller mutation cannot alter output. |
| T-07-02-04 | Denial of Service | entry validation | accept | Phase 7 is single-threaded and bounded by existing test payload sizes; full performance limits are Phase 12. |
</threat_model>

<verification>
Run `ctest --preset windows-msvc-debug-static -L tes4_bsa_writer --output-on-failure`, then `ctest --preset windows-msvc-debug-static -R public_include_boundary --output-on-failure`, and verify `git -C TES5Edit status --short` is empty.
</verification>

<success_criteria>
Writer state and validation behavior are implemented behind the public API, all validation tests pass or leave only full archive round-trip RED cases for the next plan, and no TES5Edit files are touched.
</success_criteria>

<output>
After completion, create `.planning/phases/07-tes4-family-bsa-write-new-support/07-02-SUMMARY.md`.
</output>
