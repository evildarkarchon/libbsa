---
phase: 07-tes4-family-bsa-write-new-support
plan: 04
type: tdd
wave: 4
depends_on: [03]
files_modified:
  - src/formats/bsa/tes4_bsa_writer.hpp
  - src/formats/bsa/tes4_bsa_writer.cpp
  - tests/unit/tes4_bsa_writer_tests.cpp
autonomous: true
requirements: [WBSA-01, WBSA-02, WBSA-03, WBSA-07, WBSA-10]
requirements_addressed: [WBSA-01, WBSA-02, WBSA-03, WBSA-07, WBSA-10]
must_haves:
  truths:
    - "Writer honors archive default compression and per-entry raw/compressed overrides."
    - "v103/v104 compressed entries use deflate and v105 compressed entries use LZ4 frame."
    - "Zero-byte entries remain raw even when effective policy requests compression."
    - "D-09: Public per-entry compression selection maps compressed entries to deflate for v103/v104 and LZ4 frame for v105."
    - "D-10: Archive-wide compression defaults follow target profile unless all-raw or all-compressed named options are selected."
    - "D-11: Zero-byte entries are stored raw even when effective policy requests compression."
    - "D-12: Compression failure or invalid compression request fails the write with a structured error and no raw fallback."
    - "D-13: Codec compression-level tuning stays internal."
    - "D-14: Archive compression flag matches archive-wide default and per-file toggle bits mark deviations only."
    - "D-15: Non-empty files honor effective compression policy without hidden size-threshold raw storage."
  artifacts:
    - path: "src/formats/bsa/tes4_bsa_writer.cpp"
      provides: "Compression policy resolution, size-prefix encoding, compression toggle bits"
    - path: "tests/unit/tes4_bsa_writer_tests.cpp"
      provides: "Round-trip tests for compression defaults, overrides, zero-byte raw, and metadata compression modes"
  key_links:
    - from: "src/formats/bsa/tes4_bsa_writer.cpp"
      to: "src/detail/compression_router.cpp"
      via: "detail::compress_payload"
      pattern: "compress_payload"
    - from: "src/formats/bsa/tes4_bsa_writer.cpp"
      to: "src/formats/bsa/tes4_bsa_parser.cpp"
      via: "FILE_SIZE_COMPRESS XOR toggle semantics"
      pattern: "0x40000000"
---

<objective>
Add target-routed TES4-family compression and override behavior on top of the raw writer.

Purpose: Completes WBSA-07 while preserving D-09 through D-15 and proving reader-visible compression metadata and extracted bytes for every target profile.

Decision coverage: D-09: per-entry compression resolves from `inherit`, `raw`, or `compressed` and maps compressed entries by target profile. D-10: archive-wide compression defaults follow the selected target profile unless named options request all-raw or all-compressed policy. D-11: zero-byte entries are stored raw even when effective policy requests compression. D-12: compression failures and invalid compression requests return structured errors without raw fallback. D-13: compression-level tuning remains internal and absent from the public API. D-14: archive compression flag follows archive-wide default and per-file toggle bits mark only deviations. D-15: non-empty files honor the effective compression policy without hidden tiny-file raw thresholds.
Output: Deflate and LZ4-frame writer payload encoding with archive-default/toggle semantics.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-tes4-family-bsa-write-new-support/07-SPEC.md
@.planning/phases/07-tes4-family-bsa-write-new-support/07-CONTEXT.md
@.planning/phases/07-tes4-family-bsa-write-new-support/07-RESEARCH.md
@.planning/phases/07-tes4-family-bsa-write-new-support/07-PATTERNS.md
@src/detail/compression_router.hpp
@src/detail/deflate_codec.hpp
@src/detail/lz4_frame_codec.hpp
@src/formats/bsa/tes4_bsa_parser.cpp
@src/formats/bsa/tes4_bsa_reader.cpp
@tests/unit/tes4_bsa_writer_tests.cpp
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 RED: Add compression default, override, and zero-byte tests</name>
  <read_first>
    - `.planning/phases/07-tes4-family-bsa-write-new-support/07-CONTEXT.md` decisions D-09 through D-15
    - `.planning/phases/07-tes4-family-bsa-write-new-support/07-RESEARCH.md` Pattern 2 and pitfalls 2, 5
    - `tests/unit/tes4_bsa_writer_tests.cpp`
  </read_first>
  <files>tests/unit/tes4_bsa_writer_tests.cpp</files>
  <action>Add RED tests under `[tes4_bsa_writer]` that write: (1) v103 and v104 archives with `archive_compression_policy::all_compressed`, one inherited compressed entry, one per-entry `entry_compression_policy::raw` override, and one zero-byte inherited entry; assert reopened entry metadata reports deflate for the inherited non-empty entry, none for the raw override, none for zero-byte, and extracted bytes equal sources; (2) a v105 archive with the same layout and assert LZ4-frame for the inherited non-empty entry; (3) an all-raw archive with one per-entry `compressed` override and assert the per-file toggle produces compressed metadata while archive default remains raw; (4) no public compression level knobs or codec types are referenced. Compare extracted bytes only, not compressed stored bytes.</action>
  <verify>
    <automated>ctest --preset windows-msvc-debug-static -L tes4_bsa_writer --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `tests/unit/tes4_bsa_writer_tests.cpp` contains `archive_compression_policy::all_compressed`, `entry_compression_policy::raw`, and `entry_compression_policy::compressed`.
    - `tests/unit/tes4_bsa_writer_tests.cpp` asserts `entry_compression::deflate` and `entry_compression::lz4_frame`.
    - `tests/unit/tes4_bsa_writer_tests.cpp` contains a zero-byte compression-policy case.
    - The automated command fails before Task 2 because compression encoding/toggles are incomplete.
  </acceptance_criteria>
  <done>RED tests lock compression routing, override, toggle, and zero-byte semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 GREEN: Encode compressed payloads and XOR toggle bits</name>
  <read_first>
    - `src/formats/bsa/tes4_bsa_writer.cpp`
    - `src/detail/compression_router.hpp`
    - `src/formats/bsa/tes4_bsa_parser.cpp`
    - `tests/unit/tes4_bsa_writer_tests.cpp`
  </read_first>
  <files>src/formats/bsa/tes4_bsa_writer.hpp, src/formats/bsa/tes4_bsa_writer.cpp</files>
  <action>Implement compression policy resolution exactly: `target_default` means raw for v103 and compressed for v104/v105; `all_raw` means archive default raw; `all_compressed` means archive default compressed. Resolve entry `inherit` to archive default, `raw` to raw, `compressed` to compressed. For non-empty compressed entries, map v103/v104 to `detail::compression_method::deflate` and v105 to `detail::compression_method::lz4_frame`, write a 4-byte little-endian uncompressed raw-size prefix followed by `detail::compress_payload` output, and return the router error if compression fails per D-12. For zero-byte entries, force raw per D-11. Set archive flag `0x0004` when archive default is compressed, and set record `size_flags |= 0x40000000U` only when effective non-empty compression differs from archive default per D-14. Do not add compression-level public knobs per D-13 and do not apply any hidden size threshold per D-15.</action>
  <verify>
    <automated>ctest --preset windows-msvc-debug-static -L tes4_bsa_writer --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `src/formats/bsa/tes4_bsa_writer.cpp` contains `compress_payload`, `compression_method::deflate`, `compression_method::lz4_frame`, and `0x40000000U`.
    - `src/formats/bsa/tes4_bsa_writer.cpp` contains no public compression level option names.
    - Reopened v103/v104 writer output reports deflate for compressed non-empty entries; v105 reports LZ4 frame.
    - Reopened zero-byte entries report `entry_compression::none` even under all-compressed policy.
    - The automated command passes.
  </acceptance_criteria>
  <done>Writer compression behavior is metadata-driven, target-profile-routed, toggle-correct, and reader-verified for all TES4-family targets.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| writer payload encoder→compression router | Raw caller bytes enter private compression codecs through explicit routing. |
| compressed archive→reader extraction | Compressed bytes are later decoded by exact-size reader logic. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04-01 | Tampering | compression policy resolution | mitigate | Route only by explicit target profile and public policy enums; never infer compression from extension. |
| T-07-04-02 | Tampering | compression failure handling | mitigate | Propagate `compress_payload` errors; do not silently store raw when compression was requested. |
| T-07-04-03 | Denial of Service | raw size prefix | mitigate | Check raw size fits `uint32_t` before prefix serialization; oversized inputs return structured `format_error`. |
</threat_model>

<verification>
Run `ctest --preset windows-msvc-debug-static -L tes4_bsa_writer --output-on-failure`, then `ctest --preset windows-msvc-debug-static -R "compression_router|deflate_codec|lz4_codec|tes4_bsa_reader" --output-on-failure`, and verify `git -C TES5Edit status --short` is empty.
</verification>

<success_criteria>
Compression defaults and per-entry overrides round-trip for v103/v104 deflate and v105 LZ4-frame, zero-byte entries stay raw, and tests assert metadata plus extracted bytes through `archive_reader`.
</success_criteria>

<output>
After completion, create `.planning/phases/07-tes4-family-bsa-write-new-support/07-04-SUMMARY.md`.
</output>
