---
phase: 04-tes3-bsa-read-extract
plan: 01
subsystem: testing
tags: [cxx20, cmake, catch2, tes3, bsa, fixtures, red-tests]

requires:
  - phase: 03-format-detection-and-tes4-family-bsa-read-extract
    provides: Existing archive_reader API, TES4 fixture/test patterns, and generated fixture workflow
provides:
  - TES3 success fixture generator and committed success/malformed archive contracts
  - RED TES3 public reader tests for metadata, entries, lookup, extraction, and malformed handling
  - Manifest proof of raw TES3 data-section offsets versus archive-absolute public payload offsets
affects: [tes3-reader, bsa-fixtures, archive-reader-tests, phase-04]

tech-stack:
  added: []
  patterns:
    - Separate generated TES3 fixture tool mirroring TES4 fixture provenance
    - Manifest-backed RED tests through public archive_reader APIs only

key-files:
  created:
    - tests/fixtures/generated/generate_tes3_bsa_fixtures.cpp
    - tests/fixtures/generated/archives/tes3_success.bsa
    - tests/fixtures/generated/archives/tes3_success_manifest.json
    - tests/fixtures/generated/archives/tes3_malformed_manifest.json
    - tests/unit/tes3_bsa_reader_tests.cpp
  modified:
    - tests/CMakeLists.txt
    - tests/fixtures/README.md

key-decisions:
  - "TES3 fixture manifests record both raw_tes3_data_offset and archive-absolute payload_offset so later parser work can prove D-01 through D-03 without expanding public metadata."
  - "TES3 RED tests compare manifest archive_hash values through the current public hash field until the planned Phase 04-02 neutral public rename lands."

patterns-established:
  - "TES3 fixtures are regenerated by generate_tes3_bsa_fixtures and committed from synthetic repository-owned payload bytes."
  - "TES3 malformed cases are manifest-driven and assert stable error codes only, not diagnostics."

requirements-completed: [BSA-04, BSA-08]

duration: 4 min
completed: 2026-05-08
---

# Phase 04 Plan 01: TES3 Fixture and RED Reader Test Contract Summary

**Generated TES3 success/malformed BSA fixtures with manifest-backed RED archive_reader tests for Morrowind metadata, lookup, extraction, and fail-closed behavior.**

## Performance

- **Duration:** 4 min
- **Started:** 2026-05-08T11:42:55Z
- **Completed:** 2026-05-08T11:46:58Z
- **Tasks:** 3/3
- **Files modified:** 18

## Accomplishments

- Added a standalone TES3 fixture generator target that emits a rich success archive plus required malformed archive cases from synthetic repository-owned bytes.
- Committed TES3 manifests that document legal provenance, stored TES3 hashes, low/high hash halves, raw TES3 data-section offsets, archive-absolute payload offsets, and expected payload bytes.
- Added RED Catch2 tests for TES3 metadata, entries, lookup, extraction, and malformed behavior through `archive_reader` APIs only.

## Task Commits

Each task was committed atomically:

1. **Task 1: Add TES3 fixture generator and CMake target** - `8df7b39` (feat)
2. **Task 2: Add RED TES3 reader tests for open, entries, lookup, and extraction** - `747c52e` (test)
3. **Task 3: Add RED malformed TES3 tests** - `583bc9c` (test)

**Plan metadata:** pending final docs commit

## Files Created/Modified

- `tests/fixtures/generated/generate_tes3_bsa_fixtures.cpp` - Generates TES3 success and malformed fixture bytes/manifests using `libbsa::detail::hash_tes3`.
- `tests/fixtures/generated/archives/tes3_success.bsa` - Rich legal TES3 success archive with four representative entries.
- `tests/fixtures/generated/archives/tes3_success_manifest.json` - Success contract with hash halves, raw/absolute offsets, lookup variants, and expected payload hex.
- `tests/fixtures/generated/archives/tes3_malformed_manifest.json` - Malformed-case contract with stable expected error codes.
- `tests/fixtures/generated/archives/tes3_*.bsa` - Generated malformed archive cases for truncation, invalid spans, duplicates, hash mismatch/collision, unsorted hashes, and raw-offset regression.
- `tests/unit/tes3_bsa_reader_tests.cpp` - RED tests for TES3 open/list/lookup/extract/malformed behavior.
- `tests/CMakeLists.txt` - Registers the TES3 fixture generator and TES3 test source.
- `tests/fixtures/README.md` - Documents TES3 fixture regeneration, schema, and legal provenance.

## Decisions Made

- TES3 manifests include both public `payload_offset` and manifest-only `raw_tes3_data_offset` so future parser tasks can validate data-section-relative conversion without adding a new public field.
- TES3 tests currently compare manifest `archive_hash` values against the existing public hash field name; Phase 04-02 owns the public `tes4_hash` to `archive_hash` rename.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

- RED TES3 tests fail as expected because TES3 detection/parser implementation is intentionally deferred to later Phase 04 plans. The failing assertions currently show `archive_reader::open` does not yet open TES3 fixtures, and malformed TES3 files return pre-implementation errors.

## Verification

- `cmake --build --preset windows-msvc-debug-static --target generate_tes3_bsa_fixtures` — passed and regenerated TES3 success/malformed outputs.
- Task 1 acceptance greps for `generate_tes3_bsa_fixtures_tool`, `raw_tes3_data_offset`, legal provenance text, and `git -C TES5Edit status --short` — passed.
- RED command for metadata/entries/lookup/extract labels — built successfully and failed at runtime as expected before TES3 implementation.
- RED command for malformed label — built successfully and failed at runtime as expected before TES3 implementation.
- Overall TES3 RED verification for all TES3 labels — built successfully and failed as expected before TES3 implementation.
- `git -C TES5Edit status --short` — produced no output.

## Known Stubs

None. Stub scan found the intentional zero-byte TES3 fixture entry (`Icons/EmptyMarker.txt`), which is a required success-case behavior proof rather than an unwired placeholder.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

Ready for Plan 04-02. The fixture/test contract now defines the expected TES3 behavior for detector, metadata rename, parser, lookup, extraction, and malformed validation work.

## Self-Check: PASSED

- Found created SUMMARY, TES3 generator, TES3 success archive/manifest, TES3 malformed manifest, and TES3 reader test file.
- Verified task commits exist: `8df7b39`, `747c52e`, and `583bc9c`.

---
*Phase: 04-tes3-bsa-read-extract*
*Completed: 2026-05-08*
