---
phase: 04-tes3-bsa-read-extract
plan: 05
type: execute
wave: 5
depends_on: [04-04]
files_modified:
  - src/archive.cpp
  - src/formats/bsa/tes3_bsa_reader.hpp
  - src/formats/bsa/tes3_bsa_reader.cpp
  - src/formats/bsa/tes3_bsa_parser.cpp
  - tests/fixtures/generated/generate_tes3_bsa_fixtures.cpp
  - tests/fixtures/generated/archives/tes3_hash_collision.bsa
  - tests/fixtures/generated/archives/tes3_malformed_manifest.json
  - tests/unit/tes3_bsa_reader_tests.cpp
autonomous: true
gap_closure: true
requirements: [BSA-04, BSA-08]
must_haves:
  truths:
    - "Maintainer can validate TES3 malformed hash-collision behavior with a fixture/test that reaches duplicate stored-hash validation instead of only stored-hash mismatch."
    - "TES3 `extract(path, sink)` streams selected payload bytes from the host archive in bounded chunks without pre-buffering the whole selected payload."
    - "BSA-04 remains satisfied: consumers can read, list, query, and extract TES3/Morrowind BSA archives through `archive_reader`."
    - "BSA-08 remains satisfied: TES3 extraction uses parser-materialized archive-absolute offsets converted from data-section-relative record offsets."
  artifacts:
    - path: "tests/fixtures/generated/generate_tes3_bsa_fixtures.cpp"
      provides: "TES3 hash-collision malformed fixture construction"
      contains: "tes3_hash_collision"
    - path: "tests/unit/tes3_bsa_reader_tests.cpp"
      provides: "Collision branch and bounded streaming regression tests"
      contains: "tes3_bsa_malformed"
    - path: "src/archive.cpp"
      provides: "Variant-aware extraction dispatch before whole-payload buffering"
      contains: "extract_tes3_bsa_payload"
    - path: "src/formats/bsa/tes3_bsa_reader.cpp"
      provides: "TES3 bounded host-file payload streaming"
      contains: "extraction_chunk_size"
  key_links:
    - from: "tests/fixtures/generated/generate_tes3_bsa_fixtures.cpp"
      to: "src/formats/bsa/tes3_bsa_parser.cpp"
      via: "tes3_hash_collision malformed case exercises duplicate stored hash validation"
      pattern: "duplicate stored hash records"
    - from: "src/archive.cpp"
      to: "src/formats/bsa/tes3_bsa_reader.cpp"
      via: "TES3 extraction dispatch passes host_path and selected entry before read_stored_payload"
      pattern: "state_->metadata.variant == archive_variant::tes3"
---

<objective>
Close the two Phase 4 verification gaps: malformed hash-collision fixture isolation and true sink-first bounded TES3 extraction.

Purpose: Phase 4 cannot be considered complete while one malformed fixture proves only an earlier hash-mismatch branch and TES3 extraction pre-buffers the selected payload before writing to the caller sink.
Output: Corrected TES3 collision fixture/test coverage, host-file streaming TES3 extraction, regenerated committed fixture artifacts, and green focused/full regression gates.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-tes3-bsa-read-extract/04-SPEC.md
@.planning/phases/04-tes3-bsa-read-extract/04-CONTEXT.md
@.planning/phases/04-tes3-bsa-read-extract/04-RESEARCH.md
@.planning/phases/04-tes3-bsa-read-extract/04-VERIFICATION.md
@.planning/phases/04-tes3-bsa-read-extract/04-PATTERNS.md
@.planning/phases/04-tes3-bsa-read-extract/04-01-SUMMARY.md
@.planning/phases/04-tes3-bsa-read-extract/04-03-SUMMARY.md
@.planning/phases/04-tes3-bsa-read-extract/04-04-SUMMARY.md
@src/archive.cpp
@src/formats/bsa/tes3_bsa_parser.cpp
@src/formats/bsa/tes3_bsa_reader.hpp
@src/formats/bsa/tes3_bsa_reader.cpp
@tests/fixtures/generated/generate_tes3_bsa_fixtures.cpp
@tests/unit/tes3_bsa_reader_tests.cpp

<interfaces>
From `src/archive.cpp` current extraction gap:
```cpp
auto payload = read_stored_payload(state_->host_path, *found.value());
if (!payload) {
  return payload.error();
}
if (state_->metadata.variant == archive_variant::tes3) {
  return formats::bsa::extract_tes3_bsa_payload(payload.value(), *found.value(), sink);
}
return formats::bsa::extract_tes4_bsa_payload(payload.value(), *found.value(), sink);
```

From `src/formats/bsa/tes3_bsa_reader.hpp` current helper signature:
```cpp
result<void> extract_tes3_bsa_payload(std::span<const std::byte> stored_payload, const entry_metadata& entry,
                                      payload_sink& sink);
```

From `src/formats/bsa/tes3_bsa_parser.cpp` current validation order:
```cpp
const auto stored_hash = hashes[index];
const auto computed_hash = detail::hash_tes3(names[index]);
if (stored_hash != computed_hash) {
  return error{error_code::format_error, "TES3 BSA stored hash does not match parsed name"};
}
...
if (!stored_hashes.insert(stored_hash).second) {
  return error{error_code::format_error, "TES3 BSA contains duplicate stored hash records"};
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make TES3 hash-collision fixture exercise duplicate stored-hash validation</name>
  <files>src/formats/bsa/tes3_bsa_parser.cpp, tests/fixtures/generated/generate_tes3_bsa_fixtures.cpp, tests/fixtures/generated/archives/tes3_hash_collision.bsa, tests/fixtures/generated/archives/tes3_malformed_manifest.json, tests/unit/tes3_bsa_reader_tests.cpp</files>
  <read_first>src/formats/bsa/tes3_bsa_parser.cpp; tests/fixtures/generated/generate_tes3_bsa_fixtures.cpp; tests/fixtures/generated/archives/tes3_malformed_manifest.json; tests/unit/tes3_bsa_reader_tests.cpp; .planning/phases/04-tes3-bsa-read-extract/04-VERIFICATION.md; .planning/phases/04-tes3-bsa-read-extract/04-CONTEXT.md</read_first>
  <behavior>
    - The `tes3_hash_collision` malformed case fails because the parser detects a duplicate stored hash record, not because the second entry's stored hash mismatches its parsed name.
    - D-15 remains true: non-duplicate stored hash mismatches still fail `archive_reader::open` with `error_code::format_error`.
    - D-16 and D-18 are directly covered by a collision/duplicate-hash malformed fixture or focused test.
    - D-19 remains true: tests assert stable error codes and branch-specific structural evidence, not diagnostic message text.
  </behavior>
  <action>Close the first verification gap. Update `tes3_bsa_parser.cpp` so duplicate stored-hash validation can be reached before a same-hash malformed record is rejected solely by the stored-vs-computed mismatch check. Preserve `error_code::format_error` for both duplicate stored hashes and standalone stored hash mismatches; do not expose new public APIs. Update or supplement `generate_tes3_bsa_fixtures.cpp` so `tes3_hash_collision.bsa` is intentionally documented as the duplicate stored-hash case instead of a generic mismatch case: keep `tes3_stored_hash_mismatch.bsa` as the mismatch fixture, keep `tes3_hash_collision.bsa` as the duplicate stored-hash fixture, regenerate the committed `tes3_hash_collision.bsa` and `tes3_malformed_manifest.json` with the existing generator target, and add a focused test note/assertion that distinguishes the two malformed case names while still checking only `error_code::format_error`. Do not hand-edit opaque `.bsa` bytes; regenerate them from the fixture generator per D-24.</action>
  <verify><automated>cmake --build --preset windows-msvc-debug-static --target generate_tes3_bsa_fixtures && cmake --build --preset windows-msvc-debug-static && ctest --preset windows-msvc-debug-static -L tes3_bsa_malformed --output-on-failure</automated></verify>
  <acceptance_criteria>`tests/fixtures/generated/generate_tes3_bsa_fixtures.cpp` contains `tes3_hash_collision`; `tests/fixtures/generated/archives/tes3_malformed_manifest.json` contains both `tes3_stored_hash_mismatch.bsa` and `tes3_hash_collision.bsa`; `src/formats/bsa/tes3_bsa_parser.cpp` contains `duplicate stored hash records`; `tests/unit/tes3_bsa_reader_tests.cpp` contains `tes3_hash_collision`; `ctest --preset windows-msvc-debug-static -L tes3_bsa_malformed --output-on-failure` exits 0.</acceptance_criteria>
  <done>The collision malformed fixture/test isolates duplicate stored-hash validation while stored-hash mismatch coverage remains intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Stream TES3 extraction directly from the host archive in bounded chunks</name>
  <files>src/archive.cpp, src/formats/bsa/tes3_bsa_reader.hpp, src/formats/bsa/tes3_bsa_reader.cpp, tests/unit/tes3_bsa_reader_tests.cpp</files>
  <read_first>src/archive.cpp; src/formats/bsa/tes3_bsa_reader.hpp; src/formats/bsa/tes3_bsa_reader.cpp; src/formats/bsa/tes4_bsa_reader.cpp; tests/unit/tes3_bsa_reader_tests.cpp; .planning/phases/04-tes3-bsa-read-extract/04-VERIFICATION.md; .planning/phases/04-tes3-bsa-read-extract/04-SPEC.md</read_first>
  <behavior>
    - TES3 `archive_reader::extract(path, sink)` dispatches to the TES3 helper before calling `read_stored_payload`, so TES3 no longer allocates `std::vector<std::byte>(entry.stored_size)` in `src/archive.cpp`.
    - The TES3 helper opens the host archive, seeks to `entry.payload_offset`, reads at most `extraction_chunk_size` bytes per pass, and writes each chunk to `payload_sink`.
    - D-06 remains true: zero-byte TES3 entries succeed without requiring a payload read and `extract_bytes` returns an empty vector.
    - TES4-family extraction behavior remains unchanged unless only neutral helper factoring is required.
  </behavior>
  <action>Close the second verification gap. Change the TES3 helper signature in `tes3_bsa_reader.hpp/.cpp` to accept `std::string_view host_path`, the selected `entry_metadata`, and `payload_sink&` (or an equivalent host-file streaming contract) instead of `std::span<const std::byte> stored_payload`. Move TES3 host-file opening, stream-limit checks, `seekg(entry.payload_offset)`, bounded chunk reads, exact `entry.stored_size` byte countdown, and `payload_sink::write` full-chunk enforcement into `tes3_bsa_reader.cpp`. Update `archive_reader::extract` so after lookup it immediately calls the TES3 streaming helper when `state_->metadata.variant == archive_variant::tes3`; only TES4-family extraction should continue through `read_stored_payload` + `extract_tes4_bsa_payload`. Keep `extract_bytes(path)` implemented through the public `extract(path, sink)` sink adapter so TES3 byte-vector extraction uses the same streaming path. Add or strengthen a regression test that would fail if TES3 extraction pre-buffered the selected payload before sink writes; use a custom sink that records call sizes and asserts no write exceeds the TES3 `extraction_chunk_size` behavior for a generated payload large enough to require multiple writes, or an equivalent fixture/test mutation that proves bounded host-file chunking.</action>
  <verify><automated>cmake --build --preset windows-msvc-debug-static && ctest --preset windows-msvc-debug-static -L "tes3_bsa_extract|tes4_bsa" --output-on-failure</automated></verify>
  <acceptance_criteria>`src/archive.cpp` has a TES3 variant branch before the `read_stored_payload` call; `src/formats/bsa/tes3_bsa_reader.hpp` no longer declares `extract_tes3_bsa_payload(std::span<const std::byte> stored_payload`; `src/formats/bsa/tes3_bsa_reader.cpp` contains `std::ifstream`, `seekg`, and `extraction_chunk_size`; `grep -R "std::vector<std::byte> payload(static_cast<std::size_t>(entry.stored_size))" src/archive.cpp` still finds only the TES4-family buffering path or no occurrence in a TES3 path; `ctest --preset windows-msvc-debug-static -L "tes3_bsa_extract|tes4_bsa" --output-on-failure` exits 0.</acceptance_criteria>
  <done>TES3 public extraction streams from the host archive in bounded chunks and TES4-family regression tests remain green.</done>
</task>

<task type="auto">
  <name>Task 3: Re-run Phase 4 gap-closure and boundary verification</name>
  <files>src/archive.cpp, src/formats/bsa/tes3_bsa_reader.cpp, src/formats/bsa/tes3_bsa_parser.cpp, tests/fixtures/generated/archives/tes3_malformed_manifest.json, tests/unit/tes3_bsa_reader_tests.cpp</files>
  <read_first>.planning/phases/04-tes3-bsa-read-extract/04-VERIFICATION.md; .planning/phases/04-tes3-bsa-read-extract/04-SPEC.md; .planning/phases/04-tes3-bsa-read-extract/04-CONTEXT.md; src/archive.cpp; src/formats/bsa/tes3_bsa_reader.cpp; tests/unit/tes3_bsa_reader_tests.cpp</read_first>
  <action>Run the focused commands for both verification gaps and then the full Phase 4 regression gate. Confirm the two failed truths in `04-VERIFICATION.md` are now satisfied: `tes3_hash_collision` is no longer a hollow stored-hash mismatch fixture, and TES3 extraction no longer pre-buffers the selected payload before sink writes. Verify public boundary and TES5Edit read-only constraints: public headers still do not expose TES5Edit/libdeflate/lz4/DirectXTex/private parser types, no generated fixture writes under `TES5Edit/`, and `git -C TES5Edit status --short` has no output.</action>
  <verify><automated>ctest --preset windows-msvc-debug-static -L "tes3_bsa_malformed|tes3_bsa_extract|tes4_bsa|public_include_boundary" --output-on-failure && ctest --preset windows-msvc-debug-static --output-on-failure && pwsh -NoProfile -Command '$status = git -C TES5Edit status --short; if ($status) { throw "TES5Edit has unexpected changes: $status" }'</automated></verify>
  <acceptance_criteria>Focused CTest labels `tes3_bsa_malformed`, `tes3_bsa_extract`, `tes4_bsa`, and `public_include_boundary` exit 0; full `ctest --preset windows-msvc-debug-static --output-on-failure` exits 0; `git -C TES5Edit status --short` produces no output; `tests/fixtures/generated/archives/tes3_malformed_manifest.json` includes `tes3_hash_collision.bsa`; `src/archive.cpp` dispatches TES3 extraction before whole-payload buffering.</acceptance_criteria>
  <done>Both Phase 4 verification gaps are closed with automated evidence and no TES5Edit or public-boundary regressions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| malformed TES3 bytes→parser validation | Untrusted hash records, names, offsets, and sizes determine whether archive state is exposed. |
| archive metadata→host-file extraction | Validated offsets and sizes select host-file byte ranges for streaming. |
| payload reader→caller sink | Untrusted archive bytes cross into caller-owned sink code that can accept partial writes or fail. |
| fixture generator→committed fixtures | Generated binary fixtures become CI evidence and must reflect the intended malformed branch. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-G05-01 | Tampering | `tes3_hash_collision` fixture | mitigate | Regenerate fixture from source and ensure it covers duplicate stored-hash validation rather than only hash mismatch. |
| T-04-G05-02 | Denial of Service | TES3 extraction | mitigate | Stream from host file in `extraction_chunk_size` chunks instead of allocating the selected payload before sink writes. |
| T-04-G05-03 | Tampering / Reliability | `payload_sink` writes | mitigate | Preserve full-chunk write enforcement and return `error_code::io_error` for partial accepted writes. |
| T-04-G05-04 | Tampering | TES4 regression surface | mitigate | Run `tes4_bsa` tests after extraction dispatch changes to ensure existing behavior remains unchanged. |
| T-04-G05-05 | Information Disclosure | `TES5Edit/` submodule | mitigate | Final gate fails if `git -C TES5Edit status --short` reports any mutation. |
</threat_model>

<verification>
Focused gap gate: `ctest --preset windows-msvc-debug-static -L "tes3_bsa_malformed|tes3_bsa_extract|tes4_bsa|public_include_boundary" --output-on-failure`.
Full regression gate: `ctest --preset windows-msvc-debug-static --output-on-failure`.
Boundary gate: `git -C TES5Edit status --short` must produce no output.
</verification>

<success_criteria>
Phase 4 verification report gaps are closed: TES3 collision fixture/test coverage reaches duplicate stored-hash validation, TES3 extraction is bounded and sink-first from the host archive, BSA-04 and BSA-08 remain covered, and TES4/public-boundary/TES5Edit gates stay green.
</success_criteria>

<output>
After completion, create `.planning/phases/04-tes3-bsa-read-extract/04-05-SUMMARY.md`
</output>
