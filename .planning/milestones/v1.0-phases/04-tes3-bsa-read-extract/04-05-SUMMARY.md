---
phase: 04-tes3-bsa-read-extract
plan: 05
subsystem: archive-formats
tags: [cpp20, tes3-bsa, extraction, fixtures, ctest]
requires:
  - phase: 04-tes3-bsa-read-extract
    provides: TES3 open/list/query/extract reader path and generated fixture infrastructure
provides:
  - TES3 malformed hash-collision fixture intent separated from stored-hash mismatch coverage
  - TES3 host-file extraction path that streams bounded chunks directly to payload_sink
  - Phase 4 gap-closure verification across TES3, TES4, public boundary, and TES5Edit gates
affects: [phase-04, phase-05-ba2-gnrl-read-extract, phase-10-tes3-bsa-write-new, archive_reader]
tech-stack:
  added: []
  patterns: [TDD red-green commits, generated fixture manifest branch markers, host-file bounded extraction]
key-files:
  created:
    - .planning/phases/04-tes3-bsa-read-extract/04-05-SUMMARY.md
  modified:
    - src/archive.cpp
    - src/formats/bsa/tes3_bsa_parser.cpp
    - src/formats/bsa/tes3_bsa_reader.hpp
    - src/formats/bsa/tes3_bsa_reader.cpp
    - tests/fixtures/generated/generate_tes3_bsa_fixtures.cpp
    - tests/fixtures/generated/archives/tes3_malformed_manifest.json
    - tests/unit/tes3_bsa_reader_tests.cpp
key-decisions:
  - "TES3 duplicate stored-hash validation runs before standalone stored-vs-computed hash mismatch validation so collision fixtures exercise the intended branch."
  - "TES3 public extraction dispatches to a host-path streaming helper before generic stored-payload buffering, leaving TES4-family buffering behavior unchanged."
patterns-established:
  - "Malformed fixture manifests can include structural_issue markers for tests that need branch-specific evidence while still asserting stable error codes."
  - "TES3 extraction helpers own host-file seek/read loops and enforce full sink writes per bounded chunk."
requirements-completed: [BSA-04, BSA-08]
duration: 3min
completed: 2026-05-08
---

# Phase 04 Plan 05: Gap Closure Summary

**TES3 malformed hash-collision proof and bounded host-file extraction for `archive_reader`**

## Performance

- **Duration:** 3 min
- **Started:** 2026-05-08T12:41:12Z
- **Completed:** 2026-05-08T12:44:29Z
- **Tasks:** 3
- **Files modified:** 7

## Accomplishments

- Reordered TES3 parser hash validation so duplicate stored hash records are detected before a same-hash malformed entry is masked by stored-vs-computed mismatch validation.
- Updated the TES3 malformed generator/manifest/tests to distinguish `tes3_stored_hash_mismatch` from `tes3_hash_collision` via generated structural issue markers while preserving stable `format_error` assertions.
- Changed TES3 extraction to dispatch before generic payload buffering and stream from the host archive in 64 KiB chunks with full-chunk sink write enforcement.
- Re-ran focused gap gates, full regression tests, and the TES5Edit read-only boundary check.

## Task Commits

Each task was committed atomically where code/test changes existed:

1. **Task 1 RED: TES3 malformed branch assertions** - `ba6ea66` (test)
2. **Task 1 GREEN: Duplicate hash validation isolation** - `e1f595c` (feat)
3. **Task 2 RED: Host streaming helper contract** - `9a7d52a` (test)
4. **Task 2 GREEN: Host-file bounded TES3 extraction** - `7e1b0f0` (feat)
5. **Task 3: Verification only** - no code commit; all gates passed with a clean working tree.

**Plan metadata:** pending final docs commit

## Files Created/Modified

- `src/archive.cpp` - Dispatches TES3 extraction to the TES3 helper before `read_stored_payload`; TES4-family extraction still uses the existing buffered payload path.
- `src/formats/bsa/tes3_bsa_parser.cpp` - Checks duplicate stored TES3 hashes before standalone name-hash mismatches so collision fixtures reach the intended validation branch.
- `src/formats/bsa/tes3_bsa_reader.hpp` - Changes the TES3 extraction helper contract from buffered payload span to host path plus entry metadata.
- `src/formats/bsa/tes3_bsa_reader.cpp` - Opens the host archive, seeks to `entry.payload_offset`, reads at `extraction_chunk_size` boundaries, and enforces full sink writes.
- `tests/fixtures/generated/generate_tes3_bsa_fixtures.cpp` - Documents generated malformed hash intent and writes `structural_issue` markers for mismatch vs duplicate-hash cases.
- `tests/fixtures/generated/archives/tes3_malformed_manifest.json` - Regenerated by the fixture target with structural issue markers for the two strict hash cases.
- `tests/unit/tes3_bsa_reader_tests.cpp` - Adds branch-intent assertions and bounded-chunk host streaming regression coverage.

## Decisions Made

- TES3 duplicate stored-hash validation now precedes stored-vs-computed mismatch validation to satisfy D-16/D-18 fixture isolation without exposing new public APIs.
- TES3 extraction owns host-file streaming in `tes3_bsa_reader.cpp`; `archive_reader::extract_bytes` continues to route through `extract(path, sink)`.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

- TDD RED gates failed as expected: first because generated malformed cases lacked `structural_issue`, then because the TES3 helper still accepted a buffered span.
- `generate_tes3_bsa_fixtures` regenerated the malformed manifest; `tes3_hash_collision.bsa` was already byte-identical for the intended duplicate stored-hash structure, so the binary fixture did not require a content change.

## Verification

- `cmake --build --preset windows-msvc-debug-static --target generate_tes3_bsa_fixtures && cmake --build --preset windows-msvc-debug-static && ctest --preset windows-msvc-debug-static -L tes3_bsa_malformed --output-on-failure` — passed.
- `cmake --build --preset windows-msvc-debug-static && ctest --preset windows-msvc-debug-static -L "tes3_bsa_extract|tes4_bsa" --output-on-failure` — passed.
- `ctest --preset windows-msvc-debug-static -L "tes3_bsa_malformed|tes3_bsa_extract|tes4_bsa|public_include_boundary" --output-on-failure` — 24/24 passed.
- `ctest --preset windows-msvc-debug-static --output-on-failure` — 65 passed, 1 skipped (`local game fixtures are opt-in`), 0 failed.
- `git -C TES5Edit status --short` — no output.

## Known Stubs

None.

## Self-Check: PASSED

- Verified all created/modified files listed in this summary exist.
- Verified task commits `ba6ea66`, `e1f595c`, `9a7d52a`, and `7e1b0f0` are present in git history.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Phase 4 BSA-04/BSA-08 gap closure is ready for milestone audit or transition to BA2 read phases.
- Residual risk is limited to broader real-world TES3 hash collisions, which remain outside committed synthetic fixture scope but now have the correct strict-validation branch ordering.

---
*Phase: 04-tes3-bsa-read-extract*
*Completed: 2026-05-08*
