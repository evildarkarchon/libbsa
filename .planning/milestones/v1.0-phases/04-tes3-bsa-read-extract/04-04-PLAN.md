---
phase: 04-tes3-bsa-read-extract
plan: 04
type: tdd
wave: 4
depends_on: [04-03]
files_modified:
  - src/archive.cpp
  - src/formats/bsa/tes3_bsa_parser.cpp
  - src/formats/bsa/tes3_bsa_reader.hpp
  - src/formats/bsa/tes3_bsa_reader.cpp
  - tests/unit/tes3_bsa_reader_tests.cpp
autonomous: true
requirements: [BSA-04, BSA-08]
must_haves:
  truths:
    - "Consumer can extract every generated TES3 entry through `extract(path, sink)` and `extract_bytes(path)`."
    - "TES3 extraction uses archive-absolute public offsets produced from data-section-relative raw offsets per BSA-08 and D-01 through D-09."
    - "Malformed TES3 payload spans, overlaps, counts, names, duplicates, and hash cases fail closed with stable structured errors."
    - "TES3 support does not invoke compression codecs and does not retain whole archive bytes in reader state."
    - "D-05: Invalid spans, overflow, overlapping metadata/name/hash/data regions, and overlapping payload ranges fail open with `error_code::format_error`."
    - "D-06: Zero-byte entries are valid within the data section and extract as empty payloads."
    - "D-07: TES3 raw data order and payload offset order are not required to be alphabetical or sorted, only non-overlapping and bounded."
  artifacts:
    - path: "src/formats/bsa/tes3_bsa_reader.cpp"
      provides: "TES3 raw sink extraction helper"
      contains: "entry_compression::none"
    - path: "src/formats/bsa/tes3_bsa_parser.cpp"
      provides: "Strict payload span and overlap validation"
      contains: "format_error"
    - path: "tests/unit/tes3_bsa_reader_tests.cpp"
      provides: "Extraction, malformed, final regression coverage"
      contains: "tes3_bsa_extract"
  key_links:
    - from: "src/archive.cpp"
      to: "src/formats/bsa/tes3_bsa_reader.cpp"
      via: "variant-aware extract dispatch"
      pattern: "extract_tes3_bsa_payload"
    - from: "src/formats/bsa/tes3_bsa_parser.cpp"
      to: "tests/fixtures/generated/archives/tes3_malformed_manifest.json"
      via: "malformed open failures"
      pattern: "format_error"
---

<objective>
Complete TES3 raw extraction, malformed fail-closed validation, and final Phase 4 regression gates.

Purpose: BSA-04 and BSA-08 are complete only when TES3 entries extract exact bytes using data-section-relative offset semantics and unsafe archive structures are rejected before extraction can misread data.
Output: TES3 raw extraction dispatch, strict malformed validation, green TES3/TES4/public boundary tests, and TES5Edit read-only verification.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-tes3-bsa-read-extract/04-SPEC.md
@.planning/phases/04-tes3-bsa-read-extract/04-CONTEXT.md
@.planning/phases/04-tes3-bsa-read-extract/04-RESEARCH.md
@.planning/phases/04-tes3-bsa-read-extract/04-VALIDATION.md
@.planning/phases/04-tes3-bsa-read-extract/04-PATTERNS.md
@.planning/phases/03-format-detection-and-tes4-family-bsa-read-extract/03-06-SUMMARY.md
@src/archive.cpp
@src/formats/bsa/tes3_bsa_parser.cpp
@src/formats/bsa/tes3_bsa_reader.cpp
@tests/unit/tes3_bsa_reader_tests.cpp
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement TES3 sink-first raw extraction and extract_bytes routing</name>
  <files>src/archive.cpp, src/formats/bsa/tes3_bsa_reader.hpp, src/formats/bsa/tes3_bsa_reader.cpp, tests/unit/tes3_bsa_reader_tests.cpp</files>
  <read_first>src/archive.cpp; src/formats/bsa/tes3_bsa_reader.hpp; src/formats/bsa/tes3_bsa_reader.cpp; src/formats/bsa/tes4_bsa_reader.cpp; tests/unit/tes3_bsa_reader_tests.cpp; .planning/phases/04-tes3-bsa-read-extract/04-CONTEXT.md; .planning/phases/04-tes3-bsa-read-extract/04-RESEARCH.md; .planning/phases/03-format-detection-and-tes4-family-bsa-read-extract/03-06-SUMMARY.md</read_first>
  <behavior>
    - TES3 `extract(path, sink)` finds by normalized path and streams exactly `stored_size` bytes from archive-absolute `payload_offset`.
    - D-06: TES3 `extract_bytes(path)` reuses the public sink extraction path, including zero-byte entries that write nothing and return an empty payload.
    - TES3 extraction rejects any non-`entry_compression::none` metadata as `format_error` and never calls deflate/LZ4 routing.
    - Partial sink writes return `error_code::io_error`; valid missing paths return `not_found`; invalid paths return `invalid_argument`.
  </behavior>
  <action>Add `extract_tes3_bsa_payload(std::span<const entry_metadata> entries, std::string_view host_path, std::string_view path, payload_sink& sink)` or equivalent TES3 helper in `tes3_bsa_reader.*`. Reuse the Plan 03-06 selected-entry, bounded host-file seek/read, and `payload_sink::write` full-chunk contract, but only support `entry_compression::none` and raw bytes. Update `src/archive.cpp` so `archive_reader::extract` dispatches by archive variant and `extract_bytes` continues to be a bounded vector sink wrapper over `extract(path, sink)`. Per D-04: do not recover or recompute raw TES3 offsets at extraction time; the parser must already have stored archive-absolute offsets. Per D-06: a zero-byte entry writes nothing and succeeds with an empty vector.</action>
  <verify><automated>cmake --build --preset windows-msvc-debug-static && ctest --preset windows-msvc-debug-static -L tes3_bsa_extract --output-on-failure</automated></verify>
  <acceptance_criteria>`grep -R "extract_tes3_bsa_payload" src/formats/bsa src/archive.cpp` finds TES3 extraction helper and dispatch; `grep -R "entry_compression::none" src/formats/bsa/tes3_bsa_reader.cpp` confirms raw-only extraction; `grep -R "decompress_payload_exact\|compression_router" src/formats/bsa/tes3_bsa_reader.cpp` finds no codec routing; `ctest -L tes3_bsa_extract` passes including sink and `extract_bytes` byte comparisons.</acceptance_criteria>
  <done>TES3 sink and byte-vector extraction return exact payload bytes using parser-materialized archive-absolute offsets.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce strict malformed TES3 span and overlap validation</name>
  <files>src/formats/bsa/tes3_bsa_parser.cpp, tests/unit/tes3_bsa_reader_tests.cpp</files>
  <read_first>src/formats/bsa/tes3_bsa_parser.cpp; tests/unit/tes3_bsa_reader_tests.cpp; tests/fixtures/generated/archives/tes3_malformed_manifest.json; .planning/phases/04-tes3-bsa-read-extract/04-SPEC.md; .planning/phases/04-tes3-bsa-read-extract/04-CONTEXT.md; .planning/phases/04-tes3-bsa-read-extract/04-VALIDATION.md; .planning/phases/04-tes3-bsa-read-extract/04-RESEARCH.md</read_first>
  <behavior>
    - Open rejects truncated headers/tables, invalid name spans, invalid payload spans, duplicate canonical paths, inconsistent counts/offsets, hash mismatch, hash collision, and unsorted hash records.
    - D-05: Raw-offset plus data-section-base overflow, overlapping metadata/name/hash/data regions, and overlapping payload byte ranges fail open with `error_code::format_error`.
    - Tests assert stable error codes only per D-19.
  </behavior>
  <action>Finish strict validation in `tes3_bsa_parser.cpp`: validate `hash_table_start == 12 + hash_offset_minus_header` is after the fixed record/name-offset tables; each name offset lands inside the names section and reaches a null terminator before the hash table; `data_section_start` does not overlap metadata/name/hash regions; every absolute payload span fits the archive; per D-05: overflow, invalid spans, overlapping metadata/name/hash/data regions, and overlapping non-empty payload ranges fail open with `format_error`; per D-06: zero-byte entries at valid data-section offsets are accepted; per D-07: raw data order is not required to match entry/hash order as long as payloads are bounded and non-overlapping. Ensure each malformed manifest case from Plan 04-01 fails with `format_error` or `unsupported` as declared. Do not compare diagnostic strings in tests.</action>
  <verify><automated>cmake --build --preset windows-msvc-debug-static && ctest --preset windows-msvc-debug-static -L tes3_bsa_malformed --output-on-failure</automated></verify>
  <acceptance_criteria>`grep -R "tes3_raw_offset_absolute_regression" tests/unit/tes3_bsa_reader_tests.cpp tests/fixtures/generated/archives/tes3_malformed_manifest.json` finds regression coverage; `grep -R "overlap\|payload span\|name span" src/formats/bsa/tes3_bsa_parser.cpp` finds explicit validation branches; `grep -R "message" tests/unit/tes3_bsa_reader_tests.cpp` does not show TES3 malformed diagnostic-string assertions; `ctest -L tes3_bsa_malformed` passes.</acceptance_criteria>
  <done>Malformed TES3 archives fail closed during open before unsafe extraction, with stable structured errors.</done>
</task>

<task type="auto">
  <name>Task 3: Run final Phase 4 verification and TES5Edit cleanliness gate</name>
  <files>tests/unit/tes3_bsa_reader_tests.cpp, tests/unit/tes4_bsa_reader_tests.cpp, tests/unit/public_include_boundary_tests.cpp</files>
  <read_first>tests/unit/tes3_bsa_reader_tests.cpp; tests/unit/tes4_bsa_reader_tests.cpp; tests/unit/public_include_boundary_tests.cpp; .planning/phases/04-tes3-bsa-read-extract/04-VALIDATION.md; .planning/phases/04-tes3-bsa-read-extract/04-SPEC.md; .planning/phases/04-tes3-bsa-read-extract/04-CONTEXT.md</read_first>
  <action>Run the Phase 4 quick and full validation commands without relaxing tests. Confirm the green map covers BSA-04 and BSA-08: TES3 detection/metadata, entries, lookup, extraction, malformed fixtures, existing TES4 regression, public include boundary, generated fixtures committed, and TES5Edit read-only status. If a fixture expectation is wrong, fix the generator and regenerated manifest/archive together; do not hand-edit opaque bytes. Update only Phase 4-owned code/tests for failures and preserve Phase 3 TES4 behavior except the intentional `archive_hash` field rename.</action>
  <verify><automated>cmake --build --preset windows-msvc-debug-static && ctest --preset windows-msvc-debug-static -L unit --output-on-failure && ctest --preset windows-msvc-debug-static --output-on-failure && pwsh -NoProfile -Command '$status = git -C TES5Edit status --short; if ($status) { throw "TES5Edit has unexpected changes: $status" }'</automated></verify>
  <acceptance_criteria>Quick command `ctest --preset windows-msvc-debug-static -L unit --output-on-failure` exits 0; full command `ctest --preset windows-msvc-debug-static --output-on-failure` exits 0; `git -C TES5Edit status --short` produces no output; `grep -R "tes3_bsa_metadata\|tes3_bsa_entries\|tes3_bsa_lookup\|tes3_bsa_extract\|tes3_bsa_malformed" tests/unit/tes3_bsa_reader_tests.cpp` finds all Phase 4 tags.</acceptance_criteria>
  <done>Phase 4 is green under quick/full validation and the read-only TES5Edit boundary remains intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| parsed TES3 metadata→payload reader | Untrusted offsets and sizes select host-file byte ranges. |
| archive path input→lookup/extraction | Caller input must normalize or reject before selecting an entry. |
| payload reader→caller sink | Caller sink can report partial writes or errors. |
| malformed archive bytes→open validation | Parser must reject unsafe structures before exposing partial reader state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-14 | Tampering / DoS | TES3 payload spans | mitigate | Validate archive-absolute spans and non-overlap during open; extraction reuses validated metadata only. |
| T-04-15 | Tampering | data-section-relative offsets | mitigate | Tests read bytes at public absolute offsets and include raw-offset regression fixture proving BSA-08. |
| T-04-16 | Denial of Service | extraction buffers | mitigate | Stream selected raw payload in bounded chunks and implement `extract_bytes` through bounded vector sink. |
| T-04-17 | Tampering | compression dispatch | mitigate | TES3 reader only accepts `entry_compression::none` and never invokes deflate/LZ4 codecs. |
| T-04-18 | Tampering / Reliability | caller sink | mitigate | Treat partial accepted writes as `io_error`; never report partial extraction success. |
| T-04-19 | Information Disclosure | TES5Edit submodule | mitigate | Final gate fails if `git -C TES5Edit status --short` is non-empty. |
</threat_model>

<verification>
Quick command: `ctest --preset windows-msvc-debug-static -L unit --output-on-failure`.
Full command: `ctest --preset windows-msvc-debug-static --output-on-failure`.
Final gate also runs `git -C TES5Edit status --short` and requires no output.
</verification>

<success_criteria>
Consumers can open, list, query, and extract TES3/Morrowind BSA archives through `archive_reader`; extraction proves data-section-relative offset semantics; malformed inputs fail closed; public API and TES5Edit boundaries remain intact.
</success_criteria>

<output>
After completion, create `.planning/phases/04-tes3-bsa-read-extract/04-04-SUMMARY.md`
</output>
