---
phase: 02-binary-i-o-paths-hashes-and-compression-services
plan: 04
type: tdd
wave: 4
depends_on: [02-03]
files_modified:
  - CMakeLists.txt
  - tests/CMakeLists.txt
  - src/detail/lz4_frame_codec.hpp
  - src/detail/lz4_frame_codec.cpp
  - src/detail/lz4_block_codec.hpp
  - src/detail/lz4_block_codec.cpp
  - src/detail/compression_router.hpp
  - src/detail/compression_router.cpp
  - tests/unit/lz4_codec_tests.cpp
  - tests/unit/compression_router_tests.cpp
autonomous: true
requirements: [BIN-05, BIN-06, BIN-07]
must_haves:
  truths:
    - "LZ4 frame payloads use `LZ4F_*` APIs and exact expected output sizes."
    - "Raw LZ4 block payloads use `LZ4_*safe*` APIs and exact expected output sizes."
    - "Compression routing is explicit by method enum and never inferred from file extensions."
    - "D-04: LZ4 dependency service is a private adapter only."
    - "D-18: LZ4/router tests cover round-trip, corrupt/truncated input, cross-format rejection, unsupported routes, and exact-size mismatch rejection."
    - "D-20: LZ4 and routing proof uses unit vectors before generated binary fixtures."
  artifacts:
    - path: "src/detail/lz4_frame_codec.hpp"
      provides: "Skyrim SE/AE BSA LZ4 frame adapter"
    - path: "src/detail/lz4_block_codec.hpp"
      provides: "Starfield BA2 v3 raw LZ4 block adapter"
    - path: "src/detail/compression_router.hpp"
      provides: "Explicit compression method routing"
  key_links:
    - from: "src/detail/compression_router.cpp"
      to: "src/detail/deflate_codec.hpp, src/detail/lz4_frame_codec.hpp, src/detail/lz4_block_codec.hpp"
      via: "method switch dispatch"
      pattern: "compression_method::(deflate|lz4_frame|lz4_block)"
---

<objective>
Add separate LZ4 frame/raw-block adapters and explicit compression routing.

Purpose: Archive phases must not confuse incompatible LZ4 formats or guess compression behavior from filenames.
Output: Private LZ4 frame, raw block, and compression router services with tests.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-binary-i-o-paths-hashes-and-compression-services/02-SPEC.md
@.planning/phases/02-binary-i-o-paths-hashes-and-compression-services/02-CONTEXT.md
@.planning/phases/02-binary-i-o-paths-hashes-and-compression-services/02-RESEARCH.md
@src/detail/deflate_codec.hpp
@CMakeLists.txt
@vcpkg.json

<interfaces>
Create these internal functions:
```cpp
namespace libbsa::detail {
result<std::vector<std::byte>> compress_lz4_frame(std::span<const std::byte> input);
result<std::vector<std::byte>> decompress_lz4_frame_exact(std::span<const std::byte> compressed, std::size_t expected_size);
result<std::vector<std::byte>> compress_lz4_block(std::span<const std::byte> input);
result<std::vector<std::byte>> decompress_lz4_block_exact(std::span<const std::byte> compressed, std::size_t expected_size);

enum class compression_method { none, deflate, lz4_frame, lz4_block };
result<std::vector<std::byte>> compress_payload(compression_method method, std::span<const std::byte> input);
result<std::vector<std::byte>> decompress_payload_exact(compression_method method, std::span<const std::byte> input, std::size_t expected_size);
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED specify separate LZ4 frame and raw block codecs</name>
  <files>tests/unit/lz4_codec_tests.cpp, tests/CMakeLists.txt</files>
  <read_first>tests/CMakeLists.txt; .planning/phases/02-binary-i-o-paths-hashes-and-compression-services/02-SPEC.md; .planning/phases/02-binary-i-o-paths-hashes-and-compression-services/02-RESEARCH.md</read_first>
  <behavior>
    - `compress_lz4_frame` round-trips through `decompress_lz4_frame_exact` for a repeated literal vector.
    - `compress_lz4_block` round-trips through `decompress_lz4_block_exact` for the same vector.
    - Truncated frame and block payloads return `error_code::format_error`.
    - A raw block passed to `decompress_lz4_frame_exact` returns `error_code::format_error`.
    - A frame passed to `decompress_lz4_block_exact` returns `error_code::format_error`.
    - Valid frame/block payloads with mismatched expected sizes return `error_code::format_error`.
  </behavior>
  <action>Create `tests/unit/lz4_codec_tests.cpp` with tags `[unit][malformed][compression][lz4]`, register it, and make it fail before production code exists. The cross-format rejection tests implement D-18 and the separation required by BIN-05 and BIN-06.</action>
  <verify><automated>cmake --build --preset windows-msvc-debug-static --config Debug; ctest --preset windows-msvc-debug-static -R lz4_codec --output-on-failure</automated></verify>
  <acceptance_criteria>
    - `tests/unit/lz4_codec_tests.cpp` contains `decompress_lz4_frame_exact`, `decompress_lz4_block_exact`, `raw block`, and `frame`.
    - `tests/CMakeLists.txt` contains `unit/lz4_codec_tests.cpp`.
    - No file under `include/libbsa/` contains `lz4`.
  </acceptance_criteria>
  <done>Failing LZ4 tests lock frame/block separation, malformed rejection, and exact-size behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN implement private LZ4 frame and block adapters</name>
  <files>CMakeLists.txt, src/detail/lz4_frame_codec.hpp, src/detail/lz4_frame_codec.cpp, src/detail/lz4_block_codec.hpp, src/detail/lz4_block_codec.cpp, tests/unit/lz4_codec_tests.cpp</files>
  <read_first>CMakeLists.txt; vcpkg.json; tests/unit/lz4_codec_tests.cpp; src/detail/deflate_codec.hpp</read_first>
  <action>Add `find_package(lz4 CONFIG REQUIRED)` and privately link `lz4::lz4` to `libbsa`. Implement the frame adapter using `LZ4F_compressFrameBound`, `LZ4F_compressFrame`, `LZ4F_createDecompressionContext`, `LZ4F_decompress`, `LZ4F_isError`, and `LZ4F_freeDecompressionContext`. Implement the raw block adapter using `LZ4_compressBound`, `LZ4_compress_default`, and `LZ4_decompress_safe`. Each decompressor must allocate exactly `expected_size`, reject codec errors, and reject any produced size other than `expected_size`. Add Doxygen comments and keep all LZ4 includes in `.cpp` files.</action>
  <verify><automated>cmake --build --preset windows-msvc-debug-static --config Debug; ctest --preset windows-msvc-debug-static -R lz4_codec --output-on-failure</automated></verify>
  <acceptance_criteria>
    - `src/detail/lz4_frame_codec.cpp` contains `LZ4F_compressFrame` and `LZ4F_decompress`.
    - `src/detail/lz4_block_codec.cpp` contains `LZ4_compress_default` and `LZ4_decompress_safe`.
    - `src/detail/lz4_block_codec.cpp` does not contain `LZ4F_`.
    - `src/detail/lz4_frame_codec.cpp` does not contain `LZ4_decompress_safe`.
    - The LZ4 codec test filter exits 0.
  </acceptance_criteria>
  <done>LZ4 frame and raw block adapters pass round-trip, cross-format rejection, and exact-size tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: RED/GREEN implement explicit compression router</name>
  <files>CMakeLists.txt, tests/CMakeLists.txt, src/detail/compression_router.hpp, src/detail/compression_router.cpp, tests/unit/compression_router_tests.cpp</files>
  <read_first>src/detail/deflate_codec.hpp; src/detail/lz4_frame_codec.hpp; src/detail/lz4_block_codec.hpp; tests/CMakeLists.txt</read_first>
  <behavior>
    - `compress_payload` and `decompress_payload_exact` dispatch `compression_method::deflate`, `compression_method::lz4_frame`, and `compression_method::lz4_block` to the matching codec.
    - `compression_method::none` returns the input bytes unchanged for both compress and exact decompress when `input.size() == expected_size`.
    - Unsupported enum values created with `static_cast<compression_method>(255)` return `error_code::invalid_argument`.
    - Tests do not derive methods from filename extensions such as `.dds`, `.nif`, or `.ba2`.
  </behavior>
  <action>Create tests first in `tests/unit/compression_router_tests.cpp`, then implement `compression_router.hpp/.cpp` with the enum and dispatch functions specified in this plan. The router must switch only on `compression_method` and must not accept path or extension parameters. This implements D-18 and BIN-07 explicit target-format routing.</action>
  <verify><automated>cmake --build --preset windows-msvc-debug-static --config Debug; ctest --preset windows-msvc-debug-static -R compression_router --output-on-failure</automated></verify>
  <acceptance_criteria>
    - `src/detail/compression_router.hpp` contains `enum class compression_method`.
    - `src/detail/compression_router.cpp` contains `compression_method::deflate`, `compression_method::lz4_frame`, and `compression_method::lz4_block`.
    - `src/detail/compression_router.cpp` does not contain `.dds`, `.nif`, `.ba2`, or `extension`.
    - The compression router test filter exits 0.
  </acceptance_criteria>
  <done>Explicit compression routing covers deflate, LZ4 frame, raw LZ4 block, none, and unsupported methods.</done>
</task>

</tasks>

<source_decision_coverage>
- D-04: LZ4 is linked privately and exercised only through internal adapter/router functions.
- D-18: LZ4/router tests use small round-trip vectors plus corrupt/truncated, cross-format, unsupported-route, and exact-size mismatch rejection.
- D-20: LZ4 and routing proof uses source literal unit vectors instead of committed generated binary fixtures.
</source_decision_coverage>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| compressed bytes→LZ4 codecs | Malformed archive compression data enters native LZ4 code. |
| archive metadata→compression router | Format/version/method metadata selects codec behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-01 | T/D | LZ4 frame/block adapters | mitigate | Use the correct official safe API for each format and exact-size validation. |
| T-02-04-02 | T | `compression_router` | mitigate | Dispatch only on explicit enum values and reject unsupported values. |
| T-02-04-03 | I | LZ4 headers | mitigate | Keep `lz4.h` and `lz4frame.h` includes in private `.cpp` files only. |
</threat_model>

<verification>
Run `cmake --build --preset windows-msvc-debug-static --config Debug` and `ctest --preset windows-msvc-debug-static -R "(lz4_codec|compression_router)" --output-on-failure`.
</verification>

<success_criteria>
BIN-05, BIN-06, and BIN-07 are covered; LZ4 frame/raw-block confusion is tested; routing has no filename-extension inference.
</success_criteria>

<output>
After completion, create `.planning/phases/02-binary-i-o-paths-hashes-and-compression-services/02-04-SUMMARY.md`.
</output>
