# Plan: Apply `optimize-dx10-writer-snapshots`

## OpenSpec Status

- Change: `optimize-dx10-writer-snapshots`
- Schema: `spec-driven`
- State: `ready`
- Progress: `0/13` tasks complete
- Apply instruction: read context files, work through pending tasks, mark tasks complete as they are finished, and pause on blockers or unclear requirements.

## Context Files Read

- `openspec/changes/optimize-dx10-writer-snapshots/proposal.md`
- `openspec/changes/optimize-dx10-writer-snapshots/design.md`
- `openspec/changes/optimize-dx10-writer-snapshots/specs/dx10-snapshot-read-efficiency/spec.md`
- `openspec/changes/optimize-dx10-writer-snapshots/tasks.md`

## Implementation Steps

1. Add focused BA2 DX10 writer-stage tests in `tests/unit/writer_stage_tests.cpp`.
   - Add a multi-mip planned-chunk case that calls `texture::plan_dx10_chunks`, selects a chunk with `start_mip < end_mip`, runs `formats::ba2::ba2_dx10_prepare_chunk`, and asserts `raw_size`, `packed_size`, `start_mip`, and `end_mip` match the planned chunk.
   - Add a corrupted/truncated snapshot case by creating a stage entry, mutating one snapshot file after planning, calling `formats::ba2::ba2_dx10_prepare_chunk` or `write_ba2_dx10_archive`, and asserting a failed `result` with `format_error` or `io_error` semantics.
   - If the test exercises `write_ba2_dx10_archive`, use an existing sentinel output and assert it remains unchanged to prove no publication on failure.
   - Mark tasks `1.1` and `1.2` complete in `openspec/changes/optimize-dx10-writer-snapshots/tasks.md` only after the corresponding tests are implemented.

2. Strengthen snapshot ownership coverage in `tests/unit/ba2_dx10_writer_tests.cpp`.
   - Extend the existing `add_file snapshots DDS bytes before later source file changes` test so it writes the archive after overwriting/deleting the source DDS.
   - Reopen the archive with `archive_reader`, extract the DDS, analyze it with `texture::analyze_dds_source`, and compare extracted `image_payload_bytes` against the add-time source analysis.
   - Mark task `1.3` complete after this behavior is covered.

3. Refactor DX10 chunk assembly in `src/formats/ba2/ba2_dx10_prepare.cpp`.
   - Replace the ad hoc `append_subresource_bytes` path with an anonymous-namespace private helper near `append_snapshot_bytes`.
   - Helper shape: collect `const ba2_dx10_subresource_snapshot*` values for one `texture::planned_texture_chunk`, ordered by ascending mip from `planned.start_mip` through `planned.end_mip`, and report an aggregate byte count.
   - Return `format_error` if a planned subresource is missing or aggregate size cannot be represented safely.
   - In `ba2_dx10_prepare_chunk`, call the helper before reading snapshot files.
   - Validate `aggregate_size == planned.raw_size` before compression.
   - Reserve or pre-size `raw_bytes` with `detail::reserve_byte_vector` or `detail::make_byte_vector` using `planned.raw_size` and the existing description `BA2 DX10 raw texture chunk bytes`.
   - Copy snapshot bytes by iterating the collected pointers and keeping `append_snapshot_bytes`, which preserves `detail::for_each_disk_source_chunk` and `ba2_dx10_snapshot_source_context` diagnostics.
   - Retain the existing final `raw_bytes.size() == planned.raw_size` validation before compression.
   - Do not alter compression routing, chunk ordering, snapshot cleanup ownership, public APIs, or archive record layout.
   - Mark tasks `2.1` through `2.4` as each corresponding implementation item is complete.

4. Split BA2 DX10 benchmark timing in `benchmarks/libbsa_benchmarks.cpp`.
   - Change `run_ba2_dx10_pack_extract` to return two `benchmark_result` rows or append two rows through a small helper.
   - Time `writer.add_file(...)` separately as an add-time snapshot staging scenario, for example `ba2_dx10_add_snapshot_staging`.
   - Time `writer.write_to(...)` plus reopen/extract correctness validation as finalization, for example `ba2_dx10_write_finalize_extract`.
   - Keep `worker_count` set on both rows and keep `bytes_processed` meaningful for each row.
   - Preserve the reopened metadata checks and extracted DDS size/payload-shape validation in the finalization row.
   - Update `run_benchmarks()` to append both DX10 rows for each worker count.
   - Mark tasks `3.1` through `3.3` as each benchmark requirement is complete.

5. Validate and finish OpenSpec task tracking.
   - Build first, then run focused tests.
   - Run the benchmark report target or executable and inspect the generated JSON/Markdown for both DX10 timing rows.
   - Re-run OpenSpec status.
   - Mark tasks `4.1` through `4.3` only after their commands pass.

## Files To Modify

- `src/formats/ba2/ba2_dx10_prepare.cpp`
- `tests/unit/writer_stage_tests.cpp`
- `tests/unit/ba2_dx10_writer_tests.cpp`
- `benchmarks/libbsa_benchmarks.cpp`
- `openspec/changes/optimize-dx10-writer-snapshots/tasks.md`

## Files To Inspect If Needed

- `src/detail/byte_vector.hpp`
- `src/detail/writer_disk_source.hpp`
- `src/formats/ba2/ba2_dx10_writer.cpp`
- `src/texture/dds_layout.hpp`
- `src/texture/dds_layout.cpp`
- `tests/unit/ba2_writer_execution_tests.cpp`

## Verification

1. Configure/build debug-static if needed:
   - `cmake --preset windows-msvc-debug-static`
   - `cmake --build --preset windows-msvc-debug-static`
2. Run focused tests:
   - `ctest --preset windows-msvc-debug-static -R "ba2_dx10_writer|writer-stage|ba2_writer_execution" --output-on-failure`
3. Run benchmark report:
   - `cmake --build --preset windows-msvc-debug-static --target libbsa_benchmark_report`
   - Confirm benchmark output includes `ba2_dx10_add_snapshot_staging` and `ba2_dx10_write_finalize_extract` rows for each worker count.
4. Re-check OpenSpec:
   - `openspec status --change optimize-dx10-writer-snapshots --json`

## Pause Conditions

- Pause if a multi-mip planned chunk cannot be produced without changing production chunk planning.
- Pause if benchmark consumers or existing tests require the old single `ba2_dx10_pack_extract` scenario name.
- Pause if snapshot corruption fails after output publication instead of during preparation.
- Pause if implementation requires changes to public APIs, dependencies, or files under `TES5Edit/`.
