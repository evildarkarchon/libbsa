# Plan: fix-ba2-gnrl-name-table-end-overflow

## Current State

- Change: `fix-ba2-gnrl-name-table-end-overflow`
- Schema: `spec-driven`
- OpenSpec state: `ready`
- Progress: `0/10` tasks complete
- Dynamic instruction: read context files, work through pending tasks, mark complete as each task finishes, and pause on blockers or unclear requirements.

## Context Read

- `openspec/changes/fix-ba2-gnrl-name-table-end-overflow/proposal.md`
- `openspec/changes/fix-ba2-gnrl-name-table-end-overflow/design.md`
- `openspec/changes/fix-ba2-gnrl-name-table-end-overflow/tasks.md`
- `openspec/changes/fix-ba2-gnrl-name-table-end-overflow/specs/archive-record-metadata-validation/spec.md`
- `src/formats/ba2/ba2_gnrl_parser.cpp`
- `tests/unit/ba2_gnrl_reader_tests.cpp`
- `tests/unit/parser_primitives_tests.cpp`
- `CMakePresets.json`

## Implementation Steps

1. Update `src/formats/ba2/ba2_gnrl_parser.cpp`.
   Add `using detail::add_fits_u64;` beside the existing parser primitive imports.

2. Replace the unchecked host-file parser addition after `read_names_from_file`.
   Use a local `std::uint64_t name_table_end = 0;` and call `add_fits_u64(header.value().file_table_offset, static_cast<std::uint64_t>(name_table_consumed), name_table_end)`. On failure, return `error{error_code::format_error, "BA2 GNRL filename table is too large"}` before calling `materialize_entries`.

3. Leave the in-memory path unchanged.
   Confirm it still computes `name_table_end` with `detail::add_fits` over `std::size_t` and still passes the validated range into `materialize_entries`.

4. Add regression coverage in `tests/unit/ba2_gnrl_reader_tests.cpp`.
   Add a malformed host-file test near the existing BA2 GNRL bounded/end-table/malformed tests. The test should build a high-`FileTableOffset` synthetic BA2 GNRL fixture and assert `archive_reader::open` fails with `libbsa::error_code::format_error` when the platform can represent the required sparse range. If the filesystem or standard stream limits prevent creating or seeking to the required range, `SKIP` with a short comment explaining the platform limitation instead of broadening production code for testability.

5. Add or tighten checked-add coverage in `tests/unit/parser_primitives_tests.cpp`.
   Keep the existing `add_fits_u64(max, 1)` assertion and add BA2-shaped boundary assertions such as `max - 1 + 1` succeeds and `max - 1 + 2` fails. This documents the arithmetic contract when a direct sparse fixture cannot reach the production branch on normal Windows stream limits.

6. Preserve existing successful end-table behavior.
   Do not change `ba2_gnrl_end_table opens archives with payloads before the filename table`; rely on it as the success case proving payload-before-filename-table archives still open and extract correctly.

7. Update `openspec/changes/fix-ba2-gnrl-name-table-end-overflow/tasks.md` as tasks complete.
   Mark parser tasks `1.1` through `1.4` immediately after the parser change is done. Mark regression tasks `2.1` through `2.3` after tests are added/confirmed. Mark verification tasks `3.1` through `3.3` only after the commands below pass.

## Verification

1. Build tests:
   `cmake --build --preset windows-msvc-debug-static`

2. Run focused tests:
   `ctest --preset windows-msvc-debug-static -R "ba2_gnrl|parser_primitives" --output-on-failure`

3. Validate OpenSpec change:
   `openspec validate fix-ba2-gnrl-name-table-end-overflow --strict`

4. Confirm final OpenSpec progress/status:
   `openspec status --change "fix-ba2-gnrl-name-table-end-overflow" --json`
   `openspec instructions apply --change "fix-ba2-gnrl-name-table-end-overflow" --json`

## Pause Conditions

- Pause if implementing the sparse high-offset fixture requires changing public APIs, exposing anonymous-namespace parser internals, or adding test-only hooks.
- Pause if the OpenSpec validation command reports artifact issues unrelated to the implementation.
- Pause if focused tests fail in unrelated code or reveal a behavior conflict with the existing BA2 GNRL end-table success case.
