# Plan: Apply `add-explicit-public-export-macro`

## Current State

- Change: `add-explicit-public-export-macro`
- Schema: `spec-driven`
- OpenSpec apply state: `all_done`
- Progress: `16/16` tasks complete, `0` remaining
- Dynamic instruction: all tasks are complete and the change is ready to archive after tests/review.

## Context Read

- `openspec/changes/add-explicit-public-export-macro/proposal.md`
- `openspec/changes/add-explicit-public-export-macro/design.md`
- `openspec/changes/add-explicit-public-export-macro/specs/shared-library-export-surface/spec.md`
- `openspec/changes/add-explicit-public-export-macro/tasks.md`
- `include/libbsa/export.hpp`
- `include/libbsa/archive.hpp`
- `include/libbsa/writer.hpp`
- `include/libbsa/validation.hpp`
- `CMakeLists.txt`
- `CMakePresets.json`
- `tests/unit/export_surface_policy_tests.cpp`
- `tests/export-surface/check-dll-exports.cmake`
- `tests/package-consumer/main.cpp`
- `tests/package-consumer/smoke.cmake`
- `tests/CMakeLists.txt`

## Recommended Build-Mode Steps

1. Re-run OpenSpec status before doing anything else:
   - `openspec status --change "add-explicit-public-export-macro" --json`
   - `openspec instructions apply --change "add-explicit-public-export-macro" --json`
2. If the state is still `all_done`, make no code or task-file edits.
3. Report the change as already complete and list the completed tasks from the OpenSpec instructions output.
4. Run verification to confirm the current implementation still passes:
   - `cmake --preset windows-msvc-debug-static`
   - `cmake --build --preset windows-msvc-debug-static`
   - `ctest --preset windows-msvc-debug-static`
   - `cmake --preset windows-msvc-debug-shared`
   - `cmake --build --preset windows-msvc-debug-shared`
   - `ctest --preset windows-msvc-debug-shared`
   - `git diff --check`
5. If verification passes, tell the user all tasks are complete and suggest `/opsx-archive add-explicit-public-export-macro`.

## Contingency If Status Changes

Only implement if a fresh `openspec instructions apply` reports pending tasks. Keep edits scoped to the relevant files:

- `include/libbsa/export.hpp`
- `include/libbsa/archive.hpp`
- `include/libbsa/writer.hpp`
- `include/libbsa/validation.hpp`
- `CMakeLists.txt`
- `tests/unit/export_surface_policy_tests.cpp`
- `tests/export-surface/check-dll-exports.cmake`
- `tests/package-consumer/main.cpp`
- `tests/CMakeLists.txt`

After each completed pending task, immediately update its checkbox in `openspec/changes/add-explicit-public-export-macro/tasks.md`.

## Verification Focus

- `LIBBSA_API` remains installed and public from `include/libbsa/export.hpp`.
- Shared builds define `LIBBSA_BUILDING_LIBRARY` privately for the library target.
- Static builds propagate `LIBBSA_STATIC_DEFINE` through `libbsa::libbsa`.
- `WINDOWS_EXPORT_ALL_SYMBOLS` remains disabled.
- Public emitted reader, writer, sink/factory, and validation APIs remain annotated.
- Internal `detail`, `formats`, codec, texture analyzer, and writer-stage helper symbols remain absent from shared DLL exports.
- Static and shared package-consumer smoke tests still link through `libbsa::libbsa`.

## Boundaries

- Do not edit `TES5Edit/`.
- Do not introduce dependencies.
- Do not add ABI-stability promises beyond the explicit exported surface.
- Do not change archive parsing, writing, extraction, validation, or compression behavior for this change.
