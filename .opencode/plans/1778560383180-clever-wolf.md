# Plan: Apply OpenSpec Change

Change: `add-explicit-public-export-macro`
Schema: `spec-driven`

## Current State

- The only active OpenSpec change is `add-explicit-public-export-macro`.
- `openspec instructions apply` reports `state: all_done`.
- Progress is `16/16` tasks complete.
- Context files read:
  - `openspec/changes/add-explicit-public-export-macro/proposal.md`
  - `openspec/changes/add-explicit-public-export-macro/design.md`
  - `openspec/changes/add-explicit-public-export-macro/specs/shared-library-export-surface/spec.md`
  - `openspec/changes/add-explicit-public-export-macro/tasks.md`

## Recommended Action

No implementation edits are required. The apply workflow should report that all tasks are complete and recommend archiving the change.

## Files To Modify

None.

## Verification

Before archiving, optionally re-run the read-only OpenSpec checks:

```powershell
openspec status --change "add-explicit-public-export-macro" --json
openspec instructions apply --change "add-explicit-public-export-macro" --json
```

If extra confidence is needed before archive, run the project verification commands already captured in task group 4, but no new implementation work is planned.
