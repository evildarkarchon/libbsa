# Public API Reality Check

This audit checks libbsa's current public C++20 surface against the coverage matrix, compatibility evidence, consumer documentation, package-consumer smoke source, and public-boundary policy tests. It is intentionally a proof-and-routing document, not a facade design proposal.

## Scope and evidence policy

Default proof means evidence that is reproducible from the repository without local game archives or BSArchPro-generated corpus output:

- Public headers: `include/libbsa/libbsa.hpp`, `archive.hpp`, `writer.hpp`, `validation.hpp`, and `result.hpp`.
- Public support docs: `docs/coverage-audit-matrix.md`, `docs/compatibility-evidence.md`, `docs/api-mainpage.md`, `docs/integration-examples.md`, `docs/target-format-guide.md`, and `docs/thread-safety.md`.
- Package-consumer proof: `tests/package-consumer/main.cpp` and the package-consumer CTest gates described by `tests/unit/target_format_policy_tests.cpp`.
- Public-boundary and documentation policy proof: `tests/unit/public_include_boundary_tests.cpp`, `tests/unit/export_surface_policy_tests.cpp`, `tests/unit/docs_policy_tests.cpp`, `tests/unit/target_format_policy_tests.cpp`, and `tests/unit/validation_api_tests.cpp`.

Optional local game corpora and BSArchPro-derived manifests remain advisory evidence only. They can increase confidence or justify future hardening, but they are not required for default support claims and they do not replace committed legal fixtures, writer-output tests, package-consumer checks, or docs-policy coverage. `TES5Edit/` remains a read-only reference boundary.

This audit applies the current public-API direction: keep `archive_reader`, validation, bulk extraction, `result<T>`, stable `error_code` categories, and family-specific writers as the public core. Do not introduce a broad facade or new public dependency surface unless a real consumer flow cannot be made understandable or safe through docs, examples, policy tests, or a narrowly evidenced helper.

## Flow-by-flow proof table

| Consumer flow | Real public API involved | Default proof found | Current guidance status | Conclusion / follow-up |
|---|---|---|---|---|
| Umbrella include and installed target usage | `<libbsa/libbsa.hpp>`, `libbsa::libbsa`, `LIBBSA_API` | `libbsa.hpp` includes the reader, result, validation, version, and writer headers. `tests/package-consumer/main.cpp` includes only `<libbsa/libbsa.hpp>` for representative usage. `export_surface_policy_tests.cpp` checks explicit export annotations and install/export boundaries. `package_consumer_smoke` installs libbsa, configures an external consumer against the installed `libbsa::libbsa` target, and runs writer-produced archive open/validate/extract checks for TES3 BSA, TES4-family BSA, BA2 GNRL, and BA2 DX10. | Clear for compile/link, installed target usage, missing-path runtime smoke, and every-family installed-package archive runtime proof. The matrix still separates default legal package-consumer proof from optional local corpus evidence. | No facade needed. `COV-GAP-003` is closed by installed package-consumer runtime proof for every current family without changing the public API shape. |
| Open and list metadata | `archive_reader::open`, `archive_reader::metadata`, `archive_reader::entries`, `archive_metadata`, `entry_metadata`, `archive_type`, `archive_variant`, `entry_compression` | `archive.hpp` documents the reader shape and dependency-light metadata. `coverage-audit-matrix.md` marks reader/open/list metadata as Proven for TES3, TES4-family, BA2 GNRL, and BA2 DX10. `api-mainpage.md` and `integration-examples.md` start consumers with open/list/extract. | Clear. Metadata names are public and stable enough for package consumers without exposing private parser, codec, DirectXTex, or TES5Edit types. | No API change. Keep docs examples centered on `archive_reader` rather than a new top-level facade. |
| `find` versus `contains` | `archive_reader::find`, `archive_reader::contains`, `result<std::optional<entry_metadata>>`, `result<bool>` | `archive.hpp` documents the semantic split: invalid path syntax is `invalid_argument`; missing valid paths are `std::nullopt` for `find`, while `contains` returns a bool. `tests/package-consumer/main.cpp` demonstrates `find` in the richer open/list/extract example. | Mostly clear in headers, but consumer docs currently emphasize `find` and do not give a sharp comparison for when `contains` is preferable. | Docs follow-up: add a small guidance note that `find` is for metadata-or-absence and `contains` is for existence-only checks. No helper needed because `contains` already exists. |
| Single-entry streaming extraction | `archive_reader::extract`, `payload_sink`, `payload_sink::write` | `archive.hpp` documents caller-owned sinks and partial-write failure behavior. `integration-examples.md` shows `payload_sink` extraction. The matrix marks extraction Proven for all supported families through unit fixture tests. | Clear. The public surface correctly keeps host-output policy outside archive virtual paths. | No API change. Keep examples explicit that consumers own sink behavior and host-path mapping. |
| Single-entry in-memory extraction | `archive_reader::extract_bytes`, `result<std::vector<std::byte>>` | `archive.hpp` exposes `extract_bytes`; the coverage matrix cites `extract_bytes` in TES3, TES4-family, and BA2 DX10 extraction evidence. | API is present and proven, but public docs do not surface when to choose `extract_bytes` versus `extract` through a sink. | Docs follow-up: document `extract_bytes` as the bounded convenience path for small/known payloads and `extract` as the streaming default for host output or large entries. No new helper. |
| Bulk extraction | `archive_reader::extract_entries`, `bulk_extract_request`, `bulk_extract_options`, `bulk_extract_sink_factory`, `bulk_extract_entry_result`, `payload_sink` | `archive.hpp` documents outer setup failures, per-entry failures, duplicate request coalescing, and positive `worker_count`. `thread-safety.md` covers factory/sink concurrency. `api-mainpage.md`, `integration-examples.md`, and package-consumer source compile representative usage. | Clear but nuanced. The most important sharp edges are worker count, per-entry failure inspection, duplicate coalescing, and distinct sinks. | No facade needed. Keep examples and policy tests explicit enough that consumers do not infer host-path joining, auto worker counts, or shared-sink safety. |
| Validation reports | `validate_archive`, `validation_options`, `validation_report`, `validation_diagnostic`, `validation_report::is_valid` | `validation.hpp` documents setup failures versus inspectable archive diagnostics. `validation_api_tests.cpp` proves result-level setup errors, report-level malformed diagnostics, extractability caps, generated-fixture success rows, writer-produced validation, Starfield BA2 v3 method 0 deflate and method 3 raw LZ4 block validation routes, and valid-but-risky expected-variant mismatch warnings. | Clear at the API level. Direct per-variant success proof now exists for TES4 v104/v105, BA2 GNRL Starfield v2/v3, and BA2 DX10 Starfield v3 routes; no helper/API redesign was needed. | `COV-GAP-001` is closed by direct validation proof. Keep the report/result boundary and stable warning/error codes as-is; reopen API design only if future executable proof exposes a real semantic mismatch. |
| Compatibility warnings | `compatibility_warning_code`, `compatibility_warning_severity`, `compatibility_warning`, `validation_report::warnings` | `validation.hpp` defines stable warning codes and severities. `compatibility-evidence.md` maps each current public warning code to rules and generated/writer-output evidence. `compatibility_warning_tests.cpp` proves current warning codes. | Clear for the current warning catalog. The catalog intentionally has no TES3- or BA2-DX10-specific warning. | Route `COV-GAP-004` as deferred taxonomy work: add new warning codes only when a concrete target interoperability risk is identified and can be proven by default evidence. Do not add speculative warnings. |
| Stable error-code branching | `result<T>`, `result<void>`, `error`, `error_code` | `result.hpp` exposes a C++20 expected-like type with stable `error_code` and diagnostic-only messages. `tests/package-consumer/main.cpp` has `example_handle_result_errors` branching on `io_error`, `format_error`, and `unsupported`, and runtime smoke expects missing archives to report `io_error`. | Clear. Consumers can branch on `error().code`; exact message comparisons remain discouraged. | No API change. Keep the message-is-not-stable rule visible near validation and open examples. |
| TES3 writer | `tes3_bsa_writer`, `tes3_bsa_writer_options`, `write_execution_options` | `writer.hpp` documents raw/uncompressed TES3 behavior. `public_include_boundary_tests.cpp` checks the public contract. `tests/package-consumer/main.cpp` links `tes3_bsa_writer::add_bytes` and options usage. Matrix marks TES3 writer and round-trip Proven. | Clear. TES3 deliberately has no compression, dedupe, or embedded-name controls. | No helper. Keep docs concise so consumers do not look for non-existent TES3 compression knobs. |
| TES4-family writer | `tes4_bsa_writer`, `tes4_bsa_target`, `tes4_bsa_writer_options`, `archive_compression_policy`, `entry_compression_policy`, `write_execution_options` | `writer.hpp` exposes target profiles and policy-based compression instead of raw flags. `target-format-guide.md` documents v103/v104/v105 and LZ4-frame routing. Public-boundary and package-consumer tests compile representative usage. Matrix marks writer and round-trip Proven. | Clear. Sharp edges are target selection, embedded-name compatibility, texture target restrictions, overwrite/publication policy, and worker-count controls. | No facade. Preserve target-format guidance and link writer options to compatibility behavior rather than raw archive flags. |
| BA2 GNRL writer | `ba2_gnrl_writer`, `ba2_gnrl_target`, `ba2_gnrl_writer_options`, `ba2_gnrl_entry_options`, `archive_compression_policy`, `entry_compression_policy` | `writer.hpp` documents Fallout 4, Starfield v2, and Starfield v3 targets plus Starfield header fields and compression method. `target-format-guide.md` explains deflate and raw LZ4 block routes. Public-boundary/package-consumer tests cover representative construction and entry addition. Matrix marks writer and round-trip Proven. | Clear. Starfield compression method semantics are specialized but documented through target/options rather than public codec types. | No helper. Keep method `3` raw-LZ4 and method `0` deflate guidance visible in docs/examples. |
| BA2 DX10 writer lifecycle | `ba2_dx10_writer`, `ba2_dx10_target`, `ba2_dx10_writer_options`, `ba2_dx10_writer::add_file`, `ba2_dx10_writer::write_to` | `writer.hpp`, `target-format-guide.md`, and `integration-examples.md` document DDS host-file input, snapshot-at-add, compressed-only archive-level contract, consuming `write_to`, best-effort cleanup, and residual abnormal-termination risk. Public-boundary tests forbid raw override tokens in the DX10 public contract. Matrix marks BA2 DX10 writer and round-trip Proven, and `package_consumer_smoke` now creates, opens, validates, and extracts a writer-produced BA2 DX10 archive from an inline legal DDS input through the installed package-consumer path. | Clear but high-sharp-edge. Consumer docs already explain one-shot lifecycle; policy tests should keep it from regressing. | No API helper is unavoidable. Keep package/policy proof around lifecycle wording and public-boundary absence of raw/per-chunk override surface. |

## Sharp-edge guidance coverage

| Sharp edge | Current coverage | Gap assessment |
|---|---|---|
| Archive virtual paths are not host filesystem paths. | `integration-examples.md` keeps archive virtual paths and host paths as separate parameters; public metadata uses string paths rather than host filesystem types. | Adequate. Keep examples from joining archive names directly onto output roots without caller policy. |
| Missing path versus invalid path. | `archive_reader::find` documents invalid syntax as `invalid_argument` and valid absence as empty optional; `contains` exists for bool-only checks. | Needs a short docs clarification comparing `find` and `contains`. |
| Streaming versus in-memory extraction. | `payload_sink` and `extract` are documented and example-backed; `extract_bytes` is present and fixture-proven. | Needs a docs note on `extract_bytes` as convenience, not the default for large or host-output flows. |
| Bulk extraction error shape. | `extract_entries` documents outer setup failures versus per-entry `bulk_extract_entry_result::failure`; thread-safety docs cover factory and sink concurrency. | Adequate, with policy-test reinforcement for the documented per-entry failure contract. |
| Worker count semantics. | `bulk_extract_options::worker_count` and `write_execution_options::worker_count` both default to serial and reject zero where applicable; docs mention positive worker counts. | Adequate. Keep serial default explicit. |
| Validation setup failures versus archive diagnostics. | `validation.hpp` and `integration-examples.md` explain result-level setup failures and report-level `errors`/`warnings`. | Adequate; direct per-variant success proof now exists in `tests/unit/validation_api_tests.cpp`, confirming the public type shape without helper/API redesign. |
| Stable machine branching. | `error_code` and `compatibility_warning_code` are stable; messages are human diagnostics. Package-consumer source branches on `error_code`. | Adequate. Docs can repeat that exact messages are not stable. |
| Writer output publication and overwrite behavior. | `target-format-guide.md` documents same-directory temporary output, no-overwrite, reparse-point refusal, network filesystem caveats, and `io_error` publication failures. | Adequate. |
| BA2 DX10 snapshot and consuming write lifecycle. | `writer.hpp`, `target-format-guide.md`, and `integration-examples.md` document snapshot cleanup, one-shot `write_to`, and abnormal-termination residual risk. | Adequate but important enough to keep under docs-policy coverage. |
| Optional local compatibility evidence. | `coverage-audit-matrix.md` and `compatibility-evidence.md` separate default proof from `LIBBSA_GAME_FIXTURES` / `LIBBSA_BSARCHPRO_EXPECTED` advisory checks. | Adequate. Do not strengthen public claims based only on optional local corpora. |

## Helper and API gap conclusions

No broad public facade is justified by this audit. The existing public core is coherent:

- `archive_reader` covers open/list/lookup/extraction.
- `payload_sink` and `extract_bytes` cover streaming and convenience extraction.
- `extract_entries` covers bulk extraction without hiding per-entry failure details.
- `validate_archive` and `validation_report` cover validation and compatibility warnings.
- `result<T>`, `error`, and `error_code` provide stable C++20 error branching without exposing C++23 `std::expected`.
- `tes3_bsa_writer`, `tes4_bsa_writer`, `ba2_gnrl_writer`, and `ba2_dx10_writer` expose target-specific write-new flows without leaking private codec, DirectXTex, TES5Edit, or raw parser internals.

No tiny public helper is needed for validation or package-consumer proof. The most concrete remaining friction is documentation clarity around existing public types, not missing public capability:

1. `find` versus `contains` needs consumer-facing wording.
2. `extract_bytes` needs consumer-facing guidance so callers understand when it is appropriate.
3. BA2 DX10 lifecycle wording should remain prominent and policy-tested because it is intentionally different from the other writers.

Potential future helpers should be evidence-gated. Examples that are not justified today include a catch-all archive facade, public codec selection objects, public DirectXTex/DXGI types, raw BSA/BA2 flag builders, or broad host-filesystem extraction helpers. Each would add public surface area while failing to address the remaining deferred compatibility-evidence gaps.

## Downstream routing for coverage gap IDs

| Gap ID | Audit finding | Recommended route |
|---|---|---|
| `COV-GAP-001` | Direct validation success rows now exist for TES4 v104/v105, BA2 GNRL Starfield v2/v3, BA2 DX10 Starfield v3, and writer-produced Starfield BA2 v3 method 0 deflate and method 3 raw LZ4 block routes. The validation API shape required no helper/API redesign. | Closed. Keep validation API and docs-policy tests as the guardrail; reopen only if future variants expose a real semantic mismatch. |
| `COV-GAP-003` | Installed-package runtime proof now exists: `package_consumer_smoke` installs libbsa, builds an external consumer against the installed `libbsa::libbsa` target, and uses public writer/reader/validation/extraction APIs to create, open, validate, and extract writer-produced TES3 BSA, TES4-family BSA, BA2 GNRL, and BA2 DX10 archives. | Closed. Keep the package-consumer CTest, docs-policy checks, and target-format policy checks as the guardrail; do not infer exhaustive real-game or BSArchPro corpus compatibility from this default legal smoke. |
| `COV-GAP-004` | Current compatibility warning codes are proven; TES3 and BA2 DX10 have no claimed family-specific warning taxonomy. | Defer until a concrete TES3 or BA2 DX10 interoperability risk is identified. Add warning codes only with generated/writer-output default evidence and docs in `compatibility-evidence.md`. |

## Concrete follow-up recommendation

For ongoing API stewardship, prefer docs/examples/policy proof over API expansion:

1. Update consumer docs to explicitly compare `find` and `contains`, explain `extract_bytes`, repeat stable `error_code`/diagnostic-message rules, and keep BA2 DX10 writer lifecycle guidance visible.
2. Strengthen package-consumer or policy tests so the public story is enforced by compile/link smoke and docs-boundary checks rather than this prose alone.
3. Keep executable per-variant validation proof in `tests/unit/validation_api_tests.cpp`, installed-package runtime proof in `package_consumer_smoke`, and docs-policy checks focused on the existing public surface.
4. Do not add a facade, public codec objects, public DirectXTex/DXGI types, or speculative compatibility warnings without new evidence that docs/tests cannot close the consumer flow safely.
