# GSD context snapshot (2026-05-20T05:57:54.962Z)

## Top project memories
- [MEM007] (gotcha) libbsa already has broad synthetic-fixture proof across TES3, TES4-family BSA, BA2 GNRL, and BA2 DX10; the missing deliverable for S01 is a consolidated human-first coverage matrix that cross-links those scattered proofs instead of discovering new supported families. The existing `tests/fixtures/generated/compatibility_matrix.json` is a narrow malformed-hardening matrix, not the full support matrix.
- [MEM012] (pattern) For libbsa support-claim audits, `Proven` should mean default reproducible evidence only: committed/generated legal fixtures, always-on Catch2/CTest, package-consumer checks, or docs-policy tests. Optional `LIBBSA_GAME_FIXTURES` and `LIBBSA_BSARCHPRO_EXPECTED` checks are advisory and must not turn a cell green by themselves.
- [MEM016] (pattern) For libbsa public API proof, package-consumer smoke should compile documented examples through only <libbsa/libbsa.hpp> and keep runtime checks cheap/missing-path based unless a later slice adds installed-package fixture runtime proof. COV-GAP-003 remains routed to S05 until every-family installed-package runtime archive creation/opening is proven by default.
- [MEM013] (gotcha) `tests/fixtures/generated/compatibility_matrix.json` is a malformed-hardening submatrix for libbsa, not the full support/audit matrix. Reader, extraction, writer, round-trip, package-consumer, docs-policy, and optional local-corpus evidence must be tracked separately in `docs/coverage-audit-matrix.md`.
- [MEM002] (architecture) How M001 should make current library coverage truthful Chose: Create a durable coverage/gap matrix and convert important fixed gaps into tests, policy checks, fixture assertions, package-consumer examples, or docs policy checks.. Rationale: The matrix gives planning visibility over archive family by capability, while durable tests and checks keep fixed gaps from immediately going stale..
- [MEM003] (architecture) What proof hierarchy should guide sta
…[truncated]
