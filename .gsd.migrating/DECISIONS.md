# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? | Made By |
|---|------|-------|----------|--------|-----------|------------|---------|
| D001 | M001-k9wo8b planning | api | How M001 should approach the public API surface | Keep archive_reader, validate_archive, bulk extraction, and family-specific writers as the stable public core; add only audit-proven small helpers or docs improvements in M001. | Existing headers already expose substantial reader, validation, bulk extraction, metadata, and writer capabilities. A broad facade before audit evidence would risk unnecessary redesign, while doing nothing would not address the user's concern that the API feels basic. | Yes | collaborative |
| D002 | M001-k9wo8b planning | quality | How M001 should make current library coverage truthful | Create a durable coverage/gap matrix and convert important fixed gaps into tests, policy checks, fixture assertions, package-consumer examples, or docs policy checks. | The matrix gives planning visibility over archive family by capability, while durable tests and checks keep fixed gaps from immediately going stale. | Yes | collaborative |
| D003 | M001-k9wo8b planning | verification | What proof hierarchy should guide stabilization decisions | Use generated legal fixtures as default CI proof, package-consumer tests for public API usability, and optional local game or BSArchPro-derived checks for higher-confidence compatibility evidence. | This balances repeatable legal default verification with a path to real compatibility confidence when local game archives or BSArchPro-derived manifests are available. | Yes | collaborative |
| D004 | M001-k9wo8b planning | error-handling | How M001 should handle public error and failure behavior | Keep libbsa::result<T> and stable public error_code categories; audit and stabilize inconsistent diagnostics without redesigning the error model in M001. | The existing public model is appropriate for expected archive, I/O, compression, validation, and caller-input failures. M001 needs consistency and proof, not a new failure abstraction. | Yes | collaborative |
| D005 | M001-k9wo8b planning | milestone-scope | How much remediation M001 should commit to after the audit | Fix a coherent risk-bounded tranche of highest-risk audit gaps, then explicitly defer remaining gaps with rationale and likely future ownership. | Requiring every discovered gap to close would make the milestone unbounded, while a mostly-documentation audit would not deliver enough stabilization progress. | Yes | collaborative |
| D006 | M001-k9wo8b/S01 planning | documentation | Where to store the S01 archive-family coverage and gap matrix | Create and maintain the human-first coverage matrix at docs/coverage-audit-matrix.md, with a discoverability link from existing compatibility/support documentation. | The matrix is a repo-visible support-truth artifact for future contributors and downstream slices, not a private planning artifact. Keeping it under docs makes it reviewable, testable by always-on policy tests, and separate from optional local game or BSArchPro evidence paths. | Yes | agent |
| D007 | M001-k9wo8b/S01 | compatibility-evidence | Coverage audit default proof must not depend on optional local game archives or BSArchPro-derived local comparisons. | Treat local game-corpus and BSArchPro comparison checks as advisory evidence only; default `Proven` status requires committed/generated legal fixtures, always-on tests, package-consumer checks, or docs-policy proof. | This keeps support claims reproducible in default CI without copyrighted inputs while still allowing local compatibility campaigns to add confidence and discover future work. | Yes, if a future compatibility campaign can legally commit comparable default fixtures or manifests without requiring local copyrighted archive bytes. | agent |
| D008 | M001-k9wo8b/S01 | test-policy | The coverage audit matrix contract is enforced by docs-policy tests rather than by optional fixture corpora or a machine-schema-first matrix. | Use lightweight Catch2 docs-policy tests over the public matrix and compatibility evidence docs, with `coverage_audit_matrix` names/tags for focused CTest discovery. | S01 is audit-only and human-first; token/policy tests cheaply prevent required families, axes, advisory-evidence separation, malformed-submatrix boundaries, and public-doc links from regressing without adding new runtime format behavior. | Yes, if future support-truth automation needs a machine-checkable schema in addition to the human matrix. | agent |
| D009 | M001-k9wo8b S02 planning | public-api | S02 public API reality check execution model | Audit and harden the existing archive_reader, validation, bulk extraction, and family-specific writer core through docs, package-consumer smoke coverage, and policy tests rather than introducing a broad facade or new public dependency surface. | S02 research and path scans show the public headers already expose a coherent dependency-light C++20 core; the remaining risks are consumer-proof granularity, sharp-edge documentation, and package-consumer evidence, so a facade redesign would add surface area without closing the S01 matrix gaps. | Yes, if T01 uncovers a concrete core consumer flow that cannot be made understandable or safe with docs, examples, or a tiny helper. | agent |
| D010 | M001-k9wo8b S02 | public API | S02 public API reality check did not add a broad facade or speculative helper API. | Keep archive_reader, validate_archive, extract/extract_bytes, bulk extraction, result<T>, and family-specific writers as the documented public core; close S02 consumer friction with docs, examples, package-consumer smoke, and policy tests instead of new public headers. | The audit found that existing public surfaces already cover the core consumer journey, while remaining gaps are proof/documentation or downstream validation/runtime-evidence issues. Adding a facade would expand the C++20 dependency-light public boundary without evidence that a tiny helper is unavoidable. | Yes, if S05 or later consumer/runtime proof shows a concrete repeated misuse pattern that a small helper can fix without leaking private dependencies. | agent |
